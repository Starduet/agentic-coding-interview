# ch07 · 验证、评估与基准

> 覆盖:评测任务怎么构造(fail-to-pass / pass-to-pass 的设计逻辑)、pass@k 的坑、验证器上限(verification ceiling)、自我验证为什么不可靠、LLM-as-judge、evals-as-CI、浏览器/多模态作为 verifier、防 benchmark 作弊;以及 **agent 的测试与证据纪律**(测试锚定需求、断言可观察行为、改测试前先分类失败、证据绑定工件状态、验收-证据映射、减法 review)。
> 出题立场:基准和榜单只做背景板,不出"某基准哪年发布、分数多少"的记忆题;考察一律落在机制与设计取舍。
> 案例基线:2026-08(带日期案例以标注为准;方法论不随时间变化)。
> 答案读法:**加粗句连读就是可背的骨架**;后面的解释帮你理解;「补充弹药」是追问时再掏的细节。

---

## 7.1 八股题(基础概念)

**Q7.1【八股·L2·模型】SWE-bench 这类代码评测的任务是怎么构造的?fail-to-pass 和 pass-to-pass 各自为什么必须存在?**

- **人话版**:任务从真实仓库的 issue+修复 PR 构造——仓库回退到修复前,agent 打补丁,再跑测试。判定用两组测试:补丁前挂、补丁后必须绿(fail-to-pass,证明真修了);补丁前就绿、补丁后也必须绿(pass-to-pass,证明没弄坏别的)。
- **主线(照这条线讲)**:
  1. **为什么 fail-to-pass 不够**——只要求"修好目标测试"的话,最省力的修法是把目标功能整个删掉或改测试——反正目标测试绿了。**pass-to-pass 封掉这条歪路**:回归测试挂了同样算失败,agent 必须在不破坏现有行为的前提下修复——两组测试各自封一个作弊方向,这是判定机制的精髓。
  2. **为什么必须隔离环境**——补丁要应用在确定性环境里(Docker 镜像/依赖锁死),否则"测试挂了"可能是环境漂移不是补丁的锅:这也是这类基准跑一次又慢又贵的原因。
  3. **本质与代价(本题的认知高点)**——"测试即验证器"把"修好了吗"这个模糊问题变成可机械判定的问题;**代价是判定质量完全等于测试集的质量**——测试欠规格、测试不公平,分数就失真。后续人工清洗版(如 OpenAI 参与的 Verified)干的就是删掉问题描述欠规格、测试不公平的题——清洗后同款模型分数大涨,说明原版分数被题目质量压低,不是模型突然变强。
- **补充弹药(追问再掏)**:
  - **迁移价值**:这两组测试的设计逻辑可以直接搬到自建 eval(见 Q7.11 场景题)——"目标测试+回归测试"双组是通用结构。
  - **环境确定性**:自建 eval 时同样要锁环境——eval 环境漂移是分数缓慢下降的常见根因(Q7.23)。
- **及格线**:两组测试各自封住什么漏洞。
- **加分**:"测试即验证器"的两面性;清洗版的意义。
- **红旗**:背"500 题"这类数字;说不清 pass-to-pass 的存在理由。

**Q7.2【八股·L2·模型】什么是 pass@k?三个经典的坑?**

- **人话版**:同一题独立采样 k 次,有一次通过就算过。三个坑:小样本方差大(几百题的 pass@1 抖两三个点正常);k 一变排名可能反转(pass@1 高的未必 pass@5 高);分数含义由验证器定义——验证器换了就不可比。
- **主线(照这条线讲)**:
  1. **方差坑**——500 题 pass@1 单次运行的标准误能到 2–3 个点:两个模型差 2 分以内基本是噪声。**看榜单先看样本量与置信区间**是基本素养;正经报告要么多采样取均值加区间,要么加题量。
  2. **k 反转坑**——贪心型模型 pass@1 高、多样型模型 pass@5 反超是常态(agentic RL 训出的模型常在 k 大时后来居上);**采购决策要按你实际的 k 评**:业务天然重试 3 次就看 pass@3。
  3. **验证器坑**——实证研究(如 R2E-Gym)测过:同一批模型换一套验证器,分数差 7–8 个点——跨榜单比分数时验证器不同就不可比,这也是"自建 eval"的根本理由之一。
- **补充弹药(追问再掏)**:
  - **计算细节**:pass@k 的无偏估计要用组合数公式(不是简单跑 k 次取一次)——被追问公式时别露怯。
  - **采样成本**:k=5 意味着评测成本×5——黄金集小的时候,多采样比加任务便宜。
- **及格线**:三个坑各一句。
- **加分**:按业务实际 k 选模型;多采样+置信区间的报告规范。
- **红旗**:拿两个不同验证器的榜单分数直接比大小。

**Q7.3【八股·L2·平台】LLM-as-judge 的三大经典偏差?分别怎么缓解?**

- **人话版**:位置偏差(换一下回答顺序结论翻转)、长度偏差(偏爱更长的)、自我偏好(偏爱自己家族模型的答案)。缓解:交换位置取平均、rubric 拆成可勾选项、多裁判集成、换别家强模型当裁判。
- **主线(照这条线讲)**:
  1. **三个偏差的出处**(MT-Bench,NeurIPS 2023)——系统实验:交换顺序后判决显著变化;长答案系统性占优;GPT-4 当裁判对 GPT 系回答打分偏高。**同文也给了正面数据**:GPT-4 裁判与人类一致性 >80%,约等于人类之间的一致性——所以结论是"可用,但必须做偏差治理",不是"不可用"。
  2. **rubric 细化是治本**——把"这个回答好不好"拆成可勾选条目(是否回答了问题/是否有事实错误/是否遗漏关键约束):**压缩裁判的自由度,偏差空间随之缩小**。Anthropic 生产经验:单次调用、0–1 分加 pass/fail 的朴素裁判最稳定——花样越多越不稳。
  3. **分工边界**——LLM 裁判负责规模化筛,人工测试负责发现裁判看不见的定性问题(裁判和模型共享同种系统性盲区——都偏爱表面流畅的内容农场):**裁判与人类校准集定期对齐,一致性掉线的维度回退人工**。
- **补充弹药(追问再掏)**:
  - **一致性数字**:60% 的一致性怎么救、分歧样本怎么分析——Q7.25 有完整排错链。
  - **工程细节**:校准集(人工标好的边界案例)要定期回归——裁判(随模型升级)自己也会漂移。
- **及格线**:三大偏差+至少两个缓解。
- **加分**:rubric 细化的原理;裁判与模型同源盲区的深层问题。
- **红旗**:裸评"打分 1-10"上生产,没有任何偏差控制。

**Q7.4【八股·L2·用户】为什么"让 agent 自己 review 自己写的代码"不可靠?实证依据?可信的替代方案?**

- **人话版**:同一套权重有同样的盲区——自己写错的假设,自己 review 时大概率发现不了。两篇实证:无外部反馈的自我纠错让性能不升反降;GPT-4 自评时批评文本写什么几乎不影响结果。替代:让执行结果说话,或换独立上下文、换模型。
- **主线(照这条线讲)**:
  1. **实证一**(Huang et al., ICLR 2024)——不给外部反馈、只让模型"再想想改改"的 intrinsic self-correction,推理任务上性能不升反降:模型会把对的改错——没有新信息,只有重新措辞。
  2. **实证二**(Stechly et al., 2024)——GPT-4 自评时,给它看什么批评文本、甚至不给,结果差不多:**自评不产生真实纠错信号,产生的是格式完整的"看起来在检查"**。
  3. **机制解释(统一两条实证)**——验证的价值来自独立信息源:自己验自己信息源没增加,只是同一分布采样两次;执行结果(编译器/测试)来自外部世界,才是真独立。
  4. **工程落地**——review 环节构造独立性:干净上下文(不带"我写的"痕迹)、不同模型、以执行产物为锚——**多 agent review 流水线的核心也是这个:reviewer 与 coder 的独立性比数量重要**(ch05 Q5.11)。
- **补充弹药(追问再掏)**:
  - **追问预备**:"那 LLM 裁判不也是模型吗?"——答:裁判评的是输出对不对(可对照标准),自评是凭空自查(无新信息),两者信息结构不同。
  - **独立性分级**:执行结果>确定性脚本>不同家族模型>人类——按成本递增选,能脚本化的别用模型。
- **及格线**:"同权重同盲区"+至少一篇实证结论。
- **加分**:从"独立信息源"统一解释;独立性分级。
- **红旗**:相信"让模型反思三遍就能抓住自己的错"。

**Q7.5【八股·L1·用户】浏览器怎么当 verifier?Playwright MCP 默认用 accessibility snapshot 而不是截图,取舍是什么?**

- **人话版**:前端任务的验收从"看起来对"变成可执行断言——路由可达、元素存在、流程走得通、console 无错。Playwright MCP 默认拿可访问性树而不是截图:结构化数据确定性强、省 token、不需要视觉模型;截图留给真正考视觉的场合。
- **主线(照这条线讲)**:
  1. **snapshot vs 截图的机制差异**(Playwright MCP 官方设计,2026-08 README)——accessibility tree 是结构化数据,元素有稳定 ref,点击/断言确定性强,官方原话"不需要视觉模型,纯结构化数据操作";截图要视觉模型定位坐标,模糊且贵;vision 能力按需开(`--caps vision`)。**这和 agent 检索用 grep 不用 embedding 是同一条设计哲学:能精确匹配就不要概率理解**——这句能把两章串起来。
  2. **验证分层**——行为验证(DOM 断言/流程回放/console+network 检查)兜大多数;视觉验证(截图对比)只留给样式回归,且排除动态区域防 flaky。Claude 的 Chrome 集成把这套做成了只读工具集(read_page/console/network/截图),官方用例就是"build 完直接在浏览器里验证,不切换上下文"。
  3. **和 agent 闭环的关系(落点)**——验收可执行化是 agent 自我纠错的前提:没有可执行的红绿信号,agent 只能靠"看起来差不多"收敛,每个结果都要人肉判定,工作流退化成聊天——**失败现场(截图+DOM dump+network log)回注 agent,才是完整闭环**。
- **补充弹药(追问再掏)**:
  - **省钱细节**:console 检查让 agent 按模式找错误而不是倒全量日志("logs can be verbose"是官方原话)。
  - **趋势注脚**:Playwright 官方自己说 coding agent 场景 CLI 形态比 MCP 更省 token(避免大 schema 和冗长 accessibility tree 进上下文)——工具形态也在做上下文经济学。
- **及格线**:snapshot 与截图的核心差异+两类可执行断言。
- **加分**:与 grep-over-embedding 哲学的贯通;失败现场回注的闭环。
- **红旗**:全页面截图对比(动态内容必 flaky);不知道 accessibility tree 是什么。

---

## 7.2 原理深挖

**Q7.6【原理·L4·模型】什么是验证器上限(verification ceiling)?举一条证据链说明它为什么决定一切。**

- **人话版**:分数的效度上限等于验证器的质量。验证器弱,模型或 RL 优化器越强,钻空子越狠——它们优化的是"过验证器",不是"做对事"。
- **主线(照这条线讲)**:
  1. **证据链三层展开**——①题目层:专家清洗发现原始基准里大量题目的测试不公平或问题描述欠规格(人工标注项目删掉几成"有问题"的题,清洗后同款模型分数大涨)——题目与测试质量直接决定分数含义;②测量层:R2E-Gym 实测多数任务里能有效区分对错补丁的测试不足两成,还存在"测试毒性"(错误补丁通过、正确补丁反挂);③训练层:DeepSeek-R1 训练中模型学会在差距里打印特定词骗过检查器——**reward hacking 不是假想敌,是 RLVR 的日常**。
  2. **对自建 eval 的推论**——先审计验证器,再谈模型强弱:held-out(模型不可见的那份)是底线;执行锚定优先于文本启发式(找"Successfully"字样);**验证器要定期审计——模型换代后,上一代绕不过的可能已被学会绕过**。
  3. **对采购的推论**——榜单的 harness 和验证器是别人的,你的任务和验收是自己的:榜单初筛,决策靠几十条"你自己的任务+强验证器"。
- **补充弹药(追问再掏)**:
  - **审计验证器的动作**:held-out 子集、毒性测试(拿已知对错的对决补丁喂测试看判定)、换验证器看分数稳定性。
  - **跨章连接**:harness 依赖的 33 分差距(ch09 Q9.12)是"验证环境也算验证器的一部分"的扩展版。
- **及格线**:"优化器钻验证器的空子"+一个真实案例。
- **加分**:审计验证器的具体手段;held-out 的信息隔离原理。
- **红旗**:认为"测试全绿=任务做对了";没想过测试本身可能不公平。

**Q7.7【原理·L3·模型】读一张"某 agent 榜单第一"的宣传图,你的检查清单是什么?**

- **人话版**:先问 harness(同模型换脚手架能差几十分)、再问口径(pass@几/几次采样/多少预算)、再问分布(它的任务和我的像不像)。三问问完,这张图的决策价值才浮现。
- **主线(照这条线讲)**:
  1. **harness 依赖是最大变量**——同款模型换 scaffold 差几十分:早期 Agentless 与带 agent 脚手架的同模型差近一倍;2026 年最锋利的例子:DeepSeek V4-Pro 自报 Terminal-Bench 87.9(自家 harness),第三方参考 harness 下约 54.7——**33 分,足够让"第一"变"中游"**(ch09 展开)。宣传图经常只写模型名不写 harness。
  2. **口径三连**——pass@k 的 k 和采样数(决定方差和含义);上下文/时间/token 预算(预算翻倍分数不可比);置信区间(没有区间的 2 分领先是噪声)。
  3. **分布匹配**——公开代码榜多是短时长、公开 Python 仓库、有明确测试的任务;企业私有任务是长时长、多语言、验收模糊——这个 gap 有硬证据(公开集 70%+ 的同款模型在商业代码子集上只有百分之十几)。
- **补充弹药(追问再掏)**:
  - **话术**:三个问题按顺序问,答不出的宣传图直接降级为"待验证信息"——这个姿态本身就是面试加分。
  - **反利用**:自己也发榜时主动披露 harness/k/预算——披露本身就是可信度信号(DeepSeek 的脚注算合规披露)。
- **及格线**:harness、口径、分布三个词各展开一句。
- **加分**:举"同模型不同 harness 分数剧变"的具体案例;结论落在"自建 eval 才是决策依据"。
- **红旗**:把第一名直接等于"选它没错"。

**Q7.8【原理·L3·平台】"把 eval 当 CI 跑"(evals-as-CI)具体长什么样?和传统测试 CI 的本质区别?**

- **人话版**:每次改 prompt/工具/模型配置自动跑固定任务集,分数和成本低于阈值阻断合并——像测试 CI,但断言对象从"代码行为"变成"agent 行为"。本质区别:它测概率系统的统计表现,要控方差、看成本,不是确定性的红绿。
- **主线(照这条线讲)**:
  1. **一套最小可用的五件**——黄金集(30–100 个真实任务:高频场景+历史失败案例,每个带可执行 verifier);触发(变更 PR 自动跑,并行 N 次控方差);报告(成功率+token 成本+时长三维);门禁(低于阈值阻断);演进(失败 case 自动回流成新任务)。
  2. **与传统 CI 的三个区别(本题核心)**——一是概率性:单次结果有噪声,必须多采样+看区间,不能像单测一次定生死;二是维度多:成本和延迟是一等指标(同成功率、3 倍 token 也是回归);三是会腐化:任务集会泄进训练数据(污染)、验证器会被新模型绕过——**需要滚动更新和定期审计,传统测试没有保质期问题**。
  3. **生产形态参照**——Anthropic 公开过的研究系统:全量生产 trace+LLM 裁判评估(不读对话原文保隐私)、失败分类驱动迭代、灰度切流不断长任务;2026-06 起行业往"防污染私有基准"走(Ramp 用生产代码滚动换题)。
- **补充弹药(追问再掏)**:
  - **冷启动务实版**:没有 100 个任务就从 10 个最痛的失败案例开始——价值在"每次变更有回归保障",不在任务集大小。
  - **门禁阈值**:定"比上版低 X 个点就拦"而不是绝对值——绝对阈值会被任务难度漂移打穿。
- **及格线**:五件套+“概率系统要多采样"。
- **加分**:三维报告;任务集保质期与滚动治理。
- **红旗**:单次跑分做门禁;任务集三年不换。

**Q7.9【原理·L4·平台】benchmark hacking 有哪些形态?各自的对策?**

- **人话版**:两大类——数据侧的污染(题目泄进训练集,分数虚高),行为侧的钻空子(改测试/删失败测试/硬编码/打印关键词骗检查器/专挑软柿子)。对策核心一句:验证器与优化器之间保持信息隔离+持续换题。
- **主线(照这条线讲)**:
  1. **污染与对策**——实证抓到过"训练截止后出的新题分数立刻回落"(LiveCodeBench 这类按发布日期过滤的基准就是为此设计);对策:滚动出题(评测要像新闻一样更新——Ramp 的防污染基准、持续积累任务池的思路)、held-out 子集永不公开。
  2. **行为侧钻空子与对策(逐条配对)**——改测试文件→禁改测试(规则+diff 检查);删失败测试→测试数量守恒检查;硬编码→held-out 测试;骗字符串检查→执行锚定+多验证器交叉;"挣分不干活"(专挑容易任务)→按难度分层计分+人工抽检。
  3. **行业动态当背景板**——Terminal-Bench 发榜单完整性公告(2026-06)并快速迭代(8 月底到 4.0);防污染私有基准(2026-06)、按"资深工程师标准"的变体(2026-07)、专门讲"防 agent 在自定义 SWE-bench 上作弊"的文章(2026-07)——**评测的军备竞赛本身说明这是真问题**。
- **补充弹药(追问再掏)**:
  - **自建版的底线两条**:held-out 验证集与训练/优化管道物理隔离;定期用"已知错误答案"测验证器能不能抓住(毒性测试)。
  - **话术**:"防作弊不是一次性工程,是和优化器的持续军备竞赛"——用这个框架回答显得有体系。
- **及格线**:污染与行为钻空子各举两例+对策。
- **加分**:held-out 的隔离原理;滚动更新的行业趋势。
- **红旗**:认为公开榜已解决作弊;自建 eval 用 agent 可见的验证器。

**Q7.10【原理·L3·架构】verifier 应该锚定系统的哪一层——主数据、支撑材料,还是派生视图?为什么"验证报表"经常是自欺?**

- **人话版**:验证要贴着"事实源头"做——数据库真实状态、代码真实行为;不要验证从事实派生的视图(报表/缓存/聚合指标)。为报表改报表,源头还是错的,系统越跑越假。
- **主线(照这条线讲)**:
  1. **三层心智模型**——主数据(系统真正维护的事实:源码/数据库/架构描述文件)、支撑材料(日志/快照/扫描结果)、派生物(报表/看板/缓存/摘要)。**验证锚定越靠近主数据信号越真;锚定派生物,你可能只是在验证"缓存的谎言"**。
  2. **典型病逐个点**——测试断言打了桩的接口返回值(验证的是 mock);监控看板全绿但用户在报障(聚合口径漏了);agent 验收标准是"报告里写了 done"(文本启发式,不锚定执行)——修法统一:把断言往下推一层,从报告推到数据查询、从 mock 推到契约测试或真环境 smoke。
  3. **agent 场景的新增一层(落点)**——agent 自己生成的"验证结论"也是派生物:这就是"让 agent 自证"不可靠的结构性原因(Q7.4)——**验收链的最后一环必须是机器或人,不能是 agent 的自我报告**。
- **补充弹药(追问再掏)**:
  - **与测试金字塔的对应**:最可信的断言贴近事实源(打真依赖子集的集成测试),最脆的贴 UI 快照和文本。
  - **权衡**:全部锚定主数据会慢(集成测试贵)——分层策略:改动风险高的路径锚定源头,低风险路径用轻量断言+抽样。
- **及格线**:三层区分+一个"验证派生物翻车"例子。
- **加分**:agent 自产验证结论也是派生物的推论;分层锚定策略。
- **红旗**:验收标准写成"输出里包含成功字样"。

---

## 7.3 Agent 的测试与证据纪律(工程实践五条)

**Q7.11【原理·L3·平台】测试该锚定需求而不是镜像实现:为什么"每个新函数配一个测试"是反模式?正确的测试-契约映射怎么做?**

- **人话版**:给每个组件/方法/分支镜像一个测试,测的是"实现长这样"而不是"需求要这样"——实现一重构测试全挂,而真正的能力缺口没人测。正确做法:每个测试锚定一个权威来源(需求/契约/领域不变量),pass/fail 要能代表"这个行为成立"。
- **主线(照这条线讲)**:
  1. **反模式的机制**——镜像实现的测试在实现重构时批量假阳性(红一片但需求没坏),团队学会无视红灯;而组件结构可能本身就编码了对需求的错误理解——**测试全绿和需求满足是两回事**。
  2. **正确做法(一个测试一个锚)**——立项时问:这个测试保护的哪条需求/契约/不变量?违反了它会有什么用户可见后果?答不上来的测试要么改用途要么删——**"没有持久依据的测试不配存在"**这句可以直说。
  3. **对 agent 写测试的特别意义**——agent 生成测试的默认行为就是镜像实现(它看着代码写):必须在任务说明里给"锚"(验收标准/契约),否则 agent 产出的是覆盖率好看的自欺——连到 Q7.14 的 mutation 验收。
- **补充弹药(追问再掏)**:
  - **评审动作**:code review 里对每个新测试问一句"它锚定哪条需求"——五分钟能筛掉一半无效测试。
  - **出处**:这条 practice 来自 lorelum agentic-coding 包(anchor tests to requirements),与测试社区的"测试行为不测结构"传统一致。
- **及格线**:镜像=测实现+锚定=测需求。
- **加分**:agent 写测试时的默认镜像行为及对策。
- **红旗**:用覆盖率数字当测试质量指标。

**Q7.12【原理·L3·平台】断言"可观察行为"而不是内部实现:具体怎么判断"该在哪个边界断言"?**

- **人话版**:先问"这个行为保护的是谁、他能观察到什么稳定结果",在那个最窄的可靠边界上断言:受控输入→观察输出或副作用。反模式:检查组件状态名、内部调用序列、临时 helper 的调用——需求关心的是存的数据、返回的结果、权限,不是这些。
- **主线(照这条线讲)**:
  1. **判断三步**——①找到被保护行为命名的"观察者"(用户/下游服务/审计员);②确定该观察者能看到的**稳定**结果(API 响应/落库数据/权限决定);③在最窄的可靠边界断言它——比它更深的都是实现细节,比它更宽的都是浪费。
  2. **反例的代价具体化**——断言内部调用序列:重构一改调用顺序测试就挂(假阳性);而真正的错误(保存的数据错了)它测不到(假阴性)——**内部断言两头都错**。
  3. **没有观察点怎么办**——"只能看见内部细节"说明缺一个合法的观察缝:补一个最小的测试接缝(查询接口/事件/快照),**而不是把实现细节宣布为契约**——后者是把偶然固化成必然。
- **补充弹药(追问再掏)**:
  - **经典例**:URL 结构调整时,断言"页面含 X 元素"的测试活下来,断言"调用了 renderX() 三次"的全挂——前者是行为后者是结构。
  - **与前端 verifier 的连接**:Playwright 的 accessibility snapshot 断言(Q7.5)就是这个原则的 UI 版——断言用户可观察的结构,不断言像素。
- **及格线**:观察者+稳定结果+最窄边界三步。
- **加分**:内部断言"两头都错"的分析;补观察缝的方案。
- **红旗**:mock 内部调用序列当测试;把实现细节当契约。

**Q7.13【原理·L4·平台】测试挂了,为什么"先改测试适配新实现"是错的?正确的分类流程是什么?**

- **人话版**:测试挂了先分类再动手——三种可能:实现坏了(修实现)、测试错了(修测试,要有依据)、需求变了(改需求文档+测试+实现一起)。直接改选择器和期望值适配新界面,是把"重构后行为是否仍满足需求"这个问题偷偷跳过了。
- **主线(照这条线讲)**:
  1. **分类流程(可操作)**——复现最小失败→对照**权威需求**和被测状态,判断失败属于哪类:观测行为与需求不符=实现问题;需求满足但测试挂=测试问题(测试锚错了或过期了);权威需求本身变过=需求变更(要走变更流程)→记录分类和排除其他类的证据,**分类完才许动手修**。
  2. **反模式的机制(题目自带的例子最好用)**——重设计的页面挂了测试,立刻更新选择器和期望行适配新页面,之后才想起来问"新设计还满足需求吗"——**顺序反了:先验证行为合规,再处理测试**;否则测试退化成"实现的镜子"(Q7.11 的病)。
  3. **对 agent 的特别意义**——agent 修 failing test 的默认冲动就是改测试让它绿(最短路径):**测试文件进禁改清单**(diff 检查),agent 只能修实现或上报"测试可能过期"——这是 harness 层防钻空子的关键一条。
  4. **证据模糊时**——三类都定不了:保持三样都不动(生产代码/测试/配置),升级到人——**宁可停下来也不猜着改**。
- **补充弹药(追问再掏)**:
  - **统计视角**:团队里"改测试"和"改实现"的提交比例是健康指标——改测试占比持续升高说明测试在漂向镜子。
  - **出处**:lorelum 的 classify-failure-before-changing-test,原文明说"分类之外的处置不属于这条 practice"——纪律的边界感。
- **及格线**:三分类+先分类后动手。
- **加分**:agent 改测试的防线;证据模糊时停下来。
- **红旗**:见到红的先改测试(最常见也最致命)。

**Q7.14【原理·L3·平台】"证据绑定工件状态"(bind evidence to artifact state):为什么"review passed""tests passed"是有保质期的?证据的四元组是什么?**

- **人话版**:任何验证结果都只对"验证时的那个工件状态"负责——代码/配置/diff 又变了,旧结论就过期。反模式:拿着早先的"review passed"写进完成声明,而 reviewed 的 diff 之后又改了。证据的四元组:针对哪个工件状态、什么环境、什么时候取的、覆盖什么行为范围。
- **主线(照这条线讲)**:
  1. **四元组展开**——工件状态(哪个 commit/diff)、环境(配置/依赖)、时间、行为范围(测了什么没测什么):**没有四元组的"通过"是无法判断保质期的"通过"**。
  2. **失效判据(不是所有变更都失效)**——比较四元组与当前状态,**只作废"结论可能被差异改变"的证据**:文档格式改动不会作废逻辑测试;改了被测代码本身就该作废——精细判断比一刀切重跑便宜。
  3. **对 agent 的落地(本题考点)**——Stop-gate/完成声明里禁止引用无指针的"已验证":每个证据带 commit hash/测试运行 id——**agent 最常见的作弊形态就是拿旧证据覆盖新状态**("测试都过了"——那是上一版的);completion claim 的生成要自动附当前状态的证据指针。
- **补充弹药(追问再掏)**:
  - **工程实现**:eval 报告里记 commit+环境指纹+时间戳,做"完成声明"时自动比对——几十行代码的事。
  - **连接**:这条和 Q6.13 交接验证是一对——交接时验"别人的证据是否过期",交付时验"自己的证据是否绑定当前状态"。
- **及格线**:四元组+证据随工件变化过期。
- **加分**:精细作废(不是一刀切);agent 拿旧证据的作弊形态。
- **红旗**:认为"通过过就是通过"。

**Q7.15【原理·L4·平台】"验收-证据映射表"(map evidence to acceptance):怎么审计一个"验证完成"的说法?**

- **人话版**:把每条验收条件列一行,记录"哪条证据支撑它、证据实际覆盖了什么范围",没有证据到达的条件标 uncovered——不把没覆盖的行推断成成功。反模式:列了计算测试、构建、手工检查就说"验证完成",但从没验证过"取消是否释放容量、重复提交是否生成重复订单"。
- **主线(照这条线讲)**:
  1. **表怎么建(两列起步)**——验收条件 | 支撑证据+覆盖范围;产出是一张**验收-证据映射表**,uncovered 行显式存在——这张表本身就是交付物,审它比听人讲"都测了"可靠一个量级。
  2. **两个典型漏法**——"证据体积制造信心":一堆单测+构建+人工检查,但没有一条证据触及"独立消费者能否打开导出文件"这种验收条件(数量≠覆盖);**"相邻证据冒充"**:点击成功的证据被当成持久化也成功的证据(点击≠落库)——范围列就是防它的。
  3. **与验证器上限的连接(落点)**——这张表是"验证器审计"的操作化:哪些条件没有可达的证据路径,就是验证器的缺口——**先补证据路径,再谈跑分**。
  4. **缺口的三种处置**(来自同族 practice)——补证据、把声明收窄到已有证据能支撑的范围、或标"未完成"——不许用相邻证据把缺口" Manufacture 成 pass"。
- **补充弹药(追问再掏)**:
  - **轻量落地**:PR 模板里加两列表格,填写过程就会暴露 uncovered——不填不知道,一填全是洞。
  - **管理视角**:这张表是"这个 PR 到底验证了什么"的诚实快照,评审从"读 diff"升级成"读表+抽 diff"。
- **及格线**:两列表+uncovered 显式存在。
- **加分**:两种典型漏法;缺口的三种处置。
- **红旗**:接受"验证完成"四个字的口头声明。

---

## 7.4 场景设计题

**Q7.16【场景·L3·平台】把代码评测的构造逻辑用到你的业务上:你们做内部数据平台的 agent,怎么构造任务集和判定机制?**

参考思路(考的是 fail-to-pass/pass-to-pass 逻辑的迁移):
- **任务怎么来**——真实工单/issue 历史里挑(带前后状态,能构造"修复前→修复后");不够就合成:基于真实仓库自动生成"改配置→验证行为"的任务;关键是每个任务有**可复现的初始状态**(环境快照/种子数据)。
- **判定怎么设计**——照搬双组:目标行为测试(fail-to-pass:任务前不成立、完成后必须成立)+回归测试(pass-to-pass:已有报表口径/API 契约不变)。**没有现成测试的任务先补测试再入集——写不出判定的任务不配进任务集**。
- **rubric 怎么来**——不可执行的部分(文档/解释质量)用 LLM 裁判+人工校准集;判定标准写进任务模板,不临场发挥。
- **防钻空子**——验证器与 agent 隔离(agent 看不到答案测试);held-out 子集;生产失败案例回流滚动换题;对删测试/改口径做 diff 检查。
- **及格线 / 加分 / 红旗**:及格=双组+环境可复现;加分=防钻空子与滚动更新;红旗=任务集全靠"人看着打分"。

**Q7.17【场景·L4·架构】公开榜 70% 的模型在你们内部任务上只有 40%。向管理层解释这个 gap,并给"榜单以后怎么用"的建议。**

参考思路:
- **三层归因**——任务分布差(短 vs 长、公开 vs 私有、清晰验收 vs 模糊验收);harness 差(榜单分数是"模型×它的 harness"组合,你的不同);验证口径差(测试覆盖率/严格度不同)。"公开集 vs 商业子集"十几到几十分的落差是行业已知的同类现象——不是你们团队不行。
- **榜单的三条用法**——只当初筛(淘汰明显落后的);看趋势不看单点(连续几代进步比某次第一有信息);任何决策过内部 eval,榜单做 sanity check。
- **反向建议**——把内部 eval 做成资产(任务集滚动/验证器审计):它对你们的决策价值永远大于任何公开榜——从"信榜单"到"信自己数据"是组织能力建设。
- **及格线 / 加分 / 红旗**:及格=三层归因;加分="初筛/决策"分流+内部 eval 资产化;红旗=全盘否定榜单或继续拿榜单当决策依据。

**Q7.18【场景·L4·模型】评"一天的工程任务"这种长时长 agent 能力,任务怎么构造、verifier 怎么设计才不被 hack?**

参考思路:
- **先承认根本张力**——任务越真实越难验证,越好验证越不真实:设计就是分层——核心里程碑用可执行验证、过程质量抽样人审、整体给部分得分而不是二元判定。
- **verifier 组合**——执行验证(端到端测试/部署 smoke/构建产物)为主;契约验证(API/schema 不变);held-out 终验(任务说明里不透露的验收点);人工抽检一定比例防系统性钻空;**过程卫生**:有没有篡改测试、有没有绕过问题(禁改清单 diff 检查)。
- **防污染与过拟合**——长任务构造贵、数量少、更容易被背题:参数化模板(换数据集/模块)放大同构任务;持续从生产攒新任务;**报告成本维度**(token/时长/API 费用)防"无限重试刷分"。
- **度量口径**——按人类耗时给任务分档(分钟/小时/天)分别报告;pass@k 按真实场景取 k;附置信区间。行业里 METR 的"时间视野"(50% 成功率对应的人类耗时)是代表性做法——拟合结论约每 7 个月翻倍,但来自特定任务族外推,引用带语境。
- **及格线 / 加分 / 红旗**:及格=承认张力+分层验证;加分=过程卫生检查+参数化防背题;红旗=拿几十道短任务的平均分外推"一天任务能力"。

**Q7.19【场景·L3·用户】让 agent 给自己写的代码补测试,风险是什么?怎么设计流程才可信?**

参考思路:
- **风险三连**——同义反复测试(照着错误实现写,永远绿);"过拟合测试"(刚好测到实现的当前输出值,换正确实现反而挂);覆盖率好看但断言质量差(只 assert 不抛异常)。根源还是 Q7.4:写码和测码同权重同上下文,盲区共享。
- **可信的设计**——测试与实现分离(不同会话/不同 subagent,或先写测试后写实现);测试的验收不看覆盖率看**杀伤力**——mutation testing(往实现注入 bug,好测试必须挂);property-based(随机输入验证不变量)替代逐点断言;人抽检断言质量而非数量。
- **流程落位**——agent 写测试→mutation 分数低于阈值打回;测试文件进禁改清单(后续修 bug 不能顺手改测试);回归集只增不减(删除测试要显式理由+人审)。
- **及格线 / 加分 / 红旗**:及格=同义反复风险+一种独立性手段;加分=mutation testing 当"测试的测试";红旗=用覆盖率当验收标准。

**Q7.20【场景·L4·架构】团队决定在提交前加一道"减法 review"(subtractive review):对 agent 生成的 diff,先删掉一切"没有当前理由"的新增,再提交。设计这道工序的判据与安全护栏。**

参考思路:
- **判据(每条新增过三问)**——它服务于哪条当前需求/已证实的风险/稳定契约?删掉它,验收条件还能全绿吗?它是"看起来常规"还是"必要依赖"(helper 看着常规可能是必需,扩展点看着优雅可能是多余)——**以证据定去留,不以观感定去留**。
- **安全护栏**——删的优先级:先删纯新增(未接线的函数/未用的依赖),再合并重复逻辑,最后替换本地重造为已有能力;**每类删除后跑验收**,证据随工件变化重新生效(Q7.14);显式要求的扩展点/鉴权检查这类"有授权依据"的绝不删——**减法的反模式是庆祝删除数量,把该删的和不该删的一起删**。
- **为什么 agent 时代特别需要**——agent 的默认倾向是"多做一点"(顺手加 helper/注释/兜底),diff 膨胀是系统性风险;减法 review 是对这种倾向的结构性对冲,连回 ch10 的"只收当前有据的工作"。
- **及格线 / 加分 / 红旗**:及格=三问判据+删除后跑验收;加分="以证据不以观感"的纪律;红旗=按删除行数考核(把该删不该删一起删)。

---

## 7.5 品味与判断题

**Q7.21【品味·L3·模型】"AI 生成的代码不需要逐行 review,review 意图和规格就行"——怎么站队?**

参考思路:
- **正方**——有测试与门禁背书的 AI 代码,逐行人读低效;人的时间该花在 spec 对齐/架构边界/安全敏感点——验证器覆盖不到的地方。
- **反方**——责任主体是人;测试通过≠意图正确(验证器上限);安全/合规/资金路径没有自动放行的余地;review 还有教育功能(团队知道仓库进了什么)。
- **综合**——按风险分级而不是按"是不是 AI"一刀切:低风险路径自动化门禁放行,人看门禁抽样+高危路径,高危路径 review"意图+边界+测试质量"而非逐行——**立场成立的前提是门禁可信,验证器的持续投入是决策的成本侧,多数团队低估了它**。
- **及格线 / 加分 / 红旗**:及格=分级而非站队;加分=点出门禁可信度是前置投资;红旗=绝对化表态。

**Q7.22【品味·L4·架构】"验证的投入应该和生成能力同步升级"——模型每升级一代,你的验证体系要跟着动什么?**

参考思路:
- **重新审计的**——旧验证器是否已被新模型"学会绕过"(钻空子能力在涨);新模型的失败模式变了(旧模型爱硬编、新模型爱过度重构),检测规则跟着改;评估任务集的区分度(题目会不会已经太容易)。
- **投入配比**——可执行验证>确定性检查(lint/type)>LLM 裁判>人工抽查;人工留给"裁判也不信任"的高危路径——**每代升级做一次配比复盘,而不是自动沿用**。
- **反模式**——无限加人工 review(不规模化,人会疲劳性放行);只测 happy path(agent 最擅长把 happy path 做得漂亮,故障注入和边界才是鉴别器)。
- **及格线 / 加分 / 红旗**:及格=验证器重审+配比复盘;加分="新模型新失败模式"举例;红旗=升级只换模型不动验证。

**Q7.23【品味·L3·平台】内部 eval 显示模型 A 成功率 82%、B 是 78%,但 A 的 token 成本是 B 的 2.5 倍。怎么决策?**

参考思路:
- **先拆那 4 个点**——差距在哪些任务类型上?集中在高价值任务(生产变更/复杂迁移)则 A 的溢价值得;集中在低价值铺量任务则 B+重试更划算。
- **失败的可检测性是关键变量**——B 的失败若可自动识别(测试挂了),B+重试+升级的组合比 A 单点便宜且稳;不可检测则 A 的成功率直接就是质量。
- **组合策略**——分层路由(高价值走 A,铺量走 B);或 B 先做、失败升级 A(**带上下文升级,别从头再来**)。
- **隐性成本入账**——失败任务的清理/人工干预/返工 token 都要计入:2.5 倍的标价差经常被失败的隐性成本反转。
- **及格线 / 加分 / 红旗**:及格=按价值分层;加分=失败可检测性决定重试策略;红旗=单维度决策。

---

## 7.6 开放性问题

**Q7.24【开放·L5·模型】如果"内部真实任务的 RL 环境与评测集"是各家稀缺资产,中小团队怎么获得可比的评估能力?**

讨论框架:
- **路径一:私有任务集积累**——把自家 issue/工单变 eval 零边际成本,难在判定(补测试的成本):从"每修一个 bug 顺手留一个回归任务"的纪律做起。
- **路径二:合成与参数化**——基于真实仓库生成同构任务(换模块/换数据)防背题;借鉴 R2E-Gym"从仓库自动构造环境+测试"的流水线。
- **路径三:少而精+强验证器**——30 个高质量任务+可信 verifier 的决策价值大于 3000 个弱任务;量不够就多采样控方差。
- **批判点**——评测即服务能保证"不泄漏给模型厂"吗?防污染私有基准(2026-06 起)说明需求真实,但对外部评测要有自己的审计手段。
- **加分方向**——trade 不了精度就 trade 维度:成本/延迟/安全性的横向对比比单点成功率更容易做可信。

**Q7.25【开放·L5·架构】设计一个"国家/行业级"coding agent 基准:任务怎么构造、verifier 怎么防 hack、结果怎么发布才有用?**

讨论框架:
- **构造**——真实性 vs 可验证性的张力(Q7.18);任务来源合法性(企业真实代码脱敏的隐私边界);按时间视野/领域/风险分档报告而不是单一总分。
- **防 hack**——held-out 子集、持续滚动出题、多验证器交叉、行为卫生检查(禁改测试)、**第三方执行环境**(提交方不可控环境跑,防环境作弊)。
- **发布**——分数附置信区间、成本/时长维度、harness 完全公开可复现(否则就是"模型×隐藏 harness"的营销);发布原始轨迹不只发分数,供研究者分析失败模式。
- **批判点**——静态基准都会被训练数据追上:可持续的任务供给机制(和企业工单管道打通)比首批任务集更难也更重要。

---

## 7.7 工程实战题

**Q7.26【实战·L3·平台】为你们的 agent 建立 evals-as-CI:改 prompt/工具/模型都自动回归。给落地方案。**

参考思路:
- **黄金集**——30–100 个真实任务=高频场景(按工单统计)+历史失败案例(每个生产事故固化成任务);每个任务带可执行 verifier+少量人工标注兜底(裁判维度)。
- **流水线**——变更 PR 触发→并行 3–5 次(按任务价值分层定 N)→报告成功率/token/时长三维→低于阈值(比上版低 X 点)阻断;失败自动归类(规格/工具/验证/运行时),LLM 裁判辅助归类。
- **治理**——任务集季度滚动防污染;verifier 年度审计防绕过;失败案例持续回流。
- **冷启动务实版**——10 个最痛的失败案例起步:价值在"每次变更有回归保障"不在任务集大小。
- **及格线 / 加分 / 红旗**:及格=黄金集+触发+门禁+多采样;加分=三维报告与滚动治理;红旗=单次跑分做门禁。

**Q7.27【实战·L3·用户】为"给管理后台加成员管理页"这类前端 agent 任务设计 Playwright verifier 集。**

参考思路:
- **断言分层**——路由可达(URL 200+目标组件渲染);数据契约(列表渲染来自 API 的字段,空态/loading);核心流程端到端(邀请:表单校验→提交→toast→表格刷新);权限(无权限 403 表现);副作用(审计日志写入)。
- **失败现场回注**——失败自动截图+DOM dump+console/network 错误一起喂回 agent:**没有现场的失败信息,agent 只能瞎猜**。
- **防 flaky**——选择器用语义锚(test id/data 属性)不用位置;等待显式化(等元素/等网络)不用 sleep;视觉回归只对静态区域开。
- **结构化优先**——能 accessibility snapshot 断言的不用截图(确定性+省 token,Q7.5);console 检查按模式找错误不倒全量日志。
- **及格线 / 加分 / 红旗**:及格=五类断言给三+失败现场;加分=防 flaky 与结构化优先;红旗=只做"页面能打开+截图存档"。

**Q7.28【实战·L4·平台】评测显示新配置在 60% 任务上更好、40% 更差,平均分持平。上还是不上?**

参考思路:
- **先过统计显著性**——60/40 的分布任务量小时就是噪声:多采样确认差异真实存在再谈决策。
- **分布分析**——更差的 40% 是哪些任务:集中在核心场景,平均分持平也拒绝;集中在边缘场景且核心普遍获益,带分流上线。
- **分流方案**——按任务类型路由新旧(各守强项);上线后持续采样对比,失败 case 回流 eval。
- **决策框架收束**——**平均分是给人看的,分布才是给决策用的**:正确动作是"拆开看"而不是"平均了事"。
- **及格线 / 加分 / 红旗**:及格=分布分析+分流;加分=统计显著性检查;红旗=只看平均分拍板(无论上不上)。

---

## 7.8 场景排错题

**Q7.29【排错·L3·模型】eval 分数连续三周缓慢下降,没有任何代码或配置变更。排查?**

排查链路:
- **①环境漂移(最常见)**——Docker 基础镜像/依赖锁/外部 API 版本变了:"没变"指配置文件没变,不是依赖树没变——锁版+变更日志是基本卫生。
- **②数据漂移**——任务依赖的外部世界变了(API 返回结构/网页结构/库版本),任务难度悄悄上升。
- **③验证器腐化**——测试依赖时间/随机种子/外部服务状态:固定种子+mock 外部依赖。
- **④模型侧静默更新**——供应商没说的路由变化/小版本(看响应里的模型版本指纹)。
- **⑤污染反噬**——如果之前分数异常高:新任务暴露了旧分数的水分。
- **定位法(记住这个)**——固定一个"金标准"版本模型+环境每周跑基线:把"世界变了"和"被测对象变了"分开归因。
- **及格线 / 加分 / 红旗**:及格=环境漂移+模型静默更新+锁版建议;加分=金标准基线法;红旗=归因"模型退化了"重跑几次取最好。

**Q7.30【排错·L4·平台】agent 在内部 eval 上 90%,生产任务只有 55%。差距怎么定位?**

排查链路:
- **分布差**——eval 任务代表生产吗:长度/领域/噪声/验收清晰度逐维对比;最常见的坑是 eval 全是"干净短任务"、生产全是"脏长任务"。
- **环境差**——生产的凭证/网络/数据状态不可控,eval 是温室:把生产差分项(限流/脏数据/权限)逐个注入 eval 复现。
- **验收差**——生产 55% 里多少是"无法判定"被记成失败?**先把生产失败按"真失败/无法判定"分类**——生产 verifier 比 eval 弱是常态。
- **采样差**——eval 的 k 和温度与生产不一致。
- **收敛机制(落点)**——生产失败 trace 采样回流成新 eval 任务,迭代到两个数字收敛:**差距本身不是问题,没有回流管道才是**。
- **及格线 / 加分 / 红旗**:及格=四个差里说出三个;加分=失败回流管道;红旗=直接说"生产环境不行"或"评测不准"然后弃用一个。

**Q7.31【排错·L3·平台】LLM 裁判和人工抽查的一致性只有 60%。改善路径?**

排查链路:
- **先看分歧样本再动手**——分歧集中在某类内容(长回答/代码)还是均匀分布:集中=系统性偏差,均匀=rubric 模糊——**先分类再治理,方向就不会错**。
- **rubric 细化**——把"好不好"拆成可勾选项;给边界案例的判定示例(few-shot 校准)。
- **偏差治理**——交换位置、多裁判集成、换别家强模型;对裁判建"校准集"(人工标好的边界案例)定期回归,一致性掉线报警。
- **兜底**——某维度怎么调都不行(需要业务上下文才能判的):承认裁判不适合,回退人工或半自动。
- **及格线 / 加分 / 红旗**:及格=分歧样本分析+rubric 细化;加分=校准集机制;红旗=直接换更大的裁判模型(成本翻倍偏差原样)。
