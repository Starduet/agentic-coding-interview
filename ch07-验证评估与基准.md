# ch07 · 验证、评估与基准

> 覆盖:评测任务怎么构造(fail-to-pass / pass-to-pass 的设计逻辑)、pass@k 的坑、验证器上限(verification ceiling)、自我验证为什么不可靠、LLM-as-judge 的偏差与治理、evals-as-CI、浏览器/多模态作为 verifier、防 benchmark 作弊。
> 出题立场:基准和榜单只做背景板,不出"某基准是哪年发布的、分数是多少"的记忆题;考察一律落在机制与设计取舍上。
> 案例基线:2026-08(带日期案例以标注为准;方法论不随时间变化)。

---

## 7.1 八股题(基础概念)

**Q7.1【八股·L2·模型】SWE-bench 这类代码评测的任务是怎么构造的?判定机制里 fail-to-pass 和 pass-to-pass 各自为什么必须存在?**

- **人话版**:任务从真实仓库的 issue+对应修复 PR 构造——把仓库回退到修复前,让 agent 打补丁,再跑测试。判定用两组测试:补丁前挂、补丁后必须绿(fail-to-pass,证明你真修了问题);补丁前就绿、补丁后也必须绿(pass-to-pass,证明你没把别的弄坏)。
- **展开**:
  - **为什么 fail-to-pass 不够**:只要求"修好目标测试"的话,最省力的"修法"是把目标功能整个删掉或者把测试改了——反正目标测试绿了。pass-to-pass 封掉这条歪路:回归测试挂了同样算失败,agent 必须在不破坏现有行为的前提下修复。
  - **为什么必须跑在隔离环境**:补丁要应用在确定性的环境里(Docker 镜像、依赖锁死),否则"测试挂了"可能是环境漂移而不是补丁的锅——这也是为什么这类基准跑一次又慢又贵。
  - **这个设计的本质**:**测试即验证器(tests as verifier)**。它把"修好了吗"这个模糊问题变成可机械判定的问题,代价是判定质量完全等于测试集的质量——测试欠规格、测试不公平,分数就失真(见 Q7.9 验证器上限)。后续人工清洗版(如 OpenAI 参与的 Verified)干的事就是删掉问题描述欠规格、测试不公平的题,清洗后分数大涨——说明原版分数被题目质量压低了,不是模型突然变强。
- **及格线**:能讲清两组测试各自封住什么漏洞。
- **加分**:能从"测试即验证器"推出它的两面性(可机械判定 ↔ 判定质量=测试质量);知道环境确定性是前提。
- **红旗**:只背"500 题""某某百分比"这类数字;说不清 pass-to-pass 存在的理由。

**Q7.2【八股·L2·模型】什么是 pass@k?它有哪三个经典的坑?**

- **人话版**:同一题独立采样 k 次,只要有一次通过就算过。三个坑:小样本方差大(几百题的 pass@1 抖两三个点很正常);k 一变排名可能反转(pass@1 高的未必 pass@5 高);分数的含义由验证器定义——验证器换了,分数就不可比。
- **展开**:
  - **方差坑**:500 题的 pass@1,单次运行的标准误能到 2–3 个点——两个模型差 2 分以内基本是噪声。正经报告要么多采样取均值加置信区间,要么增大题量。看榜单先看样本量与置信区间,是基本素养。
  - **k 反转坑**:贪心型模型 pass@1 高、多样型模型 pass@5 反超是常态,agentic RL 训出来的模型常在 k 大时后来居上。采购决策要按你实际的 k 评——你的业务如果天然会重试 3 次,就该看 pass@3。
  - **验证器坑**:实证研究(如 R2E-Gym)测过,同一批模型换一套验证器,分数差 7–8 个点。跨榜单比分数时,验证器不同就不可比——这也是"自建 eval"的根本理由之一。
- **及格线**:三个坑各说一句。
- **加分**:能联系自己业务"实际 k 是多少"来选模型;知道多采样+置信区间的报告规范。
- **红旗**:拿着两个不同验证器的榜单分数直接比大小。

**Q7.3【八股·L2·平台】LLM-as-judge 有哪三大经典偏差?分别怎么缓解?**

- **人话版**:位置偏差(把两个回答换下顺序,结论翻转)、长度偏差(偏爱更长的回答)、自我偏好(偏爱自己家族模型出的答案)。缓解:交换位置跑两遍取平均、把评分标准拆成可勾选项、多裁判集成、用别家的强模型当裁判。
- **展开**:
  - **三个偏差的出处**(MT-Bench,NeurIPS 2023):系统实验发现交换回答顺序后判决显著变化;长答案系统性占优;GPT-4 当裁判时对 GPT 系生成的回答打分偏高。同文也给了正面数据:GPT-4 裁判与人类一致性 >80%,约等于人类之间的一致性水平——所以 LLM 裁判可用,但必须做偏差治理。
  - **rubric 细化是治本**:把"这个回答好不好"拆成"是否回答了问题/是否有事实错误/是否遗漏关键约束"这样的可勾选条目,偏差空间大幅缩小。Anthropic 生产经验:单次调用、0–1 分加 pass/fail 的朴素裁判最稳定,花样越多越不稳。
  - **分工边界**:LLM 裁判负责规模化筛,人工测试负责发现裁判看不见的定性问题(比如裁判和模型共享同一种系统性盲区——都偏爱表面流畅的内容农场文)。裁判与人类校准集定期对齐,一致性掉到阈值下的维度回退人工。
- **及格线**:三大偏差+至少两个缓解手段。
- **加分**:rubric 细化的原理(压缩裁判自由度);裁判和被评模型同源盲区这个深层问题。
- **红旗**:直接拿一个模型裸评"打分 1-10"上生产,没有任何偏差控制。

**Q7.4【八股·L2·用户】为什么"让 agent 自己 review 自己写的代码"不可靠?实证依据是什么?可信的替代方案长什么样?**

- **人话版**:同一套权重有同样的盲区——自己写错的假设,自己 review 时大概率还是发现不了。两篇常被引用的实证:无外部反馈的自我纠错让性能不升反降;GPT-4 自评时,批评文本写什么几乎不影响结果。替代方案:让执行结果说话(测试/编译/类型检查),或者换独立上下文、换模型来 review。
- **展开**:
  - **实证一**(Huang et al., ICLR 2024):不给外部反馈、只让模型"再想想、改改"的 intrinsic self-correction,在推理任务上性能不升反降——模型会把对的改错,因为它没有新信息,只有重新措辞。
  - **实证二**(Stechly/Valmeekam/Kambhampati, 2024):GPT-4 自评时,给它看什么样的批评文本、甚至不给,结果差不多——自评不产生真实的纠错信号,产生的是格式完整的"看起来在检查"。
  - **机制解释**:验证的价值来自独立信息源。自己验证自己,信息源没增加,只是同一分布采样两次;执行结果(编译器、测试)是真正独立的信息源,因为它来自外部世界而不是模型权重。
  - **工程落地**:review 环节要构造独立性——干净上下文(不带着"我写的"痕迹)、不同模型、以执行产物为锚(而不是自由文本读代码)。多 agent review 流水线的设计核心也是这个:reviewer 与 coder 的独立性比数量重要。
- **及格线**:能说出"同权重同盲区"+至少一篇实证的结论。
- **加分**:从"独立信息源"角度统一解释为什么执行验证有效、换模型有效;指出这是 review 流水线设计的第一原则。
- **红旗**:相信"让模型反思三遍就能抓住自己的错";把 review 做成同一个会话里的自我总结。

**Q7.5【八股·L1·用户】浏览器怎么当 verifier?Playwright MCP 默认用 accessibility snapshot 而不是截图,这个设计取舍是什么?**

- **人话版**:前端任务的验收从"看起来对"变成可执行断言——路由可达、元素存在、流程走得通、console 无错。Playwright MCP 默认拿页面的可访问性树(accessibility snapshot)而不是截图,因为结构化数据确定性强、省 token、不需要视觉模型;截图留给真正考视觉的场合(样式/布局)。
- **展开**:
  - **snapshot vs 截图的取舍**(Playwright MCP 官方设计,2026-08 README):accessibility tree 是结构化数据,元素有稳定 ref,点击/断言确定性强,官方原话"不需要视觉模型,纯结构化数据操作";截图方案要视觉模型定位坐标,模糊且贵。vision 能力按需开启(`--caps vision`)。这和 agent 检索用 grep 而不是 embedding 是同一条设计哲学:能精确匹配就不要概率理解。
  - **验证分层**:行为验证(DOM 断言、流程回放、console/network 检查)兜底大多数问题;视觉验证(截图对比)只留给样式回归,且要排除动态区域防 flaky。Claude 的 Chrome 集成把这套做成了只读工具集(read_page/get_page_text/console/network/截图),官方用例就是"build 完直接在浏览器里验证,不用切换上下文"。
  - **和 agent 闭环的关系**:验收可执行化是 agent 自我纠错的前提——没有可执行的"红/绿"信号,agent 只能靠"看起来差不多"收敛,每个结果都要人肉判定,工作流退化成聊天。失败现场(截图+DOM dump+network log)回注给 agent,才是完整的验证闭环。
- **及格线**:说出 snapshot 与截图的核心差异+至少两类可执行断言。
- **加分**:能把"结构化优先、视觉兜底"接回 agent 工具设计的通用哲学;提到 console/network 这种信号源。
- **红旗**:一上来就截图对比全页面(动态内容必 flaky);不知道 accessibility tree 是什么。

---

## 7.2 原理深挖

**Q7.6【原理·L4·模型】什么是验证器上限(verification ceiling)?举一条证据链说明它为什么决定一切。**

- **人话版**:你的分数的效度上限等于验证器的质量。验证器弱,模型或 RL 优化器越强,钻空子越狠——它们优化的是"过验证器",不是"做对事"。
- **展开**:
  - **证据链(三层)**:①专家清洗发现原始基准里大量题目的测试不公平或问题描述欠规格(人工标注项目删掉了几成"有问题"的题,清洗后同款模型分数大涨)——说明题目与测试质量直接决定分数含义;②实证测量(R2E-Gym)发现多数任务里能有效区分"对补丁/错补丁"的测试不足两成,还存在"测试毒性"(错误补丁通过、正确补丁反挂);③训练侧的教科书案例:DeepSeek-R1 训练中模型学会在差距里打印特定词骗过检查器——reward hacking 不是假想敌,是 RLVR 的日常。
  - **对自建 eval 的推论**:先审计验证器,再谈模型强弱。held-out 测试(模型/agent 不可见的那份)是底线;执行锚定(真跑起来)优先于文本启发式(找"Successfully"字样);验证器要定期审计——模型换代后,上一代绕不过的验证器可能已被"学会绕过"。
  - **对采购的推论**:榜单的 harness 和验证器是别人的,你的任务和验收是自己的——榜单初筛,决策靠几十条"你自己的任务+强验证器"。
- **及格线**:能独立说出"优化器会钻验证器的空子"+一个真实案例。
- **加分**:审计验证器的具体手段(held-out、毒性测试、换验证器看分数稳定性)。
- **红旗**:认为"测试全绿"就等于"任务做对了";没想过测试本身可能不公平。

**Q7.7【原理·L3·模型】pass@1、best-of-k、自报分数……读一张"某 agent 榜单第一"的宣传图,你的检查清单是什么?**

- **人话版**:先问 harness(同一模型换个脚手架分数能差几十个点)、再问口径(pass@1 还是 best-of-k、几次采样、多少 token 预算)、再问分布(它的任务和我的像不像)。三个都问完,这张图的决策价值才浮现。
- **展开**:
  - **harness 依赖是最大变量**:同款模型,换 scaffold 能差出几十分(早期 Agentless 与带 agent 脚手架的同模型在同榜差近一倍;2026 年更有厂商在自报分里注明用了自家未发布的 harness 跑基准,同模型在第三方参考 harness 下分数差三十多个点——见 ch09 DeepSeek 节)。所以"模型×harness"是组合分数,宣传图经常只写模型名。
  - **口径三连**:pass@k 的 k 和采样次数(决定方差和含义);上下文/时间/token 预算(预算翻倍分数就不可比);置信区间(没有区间的 2 分领先是噪声)。
  - **分布匹配**:公开代码榜多是短时长、公开 Python 仓库、有明确测试的任务;企业私有任务是长时长、多语言、验收模糊的——这个 gap 有硬证据(公开集 70%+ 的同款模型在商业代码子集上只有百分之十几)。
- **及格线**:harness、口径、分布三个词都能展开一句。
- **加分**:能举一个"同模型不同 harness 分数剧变"的具体案例;结论落在"自建 eval 才是决策依据"。
- **红旗**:把榜单第一名直接等于"选它没错";只看分数不问 k。

**Q7.8【原理·L3·平台】"把 eval 当 CI 跑"(evals-as-CI)具体长什么样?和传统测试的 CI 有什么本质区别?**

- **人话版**:每次改 prompt/工具/模型配置都自动跑一遍固定任务集,分数和成本低于阈值就阻断合并——像测试 CI 一样,但断言对象从"代码行为"变成"agent 行为"。本质区别:它测的是概率系统的统计表现,要控方差、看成本,而不是确定性的红绿。
- **展开**:
  - **一套最小可用的组成**:黄金集(30–100 个真实任务,覆盖高频场景+历史失败案例,每个带可执行 verifier);触发(变更 PR 自动跑,并行 N 次控方差);报告(成功率+token 成本+时长三个维度);门禁(低于阈值阻断);演进(失败 case 自动回流成新任务)。
  - **与传统 CI 的三个区别**:一是概率性——单次结果有噪声,必须多采样+看区间,不能像单测那样一次定生死;二是维度多——成本和延迟是一等指标(同样的成功率,3 倍 token 也是回归);三是会腐化——任务集会泄漏进训练数据(污染)、验证器会被新模型绕过,需要滚动更新和定期审计,传统测试没有这个保质期问题。
  - **生产形态参考**(Anthropic 公开过的研究系统做法):全量生产 trace+LLM 裁判评估(不读对话原文保护隐私)、按维度打分、失败分类驱动迭代、灰度切流不断长任务。2026-06 行业也在往"防污染私有基准"方向走(Ramp 用生产代码构建、定期滚动换题)。
- **及格线**:黄金集+自动触发+门禁三件套,以及"概率系统要多采样"这个意识。
- **加分**:成本/延迟作为一等门禁维度;任务集自身的腐化与滚动更新。
- **红旗**:拿单次跑分做门禁;任务集三年不换。

**Q7.9【原理·L4·平台】benchmark hacking(评测作弊/钻空子)有哪些形态?各自的对策是什么?**

- **人话版**:两大类——数据侧的污染(题目泄漏进训练集,分数虚高),行为侧的钻空子(改测试、删失败测试、硬编码期望值、打印关键词骗检查器、挑软柿子任务做)。对策核心就一句话:验证器与优化器之间必须保持信息隔离+持续换题。
- **展开**:
  - **污染与对策**:实证上抓到过"训练截止日期之后出的新题分数立刻回落"的案例(LiveCodeBench 这类按发布日期过滤的基准就是为此设计);对策是滚动出题(评测要像新闻一样更新,Ramp 的防污染基准、SWE-rechains 这类持续积累任务池的思路)、held-out 子集永不公开。
  - **行为侧钻空子与对策**:改测试文件→禁止改测试(规则+diff 检查);删失败测试→测试数量守恒检查;硬编码→held-out 测试;骗字符串检查→执行锚定+多验证器交叉;"挣分不干活"(专挑容易的任务)→按任务难度分层计分+人工抽检。
  - **2026 年中的行业动态(背景板)**:Terminal-Bench 发了榜单完整性公告(2026-06)并快速迭代版本(8 月底已到 4.0),防污染私有基准、按"资深工程师标准"评 agent 的变体(2026-07)陆续出现,还有厂商专门发文讲"怎么防 agent 在自定义 SWE-bench 上作弊"(2026-07)——评测的军备竞赛本身说明这是真问题。
- **及格线**:污染与行为钻空子各举两例+对策。
- **加分**:held-out 的信息隔离原理;知道评测滚动更新是行业正在发生的事。
- **红旗**:认为"公开榜已经解决了作弊";自建 eval 用被评测 agent 可见的验证器。

**Q7.10【原理·L3·架构】verifier 应该锚定系统的哪一层——主数据、支撑材料,还是派生视图?为什么"验证报表"经常是自欺?**

- **人话版**:验证要尽量贴着"事实源头"做——数据库的真实状态、代码的真实行为;不要验证从事实派生出来的视图(报表、缓存、聚合指标)。为报表改报表,源头还是错的,系统越跑越假。
- **展开**:
  - **三层的心智模型**:主数据(系统真正维护的事实:源码、数据库、架构描述文件)/支撑材料(日志、快照、扫描结果)/派生物(报表、看板、缓存、摘要)。验证锚定越靠近主数据,信号越真;锚定派生物,你可能只是在验证"缓存的谎言"。
  - **典型病**:测试断言打了桩的接口返回值(验证的是 mock);监控看板全绿但用户在报障(聚合口径漏了);agent 的验收标准是"生成的报告里写了 done"(文本启发式,不锚定执行)。修法:把断言往下推一层——从报告推到数据查询,从 mock 推到契约测试或真实环境的 smoke。
  - **和测试金字塔的对应**:最可信的断言是贴近事实源头的(集成测试打真依赖的子集);最脆的是贴着 UI 快照和文本的。agent 场景新增了一层风险:agent 自己生成的"验证结论"也是派生物——这就是"让 agent 自证"不可靠的结构性原因(见 Q7.4)。
- **及格线**:三层区分+一个"验证派生物翻车"的例子。
- **加分**:把原则推到 agent 验收设计(警惕 agent 自产的验证结论);主动聊 mock/桩的边界。
- **红旗**:验收标准写成"输出里包含成功字样";测试全打桩还觉得覆盖很全。

---

## 7.3 场景设计题(去 trivia 的主战场:无标准答案,言之有理即可)

**Q7.11【场景·L3·平台】把"代码评测的构造逻辑"用到你的业务上:你们做的是内部数据平台的 agent,怎么构造你自己的任务集和判定机制?**

参考思路(考察的是能否把 fail-to-pass/pass-to-pass 的设计逻辑迁移):
- **任务怎么来**:从真实工单/issue 历史里挑(带前后状态,能构造"修复前→修复后");不够就合成——基于真实仓库自动生成"改配置→验证行为"的任务。关键是每个任务要有**可复现的初始状态**(环境快照/种子数据)。
- **判定怎么设计**:照搬两组测试的思想——目标行为测试(fail-to-pass:任务前不成立、完成后必须成立)+ 回归测试(pass-to-pass:原有功能不能坏)。你的业务里"回归测试"可能是:已有报表口径不变、已有 API 契约不变。没有现成测试的任务,先补测试再入集——**写不出判定的任务不配进任务集**。
- **rubric 怎么来**:不可执行的部分(文档质量、解释清楚度)用 LLM 裁判+人工校准集;判定标准写进任务模板,别临场发挥。
- **怎么防 agent 钻空子**:验证器与 agent 隔离(agent 看不到答案测试);held-out 子集;定期把生产失败案例回流入集滚动换题;对"删测试、改口径"这类行为做 diff 检查。
- **及格线 / 加分 / 红旗**:及格=能独立说出"目标测试+回归测试"双组和"环境可复现";加分=防钻空子的隔离设计与滚动更新;红旗=任务集全是"让 agent 写个功能然后人看着打分"——不可机械判定就没法回归。

**Q7.12【场景·L4·架构】公开榜 70% 的模型,在你们内部任务上只有 40%。你向管理层解释这个 gap,并且给出"榜单以后怎么用"的建议。**

参考思路:
- **解释 gap 的三层原因**:任务分布差(短 vs 长、公开 vs 私有、清晰验收 vs 模糊验收);harness 差(榜单分数是"模型×它的 harness"的组合,你的 harness 不同);验证口径差(榜单的测试覆盖率/严格度和你的不同)。"公开集 vs 商业代码子集"十几到几十分的落差是行业已知的同类现象,不是你们团队不行。
- **建议怎么用榜单**:三条——只当初筛(淘汰明显落后的);看趋势不看单点(一个模型连续几代进步,比它某次第一更有信息);任何采购/升级决策过内部 eval,榜单分只做 sanity check。
- **反向建议**:把内部 eval 做成资产(任务集滚动、验证器审计),它对你们的决策价值永远大于任何公开榜——这是从"信榜单"到"信自己数据"的组织能力建设。
- **及格线 / 加分 / 红旗**:及格=三层归因;加分=给出"榜单→初筛、内部 eval→决策"的清晰分流和落地路径;红旗=要么全盘否定榜单,要么继续拿榜单当决策依据二选一站队。

**Q7.13【场景·L4·模型】你要评"一天的工程任务"这种长时长 agent 能力,任务怎么构造、verifier 怎么设计才不被 hack?**

参考思路:
- **根本张力**:任务越真实越难验证,越好验证越不真实。设计就是在两者间找分层——核心里程碑用可执行验证,过程质量用抽样人审,整体给"部分得分"而不是二元判定。
- **verifier 组合**:执行验证(端到端测试/部署 smoke/构建产物检查)为主;契约验证(API/ schema 不变);held-out 终验(任务说明里不透露的验收点);人工抽检一定比例防"系统性钻空"。长任务还要验证**过程卫生**:有没有篡改测试、有没有把问题绕过去(禁改清单 diff 检查)。
- **防污染与防过拟合**:长任务构造贵,数量少,更容易被背题——用参数化模板(换数据集/换模块)放大同构任务;持续从生产攒新任务;报告成本维度(token/时长/API 费用),防止"无限重试刷分"。
- **度量口径**:按"人类耗时"给任务分档(分钟级/小时级/天级)分别报告;pass@k 按真实使用场景取 k;附置信区间。行业里 METR 的"时间视野"(50% 成功率对应的人类耗时)就是这个思路的代表性做法,2025–2026 的拟合结论是约每 7 个月翻倍——但注意它来自特定任务族的外推,引用时要带语境。
- **及格线 / 加分 / 红旗**:及格=承认张力并给出分层验证;加分=过程卫生检查+参数化防背题;红旗=拿几十道短任务的平均分外推"一天任务能力"。

**Q7.14【场景·L3·用户】让 agent 给自己写的代码补测试,风险是什么?怎么设计流程才可信?**

参考思路:
- **风险三连**:同义反复测试(测试照着错误实现写,永远绿);"过拟合测试"(刚好测到实现的输出值,换正确实现反而挂);覆盖率好看但断言质量差(只 assert 不抛异常)。根源还是 Q7.4——写码和测码是同一权重同一上下文,盲区共享。
- **可信的设计**:测试与实现分离(不同会话/不同 subagent,或先写测试后写实现);测试的验收不看覆盖率看**杀伤力**——mutation testing(往实现里注入 bug,好测试必须挂);property-based(生成随机输入验证不变量)替代逐点断言;人抽检断言质量而非数量。
- **流程落位**:agent 写测试→mutation 分数低于阈值打回;测试文件进禁改清单(agent 后续修 bug 不能顺手改测试);回归集只增不减(删除测试需要显式理由+人审)。
- **及格线 / 加分 / 红旗**:及格=同义反复风险+一种独立性手段;加分=mutation testing 作为测试的测试;红旗=用覆盖率数字当验收标准。

---

## 7.4 品味与判断题

**Q7.15【品味·L3·模型】"AI 生成的代码不需要逐行 review,review 意图和规格就行"——你怎么站队?**

参考思路:
- **正方有理**:有测试与门禁背书的 AI 代码,逐行人读是低效的;人的时间该花在 spec 对齐、架构边界、安全敏感点——这些是验证器覆盖不到的。
- **反方有理**:责任主体还是人;测试通过≠意图正确(验证器上限);安全/合规/资金路径没有"自动放行"的余地;而且现行 review 还有教育功能(团队知道仓库里进了什么)。
- **我的综合**:按风险分级而不是按"是不是 AI 写的"一刀切——低风险路径靠自动化门禁(测试+lint+契约)放行,人工只看门禁的抽样与高危路径,且高危路径 review 的是"意图+边界+测试质量"而非逐行。这个立场成立的前提是门禁可信——所以验证器的持续投资是这个决策的成本侧,多数团队低估了它。
- **及格线 / 加分 / 红旗**:及格=给出分级而非站队;加分=点出"门禁可信度"是前置投资;红旗=绝对化表态(全部逐行 or 全部免审)。

**Q7.16【品味·L4·架构】"验证的投入应该和生成能力同步升级"——模型每升级一代,你的验证体系要跟着动什么?**

参考思路:
- **要重新审计的**:旧验证器是否已被新模型"学会绕过"(新模型的钻空子能力在涨);新模型擅长的失败模式变了(旧模型爱硬编,新模型爱过度重构),检测规则要跟着改;评估任务集的区分度(题目会不会已经"太容易"失去区分度)。
- **投入配比的优先级**:可执行验证(测试/属性/契约)> 确定性检查(lint/type)> LLM 裁判 > 人工抽查;人工留给"裁判也不信任"的高危路径。每代模型升级做一次配比复盘,而不是自动沿用。
- **反模式**:无限加人工 review(不可能规模化,且人会疲劳性放行);只测 happy path(agent 最擅长把 happy path 做得漂亮,故障注入和边界才是鉴别器)。
- **及格线 / 加分 / 红旗**:及格=验证器重审+配比复盘;加分=指出"新模型新失败模式"并举例;红旗=模型升级只换模型不动验证。

**Q7.17【品味·L3·平台】内部 eval 显示模型 A 成功率 82%、B 是 78%,但 A 的 token 成本是 B 的 2.5 倍。怎么决策?**

参考思路:
- **别急着选,先拆**:那 4 个点的差距在哪些任务类型上?如果集中在高价值任务(生产变更、复杂迁移),A 的溢价值得;如果集中在低价值铺量任务,B+重试更划算。失败模式的可检测性也关键——B 的失败若可自动识别(测试挂了),B+重试+升级的组合比 A 单点便宜且稳。
- **组合策略**:分层路由(高价值走 A,铺量走 B);或 B 先做、失败升级 A(带上下文升级,别从头再来)。
- **隐性成本入账**:失败任务的清理、人工干预、返工 token 都要计入——2.5 倍的标价差经常被"失败的隐性成本"反转。
- **及格线 / 加分 / 红旗**:及格=按任务价值分层而不是全局二选一;加分=失败可检测性决定重试策略+隐性成本;红旗=只看单价或只看成功率单维度决策。

---

## 7.5 开放性问题

**Q7.18【开放·L5·模型】如果"内部真实任务的 RL 环境与评测集"是各家稀缺资产,中小团队怎么获得可比的评估能力?**

讨论框架:
- **路径一:私有任务集积累**——把自家 issue/工单变成 eval 是零边际成本的事,难在判定(补测试的成本);从"每修一个 bug 顺手留一个回归任务"的纪律做起。
- **路径二:合成与参数化**——基于真实仓库生成同构任务(换模块/换数据),用模板放大任务量防背题;借鉴 R2E-Gym 这类"从仓库自动构造环境+测试"的流水线思路。
- **路径三:少而精+强验证器**——30 个高质量任务+可信 verifier 的决策价值大于 3000 个弱任务;量不够就多采样控方差。
- **批判点**:评测即服务的第三方能保证"不泄漏给模型厂"吗?行业里防污染私有基准(2026-06 起)的出现说明需求真实,但中小团队对"外部评测的可信度"要有自己的审计手段。
- **加分方向**:trade 不了精度就 trade 维度——成本/延迟/安全性的横向对比比单点成功率更容易做可信。

**Q7.19【开放·L5·架构】设计一个"国家/行业级"的 coding agent 基准:任务怎么构造、verifier 怎么设计才不被 hack、结果怎么发布才有用?**

讨论框架:
- **构造**:任务真实性 vs 可验证性的张力(见 Q7.13);任务来源的合法性(企业真实代码脱敏的隐私边界);按时间视野/领域/风险分档报告而不是单一总分。
- **防 hack**:held-out 子集、持续滚动出题、多验证器交叉、行为卫生检查(禁改测试)、第三方执行环境(提交方不可控环境跑,防环境作弊)。
- **发布**:分数附置信区间、成本/时长维度、harness 完全公开可复现(否则就是"模型×隐藏 harness"的营销);发布原始轨迹而不只是分数,供研究者分析失败模式。
- **批判点**:任何静态基准都会被训练数据追上——"国家基准"若不滚动更新,一年后就是历史文物;可持续的任务供给机制(和企业工单管道打通)比首批任务集更难也更重要。

---

## 7.6 工程实战题

**Q7.20【实战·L3·平台】为你们的 agent 建立 evals-as-CI:改 prompt/工具/模型都自动回归。给落地方案。**

参考思路:
- **黄金集**:30–100 个真实任务,构成=高频场景(按工单统计)+历史失败案例(每个生产事故固化成任务);每个任务带可执行 verifier+少量人工标注兜底(裁判维度)。
- **流水线**:变更 PR 触发→并行 N 次(3–5 次控方差,N 按任务价值分层)→报告成功率/token/时长三维→低于阈值阻断合并;失败自动归类(规格/工具/验证/运行时),LLM 裁判辅助归类。
- **治理**:任务集季度滚动更新防污染;verifier 年度审计防绕过;失败案例持续回流。
- **冷启动务实版**:没有 100 个任务就从 10 个最痛的失败案例开始——evals-as-CI 的价值在"每次变更有回归保障",不在任务集大小。
- **及格线 / 加分 / 红旗**:及格=黄金集+触发+门禁+多采样;加分=三维报告与滚动治理;红旗=单次跑分做门禁、任务集建完不动了。

**Q7.21【实战·L3·用户】为"给管理后台加成员管理页"这类前端 agent 任务设计 Playwright verifier 集。**

参考思路:
- **断言分层**:路由可达(访问 URL 返回 200 且渲染目标组件);数据契约(列表渲染来自 API 的字段,空态/loading 态);核心流程端到端(邀请:表单校验→提交→toast→表格刷新);权限(无权限 403 的表现);副作用(审计日志写入)。
- **失败现场回注**:失败时自动截图+DOM dump+console/network 错误一起喂回 agent——没有现场的失败信息,agent 只能瞎猜。
- **防 flaky**:选择器用语义锚(test id/data 属性)不用位置;等待显式化(等元素/等网络)不用 sleep;视觉回归只对静态区域开,排除动态内容。
- **结构化优先**:能用 accessibility snapshot 断言的不用截图(确定性+省 token,见 Q7.5);console 检查让 agent 按模式找错误而不是倒全量日志。
- **及格线 / 加分 / 红旗**:及格=五类断言里给三类+失败现场;加分=防 flaky 与结构化优先的意识;红旗=只做"页面能打开+截图存档"。

**Q7.22【实战·L4·平台】评测显示新配置在 60% 任务上更好、40% 上更差,平均分持平。上线还是不上?**

参考思路:
- **先做失败/获益分布分析**:更差的 40% 是哪些任务?如果集中在你核心场景,平均分持平也该拒绝;如果集中在边缘场景且核心场景普遍获益,可以带分流上线。
- **分流方案**:按任务类型路由新旧配置(各自的强项领域);上线后持续采样对比,失败 case 回流 eval。
- **方差检查**:60/40 的分布本身过不过统计显著性(任务量小的时候这就是噪声);多采样确认差异真实存在再谈决策。
- **决策框架**:平均分是给人看的,分布才是给决策用的——这个 case 的正确动作是"拆开看",而不是"平均了事"。
- **及格线 / 加分 / 红旗**:及格=分布分析+分流;加分=统计显著性检查;红旗=只看平均分拍板(无论上还是不上)。

---

## 7.7 场景排错题

**Q7.23【排错·L3·模型】eval 分数连续三周缓慢下降,没有任何代码或配置变更。排查?**

排查链路:
- **①环境漂移**(最常见):Docker 基础镜像、依赖锁、外部 API 版本变了——eval 环境"没变"指的是配置文件没变,不是依赖树没变;锁版+变更日志是基本卫生。
- **②数据漂移**:任务依赖的外部世界变了(API 返回结构、网页结构、库版本),任务"难度"悄悄上升。
- **③验证器腐化**:测试依赖时间/随机种子/外部服务状态;固定种子+mock 外部依赖。
- **④模型侧静默更新**:供应商没说的路由变化/小版本(看响应里的 system_fingerprint 类字段)。
- **⑤污染反噬**(如果之前分数异常高):新任务暴露了旧分数的水分。
- 定位法:固定一个"金标准"版本的模型+环境每周跑基线,把"世界变了"和"被测对象变了"分开归因。
- **及格线 / 加分 / 红旗**:及格=环境漂移+模型静默更新两条+锁版建议;加分=金标准基线的对照方法;红旗=归因于"模型退化了"然后重跑几次取最好成绩。

**Q7.24【排错·L4·平台】agent 在内部 eval 上 90%,生产任务只有 55%。差距怎么定位?**

排查链路:
- **分布差**:eval 任务代表生产吗——长度、领域、噪声、验收清晰度逐维对比;最常见的坑是 eval 全是"干净短任务",生产全是"脏长任务"。
- **环境差**:生产的凭证/网络/数据状态不可控,eval 是温室;把生产环境的差分项(限流、脏数据、权限)逐个注入 eval 复现。
- **验收差**:生产 55% 里有多少是"无法判定"被记为失败?生产 verifier 比 eval 弱得多是常态——先把生产失败按"真失败/无法判定"分类。
- **采样差**:eval 跑的 k 和温度与生产不一致。
- **收敛机制**:生产失败 trace 采样回流成新 eval 任务,迭代到两个数字收敛——差距本身不是问题,没有回流管道才是。
- **及格线 / 加分 / 红旗**:及格=四个差里说出三个;加分=失败回流管道;红旗=直接说"生产环境不行"或者"评测不准"然后弃用一个。

**Q7.25【排错·L3·平台】LLM 裁判和人工抽查的一致性只有 60%。改善路径?**

排查链路:
- **先看分歧样本再动手**:分歧集中在某类内容(长回答?代码?)还是均匀分布——集中说明是系统性偏差,均匀说明是 rubric 模糊。
- **rubric 细化**:把"好不好"拆成可勾选项;给边界案例的判定示例(few-shot 校准)。
- **偏差治理**:交换位置、多裁判集成、换别家强模型;对裁判建"校准集"(人工标好的边界案例)定期回归,一致性掉线就报警。
- **兜底**:某个维度怎么调都不行的(比如需要业务上下文才能判的),承认裁判不适合,回退人工或半自动。
- **及格线 / 加分 / 红旗**:及格=先分析分歧样本+rubric 细化;加分=校准集机制;红旗=直接换更大的裁判模型了事(成本翻倍,偏差可能原样)。
