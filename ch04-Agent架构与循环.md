# ch04 · Agent 架构与循环

> 覆盖:agent loop 的工程实现与停止条件、权限系统、hooks、plan mode、system prompt 的结构化拼装、长任务的会话接力、worktree 隔离、checkpoint/rewind、headless 自动化。
> 案例基线:2026-09。机制题不挂时间;带日期案例以标注为准。
> 答案读法:**加粗句连读就是可背的骨架**;正文按工程师语域写——通用工程词(白名单、幂等、熔断、事务)直接用,**领域黑话(fail-closed、stop gate、worktree 这类)随用随释**,只在括号里给一句解释;**宁可写长讲透,不压缩到失真**。
> 术语白话速查:**turn(轮)**=一次"模型思考+调工具+拿结果";**停止条件**=agent 什么时候算干完/该停;**max_turns**=最大轮数上限;**hooks**=在指定时机自动触发的检查脚本(如每次改文件后跑 lint);**权限 allowlist**=白名单式权限:只允许清单上的操作;**plan mode**=先出计划人批准再动手的模式;**worktree**=同一仓库的独立工作副本,多任务互不打架;**checkpoint(检查点)**=存档,可回滚;**熔断**=异常时自动暂停防雪崩;**fail-closed**=出错时默认拒绝而不是放行。

---

## 4.1 八股题(基础概念)

**Q4.1【八股·L2·平台】写出 Claude Code 风格 agent 主循环的伪代码,标出所有终止条件。**

- **人话版**:一个 while 循环——发请求、看模型要不要调工具,要就执行并把结果追加进历史,不要就返回最终文本。终止条件四类:模型不再调工具(自然完成)、轮次/预算上限(防失控)、外部闸门(hook 拦截)、上下文满了触发压缩续命。
- **主线(照这条线讲)**:
  ```
  while true:
      resp = API(system_prompt, tools, history)
      if resp.stop_reason == "end_turn":          # 终止1:自然完成
          return resp.text
      for call in resp.tool_calls:                 # stop_reason == "tool_use"
          (可选)权限检查 / PreToolUse hook        # 终止2:hook exit 2 阻断
          result = execute(call)                   # 终止3:max_turns / 预算熔断
          history.append(tool_result)
      if 接近上下文上限:
          compact(history)                         # 不是终止,是续命
  ```
  1. **代码骨架要能默写**——循环、两个 stop_reason 分支、权限检查的位置、压缩的位置;这四样是所有 agent runtime 的公约数。
  2. **每类终止条件防什么要配对讲**——自然停止防不住"模型自认为完成"(要靠 Stop hook 复查);max_turns/预算防失控烧钱(只计工具轮);权限 hook 防危险动作;第四类退化检测(重复调用检测)是兜底——**四层闸门是分层的,不是平列的**。
  3. **官方三阶段描述纠正一个常见误解**——gather context→take action→verify results:循环不是"调工具直到停",是"收集→行动→验证"的反复;自研循环最常见的缺陷就是漏掉 verify 段(漏掉的后果见 ch07 自我验证不可靠)。
- **补充弹药(追问再掏)**:
  - **多轮思考的插入点**:interleaved thinking 让思考块出现在每次工具调用之间——循环结构不变,历史里多了 thinking 块。
  - **产品对照**:主流 agent(Claude Code/Codex)的主干都是这个循环+十几个好工具——框架不是必需品(ch01 Q1.7)。
- **及格线**:循环结构+至少三类终止条件。
- **加分**:终止条件的分层逻辑;主动提 verify 段。
- **红旗**:伪代码里没有权限和轮次上限;以为循环写完 agent 就完了。

**Q4.2【八股·L2·平台】Claude Code 的权限模式有哪几种?规则评估顺序?"fail-closed"体现在哪?**

- **人话版**:从"每次都问人"到"全自动分类器审批"的一串档位——default/acceptEdits/plan(只读)/auto(分类器审危险操作)/bypass(仅隔离环境)。规则顺序 deny→ask→allow,任何一层的 deny 无法被覆盖;没匹配上就问人/拒绝——默认不放心。
- **主线(照这条线讲)**:
  1. **模式光谱的本质是信任范围**——default 人审一切(最安全最烦);acceptEdits 放行编辑但 Bash 仍问(信任"改文件"但不信任"跑命令"——文件有 git 兜底,命令的副作用没有);plan 只读(探索安全);auto 用独立分类器模型逐动作评估风险,按"这个具体动作危险不危险"决策而不是按工具名一刀切(2026 年起成默认,官方口径拦截约 89% 人类会漏判的危险操作);bypass 只该出现在一次性隔离容器(用完即弃,不该有生产凭证)。
  2. **fail-closed 的含义讲透**——未匹配规则的默认行为是"拦下问人"而不是"放行":配置错误的后果是多问几次,不是静默放行。**安全设计的黄金原则:失败时落到安全侧**——这四个字比背五个模式重要;反例是 fail-open 设计,一条配置写错就是一个漏洞。
  3. **层级覆盖的组织价值**——deny 绝对优先(managed 企业策略>项目>用户):企业可以下发"永远禁止 rm -rf"而个人配置无法绕过——多人组织的刚需;反过来,个人的 allow 永远不能解锁企业的 deny,**信任的单向流动是组织治理的地基**。
- **补充弹药(追问再掏)**:
  - **auto 模式的争议**:误放行的尾部风险不可审计;2026-08 它成为默认引发过权限模型争论(展开见 Q4.14 品味题)。
  - **配置卫生**:高频弹窗=该分层没分——Q4.22 给了完整配置方案。
- **及格线**:模式列举+deny 优先+fail-closed。
- **加分**:auto 分类器机制及争议;managed 层的组织价值。
- **红旗**:把 bypass 当效率工具日常用;以为 allow 能覆盖 deny。

**Q4.3【八股·L2·平台】hooks 的核心事件有哪些?退出码语义?三个典型用法?**

- **人话版**:hook 是插进 agent 循环的自定义 shell 命令,按事件触发——工具前后、用户输入时、会话结束(Stop)、压缩前。退出码 0 正常、2 阻断并把 stderr 喂给模型、其他非零只是警告。
- **主线(照这条线讲)**:
  1. **事件清单按用途记**——PreToolUse/PostToolUse(工具前后)、UserPromptSubmit(输入时注入上下文)、Stop(agent 要结束时)、SubagentStop、PreCompact、SessionStart;matcher 支持精确名和正则(`Bash(git push:*)`)。
  2. **退出码设计的精髓在 2**——stderr 回喂模型:模型知道为什么被拦、能调整再试。错误信息即纠错信号(和 ch03 工具设计的原则同源);高级 JSON 输出还能改写工具输入、注入上下文、返回 allow/deny/ask。
  3. **三个典型用法对应三种约束**——①PostToolUse(Edit|Write)→自动 format/lint:"必须发生的"交给代码;②PreToolUse 拦危险命令(allow 全部 Bash 再用 hook 拦 rm -rf、git push --force——hook 能看参数做逻辑判断,比静态 allow 规则灵活);③Stop→全量测试通过才许结束(防"自认为完成",连续拦截有限次后放行防死锁)。
  4. **安全注意收尾**——hook 本身是 shell 执行:不可信仓库的 settings 里可能藏恶意 hook(真实攻击面,ch08);只信任入库审查过的配置。
- **补充弹药(追问再掏)**:
  - **死锁设计**:Stop hook 连续拦截约 8 次后强制放行——gate 和 agent 互搏必须有出口,这个细节体现工程成熟度。
  - **三分类口诀**:必须发生的给 hooks、不许发生的给权限、希望发生的给 prompt(Q4.12 品味题展开)。
- **及格线**:三类事件+退出码 2+一个用法。
- **加分**:三用法覆盖"必须/不许/不许假完成"三分类;hook 是攻击面。
- **红旗**:用 hook 逐条命令做 allow(该用权限系统)。

**Q4.4【八股·L1·用户】plan mode(计划模式)解决什么问题?和直接执行的差别?**

- **人话版**:让 agent 先只读探索、产出实施计划,人批准后再动手。解决两件事:探索噪声不污染执行上下文(批准后从干净上下文+计划开始);人在方向错之前就能打断——改一行计划比改一百行代码便宜。
- **主线(照这条线讲)**:
  1. **上下文视角**——探索阶段可能读几十个文件,这些过程留在上下文就是噪声(还持续稀释注意力,ch02 的 rot 账);plan mode 探索完出计划、执行阶段从计划重开——**内置的上下文隔离设计**(和 subagent 隔离同族思路:贵的探索过程在一次性上下文里烧掉,只留结论)。
  2. **风险视角**——长任务里方向错误的每一步都是负资产,越晚发现返工越贵;plan 是最便宜的纠偏点。纠偏成本随任务推进超线性增长(第一分钟改一句话,第一百分钟改一百行代码加它们的测试),这是"早打断"的数学依据——和 Q4.15 的干预阈值是同一原理的两处应用。
  3. **何时不该用**——官方判据很朴素:"能用一句话描述 diff 的改动就跳过计划"。计划本身有成本(一轮探索+人审延迟),小改动上 plan mode 是仪式主义——**审批流的价值密度要和任务风险成正比**。
  4. **与 spec 的关系**——plan 本质是轻量 spec 入口:人批准的计划就是任务级 spec;多天任务从 plan 升级到正式 spec 文件(ch10)。
- **补充弹药(追问再掏)**:
  - **与 subagent 的配合**:探索量大时让 Explore subagent 做只读侦查,主 agent 只拿结论再出计划——两层隔离叠加。
  - **行为学观察**:模型被训练过"批准后严格执行计划"——计划质量直接决定执行质量,所以 plan 阶段值得人认真看。
- **及格线**:两个解决的问题(噪声隔离/提前纠偏)。
- **加分**:上下文隔离视角;跳过判据。
- **红旗**:所有任务无脑 plan(不识成本);认为 plan 是给新手的。

**Q4.5【八股·L2·平台】生产级 agent 的 system prompt 是"一段话"吗?模块化拼装的结构启示?**

- **人话版**:不是。对泄露的 Claude Code system prompt 的分析显示是数百个字符串片段按环境/配置动态拼出来的——tone 指令、任务管理、安全边界、各工具使用说明各是独立片段。启示:system prompt 是配置管理系统,不是作文。
- **主线(照这条线讲)**:
  1. **为什么必须模块化**——按环境开关(沙箱内外指令不同:沙箱内可以放宽命令限制,沙箱外收紧)、按配置拼装(挂了 MCP 才有 MCP 段,开了 plan mode 才有 plan 段)、可独立测试(每个片段是一个可回归的行为约束——改这段→哪类行为变,有对应用例);**拼接顺序固定**同时是缓存纪律的要求(system prompt 必须逐字稳定,拼装的确定性是行为确定性和经济性的双重前提,ch02 Q2.3)。
  2. **内容按类型学组织**——tone(简洁、file:line 引用格式)、任务管理(强制 todo 跟踪)、安全(外部内容不可信边界)、工具流程(某工具怎么用好)。按类型组织而不是想到哪写到哪——这直接可抄;类型化之后才能做"哪类约束放哪层"的治理(prompt/hook/权限的分工,Q4.12)。
  3. **多数团队的反面现状**——一篇不断追加的"巨型作文":改一处怕动全身,没人敢删,五年后一万 token 的说明书里九成是化石。模块化+定期清理(每段标注作用与触发条件)是可持续做法——**prompt 的维护成本结构应该和代码一致:可diff、可回滚、可追责**。
- **补充弹药(追问再掏)**:
  - **量级**:泄露分析口径是数百条片段、从十几个 token 到上万 token 的段落都有——生产 prompt 的复杂度远超直觉。
  - **测试思路**:每个模块对应一个行为测试用例(改这段→哪类行为变),prompt 变更才敢走 CI。
- **及格线**:拼装而非一段话+两个模块化理由。
- **加分**:从缓存稳定性讲拼接顺序;内容类型学。
- **红旗**:把 system prompt 当越写越长的说明书。

**Q4.6【八股·L2·用户】headless/自动化模式怎么用?典型场景和坑?**

- **人话版**:`claude -p "任务" --output-format stream-json` 一次性调用,输入输出全结构化,进 CI 和脚本。最大的坑是拿交互环境的假设用 CI:审批没人点(挂起)、轮次没上限(烧钱)。
- **主线(照这条线讲)**:
  1. **关键参数与它们堵的坑**——`--permission-mode`+白名单(`--allowedTools "Edit,Bash(git commit *)"`)堵审批挂起:**CI 里权限问题的答案必须确定**(白名单放行或 dontAsk 拒绝都是确定,等待人工就是挂起——CI 没有"人"这个组件);`--max-turns` 堵无限循环(没人看着的循环必须有限);stream-json 堵"脚本没法消费输出"(结构化输出才能接后续自动化)。
  2. **三个典型场景**——批量迁移的 for 循环扇出(N 个子任务并行开 PR);GitHub Actions 里 @claude 处理 issue(机器人的标准形态);全库批量修 lint(确定性收益、风险低,自动化首选场景)。
  3. **CI 特有的配套**——结构化结果(exit code 语义+JSON 报告,下游按结果路由);预算熔断(单任务和整批两层,防一个失控任务烧掉整个 pipeline 的配额);**部分完成的 diff 保留供人接手**(超限≠白干,半成品也有价值——分支留着,人看完决定续跑还是丢弃)。
- **补充弹药(追问再掏)**:
  - **任务粒度经验**:CI 里单任务目标一个 PR;粒度太大是 max_turns 超限的头号原因(和工具反馈差并列)。
  - **对照交互模式**:同一个任务在 CI 失败率高,先查权限差异和工具反馈差异,再怀疑模型——环境差异先于模型差异。
- **及格线**:结构化输入输出+审批确定化。
- **加分**:部分成果保留设计;批量扇出实践。
- **红旗**:CI 用 default 权限(等到天荒地老);不设轮次和预算上限。

---

## 4.2 原理深挖

**Q4.7【原理·L3·平台】设计 agent 的停止条件体系:四层闸门分别防什么?**

- **人话版**:①自然停止(模型说完了)——主路径,防不住自认为完成;②硬上限(max_turns/预算)——防失控烧钱;③确定性闸门(Stop hook/独立验证)——防假完成;④退化检测(重复动作/thrashing)——防僵尸循环。层层递进,缺一层多一类事故。
- **主线(照这条线讲)**:
  1. **第③层是质量核心展开讲**——模型"我认为完成了"和"测试全绿"是两回事(自我验证不可靠的实证见 ch07:模型自检的通过率和实际正确率差距巨大)。Stop hook 把验收从模型的自我判断换成确定性检查;**拦截要有上限**(连续拦约 8 次后放行并升级人工——防 agent 和 gate 死锁互搏:agent 说完成、gate 说不行、agent 原样再交、gate 再拦……没有出口就是死循环,这个细节是设计成熟度的标志)。
  2. **第④层的具体信号**——重复签名(同工具同参数的滑窗重复率)、同一文件 N 轮内反复改、错误信息相似度不降——检测到就注入"你已尝试 X 次相同方案"的提醒或强制终止。**三条信号都是廉价可算的**:不需要理解语义,hash 和计数就够——检测层故意做在比模型低的层级上。
  3. **经验法则给数字**——同一问题连续 2 次修正失败就该中断换思路:继续堆错误上下文是在给模型喂误导,错误上下文的边际成本是负的(每轮失败尝试都在教它错误的方向)。这条同时是效率建议和质量建议。
  4. **收尾点出分层的意义**——自然停止是常态、硬上限是保险、闸门是质量关、退化检测是兜底;四层不是四个开关,是四个"谁先拦住"的优先级序列——**设计停止条件时按"最坏情况穿了几层"来审计**:一个任务烧穿四层才停,每层为什么没拦住都要有答案。
- **补充弹药(追问再掏)**:
  - **预算的计量单位**:轮数(便宜粗)vs token(准)vs 金额(管理层语言)——三层都要有,汇报用金额,调试用轮数。
  - **与 ch12 大题的连接**:无人值守批量任务里 Stop-gate 变成"早晨人审"的批量闸门——ch12 Q12.2 的 Agent Fleet 是它的规模化形态。
- **及格线**:四层+各自防什么。
- **加分**:拦截上限的防死锁设计;重复检测的具体信号。
- **红旗**:只有 max_turns 一层;把"模型说完成"当完成。

**Q4.8【原理·L4·平台】长任务跨多个会话/上下文窗口,怎么设计"接力"?讲"轮班工程师"模式的工程方案。**

- **人话版**:把长任务当轮班工程师——每个会话像接班的人,没有上一班的记忆,所以要留交接文档。方案:初始化会话建环境和任务清单,后续每个会话开机先读 git log 和进度文件、领一个任务、做完 commit——**git commit 就是 checkpoint**。
- **主线(照这条线讲)**:
  1. **开机序列(固定动作)是方案的核心**——读 git log(代码状态)→读进度文件(任务状态)→选最高优先级未完成项→先跑端到端测试确认基线→开工。每个会话只做一个 feature:上下文永远新鲜,不依赖跨会话记忆。
  2. **两个反直觉细节最出彩**——任务清单用 JSON 不用 Markdown:Markdown 太"好改",agent 会顺手重写美化,破坏状态的可审计性(展开见 ch06);用浏览器(Playwright)截图像人类用户一样验证前端:测试不只是断言,是真实体验。
  3. **本质一句话**——把"记忆"从上下文迁移到 repo:进度文件+git 历史+测试构成跨会话的持久状态,每个会话**从状态重建上下文**而不是恢复对话。这是"会话易失、仓库持久"这一事实的直接推论。
  4. **配套**——每会话结束前更新 handoff(阻塞点/下一步/给下一班的忠告);人工检查点设在里程碑而不是每一步。
- **补充弹药(追问再掏)**:
  - **实证参照**:公开的多天级实验(编译器、长程任务)都收敛到这个模式;Claude Plays Pokémon 这类数十小时任务靠进度笔记维持一致性——模式跨场景验证过。
  - **与 resume 的对比**:resume 恢复"对话文本"不恢复"工作状态",历经压缩的长会话 resume 回来的是摘要不是记忆(ch06 Q6.7)。
- **及格线**:开机序列+commit 即 checkpoint。
- **加分**:JSON 清单的理由;"从状态重建 vs 恢复会话"的哲学差异。
- **红旗**:指望一个会话 resume 到天荒地老。

**Q4.9【原理·L3·架构】worktree 隔离解决什么问题?实现要点?**

- **人话版**:多个 agent 任务共用一个工作目录会互相污染——半成品文件、dev server 端口冲突、git 状态打架。git worktree 给每个任务一棵独立工作树+分支,互不干扰,合并走正常 git 流程。
- **主线(照这条线讲)**:
  1. **三类冲突先摆清**——文件写冲突(两个 agent 改同一文件后写覆盖:后写的赢,前一个的工作静默蒸发)、环境冲突(dev server 端口/构建产物/锁文件:两个任务抢 node_modules,install 互相踩)、git 状态污染(一个 agent 的 WIP 提交挡住另一个的 checkout/rebase)。共享工作区是并行 agent 的头号杀手,不是"小心点"能解决的——**冲突不是概率问题是结构问题**。
  2. **实现要点(以 Claude Code --worktree 为例)**——在 .claude/worktrees/ 下建工作树;`.worktreeinclude` 声明要复制的未跟踪文件(.env 这类不在 git 里但运行必需的);运行期持有 worktree lock 防误删;**隔离会话有检查阻止逃逸回主目录**(git -C、GIT_DIR 绕路都被拦——隔离的价值取决于边界硬不硬,软边界的隔离是装饰);subagent 可声明独立 worktree。
  3. **配套纪律**——一任务一树一分支;合并按依赖顺序串行(ch05:并行解决"做",串行解决"合");失败任务的树保留现场 N 天再清(排错需要现场)。
- **补充弹药(追问再掏)**:
  - **常见追问**:branch 不够吗?——branch 解决版本隔离不解决工作目录共享;worktree 两者都解。worktree=branch 的隔离+目录的隔离。
  - **生态**:git-worktree-runner、claude-squad 这类多 agent 工具都以它为地基——模式已被行业验证。
- **及格线**:三类冲突+一任务一树。
- **加分**:逃逸检查的实现意识;.env 复制细节。
- **红旗**:以为 git branch 就够;让两个 agent 在同一目录"小心点"。

**Q4.10【原理·L3·平台】checkpoint/rewind 机制和 git 的关系?为什么"agent 的回滚"和传统事务回滚是两回事?**

- **人话版**:Claude Code 每次编辑前自动快照,双击 Esc 回滚代码/对话/环境三态。但它只覆盖编辑类工具——bash 的外部副作用(装的依赖、发的请求)不在快照里。回滚粒度是"agent 的动作",不是"世界的状态"。
- **主线(照这条线讲)**:
  1. **三态回滚比 git 多两层**——代码、对话、环境分别可回退;官方明确"不是 git 替代",文件历史本地保留约 30 天。对话回滚的价值:退回"走错的那个决策点"重来,不只是退文件——**错误经常发生在决策而不是编辑**。
  2. **本质差异是重点**——传统事务有 begin/rollback 边界,副作用全在事务内,回滚即全撤销;agent 的副作用跨越数字边界:外部 API 调用、发出去的邮件、写过的生产库、通知过的用户——**这些没有快照可回**。事务的世界里回滚是完备的;agent 的世界里回滚只覆盖"数字编辑"这一层,世界的其余部分只进不退。
  3. **设计推论(这题的落点)**——agent 安全设计的重心不是"能回滚",是**限制不可逆动作的发生**:权限分级、审批、结构性禁止。可逆性要成为任务设计的一等信息:能 undo 的交给 agent 自动做,不能 undo 的结构性隔离(贯穿 ch08)。
- **补充弹药(追问再掏)**:
  - **编排层的对照**:批处理框架有真正的分布式事务;agent 编排没有——每步都可能部分成功,所以"步骤幂等+补偿动作"是 agent 流程设计的默认假设。
  - **测试价值**:checkpoint 让"试错"变便宜——敢让 agent 尝试有风险的修改再回滚,这是工作流的隐性收益。
- **及格线**:编辑类覆盖/bash 副作用不在内。
- **加分**:三态概念;从"回滚有限"推出"前置限制不可逆"的设计转向。
- **红旗**:把 checkpoint 当完备回滚方案放心让 agent 干任何事。

**Q4.11【原理·L4·架构】"权限/审批"(guardrails)和"验证闭环"(verification)是什么关系?只有一边的 agent 会怎样?**

- **人话版**:guardrails 管"不能做什么"(权限、审批、deny),verification 管"做的是不是对的"(测试、验收、stop gate)。只有 guardrails:安全地交付垃圾;只有 verification:高质量地闯祸。
- **主线(照这条线讲)**:
  1. **两句话的反例先立住**——这两句是本题的记忆锚点;面试官要的就是这个区分度。
  2. **两类系统回答不同问题**——guardrails 是事前事中的约束(动作层),verification 是事后的判定(结果层);工程上常被混为一谈:"配了权限所以质量有保障"不成立——权限拦不住"错误但合规"的输出(每个动作都在白名单里,连起来的结果照样是错的)。
  3. **常见失衡是加分点**——企业普遍 guardrails 过剩(层层审批)而 verification 缺位(没有可执行验收):agent 干得慢还干不对,人审负担反而上升。正确的投资方向是补 verification(可执行验收、stop gate),guardrails 按风险分级收敛到真正的不可逆动作。
  4. **四项检查清单收尾**——agent 在哪跑(隔离环境)?能看到正在发生什么吗(可观测)?怎么证明做对了(验证)?失败后系统会不会变好(失败升级为规则)?——评估任何 agent 系统(自研或采购)用这四问过一遍。
- **补充弹药(追问再掏)**:
  - **第四问的归属**:失败升级为规则(failure taxonomy)是 ch10 反脆弱的核心——四问里它最常被漏,漏了系统就重复踩坑。
  - **采购场景用法**:拿四问当 vendor 问卷,答不全第三、四问的产品再强也慎用。
- **及格线**:两个"会怎样"的反例。
- **加分**:四项清单;"过剩/缺位"的失衡诊断。
- **红旗**:认为配好权限就万事大吉;或"测试全绿不需要权限"。

---
## 4.3 品味与判断题

**Q4.12【品味·L3·平台】"必须发生的事交给 hooks,不许发生的事交给权限,希望发生的事交给 prompt"——critique 这个分工。**

参考思路:
- **先肯定合理内核**——确定性递增:hook 是代码(确定执行)、权限是策略(确定拦截)、prompt 是软引导(尽力而为);硬约束越靠确定性一侧越可靠。这个分工把"约束的强度"和"实现的机制"对上了,比"全写进 prompt"是一大进步。
- **再打三个洞**——①hooks 本身是攻击面:不可信配置里的 hook 就是恶意代码,"确定性"成了确定执行恶意——第三方的 hooks.json 和第三方的可执行文件没有本质区别(ch08);②权限管工具层管不了语义层:allow 了 Bash 拦不住脚本里的恶意内容——工具调用粒度和语义粒度之间有一道权限够不着的缝;③prompt 层的责任被低估:信任边界声明(外部内容不可信)是注入防御第一道,只能在 prompt 层做——它不是"软"就是"不重要"。
- **补上漏掉的第四格**——验证(Stop gate/verifier)是"必须不发生假完成"的确定性:前三格管动作,第四格管结果——这格最常被忘,忘掉的系统的终态是"每个动作都合规,交付的东西是错的"。
- **及格线 / 加分 / 红旗**:及格=内核+至少一个漏洞;加分=第四格;红旗=当口诀背不批判。

**Q4.13【品味·L4·架构】agent 系统"胖 loop 瘦工具"还是"瘦 loop 胖工具"?**

参考思路:
- **胖 loop**——编排逻辑在 harness(状态机/重试/路由/分支):可控可审计(每步转移看得见),但僵硬,且把智能从模型挪回代码——模型每升级一代,编排里那些"教模型怎么干活"的规则就过时一遍,重写成本反复发生。
- **瘦 loop**——while+少量正交工具,智能在模型:灵活、随模型能力水涨船高(模型变强你自动受益),但可预测性差,失败模式难枚举。
- **行业收敛的答案**——瘦 loop+好工具+关键节点的确定性闸门(权限/验证/停止);"把控制流写死"的重框架在 coding 场景全面输给裸循环(头部产品的形态就是证据);但错误成本高的节点(发布/资金/生产变更)保留确定性代码——**闸门设在节点上而不是路径上**:路径留给模型,节点握在手里。
- **落到判断函数**——错误成本越高,越多逻辑下沉到确定性代码;探索性越强,越多留给模型。**这不是立场是参数**——给参数的答案才是好答案,站队的答案是审美。
- **及格线 / 加分 / 红旗**:及格=两边代价+收敛方向;加分=判断函数+"闸门在节点不在路径";红旗=绝对站队。

**Q4.14【品味·L4·管理】某头部 coding agent 在 2026-08 把"分类器自动审批"设为默认权限模式(此前需显式开启),引发权限模型争议。你敢在生产开自动审批吗?**

参考思路:
- **支持的论据**——官方评测:自动模式拦截约 89% 人类会漏判的危险操作;现实:人对高频审批疲劳性全点允许——"人工兜底"经常是幻觉,**人是最弱的闸门**(这个反直觉论点是加分项:审批流的安全性假设是"人会认真看",高频场景下这个假设先崩)。
- **反对的论据**——分类器误放行的尾部风险不可审计(为什么放行没有可解释记录——出了事没法复盘"它当时为什么放过去");默认值的力量:多数用户不会改默认,**厂商用默认替用户做了风险决策**——授权链条从"我决定"变成"我没反对",治理含义完全不同;敏感环境(生产凭证/付费操作)误放行不可逆。
- **决策框架**——按动作分级:读操作自动;写工作区自动+沙箱;外发/付费/生产变更保留人工硬闸;分级清单 version control(分级本身是配置,变更走 review)。再区分"默认开"和"我显式开":前者是厂商替你担责(其实是替你卸责),后者是你自己的决策——敢开的前提是你有自己那份分级清单,而不是接受厂商的默认。
- **及格线 / 加分 / 红旗**:及格=正反两面+分级;加分="人是最弱闸门"或"默认值的治理含义";红旗=全盘接受或拒绝。

**Q4.15【品味·L3·用户】你什么时候打断正在运行的 agent?给干预阈值。**

参考思路:
- **立即打断**——方向性错误(读了三个文件就在错模块动手):方向错误的每一步都是负资产,纠偏成本随推进超线性涨;危险动作审批永远人工(这是权限层的事,不属于"打断"范畴,但值得点一句边界)。
- **不拦**——小错误在验证环里自愈(测试红了它自己修):打断等于抢走学习机会还搞乱上下文——你插入的那条消息本身也在扰动它的轨迹;**能被验证环消化的错误都不值得人工**。
- **该打断但多数人不打的两类**——连续 2 次修同一问题失败(该换思路或换会话——Q4.7 的经验法则在人工侧的对应);"无限探索"苗头(读几十上百文件还没动手)——失控前兆,早期打断成本最低。
- **底层原则**——干预粒度要匹配验证环长度:秒级反馈环不用管,分钟级以上的环节才值得盯——**人的注意力是稀缺资源,花在反馈环覆盖不到的地方**。
- **打断本身有设计问题**——你插入的消息也在扰动轨迹:agent 要重新对齐你的意图和它已有的计划,措辞含糊的打断(“你确定这样对吗?”)比不打还糟——模型可能过度反应,推翻本来正确的方向;**打断要给信息量(哪里错了、以什么为准),不给情绪量**。
- **两个工程化替代**——高频出现的打断模式应该固化成机制而不是靠人盯:方向性检查做成里程碑 gate(agent 每完成一个阶段主动停下等确认,把“打断时机”从人的临场判断变成流程约定);连续失败做成自动熔断(同一错误第二次即暂停并通知人,而不是让人刷日志发现)——**机制接管可预模式的干预,人才腾得出手管真异常**。
- **及格线 / 加分 / 红旗**:及格=三类阈值;加分=纠偏成本超线性或验证环匹配;红旗=全程盯(浪费)或完全不管(赌博)。

---

## 4.4 开放性问题

**Q4.16【开放·L5·架构】agent loop 会演化成什么?"loop 消失"(模型原生多步执行)意味着什么?**

讨论框架:
- **正在发生的消解**——interleaved thinking 把思考嵌进循环(思考成为历史的一部分而不是循环外的一次调用);agentic RL 让多轮策略进权重(模型"天生会"工具调用,不需要 harness 教);厂商开始把循环行为和模型协同设计(ch09 DeepSeek 专节:模型×harness 联合优化,同一个模型换 harness 差 33 分)。
- **loop 的工程职责去哪**——权限、验证、恢复这些确定性职责:要么下沉为平台原语(服务商内建,如 configuration_update 这类协议化机制),要么留在 harness;**谁拥有循环决定可观测与可控制的边界**——厂商拥有,你看到的粒度由厂商决定;自己拥有,看得到全部但要自己维护。
- **可观测性挑战**——不轮询的连续执行流怎么审计"第 37 步为什么这么做":trace 粒度从"每轮"变"每决策点";合规场景(金融/医疗)对"决策可解释"的硬要求,可能反过来把 loop 留在应用层。
- **判断收束**——loop 作为代码结构会淡化,作为责任边界(出事谁兜)不会消失——**结构会消失,职责不会**;面试里能给出这层区分,说明想过"我们到底在 loop 里管什么"。
- **及格线 / 加分 / 红旗**:及格=消解的三条趋势;加分="结构消失职责留下"的收束+合规反推;红旗=以为 loop 消失=agent 工程师失业。

**Q4.17【开放·L4·架构】"把 agent 当操作系统进程设计"(隔离/资源限额/退出码/审计)——类比哪里成立、哪里失效?**

讨论框架:
- **成立**——隔离(worktree/容器≈namespace)、资源限制(token/时长/预算≈cgroup)、退出码(headless 的 exit code 语义)、日志(trace≈journal):半个世纪的运维体系直接借用——**成熟的运维直觉在 agent 世界大半是对的**,这是类比的真正价值(不是精确对应,是经验迁移)。
- **失效**——执行是概率性的:重放不可精确复现(同一输入两次运行路径不同,进程世界的 core dump 思路失效);fork 昂贵(上下文复制=状态深拷贝,没有 copy-on-write——分支一个 agent 的成本是 O(上下文长度));副作用跨越数字边界(外部 API 无事务,进程至少只管本机);"信号"无可靠等价物(不能 SIGTERM 一个正在推理的模型——中断的粒度对不上推理的粒度)。
- **推论**——隔离与审计可以放心借鉴进程模型(这两块失效最少);**验证模型必须重构**——进程世界失败是异常(出了 bug),agent 世界失败是分布的一部分(正常波动),用"异常处理"的思路设计 agent 的失败应对,会把正常波动当事故处理。
- **及格线 / 加分 / 红旗**:及格=成立/失效各两条;加分="失败是分布不是异常"的重构;红旗=类比全套照搬或全盘否定。

---

## 4.5 工程实战题

**Q4.18【实战·L3·平台】从零实现一个最小 coding agent(约 300 行),列组件清单与关键决策。**

参考思路:
- **组件六件**——LLM client(消息 API+tools 定义+usage 统计);工具集 Read/Write/Edit/Glob/Grep/Bash(**每个都带输出截断**);权限层(默认 ask+白名单);主循环(含 max_turns/预算熔断);compaction 触发器(token 阈值,先清旧工具输出);transcript 落盘(JSONL 可重放)。
- **四个关键决策**——工具输出上限(不做截断的第一个长任务就爆上下文——这是 ch02 的教训在 MVP 里的最小体现);tools 定义冻结(缓存:改定义是缓存上最贵的操作);错误回喂格式(语义化非 traceback——模型要的是"下一步怎么办"不是调用栈);终止条件分层(不止 max_turns——至少留一个 verify 挂点)。
- **加分件**——hook 点位预留(PreToolUse/Stop 两个就够,后期长出治理能力)、subagent 抽象(高噪声任务隔离)、stream-json 输出(接自动化的口子)。
- **隐藏考点**——好的回答会主动说"这个 MVP 缺 stop gate 和失败恢复,生产还要加":知道"300 行能跑"和"生产可用"之间差的是验证层与恢复机制,这才是架构意识——**MVP 的价值是把"能跑"和"能用"之间的差价显性化**。
- **及格线 / 加分 / 红旗**:及格=六组件+四决策;加分=主动指出 MVP 与生产的差距;红旗=组件清单没有权限和预算。

**Q4.19【实战·L3·平台】设计 Stop-gate:任务"必须通过全部测试+lint+无未说明的文件变更"才算完成。**

参考思路:
- **机制**——Stop hook 拉起验证脚本;失败 exit 2+失败摘要回喂(模型需要失败详情,不是"挂了"两个字——喂摘要而不是结论,它才有修复的原料);连续拦截上限(如 3 次后不再硬拦,输出 blocker 报告升级人工——防死锁:gate 和 agent 互搏必须有出口,Q4.7 的纪律)。
- **防作弊(可信前提)**——验证脚本与测试文件不在 agent 可写路径:gate 的可信度建立在 agent 改不了它之上——**裁判不能拿被考核者的工资**;git status 检查未说明变更(任务说改两个文件,diff 里出现第三个,要么是 bug 要么是越权,都该拦);测试只读(agent 不能"修测试让它变绿"——最经典的作弊路径,直接物理封死)。
- **验证产物注入**——测试报告作为 additionalContext 给模型:失败用例名+断言 diff+相关日志段,按"模型修复需要什么信息"裁剪——和工具返回设计是同一门手艺(ch03)。
- **失败兜底**——超限放行后,blocker 报告进任务记录,人工审查时看"它三次都没过什么"——拦截上限不是放弃验收,是把验收转交更强的裁判(人)。
- **及格线 / 加分 / 红旗**:及格=hook+exit 2+上限;加分=防篡改设计+报告转人工的闭环;红旗=gate 脚本放在 agent 可写路径。

**Q4.20【实战·L4·架构】设计支持 20 个并行 agent 任务的执行环境层(不依赖商业产品)。**
> 场景白话:20 个 agent 同时干活,各自要有自己的代码目录(改动互不打架)、自己装的一套依赖库、自己的运行进程——这套"各干各的、互不污染"的底层设施就是"执行环境层"。本题要求不买商业产品,自己搭。

参考思路:
- **隔离**——每任务一个 worktree(或容器,按敏感度:代码任务 worktree 够,跑不可信依赖的任务上容器);统一任务队列+锁文件认领(防两个任务领同一件事——认领制是最简单的去重:抢到锁的干,抢不到的取下一件,**用文件锁把"谁干什么"从协调问题变成抢占问题**)。
- **资源**——共享只读缓存(依赖/构建产物:20 份重复下载既慢又贵——只读共享是并行安全的最简单形态,没有写就没有冲突);资源限额(CPU/时长/token/网络,每任务独立配额——一个任务失控烧的是自己的配额,不是整个池子)。
- **网络与回收**——默认禁网+白名单(按任务 profile:迁移任务给 registry 白名单,不需要公网);出站走代理可审计(所有外部交互一条日志链);完成即清理,失败现场保留 N 天(排错需要现场——回收策略区分成功和失败);人工介入通道(暂停/审批/接管——20 个任务里总有一两个需要人,通道要预先存在而不是临时 ssh 上去 kill)。
- **参照物**——公开的 16-agent 编译实验(锁文件认领、gcc 当 oracle——确定性验证器当裁判)和各家 worktree 方案:自己设计前先对齐这些已验证的模式,别重新发明。
- **及格线 / 加分 / 红旗**:及格=隔离+认领+限额;加分=共享缓存、失败留证与人工通道;红旗=20 个任务共用一个工作目录。

---

## 4.6 场景排错题

**Q4.21【排错·L3·平台】agent 在同一问题上循环 40 分钟,反复"读文件→改→跑测试→改回"。loop 级的检测与干预?**

排查链路:
- **检测信号三条**——重复签名(工具+参数 hash 的滑窗重复率)、同一测试文件 N 轮内被反复改、错误信息相似度不降——三条都是廉价可算的指标,先有检测才谈干预。40 分钟的循环不是突然发生的:重复率从第 5 分钟就在爬升,没有报警是因为没人看这个指标。
- **干预分级(先软后硬)**——注入提醒("你已尝试 X 次相同方案,请换思路"——最便宜,一轮就能见效);强制 compact(可能早期约束已丢,压缩重注入等于"提醒它最初的使命");Stop-gate 拦截要求输出 blocker 报告(结构化的失败陈述,升级的原料);升级人工。**从软到硬的成本递增,干预力度和证据强度要匹配**。
- **根治在 loop 外(本题的灵魂)**——这种现场通常说明任务缺验收标准(agent 不知道"好了"长什么样,只能在"改了→测→还红"里打转)或工具反馈误导(测试 flaky/错误信息不可读——它每次看到的"失败"都不一样,策略自然收敛不到解):**修 loop 的检测不如修验证器和任务定义**——检测是止损,根治在输入。
- **及格线 / 加分 / 红旗**:及格=检测信号+分级干预;加分=根因在验证器/任务定义的判断;红旗=调大 max_turns 了事(治标)。

**Q4.22【排错·L2·用户】agent 每次都在权限弹窗上等你,一天 200 次点击,但你又不敢全放行。怎么配置?**

排查链路(按风险分层放行,目标是"弹窗只剩真正需要人判断的动作"):
- **读类全放**——Read/Grep/Glob 无副作用,allow。这类弹窗是最纯的噪音:信息为零,只消耗注意力。
- **写类限路径**——Edit/Write 限工作区(gitignore 语法):工作区内的写有 git 兜底,天然可回滚——**可逆性是放行的最大依据**。
- **Bash 前缀白名单**——`Bash(git *)`、`Bash(pnpm test*)`;高危(git push/rm/curl 外网)交给 PreToolUse hook 精确拦截——**hook 能看参数做逻辑判断,比静态 allow 规则灵活**(allow 只能匹配前缀,hook 能解析"这条命令里有没有 rm -rf")。
- **"必须发生的"自动化**——format 之类 PostToolUse 自动跑,不走审批:它根本不该是审批对象——每次 Edit 后必跑 format,把它设计成弹窗就是配置错误。
- **度量收尾**——目标形态可量化:一天 200 次点击配到 10 次以内,且每次都是真决策(推送/外发/删除);**高频弹窗=配置债**,债的利息是你的注意力和对弹窗的疲劳性放行。
- **及格线 / 加分 / 红旗**:及格=分层思路;加分=hook 拦危险命令模式+"高频弹窗=配置债"+量化目标;红旗=烦了直接 bypassPermissions。

**Q4.23【排错·L4·平台】headless 模式在 CI 里随机挂:一半 max_turns 超限,一半等审批超时。诊断与修复?**

排查链路:
- **审批超时的根因**——CI 用了交互默认值:审批必须确定化(白名单+acceptEdits/auto;dontAsk 的"拒绝"也是确定答案——**确定地失败好过不确定地等待**,CI 的超时机制会把挂起翻译成失败,还不如自己先给出答案);检查挂起时刻的 pending 调用,看哪些工具没进白名单——挂起的工具名单就是白名单的补集,直接照着补。
- **max_turns 超限二分**——任务粒度太大(拆小,单任务目标一个 PR)vs 工具反馈差导致绕圈(看 trace 里重复调用的工具——修它的返回质量:错误信息不可读/分页边界误判,ch03 Q3.12 的清单);**trace 是分诊台**:重复调用集中在哪个工具,病灶就在哪。
- **"随机"的排错价值**——一半一半说明不是单一根因:按失败类别分桶统计,两类分别归因——"随机挂"经常是两三个确定性问题的叠加,分桶之后各自都不随机。
- **CI 特有配套**——结构化结果(exit code 语义+JSON 报告);预算熔断;部分完成的 diff 保留(超限≠白干——分支留着供人接手)。
- **及格线 / 加分 / 红旗**:及格=两类根因各自的修复;加分=从 trace 定位绕圈工具+失败分桶;红旗=max_turns 调大 10 倍"解决"。

---

## 4.7 开放加餐:偏好与品味追问

**Q4.24【开放·L2·用户】你给 agent 的权限配置:哪些操作全自动、哪些必须人审?这条线你是怎么划的?**

参考思路(一个好答案长什么样):
- **划线的三个判据**——可逆性(错了能不能 undo)、爆炸半径(错了影响多少东西)、可验证性(错了能不能被发现):读类全自动,工作区内的写自动,白名单外的命令、网络外发、删除、push 人审——**典型的个人配置能张口报出来**,报不出来说明从来没审过自己的授权面。
- **配置是踩坑踩出来的**——"我把 rm 和 git push 加进人审,是因为有一次它在清理脚本里把 build 目录连带源码删了":一条规则一个故事,和背默认值的人立刻区分开——**规则的来历比规则本身更能证明理解**。
- **知道自己的取向**——fail-open(默认放行,出事再收)还是 fail-closed(默认拦截,确认再放):两种取向适合不同场景(个人项目 vs 生产环境),说得出自己属于哪种、为什么——取向没有对错,不知道自己有取向才是问题。
- **及格线 / 加分 / 红旗**:及格=三判据+一条具体规则;加分=踩坑来历+fail 取向的自觉;红旗=全默认没动过,或全自动"我相信它"。
