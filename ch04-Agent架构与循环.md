# ch04 · Agent 架构与循环

> 覆盖:agent loop 的工程实现与停止条件、权限系统、hooks、plan mode、system prompt 的结构化拼装、长任务的会话接力、worktree 隔离、checkpoint/rewind、headless 自动化。
> 案例基线:2026-08。机制题不挂时间;带日期案例以标注为准。

---

## 4.1 八股题(基础概念)

**Q4.1【八股·L2·平台】写出 Claude Code 风格 agent 主循环的伪代码,标出所有终止条件。**

- **人话版**:一个 while 循环——发请求、看模型是不是要调工具,要就执行并把结果追加进历史,不要就把最终文本返回。终止条件四类:模型不再调工具(自然完成)、轮次/预算上限(防失控)、外部闸门(hook 拦截)、上下文满了触发压缩继续。
- **展开**:
  ```
  while true:
      resp = API(system_prompt, tools, history)
      if resp.stop_reason == "end_turn":          # 终止1:自然完成
          return resp.text
      for call in resp.tool_calls:                 # stop_reason == "tool_use"
          (可选)权限检查 / PreToolUse hook        # 终止2:hook exit 2 阻断
          result = execute(call)                   # 终止3:max_turns / 预算熔断
          history.append(tool_result)
      if 接近上下文上限:
          compact(history)                         # 不是终止,是续命
  ```
  - **每个终止条件防的事**:自然停止防不住"模型自认为完成"(要靠 Stop hook 复查);max_turns/预算防失控烧钱(只计工具轮,纯文本轮不计);权限 hook 防危险动作。四类闸门是分层的——自然停止是常态,硬上限是保险,确定性闸门是质量关,退化检测(重复调用检测)是兜底。
  - **官方的三阶段描述**:gather context → take action → verify results——循环不是"调工具直到停",是"收集→行动→验证"的反复,验证段经常被自研循环漏掉(漏掉的下场见 ch07 自我验证不可靠)。
- **及格线**:循环结构+至少三类终止条件。
- **加分**:每类终止条件"防什么"说得清;主动提 verify 段。
- **红旗**:伪代码里没有权限检查和轮次上限;认为循环写完 agent 就完了(验证与恢复是另一半)。

**Q4.2【八股·L2·平台】Claude Code 的权限模式有哪几种?规则评估的顺序是什么?"fail-closed"体现在哪?**

- **人话版**:从"每次都问人"到"全自动分类器审批"的一串档位——default(逐次批准)、acceptEdits、plan(只读)、auto(独立分类器审危险操作)、bypassPermissions(仅限隔离环境)。规则顺序是 deny→ask→allow,任何一层的 deny 都无法被别的层覆盖;没匹配上任何规则就问人/拒绝——默认不放心。
- **展开**:
  - **模式光谱的本质是"信任范围"**:default 人审一切(最安全最烦);acceptEdits 放行文件编辑但 Bash 仍问;plan 只读(探索安全);auto 用一个独立分类器模型评估每个动作的风险再决定放不放(2026 年起成为默认,官方口径拦截了约 89% 人类会漏判的危险操作);bypass 只该出现在一次性的隔离容器里。
  - **fail-closed 的含义**:权限系统的默认行为是"不确定就拦"——未匹配规则不是默认放行而是进入询问。安全设计的黄金原则:失败时落到安全侧。配置错误的后果是多问几次,不是静默放行。
  - **层级覆盖**:deny 的绝对优先(managed 企业策略 > 项目 > 用户)意味着企业可以下发"永远禁止 rm -rf"而个人配置无法绕过——这在多人组织里是刚需。
- **及格线**:模式列举+deny 优先+fail-closed 含义。
- **加分**:auto 模式的分类器机制及其争议(误放行的尾部风险,见 Q4.14);managed 层的组织价值。
- **红旗**:把 bypassPermissions 当效率工具在日常用;以为 allow 规则能覆盖 deny。

**Q4.3【八股·L2·平台】hooks 的核心事件有哪些?退出码语义是什么?三个典型用法?**

- **人话版**:hook 是你插进 agent 循环里的自定义 shell 命令,按事件触发——工具调用前(PreToolUse)、后(PostToolUse)、用户提交时、会话结束(Stop)、压缩前(PreCompact)等。退出码 0 正常、2 阻断并把 stderr 喂给模型、其他非零只是警告。
- **展开**:
  - **事件清单(常用版)**:PreToolUse/PostToolUse(工具前后)、UserPromptSubmit(用户输入时注入上下文)、Stop(agent 要结束时)、SubagentStop、PreCompact、SessionStart。matcher 支持精确名和正则(如 `Bash(git push:*)`)。
  - **退出码的设计**:0=成功(stdout 只进 transcript 不给模型);2=阻断且 stderr 回喂模型(模型知道为什么被拦、能调整);其他=非阻塞告警。还有高级 JSON 输出能改写工具输入、注入上下文、返回 allow/deny/ask 决策。
  - **三个典型用法**:①PostToolUse(Edit|Write)→自动 format/lint——"必须发生的"交给代码;②PreToolUse 拦危险命令(allow 全部 Bash 再用 hook 精确拦 rm -rf、git push --force——比逐条 allow 规则灵活);③Stop→全量测试通过才许结束(防"自认为完成",连续拦截有限次后放行防死锁)。
  - **安全注意**:hook 本身是 shell 执行——不可信仓库的 settings 里可能藏恶意 hook(这是真实攻击面,见 ch08);只信任入库审查过的配置。
- **及格线**:三类事件+退出码 2 的语义+一个用法。
- **加分**:三个用法覆盖"必须发生/不许发生/不许假完成"三分类;hook 是攻击面的意识。
- **红旗**:用 hook 做本该权限系统做的事(逐条命令 allow);不知道 stderr 回喂机制。

**Q4.4【八股·L1·用户】plan mode(计划模式)解决什么问题?它和直接执行的差别在哪?**

- **人话版**:让 agent 先只读探索、产出实施计划,人批准后再动手。解决两件事:探索产生的大量检索噪声不污染执行上下文(批准后重新开始);人在方向错之前就能打断,避免"写完才发现理解歪了"。
- **展开**:
  - **上下文视角**:探索阶段 agent 可能读几十个文件,这些过程留在上下文里就是噪声;plan mode 探索完出计划,执行阶段从干净上下文+计划开始——这是内置的上下文隔离设计(和 subagent 隔离同族)。
  - **风险视角**:长任务里方向错误的每一步都是负资产,而且越晚发现返工越贵;plan 是最便宜的纠偏点——改一行计划 vs 改一百行代码。
  - **何时不该用**:官方判据很朴素——"能用一句话描述 diff 的改动就跳过计划"。计划本身有成本(一轮探索+人审延迟),小改动上 plan mode 是仪式主义。
  - **与 spec 的关系**:plan 本质是轻量的 spec 入口——人批准的计划文档就是任务级 spec;重度场景(多天任务)从 plan 升级到正式 spec 文件。
- **及格线**:两个解决的问题(噪声隔离/提前纠偏)。
- **加分**:上下文隔离视角;跳过计划的判据。
- **红旗**:所有任务无脑 plan(不识成本);认为 plan 是"给新手用的"。

**Q4.5【八股·L2·平台】生产级 agent 的 system prompt 是"一段话"吗?模块化拼装的结构启示是什么?**

- **人话版**:不是。对泄露的 Claude Code system prompt 的分析显示是数百个字符串片段按环境/配置动态拼出来的——tone 指令、任务管理、安全边界、各工具的使用说明各是独立片段。启示:system prompt 是配置管理系统,不是作文。
- **展开**:
  - **为什么要模块化**:按环境开关(沙箱内外不同指令)、按配置拼装(挂了 MCP 才有 MCP 段)、可独立测试(每个片段是一个可回归的行为约束);拼接顺序固定——这也是缓存纪律(system prompt 必须逐字稳定)的要求。
  - **内容的类型学**:tone(简洁、引用格式 file:line)、任务管理(强制用 todo 跟踪)、安全(外部内容不可信边界)、工具流程(某工具怎么用好)。写 system prompt 时按类型组织,而不是想到哪写到哪。
  - **实践差距**:多数团队的 system prompt 是一篇不断追加的"巨型作文"——改一处怕动全身,没人敢删。模块化+定期清理(每段标注作用与触发条件)是可持续的做法。
- **及格线**:拼装而非一段话+两个模块化理由。
- **加分**:从缓存稳定性角度讲拼接顺序;内容类型学的组织法。
- **红旗**:把 system prompt 当越写越长的说明书;没意识到它和配置管理/测试的关系。

**Q4.6【八股·L2·用户】headless/自动化模式怎么用?典型场景和坑?**

- **人话版**:`claude -p "任务" --output-format stream-json` 一类的一次性调用,输入输出全是结构化的,进 CI 和脚本。最大的坑是拿交互环境的假设去用 CI:审批没人点(超时)、轮次没上限(烧钱)。
- **展开**:
  - **关键参数**:`--permission-mode` 配合白名单(`--allowedTools "Edit,Bash(git commit *)"`)给审批一个确定答案——CI 里权限问题的答案必须确定(要么白名单放行要么拒绝),等待人工就是挂起;`--max-turns` 限轮次;stream-json 输出供脚本消费。
  - **场景**:批量迁移的 for 循环扇出(N 个子任务并行开 PR)、GitHub Actions 里 @claude 处理 issue、代码库批量修 lint。
  - **坑**:任务粒度太大导致超轮次(拆小);工具反馈差导致绕圈(修工具);失败时要把部分完成的 diff 保留供人接手(exit code 语义+JSON 报告);预算熔断必须有。
- **及格线**:结构化输入输出+审批确定化两个要点。
- **加分**:失败时的"部分成果保留"设计;批量扇出的实践经验。
- **红旗**:在 CI 用 default 权限模式(等到天荒地老);不设轮次和预算上限。

---

## 4.2 原理深挖

**Q4.7【原理·L3·平台】设计 agent 的停止条件体系:四层闸门分别防什么?**

- **人话版**:①自然停止(模型说完了)——主路径,但防不住自认为完成;②硬上限(max_turns/预算)——防失控烧钱;③确定性闸门(Stop hook/独立验证)——防假完成;④退化检测(重复动作/上下文 thrashing)——防僵尸循环。层层递进,缺一层就多一类事故。
- **展开**:
  - **第③层是质量核心**:模型"我认为任务完成了"和"测试全绿"是两回事(自我验证不可靠的实证见 ch07)。Stop hook 把验收从模型的自我判断换成确定性检查;拦截要有上限次数(连续拦 8 次后放行并升级人工——防止 agent 和 gate 死锁互搏)。
  - **第④层的信号**:重复签名检测(同工具同参数的滑窗重复率)、同一文件 N 轮内反复改、错误信息相似度——检测到就该注入"你已尝试 X 次相同方案"的提醒或强制终止。
  - **经验法则**:同一问题连续 2 次修正失败就该中断换思路——继续堆错误上下文是在给模型喂误导。这个法则同时是效率和质量建议:错误上下文的边际成本是负的。
- **及格线**:四层+各自防什么。
- **加分**:第③层的拦截上限设计(防死锁);重复检测的具体信号。
- **红旗**:只有 max_turns 一层(烧完钱才停);把"模型说完成"当完成。

**Q4.8【原理·L4·平台】长任务跨多个会话/上下文窗口,怎么设计"接力"?讲讲"轮班工程师"模式的工程方案。**

- **人话版**:把长任务当成几个班的轮班工程师——每个会话像接班的人,没有上一班的记忆,所以要留交接文档。方案:一个初始化会话建好环境和任务清单,后续每个会话开机先读 git log 和进度文件、领一个任务、做完 commit——git commit 就是 checkpoint。
- **展开**:
  - **开机序列(固定动作)**:读 git log(代码状态)→读进度文件(任务状态)→选最高优先级未完成项→先跑端到端测试确认基线→开工。每个会话只做一个 feature——上下文永远新鲜,不依赖跨会话记忆。
  - **两个反直觉的细节**:任务清单用 JSON 而不是 Markdown——Markdown 太"好改",agent 会顺手重写美化,破坏状态的可审计性;结构化格式有 schema 约束,增删可校验(详见 ch06)。用浏览器(Playwright)截图像人类用户一样验证前端——测试不只是断言,是真实体验。
  - **本质**:把"记忆"从上下文迁移到 repo——进度文件+git 历史+测试构成了跨会话的持久状态,每个会话从状态重建上下文而不是恢复对话。这是"会话易失、仓库持久"这一事实的直接推论。
  - **配套**:每会话结束前更新 handoff(阻塞点/下一步/给下一个班的忠告);人工检查点设在里程碑而不是每一步。
- **及格线**:开机序列+commit 即 checkpoint。
- **加分**:JSON 清单的理由;"从状态重建"vs"恢复会话"的哲学差异。
- **红旗**:指望 resume 一个超长会话跑完数天任务(compaction 的信息损失会累积);没有端到端测试基线就开工。

**Q4.9【原理·L3·架构】worktree 隔离解决什么问题?实现要点是什么?**

- **人话版**:多个 agent 任务共用一个工作目录会互相污染——半成品文件、dev server 端口冲突、git 状态打架。git worktree 给每个任务一棵独立的工作树+分支,互不干扰,合并走正常 git 流程。
- **展开**:
  - **问题清单**:文件写冲突(两个 agent 改同一文件后写覆盖)、环境冲突(dev server/构建产物/锁文件)、git 状态污染(一个 agent 的 WIP 提交挡住另一个)。共享工作区是并行 agent 的头号杀手。
  - **实现要点**(以 Claude Code 的 `--worktree` 为例):在 .claude/worktrees/ 下建工作树;`.worktreeinclude` 声明要复制的未跟踪文件(.env 之类);运行期持有 worktree lock 防误删;隔离会话有检查阻止 agent 逃逸回主目录(git -C、GIT_DIR 这类绕路都会被拦)。subagent 可声明独立 worktree 隔离。
  - **配套纪律**:一任务一树一分支;合并顺序按依赖串行(见 ch05 并行写工程);失败任务的树保留现场 N 天再清。
- **及格线**:三类冲突+一任务一树。
- **加分**:逃逸检查的实现意识;.env 复制这类细节;和 CI 批量扇出的配合。
- **红旗**:以为 git branch 就够了(不解决工作目录共享);让两个 agent 在同一目录"小心点"。

**Q4.10【原理·L3·平台】checkpoint/rewind 机制和 git 的关系?为什么说"agent 的回滚"和传统事务回滚是两回事?**

- **人话版**:Claude Code 每次编辑前自动快照,双击 Esc 能回滚代码/对话/环境三态。但它只覆盖 agent 的编辑类工具——bash 造成的外部改动(装的依赖、发的请求)不在快照里。回滚粒度是"agent 的动作",不是"世界的状态"。
- **展开**:
  - **三态回滚**:代码状态、对话状态、环境状态分别可回退——比 git 多了对话和环境两层;官方明确说"不是 git 替代",文件历史本地保留约 30 天。
  - **本质差异**:传统事务有 begin/rollback 的边界,副作用全部在事务内;agent 的副作用跨越数字边界——外部 API 调用、发出去的邮件、写过的生产库,这些没有快照可回。所以 agent 安全设计的重心不是"能回滚",是**限制不可逆动作的发生**(权限分级、审批)。
  - **设计推论**:可逆性要成为任务设计的一等信息——把"能 undo 的"交给 agent 自动做,"不能 undo 的"结构性隔离。这个思路贯穿 ch08 的沙箱设计。
- **及格线**:编辑类覆盖/bash 副作用不在内。
- **加分**:三态的概念;从"回滚有限"推出"前置限制不可逆动作"的设计转向。
- **红旗**:把 checkpoint 当完备的回滚方案放心让 agent 干任何事。

**Q4.11【原理·L4·架构】"权限/审批"(guardrails)和"验证闭环"(verification)是什么关系?只有其中一边的 agent 会怎样?**

- **人话版**:guardrails 管"不能做什么"(权限、审批、deny 规则),verification 管"做的是不是对的"(测试、验收、stop gate)。只有 guardrails 没有 verification,agent 会安全地交付垃圾;只有 verification 没有 guardrails,agent 会高质量地闯祸。
- **展开**:
  - **两类系统回答不同的问题**:guardrails 是事前和事中的约束(动作层面),verification 是事后的判定(结果层面)。工程上经常被混为一谈——"我们配了权限所以质量有保障"是不成立的,权限拦不住"错误但合规"的输出。
  - **常见失衡**:企业环境普遍 guardrails 过剩(层层审批)而 verification 缺位(没有可执行验收)——结果是 agent 干得慢还干不对,人审负担反而上升。正确的投资方向是补 verification(可执行验收、stop gate),guardrails 按风险分级收敛到真正的不可逆动作。
  - **组合的完整形态**:四个问题齐备才算闭环——agent 在哪跑(隔离环境)?能看到正在发生什么吗(可观测)?怎么证明做对了(验证)?失败后系统会不会变好(失败升级为规则)?这四项是评估任何 agent 系统(自研或采购)的检查清单。
- **及格线**:两个"会怎样"的反例。
- **加分**:四项检查清单;指出企业常见的"guardrails 过剩+verification 缺位"失衡。
- **红旗**:认为配好权限就万事大吉;反过来认为"测试全绿不需要权限"。

---

## 4.3 品味与判断题

**Q4.12【品味·L3·平台】"必须发生的事交给 hooks,不许发生的事交给权限,希望发生的事交给 prompt"——critique 这个分工。**

参考思路:
- **合理内核**:确定性递增——hook 是代码(确定执行)、权限是策略(确定拦截)、prompt 是软引导(尽力而为)。硬约束放越靠确定性的一侧越可靠。
- **三个漏洞**:①hooks 本身是攻击面(不可信配置里的 hook 就是恶意代码,"确定性"反而成了确定执行恶意);②权限管工具层管不了语义层(allow 了 Bash 拦不住脚本里的恶意内容);③prompt 层的责任被低估——信任边界的声明(外部内容不可信)是注入防御的第一道,它只能在 prompt 层。
- **漏掉的第四格**:验证(Stop gate/verifier)是"必须不发生假完成"的确定性,经常被忘——前三格管动作,第四格管结果。
- **及格线 / 加分 / 红旗**:及格=合理内核+至少一个漏洞;加分=第四格;红旗=当口诀背不做批判。

**Q4.13【品味·L4·架构】agent 系统"胖 loop 瘦工具"还是"瘦 loop 胖工具"?**

参考思路:
- **胖 loop**:编排逻辑在 harness(状态机、重试、路由)——可控可审计,但僵硬,而且把智能从模型挪回代码,模型每升级一代你要重写一遍编排。
- **瘦 loop**:while+少量正交工具,智能在模型——灵活、随模型能力水涨船高,但可预测性差。
- **2024–2026 的行业收敛**:瘦 loop+好工具+关键节点的确定性闸门(权限/验证/停止)。"把控制流写死"的重框架在 coding 场景全面输给了裸循环;但错误成本高的节点(发布、资金、生产变更)保留确定性代码。
- **判断函数**:错误成本越高,越多逻辑下沉到确定性代码;探索性越强,越多留给模型。这不是立场是参数。
- **及格线 / 加分 / 红旗**:及格=两边代价+行业收敛方向;加分=判断函数;红旗=绝对站队。

**Q4.14【品味·L4·管理】某头部 coding agent 在 2026-08 把"分类器自动审批"设为默认权限模式(此前需要用户显式开启),引发权限模型争议。你敢在生产开自动审批吗?**

参考思路:
- **支持的论据**:官方评测显示自动模式拦截约 89% 人类会漏判的危险操作;现实是人对高频审批会疲劳性全点允许——"人工兜底"经常是幻觉,人是最弱的闸门。
- **反对的论据**:分类器误放行的尾部风险不可审计(为什么放行没有可解释记录);默认值的力量——绝大多数用户不会改默认,厂商用默认替用户做了风险决策;敏感环境(生产凭证、付费操作)的误放行成本不可逆。
- **决策框架**:按动作分级——读操作自动;写工作区自动+沙箱;外发/付费/生产变更保留人工硬闸;分级清单 version control。另外:把"默认开"和"我显式开"区分开——前者是厂商替你担责,后者是你自己的决策,治理含义不同。
- **及格线 / 加分 / 红旗**:及格=正反两面+分级;加分=默认值的治理含义;红旗=全盘接受或全盘拒绝自动审批。

**Q4.15【品味·L3·用户】你什么时候打断正在运行的 agent?给出干预阈值。**

参考思路:
- **立即打断**:方向性错误(读了三个文件就在错模块动手)——方向错误的每一步都是负资产,越早打断越便宜;危险动作的审批弹窗永远人工。
- **不拦**:小错误在验证环里自愈(测试红了它自己会修)——打断等于抢走它的学习机会,还把上下文搞乱。
- **该打断但多数人不打**:连续 2 次修同一问题失败(该换思路或换会话了);agent 开始"无限探索"(读了几十上百个文件还没动手)——这是失控的前兆。
- **底层原则**:纠偏成本随任务推进超线性增长,所以干预要趁早;但干预的粒度要匹配验证环的长度——秒级反馈环里不用管,分钟级以上的环节才值得盯。
- **及格线 / 加分 / 红旗**:及格=三类阈值;加分=纠偏成本的超线性直觉;红旗=要么全程盯(浪费)要么完全不管(赌博)。

---

## 4.4 开放性问题

**Q4.16【开放·L5·架构】agent loop 会演化成什么?"loop 消失"(模型原生多步执行)意味着什么?**

讨论框架:
- **正在发生的消解**:interleaved thinking 把思考嵌进循环;训练侧 agentic RL 让多轮策略进权重(模型"天生会"多步工具调用);厂商开始把循环行为和模型协同设计(见 ch09 DeepSeek)。
- **loop 的工程职责会去哪**:权限、验证、恢复这些确定性职责,要么下沉为平台原语(服务商内建),要么保留在 harness——但"谁拥有循环"决定了可观测和可控制的边界。
- **可观测性挑战**:一个不轮询的连续执行流,怎么审计"它第 37 步为什么这么做"——trace 的粒度要从"每轮"变成"每决策点"。
- **判断**:loop 作为代码结构会淡化,作为责任边界(出了事谁兜)不会消失。

**Q4.17【开放·L4·架构】"把 agent 当操作系统进程设计"(隔离/资源限额/退出码/审计)——这个类比哪里成立、哪里失效?**

讨论框架:
- **成立**:隔离(worktree/容器≈namespace)、资源限制(token/时长≈cgroup)、退出码(headless 的 exit code)、日志(trace≈journal)——运维体系可以直接借用。
- **失效**:agent 的"执行"是概率性的——重放不可精确复现;fork 语义昂贵(上下文复制=状态深拷贝,不像进程的 copy-on-write);副作用跨越数字边界(调外部 API 没有事务);"信号"没有可靠等价物(你不能 SIGTERM 一个正在推理的模型)。
- **推论**:进程模型可以借鉴隔离与审计,验证模型必须重构——进程的世界里失败是异常,agent 的世界里失败是分布的一部分。

---

## 4.5 工程实战题

**Q4.18【实战·L3·平台】从零实现一个最小 coding agent(约 300 行),列出组件清单与关键决策。**

参考思路:
- **组件清单**:LLM client(消息 API+tools 定义+usage 统计);工具集 Read/Write/Edit/Glob/Grep/Bash(每个都带输出截断!);权限层(默认 ask,白名单放行);主循环(含 max_turns/预算熔断);compaction 触发器(token 阈值,先清旧工具输出);transcript 落盘(JSONL 可重放)。
- **四个关键决策**:工具输出上限(不做截断的第一个长任务就爆上下文);tools 定义冻结(缓存);错误回喂格式(语义化,不是 traceback);终止条件分层(不止 max_turns)。
- **加分件**:hook 点位预留、subagent 抽象、stream-json 输出。
- **这题的隐藏考点**:候选人是否知道"300 行能跑"和"生产可用"之间差的是验证层和恢复机制——好的回答会主动说"这个 MVP 缺 stop gate 和失败恢复,生产还要加"。
- **及格线 / 加分 / 红旗**:及格=六组件+四个决策;加分=主动指出 MVP 与生产的差距;红旗=组件清单里没有权限和预算。

**Q4.19【实战·L3·平台】设计 Stop-gate:任务"必须通过全部测试+lint+无未说明的文件变更"才算完成。**

参考思路:
- **机制**:Stop hook 拉起验证脚本;失败 exit 2+失败摘要回喂;连续拦截上限(如 3 次后不再硬拦,改为输出 blocker 报告升级人工——防死锁)。
- **防作弊**:验证脚本和测试文件不在 agent 可写路径(gate 的可信前提是 agent 改不了它);git status 检查未说明变更;测试只读。
- **验证产物注入**:测试报告作为 additionalContext 给模型——它需要的是失败详情不是"挂了"两个字。
- **及格线 / 加分 / 红旗**:及格=hook+exit 2+上限;加分=防篡改设计;红旗=gate 脚本放在仓库里 agent 可改的路径。

**Q4.20【实战·L4·架构】设计支持 20 个并行 agent 任务的执行环境层(不依赖商业产品)。**

参考思路:
- **隔离**:每任务一个 worktree(或容器,按敏感度);统一任务队列+锁文件认领(防两个任务领同一件事)。
- **资源**:共享只读缓存(依赖、构建产物——20 份重复下载既慢又贵);资源限额(CPU/时长/token/网络)。
- **网络**:默认禁网+白名单(按任务 profile);出站走代理可审计。
- **回收与留证**:完成即清理;失败现场保留 N 天;人工介入通道(暂停/审批/接管)。
- **参照物**:公开的多 agent 编译实验(16 agent 并行、锁文件认领、gcc 当 oracle)和各家 worktree 方案——自己设计前先对齐这些已验证的模式。
- **及格线 / 加分 / 红旗**:及格=隔离+认领+限额;加分=共享缓存与失败留证;红旗=20 个任务共用一个工作目录。

---

## 4.6 场景排错题

**Q4.21【排错·L3·平台】agent 在同一问题上循环 40 分钟,反复"读文件→改→跑测试→改回"。loop 级的检测与干预?**

排查链路:
- **检测信号**:重复签名(工具+参数 hash 的滑窗重复率)、同一测试文件 N 轮内被反复改、错误信息相似度不降。
- **干预分级**:先软后硬——注入提醒("你已尝试 X 次相同方案,请换思路");再强制 compact(可能早期约束已丢);再 Stop-gate 拦截要求输出 blocker 报告;最后升级人工。
- **根治在 loop 外**:这种现场通常说明任务缺验收标准(agent 不知道"好了"长什么样)或工具反馈误导(测试本身 flaky/错误信息不可读)——修 loop 的检测不如修验证器和任务定义。
- **及格线 / 加分 / 红旗**:及格=检测信号+分级干预;加分=指出根因在验证器/任务定义;红旗=直接砍掉 max_turns 了事(治标)。

**Q4.22【排错·L2·用户】agent 每次都在权限弹窗上等你,一天 200 次点击,但你又不敢全放行。怎么配置?**

排查链路(按风险分层放行):
- **读类全放**:Read/Grep/Glob 无副作用,allow;
- **写类限路径**:Edit/Write 限工作区(gitignore 语法);
- **Bash 用前缀白名单**:`Bash(git *)`、`Bash(pnpm test*)`;高危(git push、rm、curl 外网)交给 PreToolUse hook 精确拦截——hook 能看参数做逻辑判断,比静态 allow 规则灵活;
- **把"必须发生的"自动化**:format 之类 PostToolUse 自动跑,别走审批。
- **目标状态**:弹窗只剩真正需要人判断的动作(一天个位数)。每次点击都是配置债——高频弹窗说明该分层了。
- **及格线 / 加分 / 红旗**:及格=分层思路;加分=hook 拦危险命令的模式;红旗=烦了直接 bypassPermissions。

**Q4.23【排错·L4·平台】headless 模式在 CI 里随机挂:一半 max_turns 超限,一半等审批超时。诊断与修复?**

排查链路:
- **审批超时的根因**:CI 环境用了交互默认值——审批必须确定化(白名单+acceptEdits/auto;dontAsk 的"拒绝"也是确定答案);检查哪些工具没进白名单(看挂起时刻的 pending 调用)。
- **max_turns 超限的二分**:任务粒度太大(拆小,单任务目标一个 PR)vs 工具反馈差导致绕圈(看 trace 里重复调用的工具——修它的返回质量)。
- **CI 特有的配套**:结构化结果输出(exit code 语义+JSON 报告);预算熔断;部分完成的 diff 保留供人接手(超限不等于白干)。
- **及格线 / 加分 / 红旗**:及格=两类根因各自的修复;加分=从 trace 定位绕圈工具;红旗=把 max_turns 调大 10 倍"解决"。
