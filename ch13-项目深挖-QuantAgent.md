# ch13 · 项目深挖:QuantAgent(开源量化 Agent 系统)

> 本章素材来自开源项目 QuantAgent(github.com/BqLee-AI/QuantAgent,Python,仓库内容截至 2026-06):一套事件驱动的量化分析 agent 系统——外部事件采集(RSS 等 source 插件)→ 事件总线 → 行业路由 → 行业 MainAgent 受控分析 → 评分/辩论(作为工具)→ Policy Gate 审批 → 通知与受控执行。
> 用法:这是"候选人带着自己的项目来面试"的标准深挖题集。**项目题的正确答法是演进叙事,不是设计说明书**:每道设计题都按「遇到的问题 → 最初的方案 → 测试中发现为什么不合适 → 换成的方案 → 优势 → 成效」六段讲——面试官想听的是"你踩过坑并消化了坑",不是"你抄了一份完美架构"。
> 所有题干自包含,不读仓库也能看懂;叙事基于仓库真实设计( docs/agent/ 系列文档)与真实测试观察。

---

## 13.1 项目叙事(怎么讲项目)

**Q13.1【开放·L3·综合】"用 30 秒介绍一下 QuantAgent。"然后面试官说"展开讲讲,给你 3 分钟。"**

参考思路(项目作者的答法):
- **30 秒版要背下来,原话是**——"QuantAgent 是一套事件驱动的量化分析 agent 系统:外部事件(财报、行业新闻)进来后,经行业路由交给对应行业的 MainAgent 做受控分析,产出的不是自动交易,而是一份结构化行动计划(ActionPlan),经过用户策略的审批门,你授权后由系统按计划自带的时效规则直接执行(超时自动作废)——**执行环节没有 agent**。**它是'分析→行动意图→受控执行'的平台,不是交易机器人**。"——最后一句的"不是什么"就是差异化,面试官大概率顺着它挖。
- **3 分钟版四段结构(问题→方案→一个演进故事→状态)**——问题:信息驱动的研究工作流里,采集、分析、行动是割裂的;通用 chat agent 干不了这个活,因为它不可审计、不可扩展、没有行动边界。方案:事件驱动骨架(采集/路由/分析/审批各自独立)+ 行业包插件化 + DeepAgents 做 agent harness + 审批门控。**中间必须嵌一个 30 秒的演进故事当钩子**(推荐多工具收敛成 submit_action_plan 那个,Q13.8 的六段浓缩版):"我们最早给 agent 一堆行动工具,测试里发现它不遵循 prompt、参数传得五花八门,后来把所有行动收敛成一个提交口,三类问题一次性消失"——**有翻车有修复的故事是面试官记住你的部分**。收尾状态:半导体行业包已跑通 RSS→路由→分析→Discord 通知全链路,内置事件回放和链路诊断工具。
- **讲项目的纪律**——按"问题→方案→演进→状态"讲,不按技术栈清单讲;每个设计都配一句"为什么不是另一条路"。
- **及格线 / 加分 / 红旗**:及格=30 秒版有定位有边界;加分=3 分钟版里嵌了演进故事;红旗=从"FastAPI+React+PostgreSQL"开始背、3 分钟讲不到任何一个决策或翻车。

**Q13.2【开放·L4·综合】"这个项目里你做的最难的技术决策是什么?"——讲两三个真实的,要有取舍过程。**

参考思路(讲决策的模板+仓库里现成的三个):
- **决策讲述模板六段**——背景约束(当时要解决什么)→ 备选项(至少两个,否则不叫决策)→ 判据(你用什么标准比的)→ 选择和放弃(明确说放弃了什么)→ 事后验证(上线后什么信号说明选对了)→ 如果重来(会不会不同)——**"放弃了我什么"和"事后信号"是区分背答案和真做过的两段**。
- **决策一(行动入口收敛,最推荐讲)**——最初给 agent 多个行动工具(通知、提交计划、请求审批、执行操作各一个),期望靠 prompt 说明调用次序;测试中发现 agent 经常不遵循 prompt(该先审批却直接通知、跳过计划直接要执行),不同工具的调用结果有偏差,参数传入不一致;根因是"工具并列存在=模型有自由选择空间,prompt 是软约束管不住工具面";收敛成 submit_action_plan 单入口后,出错面从"任意工具序列"缩到"一个 JSON 的合法性"(完整六段见 Q13.8)。这题还有第二跳可讲:**执行权随后也移出 agent**——金融场景里 plan 本身就是 agent 产的、参数错误就是真金白银的损失,而且时间敏感的操作等不起 agent 的思考;最终人授权后由系统按 plan 的时效规则(超时 5 分钟自动拒绝、授权后 2 分钟内立即执行)直接执行(Q13.13)。
- **决策二(不自建执行框架)**——备选是自建 LangGraph 式状态机 vs 用 DeepAgents 现成能力+使用纪律;判据是"我需要的是能力还是性质"——规划/委派/skills 框架已给,我要的可审计/可控可以靠纪律获得;放弃的是"完全可控的执行内部",换来的是没有长期维护负债(Q13.12)。
- **决策三(MVP 只拆一个 SubAgent)**——最初设想按职能拆五六个专家子代理;设计阶段就否决:每个专家的 instruction、工具面、验收标准当时都定义不出来,拆了也是拆成想象的形状;只拆边界最清晰、成本最集中的检索子代理,其余等真实瓶颈信号(Q13.11)。
- **及格线 / 加分 / 红旗**:及格=两个决策有备选和取舍;加分=六段模板齐+事后信号;红旗=把"用了什么技术"当决策(没比较过备选的不足决策)、只讲成功没有放弃。

**Q13.3【品味·L4·综合】面试官挑衅:"这和直接把新闻丢给 ChatGPT 问'该不该买 NVDA'有什么区别?"**

参考思路:
- **先给一个具体对照,再上维度**——"你把 NVDA 财报新闻贴给 ChatGPT,它给你一段话;QuantAgent 里同一个事件会:被 scheduler 抓到→URL 去重落库→路由到半导体行业→MainAgent 拉起检索子代理验证关键数字、查公开对照材料→收敛成结构化 IndustryAnalysis→生成带风控和监控建议的 ActionPlan→按用户策略走'只通知'或'待审批'→你在 Discord 授权后,系统按 plan 自带的时效规则(超时自动作废、授权即限时执行)直接完成受控执行——执行环节没有 agent——全程有 run 记录可回放"——**同一个输入,一边是一次性对话,一边是一条可运营的管线**。
- **四个维度的机制差别**——可扩展(加一个行业=加一个插件包,不是重写 prompt;源与频率是配置 binding);可审计(run 记录+工具调用摘要+阶段摘要+事件回放);受控行动(agent 只能提交 ActionPlan 过 Policy Gate——chat 的建议没有执行边界也就没有责任边界);一致性(同一事件走同一条受控管线,chat 每次即兴)。
- **诚实承认重叠**——分析内核同样是 LLM 调用,差别不在聪明程度在工程化:"把一次性的对话能力变成可运营的系统";这个承认立住"你分得清 demo 和产品",不丢分。
- **收尾一句**——"chat 是引擎,QuantAgent 是带变速箱和刹车的车。"
- **及格线 / 加分 / 红旗**:及格=两维度+机制;加分=具体事件的全链路对照+承认重叠;红旗=贬低 chat 模式(它是这个系统的子集,不是对手)。

---

## 13.2 事件驱动骨架(演进叙事)

**Q13.4【原理·L4·平台】为什么 api、worker、scheduler 是三个独立进程,中间用事件总线(Kafka)连接?你们最初就是这样的吗?**

- **人话版**:最初是一个 FastAPI 单体里顺手干所有事,AI 分析一慢接口就跟着超时;拆成三入口后,抓取、分析、服务各自活各自的,中间靠事件契约说话。
- **遇到的问题**:第一版把抓取调度、AI 分析和 HTTP 接口放在一个进程里。测试时两个现象反复出现:一是 worker 型任务(Readability 正文增强、Router Agent 分析)一卡,API 的响应跟着超时——**慢任务和快接口共享一个事件循环,互相拖累**;二是想单独验证"抓取对不对"时必须把整个系统跑起来,问题定位像大海捞针。
- **最初的方案**:先试过"单体里加后台线程+定时器"——不拆进程,只把重活挪出请求路径。**发现不合适**:线程模型下抓取周期、消费速率和请求负载仍然共享资源预算,GIL 下 AI 密集任务照样挤压接口;更麻烦的是测试语义混乱——单进程测试"通过"了,真到多进程部署行为就不一样。
- **换成的方案**:按负载模型拆三个入口——`api`(低延迟请求响应)、`worker`(常驻消费:抓取增强→路由→AI 分析,慢而重)、`scheduler`(周期 tick:扫描 due 的 SourceBinding→抓取→发布 `source.event.captured`);中间用事件总线解耦,**模块间只依赖事件契约不依赖彼此的存活**。配套两个关键决策:①事件总线做双 backend——生产/跨进程默认 kafka,单进程单测才允许 memory(内存总线没有跨进程语义,当真总线用会测出"假通过");②数据库迁移(migrate)做成显式的一次性服务(`--profile migration`),不藏在 api 启动里——起个服务不应该可能隐式改库,有副作用的动作必须显式调用。
- **优势**:故障隔离(AI 分析卡死不影响抓取入队,事件还在总线上等);独立扩缩(将来分析慢了加 worker 实例,接口无感);模块边界清晰(事件 schema 就是契约文档);本地可分层验证(run_once 单进程 smoke 分别验 scheduler 和 worker)。
- **成效**:接口超时和"测试假通过"两类问题结构性消失;新人可以用最小组合跑通目标链路(只验抓取就 db+scheduler,不必带 worker 和 kafka)——**可调试性是拆分换来的对价,这个对价我们主动付了**(diagnose 工具、smoke 入口、双 backend)。
- **及格线 / 加分 / 红旗**:及格=负载模型+故障隔离;加分=memory backend 的测试语义陷阱、migrate 显式化同源逻辑、拆分的对价意识;红旗=认为拆进程是"为了微服务而微服务"。

**Q13.5【原理·L3·平台】去重逻辑:监控日志显示 `captured_count>0 但 created=0、duplicate>0`,这是什么状态?你们为什么把 URL 去重放在入库层,而不是让 worker 消费时自己判重?**

- **人话版**:这个日志组合是"抓到了,但全是抓过的旧闻"——去重在正常工作,不是故障。去重放入库层是因为它是唯一入口:binding 重叠、源没更新、回放重推,全都在这拦住,下游永远只见到真正的新事件。
- **遇到的问题**:早期没有入口去重,同一 URL 从不同 binding(多个半导体源引用同一篇文章)重复进来,worker 把同一条新闻分析了两次,用户在 Discord 被同一件事打扰了两遍;做事件回放测试时,旧事件重推又被完整处理一遍——**重复不是噪声问题,是"同一事实产生两次行动"的责任问题**。
- **最初的方案**:让 worker 消费时按 URL 判重——**发现不合适**:①多个 worker 实例并发消费同一事件,判重逻辑要考虑并发竞争,每个消费者都得实现一遍;②下游不止 worker(审计、UI 读模型),每个下游都要各自防重,复杂度按消费者数量翻倍;③判重放在消费侧,重复事件已经占了总线带宽和消费预算。
- **换成的方案**:落库时按 URL 去重,RawEvent(事件)与 RawEventCaptures(捕获记录)分离建模——同一 URL 被多个 binding 抓到只产生一条事件,但保留"从哪抓到"的多条捕获记录;只有新建的事件才发 `source.event.captured`。
- **优势**:幂等收口在最早的层,下游所有消费者免判重;"事件/捕获"分离让"同一事实多来源"变成可分析的数据而不是重复行;去重结果直接可观测(日志里 created/duplicate 两个计数)。
- **成效**:"用户被同一新闻打扰多次"这类故障从偶发变为结构不可能;`created=0 duplicate>0` 成为可判读的正常静默信号(新人第一课就是学会读它,别去重启 worker);回放时旧事件天然不会绕过去重制造假的新事件。
- **及格线 / 加分 / 红旗**:及格=日志解读正确+入口幂等;加分=消费侧判重的并发论证、事件/捕获分离;红旗=看到 created=0 就当故障修。

**Q13.6【原理·L3·平台】项目内置了 `source-event-replay`(diagnose 诊断 + replay 回放,带 `--dry-run` 和 `--clear-routed` 开关)。为什么一个事件驱动系统要内置回放?这套工具本身又是怎么演进的?**

- **人话版**:事件驱动系统出了问题最难答"到底哪一环没走"——最早靠人查五张表考古;后来把排查链路固化成命令,又发现历史事件是免费的回归测试集。
- **遇到的问题**:链路是"抓取→落库→入队→消费→路由→落库"五环,任何一个"/runtime 没新输出"的反馈都要人工按顺序查 DB 计数、翻日志、看 kafka topic 和 consumer group——**每次排障都是一次考古,结论还不可复制**,新人来了要重新学会一遍。
- **最初的方案**:写一份排障文档,列出每一步该查什么表。**发现不合适**:文档会过时(表名/字段一变就错);照文档手工查还是慢;而且它只解决"诊断"没解决"验证"——怀疑某条事件卡住时,没有办法单独把它重跑一遍确认。
- **换成的方案**:做成 `source-event-replay` 工具集——`diagnose --limit 10` 一次输出运行配置、DB 各表计数、最近已 capture 但未路由的新闻、最近路由摘要、kafka topic/consumer group 状态(把"没输出"拆成四个可判定子问题:没抓取?没入队?没消费?没落库?);`replay` 按事件 ID/capture/binding 把已入库事件重新推回总线重走全链路,支持 `--limit` 批量。
- **优势**:排查从"凭记忆查五张表"变成"跑一条命令读分层输出";回放让单条事件可单独验证,也让"改了路由逻辑/agent prompt 后拿历史事件重跑对比"成为标准验证动作——**事件已持久化这个架构性质,免费送了一套 eval 基础设施**。
- **成效与两个开关的由来**:回放本身引入过新问题——直接重放会重新触发 AI 分析(花钱)和重新发通知(打扰用户),所以加 `--dry-run` 先预览将重放哪些;新旧路由结果叠在 UI 里分不清哪次是哪次,所以加 `--clear-routed` 先清旧结果再入队——**回放工具自己也要幂等和可观察,这是把工具当产品设计的意识**。
- **及格线 / 加分 / 红旗**:及格=回放的两个价值(排错+回归)+dry-run;加分=clear-routed 的由来叙事、"事件溯源红利"的抽象;红旗=认为回放只是调试便利(它是架构性质的能力)。

**Q13.7【原理·L3·架构】行业 MainAgent 有一份"不做清单":不直接读写数据库、不直接调 provider SDK、不直接调 broker、不绕过 ToolRegistry 和 Policy Gate、不保存完整 chain-of-thought/完整 prompt/原始响应。这份清单是拍脑袋写的还是每条都有事故?逐条讲讲。**

- **人话版**:每条"不做"都对应一类翻过或可预见必然翻的车:给 agent SQL 就是给了它一切;让"表达交易意图"和"执行交易"是同一个人,审批就是空的;全量存 CoT 是自建合规债。清单是把教训变成结构。
- **① 不直接读写数据库**——最初直觉是给个只读 SQL 工具"查什么都行,多灵活";设计评审时否决:agent 的查询会拉回整表无关列(上下文爆炸)、连接串和 secret 可能被带进日志、写权限更是不可逆风险。换成 `get_account_context` 这类受控工具,返回**压缩摘要**而不是裸表——工具成为 agent 的数据防火墙。
- **② 不直接调 provider SDK**——SDK 的鉴权、重试、版本差异一旦散进 agent 行为,升级一次 provider 全链路行为漂移;统一走平台封装,provider 是可替换的实现细节。
- **③ 不直接调 broker**——"表达交易意图"和"执行交易"必须是两个人:直连 broker 意味着 agent 可以在没有任何门的情况下真实下单,参数传错一次就是真金白银的损失——这正是引入 plan 机制的出发点;而且就算人授权了,让 agent 执行也扛不住时效性——它要想一轮,时间敏感的操作窗口就过了。收敛后:broker 操作只有 Policy Gate 之后的一个入口,人授权后由**系统**按 plan 配置的时效规则(超时 5 分钟自动拒绝、授权后 2 分钟内立即执行)确定性执行,幂等和 dry-run/mock 边界只需在一处保证(Q13.8/13.13)。
- **④ 不绕过 ToolRegistry / Skill Registry**——Registry 是工具的准入点和审计点(工具描述有没有被投毒、谁挂载的、SubAgent 用的是哪份 tool profile);绕过它=注入面失控+审计断链。这条和"SubAgent 不自动继承 MainAgent 工具"配对(Q13.10),否则权限边界是一棵没人看得清的继承树。
- **⑤ 不保存完整 CoT/prompt/原始响应**——最初为了"方便调试"全量存过;发现三个问题:体积(全量是摘要的几十倍,跑起来才知道疼)、隐私合规(完整 prompt 必然含用户数据,原始响应是第三方内容,日志里攒这些等于自建合规债)、**以及真排错时根本不用它**(定位靠工具调用轨迹,CoT 里模型"说的"和实际"做的"不一定一致)。换成"阶段摘要+工具调用摘要+失败路径"+run artifact 存结构化中间产物(Q13.14 展开)。
- **共同的实现纪律**:这些边界靠 runtime 强制(工具注入白名单、schema 校验),不靠 system prompt 叮嘱——**prompt 是软约束,runtime 是硬边界**。
- **及格线 / 加分 / 红旗**:及格=五条里四条讲出对应风险;加分=每条带"最初直觉→否决理由"、runtime 强制 vs 叮嘱的区分;红旗=认为这些边界"限制 agent 能力上限"(上限和边界是两回事)。

---

## 13.3 Agent 设计含金量(演进叙事)

**Q13.8【原理·L4·平台】`submit_action_plan` 的由来:最初你们给 agent 提供了多个工具分别用来通知、提交计划、执行操作,后来收敛成这一个入口。把完整演进讲一遍——当时到底观察到什么,为什么 prompt 管不住,新方案怎么让问题"结构性消失"?**

- **人话版**:最早工具箱里 notify_user、request_approval、request_broker_action 各自独立,期望 prompt 写清调用次序;测试里 agent 经常不遵循 prompt、调用结果有偏差、参数传得不一致——根因是工具并列本身就是自由选择空间。收敛成单入口后,三类问题不是变少了,是**类别性消失**。
- **遇到的问题(最初的工具面)**——第一版给 MainAgent 的行动类工具大约是:notify_user(发通知)、submit_plan(提交分析结论)、request_approval(请求审批)、request_broker_action(要求交易操作),期望的工作流是"分析→提交计划→按策略请求审批→审批过了才执行→通知用户"。**测试中反复观察到三类症状**:①**不遵循 prompt**:该走"审批→执行"的路径,agent 有时直接 notify_user 告诉用户"已完成操作",有时跳过提交计划直接 request_broker_action;同一类事件两次 run 走的工具组合不一样——**行为不可枚举,也就不可审计**;②**调用结果有偏差**:四个工具各自返回各自的状态结构,"通知已发"和"审批 pending"在上下文里的口径不一致,agent 综合这些状态生成的用户摘要时常和真实平台状态对不上;③**参数传入不一致**:同一个事件,有的工具要 event_id、有的要 event_ref 路径字符串、有的要 industry_id——模型填混是常态,而且是**静默错误**(跑偏了不报错,回看才发现)。
- **为什么发现不合适(机制层的归因,本题的灵魂)**——不是模型不够聪明,是设计给了它不可能稳定的任务:①**prompt 是软约束**:指令的约束力随上下文变长而衰减,工具越多、枚举"什么时候用哪个"的规则越多,遵循率越低——我们要求模型记住一张调用次序表,这张表在压力下必然被简化;②**工具并列=自由选择空间**:四个工具在模型眼里是四个平行选项,"先通知再审批"和"直接执行"在语法上同样合法,选哪个只取决于概率分布;③**参数 schema 各异=出错面线性叠加**:每个工具一套参数,错误率随工具数累加,而校验逻辑分散在各工具内部,没有一个统一的地方拦截。
- **换成的方案**——行动类工具收敛为**一个** `submit_action_plan`:MainAgent 只提交结构化 ActionPlan(订单草案、风险控制、监控建议、用户摘要草案),schema 统一校验;入口内部由平台编排:ApprovalPolicyResolver 按用户策略解析模式→Policy Gate→审批/通知/broker dry-run/监控创建→返回统一的 SubmitActionPlanResult(通过/待审批/被拒+原因)。notify_user、request_approval、request_broker_action 从模型的工具面**移除**——能力还在,但只归平台编排调用。
- **第二次演进:执行权也从 agent 手里拿走(时效性问题)**——收敛成单入口后,最初的设想仍是"审批通过后把批准结果发回 agent,由 agent 调用执行工具完成操作"。推演和测试又发现两个问题:①**plan 本身就是 agent 产出的**——执行再经一遍 agent,等于给参数错误两次进入机会;引入 plan 机制的初衷恰恰是规避 agent 直接调用交易工具时参数传入出错造成损失,执行环节绕回 agent 等于初衷落空;②**时效性**:金融场景里不少操作对即时性极其敏感(盘后波动窗口、突发消息的反应时间),agent 收到批准后还要"想一轮再动手",思考的时间就把最佳时机错过了——**慢思考的执行者天生不适合快窗口的动作**。最终形态:人类一旦收到通知并完成授权,**系统(确定性代码)直接按 plan 执行**,agent 在执行链路里彻底出局;时效敏感性作为**声明式配置写在 plan 上**——授权窗口有 TTL(例如超时 5 分钟未授权自动拒绝执行,防止旧计划在行情变化后被误批误执行),授权后有执行窗口(例如 2 分钟内立即执行,窗口一过计划作废)——**时机判断从模型的临场反应变成 plan 上的一条规则**。
- **优势**——①出错表面积从"任意工具序列×各自参数"收敛为"一个 JSON 的合法性",校验前置到 schema 层;②调用次序从"概率性遵循"变为"确定性编排"——通知不会先于审批、执行必须过门,这不是提示出来的,是结构里写死的;③"重复通知""绕过审批""参数错填"三个故障类别从"偶发"变为**结构性不可能**;④执行延迟与 agent 解耦:人授权到实际执行的间隔从"agent 的思考时长"变为"系统的毫秒级调度",且过期计划想执行也执行不了;⑤ broker 操作和仓位变化只有唯一入口,审计、幂等、dry-run 边界只需实现一处;⑥以后新增行动类型(比如新的通知渠道),不用重新教 agent"次序表"——**平台加编排分支,模型侧零学习成本**。
- **成效**——回归测试里上述三类症状再没出现过;一次 run 的行动轨迹变成"至多一次 submit_action_plan 调用+它的结果",审计从"还原工具序列"变成"读一个结构化记录";用户收到的永远是"分析摘要+审批状态"的一致消息,不再有口径打架;时间敏感型操作从"等 agent 想好"变为"授权即达、超时即废",错失时机和过期执行两类风险同时被结构堵死。
- **及格线 / 加分 / 红旗**:及格=三类症状+收敛方案;加分=机制层归因三条(软约束衰减/并列=自由度/出错面叠加)、执行权移交系统的时效性第二跳、"类别性消失"的表述、新增行动类型的零学习成本;红旗=把原因归为"模型笨换个强的就好"(换模型不改变结构问题)、认为"授权后让 agent 执行也行"(时效与二次出错都不答应)。

**Q13.9【原理·L3·平台】Run 绑定的上下文工具:现在 agent 的工具(search_web/get_market_snapshot/get_account_context)不需要传 agent_run_id、event_id 或文件路径,运行时自动绑定;文件系统工具(ls/read/write/glob/grep)默认也不暴露。这两条各自是怎么演进来的?**

- **人话版**:最早工具签名里全是 ref 参数,模型手填路径和 ID,错一个字符整个 run 静默跑偏;后来 runtime 自动绑定,模型只表达业务问题,这一类错误直接消失。
- **遇到的问题(手填 ref 的时代)**——第一版工具长这样:`collect_evidence(event_ref="/input/event.json", industry_id="semiconductor", run_ref="...")`。测试观察:模型填 event_ref 的大小写、斜杠、扩展名时有出入;event_id 和 event_ref 两种口径混用;最要命的是**这些错误不抛异常**——工具要么按错误 ref 返回了空结果(agent 基于空证据继续分析),要么路径恰好落在一个存在的默认位置(分析了上一个事件)——**静默错误比崩溃贵一个数量级,因为它污染的是结论不是日志**。
- **最初的方案**:prompt 里详细规定 ref 怎么填+工具内部加宽松匹配。**发现不合适**:规定越写越长,遵循率照旧衰减(和 Q13.8 同病:用软约束解决硬问题);宽松匹配把"明确的错"变成了"猜出来的对",更不可审计。
- **换成的方案**——两条:①**run 绑定**:一次事件绑定一次 AgentRun,AgentRuntime 注入工具时自动绑定 agent_run_id/event_id/行业上下文,工具签名里只剩业务参数——`get_market_snapshot(symbols=["NVDA"], fields=["after_hours","volume"])`;②**业务上下文走专用工具**:`get_run_context` 读当前事件/路由上下文/行业 profile/风险策略,大结果(搜索、行情 bars)超过阈值自动存 run artifact 只返回摘要——**工具自己当上下文管理员,不把截断决策推给模型**。
- **为什么文件系统工具不暴露**——另一条演进:最初按 DeepAgents 默认能力把文件系统工具也挂上了,想着"agent 可以自己翻 workspace";观察到的风险是:通用工具的输出体积不可控(一次 read 拉回超大文件=上下文爆炸),语义太通用(什么都能干=什么都可能被诱导干),而且模型自由寻址把我们刚消灭的"手填路径错误"又请了回来。MVP 改为:文件系统工具默认不可见,中间产物(证据板/子报告)由平台工具写入 run artifact,模型看结论不摸文件——**分析型 agent 的上下文是"喂"的,不是"找"的;coding agent 相反,这是任务形态决定的,不是先进落后**(ch03)。
- **成效**——"工具参数错误"从排错日志里的高频类降为基本绝迹(它不是被兜住了,是被消灭了);每次工具调用自动关联 run,审计不需要模型"记得报备";主上下文里再没出现过一次 read 回来的超大文件。
- **及格线 / 加分 / 红旗**:及格=静默错误的机制+run 绑定;加分=宽松匹配为什么更糟、文件系统工具的取舍对照 coding agent、"消灭一类错误而不是兜住它";红旗=认为加参数校验就能解决(校验拦得住格式拦不住"填了错误的合法值")。

**Q13.10【原理·L3·架构】SubAgent 调用纪律:现在 task 的 instruction 是完整任务书(目标/可用工具/"最多 5 次检索"的预算/"不要生成交易计划、不要读取账户"的禁区/artifact_id+safe_summary+gaps 的输出契约),且 SubAgent 不自动继承 MainAgent 的工具。这套纪律是被什么逼出来的?**

- **人话版**:子代理无状态、不继承记忆——你不写它就不知道;早期"帮我查一下 NVDA 财报"这种一句话指令,换来的是跑题、超范围、检索次数失控三连。纪律是把任务书写成合同。
- **遇到的问题**——早期调用检索子代理的 instruction 就一句话:"帮我查一下这期 NVDA 财报的验证材料"。观察到三类失败:①**跑题**:它不知道"验证"要验证什么(公告里的关键数字?市场共识?),返回一大包相关性参差的材料;②**超范围**:检索员顺手在报告末尾给了"建议关注的交易方向"——职责边界糊了,出了问题不知道算谁的;③**预算失控**:没有检索上限的约束,它一轮 run 里的搜索调用次数能差三倍(4 次到 12 次),token 成本不可预测。
- **最初的方案**:给子代理写更长的 system prompt 描述职责。**发现不合适**:system prompt 是角色描述,不是任务指令——每次调用要"查什么、查到什么程度、不能干什么"都不同,静态角色描述承载不了动态任务边界;而且 DeepAgents 的 task 是一次性执行单元,**不继承上一轮对话的任何记忆**,我们以为"说过了"的信息它根本不知道。
- **换成的方案**——instruction 改成**每次调用一份完整任务书**,五要素:目标(验证公告关键数字+找对照+找冲突证据,绑定的具体事件写明)、可用能力(get_run_context/search_web)、**预算(最多 5 次检索——写进任务书才是合同,不写就是没有)**、**禁区(不生成交易计划、不读账户——只有正面目标没有负面清单,子代理会"热心越界")**、输出契约(保存证据板,返回 artifact_id+safe_summary+**gaps**——gaps 显式报告"没查到什么",防止把"没找到"伪装成"不存在")。配套第二条:SubAgent 不自动继承 MainAgent 的 tools/skills,行业包**显式声明**每个 SubAgent 的 tool profile——隐式继承等于"MainAgent 的权限默认下放",权限边界必须是可审计的清单,不是继承树(和 Q13.7 的不做清单配对)。
- **优势**:无状态协作从"靠默契"变成"靠合同"——输出可用性稳定;预算和禁区从软提示变成任务书条款;权限边界按子代理逐个声明,出问题能定位到"哪个 profile 放多了"。
- **成效**:检索调用次数收敛到预算附近(超 5 次的比例显著下降),"检索员给交易建议"这类越界输出在任务书加禁区后消失;gaps 字段让上游能区分"证据不足"和"没找",MainAgent 综合结论时不再把子代理的沉默当确认。
- **及格线 / 加分 / 红旗**:及格=无状态推论+预算/禁区进任务书;加分=system prompt vs 任务书的辨析、gaps 的诚实设计、权限清单化;红旗=认为"模型够聪明不用写这么细"。

**Q13.11【品味·L4·架构】MVP 只固定一个可选 SubAgent(evidence_research_analyst),行情反应、需求判断、风险挑战都由 MainAgent+行业 skill 承接,"更多专家等真实瓶颈出现再加"。你们最初的设想就是这样的吗?什么信号出现才值得拆第二个?**

参考思路:
- **最初的设想恰恰相反**——设计早期画过"专家集群":行情分析师、需求分析师、供应链分析师、风险挑战员、交易计划师五六个 SubAgent,直觉是"分工会带来专业"。**在设计阶段就否决了**,理由是三条硬的:①每个专家需要自己的 instruction 模板、工具面、验收标准——**当时一个都定义不出来**,拆出来的是想象中的形状不是真实的职能(和 Q13.10 呼应:连一个子代理的任务书都要靠迭代踩出来,六个更不可能凭空设计对);②多 agent 的协调开销、上下文传递损耗、调试复杂度是立即支付的,而"专家质量"收益要等很久;③MainAgent 的上下文会被"收集各专家报告"撑爆——拆分反而制造上下文压力。
- **只拆检索的理由**——它是唯一同时满足"边界最清晰(输入问题/输出证据,验收就是证据相关性和覆盖)"和"成本最集中(搜索调用最贵、最需要预算约束)"的子任务——**先拆边际收益最大的那个,其余不动**。
- **值得拆第二个的三个信号**——①MainAgent 上下文被某类中间产物系统性挤占(隔离收益显性化);②同类子任务在多个行业包里重复出现(抽象成熟了);③该子任务的失败模式稳定且可单独验证(**能给它写 verifier 了才配独立成 agent**——没有独立验收标准的模块化是伪模块化,ch07 纪律)。
- **反方也要接**——单 MainAgent 的风险是行业 skill 越挂越多撑爆上下文,所以信号①是双向的:**拆不拆看上下文压力,不看组织架构美学**。
- **及格线 / 加分 / 红旗**:及格=最初设想+否决理由;加分=三信号(尤其 verifier 前置)或"上下文压力决定论";红旗=认为"专家集群更先进"或"永远不拆"。

**Q13.12【品味·L4·架构】项目文档写:"不自建 LangGraph 式状态机,PlannerExecutor 是对 DeepAgents 现成能力的使用纪律。"你们最初考虑过自建吗?这个决策怎么讲?**

参考思路:
- **最初真实考虑过自建**——理由很正当:想要"完全可控"的执行循环(自己定义状态转移、每步拦截、定制恢复语义)。备选项摆在一起:自建状态机 vs 在 DeepAgents 上写使用纪律。
- **发现自建不合适的三个理由**——①**要解决的问题框架已解决**:规划(write_todos)、委派(task)、技能按需加载(skills)、恢复(checkpointer)都是现成能力,自建的增量只在"内部实现可控",而可控的正确姿势是**约束使用方式,不是重写内部**;②**成本不对称**:写纪律是几页文档+若干平台边界,自建是一个要长期养的框架(边角 case、版本升级、文档、新人学习);③**真正的需求是"性质"不是"能力"**——可审计、受控、可恢复这些性质,靠纪律(todo 必须含预算和停止条件、SubAgent 必须走 task 且任务书完整、文件工具不暴露)就能获得,自建并不额外买到。
- **"纪律"长什么样(这句是答题的高光)**——同样的框架,不加纪律是玩具,加了纪律才是生产系统:**框架给你能力,纪律给你性质**。
- **没有全盘依赖的切分**——把不可替换的部分(ToolRegistry、Policy Gate、submit_action_plan 编排、run artifact)留在自己手里,可替换的(执行循环)交给框架——**可替换的依赖框架,不可替换的自建**:框架明天换了,纪律和边界资产都带走。
- **什么时候自建才值得**——三个条件同时满足:框架能力边界真卡住(不是不熟是做不到)、有专职团队养它、差异化就在执行模型本身;大多数项目三个都不满足,自建是简历驱动。
- **及格线 / 加分 / 红旗**:及格=成本不对称+性质 vs 能力;加分=依赖治理切分、"框架给能力纪律给性质";红旗=绝对站队(自建教条或框架教条)。

**Q13.13【原理·L3·平台】审批与执行:金融是高度敏感领域,"是否执行"不能完全交给 agent 决定,而要执行的计划本身就是 agent 产出的——所以审批链的最终形态是:ApprovalPolicyResolver 按用户策略解析出 notify_only / approval_required,人类授权后由系统按 plan 配置的时效性规则直接执行(如超时 5 分钟未授权自动拒绝、授权后 2 分钟内立即执行)。把这条链的演进完整讲一遍。**

- **人话版**:最初想让 agent 自评风险、自己决定要不要请求审批,测试里它几乎总认为自己的计划是低风险的——考生不能自己判卷;后来又发现就算人授权了,让 agent 去执行也扛不住时效性(它要想一轮,时机就过了)。最终形态:agent 只产出 plan,是否执行由人授权,怎么执行由系统按 plan 上的时效规则确定性完成。
- **遇到的问题(两层不信任叠在一起)**——第一层,金融场景里 agent 的"是否执行"决定权不可接受:最初的设计是 prompt 里写"如果你认为这个计划风险高,就调用 request_approval",测试观察:①模型**系统性低估自己动作的风险**,几乎所有计划都被自评为低风险可直接通知;②自评口径漂移,同样杠杆的计划这次要审批下次不要——审批成了概率事件,用户无法建立预期;③多工具并存期(Q13.8),agent 偶尔把"请求审批"整步跳过。第二层,**plan 本身是 agent 产的**:参数错了就是真金白银的损失——这正是引入 plan 机制而不是让 agent 直接调交易工具的原因;而最初"审批通过后由 agent 执行"的设想,又给参数错误开了第二次门,还引入新问题——**时间敏感的操作等不起 agent 的思考**(盘后窗口、突发消息的反应都是分钟级甚至秒级)。
- **为什么发现不合适(机制层)**——①**让被监管者行使监管权,结构上就是空的**:agent 的目标是完成任务,审批恰恰要拦住"完成任务路上风险过大的动作",利益天然对立;②"什么算高风险"是用户的风险偏好,不是模型的技术判断——这是权限问题不是能力问题,模型再聪明也不该有这个决定权;③**慢思考的执行者不适合快窗口的动作**:授权后每多一秒思考,时效性价值就衰减一分,而让 agent"想快一点"又不可控——执行环节要求的是确定性,不是智能。
- **换成的方案(三件套)**——①**policy-as-code**:用户策略(可版本化、可按行业/动作类型细分)进 ApprovalPolicyResolver,每个 ActionPlan 确定解析为 notify_only(低风险高频:日常简报)或 approval_required(高风险:挂起等待);agent 在审批链里只剩两个动作:提交 plan、把结果转述给用户。②**执行权移交系统**:人类收到通知并授权后,由确定性代码直接按 plan 执行,agent 彻底退出执行链路;③**时效性规则声明在 plan 上**:授权窗口带 TTL(例如**超时 5 分钟未授权自动拒绝执行**——防旧计划在行情变化后被误批;不批绝不等于默认批),授权后带执行窗口(例如**2 分钟内立即执行**、窗口过期计划作废——保住时效敏感操作的最佳时机)。配合 broker 侧永远先 dry-run/mock:**"批准"买到的也是一次模拟,不是真实下单权限的立即放开**——分层放开。
- **优势**——审批从概率行为变成确定性规则,规则的责任在人和代码(可审计、可版本化)不在模型临场发挥;执行延迟从"agent 的思考时长"变成"系统的毫秒级调度";时效权衡(等多久、过期怎么处理)从模型的临场反应变成 plan 上**用户可见可调的声明式配置**;notify_only/approval_required 分级把"审批注意力"当稀缺资源经营——全审批会导致审批疲劳,用户闭眼全批,等于没有审批。
- **成效**——"该审批的没审批""过期计划被执行""时机被思考耽误"三个风险类别都变为结构性不可能;用户可预期(同类计划永远走同样的门、同样的时效规则);TTL 防住了"积压的旧审批在行情变化后被误批";时间敏感型操作的授权→执行延迟进入秒级。
- **及格线 / 加分 / 红旗**:及格=利益分离+策略外置+执行权在系统;加分=两层不信任的分析(决定权不可给+plan 本身要设防)、时效规则作为声明式配置的设计、审批疲劳的经营视角;红旗=认为"模型够聪明就可以自批/自执行"、把时效问题当成"让 agent 想快点"能解决的。

**Q13.14【品味·L4·安全】审计设计:不保存完整 chain-of-thought、完整 prompt 和 provider 原始响应,保留"阶段摘要+工具调用摘要+失败路径"。这个取舍的演进过程?不存原始证据,事后争议怎么复核?**

参考思路:
- **最初全量存**——动机单纯:"方便调试",每次 run 的完整 prompt、完整 CoT、raw response 全落库。**三个问题陆续暴露**:①体积——全量是摘要的几十倍,跑了一阵子存储曲线肉眼可见地陡;②合规——完整 prompt 必然含用户数据,原始响应是第三方内容,日志/库里攒这些等于自建合规债(用户有权要求删,你却拿它当调试资产);③最打脸的是——**真排错时几乎用不上它**:定位问题靠的是"调了什么工具、拿到什么结果、走到哪步失败",CoT 里模型"说的"和实际"做的"经常对不上,拿它当证据反而误导。
- **换成什么**——阶段摘要(每个分析阶段的结论)+工具调用摘要(调了什么、关键返回)+失败路径(在哪步、什么错),配 run artifact 存结构化中间产物(证据板、子报告):**审计要的是决策轨迹的可复活性,不是思维全息**。
- **事后争议怎么复核**——replay(Q13.6):事件和 capture 已持久化,争议时把该事件重放走当前管线复核;配合 run 元数据里的 prompt/模型版本记录,差异可解释(Q13.16)。诚实承认局限:重放受非确定性影响,**所以关键决策的 ActionPlan 及审批记录才是唯一权威记录**——复现用于理解,记录用于定责。
- **及格线 / 加分 / 红旗**:及格=三笔账+保留物;加分=「CoT 当证据反而误导」的实战观察、复现 vs 记录的分职;红旗=认为"全存了才叫可审计"(检索不出来的证据等于不存在,那叫数据坟场)。

---

## 13.4 场景排错

**Q13.15【排错·L4·平台】用户反馈"/runtime 页面一直没有新的 Router Agent 输出"。给完整诊断树——最好能讲出你自己踩过的坑。**

排查链路(四层,每层先看什么、再下什么结论):
- **第 0 步:先跑固化好的诊断**——`source-event-replay diagnose --limit 10`:一条命令拿到运行配置(EVENT_BUS_BACKEND 是什么)、DB 计数、未路由新闻、最近路由摘要、kafka topic/group 状态——**先用工具再手工**,这是这个命令被做出来的原因(以前每次都人工考古,Q13.6)。
- **第一层:抓取发生了吗**——scheduler 日志四个标志:`tick started: due_bindings=N`(N=0→绑定没装或 next_run_at 未到:是不是只装了单源 smoke 绑定?装默认包用 `install-semiconductor-defaults`,求稳加 `--no-expansion`);`captured_count>0 但 created=0 duplicate>0`→**正常去重不是故障**(Q13.5,我自己最早把这当 bug 修过一次),等新内容或加 expansion 源;`captured_count=0`→源本身的问题(feed 失效/网络)。
- **第二层:事件到 worker 了吗**——**高频坑:EVENT_BUS_BACKEND=memory 下 scheduler 和 worker 是两个进程,消息根本到不了对方**——内存总线只对单进程 smoke 有意义(这个"假通过"坑我们在测试语义上专门防:跨进程链路必须 kafka);是 kafka 就看 topic 有没有新消息、consumer group 的 lag(diagnose 直接给)。
- **第三层:worker 消费了、结果落库了吗**——worker 日志的消费与路由记录;`event_intake_routed_events` 计数涨没涨:**涨了但 UI 不显示→问题在 read model/前端,不在链路**——"没输出"要区分"没生产"和"没展示",查错层能省一半时间。
- **验证收尾**——修完后 `replay --binding-id ... --limit 5` 重放几条确认全链路通,别只看新事件。
- **及格线 / 加分 / 红旗**:及格=四层+去重误报;加分=memory backend 陷阱、diagnose 优先、"没生产 vs 没展示"的区分;红旗=重启全部服务了事(丢定位信息,下周复发)。

**Q13.16【排错·L3·平台】回放一条历史事件,这次 Router 输出和当初落库的不一样。是 bug 吗?怎么归因,怎么让回放结果可比?**

排查链路:
- **先定性:大概率不是 bug**——三个合法来源:①非确定性(采样/模型随机);②上游漂移(同一 URL 正文被源站改过,Readability 增强结果不同);③版本变化(prompt/模型跨了版本)。
- **归因:对比轨迹,不是对比结论**——调出两次 run 的工具调用摘要和阶段摘要逐项比:**输入 artifact 就不一样→上游漂移;输入一样、调用的工具一样、只输出不同→非确定性;时间点跨了版本记录→版本变化**——先看 run 元数据里的 prompt/模型版本(这个字段就是为这一刻存在的)。
- **让回放可比的工程手段**——`--clear-routed` 清旧结果避免新旧混淆;输入用当时的持久化快照(事件+capture 本来就没变);关键结论对照用多采样分布而不是单次(ch07 的方差处理);prompt/模型版本显式记录在 run 元数据。
- **接受不可比特级复现**——LLM 系统的可复现性是"输入可比+轨迹可比",不是"输出比特相同":**工程目标从确定性改为可解释的差异**;真要定责看 ActionPlan 和审批记录(Q13.14)。
- **及格线 / 加分 / 红旗**:及格=三个合法来源;加分=轨迹对比法、版本元数据、复现 vs 记录分职;红旗=见不同就当 bug 修(越修越乱)。

---

## 13.5 反思与压力测试(开放)

**Q13.17【开放·L4·架构】"如果事件量涨 100 倍、要接 10 个行业,现在的设计哪里先崩?"**

参考思路(按层找瓶颈,每层给"先崩的信号"+"第一刀"):
- **抓取层先崩的通常是最不起眼的扫描循环**——单 scheduler 轮询 due bindings:绑定多了 tick 内抓不完,next_run_at 系统性漂移(信号:tick duration 逼近 tick 间隔);第一刀:按行业分片 scheduler,抓取本来就是 IO 密集可并行。
- **AI 消费层崩在经济学**——每条事件过 Readability+Router+行业分析,100 倍事件=100 倍 token 账单和延迟;第一刀不是加 worker,是**路由前置过滤**(不值得全量分析的事件在路由层早扔)+轻事件/重事件分流+按行业分区消费——ch09 路由经济学的实战版。
- **行业包层:瓶颈从代码转移到内容运营**——插件机制(plugin.yaml+Registry)为多行业备好了,但 10 个行业的源、映射、风险策略要人维护:**扩展性问题会从架构转移到内容质量运营**,这是最容易被低估的一层。
- **审批层的审批疲劳**——量上来后 approval_required 占比必须数据驱动地调,否则用户闭眼全批;分级要细到动作类型/行业。
- **答法要点**——每层"信号→第一刀"成对出现,展示 scalability 的思考方法而不是背答案;能主动说"哪层我有数据、哪层我在猜"是 L4 信号。
- **及格线 / 加分 / 红旗**:及格=两层+信号+修法;加分=路由前置过滤的经济学、瓶颈转移到内容运营;红旗=只答"加机器/上 K8s"。

**Q13.18【开放·L3·综合】"这个项目里你踩过的最大的坑是什么?"——用真实演进讲一个完整的坑。**

参考思路(四段模板+两个现成的深度素材):
- **模板:现象→误判→真实根因→固化**——误判段最值钱(证明你诚实):第一反应错在哪、为什么当时会那么想;固化段不能少:**没有固化的坑还会再踩,固化了坑才变成资产**。
- **素材一(推荐主讲):多工具行动面**——现象:测试中 agent 不遵循 prompt、工具调用结果有偏差、参数传不一致;误判:一开始以为是 prompt 写得不够细,又加了一倍的场景说明——遵循率没有质变;真实根因:工具并列本身就是自由选择空间,软约束管不住工具面;固化:submit_action_plan 单入口+schema 校验,平台编排次序——三类症状类别性消失(完整版 Q13.8)。**讲这个坑的加分点在于误判段**:多数人会先去改 prompt,承认自己也改过,才是真实的过程。
- **素材二:memory backend 的"假通过"**——现象:本地测试链路全绿,部署后 scheduler/worker 之间消息死活不通;误判:怀疑 kafka 配置/网络,查了半天;真实根因:测试时 EVENT_BUS_BACKEND=memory,单进程里它当然通,分进程它根本没有跨进程语义——**测试通过≠链路正确,是 backend 语义决定测试覆盖了什么**;固化:文档明确"跨进程链路必须 kafka,memory 仅限单进程 smoke",并配 run_once 分层冒烟入口。
- **和 AGENTS.md 的连接**——这个项目的 AGENTS.md 明文规定沉淀纪律:一次性调试结论不进长期规则、重复出现的判断必须沉淀(进 AGENTS.md/docs/design/openspec 分层)、下层规则只收紧不放宽——**坑的价值靠沉淀机制兑现,不靠个人记性**(ch10 反脆弱的项目实证:同类错误第三次出现前,系统里已经多了规则)。
- **讲坑的禁忌**——不推责给模型/框架;不选假坑;没有误判段的坑故事不可信,没有固化段的坑故事没成长。
- **及格线 / 加分 / 红旗**:及格=四段完整;加分=误判段真实+固化物可指认+沉淀机制自觉;红旗=坑里没有误判(不真实)或没有固化(没成长)。
