# ch13 · 项目深挖:QuantAgent(开源量化 Agent 系统)

> 本章素材来自开源项目 QuantAgent(github.com/BqLee-AI/QuantAgent,Python,仓库内容截至 2026-06):一套事件驱动的量化分析 agent 系统——外部事件采集(RSS 等 source 插件)→ 事件总线 → 行业路由 → 行业 MainAgent 受控分析 → 评分/辩论(作为工具)→ Policy Gate 审批 → 通知与受控执行。
> 用法:这是"候选人带着自己的项目来面试"的标准深挖题集——面试官照着问,项目作者照着准备。所有题干自包含,不读仓库也能看懂;答案基于仓库真实设计。
> 答案读法:**加粗句连读就是可背的骨架**;细节与出处放「补充弹药」。

---

## 13.1 项目叙事(怎么讲项目)

**Q13.1【开放·L3·综合】"用 30 秒介绍一下 QuantAgent。"然后面试官说"展开讲讲,给你 3 分钟。"**

参考思路(项目作者的答法):
- **30 秒版(一句话定位+一个差异化点)**——"QuantAgent 是一套事件驱动的量化分析 agent 系统:外部事件(比如财报、行业新闻)进来后,经过行业路由交给对应行业的 MainAgent 做受控分析,产出的不是自动交易,而是一份**结构化行动计划(ActionPlan)**,经过用户策略的审批门(Policy Gate)后才触发通知或模拟执行——**它是'分析→行动意图→受控执行'的平台,不是交易机器人**"。这个"不是什么"的对比就是差异化。
- **3 分钟版的四段结构**——问题(为什么做:信息驱动的研究工作流里,采集/分析/行动是割裂的,通用 chat agent 干不了这个活因为不可审计不可扩展)、方案(事件驱动骨架+行业包插件化+DeepAgents 做 agent harness+审批门控)、最有含金量的两个决策(挑 Q13.8/Q13.9 讲 30 秒)、当前状态(半导体行业包跑通 RSS→分析→Discord 通知全链路)。
- **讲项目的通用纪律**——按"问题→方案→决策→状态"讲,不按技术栈清单讲:面试官想听的是判断,不是"FastAPI+React+PostgreSQL"的背书。
- **及格线 / 加分 / 红旗**:及格=30 秒版有定位有边界;加分="不是什么"的主动界定+四段结构;红旗=从技术栈开始背、3 分钟还没讲到任何一个决策。

**Q13.2【开放·L4·综合】"这个项目里你做的最难的技术决策是什么?"——讲两三个真实的。**

参考思路(仓库里现成的三个好答案):
- **决策一:行动提交只留一个口**——MainAgent 不直接拥有通知/审批/broker 工具,只能调 `submit_action_plan` 提交结构化意图,由平台编排审批和执行:**难在抵抗"给模型多点工具更灵活"的直觉**——收敛入口换来的是审计、幂等和防重复通知(ch13 Q13.9 展开)。
- **决策二:不自建执行框架**——项目明确"不自建 LangGraph 式状态机",PlannerExecutor 是对 DeepAgents 现成能力(write_todos/task/skills)的使用纪律:**难在承认自己要的是纪律不是框架**——自建状态机的诱惑在"可控",代价是长期维护(ch13 Q13.12)。
- **决策三:MVP 只拆一个 SubAgent**——只固定 evidence_research_analyst(检索子代理),行情反应、风险挑战等都由 MainAgent+行业 skill 承接,更多专家 agent"等真实瓶颈出现再加":**难在克制**——多 agent 系统的默认病是一上来就拆专家集群(ch13 Q13.11)。
- **讲决策的模板**——选项→判据→取舍(放弃了什么)→事后看对不对:四个要素齐的决策故事才有说服力。
- **及格线 / 加分 / 红旗**:及格=两个决策+取舍;加分=模板四要素+"事后看"的复盘;红旗=把"用了什么技术"当决策讲(没有比较过备选的不叫决策)。

**Q13.3【品味·L4·综合】面试官挑衅:"这和直接把新闻丢给 ChatGPT 问'该不该买 NVDA'有什么区别?"**

参考思路:
- **四个维度的差别,每个都落到机制**——可扩展性(事件驱动+行业包插件:加一个行业是加一个插件包,不是重写 prompt);可审计(每次分析有 run 记录、工具调用摘要、阶段摘要,chat 没有结构化轨迹);受控行动(agent 只能提交 ActionPlan 过 Policy Gate,chat 的"建议"没有执行边界也就没有责任边界);一致性(同一事件走同一条受控管线,chat 每次都是即兴发挥)。
- **诚实承认重叠的部分**——分析内核确实也是 LLM 调用:差别不在"聪明程度",在**工程化**——把一次性的对话能力变成可运营的系统;这个承认不丢分,反而立住"你分得清 demo 和产品"。
- **一句收尾**——"chat 是引擎,QuantAgent 是带变速箱和刹车的车"。
- **及格线 / 加分 / 红旗**:及格=两维度+机制;加分=承认重叠+"demo vs 产品"的定位;红旗=贬低 chat 模式(它就是这个系统的子集,不是对手)。

---

## 13.2 事件驱动骨架(八股/原理)

**Q13.4【原理·L4·平台】QuantAgent 为什么把 api、worker、scheduler 拆成三个独立进程,中间用事件总线(Kafka)连接,而不是做一个单体服务?连数据库迁移(migrate)都做成显式的一次性服务而不是 API 启动时自动跑——这一串"拆+显式"的决策逻辑是什么?**

- **人话版**:三个角色的负载模型完全不同——api 要快、worker 要稳、scheduler 要准,放一个进程里就互相拖累;事件总线让它们只共享事件契约不共享生命周期。migrate 显式化是同一条逻辑:有副作用的动作不该藏在"启动"里面。
- **主线(照这条线讲)**:
  1. **负载模型不同**——api 是低延迟请求响应、worker 是常驻消费(抓取增强+路由+AI 分析,慢而重)、scheduler 是周期 tick:**混在一个进程里,一次 AI 分析的卡顿就会拖死 HTTP 接口**;拆开后各自扩缩容、各自故障隔离(worker 挂了不影响抓取入队,事件还在总线上)。
  2. **事件总线是解耦边界**——scheduler 发布 `source.event.captured`,worker 消费后发布 `industry.analysis.requested`:模块间只依赖事件契约不依赖彼此的实现和存活状态;**这也解释了为什么 memory backend 只能用于单进程测试**——内存总线没有跨进程语义,把它当真总线用,测试就会测出"假通过"。
  3. **显式 migrate 的同一逻辑**——迁移是有破坏性副作用的动作,藏在 `api` 启动里意味着"起个服务"可能隐式改库:**副作用的动作要显式调用(`--profile migration` 一次性跑),无副作用的才能自动化**——这和 agent 安全里"结构性禁止优于叮嘱"是同一个审美(ch08)。
  4. **代价也要说**——三进程的运维成本(本地要同时起 db+kafka+scheduler+worker 才能跑通主链路)、排错跨进程:所以项目配了 diagnose 命令和单进程 smoke 入口(run_once)对冲——**拆分不是免费的,配套的可调试性投资是拆分的对价**。
- **补充弹药(追问再掏)**:
  - **技术细节**:Compose 里 db 绑 127.0.0.1:15432 避开本机 5432;kafka 用 apache/kafka-native:4.0.0(没有确认可拉的 alpine tag 就不猜)——这些小决策反映"本地开发体验"被认真设计过。
  - **参照**:ch12 Q12.1 平台分层题的同款逻辑,QuantAgent 是它的具体而微。
- **及格线**:负载模型+故障隔离两条。
- **加分**:memory backend 的测试语义;migrate 显式化的同一决策逻辑;拆分的对价。
- **红旗**:认为拆进程是"为了微服务而微服务";说不清 memory backend 为什么不能跨进程。

**Q13.5【原理·L3·平台】主链路是 RSS 抓取 → 落库 RawEvent(URL 去重)→ 发 `source.event.captured` → worker 消费做正文增强和路由。监控看到日志 `captured_count>0 但 created=0、duplicate>0`,这说明什么?URL 去重为什么放在入库这一层而不是 worker 消费时再做?**

- **人话版**:这个日志组合说明抓取正常、去重正常——都是抓过的旧闻,不是故障。去重放入库层是因为入库是唯一入口:同一 URL 从两个 binding 抓到、回放重推、源重复推送,都能在这里拦住,下游永远只见到"新事件"。
- **主线(照这条线讲)**:
  1. **先会读日志**——captured>0 说明 RSS 真的抓到了条目;created=0 duplicate>0 说明全部命中去重:**这是正常行为的特征,新人最容易把它当"worker 没工作"误修**——区分"正常静默"和"故障静默"是运维这个系统的第一课。
  2. **去重在入库层的幂等意义**——上游的一切重复(binding 之间源重叠、抓取间隔内源没更新、回放重推)在落库时收敛为一条 RawEvent:**事件只增一次,下游所有消费者(worker、审计、UI)不用各自实现幂等**——幂等收口在越早的层,下游越简单。
  3. **设计反问(面试官爱追)**——如果去重放 worker:多个 worker 实例并发消费同一条事件怎么办?回放时旧事件会不会被处理两遍?——每层都要自己判重,复杂度翻倍:**去重是"单一事实入口"模式的标准应用**。
- **补充弹药(追问再掏)**:
  - **表结构线索**:raw_events 与 raw_event_captures 分开存(事件与"从哪个 binding 抓到"的捕获记录分离)——同一事件多源捕获不产生多条事件。
  - **连接**:ch07 Q7.14 证据绑定工件状态——审计口径要能回答"这条分析基于哪个 capture"。
- **及格线**:日志解读+入口幂等。
- **加分**:worker 侧判重的复杂度论证;事件/捕获记录分离。
- **红旗**:看到 created=0 就去重启 worker。

**Q13.6【原理·L3·平台】项目内置了 `source-event-replay` 工具:diagnose 能输出运行配置、DB 计数、最近未路由的新闻和 Kafka 诊断;replay 支持把已入库的事件重新推回总线重走全链路,还有 `--dry-run` 和 `--clear-routed`。为什么一个事件驱动系统要内置"回放"?这套设计里每个开关防什么坑?**

- **人话版**:事件驱动系统出了问题,最难的是"到底哪一环没走"——回放和诊断就是把链路从黑盒变白盒。回放还是免费的回归测试:改了路由逻辑或 agent prompt,拿历史事件重跑一遍就知道变好还是变坏。
- **主线(照这条线讲)**:
  1. **diagnose 先分层定位**——输出按"有没有新抓取→有没有入队→worker 消没消费→AI 结果落没落库"排:把"没输出"这个症状拆成四个可判定的子问题——**诊断工具的本质是把排查链路固化成命令,让人不用每次凭记忆查五张表**。
  2. **replay 的两个价值**——排错(哪条事件卡了,重推它单独验证)和回测(改了 prompt/路由策略后,历史事件重放=免费的 eval 集)——**"事件已持久化"让系统天然可重放,这是事件溯源架构送的红利**(ch06 检查点话题的分布式版)。
  3. **两个开关各防一个坑**——`--dry-run`:回放有真实副作用(重新触发 AI 分析花钱、重新发通知打扰用户),先预览再执行;`--clear-routed`:不清掉旧的路由结果,回放的新结果和旧的叠在一起,UI 上分不清哪次是哪次——**回放工具自己也要幂等和可观察**。
- **补充弹药(追问再掏)**:
  - **对照**:Claude Code 的 resume/fork、dsh 的 replay 同族(ch09 Q9.10)——"会话可重放"正在成为 agent 系统的标配能力。
  - **边界**:replay 能保证事件一样,不能保证模型输出一样(非确定性,Q13.16 展开)。
- **及格线**:回放的两个价值+dry-run。
- **加分**:clear-routed 的幂等考量;"事件溯源红利"的抽象。
- **红旗**:认为回放是锦上添花的调试便利(它是架构性质的能力)。

**Q13.7【原理·L3·架构】行业 MainAgent 有一份明确的"不做清单":不直接读写数据库、不直接调 provider SDK、不直接调 broker、不绕过 ToolRegistry 和 Policy Gate、不保存完整 chain-of-thought/完整 prompt/原始响应。逐条讲讲每条防的是什么。**

- **人话版**:这是给 agent 划权限边界——每条"不做"对应一类真实事故:绕过 ToolRegistry 就是注入面失控,直连 broker 就是越权交易,存完整 CoT 就是审计和隐私灾难。
- **主线(照这条线讲)**:
  1. **不直接读写数据库**——agent 拿到 SQL 就是拿到了一切:越权读、误写、把 secret 拉进上下文;数据访问走受控工具(get_account_context 返回压缩摘要而不是裸表),**工具是 agent 的数据防火墙**(ch10 Q10.15 外部依赖改造的同款原则)。
  2. **不直接调 provider SDK / broker**——SDK 升级、重试、鉴权这些复杂性会散进 agent 行为里;broker 更是红线:**"表达交易意图"和"执行交易"必须是两个人**(Q13.9 的 Policy Gate 就是这条的制度化)。
  3. **不绕过 ToolRegistry / Policy Gate**——Registry 是工具的准入和审计点(谁挂的工具、描述是什么、有没有被投毒);绕过它=注入攻击面失控+审计断链。
  4. **不保存完整 CoT/prompt/原始响应**——存了就是三重负担:存储成本(每 run 全量)、隐私合规(用户数据进日志)、法律风险(CoT 被 subpoena);**替代方案是"阶段摘要+工具调用摘要"——审计要的是决策轨迹,不是思维全息**(这个取舍单独成题,Q13.14)。
- **补充弹药(追问再掏)**:
  - **实现层**:这些边界靠 runtime 强制(工具注入白名单),不靠 system prompt 叮嘱——又是"结构性禁止"(ch08)。
  - **对照**:这份清单和 ch08 Q8.6"上下文不是安全边界"同源,都是"给能力不给钥匙"。
- **及格线**:五条里四条讲出对应风险。
- **加分**:工具=数据防火墙;CoT 那条的替代方案;靠 runtime 不靠叮嘱。
- **红旗**:认为这些边界会"限制 agent 能力上限"(上限和边界是两回事)。

---

## 13.3 Agent 设计含金量(原理/品味)

**Q13.8【原理·L4·平台】`submit_action_plan`:MainAgent 唯一的"行动类"工具。它提交结构化的 ActionPlan(订单草案/风控/监控建议/用户摘要),内部走 ApprovalPolicyResolver → Policy Gate → 审批/通知/broker dry-run → 返回结果。为什么不直接给模型 notify_user、request_approval、request_broker_action 这三个工具?这个设计的本质是什么?**

- **人话版**:agent 的职责是提出意图,不是编排平台状态机——通知、审批、执行合并成一个入口,重复消息、审批绕行、重复下单这三类事故从结构上就不可能发生。
- **主线(照这条线讲)**:
  1. **三个工具分开给的三个事故**——notify+approval 分开:agent 一边通知用户"建议买"一边提交审批,用户收到两条口径不一的消息;审批工具直接给:审批必须关联平台的 ActionRequest/policy/过期策略,agent 自建的审批没有审计地位;broker 直接给:**没有 Policy Gate 和幂等的下单入口=agent 可以重复下单**——三个工具各自方便,组合起来就是灾难。
  2. **本质一:意图与执行分离**——ActionPlan 是声明式的(要什么),平台决定怎么走(先过什么门、发什么通知):**执行路径的复杂度(策略解析/分支/重试)留在确定性代码里,模型的输出面收敛到一个 schema**——出错的表面积从"任意工具序列"缩到"一个 JSON 的合法性"。
  3. **本质二:单入口才有审计**——"broker 操作和仓位变化只有一个入口":审计、幂等、dry-run/mock 边界都只需要在一处实现——**入口的数量和系统的可审计性成反比**。
  4. **对照通用模式**——这就是 agent 圈"行动闸门(Stop-gate/Action-gate)"的行业化版本(ch04 的停止条件四层):llm 生成意图 → 确定性代码验证和执行;和 Devin 的"计划确认后执行"、Claude Code 的权限门同构。
- **补充弹药(追问再掏)**:
  - **模式名**:policy-as-code 的 resolver(自动审批来自用户预设策略,不来自 agent 自行判断)——"agent 不能自己给自己开权限"。
  - **输出契约**:SubmitActionPlanResult 回给 agent 的是执行状态(通过了/待审批/被拒),agent 拿它决定怎么说人话总结。
- **及格线**:三个事故+意图/执行分离。
- **加分**:出错表面积的量化表述;单入口与可审计性;和 Stop-gate 的同构。
- **红旗**:认为三个工具分开给"更灵活"(灵活的代价没算)。

**Q13.9【原理·L3·平台】工具设计细节:QuantAgent 的 agent 工具(search_web/get_market_snapshot/get_account_context)不需要模型传 agent_run_id、event_id 或文件路径——运行时自动绑定;模型也不暴露 ls/read_file/write_file/glob/grep 这些文件系统工具。这两个设计各解决什么问题?**

- **人话版**:让模型手填 ID 和路径,它就会填错——运行时绑定让工具"知道自己属于哪次运行";文件系统工具太通用,通用工具的滥用面最大,MVP 干脆不给。
- **主线(照这条线讲)**:
  1. **run 绑定:消灭一类错误而不是兜住它**——模型手填 `event_ref="/input/event.json"` 这类参数,错一个字符整次分析就跑偏,而且错得静默;runtime 注入工具时自动绑定运行上下文,**模型只表达业务问题("查 NVDA 的盘后和成交量"),不表达寻址**——"最小知识原则"的 API 设计版:调用方不该知道也不该填它不需要知道的。
  2. **附带收益:审计天然成立**——每次工具调用自动关联到 run,不需要模型"记得报备"自己的 run_id。
  3. **不暴露文件系统工具:收敛自由度**——ls/grep/write 这类工具的输出体积不可控(上下文爆炸)、语义太通用(什么都能干=什么都可能被诱导干);中间产物走 run artifact(证据板/子报告),由平台工具写入,**模型看得到结论,不直接摸文件系统**。
  4. **大结果自动降级**——搜索结果太大自动存 artifact 返回摘要:**工具自己是上下文管理员,不把"要不要截断"的决策推给模型**(ch03 工具设计方法论的项目版)。
- **补充弹药(追问再掏)**:
  - **什么时候可以放开 FS 工具**:等出现"模型必须自主浏览文件"的真实场景,再按白名单目录开放——和 coding agent 相反:分析型 agent 的上下文是"喂"的,不是"找"的。
  - **对照**:coding agent 里 Grep/Read 是核心工具,这里是禁区——**工具面由任务形态决定,没有普适答案**(ch03 的主题)。
- **及格线**:两类错误+自由度收敛。
- **加分**:审计附带收益;工具自管上下文;"任务形态决定工具面"的对比。
- **红旗**:认为不给文件工具是"功能残缺"。

**Q13.10【原理·L3·架构】SubAgent 调用纪律:DeepAgents 的 task 是一次性无状态调用,MainAgent 调它时 instruction 必须自带完整任务说明(目标/可用工具/"最多 5 次检索"的预算/"不要生成交易计划,不要读取账户"的禁区/输出契约);SubAgent 也不自动继承 MainAgent 的工具和技能,每个 SubAgent 显式声明 tool profile。为什么这么多"必须显式"?**

- **人话版**:子代理没有记忆也没有上下文——你不写的话它就不知道;不显式声明工具,它就" MainAgent 有什么我有什么",权限边界就漏了。显式是无状态协作的纪律。
- **主线(照这条线讲)**:
  1. **instruction 完整性:无状态的直接推论**——task 创建的执行单元不继承上轮记忆,"我记得跟它说过"是不存在的:**一次调用一份完整任务书**(目标/预算/禁区/输出契约)——写得越完整,子代理的输出越可用;这是 ch05 subagent 上下文隔离机制在项目里的落地形态。
  2. **预算写进任务书**——"最多 5 次检索"不是 TODO 是合同:**把预算声明在 instruction 里,模型才会把预算当约束**——软约束不写就是没有。
  3. **禁区写进任务书**——"不要生成交易计划,不要读取账户":子代理的职责边界用负面清单+正面输出契约双保险;**只有正面目标没有负面禁区,子代理会"热心越界"**(检索员顺手给了交易建议,责任边界就糊了)。
  4. **工具不自动继承:最小权限的实例化**——隐式继承=" MainAgent 的权限默认下放",和 Q13.7 的不做清单冲突;**每个 SubAgent 显式 tool profile,权限边界才是可审计的清单而不是继承树**。
- **补充弹药(追问再掏)**:
  - **输出契约**:返回 artifact_id+safe_summary+gaps——artifacts 是中间产物的真源,safe_summary 是进主上下文的"压缩接口",gaps 显式报告"没查到什么"(防止把"没找到"伪装成"不存在")。
  - **对照**:ch05 Q5.4 同机制;这里的增量是"预算/禁区/契约三件套写进 instruction"的可操作化。
- **及格线**:无状态推论+工具不继承。
- **加分**:预算/禁区作为合同;gaps 字段的诚实设计。
- **红旗**:认为"模型应该聪明到不用写这么细"。

**Q13.11【品味·L4·架构】MVP 只固定一个可选 SubAgent(evidence_research_analyst,检索子代理),行情反应、需求判断、风险挑战这些"专家职能"都由 MainAgent 的 prompt+行业 skill 承接,文档明说"更多专家 SubAgent 等真实瓶颈出现后再加"。你同意这个克制吗?什么信号出现才值得拆第二个?**

参考思路:
- **同意的理由**——多 agent 的成本前置而收益后置:协调开销、上下文传递损耗、调试复杂度立刻付,而"专家质量"的提升依赖你先知道"专家该长什么样"——**MVP 阶段你还不知道,拆了也是拆成自己想象的形状**;检索子代理先拆,是因为它边界最清晰(输入问题/输出证据)且成本最集中(搜索调用贵)。
- **值得拆的信号(三个可操作的)**——①MainAgent 的上下文被某类中间产物塞爆(检索结果污染主上下文→上下文隔离的收益显性化);②同一类子任务在多个行业包里重复出现(抽象成熟了);③主 agent 在该子任务上的失败模式稳定且可单独验证(能给它写 verifier 了——**没有独立验收标准的模块不配独立成 agent**,ch07 纪律)。
- **反方也要接一手**——单 MainAgent 的风险是"一个上下文装所有职责",行业 skill 越加越多会撑爆:所以拆分信号①是双向的——**拆不拆看的是上下文压力,不是组织架构美学**。
- **及格线 / 加分 / 红旗**:及格=同意+至少两个信号;加分=三信号或"上下文压力决定论";红旗=认为"专家集群更先进"或"永远不拆"。

**Q13.12【品味·L4·架构】项目文档写:"不自建 LangGraph 式状态机,PlannerExecutor 是对 DeepAgents 现成能力(write_todos/task/skills)的使用纪律和平台边界约束。"你怎么理解"用框架的纪律"和"自建框架"?什么时候后者才值得?**

参考思路:
- **这句话的内核**——要解决的问题(规划/委派/技能加载)框架已经解决,自建的冲动多半来自"想要完全可控":**但可控的正确姿势是约束框架的使用方式(纪律),不是重写它的内部**——写纪律的成本是几页文档,写框架的成本是长期维护负债。
- **"纪律"具体长什么样**——todo 必须反映分析阶段/工具预算/停止条件(不是随意清单);SubAgent 必须走 task 且 instruction 完整;文件系统工具不暴露:同样的框架,不加纪律就是玩具,加了纪律才是生产系统——**框架给你能力,纪律给你性质**(可审计/可控/可恢复)。
- **什么时候才值得自建**——三个条件同时满足:框架的能力边界确实卡住你(不是"不熟"是"做不到")、你有专职团队养它、你的差异化就在执行模型本身(你是做 agent 平台的公司)——**大多数项目三个都不满足,自建是简历驱动**。
- **诚实的一面**——依赖框架也有锁死风险:所以 QuantAgent 把平台边界(ToolRegistry/Policy Gate/artifact)留在自己手里,框架可换、纪律带走——**可替换的部分依赖框架,不可替换的部分自建**,这是依赖治理的正确切分。
- **及格线 / 加分 / 红旗**:及格=纪律 vs 重写的成本对比;加分="能力 vs 性质"的表述/依赖治理切分;红旗=绝对站队(自建教条或框架教条)。

**Q13.13【原理·L3·平台】审批流:ActionPlan 提交后由 ApprovalPolicyResolver 按用户策略解析出 notify_only(只通知)或 approval_required(需审批)两种模式,自动审批的依据是用户预设策略而不是 agent 自己的判断。为什么"agent 不能自己给自己开权限"?notify_only 和 approval_required 的分支设计解决什么?**

- **人话版**:谁行使权力谁就不能是权力的受益者——agent 提出行动,如果审批也由它判断,那审批就是空的。两种模式把"通知你"和"请求你批准"分成两条明确路径,用户策略决定走哪条。
- **主线(照这条线讲)**:
  1. **利益分离原则**——agent 的目标是"完成任务",审批的存在恰恰是拦住"完成任务路上风险过大的动作":**让 agent 自评风险等于让考生自己判卷**——概率上它会低估自己动作的风险(过度自信是模型通病)。
  2. **策略外置:policy-as-code**——"什么级别的操作要审批"是用户的风险偏好,写在用户策略里(可审计/可版本化/可细分到行业和动作类型),不是 agent 的现场判断:**规则的责任在人和代码,不在模型的临场发挥**——和 ch08"权限层与模型判断正交"同构。
  3. **两分支的现实意义**——notify_only:低风险高频率(日常简报),全审批会把用户烦死,审批疲劳的结果是全部闭眼批——**分级是把"审批注意力"当稀缺资源来经营**(ch10 Q10.11 风险配比的审批版);approval_required:高风险动作挂起等待,带过期策略(用户不批不等于默认批)。
  4. **和 dry-run 的配合**——broker 侧永远先 dry-run/mock:审批通过后的执行也先出模拟单,**"批准"买到的也是一次模拟,不是真实下单权限的立即放开**——分层放开权限。
- **补充弹药(追问再掏)**:
  - **过期策略**:审批请求带 TTL,过期作废重提——防"积压的旧审批在行情变化后被误批"。
  - **对照**:Claude Code 的权限模式(allowlist/plan mode)同族——生产 agent 的共同收敛点:意图生成可以自动化,权力行使不能。
- **及格线**:利益分离+策略外置。
- **加分**:审批疲劳的经营视角;TTL;分层放开。
- **红旗**:认为"模型够聪明就可以自批"。

**Q13.14【品味·L4·安全】审计设计:QuantAgent 不保存完整 chain-of-thought、完整 prompt 和 provider 原始响应,保留的是"阶段摘要+工具调用摘要+失败路径"。这个取舍你怎么评价?不存原始证据,事后争议怎么复核?**

参考思路:
- **为什么砍**——三笔账:存储(每 run 全量 CoT/raw response 的体积是摘要的几十倍,跑起来才知道心疼)、隐私合规(完整 prompt 里必然带用户数据,原始响应是第三方内容——日志里攒这些=自建合规债)、以及 CoT 的证据价值低(模型嘴里的推理和实际决策的相关性存疑,审计要的是"做了什么"不是"它说它想了什么")。
- **保下来的更聪明**——阶段摘要+工具调用摘要+失败路径:**决策轨迹的可复活性**比思维全息更有用——知道它调了什么工具、拿到什么结果、走到哪一步失败,就能定位问题;配合 run artifact(证据板/中间报告),"证据"存在结构化产物里而不是聊天记录里。
- **复核争议的现实方案**——replay(Q13.6):事件已持久化,争议时重放该事件走当前管线复核——**复核靠"可重放的输入+结构化的轨迹",不是靠存全量对话**;诚实承认局限:重放受非确定性影响(Q13.16),所以关键决策的 ActionPlan 及其审批记录是唯一权威记录。
- **及格线 / 加分 / 红旗**:及格=三笔账+保留了什么;加分=replay 复核+权威记录的界定;红旗=认为"全存了才叫可审计"(全存叫数据坟场,检索不出来的证据等于不存在)。

---

## 13.4 场景排错

**Q13.15【排错·L4·平台】用户反馈"/runtime 页面一直没有新的 Router Agent 输出"。给完整诊断树。**

排查链路:
- **第一层:抓取有没有发生**——scheduler 日志找四个标志(tick started 的 due_bindings / fetch succeeded 的 captured_count / persisted 的 created-duplicate / published 的 item_count):due_bindings=0 → 绑定没装或 next_run_at 没到(是不是只装了 smoke 绑定,单源且全被去重);captured>0 但 duplicate 全命中 → **正常去重不是故障**(Q13.5),等新内容或装 expansion 源。
- **第二层:事件有没有到 worker**——EVENT_BUS_BACKEND 是不是 kafka(内存 backend 下 scheduler 和 worker 分进程=消息根本到不了);kafka topic 有没有消息、consumer group 有没有 lag(diagnose 命令直接给)。
- **第三层:worker 消费了有没有产出**——worker 日志的消费和路由记录;AI intake 落库计数(event_intake_routed_events)涨没涨:涨了但 UI 没显示 → 问题在 read model/前端,不在链路——**"没输出"要区分"没生产"和"没展示"**。
- **工具优先**——一条 `source-event-replay diagnose --limit 10` 把上面三层的信息一次给全:**先用固化好的诊断命令,再手动查**。
- **及格线 / 加分 / 红旗**:及格=三层分层+去重误报识别;加分=memory backend 的进程语义陷阱+diagnose 优先;红旗=直接重启全部服务(丢失定位信息,下周复发)。

**Q13.16【排错·L3·平台】你回放(replay)一条历史事件,这次 Router 输出和当初落库的不一样。是 bug 吗?怎么让回放结果可比?**

排查链路:
- **先定性:大概率不是 bug**——非确定性(采样温度/模型本身的随机性)、上游漂移(同一 URL 的正文被源站更新过,Readability 结果不同)、模型或 prompt 版本变了:三个都是"合法的不同",先区分再谈修复。
- **归因手段**——对比两次 run 的工具调用摘要和阶段摘要:调的工具一样、输入 artifact 一样、只输出不同 → 非确定性;输入 artifact 就不一样 → 上游漂移;时间点跨了版本发布 → 版本变化——**对比轨迹而不是对比结论**。
- **让回放可比的工程手段**——`--clear-routed` 清掉旧结果避免混淆;回放固定用当时的输入快照(事件+capture 已持久化,天然可用);关键结论的对照基准用"多采样+分布"而不是单次(ch07 的方差处理);prompt/模型版本显式记录在 run 元数据里。
- **接受不可完全复现**——LLM 系统的可复现性是"输入可比+轨迹可比",不是"输出比特相同":**工程目标从"确定性"改为"可解释的差异"**。
- **及格线 / 加分 / 红旗**:及格=三个合法来源;加分=轨迹对比法+版本元数据;红旗=见不同就当 bug 修(越修越乱)。

---

## 13.5 反思与压力测试(开放)

**Q13.17【开放·L4·架构】面试官追问:"如果事件量涨 100 倍、要接 10 个行业,现在的设计哪里先崩?"**

参考思路:
- **抓取层**——单 scheduler 扫描 due bindings:绑定多了 tick 内抓不完,next_run_at 漂移;抓取是 IO 密集,scheduler 要么分片要么按行业拆——**最先崩的通常是最不起眼的扫描循环**。
- **AI 消费层**——worker 每个 RSS 条目都要过 Readability+Router Agent+行业分析:100 倍事件=100 倍 token 账单和延迟;按行业/按事件类型分区消费、轻事件(路由打分)和重事件(全分析)分流、**路由前置过滤(不值得分析的早点扔)是经济学关键**——ch09 路由题的实战版。
- **行业包层**——10 个行业=10 份行业 profile/skill/工具面:插件机制已经备好了(plugin.yaml+Registry),但**行业包的质量运营**(每个行业的源、映射、风险策略要人维护)成为新瓶颈——扩展性问题会从代码转移到内容运营。
- **审批层**——approval_required 的量上来后审批疲劳:分级策略必须更细(按动作类型/金额/行业),notify_only 的占比要数据驱动地调。
- **答法要点**——按层找瓶颈、每层给"先崩的信号"和"第一刀的修法":**展示的是 scalability 的思考方法,不是背好的答案**。
- **及格线 / 加分 / 红旗**:及格=两层的瓶颈+修法;加分=路由前置过滤的经济学+瓶颈转移到内容运营的洞察;红旗=只答"加机器/上 K8s"(没分层分析)。

**Q13.18【开放·L3·综合】"这个项目里你踩过的最大的坑是什么?"——用项目的真实机制讲一个"踩坑→固化"的完整故事。**

参考思路(项目里现成的素材方向):
- **模板:现象→误判→真实根因→固化**——好故事的四段:当时看到什么(症状)、第一反应错在哪(误判——这段最值钱,证明你诚实)、根因是什么(机制层)、现在系统里多了什么(固化成规则/工具/文档,不再靠记性)。
- **可用的真实素材方向**——本地链路"跑通了其实没跑通"(EVENT_BUS_BACKEND=memory 下分进程测试的假通过 → 固化为"跨进程链路必须 kafka"的文档语义);重复消息/重复处理(→ 去重收口在入库层+replay 的幂等开关);migrate 隐式副作用(→ 显式 migration profile);AI 工具手填 ref 的静默错误(→ run 绑定上下文)——**每个坑的"固化物"都在仓库里能指认,这是最硬的可信度**。
- **和 AGENTS.md 的连接**——这个项目的 AGENTS.md 明确规定了沉淀纪律:一次性调试结论不进长期规则、重复出现的判断要沉淀、规则分层(根/子目录只收紧不放宽):**踩坑的价值靠沉淀机制兑现,不靠个人记性**——ch10 反脆弱("第三次出现时系统里多了规则")的项目实证。
- **讲坑的禁忌**——不推责给模型/框架;不选假坑("没踩过什么坑"或纯业务难题);固化段不能少(没有固化的坑还会再踩)。
- **及格线 / 加分 / 红旗**:及格=四段完整;加分=固化物可指认+沉淀机制自觉;红旗=坑的故事里没有误判段(不真实)或没有固化段(没成长)。
