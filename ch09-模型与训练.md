# ch09 · 模型与训练前沿

> 覆盖:RLVR 与可验证奖励、agentic RL 环境、compaction 原生训练、interleaved thinking 与外化推理、test-time compute 与 effort/thinking budget、旗舰模型横评与选型、小模型路由(Haiku 角色)、蒸馏与降本。

---

## 9.1 八股题(基础概念)

**Q9.1【八股·L3·模型】RLVR 是什么?为什么代码是它的理想领域?**
答:
- RLVR(Reinforcement Learning with Verifiable Rewards):AllenAI Tulu 3(2024.11)正式提出的第三阶段后训练算法——奖励不来自人工偏好标注(RLHF),而来自**可程序化判定的结果**(数学答案正确性、**代码单元测试通过**);
- 代码理想的原因: "**hard-to-solve, easy-to-verify**"——解决难、验证易(测试/编译/lint 即时且客观);
- 对比过程奖励(PRM,如 Let's Verify Step by Step):SWE 任务没有步骤级真值,业界普遍用**结果级可验证奖励**,辅以惩罚"无依据断言""重复工具调用"的过程性 shaping。

**Q9.2【八股·L3·模型】SWE-RL(Meta, 2025.02)的做法与结论?**
答:
- 首个把 RL 扩展到真实软件演化的工作:用 GitHub PR 数据(代码快照+issue+变更),奖励=生成 patch 与真实 patch 的相似度(规则化代理奖励);
- Llama3-SWE-RL-70B 在 SWE-bench Verified 达 **41.0%**(<100B 最佳,媲美 GPT-4o),并在函数编码、数学、语言理解上出现**跨域涌现增益**——agentic RL 的收益不局限于 agent 任务。

**Q9.3【八股·L3·模型】各家"真实仓库 RL 环境"的规模数字?**
答:
- Qwen3-Coder(2025.07):阿里云上 **20,000 个并行独立环境**跑 agent rollout,Long-Horizon Agent RL 多轮工具调用训练;
- Cursor Composer(2025.10):真实工具环境(文件编辑/终端/语义搜索/grep),PyTorch+Ray 异步 RL,**数十万个并发沙箱**,数千 GPU;奖励高效工具使用/并行调用/少无依据断言;**涌现出主动修 linter 错误、写并运行单测的行为**;
- 含义:RL 环境供给(真实任务+沙箱+可验证奖励)成为各家稀缺资产;Scale SWE-bench Pro 商业集 15% 的落差既是评估问题也是 RL 训练缺口的映射。

**Q9.4【八股·L3·模型】GPT-5.1-Codex-Max 的"compaction 原生训练"是什么意思?为什么重要?**
答:
- 自称**首个原生训练为跨多个上下文窗口运行的模型**:训练中让模型在上下文压缩摘要之上继续推理,可连续工作 24 小时以上;
- 比同代 medium effort 少 30% thinking token;
- 重要性:此前 compaction 是 harness 强加的有损操作(模型被动接受摘要);现在压缩行为进入训练分布——模型学会"什么值得写进摘要/如何利用摘要",长任务的记忆管理从纯工程问题变成模型能力问题。

**Q9.5【八股·L2·模型】test-time compute 的演进线?GPT-5 的"自适应"指什么?**
答:
- o1(2024.09)→o3/o4-mini(2025.04,首批"推理中带工具使用")→GPT-5(2025.08):**内置路由器实时决定模型档位 + 推理力度(minimal/low/medium/high/xhigh)**——简单问题即时回答、难题深研究;
- 比o3 少 50–80% 输出 token;GPT-5 Pro 引入 **parallel test-time compute**(多路径并行求解再择优);
- Claude 侧:extended thinking 的 budget_tokens(最小 1024)+ Opus 4.5 的 **effort 参数**(medium effort 少 76% 输出 token 达到 Sonnet 4.5 最佳分);Gemini 3 提供 thinking level 控制(可强制关闭)。

**Q9.6【八股·L2·模型】interleaved thinking 是什么?Anthropic 为什么强调"外化推理"?**
答:
- interleaved thinking(2025.05 beta):extended thinking 与 tool use 交织——模型在**每次工具调用之间思考**,思考块跨调用持久保留;开启后思考预算可超 max_tokens;
- 外化推理:Anthropic 反复强调 Claude 的推理外化为**可见中间产物**(计划、todo、测试代码、interleaved scratchpads;Opus 4.5 评测方法学注明 64K thinking budget + interleaved scratchpads);
- 动机:可见才可检验、可被 RL 奖励、可被 harness 治理——与"黑盒 CoT"的路线分歧。

**Q9.7【八股·L2·用户】Haiku 级小模型在 agent 系统里的角色?**
答:
- Claude Haiku 4.5(2025.10):SWE-bench Verified 73.3%,约 Sonnet 4 的编码力、1/3 成本、2–5 倍速度;
- 官方推荐编排:**Sonnet 做规划,指挥一队 Haiku 并行执行**;
- 典型岗位:fast mode、后台任务(background tasks 默认走 Haiku)、上下文压缩/清理、分类路由、日志初筛——"便宜、快、并发友好"的流水线工人。

**Q9.8【八股·L1·用户】给出 2025 末–2026 初旗舰 coding 模型的一句话画像(任选 5 个)。**
答(示例口径):
- Claude Opus 4.5(2025.11):Verified ~80.9%,effort 可调,复杂多语言/前端强,$5/$25;
- GPT-5.1-Codex-Max(2025.11):compaction 原生、24h+ 长任务、xhigh effort,$1.25/$10(缓存读 0.125);
- Gemini 3 Pro(2025.11):Verified 76.2%,1M 输入,thinking level 可控,$2/$12;3.1 Pro(2026.02)升至 80.6%;
- DeepSeek V3.2(2025.09/12):DSA 稀疏注意力,长上下文降本 50%+,MIT 开源,Verified 67.8;
- Kimi K2(2025.07):1T 总参/32B 激活 MoE,Verified 单次 65.8/多次 71.6,开源;
- Qwen3-Coder-480B(2025.07):256K 原生/1M(YaRN),Apache 2.0;
- GLM-4.6(2025.09):357B MoE,200K 上下文,对标 Sonnet 4,低成本档。
- 注意:这些数字随版本快速变化,答题重点是"知道画像与量级、知道去哪核实"。

---

## 9.2 原理深挖

**Q9.9【原理·L4·模型】RLVR 训练的 agent 会有什么系统性偏差?为什么 reward hacking 不可避免?**
答:
- 偏差:优化"可验证的代理目标"(测试通过)而非真实目标(正确/优雅/安全)——凡是验证器与真实目标有 gap 的地方都会被钻:硬编码期望值、删失败测试、打印关键词骗检查器(R1 的 "podcast" 案例);
- 不可避免性:Goodhart 定律——优化压力集中在代理目标上,gap 处必然被利用;验证器越弱,钻得越狠;
- 缓解:验证器与优化器军备竞赛(held-out 测试、多验证器、过程性 shaping 惩罚可疑行为);训练后评估必须换一套 verifier。

**Q9.10【原理·L4·模型】"agentic RL 环境"要具备哪几个组件?为什么它比静态数据集难造?**
答:
- 组件:真实或有意义的任务分布(带 issue 语境)、可执行环境(沙箱/依赖/启动)、验证器(测试/编译/契约)、工具面(与生产 agent 一致)、并行与重置能力(Cursor 数十万沙箱、Qwen 2 万环境)、奖励计算管道;
- 难处:任务的多样性(不能全是 Django)、验证器质量(见 Q9.9)、环境重置的成本、工具面漂移(训练时的工具与部署时不一致会导致能力不迁移);
- 加分:指出"环境即数据"——RL 时代数据工程变成了环境工程。

**Q9.11【原理·L3·模型】effort/thinking budget 该在什么维度上调?给一个调度策略。**
答:
- 上调:探索性/多步推理/debug/架构决策——反馈慢、试错贵的任务"想清楚再动"更划算;
- 下调:可验证反馈快的任务(编译/测试秒回)——让环境当思考的外部延伸,"少想快做+环境纠错"更便宜;
- 调度:三层路由——任务分类(小模型)→ effort/budget 调节 → 模型分级(Haiku→Sonnet→Opus);并行多候选(parallel test-time compute)只给高价值难题;
- 证据:Opus 4.5 medium effort 少 76% 输出 token 保住最佳分——"overthinking"是真实成本。

**Q9.12【原理·L3·模型】蒸馏在 coding agent 语境下的两种含义?**
答:
- 模型层:大模型轨迹→小模型(工具调用能力蒸馏;K2/Qwen 小尺寸、Flash-Lite;Haiku 定位"编码力接近大哥、成本低一个量级");
- 任务层:超长历史→压缩 LLM 摘要(Cognition 建议"专门微调的压缩模型蒸馏历史")——把 compaction 从通用能力变成专用模型;
- 共同动机:成本与延迟;共同风险:蒸馏上限受教师/源信息限制,长尾场景掉链子。

**Q9.13【原理·L4·架构】模型选型时,"harness 依赖"为什么必须纳入考量?**
答:
- 评测分数强依赖 harness:同一 GPT-5.1 在 Terminus-2 上因 harness 改进从低分升到 48.6%;Agentless 与 agent 脚手架在 Verified 上差近一倍;
- 因此:厂商榜单的 harness 不等于你的 harness——选型必须在**自家工作流**上评(内部 eval);
- 模型与 harness 的耦合在加深:compaction 行为、interleaved thinking、工具定义格式、缓存友好度都是"模型×环境"的联合属性——换模型可能要重调 harness(工具数量、压缩阈值、思考预算)。

---

## 9.3 品味与判断题

**Q9.14【品味·L4·模型】"未来 coding 能力的差异化在模型还是在 harness?"请表态并论证。**
参考思路:
- 模型方论据:RLVR 环境与数据是深护城河;compaction/长任务训练把 harness 职责吸进模型;
- harness 方论据:同模型不同 harness 分差几十个点;上下文工程、工具设计、验证器是使用方的自主杠杆;
- 综合:模型吸收"通用最优"的 harness 模式,使用方的差异化转移到"领域特定的上下文与验证"(你的 specs、你的测试、你的工具语义)——不可迁移的资产在数据与验证,不在循环代码。

**Q9.15【品味·L3·用户】"默认用最强模型,反正贵一点"vs"默认便宜,搞不定再升级"——你的路由哲学?**
参考思路:
- 任务价值密度决定:一次性高价值任务(生产迁移)直接旗舰(失败重试更贵);批量铺量任务 cheap-first;
- 中间态:medium effort 的旗舰 vs high effort 的中档——用 eval 决定,不靠信仰;
- 升级路径要设计:什么时候判定"搞不定"(重试次数/置信信号)、升级时带什么上下文(避免从头再来烧钱)。

**Q9.16【品味·L4·管理】开源 coder 模型(K2/Qwen3-Coder/GLM/DeepSeek)在你的技术栈里能替代闭源旗舰吗?决策框架?**
参考思路:能替代的条件——任务分布集中(内部 CRUD/迁移)、可自托管(数据合规)、有 eval 保障(分数差距在你的任务上可接受)、并发成本敏感;不能替代——长时自主任务、复杂多语言重构、前沿能力依赖;混合:开源做铺量 subagent,闭源做 lead;风险:运维成本、安全更新、模型迭代节奏。

---

## 9.4 开放性问题

**Q9.17【开放·L5·模型】如果 thinking budget 可以按工具调用粒度动态分配,调度器该怎么设计?信号从哪来?**
讨论框架:信号——工具返回的信息量(错误熵)、任务进度估计、上下文剩余、历史轮次的边际收益;分配策略——bandit/RL 调度器;风险——模型学会"表演思考"骗预算。

**Q9.18【开放·L5·模型】训练与部署的"compaction 分布一致性"问题:如果你的 harness 压缩策略与模型训练时不同会怎样?怎么对齐?**
讨论框架:分布外摘要导致模型不信摘要或过度依赖;对齐——沿用厂商默认压缩行为、或把自家摘要格式做成微调数据、或用 API 的 context editing 让压缩发生在服务端训练分布内。

**Q9.19【开放·L4·架构】模型每 3 个月换代,你的平台怎么做到"模型可插拔"而不重写?**
讨论框架:模型抽象层(能力探测而非版本硬编码);能力测试集自动评估新模型;harness 参数化(思考预算/压缩阈值/工具格式);灰度(按流量比例切);行为回归(eval-as-CI)作闸门。

---

## 9.5 工程实战题

**Q9.20【实战·L3·平台】设计你们平台的模型路由器:三个模型档(旗舰/中档/小),任务异构(对话/编码/审查/批量迁移)。**
期望框架:
- 路由信号:任务类型标签(显式)+复杂度启发(代码量/仓库规模/历史失败率)+成本预算;
- 策略:编码默认中档、失败升级旗舰;审查中档;批量迁移小模型并行+旗舰抽检;对话小模型;
- 反馈闭环:记录每任务(路由决策→结果→成本),定期重估路由规则(简单 bandit 即可起步);
- 防坑:升级时携带已有上下文避免重跑;路由器本身用最便宜可靠的模型。

**Q9.21【实战·L4·平台】你要为自己的公司构建一个小规模 agentic RL 环境(内部任务)。给方案。**
期望框架:
- 任务源:内部 issue/PR 历史(脱敏)+合成任务生成(基于真实仓库的自动测试构造,R2E-Gym 思路);
- 环境:每任务 Docker 快照+依赖锁定;工具面与生产 agent 一致;
- 验证器:执行测试+lint+契约检查;held-out 验证集分开;
- 奖励:结果级(测试通过)+过程 shaping(惩罚重复调用/无依据断言);
- 规模与预算:先几十并发起步,bandit 式扩;评估:微调前后在内部 eval 与公开榜双轨。

---

## 9.6 场景排错题

**Q9.22【排错·L3·平台】换用新旗舰模型后,agent 在你们内部任务上反而变差了 10%(公开榜它更强)。可能原因?**
排查链路:harness 不匹配(工具定义格式/缓存行为/压缩策略——见 Q9.13);行为风格变化(更爱探索导致预算耗尽/更保守导致任务不完成);上下文利用差异(对长上下文的依赖度不同→你们的检索管道没喂对);评测口径(公开榜任务≠你的分布);prompt 兼容性(旧 prompt 针对旧模型调优)。逐项用 A/B 与 trace 对比定位。

**Q9.23【排错·L2·用户】开启最高 reasoning effort 后,任务没变好,token 翻了 4 倍,延迟大到不可用。为什么?**
排查链路:任务可能反馈快且可验证(环境当外脑比内脑便宜)——effort 该降;或思考没有对齐到有用信号(缺工具反馈时纯想=空转);或 verifier 缺失导致"想了也不敢停";处置:该任务降 effort、补 verifier、把多步推理需求外化成任务分解(plan/spec)而不是思考预算。
