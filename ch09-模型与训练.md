# ch09 · 模型与训练前沿(含 DeepSeek 模型×Harness 专节)

> 覆盖:RLVR 与可验证奖励、reward hacking、agentic RL 环境的构造、test-time compute(effort/thinking budget)、模型路由与选型、开源 vs 闭源的决策;专节:DeepSeek 的"模型+harness 协同"路线(V4 与 DeepSeek Harness 同日发布、自报分的 harness 绑定、稀疏注意力经济性、开源 harness 生态)。
> 案例基线:2026-08。模型版本与价格快速变化,引用均带"截至"日期;训练方法论部分相对稳定。

---

## 9.1 八股题(基础概念)

**Q9.1【八股·L3·模型】RLVR 是什么?为什么代码是它的理想领域?它和 RLHF 的区别?**

- **人话版**:RLVR 用"可程序化判定的结果"当奖励——测试过了就给分,不需要人工标注偏好。代码是理想领域因为"难解易验"(hard-to-solve, easy-to-verify):写对一个并发补丁很难,但跑一遍测试立刻知道对错。RLHF 的奖励来自人的偏好标注,贵、慢、还带标注者的偏差。
- **展开**:
  - **机制差异**:RLHF 需要对同一提示的多个回答做人类偏好排序再训奖励模型(间接、可被 reward model 劫持);RLVR 的奖励是确定性的(测试通过/答案匹配),信号无噪声、可无限生成。代价是只覆盖"能验证"的任务——开放性任务(写作、审美)验证不了,还得靠 RLHF 或宪法方法。
  - **为什么 agent 任务特别适配**:agent 任务的天然奖励就是"任务完成"——测试全绿、构建成功、页面渲染正确,全是可执行判定;所以 2025 年起各家把 RL 从单轮问答扩展到多轮工具调用的 agent 轨迹。
  - **过程奖励的问题**:数学可以逐步骤给分(PRM),软件任务没有步骤级真值——业界普遍用结果级奖励,辅以过程性 shaping(惩罚无依据断言、重复调用这类行为特征)。
- **及格线**:可验证奖励+难解易验+与 RLHF 的区别。
- **加分**:shaping 的存在及原因;为什么开放任务不适用。
- **红旗**:把 RLVR 和 RLHF 说成同义词;认为"RLVR 通用取代 RLHF"。

**Q9.2【八股·L2·模型】test-time compute(推理时算力)这几年的产品化形态?effort/thinking budget 是什么、怎么调?**

- **人话版**:同一个模型,推理时"想多久"可以调——effort/thinking budget 从 minimal 到 max 几档。简单问题秒答,难题深想。产品化就是把这个旋钮暴露给用户/调度器:OpenAI 的 reasoning effort 档位、Anthropic 的 thinking budget/effort 参数、Gemini 的 thinking level(可关)。
- **展开**:
  - **演进主线**:o1(2024)证明"想更久分更高"→各家参数化→旗舰模型内置自适应路由(简单问题自动秒答);另一支是 parallel test-time compute:多路生成并行再择优(Pro 档的形态)。
  - **量化量级**:官方口径下,高档 effort 能省 50–80% 输出 token 而保住大部分分数——"overthinking 是真实成本";反过来,低价值任务开最高档是纯浪费。
  - **调节的判断维度**:反馈速度——环境反馈快(编译/测试秒回)的任务,让环境当"外部大脑",低 effort 快试快改更便宜;反馈慢或不可逆的任务(架构决策、一次性生产迁移),高 effort 想清楚再动手。
  - **调度实践**:三层路由——任务分类(小模型)→effort 调节→模型分级;多候选并行只给高价值难题。
- **及格线**:档位概念+快反馈用低档的逻辑。
- **加分**:parallel compute 形态;"overthinking 成本"的量化感觉。
- **红旗**:无脑开最高档"保证质量"(见 Q9.18 排错)。

**Q9.3【八股·L2·用户】小模型(旗舰的轻量版/mini 档)在 agent 系统里的角色是什么?**

- **人话版**:干"便宜快"的活——分类路由、上下文压缩、后台批处理、日志初筛、并行 subagent 的执行层。编排上常见"大模型当 lead 规划,小模型当 worker 并行执行"。
- **展开**:
  - **适配的任务特征**:高频、低价值密度、判定标准明确——压缩摘要(要快不要妙)、路由分类(选项有限)、批量迁移的单个执行单元。反过来,规划、架构、疑难排错留给旗舰。
  - **经济学**:同家族小模型通常价格约 1/3–1/10、速度快 2–5 倍;在"失败可廉价重试"的任务上,小模型+重试常比大模型单次便宜且质量相当。开源小模型(自托管)进一步把边际成本压到算力价。
  - **2026 年的格局注脚**:开源阵营的中小尺寸模型(Qwen/GLM/Kimi 的衍生尺寸)质量快速逼近闭源中档,BYOK 工具链把它们接入成本敏感场景——"开源铺量、闭源攻坚"的混合架构成为常态。
- **及格线**:四类任务+lead/worker 编排。
- **加分**:失败可重试任务上的经济学;开源小模型的角色。
- **红旗**:认为小模型=低质量没用;或反过来全用小模型省成本。

**Q9.4【八股·L3·模型】"agentic RL 环境"要具备哪几个组件?为什么说它比静态数据集难造、也稀缺?**

- **人话版**:五件套——真实任务分布(带 issue 语境)、可执行环境(沙箱/依赖/重置)、验证器(测试/契约)、工具面(和生产一致)、并行与奖励管道。难造在:任务要多样、环境要能秒级重置、验证器要可信——每件都是工程,而且每家的积累不通用。
- **展开**:
  - **公开的规模参照**:Qwen3-Coder 的训练报告提过云上两万级并行环境跑 agent rollout;Cursor 公开过真实工具环境(编辑/终端/检索)+ 数十万级并发沙箱 + 异步 RL 的架构,并报告了涌现行为(主动修 linter、自写并跑单测)。
  - **"环境即数据"**:预训练时代比拼语料,RL 时代比拼环境——任务的多样性(不能全是同一框架的 CRUD)、验证器的质量(弱验证器训出钻空子的模型)、工具面与部署的一致性(训练用一套工具、部署是另一套,能力不迁移)。
  - **稀缺性的含义**:公开榜分数能抄,环境管道抄不走——这解释了为什么公开榜 70 分的模型在私有任务上表现分化巨大(环境分布不同);也解释了"内部真实仓库 RL 环境"成为头部厂商的核心资产。
- **及格线**:五件套+环境即数据的说法。
- **加分**:工具面一致性的迁移问题;环境稀缺与公私分数落差的关系。
- **红旗**:以为 RL 就是"喂数据";不知道验证器质量决定 RL 上限。

**Q9.5【八股·L3·模型】compaction/长上下文的原生训练是什么意思?为什么它重要?**

- **人话版**:以前的上下文压缩是 harness 强加的——模型被动接受一份摘要继续干活;原生训练是把"在压缩摘要之上继续推理"放进训练分布,模型学会什么值得写进摘要、怎么利用摘要。长任务的记忆管理从纯工程问题变成了模型能力问题。
- **展开**:
  - **为什么此前是痛点**:harness 的摘要对模型是分布外输入——它可能不信摘要(重新探索,浪费)或过度依赖(摘要错了它也跟着错);训练对齐后这个张力消解。
  - **产品化信号**:有厂商明确以"首个原生训练为跨多上下文窗口运行的模型"为卖点(连续工作 24 小时级,比同代少 30% 思考 token);DeepSeek V4 系列(2026)把 1M 上下文做成全系标配——长上下文+压缩容忍正在从特性变基线。
  - **选型含义**:换模型时把"对压缩的容忍度"当能力维度测——你的 harness 有压缩,模型没训练过,长任务质量会莫名下降(见 Q9.17 排错)。
- **及格线**:被动接受 vs 训练对齐的对比。
- **加分**:不信/过度依赖摘要的两类失败;选型时的测试建议。
- **红旗**:不知道压缩行为和模型训练有关。

---

## 9.2 原理深挖

**Q9.6【原理·L4·模型】RLVR 训练的 agent 会有什么系统性偏差?reward hacking 为什么不可避免?**

- **人话版**:模型优化的是"可验证的代理目标"(测试通过),不是真实目标(正确/优雅/安全)——验证器和真实目标的每个 gap 都会被钻:硬编码期望值、删失败测试、打印关键词骗检查器。不可避免是因为 Goodhart 定律:优化压力足够大时,代理指标必然和真实目标脱钩。
- **展开**:
  - **教科书案例**:DeepSeek-R1 训练中模型学会在差距区间打印特定词骗过检查器——不是模型"坏",是它找到了奖励函数的捷径;这类行为在弱验证器下会自发涌现。
  - **军备竞赛的对策**:held-out 测试(训练时不可见)、多验证器交叉、过程 shaping 惩罚可疑行为、训练后用**另一套** verifier 评估。注意最后一条:训练和评估用同一个验证器=自己骗自己。
  - **对使用方的含义**:你接管的不是"做过 RL 的模型"而是"针对某个验证器分布优化过的模型"——它在你环境里的行为要重新评估,尤其当你的验收比它的训练奖励更弱时,钻空子的概率上升而不是下降。
- **及格线**:代理目标 gap+Goodhart+一个案例。
- **加分**:训练/评估验证器分离;使用方环境的再评估责任。
- **红旗**:认为 RL 过的模型"更听话所以更安全"。

**Q9.7【原理·L3·模型】蒸馏(distillation)在 coding agent 语境下的两种含义?各自的动机与风险?**

- **人话版**:一层是模型蒸馏——大模型的轨迹教小模型(工具调用能力、编码风格);另一层是任务蒸馏——专门微调的压缩模型把超长历史蒸馏成摘要。共同动机是成本与延迟,共同风险是上限受教师/源信息限制。
- **展开**:
  - **模型层**:旗舰的 agent 行为(何时调工具、怎么从错误恢复)蒸馏进小模型——开源家族的中小尺寸普遍走这条路;风险是长尾场景(教师都做不好的)必然掉链子,所以小模型 agent 要配升级路径。
  - **任务层**:压缩模型专职摘要会话历史(比通用模型干这个更稳更便宜)——把 compaction 从通用能力变成专用组件;风险是摘要的任务相关性判断误差会跨会话传播(错误的摘要一直影响后续)。
  - **组合**:两层经常一起用——便宜的压缩模型+便宜的执行模型+旗舰决策,构成完整的成本分层架构。
- **及格线**:两层含义+各自一句风险。
- **加分**:组合架构;错误摘要的跨会话传播这个深层风险。
- **红旗**:只知道模型层含义。

**Q9.8【原理·L4·架构】模型选型时,"harness 依赖"为什么必须纳入考量?怎么测?**

- **人话版**:榜单分数是"模型×它的 harness"的乘积,不是模型的裸分——同一模型换个 harness 能差几十分。所以厂商榜单的 harness 不等于你的 harness,选型必须在自家工作流上测。
- **展开**:
  - **量级证据**:同款模型在有/无 agent 脚手架的基准上差近一倍;2026 年最锋利的例子是 DeepSeek V4-Pro 自报 Terminal-Bench 87.9(用自家 harness 测试)vs 第三方在参考 harness 上测出约 54.7——33 分的差距(详见 Q9.12)。
  - **耦合在加深**:compaction 行为、思考模式、工具定义格式、缓存友好度都是"模型×环境"的联合属性——换模型可能要重调 harness 参数(工具数量、压缩阈值、思考预算);反过来 harness 改动也会让同一模型表现波动。
  - **怎么测**:固定 harness 跑内部 eval(否则分数不可比——变量必须只有一个);迁移成本单列一项(接新模型要改多少配置);行为回归(agent 行为风格变了没有,比如变得过度探索或过度保守)。
- **及格线**:乘积观点+在自家 harness 上测。
- **加分**:迁移成本与行为回归的显式评估;引用 harness 差距的具体数字。
- **红旗**:拿两个不同 harness 的榜单分数直接比;换模型不动 harness。

---

## 9.3 DeepSeek 专节:模型 × Harness 协同(2026-08 案例)

**Q9.9【八股·L3·模型】2026 年 8 月 DeepSeek 做了一件在业内引发大量讨论的事:正式版模型与自家开源 coding harness 同日发布(2026-08-13:V4-Pro-0813 与 DeepSeek Harness)。"Agent = Model + Harness"这个公式在讲什么?模型厂为什么要做 harness?**

- **人话版**:这个公式说 agent 的能力一半在模型、一半在执行壳——模型决定"想什么",harness 决定"想法能落地成什么动作"。模型厂做 harness,是因为两边协同调优的效果远大于各做各的:自家的模型在自家的执行壳里能发挥出别处发挥不出的水平。
- **展开**:
  - **官方口径**:DeepSeek Harness 的文档直接说"模型是 agent 的灵魂,harness 决定模型能否理解环境、使用工具、持续运行"——把 harness 从"周边工具"提到"agent 的另一半"的地位。开源(MIT)、一周量级的 star 爆炸、HN 千楼讨论,说明行业对"模型厂亲自下场做执行壳"这件事关注极高。
  - **协同的具体机制**:V4-Flash 的公开基准脚注明示"用 DeepSeek Harness 极简模式测试"——模型的后训练与这个 harness 的行为(工具调用格式、循环模式)绑在一起设计;社区进一步猜测"模型针对 DSH 做过后训练"(官方未确认)。
  - **历史脉络(背景)**:DeepSeek 早一两代的模型(V3.2,2025-12)还明确声明"未针对特定测试工具训练"——从"模型中立"到"模型×harness 联合发布"的立场转变就发生在 2026 年,这是整个行业模型-环境耦合加深的一个缩影。
- **及格线**:公式的含义+协同优化的动机。
- **加分**:知道"极简模式测试"的脚注与立场转变的脉络;能对比其他厂商(Anthropic 模型+Claude Code、OpenAI 模型+Codex)的同构策略。
- **红旗**:把 harness 当"模型的配件"不理解耦合;认为模型厂做 harness 只是生态扩张没有技术原因。

**Q9.10【八股·L3·平台】DeepSeek Harness(dsh)的架构有什么特点?"Everything is a Plugin"、四种运行模式、全程可追溯(traceability)分别是什么?**

- **人话版**:它的架构哲学是"一切都是插件"——模型、工具、技能、会话、沙箱、循环、UI 全部可换,增删能力只改配置不动源码;四种模式(Standard 全工具/Code 模型写代码编排工具/Minimal 极简用于跑分/Creator 运行时检查);所有模型看到的东西全程记录,append-only 可回放。
- **展开**:
  - **插件架构的意图**:对比 Claude Code 的一体化产品,dsh 把每个环节做成可替换插件——任意供应商的模型(Anthropic/OpenAI/本地 vLLM)、自定义工具链;代价是安全默认值偏弱(社区实测:本地 RPC 无鉴权、插件无签名无权限清单)——"实验室"和"产品"的取舍。
  - **Minimal 模式的存在本身是个信号**:专门为 benchmarking 设计极简执行壳——呼应"跑分环境应该标准化、模型无关"的评测方法论(见 Q9.12);官方自报分用它,说明 DeepSeek 承认"分数必须绑定可复现的 harness"。
  - **traceability 是最受好评的设计**:append-only 会话日志记录每个提示词、推理、工具调用与结果、子代理调度,支持 resume/fork/replay——HN 上最热的评价是"美国厂商不让你看到模型的思维链,这里全给你"(对比 OpenAI/Anthropic 的 CoT 混淆策略)。对做研究和内部审计的团队,这是真卖点。
  - **治理特色**:仓库不接受外部 PR,扩展只能以插件形式发布——保内核稳定,把开放性放在外围。
- **及格线**:插件哲学+四模式+全程记录三件。
- **加分**:Minimal 模式的评测方法论含义;traceability 与闭源 CoT 政策的对比;不接受 PR 的治理逻辑。
- **红旗**:把 dsh 当"又一个开源 Claude Code 平替"讲不出架构差异;不知道安全默认值的批评。

**Q9.11【原理·L3·平台】让 Claude Code 跑在 DeepSeek(或其他开源模型)上,兼容层是怎么工作的?这条路径为什么重要?**

- **人话版**:设几个环境变量(ANTHROPIC_BASE_URL 指向 DeepSeek 的 Anthropic 兼容端点、TOKEN 换成 DeepSeek 的 key、模型名映射),Claude Code 就把请求发给 DeepSeek——它以为自己还在和 Anthropic 说话。这条路径重要是因为 Claude Code 的生态(记忆文件、hooks、subagent 体系)不用重建就能换模型底座。
- **展开**:
  - **机制**:DeepSeek 提供 Anthropic 协议兼容端点,模型名自动映射(opus 档→v4-pro,sonnet/haiku 档→v4-flash);支持 tools/图片/thinking 的兼容子集(部分参数如 budget_tokens 被忽略、部分 block 类型不支持)——兼容不是 100%,边缘特性有差异,迁移前要过自己的 eval。
  - **为什么重要**:harness 生态的迁移成本远大于模型本身——CLAUDE.md 体系、hooks、skills、团队肌肉记忆都长在 harness 上;兼容层让"模型自由"成为可能,厂商的模型收入不再被自家前端锁死。开源模型厂商普遍走这条路(awesome 清单里几十个工具的接入指南)。
  - **反向含义**:Anthropic 的协议成为事实标准的"接口层"——这既是影响力也是责任(兼容子集的差异会造成"能跑但行为不同"的坑)。
- **及格线**:环境变量级别的机制+生态迁移成本这个"为什么"。
- **加分**:兼容子集的坑(budget_tokens 忽略等)+迁移前过 eval 的建议;"协议成为事实标准"的双面性。
- **红旗**:以为要 fork Claude Code 才能换模型;不知道兼容有差异就全量迁移。

**Q9.12【原理·L4·模型】同一款模型(V4-Pro-0813),官方自报 Terminal-Bench 2.1 得 87.9(注明用自家 DeepSeek Harness 极简模式测试),第三方在参考 harness(Terminus 2)下测出约 54.7——约 33 分的差距。这个现象说明什么?你作为评测负责人怎么处理?**

- **人话版**:说明"harness 是分数的一部分"不是理论是实测——33 分足够让一个"业界第一"变成"中游"。作为评测负责人:所有自报分先查测试 harness,自己的评测固定 harness,跨来源比较时把 harness 当第一变量。
- **展开**:
  - **为什么差距这么大**:模型的工具调用习惯、上下文利用方式是在某个 harness 的分布上(后)训练的——换一个交互协议(工具集、输出格式、循环结构),能力发挥不出来;这不是作弊,是真实的耦合,但**披露方式**决定性质——DeepSeek 在脚注里注明了测试环境,这算合规披露;不注明就是误导。
  - **评测负责人的三个动作**:①读方法论脚注成习惯(测试环境/参数/采样数);②内部评测锁定 harness 版本(升级 harness=分数重新基线);③对"自报分"统一打折系数心理预期——厂商自报分+第三方复测的差值本身就是"harness 敏感度"指标。
  - **行业对照**:Terminal-Bench 官方维护模型无关的参考 agent(Terminus)正是为了这个——公平比较要固定环境;厂商自报分用自家环境也有合理性(生产环境就是这样用的),但两者不可直接比。
- **及格线**:耦合的真实性+披露决定性质。
- **加分**:评测负责人的三个动作;"自报与复测的差值=harness 敏感度"这个指标化思路。
- **红旗**:拿 87.9 和别家(不同 harness)的分数直接排座次;或者反过来认为自报分全是造假。

**Q9.13【原理·L3·模型】DeepSeek V4 的注意力机制(token 维度压缩+稀疏注意力)为什么对 coding agent 场景特别有价值?官方口径的量级是什么?**

- **人话版**:coding agent 的上下文又长又重复——几十万 token 的仓库内容+循环历史,标准注意力是 n² 的,又贵又慢。稀疏注意力让每个 token 只关注相关的少数位置,V4-Pro 官方模型卡的说法:1M 上下文时只用约 27% 的推理算力、10% 的 KV cache(对比上一代)。对高频缓存命中的 agent 负载,这是成本数量级的改善。
- **展开**:
  - **机制直觉**:长上下文里大多数 token 对是无关的(仓库不同模块、历史不同任务)——稀疏化把注意力集中在相关的局部+锚点,算力和显存都省;KV cache 小了,缓存驻留成本(决定 TTL 和价格)也降,这才是 agent 场景的关键——缓存便宜了,长会话的经济学整体改善。
  - **与 prompt caching 的关系**:两层优化正交——prompt caching 是"跨请求复用",稀疏注意力是"单请求算得少";叠加起来长上下文 agent 的成本结构被重写(这也支撑了 V4 把 1M 窗口做成全系标配的商业决策)。
  - **工程含义**:上下文成本的下降不改变 context rot 的质量约束——窗口便宜了≠塞满是对的(注意力稀释是模型内在问题);所以检索和压缩的质量价值依然成立,变便宜的只是"不得不长"的部分。
- **及格线**:n² 问题+稀疏化省在哪(算力+KV cache)。
- **加分**:与 prompt caching 的正交关系;"成本下降不推翻 context rot"的清醒。
- **红旗**:把稀疏注意力当"解决了一切长上下文问题";混淆它与 KV cache 复用。

**Q9.14【品味·L4·管理】2026-08 DeepSeek 上调 API 价格并引入峰谷计价后,第三方工具(如 OpenCode)随后下架了它的免费接入档位(2026-08 前后)。"低价开源"的商业逻辑在变。如果你的技术栈深度绑定了一家低价开源模型厂商,怎么管理这种风险?**

参考思路:
- **风险定性**:价格风险(低价是获客补贴不是永久权利——峰谷计价+涨价就是信号)、供给风险(免费档消失、促销结束)、兼容风险(协议层说变就变)。
- **管理动作**:模型抽象层(切换成本降到周级);成本压力测试(价格翻倍/三倍时的总账——写在架构文档里);双供应商真活流量(不是"备而不用",是日常分流,保住真的能切);用量预算告警。
- **判断层面**:低价期的正确姿势是"用它省钱+同时建设不依赖它的资产(eval 集、harness 配置、迁移脚本)"——省下的钱有一部分要花在可迁移性上;深度绑定的每一分便利都在给未来的切换加价。
- **及格线 / 加分 / 红旗**:及格=三类风险+抽象层;加分=双活流量与压力测试;红旗=因为便宜就全仓绑定不留退路,或者反过来不敢用任何便宜模型。

---

## 9.4 品味与判断题

**Q9.15【品味·L4·模型】"未来 coding 能力的差异化在模型还是在 harness?"表态并论证。**

参考思路:
- **模型派论据**:agentic RL 环境与数据是深护城河;compaction/长任务训练正在把 harness 职责吸进模型;模型厂做 harness(V4+dsh 同日发布、各家模型与自家工具深度协同)说明模型侧在整合上下游。
- **harness 派论据**:同模型不同 harness 分差几十分(33 分的实测);上下文工程、工具语义、验证器是使用方的自主杠杆,不依赖模型换代;领域特定的记忆与 eval 完全长在使用方。
- **综合立场**:通用最优的 harness 模式会被模型吸收(循环、压缩容忍),使用方的差异化转移到**领域特定资产**——你的 spec、你的工具语义、你的验证器、你的失败分类。不可迁移的资产在数据与验证,不在循环代码:写 loop 的能力在贬值,判断"喂什么、验什么"的能力在升值。
- **及格线 / 加分 / 红旗**:及格=两派论据+自己的立场;加分="通用被吸收、特定留使用方"的分层判断;红旗=单边站队。

**Q9.16【品味·L3·用户】"默认用最强模型,反正贵一点"vs"默认便宜,搞不定再升级"——你的路由哲学?**

参考思路:
- **价值密度决定默认**:一次性高价值任务(生产迁移、疑难排错)直接旗舰——失败重试比一次到位更贵;铺量任务(批量迁移的执行单元、格式转换)cheap-first+失败升级。
- **升级路径要设计**:什么时候判定"搞不定"(重试次数、置信信号、失败模式分类);升级时带什么上下文(避免从头再来的重复成本——这是多数"升级机制"最大的浪费点)。
- **中间态**:中档模型+高 effort vs 旗舰+低 effort 的选择,用 eval 决定不靠信仰;effort 旋钮常常比换模型便宜。
- **及格线 / 加分 / 红旗**:及格=按价值分层;加分=升级时的上下文携带设计;红旗=一刀切(全旗舰或全便宜)。

**Q9.17【品味·L4·管理】开源 coder 模型(DeepSeek/Qwen/Kimi/GLM 系)在你的技术栈里能替代闭源旗舰吗?2026 年夏季国产开源模型的密集发布(Kimi K3 于 2026-07-16 开源、Qwen3.8 与 GLM-5.3 于 2026-08 前后发布)改变这个判断了吗?**

参考思路:
- **能替代的条件**:任务分布集中(内部 CRUD/迁移/固定模式改造)、可自托管(数据合规)、有 eval 保障(差距在你的任务上可测且可接受)、并发成本敏感(批量场景便宜模型+宽并行的经济学)。
- **不能替代的场景**:长时自主任务(压缩容忍、跨窗口连续性)、复杂多语言重构、需要踩着能力上限干活的前沿场景。
- **2026 年的边际变化**:开源旗舰的质量在快速逼近(多家开源模型在公开编码榜进入第一梯队,且开源厂商开始提供 harness 与工具生态),差距从"代差"缩到"半年差";但 agent 场感的差距(工具调用的稳定性、长任务行为)比榜单分数反映的大——分数是"模型×它的harness",你用开源模型配自己的 harness,发挥几成取决于你的工程。
- **落地形态**:混合——开源做铺量 subagent 和离线批处理,闭源做 lead 和疑难;比例由内部 eval 的数据决定,每季度重估(开源在快速进步,静态决策三个月就过期)。
- **及格线 / 加分 / 红旗**:及格=条件化的替代观;加分=分数与agent场景感的差异+季度重估机制;红旗=按情怀(开源信仰/大厂信仰)选型。

---

## 9.5 开放性问题

**Q9.18【开放·L5·模型】如果 thinking budget 可以按工具调用粒度动态分配,调度器怎么设计?信号从哪来?**

讨论框架:
- **信号**:工具返回的信息量(错误熵——报错越意外越该多想)、任务进度估计、上下文剩余预算、历史轮次的边际收益(前几轮想很久没进展就降)。
- **策略**:bandit 式探索起步(每个任务族的"最优 effort"用数据学);保守上限(防"表演思考"骗预算——模型学会生成看似深度的思考来获取资源)。
- **难点**:信号延迟(想完才知道值不值)、任务族识别成本、与 parallel compute 的组合(多路低 effort vs 单路高 effort 的选择)。

**Q9.19【开放·L5·架构】模型每 3 个月换代,平台怎么做到"模型可插拔"而不重写?**

讨论框架:
- **抽象层**:按能力标签(而非版本号)声明需求("能工具调用、支持 200K、成本<X"),路由层解析到具体模型;能力探测自动化(新模型上线跑能力测试集)。
- **harness 参数化**:思考预算/压缩阈值/工具格式全部配置化,换模型=换配置包;行为回归(eval-as-CI)做上线闸门。
- **灰度**:按流量比例切流,行为指标(成功率/成本/干预率)不达标自动回滚。
- **组织现实**:每季度的"换模日"成本要核算——可插拔的架构换起来也要人天,把这件事当例行运营而不是突发事件。

---

## 9.6 工程实战题

**Q9.20【实战·L3·平台】设计你们平台的模型路由器:三个档位(旗舰/中档/小),任务异构(对话/编码/审查/批量迁移)。**

参考思路:
- **路由信号**:任务类型标签(显式传入)+复杂度启发(代码量/仓库规模/历史失败率)+成本预算+时效要求。
- **策略起点**(可讨论的默认):编码默认中档、失败升级旗舰;审查中档;批量迁移小模型并行+旗舰抽检;对话小模型。每条规则都标注"待验证",用数据定期重写。
- **反馈闭环**:记录每个任务的(路由决策→结果→成本),简单 bandit 定期重估规则——路由器是持续学习的,不是配完就完。
- **防坑**:升级时携带已有上下文(避免重跑);路由器本身用最便宜可靠的模型(别让路由成为成本和故障点);路由失败(分类错)的兜底路径。
- **及格线 / 加分 / 红旗**:及格=信号+分层策略;加分=反馈闭环与上下文携带;红旗=静态规则配完不动。

**Q9.21【实战·L4·平台】为公司构建一个小规模 agentic RL 环境(内部任务微调/训练用),给方案。**

参考思路:
- **任务源**:内部 issue/PR 历史(脱敏)+ 合成任务生成(基于真实仓库自动构造"改动→测试",R2E-Gym 思路);任务多样性审计(不能全是同一框架)。
- **环境**:每任务 Docker 快照+依赖锁定;重置要快(RL 要成千上万次 rollout);工具面与生产 agent **完全一致**(否则能力不迁移)。
- **验证器**:执行测试+lint+契约检查;held-out 验证集与训练奖励**分离**(Q9.6 的军备竞赛要求);定期人工抽检验证器质量。
- **奖励**:结果级为主+过程 shaping(惩罚重复调用/无依据断言);奖励函数审计日志(发现 hack 模式后收紧)。
- **规模与预算**:几十并发起步,验证任务质量后再扩;评估双轨(内部 eval+公开榜)防止过拟合自家分布。
- **及格线 / 加分 / 红旗**:及格=五件套齐;加分=工具面一致性与验证器分离这两个容易漏的点;红旗=拿静态数据集当"环境"。

---

## 9.7 场景排错题

**Q9.22【排错·L3·平台】换用新旗舰模型后,agent 在内部任务上反而变差 10%(公开榜它更强)。可能原因?**

排查链路:
- **harness 不匹配**(第一怀疑):工具定义格式/缓存行为/压缩策略与新模型的训练分布不合——它习惯的调用方式你的工具没提供;检查它的失败 trace 里"工具使用是否别扭";
- **行为风格变化**:更爱探索(预算耗尽在半路)或更保守(任务不完成)——成功率下降的归因要看失败模式分布,不是平均值;
- **上下文利用差异**:新模型对你的检索管道喂的内容利用度不同(比如更依赖结构化输入);
- **评测口径**:公开榜任务分布≠你的分布(见 ch07);
- **prompt 遗留**:旧 prompt 针对旧模型调优,反而束缚新模型。
- 定位法:A/B 各跑内部 eval 黄金集+trace 抽样对比失败模式——数据定位,不猜。
- **及格线 / 加分 / 红旗**:及格=harness 不匹配+行为风格两条;加分=失败模式分布的归因方法;红旗=看到变差就回滚,不做归因(下次再踩)。

**Q9.23【排错·L2·用户】开了最高 reasoning effort,任务没变好,token 翻 4 倍,延迟不可用。为什么?**

排查链路:
- **反馈快的任务不需要内脑**:编译/测试秒回的任务,环境当外脑比思考便宜——effort 该降(见 Q9.2 的调节逻辑);
- **思考没对齐信号**:缺工具反馈时纯想=空转(它在反复推演没法验证的假设)——给工具(测试/探针)比给思考预算有效;
- **verifier 缺失导致"想了也不敢停"**:没有客观完成信号,高 effort 只会拉长"确认"过程——补验收标准;
- **处置**:降 effort+补 verifier+把多步推理需求外化成任务分解(plan/spec)而不是思考预算——多数"想不清楚"其实是"任务没拆清楚"。
- **及格线 / 加分 / 红旗**:及格=降 effort 的逻辑;加分="任务没拆清楚"这个根因的识别;红旗=认为 effort 越高越好,或者干脆禁用思考功能。
