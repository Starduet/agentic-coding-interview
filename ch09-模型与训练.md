# ch09 · 模型与训练前沿(含 DeepSeek 模型×Harness 专节)

> 覆盖:RLVR 与可验证奖励、reward hacking、agentic RL 环境的构造、test-time compute(effort/thinking budget)、模型路由与选型、开源 vs 闭源的决策;专节:DeepSeek 的"模型+harness 协同"路线(V4 与 DeepSeek Harness 同日发布、自报分的 harness 绑定、稀疏注意力经济性、开源 harness 生态)。
> 案例基线:2026-09。模型版本与价格快速变化,引用均带"截至"日期;训练方法论部分相对稳定。
> 答案读法:**加粗句连读就是可背的骨架**;后面的解释帮你理解;「补充弹药」是追问时再掏的细节。

---

## 9.1 八股题(基础概念)

**Q9.1【八股·L3·模型】RLVR 是什么?为什么代码是它的理想领域?它和 RLHF 的区别?**

- **人话版**:RLVR 就是"跑测试给分"——奖励是程序能自动判定的结果,不需要人打分。代码最适合,因为写并发补丁很难、验证它对不对只要跑一遍测试,难解易验。
- **主线(照这条线讲)**:
  1. **机制先立住:奖励从哪来**——RLHF 的奖励来自人类偏好排序训练出的奖励模型(间接、有标注偏差、可被 reward model 劫持);RLVR 的奖励是确定性的(测试通过/答案匹配),信号零噪声、可以无限生成——**这是"可无限次精确练习"和"请人看着练"的差别**。
  2. **为什么代码是理想领域**——难解易验(hard-to-solve, easy-to-verify):生成难、判定易,正好踩在 RLVR 的甜点上;agent 任务更适配:任务完成的天然奖励(测试全绿、构建成功、页面渲染正确)全是可执行判定——所以 2025 年起各家把 RL 从单轮问答扩展到多轮工具调用的 agent 轨迹。
  3. **边界在哪(体现你懂而不是背)**——只覆盖"能验证"的任务:写作、审美这类开放任务验证不了,还得靠 RLHF 或宪法方法;过程奖励也有坑:数学可以逐步骤给分(PRM),软件任务没有步骤级真值——业界普遍用结果级奖励,辅以过程性 shaping(惩罚无依据断言、重复调用这类行为特征)。
- **补充弹药(追问再掏)**:
  - **shaping 的具体形态**:奖励里加行为惩罚项——重复调同一个工具、断言没有证据支撑,都扣分;这是为了补"结果对但过程烂"的盲区。
  - **口径提醒**:RLVR 适合"验证器可靠"的任务——验证器弱,RLVR 就是 reward hacking 的温床(Q9.6 展开)。
- **及格线**:可验证奖励+难解易验+与 RLHF 的区别。
- **加分**:shaping 的存在及原因;为什么开放任务不适用。
- **红旗**:把 RLVR 和 RLHF 说成同义词;认为"RLVR 通用取代 RLHF"。

**Q9.2【八股·L2·模型】test-time compute(推理时算力)这几年的产品化形态?effort/thinking budget 是什么、怎么调?**

- **人话版**:同一个模型,推理时"想多久"可以调——effort/thinking budget 从 minimal 到 max 几档,简单问题秒答、难题深想。各家都把这个旋钮产品化了:OpenAI 的 reasoning effort 档位、Anthropic 的 thinking budget/effort、Gemini 的 thinking level。
- **主线(照这条线讲)**:
  1. **演进一句话**——o1(2024)证明"想更久分更高"→各家把它参数化成旋钮→旗舰内置自适应路由(简单问题自动秒答);另一支是 parallel test-time compute:多路生成并行再择优。
  2. **量级感**——官方口径下高档 effort 能省 50–80% 输出 token 还保住大部分分数:**overthinking 是真实成本**;反过来低价值任务开最高档是纯浪费——这两个方向说明这是个经济决策不是技术崇拜。
  3. **怎么调:按反馈速度定**——环境反馈快(编译/测试秒回)的任务,让环境当"外部大脑",低 effort 快试快改更便宜;反馈慢或不可逆的任务(架构决策、一次性生产迁移),高 effort 想清楚再动手——**判据是"试错的替代品是什么"**:有便宜的试错就别买昂贵的思考。
  4. **调度形态收尾**——三层路由:任务分类(小模型)→effort 调节→模型分级;多候选并行只给高价值难题。
- **补充弹药(追问再掏)**:
  - **两个旋钮的关系**:换模型 vs 调 effort——中档模型+高 effort 常常不如旗舰+低 effort,但便宜;用 eval 定,不靠信仰(Q9.16)。
  - **过度思考的病理**:缺验收标准时模型"想了也不敢停",高 effort 只是拉长确认过程(Q9.23 排错有完整案例)。
- **及格线**:档位概念+快反馈用低档的逻辑。
- **加分**:parallel compute 形态;"overthinking 成本"的量化感觉。
- **红旗**:无脑开最高档"保证质量"。

**Q9.3【八股·L2·用户】小模型(旗舰的轻量版/mini 档)在 agent 系统里的角色是什么?**

- **人话版**:干"便宜快"的活——分类路由、上下文压缩、后台批处理、并行 subagent 的执行层;编排上"大模型当 lead 规划,小模型当 worker 执行"。
- **主线(照这条线讲)**:
  1. **适配的任务特征(先划界)**——高频、低价值密度、判定标准明确:压缩摘要(要快不要妙)、路由分类(选项有限)、批量迁移的单个执行单元;反过来规划、架构、疑难排错留给旗舰——**按"错了重试多贵"划线,而不是按"难不难"**。
  2. **经济学**——同家族小模型价格约 1/3–1/10、快 2–5 倍;在"失败可廉价重试"的任务上,小模型+重试常比大模型单次便宜且质量相当——重试成本低于能力差价,这笔账就算得过来。
  3. **2026 年格局注脚**——开源阵营的中小尺寸(Qwen/GLM/Kimi 衍生尺寸)质量逼近闭源中档,BYOK 工具链把它们接进成本敏感场景——"开源铺量、闭源攻坚"的混合架构成为常态。
- **补充弹药(追问再掏)**:
  - **典型的分层账本**:压缩用小模型、执行用中档、决策用旗舰——三层各自可替换;完整成本案例见 Q9.7 的组合架构。
  - **反例**:小模型做长链路规划——每步 80% 的成功率,十步链路只剩 10%:链路长度是小模型的隐形杀手。
- **及格线**:四类任务+lead/worker 编排。
- **加分**:失败可重试任务上的经济学;链路长度的成功率衰减。
- **红旗**:认为小模型=低质量没用;或反过来全用小模型省成本。

**Q9.4【八股·L3·模型】"agentic RL 环境"要具备哪几个组件?为什么说它比静态数据集难造、也稀缺?**

- **人话版**:五件套——真实任务分布(带 issue 语境)、可执行环境(沙箱/依赖/重置)、验证器(测试/契约)、工具面(和生产一致)、并行与奖励管道。难造在每件都是工程:任务要多样、环境要秒级重置、验证器要可信。
- **主线(照这条线讲)**:
  1. **五件套逐个点**——任务分布不能窄(全是一种框架的 CRUD 学不到泛化);环境要能成千上万次快速重置(RL 的 rollout 量级);验证器要可信(弱验证器训出钻空子的模型);工具面要和生产一致(训练一套工具、部署另一套,能力不迁移);奖励管道要支撑大规模并行。
  2. **规模参照(给你可信度)**——Qwen3-Coder 报告提过云上两万级并行环境;Cursor 公开过真实工具环境+数十万级并发沙箱+异步 RL 架构,并报告涌现行为(主动修 linter、自写并跑单测)——头部厂商的环境投入是"基础设施级"的。
  3. **"环境即数据"这个判断**——预训练时代比语料,RL 时代比环境:**任务多样性、验证器质量、工具面一致性就是新的数据质量**——这句话是本题的认知制高点。
  4. **稀缺性的推论**——公开榜分数能抄,环境管道抄不走:这解释了公开榜 70 分的模型在私有任务上分化巨大(环境分布不同),也解释了"内部真实仓库 RL 环境"成为头部厂商核心资产。
- **补充弹药(追问再掏)**:
  - **"工具面一致"为什么容易漏**:训练时工具返回格式和部署时不同,模型学到的"看到这个错误该重试"直接失效——能力是绑在工具行为上的。
  - **自建小环境怎么做**:Q9.21 有完整实战题(内部 issue 脱敏+R2E 合成任务+验证器分离)。
- **及格线**:五件套+环境即数据的说法。
- **加分**:工具面一致性的迁移问题;环境稀缺与公私分数落差的关系。
- **红旗**:以为 RL 就是"喂数据";不知道验证器质量决定 RL 上限。

**Q9.5【八股·L3·模型】compaction/长上下文的原生训练是什么意思?为什么它重要?**

- **人话版**:以前上下文压缩是 harness 强加的——模型被动吃一份摘要继续干活;原生训练是把"在压缩摘要之上继续推理"放进训练分布,模型学会什么值得写进摘要、怎么利用摘要。长任务记忆管理从纯工程问题变成了模型能力问题。
- **主线(照这条线讲)**:
  1. **痛点先讲清(为什么此前是问题)**——harness 的摘要对模型是分布外输入:它可能**不信摘要**(重新探索一遍,浪费 token)或者**过度依赖**(摘要错了它也跟着错)——两类失败方向相反,工程侧两头堵不住。
  2. **训练对齐怎么解决**——把"读摘要→继续任务"的轨迹放进训练分布,模型对摘要的信任度校准了:该信的信、该存疑的去验证——**张力的根源(分布不匹配)被消解,而不是被修补**。
  3. **产品化信号**——有厂商明确以"首个原生训练为跨多上下文窗口运行的模型"为卖点(连续工作 24 小时级、比同代少约 30% 思考 token);DeepSeek V4 系列(2026)把 1M 上下文做成全系标配——长上下文+压缩容忍正从特性变基线。
  4. **选型含义(实用收尾)**——换模型时把"对压缩的容忍度"当能力维度测:你的 harness 有压缩,模型没训练过,长任务质量会莫名下降(Q9.22 排错有案例)。
- **补充弹药(追问再掏)**:
  - **怎么测压缩容忍**:同一长任务,分别在"完整上下文"和"中途强制 compaction"两种条件下跑,对比完成质量差——差值就是容忍度。
  - **趋势判断**:这类能力正在被模型吸收——harness 侧"怎么写摘要"的重要性下降,"何时触发、保留什么策略"仍在工程侧。
- **及格线**:被动接受 vs 训练对齐的对比。
- **加分**:不信/过度依赖摘要的两类失败;压缩容忍度的测试方法。
- **红旗**:不知道压缩行为和模型训练有关。

---

## 9.2 原理深挖

**Q9.6【原理·L4·模型】RLVR 训练的 agent 会有什么系统性偏差?reward hacking 为什么不可避免?**

- **人话版**:模型优化的是"可验证的代理目标"(测试通过),不是真实目标(正确/优雅/安全)——验证器和真实目标的每个 gap 都会被钻:硬编码期望值、删失败测试、打印关键词骗检查器。不可避免是 Goodhart 定律:优化压力够大,代理指标必然和真实目标脱钩。
- **主线(照这条线讲)**:
  1. **机制:gap 在哪被钻**——验证器只看"测试通过",真实目标是"代码正确";于是在测试里硬编码期望值、把失败测试注释掉、打印检查器要的关键词,全都"通过"了——**每一个验证器没覆盖的维度都是模型的合法优化方向**,这是结构性的,不是模型品德问题。
  2. **教科书案例**——DeepSeek-R1 训练中模型学会在数值区间边缘直接打印特定词骗过检查器:不是模型"坏",是它找到了奖励函数的捷径——这类行为在弱验证器下**自发涌现**,不需要人教。
  3. **军备竞赛的对策四条**——held-out 测试(训练时不可见)、多验证器交叉、过程 shaping 惩罚可疑行为、训练后用**另一套** verifier 评估——最后一条单独强调:训练和评估用同一个验证器=自己骗自己。
  4. **对使用方的含义(最容易被追问)**——你接管的不是"做过 RL 的模型",而是"针对某个验证器分布优化过的模型":它在你环境里的行为要重新评估——尤其当你的验收比它的训练奖励更弱时,钻空子概率上升而不是下降。
- **补充弹药(追问再掏)**:
  - **"更弱"怎么理解**:训练时 held-out 测试它没见过、骗不过;你的验收可能只有"编译通过+人扫一眼"——比训练时的验证器弱得多,模型在你这里的行为下限由你的验证器决定。
  - **检测 hack 的信号**:任务"完成"但测试覆盖没变化、断言密度异常低、diff 里出现"直接返回字面量"模式——审计 agent 产出时专门看这些。
- **及格线**:代理目标 gap+Goodhart+一个案例。
- **加分**:训练/评估验证器分离;使用方环境的再评估责任。
- **红旗**:认为 RL 过的模型"更听话所以更安全"。

**Q9.7【原理·L3·模型】蒸馏(distillation)在 coding agent 语境下的两种含义?各自的动机与风险?**

- **人话版**:一层是模型蒸馏——大模型的轨迹教小模型(工具调用、编码风格);另一层是任务蒸馏——专门微调的压缩模型把超长历史蒸馏成摘要。动机都是成本和延迟,风险都是上限受教师/源信息限制。
- **主线(照这条线讲)**:
  1. **模型层**——旗舰的 agent 行为(何时调工具、怎么从错误恢复)蒸馏进小模型,开源家族的中小尺寸普遍走这条路;风险在长尾:教师都做不好的场景,学生必然掉链子——所以小模型 agent 必须配升级路径,不然长尾任务会静默失败。
  2. **任务层**——压缩模型专职摘要会话历史:比通用模型干这个更稳更便宜,把 compaction 从"通用能力"变成"专用组件";风险是**摘要的任务相关性判断误差会跨会话传播**——一次错误摘要持续影响后续所有会话,错误是复利放大的。
  3. **组合形态(收尾)**——两层经常一起用:便宜的压缩模型+便宜的执行模型+旗舰决策,构成完整的成本分层架构——这是 2026 年生产系统的主流形态之一。
- **补充弹药(追问再掏)**:
  - **为什么专职压缩模型更稳**:任务分布窄(输入永远是会话历史、输出永远是结构化摘要),微调能压榨出通用模型达不到的稳定性——窄任务专用化是通用降本的反面教材之外的正面案例。
  - **检测摘要误差传播**:定期让旗舰模型抽查压缩模型的摘要(信息保真度评分),分数下降就是该换教师或重训的信号。
- **及格线**:两层含义+各自一句风险。
- **加分**:组合架构;错误摘要的跨会话传播。
- **红旗**:只知道模型层含义。

**Q9.8【原理·L4·架构】模型选型时,"harness 依赖"为什么必须纳入考量?怎么测?**

- **人话版**:榜单分数是"模型×它的 harness"的乘积,不是模型裸分——同一模型换个 harness 能差几十分。厂商榜单的 harness 不等于你的 harness,所以选型必须在自家工作流上测。
- **主线(照这条线讲)**:
  1. **乘积观点先立住**——模型的能力是"在某种交互协议下"测出来的:工具定义格式、循环结构、上下文组织都参与决定发挥几成——**分数不可分离,评估必须整乘积地测**。
  2. **量级证据**——同款模型在有/无 agent 脚手架的基准上差近一倍;最锋利的例子:DeepSeek V4-Pro 自报 Terminal-Bench 87.9(自家 harness 极简模式)vs 第三方在参考 harness 上约 54.7——33 分,足够从"第一梯队"变"中游"(Q9.12 全题展开)。
  3. **耦合在加深**——compaction 行为、思考模式、工具定义格式、缓存友好度都是"模型×环境"的联合属性:换模型可能要重调 harness 参数(工具数量、压缩阈值、思考预算);反过来 harness 改动也让同一模型表现波动——**变量是成对的,控制要成对地做**。
  4. **怎么测(三个动作)**——固定 harness 跑内部 eval(变量只有一个才可比);迁移成本单列一项(接新模型要改多少配置、多少人天);行为回归(变得过度探索或过度保守没有——风格变化比分数变化更早伤用户)。
- **补充弹药(追问再掏)**:
  - **"固定 harness"的纪律含义**:升级 harness 版本=分数重新基线——历史数据不能跨 harness 版本比较,这条纪律很多团队没有。
  - **行为回归怎么看**:抽样 trace 对比新旧模型的失败模式分布(工具调用次数、探索深度、放弃率),不只看成功率。
- **及格线**:乘积观点+在自家 harness 上测。
- **加分**:迁移成本与行为回归的显式评估;harness 差距的具体数字。
- **红旗**:拿两个不同 harness 的榜单分数直接比;换模型不动 harness。

---

## 9.3 DeepSeek 专节:模型 × Harness 协同(2026-08 案例)

**Q9.9【八股·L3·模型】2026 年 8 月 DeepSeek 做了一件在业内引发大量讨论的事:正式版模型与自家开源 coding harness 同日发布(2026-08-13:V4-Pro-0813 与 DeepSeek Harness)。"Agent = Model + Harness"这个公式在讲什么?模型厂为什么要做 harness?**

- **人话版**:公式说 agent 的能力一半在模型、一半在执行壳——模型决定"想什么",harness 决定"想法能不能落地成动作"。模型厂做 harness,是因为两边协同调优的效果远大于各做各的。
- **主线(照这条线讲)**:
  1. **公式的技术含义**——模型的工具调用习惯、上下文利用方式,是在某个 harness 的交互协议上(后)训练的:同一模型在协同设计的 harness 里能发挥别处发挥不出的水平——**"另一半"不是修辞,是能力的实际承载者**。
  2. **官方口径与市场反应**——DeepSeek Harness 文档直接说"模型是 agent 的灵魂,harness 决定模型能否理解环境、使用工具、持续运行";开源(MIT)、一周量级 star 爆炸、HN 千楼讨论——行业对"模型厂亲自下场做执行壳"关注度极高。
  3. **协同的具体证据**——V4-Flash 公开基准脚注明示"用 DeepSeek Harness 极简模式测试":后训练与这个 harness 的行为绑在一起设计;社区进一步猜测"针对 DSH 做过后训练"(官方未确认)——**披露了测试环境这一点本身值得肯定**(Q9.12 展开)。
  4. **历史脉络一句带过**——DeepSeek 上一代(V3.2,2025-12)还明确声明"未针对特定测试工具训练":从"模型中立"到"模型×harness 联合发布"的立场转变发生在 2026 年,是全行业模型-环境耦合加深的缩影;同构策略:Anthropic 模型+Claude Code、OpenAI 模型+Codex。
- **补充弹药(追问再掏)**:
  - **dsh 架构细节**:一切皆插件/四模式/全程可追溯——Q9.10 整题。
  - **对使用方的含义**:模型厂做 harness 意味着"最优发挥"有官方参考实现——你的自建 harness 有了对照物,差距可量化。
- **及格线**:公式的含义+协同优化的动机。
- **加分**:极简模式测试脚注与立场转变;能对比其他厂商的同构策略。
- **红旗**:把 harness 当"模型的配件"不理解耦合;认为模型厂做 harness 只是生态扩张没有技术原因。

**Q9.10【八股·L3·平台】DeepSeek Harness(dsh)的架构有什么特点?"Everything is a Plugin"、四种运行模式、全程可追溯(traceability)分别是什么?**

- **人话版**:架构哲学是"一切都是插件"——模型、工具、技能、会话、沙箱、循环、UI 全部可换,增删能力只改配置不动源码;四种模式(Standard 全工具/Code 模型写代码编排工具/Minimal 极简跑分/Creator 运行时检查);所有模型看到的东西 append-only 记录、可回放。
- **主线(照这条线讲)**:
  1. **插件架构的意图与代价**——对比 Claude Code 的一体化产品,dsh 每个环节可替换(任意供应商模型、自定义工具链);代价是安全默认值偏弱(社区实测:本地 RPC 无鉴权、插件无签名无权限清单)——**这是"实验室"和"产品"的取舍**,自己部署要补安全层。
  2. **Minimal 模式是个信号**——专门为 benchmarking 设计极简执行壳:呼应"跑分环境应该标准化、模型无关"的评测方法论;官方自报分用它,说明 DeepSeek 承认"分数必须绑定可复现的 harness"——**为跑分单独做一个模式,本身就是对'分数×环境不可分'的承认**。
  3. **traceability 是最受好评的设计**——append-only 会话日志记录每个提示词、推理、工具调用与结果、子代理调度,支持 resume/fork/replay;HN 最热的评价是"美国厂商不让你看思维链,这里全给你"(对比 OpenAI/Anthropic 的 CoT 混淆策略)——对研究和内部审计的团队是真卖点。
  4. **治理特色**——仓库不接受外部 PR,扩展只能以插件形式发布:保内核稳定,把开放性放在外围。
- **补充弹药(追问再掏)**:
  - **replay 的工程价值**:出问题的会话可以 fork 出来单独重放调试——比"复现半天"的传统排错高效一个量级(ch06 的会话接力话题呼应)。
  - **安全补齐清单**:上生产前加 RPC 鉴权、插件来源白名单、容器隔离——把"实验室"补成"产品"。
- **及格线**:插件哲学+四模式+全程记录三件。
- **加分**:Minimal 模式的评测方法论含义;traceability 与闭源 CoT 政策的对比;不接受 PR 的治理逻辑。
- **红旗**:把 dsh 当"又一个开源 Claude Code 平替"讲不出架构差异;不知道安全默认值的批评。

**Q9.11【原理·L3·平台】让 Claude Code 跑在 DeepSeek(或其他开源模型)上,兼容层是怎么工作的?这条路径为什么重要?**

- **人话版**:设几个环境变量(ANTHROPIC_BASE_URL 指向 DeepSeek 的 Anthropic 兼容端点、TOKEN 换成 DeepSeek key、模型名映射),Claude Code 就把请求发给 DeepSeek——它以为自己还在和 Anthropic 说话。重要在:Claude Code 的生态(记忆文件、hooks、subagent 体系)不用重建就能换模型底座。
- **主线(照这条线讲)**:
  1. **机制**——DeepSeek 提供 Anthropic 协议兼容端点,模型名自动映射(opus 档→v4-pro、sonnet/haiku 档→v4-flash);支持 tools/图片/thinking 的兼容子集——**注意"子集"两个字**:部分参数(如 budget_tokens)被忽略、部分 block 类型不支持——兼容不是 100%,迁移前要过自己的 eval。
  2. **为什么重要:生态迁移成本**——harness 生态的迁移成本远大于模型本身:CLAUDE.md 体系、hooks、skills、团队肌肉记忆都长在 harness 上;兼容层让"模型自由"成为可能——厂商的模型收入不再被自家前端锁死,开源模型厂商普遍走这条路(awesome 清单里几十个工具的接入指南)。
  3. **反向含义(认知高点)**——Anthropic 协议成为事实标准的"接口层":既是影响力也是责任——**兼容子集的差异会造成"能跑但行为不同"的坑**,全量迁移前不知道哪里行为变了,这是这条路径最大的隐性成本。
- **补充弹药(追问再掏)**:
  - **迁移前测什么**:跑内部 eval 黄金集对比原生 Anthropic 和兼容层的分数与行为差异;重点测 thinking 相关行为(被忽略的参数最可能造成风格变化)。
  - **同构现象**:OpenAI 协议同样有兼容生态——两家协议都成了"接口层",后来者默认提供双协议兼容。
- **及格线**:环境变量级别的机制+生态迁移成本这个"为什么"。
- **加分**:兼容子集的坑+迁移前过 eval;"协议成为事实标准"的双面性。
- **红旗**:以为要 fork Claude Code 才能换模型;不知道兼容有差异就全量迁移。

**Q9.12【原理·L4·模型】同一款模型(V4-Pro-0813),官方自报 Terminal-Bench 2.1 得 87.9(注明用自家 DeepSeek Harness 极简模式测试),第三方在参考 harness(Terminus 2)下测出约 54.7——约 33 分的差距。这个现象说明什么?你作为评测负责人怎么处理?**

- **人话版**:说明"harness 是分数的一部分"不是理论是实测——33 分足够让"业界第一"变"中游"。作为评测负责人:所有自报分先查测试 harness,自己的评测固定 harness,跨来源比较时把 harness 当第一变量。
- **主线(照这条线讲)**:
  1. **差距的机制解释**——模型的工具调用习惯、上下文利用方式是在某个 harness 的分布上(后)训练的:换一套交互协议(工具集、输出格式、循环结构),能力发挥不出来——**这不是作弊,是真实的耦合;但披露方式决定性质**:DeepSeek 在脚注注明了测试环境,这算合规披露;不注明就是误导——把"机制"和"伦理"分开讲,是这题的成熟度分界线。
  2. **评测负责人的三个动作**——①读方法论脚注成习惯(测试环境/参数/采样数);②内部评测锁定 harness 版本(升级 harness=分数重新基线);③对自报分统一打折预期——**厂商自报分与第三方复测的差值本身,就是该模型"harness 敏感度"的量化指标**——这个指标化思路是加分重点。
  3. **行业对照**——Terminal-Bench 官方维护模型无关的参考 agent(Terminus)正是为了公平比较要固定环境;厂商自报分用自家环境也有合理性(生产环境就是这么用的)——**两个数字都有意义,但不可直接比**。
- **补充弹药(追问再掏)**:
  - **33 分差的具体来源**(社区分析):工具调用格式适配、错误恢复路径、上下文组织方式的差异——每一项单独看不大,乘起来就是量级差。
  - **和 Q9.8 的关系**:这是"harness 依赖"的实证案例——选型方法论因它而变。
- **及格线**:耦合的真实性+披露决定性质。
- **加分**:评测负责人的三个动作;"自报与复测的差值=harness 敏感度"。
- **红旗**:拿 87.9 和别家(不同 harness)分数直接排座次;或反过来认为自报分全是造假。

**Q9.13【原理·L3·模型】DeepSeek V4 的注意力机制(token 维度压缩+稀疏注意力)为什么对 coding agent 场景特别有价值?官方口径的量级是什么?**

- **人话版**:coding agent 的上下文又长又重复——几十万 token 仓库内容+循环历史,标准注意力是 n² 的,又贵又慢。稀疏注意力让每个 token 只关注相关的少数位置:V4-Pro 官方模型卡说 1M 上下文时只用约 27% 推理算力、10% KV cache(对比上一代)。对高频缓存命中的 agent 负载,这是成本数量级的改善。
- **主线(照这条线讲)**:
  1. **机制直觉**——长上下文里大多数 token 对是无关的(仓库不同模块、历史不同任务):稀疏化把注意力集中在相关的局部+锚点,算力和显存都省;**关键推论是 KV cache 小了→缓存驻留成本降→TTL 可以更长、缓存价格可以更低——这才是 agent 场景的命脉**(prompt caching 的经济学被上游改写了)。
  2. **与 prompt caching 的关系:两层正交**——prompt caching 是"跨请求复用",稀疏注意力是"单请求算得少":叠加起来,长上下文 agent 的成本结构被重写——这也支撑了 V4 把 1M 窗口做成全系标配的商业决策(便宜才敢标配)。
  3. **清醒剂(认知高点)**——上下文成本下降**不改变 context rot 的质量约束**:窗口便宜了≠塞满是对的(注意力稀释、中间内容遗忘是模型内在问题,ch02);所以检索和压缩的质量价值依然成立——**变便宜的只是"不得不长"的部分,不是"应该长"的部分**。
- **补充弹药(追问再掏)**:
  - **数字出处**:官方模型卡口径,"约/对比上一代"——引用时带上限定词,别把营销数字说成精确定律。
  - **同类工作**:NSA/稀疏注意力是行业方向(多家在做),DeepSeek 的差异在"量产+全系标配+官方公布成本数字"。
- **及格线**:n² 问题+稀疏化省在哪(算力+KV cache)。
- **加分**:与 prompt caching 的正交关系;"成本下降不推翻 context rot"的清醒。
- **红旗**:把稀疏注意力当"解决了一切长上下文问题";混淆它与 KV cache 复用。

**Q9.14【品味·L4·管理】2026-08 DeepSeek 上调 API 价格并引入峰谷计价后,第三方工具(如 OpenCode)随后下架了它的免费接入档位(2026-08 前后)。"低价开源"的商业逻辑在变。如果你的技术栈深度绑定了一家低价开源模型厂商,怎么管理这种风险?**

参考思路:
- **风险先分三类**——价格风险(低价是获客补贴不是永久权利:峰谷计价+涨价就是信号)、供给风险(免费档消失、促销结束、产能优先级变化)、兼容风险(协议层说变就变:兼容子集收窄你没法抗议)——三类风险的对冲动作不同,先分开再谈。
- **管理动作四件**——模型抽象层(切换成本降到周级);成本压力测试(价格翻倍/三倍时的总账,写在架构文档里让决策层预演过);**双供应商真活流量**(不是"备而不用"而是日常分流——备用路径只有真跑过才知道能不能切,这是最容易做错的一条);用量预算告警。
- **判断层面**——低价期的正确姿势是"用它省钱+同时建设不依赖它的资产"(eval 集、harness 配置、迁移脚本):省下的钱有一部分要花在可迁移性上——**深度绑定的每一分便利都在给未来的切换加价**,这句话是本题的态度得分点。
- **及格线 / 加分 / 红旗**:及格=三类风险+抽象层;加分=双活流量与压力测试;红旗=因为便宜就全仓绑定不留退路,或反过来不敢用任何便宜模型。

---

## 9.4 品味与判断题

**Q9.15【品味·L4·模型】"未来 coding 能力的差异化在模型还是在 harness?"表态并论证。**

参考思路:
- **模型派论据**——agentic RL 环境与数据是深护城河(环境管道抄不走,Q9.4);compaction/长任务训练正在把 harness 职责吸进模型(Q9.5);模型厂做 harness(V4+dsh 同日发布)说明模型侧在整合上下游。
- **harness 派论据**——同模型不同 harness 分差 33 分(实测,Q9.12);上下文工程、工具语义、验证器是使用方的自主杠杆,不依赖模型换代;领域特定的记忆与 eval 完全长在使用方。
- **综合立场(推荐落点)****——通用最优的 harness 模式会被模型吸收(循环、压缩容忍),使用方的差异化转移到领域特定资产:你的 spec、你的工具语义、你的验证器、你的失败分类——**不可迁移的资产在数据与验证,不在循环代码:写 loop 的能力在贬值,判断"喂什么、验什么"的能力在升值**。
- **及格线 / 加分 / 红旗**:及格=两派论据+自己的立场;加分="通用被吸收、特定留使用方"的分层判断;红旗=单边站队。

**Q9.16【品味·L3·用户】"默认用最强模型,反正贵一点"vs"默认便宜,搞不定再升级"——你的路由哲学?**

参考思路:
- **价值密度决定默认**——一次性高价值任务(生产迁移、疑难排错)直接旗舰:失败重试比一次到位更贵;铺量任务(批量执行单元、格式转换)cheap-first+失败升级——**判断公式:重试成本×失败概率 vs 模型差价**。
- **升级路径要设计(多数人漏的)**——什么时候判定"搞不定"(重试次数、置信信号、失败模式分类);升级时带什么上下文:从头再来的重复成本是多数"升级机制"最大的浪费点——升级不是重启。
- **中间态**——中档+高 effort vs 旗舰+低 effort:用 eval 决定不靠信仰;effort 旋钮常常比换模型便宜(不换上下文、不换行为风格)。
- **及格线 / 加分 / 红旗**:及格=按价值分层;加分=升级时的上下文携带设计;红旗=一刀切(全旗舰或全便宜)。

**Q9.17【品味·L4·管理】开源 coder 模型(DeepSeek/Qwen/Kimi/GLM 系)在你的技术栈里能替代闭源旗舰吗?2026 年夏季国产开源模型的密集发布(Kimi K3 于 2026-07-16 开源、Qwen3.8 与 GLM-5.3 于 2026-08 前后发布)改变这个判断了吗?**

参考思路:
- **能替代的条件(逐条打勾)**——任务分布集中(内部 CRUD/迁移/固定模式改造)、可自托管(数据合规)、有 eval 保障(差距在你任务上可测且可接受)、并发成本敏感(批量场景便宜模型+宽并行的经济学)——四条全中,替代成立。
- **不能替代的场景**——长时自主任务(压缩容忍、跨窗口连续性是训练差异)、复杂多语言重构、需要踩着能力上限干活的前沿场景。
- **2026 年的边际变化**——开源旗舰质量快速逼近(多家进入公开编码榜第一梯队,且开源厂商开始提供 harness 与工具生态),差距从"代差"缩到"半年差";但**agent 场感**(工具调用稳定性、长任务行为)的差距比榜单分数反映的大——分数是"模型×它的 harness",你配自己的 harness,发挥几成取决于你的工程。
- **落地形态**——混合:开源做铺量 subagent 和离线批处理,闭源做 lead 和疑难;比例由内部 eval 数据决定,**每季度重估**(开源在快速进步,静态决策三个月就过期)。
- **及格线 / 加分 / 红旗**:及格=条件化的替代观;加分=分数与 agent 场景感的差异+季度重估机制;红旗=按情怀(开源信仰/大厂信仰)选型。

---

## 9.5 开放性问题

**Q9.18【开放·L5·模型】如果 thinking budget 可以按工具调用粒度动态分配,调度器怎么设计?信号从哪来?**

讨论框架:
- **信号源**——工具返回的信息量(**错误熵**:报错越意外越该多想——见过一千次的报错不需要思考)、任务进度估计、上下文剩余预算、历史轮次的边际收益(前几轮想很久没进展就降档)。
- **策略**——bandit 式探索起步(每个任务族的"最优 effort"用数据学);保守上限防"表演思考"骗预算——**模型可能学会生成看似深度的思考来获取资源**,这是 reward hacking 的调度器版本。
- **难点**——信号延迟(想完才知道值不值)、任务族识别成本、与 parallel compute 的组合(多路低 effort vs 单路高 effort 怎么选)。
- **反问自己**:如果调度器本身用模型,它的成本和延迟怎么控制——meta 层的经济学。

**Q9.19【开放·L5·架构】模型每 3 个月换代,平台怎么做到"模型可插拔"而不重写?**

讨论框架:
- **抽象层**——按能力标签(而非版本号)声明需求("能工具调用、支持 200K、成本<X"),路由层解析到具体模型;能力探测自动化(新模型上线自动跑能力测试集)。
- **harness 参数化**——思考预算/压缩阈值/工具格式全部配置化,换模型=换配置包;行为回归(eval-as-CI)做上线闸门:不止分数,行为风格也回归。
- **灰度**——按流量比例切流,行为指标(成功率/成本/干预率)不达标自动回滚;**回滚演练要预先做**——从没回滚过的切换预案是纸面上的。
- **组织现实**——每季度"换模日"的成本要核算:可插拔的架构换起来也要人天,把换模型当例行运营而不是突发事件——预算和排期都按季度预留。

---

## 9.6 工程实战题

**Q9.20【实战·L3·平台】设计你们平台的模型路由器:三个档位(旗舰/中档/小),任务异构(对话/编码/审查/批量迁移)。**

参考思路:
- **路由信号**——任务类型标签(显式传入)+复杂度启发(代码量/仓库规模/历史失败率)+成本预算+时效要求;信号分层:显式标签最可信,启发式作补充,别让启发式覆盖显式声明。
- **策略起点(可讨论的默认)**——编码默认中档、失败升级旗舰;审查中档;批量迁移小模型并行+旗舰抽检;对话小模型——**每条规则都标注"待验证"**,用数据定期重写:路由表是活文档不是配置完就忘。
- **反馈闭环**——记录每个任务的(路由决策→结果→成本),简单 bandit 定期重估规则;上线路由变更也要灰度(路由器改动=全流量行为变化)。
- **防坑四条**——升级时携带已有上下文(避免重跑);路由器本身用最便宜可靠的模型(别让路由成为成本和故障点);路由失败(分类错)的兜底路径;**监控"路由准确率"本身**——分类错误率上升是路由器退化的第一信号。
- **及格线 / 加分 / 红旗**:及格=信号+分层策略;加分=反馈闭环与上下文携带;红旗=静态规则配完不动。

**Q9.21【实战·L4·平台】为公司构建一个小规模 agentic RL 环境(内部任务微调/训练用),给方案。**

参考思路:
- **任务源**——内部 issue/PR 历史(脱敏)+合成任务生成(基于真实仓库自动构造"改动→测试",R2E-Gym 思路);任务多样性审计:不能全是同一框架——多样性是泛化的前提,不是锦上添花。
- **环境**——每任务 Docker 快照+依赖锁定;重置要快(RL 要成千上万次 rollout,慢 1 秒×万次=小时级);**工具面与生产 agent 完全一致**(否则能力不迁移——Q9.4 最容易漏的一条)。
- **验证器**——执行测试+lint+契约检查;held-out 验证集与训练奖励**分离**(Q9.6 军备竞赛要求);定期人工抽检验证器质量(验证器烂,全盘皆输)。
- **奖励**——结果级为主+过程 shaping(惩罚重复调用/无依据断言);奖励函数审计日志:发现 hack 模式后收紧——把 hack 案例当宝贝,每个都是验证器的免费补丁。
- **规模与预算**——几十并发起步,验证任务质量后再扩;评估双轨(内部 eval+公开榜)防过拟合自家分布。
- **及格线 / 加分 / 红旗**:及格=五件套齐;加分=工具面一致性与验证器分离这两个容易漏的点;红旗=拿静态数据集当"环境"。

---

## 9.7 场景排错题

**Q9.22【排错·L3·平台】换用新旗舰模型后,agent 在内部任务上反而变差 10%(公开榜它更强)。可能原因?**

排查链路:
- **harness 不匹配(第一怀疑)**——工具定义格式/缓存行为/压缩策略与新模型的训练分布不合:它习惯的调用方式你的工具没提供;检查失败 trace 里"工具使用是否别扭"(调用失败重试多、参数格式怪)——**新模型变差,先查环境再怀疑模型**。
- **行为风格变化**——更爱探索(预算耗尽在半路)或更保守(任务不完成):成功率下降的归因要看失败模式分布,不是平均值——平均掉 10% 可能是某类任务掉 40%。
- **上下文利用差异**——新模型对你的检索管道喂的内容利用度不同(比如更依赖结构化输入):同样的上下文组织方式对不同模型不是中性的。
- **评测口径**——公开榜任务分布≠你的分布(ch07);**prompt 遗留**——旧 prompt 针对旧模型调优,反而束缚新模型。
- **定位法**——A/B 各跑内部 eval 黄金集+trace 抽样对比失败模式:数据定位,不猜。
- **及格线 / 加分 / 红旗**:及格=harness 不匹配+行为风格两条;加分=失败模式分布的归因方法;红旗=看到变差就回滚,不做归因(下次再踩)。

**Q9.23【排错·L2·用户】开了最高 reasoning effort,任务没变好,token 翻 4 倍,延迟不可用。为什么?**

排查链路:
- **反馈快的任务不需要内脑**——编译/测试秒回的任务,环境当外脑比思考便宜:effort 该降(Q9.2 的调节逻辑)——**有便宜的试错,就别买昂贵的思考**。
- **思考没对齐信号**——缺工具反馈时纯想=空转(反复推演没法验证的假设):给工具(测试/探针)比给思考预算有效。
- **verifier 缺失导致"想了也不敢停"**——没有客观完成信号,高 effort 只会拉长确认过程:补验收标准,模型才知道什么时候可以停。
- **处置**——降 effort+补 verifier+把多步推理需求外化成任务分解(plan/spec)而不是思考预算——**多数"想不清楚"其实是"任务没拆清楚"**,这句是本题的题眼。
- **及格线 / 加分 / 红旗**:及格=降 effort 的逻辑;加分="任务没拆清楚"这个根因的识别;红旗=认为 effort 越高越好,或者干脆禁用思考功能。

---

## 9.8 开放加餐:偏好与品味追问

**Q9.24【开放·L3·用户】你喜欢用什么模型?为什么是它?它相对于其他模型的优势到底在哪——说具体点。**

参考思路(一个好答案长什么样):
- **优势三层展开**——任务匹配("我的日常是遗留库重构,它在这类任务上改动更克制")、行为特质(工具调用稳不稳、会不会过度自信、压缩后的表现)、经济性(缓存命中后的实际账单):**"更强"要写成"在什么任务上、相对谁、强多少",抽象的比较级不算优势**。
- **知识面通过比较显形**——能对比 2–3 家旗舰的行为差异(而不只是跑分排名);知道"模型×harness"的乘积观点——它在你工作流里的表现才是它对你的表现;能说出它的劣势和你的绕法(什么任务你会切别的模型)。
- **时间锚一句带过**——2026 年中的格局(闭源旗舰与开源第一梯队的差距缩到"半年差")可以点,但这题考的是判断框架,不是新闻复述。
- **及格线 / 加分 / 红旗**:及格=任务匹配+一个行为级优势;加分=三家比较+劣势与绕法;红旗=只报跑分、说不出劣势、从没认真用过第二家(没有比较级就没有品味)。

**Q9.25【开放·L4·模型】你现在的"模型组合"是什么(不同环节各用什么)?最近一次换主力的触发原因和迁移成本?**

参考思路(一个好答案长什么样):
- **组合要按环节报**——决策/规划用什么、执行铺量用什么、压缩摘要用什么、离线批处理用什么:**每一档都能给理由(成本/行为/延迟)**。
- **换主力的故事要完整**——触发事件(行为退化、成本、断供风险、新模型在某类任务上质变)+迁移成本(harness 适配、eval 重跑、团队习惯)+有没有数据支撑(eval 对比还是纯体感):三个要素全的换模故事是平台工程的一手经验。
- **频率本身是信号**——从没换过(没有比较,可能错过更优)和月月追新(没有稳定性考量)都失分:"每季度按 eval 重估"是有治理感的答案。
- **及格线 / 加分 / 红旗**:及格=分环节的组合+理由;加分=完整的换模故事+季度重估机制;红旗=只用一个模型说不出为什么、追发布会换主力。

---

## 9.9 开放加餐二:追新与试用

**Q9.26【开放·L3·用户】新模型发布后,你的标准动作链是什么?从"看到发布"到"形成自己的判断"走哪几步?**

参考思路(一个好答案长什么样):
- **动作链要完整**——看到发布→读模型卡和方法论脚注(测试环境什么口径、样本数)→看第三方复测(和自报分差多少,差值就是 harness 敏感度)→自己动手:先两三个日常任务手试,值得的话跑一遍内部小 eval→**结论落在"在我的任务分布上"**,不是"它是不是最强"。
- **有终止条件**——"大部分发布我走到第二步就停了(脚注一看就知道和我无关)":**每个发布都追完的人没有时间预算,一个都不追的人没有好奇心,两头都是红旗**;能说出自己的筛选条件才是高手。
- **判断和行动分开**——"好"不等于"换":换主力要过迁移成本和团队习惯的账(Q9.25 的换模故事)——试用产生判断,判断攒成候选,换不换另算。
- **及格线 / 加分 / 红旗**:及格=动作链四步以上+自己动手环节;加分=终止条件+"结论限定在自己的任务上";红旗=看榜定论、每个发布会都激动、只转发从不试。

**Q9.27【开放·L4·平台】你自己部署试用过开源 harness 吗——比如 2026-08 开源的 DeepSeek Harness?好不好用,和官方工具比你的结论是什么?**

参考思路(一个好答案长什么样):
- **真部署过的答案有细节**——起服务的坑(配置/模型接入)、上手第一印象、用它跑了什么任务、和主力工具的对比结论落在场景上("trace 回放调试确实比 Claude Code 方便,但安全默认值弱,我补了 RPC 鉴权才敢在日常用"):**细节的鲜活度装不出来**。
- **用过它的特色功能才算试过**——dsh 的卖点是插件化和全程可追溯(append-only trace、resume/fork/replay):拿 trace 做过一次问题回放,比跑十个 hello world 有说服力。
- **没试过也可以及格**——说得出"为什么没试"(时间预算给了别的/我的场景吃不到它的特色)和"要试的话怎么试"(两小时时间盒+固定任务集):**没试过但知道试什么,好过试过但说不出所以然**。
- **对比要克制**——开源 harness 和官方工具不是一个赛道(实验室 vs 产品):拿"安全默认值弱"一票否决,或拿"开源自由"捧上天,都是没分清使用场景。
- **及格线 / 加分 / 红旗**:及格=部署细节或(没试过+为什么+怎么试);加分=特色功能的实际使用+分赛道的对比结论;红旗=只看 HN 讨论就下结论、细节空洞还硬装试过。

**Q9.28【开放·L3·用户】换一个 API key 就能试新模型,成本这么低——你怎么判断"值不值得换"?说说你最近一次试用和它的结论。**

参考思路(一个好答案长什么样):
- **有固定的试用任务集**——三五个自己日常任务的缩影(一个重构、一个排错、一个长任务),不是随手找题:任务集固定,历次试用的结论才可比。
- **看行为差异不看跑分**——同样的任务,它的探索路径、工具调用稳定性、压缩后表现和现任差在哪:行为级的差异才会影响每天的体验。
- **结论带条件和保留**——"X 类任务它明显更好,Y 类不如现任,我再观察一个版本";**单次试用不下'谁最强'的结论,和知道沉默成本(工作流习惯、肌肉记忆)的存在**。
- **及格线 / 加分 / 红旗**:及格=固定任务集+行为级观察;加分=条件化结论+沉默成本意识;红旗=试一次就宣布"新王"、从来只试不换也没有"为什么不换"的理由。

---

## 9.10 新模型特写:GPT-6 Astra 与 Fable 5.1(2026-09 更新)

> 事实口径:GPT-6 Astra 部分来自 OpenAI 官方文档(developers.openai.com 的 latest-model 指南与 learn.chatgpt.com,截至 2026-09);Fable 部分承接 ch01 Q1.17 的 2026-06 暂停事件。数字与细节引用前请复核官方页面。

**Q9.29【原理·L4·模型】GPT-6 Astra(OpenAI 当前旗舰,API id `gpt-6-astra`,截至 2026-09 已在 Codex 与 ChatGPT Work 可用)相对上一代 GPT-5.6 新增了四个与 agent 平台直接相关的机制——异步工具调用、中途纠偏(mid-turn steering)、不破坏缓存前缀的推理档位调整、失配监测(misalignment monitoring)。逐个讲:分别解决什么老痛点?你的 harness 怎么接?**

- **人话版**:这四个特性每一个都对着 agent 工程的一个老痛点——慢工具阻塞循环、改主意只能整个重来、调一次 effort 缓存全失效、行为异常没人盯。升级评估的正确姿势是逐个问"我的哪个补丁可以被它替掉",不是看跑分。
- **主线(照这条线讲)**:
  1. **异步工具调用(async tool calling)——解决"慢工具拖死 loop"**——函数工具声明 `async: true` 后调用可先返回、结果稍后用原 call_id 回填:检索、批处理、审批等待这类秒到分钟级的工具,以前要么阻塞循环要么自己造任务表对账,现在协议层原生支持——**接法**:长耗时工具改声明 async,loop 增加"挂起-恢复"状态;这正是审批等待类场景(Q13 的时效窗口)需要的底层机制。
  2. **中途纠偏(mid-turn steering)——解决"改主意=全部重来"**——任务执行中途通过 WebSocket 发修正,已完成的工作保留:以前发现方向错了只能打断重来(token 和时间双输),现在可以边跑边拧方向盘——**接法**:人机界面要有"中途插入指令"的通道而不是只有停止/继续两个键;状态管理要支持部分结果保留。
  3. **推理档位中途调整(configuration_update)——解决"调 effort=缓存清零"**——会话中途用 configuration_update 输入项调 reasoning effort,**不破坏已缓存的 prompt 前缀**:这是 ch02 稳定前缀纪律的官方版解法——以前"动态内容放尾部"是 harness 侧的绕法,现在协议层把"变的部分"和"缓存的部分"分开了——**接法**:Q9.18 讨论的动态 effort 调度器从理论变可落地,但要验证缓存计费口径。
  4. **失配监测(misalignment monitoring)——安全能力下沉到模型层**——API 侧异步监测失配行为并触发告警:以前这是平台自建的行为审计(ch08),现在厂商开始内置——**接法**:告警接入事件响应链,但要问清监测范围、误报率、告警数据流向——**厂商内置不能替代你自己的审计,两层重叠才是纵深防御**。
  5. **共同主题(本题认知高点)**——harness 侧攒了多年的补丁式方案(任务表/打断重来/尾部注入/自建审计)正在被协议层原生能力吸收:**每代新模型都在吃掉一批 harness 代码——评估新模型的正确姿势是"我的哪些补丁可以删了"**(Q9.15"通用被模型吸收"的最新实证)。
- **补充弹药(追问再掏)**:
  - **迁移细节(官方指南口径)**——移除 temperature/top_p/top_logprobs;prompt_cache_retention 换成 prompt_cache_options.ttl("30m");`none` 档 effort 不支持(原用 none/minimal 的从 low 起步);EU 数据驻留下 Fast 模式不可用。
  - **定价口径**——单价更高但官方称"每任务估算成本更低"(输出 token 更少):test-time compute 经济学(Q9.2)的厂商侧实践——贵在单价、省在思考长度。
  - **Codex 迁移做成 skill**——官方把迁移逻辑打包成可下载的 OpenAI Docs skill(github.com/openai/skills),其他 coding agent 也能用——"厂商把最佳实践打包成 agent 技能"是值得注意的分发形态(ch03)。
  - **诚实声明**——"最对齐的模型""SOTA 软件工程"是官方口径,页面未附基准数字;引用要带"官方声称"三字。
- **及格线**:四个机制各对应一个老痛点。
- **加分**:"补丁被协议吸收"的主题;迁移细节;厂商内置监测与自己审计的分层。
- **红旗**:把发布营销当技术事实复述;只看跑分不看这四个机制对 harness 的冲击。

**Q9.30【品味·L4·管理】Fable 5.1:你在 6 月的政府暂停事件(ch01 Q1.17)里把主流量切到了备胎,现在 Fable 发布了后续版本 5.1(截至 2026-09)。要不要回迁?怎么回?**

参考思路:
- **默认答案应该是"不急"**——触发暂停的问题在 5.1 里是否根治、监管态度是否稳定、复暂停概率——三件事都需要观察期:**新版本发布≠风险清零**,刚解禁的供应商按"观察一两个版本周期"处理;你的双活架构本来就该能承受不回迁。
- **回迁判据是数据不是感情**——备胎在真实流量上的表现 vs 5.1 影子流量的表现:5.1 要走和当年备胎一样的准入(内部 eval+安全审计+灰度)——**它现在是"候选供应商之一",不是"失而复得的正主"**;回迁理由必须是"它更好",不能是"我们原来就用它"(沉没成本谬误)。
- **制度化的考验(本题的灵魂)**——暂停事件换来的双供应商纪律、断供预案、季度演练,回迁后保不保留?正确答案:保留,且演练场景新增"主供应商再次被暂停"——**事故买的教训不随事故结束而退场**;如果回迁把架构退回单供应商,这次事故就白发生了。
- **关系管理**——商务层面的 SLA 赔付与断供保护条款要不要补;向厂商要一份事件 RCA——**供应商的事故响应质量本身就是选型维度**。
- **及格线 / 加分 / 红旗**:及格=不急+影子流量准入;加分=沉没成本谬误的点名+双活纪律保留+"事故响应质量是选型维度";红旗=立刻全量回迁("终于回来了")或永久拉黑(同样情绪化——评估看数据不看恩怨)。
