# ch12 · 综合实战与场景题

> 本章是"大题集":跨章节的 system design、复合故障的排错链、take-home 实操。适合终面/加面,每题预期讨论 30–60 分钟。题目按面试官视角分组,附追问链(面试官可顺着挖)与期望回答框架。

---

## 12.1 System Design 大题

**Q12.1【实战·L4·平台】(平台工程师终面)从零设计一个企业级 coding agent 平台:500 开发者、多家模型、内网部署、要求成本可控可审计。**
期望框架:
- 分层:模型网关(多厂商路由/预算/缓存策略/failover)→ agent runtime(loop/权限/hooks/subagent)→ 工具层(内置工具+内部系统 MCP)→ 记忆层(AGENTS.md 体系+会话存储)→ 验证层(eval-as-CI/verifier 库)→ 治理层(审计/成本看板/策略下发)。
- 关键决策点:
  - 上下文工程:稳定前缀纪律、工具定义冻结、压缩策略、token 计量中间件;
  - 安全:沙箱分层(容器/microVM)、网络白名单代理、凭证不进上下文、输出 DLP、审计 trace 全落盘;
  - 多模型:任务路由器(分类→分级)、BYOK 与集中采购的成本对比;
  - 记忆与规范:组织级/项目级模板、rules 作用域、gardening 流程;
  - 可靠性:失败分类、黄金集回归、灰度切流(rainbow deployment)。
- 追问链:预算被砍一半先保什么?(缓存与路由——无损)→ 审计要求"复现任何一次 agent 决策"怎么设计?(trace+工具输入输出+版本快照)→ 某团队要接生产数据库?(只读副本+审批+脱敏)
- 评分点:分层是否清晰、是否主动谈成本与安全的 trade-off、是否有度量闭环、是否提"agent 挣扎先问系统缺什么"的反脆弱意识。

**Q12.2【实战·L4·架构】(架构师终面)设计一个"Agent Fleet":夜间无人值守运行 100 个并行 agent 任务,修复 backlog 里的中等难度 issue,早晨人审 PR。**
期望框架:
- 任务准入:可验证性筛选(有测试/可写测试)、风险分级(不可逆操作禁止)、上下文包(issue+相关文件索引+约束)预组装;
- 执行:每任务一次性环境(worktree+容器)、任务认领与去重(防 18/30 同名分支式从众)、资源限额(时长/token/网络)、重试策略(不同思路重试而非同思路硬撞);
- 验证:每 PR 机械验证(测试/lint/契约)+ reviewer agent 预审 + 早间人工抽样;
- 收敛:产出队列按置信度排序,低置信度直接丢弃(人只看 top N);
- 兜底:夜间熔断(异常率阈值全停)、事故预案(误操作回滚——依赖准入阶段的结构性禁止);
- 追问链:为什么 100 个而不是 10 个?(token 买并行,但要看 verifier 上限)→ 两个 agent 修同一文件怎么办?(准入阶段文件所有权分配)→ 早晨人看不完怎么办?(风险排序+自动化门禁前置)。

**Q12.3【实战·L4·架构】设计 GraphSpec(书的主线项目):一个"双向可读写、支持无限嵌套的架构态势感知系统"——对人是无级缩放白板,对 AI 是 MCP Server。**
期望框架(书中的定义+你的展开):
- 核心价值:解决"上下文黑盒"——AI 默认只拿顶层节点图,需要深入时 `zoom_in` 获取子模块,像人一样顺着图谱请求上下文(动态上下文路由,ch02 Q2.32 的深化);
- 数据:主资产是架构图(scanner 只是证据层——事实/证据/投影分层的应用),`.graphspec/architecture.json` 可 git 追踪;
- 工具面:get_architecture/zoom_in/add_node/update_relation/diff_architecture;写操作高亮草稿+人工 Approve;
- 反向演进:Watcher 捕捉代码依赖变化→提示补节点;
- 深挖点:与"全仓库 embedding 检索"对比(图谱是人工/半人工策划的高信号索引 vs 概率检索);版本化与多人协作冲突;分层深度限制。
- 追问链:为什么不用 RAG?(图谱的准确性/可审计/可编辑)→ 图谱过期怎么办?(证据层校验+diff 提示,承认主资产维护成本)→ MCP 工具为什么少而粗?(动态上下文路由的本意就是少喂)。

**Q12.4【实战·L3·平台】设计一个"agent 友好"的 CLI 工具(你们内部系统的命令行),使 agent 使用成功率从 50% 提升到 90%。**
期望框架:
- 接口设计:一致动词名词结构、`--json` 结构化输出、语义化错误(带修复建议与示例)、退出码语义化、dry-run 普遍支持;
- 发现性:--help 面向 agent 优化(开头一句话用途+示例命令)、错误信息里给"下一步命令"建议;
- 安全:危险操作二次确认参数、审计日志;
- 反馈循环:收集 agent 使用轨迹(失败命令→高频错误模式)迭代;
- 验证:用固定任务集测 agent 成功率,把"接口改一行"与"成功率"关联。

**Q12.5【实战·L4·模型】设计你们公司的 coding 模型评估体系,支撑模型选型与升级决策(每季度一次)。**
期望框架:
- 评测三层:公开榜(仅初筛,注意 harness 依赖)、内部黄金集(30–100 真实任务+可执行 verifier)、在线影子评测(生产流量回放);
- 方法论:固定 harness(否则分数不可比)、多采样控方差(pass@k 按真实使用场景)、成本/延迟与质量同报(Pareto);
- 治理:verifier 定期审计(防被新模型绕过)、任务滚动更新(防污染)、失败分类进报告;
- 决策流程:评测→灰度(5%→50%)→全量,质量回滚阈值;
- 追问:供应商说"我们内部也测了,分数更高"?(口径对齐:harness/k/成本)→ 预算只够测 20 个任务?(选分布覆盖,不选难度覆盖)。

---

## 12.2 复合场景排错(跨章节)

**Q12.6【排错·L4·平台】场景:你们的云端 agent 服务上线一个月,出现"三联症":①成本周环比 +80%;②任务成功率 71%→63%;③用户抱怨"半夜还有请求在跑"。给完整的诊断树。**
期望诊断树:
- 成本:缓存命中率走势(系统 prompt 是否被动态化/tools 抖动)→ 上下文长度分布(哪个任务类型爆炸)→ 模型分布(路由器是否失效全走旗舰)→ 重复请求(重试风暴/轮询踩踏);
- 成功率:失败分类变化(规格/工具/验证/运行时——新失败类别是什么)→ 上下文压缩相关失败(compaction 后丢失约束?)→ 环境(依赖漂移);
- 半夜请求:无人值守任务的调度失控(定时器重复注册)、失败重试无上限、会话未清理;
- 交叉根因猜测:一次工具定义变更同时破坏缓存(①)与工具选择准确率(②),后台任务因失败而无限重试(③)——单一变更多症状;
- 修复优先级:先止血(熔断后台重试/冻结 tools 版本),再结构(缓存修复/路由修复),后验证(eval 回归)。

**Q12.7【排错·L4·安全】场景:安全团队发现一个 agent 会话曾把生产数据库的部分用户表结构+20 行样本数据发送到一个外部"文档搜索" API。事件响应与根因。**
期望:
- 应急:吊销该 API 凭证与 agent 凭证、隔离环境、取证(trace/网络日志)、评估泄漏范围、通知链;
- 根因链:任务需要"理解 schema"→agent 用了配置里的搜索工具(白名单内)→数据进了请求体(网络策略只管域名不管内容);三层失守:敏感数据进了模型上下文(设计错误)、出站无 DLP(漏洞)、工具组合未被审计(流程);
- 修复:敏感数据分级+上下文脱敏(模型上下文不是安全边界)、出站内容扫描、数据类工具只返回 schema 不返回样本、per-task 凭证;
- 追问:是注入还是任务正常行为的误用?(看 trace 的决策点——区分"被诱导"与"合理但危险的任务执行",两者修复不同)。

**Q12.8【排错·L3·用户】场景:同一个重构任务,agent 上周一次通过,这周同样指令连续失败 5 次。你没有改任何配置。**
期望排查链:
- 变量盘点:模型(供应商静默更新/路由变化——system_fingerprint)、仓库(依赖升级/他人提交)、环境(测试基础设施状态)、上下文(上周会话的 CLAUDE.md/记忆已变);
- 定位法:复跑上周会话(--resume/重放 trace);二分新提交;
- 常见根因:上游依赖破坏了测试环境(agent 在修"不是它该修的")、模型行为变化(更保守不敢大改)、仓库漂移(上周的假设已不成立);
- 修复观:不要在失败上下文里硬磨——新会话+更新的任务描述(把仓库变化写进约束)。

**Q12.9【排错·L3·架构】场景:multi-agent review 流水线上线后,PR 平均合并时间从 4 小时涨到 9 小时,review 质量没变化。**
期望:
- 分解延迟:排队(并发限制)、subagent 执行(每个都在重新检索仓库——没有共享上下文缓存)、主 agent 综合(等最慢的)、人在回路(更多评论=更多轮);
- 根因候选:subagent 重复探索(无共享 repo map/缓存)、触发时机(PUSH 即全量重审)、评论粒度(碎评论引发长对话);
- 修复:增量 review(只看新 diff)、共享检索缓存、subagent 输出结构化(减少综合轮次)、评论聚合策略;
- 度量:质量不变的前提下,把"review 延迟"和"每 PR token"作为北极星。

---

## 12.3 Take-home 与实操题

**Q12.10【实战·L3·平台】(take-home,4 小时)用任意语言实现一个 mini coding agent:循环+5 个工具(Read/Write/Edit/Grep/Bash)+ 权限确认 + max_turns + 成本统计,在给定仓库完成"把所有 console.log 替换为 logger 并跑通测试"的任务。提交代码+设计说明+成本报告。**
评分点:
- 循环正确性(终止条件、错误回喂);工具输出截断(不截断的直接扣分——上下文意识);权限默认拒绝(Bash 需确认);成本报告含缓存意识(tools 稳定);设计说明里对"如果给 40 小时我会加什么"(compaction/subagent/eval)的判断。
- 加分:transcript 落盘可重放;失败任务的失败分类。

**Q12.11【实战·L3·用户】(take-home,2 小时)给一个陌生开源仓库写 AGENTS.md,然后用它让 agent 完成一个 hello 任务(添加一个小功能),报告:写前的探索方法、文件结构取舍、agent 一次通过率、迭代过程。**
评分点:是否先跑/读/试再写(基于事实而非猜测)、是否克制(<200 行、只写不可推断的事实)、是否包含验证命令与禁区、一次通过率的归因分析(哪些失败源于文件缺失→回写改进)。

**Q12.12【实战·L4·平台】(一周项目)为一个真实痛点建一个 eval 集:≥30 个任务+可执行 verifier+自动评分脚本,并用它对比两个模型/两种配置。交付:任务构造方法、verifier 说明、对比报告(质量/成本/延迟 Pareto)。**
评分点:任务分布论证(覆盖高频+历史失败)、verifier 的可信度自审(有没有被 hack 的空间)、方差处理(多采样/置信区间)、报告的决策可用性(给结论:哪 个配置在什么任务上用)。

---

## 12.4 压力面快问快答(节选,考反应与底层理解)

**Q12.13【快问·L2】agent 每轮重发全部历史,为什么这不是 O(N²) 的浪费?**
答:prompt caching 使旧前缀按 0.1× 计费,实际计算也被 KV cache 跳过——"重发"在计费与计算上都近似增量,只有首因(ttft)与 token 计量上是全量。

**Q12.14【快问·L2】为什么把时间戳放 system prompt 是事故,放最新用户消息里没事?**
答:前者破坏稳定前缀(缓存全失效);后者在尾部,不影响已缓存部分——这就是 system-reminder 模式的存在理由。

**Q12.15【快问·L3】subagent 失败了,主 agent 毫不知情继续,怎么兜底?**
答:分派方必须校验:结果 schema 强制(含 status/confidence)、超时与预算约束、主 agent 对"空/低置信"结果的显式处理分支;失败沉默通常源于"成功与失败返回同构"。

**Q12.16【快问·L3】一句话说明为什么"让 agent 自己 review 自己"不可靠?**
答:同一套权重有同样的盲区——自我验证缺少独立信息源,实证上无外部反馈的自我纠错不升反降;要独立就换模型/换上下文/换验证器(执行结果)。

**Q12.17【快问·L4】面试官问:"你的 harness 花了三个月,模型一升级是不是全白做了?"怎么接?**
答:分层回答——被吸收的(通用 loop 模式、通用工具设计)与不可吸收的(领域工具语义、内部 spec/记忆、verifier 库、失败分类);升级模型时 harness 的杠杆确实在贬值,但验证与上下文资产随时间增值;引用 harness 依赖(同模型不同 harness 差几十个点)反证 harness 仍有一等价值。

**Q12.18【快问·L2】cache TTL 5 分钟,你读代码审了 8 分钟再回复,会发生什么?**
答:缓存过期,下一轮全量重算(计费 miss + TTFT 显著变长);对策:1h TTL(写价 2× 但长间隔场景仍划算)、或养成"先想清楚再进会话"的节奏、或用平台的长 TTL 默认(如订阅模式)。

---

## 12.5 面试官使用说明

1. **组合建议**:一场 60 分钟技术面 = 2 道八股(暖场,定基调)+ 1 道原理(验证深度)+ 1 道实战/排错(主体)+ 1 道品味/开放(区分度);L4+ 候选人把品味题权重加到 40%。
2. **追问原则**:每道大题都有追问链,顺着一个候选人的答案挖到他/她的边界,比换题更能测出真实水平。
3. **红旗信号**:只背名词不讲机制;所有判断都站一边(无 trade-off 意识);把工具当信仰(答不出"什么时候不该用");数字张口就来但说不出处与语境。
4. **绿旗信号**:主动给判据与度量;区分"我知道的"与"我猜的";失败经验具体且有归因;能把 ch02–ch10 的知识串成系统(比如从缓存谈到成本谈到架构再谈到治理)。
