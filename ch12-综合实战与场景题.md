# ch12 · 综合实战与场景题

> 本章是"大题集":跨章节的 system design、复合故障排错、take-home 实操。适合终面/加面,每题预期讨论 30–60 分钟。题目按场景分组,附追问链(面试官可顺着挖)与参考思路。
> 案例基线:2026-08。
> 答案读法:**加粗句连读就是答题的骨架**;追问链是面试官的挖矿路线,候选人提前看等于拿到了考点地图。

---

## 12.1 System Design 大题

**Q12.1【实战·L4·平台】(平台工程师终面)从零设计一个企业级 coding agent 平台:500 开发者、多家模型、内网部署、成本可控可审计。**

参考思路:
- **先给六层骨架**——模型网关(多厂商路由/预算/缓存策略/failover——含"主模型被政府暂停"类断供预案,ch01 Q1.17)→ agent runtime(loop/权限/hooks/subagent)→ 工具层(内置工具+内部系统 MCP)→ 记忆层(AGENTS.md 体系+会话存储)→ 验证层(eval-as-CI/verifier 库)→ 治理层(审计/成本看板/策略下发)——**分层本身不加分,每层上的决策点才加分**。
- **四个关键决策点**:
  - **上下文工程**——稳定前缀纪律、工具定义冻结、压缩策略、token 计量中间件(ch02);
  - **安全**——沙箱分档(容器/microVM 按敏感度)、网络白名单代理、**凭证不进上下文**、输出 DLP、trace 全落盘(ch08);
  - **多模型**——任务路由器+失败升级;BYOK 与集中采购的成本对比;**双供应商日常分流**而不是"备而不用"(ch09);
  - **可靠性**——失败分类黄金集回归、灰度切流(rainbow deployment——切流不打断长任务中的 agent)。
- **追问链**——预算砍一半先保什么?(缓存与路由修复——无损优化先做)→ 审计要求"复现任何一次 agent 决策"怎么设计?(trace+工具输入输出+版本快照+提示词版本化)→ 某团队要接生产数据库?(只读副本+审批+脱敏,凭证不进上下文)。
- **评分点**——分层是否清晰;是否主动谈成本与安全的 trade-off;有没有度量闭环;遇到"agent 挣扎先问系统缺什么"的反脆弱意识。
- **及格线 / 加分 / 红旗**:及格=六层齐+四决策点;加分=断供预案与 trace 复现设计;红旗=只画框图不给任何决策点与 trade-off。

**Q12.2【实战·L4·架构】(架构师终面)设计"Agent Fleet":夜间无人值守跑 100 个并行 agent 任务,修复 backlog 的中等难度 issue,早晨人审 PR。**

参考思路:
- **任务准入(第一道也是最重要的闸)**——可验证性筛选(**有测试/可写测试的才进队列**——没有 verifier 的无人值守任务是开盲盒)、风险分级(不可逆操作结构性禁止)、上下文包预组装(issue+相关文件索引+约束——不要让 100 个 agent 各自探索一遍)。
- **执行**——每任务一次性环境(worktree+容器);任务认领与去重(防从众——红队实测 30 个 agent 里 18 个取同名分支的教训,ch05);资源限额(时长/token/网络);重试策略(**换思路重试,不是同思路硬撞**)。
- **验证与收敛**——每 PR 机械验证(测试/lint/契约)+reviewer agent 预审+早间人工**抽检**(风险排序,不是全看);产出按置信度排序,低置信度直接丢弃——**人只看 top N,这是规模化的人机分工**。
- **兜底**——夜间熔断(异常率阈值全停);事故预案(误操作的回滚依赖准入阶段的结构性禁止——不是事后补救)。
- **追问链**——为什么 100 不是 10?(token 买并行,但先看 verifier 上限——验证跟不上,产出的都是不可信 PR)→ 两个 agent 修同一文件?(准入阶段文件所有权分配)→ 早晨人看不完?(风险排序+自动化门禁前置,人只看边界样本)。
- **及格线 / 加分 / 红旗**:及格=准入/隔离/验证/熔断四模块;加分=结构性禁止与置信度收敛;红旗=只答"开 100 个 Claude Code 并行"。

**Q12.3【实战·L4·架构】设计一个"架构态势感知系统":大仓库的架构图作为可读写资产(对人是可缩放的白板,对 agent 是按需下钻的检索接口),解决 agent 面对 50 万行仓库的"上下文黑盒"。**

参考思路:
- **核心价值**——agent 默认只拿顶层结构图,深入时用 `zoom_in(node)` 获取子模块:**像人一样顺着结构请求上下文,而不是全仓塞入或盲目 grep**(与 ch02 Q2.34 的按需下钻、ch03 的 repo map 同族)。
- **数据与分层的纪律**——架构描述文件是唯一主资产(版本化、可 git 追踪);代码扫描器只是校验它的证据来源(从代码提取依赖关系供核对);报表/视图全部是派生物——**写操作只锚定主资产,不许为报表改报表**,否则系统"越做越假"(ch07 Q7.10 的验证锚定原则)。
- **工具面(少而粗)**——get_architecture/zoom_in/add_node/update_relation/diff_architecture;写操作产出高亮草稿+人工 Approve。
- **反向演进与诚实的成本评估**——watcher 捕捉代码依赖变化,提示"图过期了,建议补节点":**图谱的维护成本是这个方案的主要风险**——长期不维护的结构图比没有更危险,敢在方案里写这句是加分不是减分。
- **追问链**——为什么不用全仓 embedding 检索?(图谱准确/可审计/可编辑 vs 概率检索;但中小仓库 grep 就够——适用边界要自己说清)→ 图过期怎么办?(证据来源校验+diff 提示;承认成本)→ 工具为什么少而粗?(动态下钻的本意就是少喂)。
- **及格线 / 加分 / 红旗**:及格=按需下钻+版本化资产+写操作审批;加分=证据来源校验与维护成本的诚实评估;红旗=设计成几十个细粒度 getter(另一种上下文爆炸)或全仓 embedding 化(丢掉可审计性)。

**Q12.4【实战·L3·平台】设计一个"agent 友好"的内部 CLI 工具,使 agent 使用成功率从 50% 提升到 90%。**

参考思路:
- **接口设计四件**——一致的动词-名词结构;`--json` 结构化输出;**语义化错误(带修复建议与下一步命令示例**——参考 Claude Code Grep 工具把 ripgrep 诊断回传的设计,ch03 Q3.12);退出码语义化;危险操作默认 dry-run。
- **发现性**——--help 面向 agent 优化(开头一句话用途+三条示例命令,不是 man 手册);错误信息里给"你可能想要的是 X 命令"——**agent 是照着 help 和报错学的,这两处就是它的教材**。
- **安全与反馈闭环**——危险操作二次确认参数;审计日志;收集 agent 使用轨迹(高频失败的命令/参数模式)迭代接口——**"接口改一行,成功率动几个点"要能量化**,没有度量的"agent 友好"是玄学。
- **验证**——固定任务集测 agent 成功率,把接口变更与成功率关联。
- **及格线 / 加分 / 红旗**:及格=结构化输出+语义化错误+示例;加分=轨迹驱动的迭代闭环;红旗=加个 `--verbose` 就算 agent 友好。

**Q12.5【实战·L4·模型】设计你们公司的 coding 模型评估体系,支撑选型与升级决策(每季度一次)。**

参考思路:
- **三层评测**——公开榜(仅初筛,注意 harness 依赖——ch09 的 33 分教训)、内部黄金集(30–100 真实任务+可执行 verifier,ch07)、在线影子评测(生产流量回放,新模型先影子跑)——**三层各回答不同问题:值不值得测/好不好用/上线后什么样**。
- **方法论**——固定 harness(否则分数不可比);pass@k 按真实使用场景取 k;质量/成本/延迟三维同报(Pareto 而不是单指标)。
- **治理**——verifier 定期审计(防被新模型绕过)、任务滚动更新(防污染)、失败分类进报告(不只是分数)。
- **决策流程**——评测→灰度(5%→50%)→全量,行为指标(成功率/成本/干预率)不达标自动回滚。
- **追问**——供应商说"我们内部测得更高"?(口径对齐:harness/k/成本——三个都问)→ 预算只够测 20 个任务?(选分布覆盖不选难度覆盖)。
- **及格线 / 加分 / 红旗**:及格=三层+固定 harness;加分=灰度回滚与口径追问的应对;红旗=直接采信厂商自报分。

---

## 12.2 复合场景排错(跨章节)

**Q12.6【排错·L4·平台】云端 agent 服务上线一个月出现"三联症":①成本周环比+80%;②任务成功率 71%→63%;③用户抱怨"半夜还有请求在跑"。给完整诊断树。**

参考诊断树:
- **成本线**——缓存命中率走势(system 被动态化/tools 抖动?)→ 上下文长度分布(哪类任务在膨胀)→ 模型分布(路由器失效全走旗舰?)→ 重复请求(重试风暴/轮询踩踏)。
- **成功率线**——失败分类的变化(新失败类别是什么——规格/工具/验证/运行时)→ 压缩相关失败(compaction 后丢约束?)→ 环境漂移(依赖/外部 API)。
- **半夜线**——无人值守任务调度失控(定时器重复注册)、失败重试无上限、会话不清理。
- **交叉根因猜想(这题的灵魂)**——**单一变更多症状**:一次工具定义变更同时破坏缓存(①)与工具选择准确率(②),后台任务因失败无限重试(③)——三条线一个根因;诊断时先找"最近变更",再看三条线能否被它统一解释——**能被一个根因统一解释的三联症,大概率就是一个根因**。
- **修复顺序**——先止血(熔断后台重试/冻结 tools 版本),再结构(缓存/路由修复),后验证(eval 回归)。
- **及格线 / 加分 / 红旗**:及格=三条线各自排查;加分=单一根因的统一解释意识;红旗=三条线三个团队各修各的(根因没动,下周复发)。

**Q12.7【排错·L4·安全】安全团队发现一个 agent 会话曾把生产库的部分表结构+20 行样本数据发送到外部"文档搜索" API。事件响应与根因。**

参考思路:
- **应急五步**——吊销凭证(agent 的+该 API 的)、隔离环境、取证(trace/网络日志)、评估泄漏范围、通知链——**先断损失再查因**,顺序错了会二次泄漏。
- **根因链(三层失守)**——敏感数据进了模型上下文(设计错误——数据类工具不该返回样本行);出站无内容审计(只管域名不管内容——DLP 缺失);**工具组合未被审计**(一个能读生产数据的工具+一个能外发的工具同时挂在 agent 上,组合风险没人评估)。
- **修复**——数据类工具只返回 schema 与统计;出站内容扫描(URL 长度/编码模式/敏感字段检测);per-task 凭证;上下文敏感数据分级。
- **追问**——是注入还是任务正常行为的误用?看 trace 的决策点:**"被诱导"(检索内容里有指令,修输入防御)与"合理但危险的任务执行"(让它理解数据结构,它自作聪明拿了样本——修工具设计)的修复方向完全不同**(ch08 Q8.19 同题)。
- **及格线 / 加分 / 红旗**:及格=应急+三层根因;加分=注入 vs 误用的判别与差异化修复;红旗=封掉那个 API 域名就结案(组合风险还在,换个出口复发)。

**Q12.8【排错·L3·用户】同一个重构任务,agent 上周一次通过,这周同样指令连续失败 5 次,你没改任何配置。**

参考排查链:
- **变量盘点**——模型(供应商静默更新/路由变化——查响应里的模型版本指纹)、仓库(依赖升级/他人提交——git log 对比)、环境(测试基础设施状态)、上下文(记忆文件被改/会话历史不同)——**"你没改"不等于"没变":四个变量都在别人手里**。
- **定位法**——复跑上周会话(resume/trace 重放)看是否复现;二分新提交;最小化任务(砍到能复现的最小单元)。
- **常见根因**——上游依赖破坏了测试环境(agent 在修"不是它该修的"——看它失败在测试还是实现)、模型行为变化(更保守不敢大改)、仓库漂移(上周的假设已不成立——它上周依赖的一个 API 这周被同事改了)。
- **修复观**——不要在失败上下文里硬磨:新会话+更新的任务描述(把仓库变化写进约束);长期看,环境漂移的检测(依赖锁 diff 告警)是预防手段。
- **及格线 / 加分 / 红旗**:及格=四变量+重放定位;加分=二分法与环境漂移预防;红旗=归因"模型退化了"然后换模型(没定位,换完接着踩)。

**Q12.9【排错·L3·架构】multi-agent review 流水线上线后,PR 平均合并时间从 4 小时涨到 9 小时,review 质量没变化。**

参考排查链:
- **分解延迟四段**——排队(并发限制)→ subagent 执行(**每个都在重新探索仓库——没有共享的检索缓存/repo map**)→ 主 agent 综合(等最慢的)→ 人在回路(碎评论引发长对话)。
- **根因候选**——subagent 重复探索(三个 reviewer 各自 grep 一遍同样的代码——共享缓存能砍一半时间);触发时机(PUSH 即全量重审——改增量 review 只看新 diff);评论粒度(每条发现一条评论=多轮拉扯)。
- **修复**——增量 review、共享检索缓存、subagent 输出结构化聚合(减少综合轮次)、评论聚合策略(同类发现合并成一条)。
- **度量**——质量不变的前提下,把"review 延迟"和"每 PR token"当北极星:**改进要用这两条曲线证明,不是体感**。
- **及格线 / 加分 / 红旗**:及格=四段分解+重复探索根因;加分=增量 review 与聚合策略;红旗=加并发硬扛(成本线性涨,根因没动)。

---

## 12.3 Take-home 与实操题

**Q12.10【实战·L3·平台】(take-home,4 小时)用任意语言实现 mini coding agent:循环+5 个工具(Read/Write/Edit/Grep/Bash)+权限确认+max_turns+成本统计,在给定仓库完成"把所有 console.log 替换为 logger 并跑通测试"。提交代码+设计说明+成本报告。**

评分点:
- **五个硬性评分点**——**循环正确性**(终止条件、错误回喂);**工具输出截断**(不截断的直接扣——上下文意识);**权限默认拒绝**(Bash 需确认);**成本报告含缓存意识**(tools 稳定性的讨论);设计说明里"如果给 40 小时会加什么"(compaction/subagent/eval)的判断力。
- **加分**——transcript 落盘可重放;失败任务的失败分类。
- **红旗**——Grep 返回整个文件;Bash 无确认直接执行;成本报告只有总 token 没有结构分析。

**Q12.11【实战·L3·用户】(take-home,2 小时)给一个陌生开源仓库写 AGENTS.md,然后让 agent 完成一个 hello 任务(小功能),报告:写前的探索方法、内容取舍、agent 一次通过率、迭代过程。**

评分点:
- **四个评分点**——是否先跑/读/试再写(**基于事实不是想象**);是否克制(<200 行、只写不可推断的事实);是否包含验证命令与禁区;一次通过率的归因分析(哪些失败源于文件缺了什么→回写改进)。
- **加分**——对照实验(删掉某段,通过率怎么变——验证每段的价值)。
- **红旗**——写成产品 README 的复述;500 行全量文档。

**Q12.12【实战·L4·平台】(一周项目)为真实痛点建 eval 集:≥30 个任务+可执行 verifier+自动评分,并用它对比两个模型/配置。交付:任务构造方法、verifier 说明、对比报告(质量/成本/延迟 Pareto)。**

评分点:
- **四个评分点**——任务分布论证(覆盖高频+历史失败,不是挑好做的);**verifier 的可信度自审**(有没有被 hack 的空间——held-out?禁改测试?);方差处理(多采样/置信区间);报告的决策可用性(给结论:哪个配置在什么任务上用)。
- **加分**——verifier 的毒性测试(错误 patch 能不能通过)。
- **红旗**——任务全是 happy path;单次跑分下结论。

---

## 12.4 压力面快问快答(考反应与底层理解)

**Q12.13【快问·L2】agent 每轮重发全部历史,为什么这不是 O(N²) 的浪费?**
答:prompt caching 使旧前缀按约 0.1× 计费、计算被 KV cache 跳过——**重发在计费与计算上都近似增量,只有 TTFT 和 token 计量上是全量**。

**Q12.14【快问·L2】为什么把时间戳放 system prompt 是事故,放最新用户消息里没事?**
答:前者破坏稳定前缀,**缓存从那一点全失效**;后者在尾部追加,不影响已缓存部分——尾部注入模式的存在理由。

**Q12.15【快问·L3】subagent 失败了,主 agent 毫不知情继续,怎么兜底?**
答:结果 schema 强制(含 status/confidence 字段)、超时与预算约束、主 agent 对"空/低置信"结果的显式处理分支——**失败沉默通常源于"成功与失败返回同构"**。

**Q12.16【快问·L3】一句话说明为什么"让 agent 自己 review 自己"不可靠?**
答:**同一套权重有同样的盲区**——自我验证没有引入独立信息源,实证上无外部反馈的自我纠错不升反降;要独立就换模型/换上下文/换验证器(执行结果)。

**Q12.17【快问·L4】面试官问:"你的 harness 花了三个月,模型一升级是不是全白做了?"怎么接?**
答:分层回答——会被吸收的(通用 loop 模式、通用工具设计原则)与吸收不了的(**领域工具语义、内部 spec/记忆、verifier 库、失败分类**);harness 的部分杠杆确实随模型升级贬值,但验证与上下文资产随时间增值;反证:同模型不同 harness 分差几十点是常态(2026 年有 33 分的实测),harness 仍是一等价值。

**Q12.18【快问·L2】cache TTL 5 分钟,你读代码审了 8 分钟再回复,会发生什么?**
答:缓存过期,**下一轮全量重算(计费 miss+TTFT 显著变长)**;对策:1h TTL(写价贵些但长间隔场景仍划算)、或调整工作节奏"想清楚再进会话"。

**Q12.19【快问·L3】为什么 coding agent 的 Grep 工具要把"退出码 1"解释成"无匹配"而不是"命令失败"?**
答:**防止模型把"没搜到"误判为"工具坏了"去重试或换工具**——工具输出的语义设计直接决定 agent 的行为路径(ch03 Q3.12 的设计意图题浓缩版)。

---

## 12.5 开放加餐:知识面与独立判断

**Q12.20【开放·L5·综合】最近三个月,这个领域里你认为最被低估的一件事和最被高估的一件事各是什么?为什么?**

参考思路(一个好答案长什么样):
- **双探针的设计意图**——"最被低估"考你是否看到了主流叙事之外的价值(某个模型特性、某个不起眼的工程实践、某个被噪声淹没的信号),"最被高估"考你敢不敢对热闹的东西泼冷水:**两问都答好,知识面和独立性一起显形**。
- **论证要有机制**——为什么低估:它解决了谁的什么瓶颈、为什么大众还没反应过来(营销噪声/迁移成本);为什么高估:把相关性当因果、演示场景到生产还差几步、经济学不成立——**结论可以主观,论证必须机制化**。
- **时效与诚实**——案例优先取本章案例基线内(2026-06~08)的动态;敢说"这个我判断不了,我观察到的是…"的克制也是加分项:**L5 的开放题里,知道自己不知道比硬编观点值钱**。
- **及格线 / 加分 / 红旗**:及格=各一个具体对象+理由;加分=机制化论证+点名敢站队;红旗=复述媒体叙事、两个都答成"各有道理"的万金油。

**Q12.21【开放·L5·综合】讲一个你最近自己动手折腾的 agent 相关的东西——部署、改造、写小工具、做实验都行:为什么折腾它,过程怎样,结论是什么?**

参考思路(一个好答案长什么样):
- **这道题是"一手经验密度"的探针**——"折腾"的定义很宽(把开源 harness 部署起来跑自己的任务、给工作流写个 hook/小工具、拿兼容层把主力工具换到别的模型上试一周、给自己搭个 eval 跑分),但真折腾过的人**细节是鲜活的**:踩的坑、中途改的方向、现在还在不在用。
- **动机要来自自己的痛点**——"我天天被 X 烦,所以想试试 Y 能不能解决":为解决具体问题而折腾,好过为简历添一行而折腾(后者讲起来全是成果没有过程)。
- **有结论、有遗留**——值不值(时间投入 vs 得到什么)、还缺什么(如果没继续,为什么;如果继续,下一步):**"还在用"是最有力的结论**——自己的东西骗不了自己。
- **及格线 / 加分 / 红旗**:及格=一个具体的折腾对象+过程+结论;加分=痛点驱动的动机+鲜活细节+"还在用";红旗=把工作任务包装成"自主折腾"、细节全是博客语汇没有一手痕迹。

---

## 12.6 面试官使用说明

1. **组合建议**:60 分钟技术面 = 2 道八股(暖场定基调)+1 道原理(验证深度)+1 道实战/排错(主体)+1 道品味/场景(区分度);L4+ 候选人品味与场景题权重加到 40%。
2. **追问原则**:每道大题都有追问链,顺着候选人的答案挖到他/她的边界,比换题更能测真实水平。
3. **红旗信号**:只背名词不讲机制;所有判断都站一边(无 trade-off 意识);把工具当信仰(答不出"什么时候不该用");数字张口就来但说不出处与语境;用"书上说的"当论据(说明没有一手实践)。
4. **绿旗信号**:主动给判据与度量;区分"我知道的"与"我猜的";失败经验具体且有归因;能把 ch02–ch10 的知识串成系统(从缓存谈到成本谈到架构再谈到治理);答题时自然带出"我一般这么干"的一手细节。
