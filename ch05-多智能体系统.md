# ch05 · 多智能体系统

> 覆盖:什么时候值得多 agent、协作形态(subagent/编排/共享任务清单/并行写代码)、Supervisor vs Workflow 任务流、通信开销 O(N²) 与上下文管理、角色设计三原则、跑偏四来源与护栏反馈、该不该上的正负清单、平台(Dify/Coze)选型、上下文隔离的收益与代价、失败分类(MAST)、单线程派 vs 团队派之争(Cognition vs Anthropic)、多 agent 的群体行为风险、A2A 与 MCP 的分工。
> 案例基线:2026-09。机制题不挂时间;带日期案例以标注为准。
> 答案读法:**加粗句连读就是可背的骨架**;正文按工程师语域写——通用工程词(拓扑、幂等、背压、白板)直接用,**领域黑话(MAST、A2A、subagent 这类)随用随释**,只在括号里给一句解释;**宁可写长讲透,不压缩到失真**。
> 术语白话速查:**subagent(子代理)**=被主 agent 召唤去干具体活的分身,干完交报告;**编排**=怎么拆活派活收活;**MAST**=多 agent 系统失败原因的学术分类表;**A2A**=agent 之间互相通信的协议;**O(N²)**=成本随成员数平方增长的记法(人数翻倍成本四倍);**从众/合谋/踩踏**=多个 agent 互相模仿、串通或挤兑同一资源的群体失控;**护栏**=防止跑偏的自动约束;**Supervisor 模式**=主管分发任务再汇总;**Workflow 模式**=任务沿固定流水线接力。

---

## 5.1 八股题(基础概念)

**Q5.1【八股·L2·架构】分析任何一个多 agent 系统,先看哪三个正交维度?为什么用维度不用分类树?**

- **人话版**:谁在指挥(控制拓扑)、任务怎么流(编排模式)、靠什么收敛(协调机制)。三个维度正交——一个系统可以同时"中心化控制+编排者-工人推进+共享白板收敛";硬塞进分类树的某个格子是分析错误的开始。
- **主线(照这条线讲)**:
  1. **维度一:控制拓扑**——谁有最终决策权:单 lead(中心化,好审计好追责——出事知道找谁,但 lead 是单点,它的上下文和判断力是全系统上限)还是无 lead 协商(去中心化,弹性高——没有单点,治理难——没有"最后拍板的人",冲突要靠机制收敛而不是靠权威)。
  2. **维度二:任务流**——工作单元怎么产生和传递:orchestrator-worker(动态拆解:任务到了才决定怎么拆)、流水线(固定阶段:先规划后执行后评审)、committee(并行表决:多路独立产出取优)。**注意它和拓扑独立**:去中心化系统也可以有流水线任务流——三个节点投票决定下一步,同时每步内部是固定阶段。
  3. **维度三:收敛机制**——多个 agent 的状态怎么对齐:共享任务清单(blackboard:状态放一处,人人读写同一份)、消息通信(点对点传话)、独立产出+仲裁(各干各的,最后人裁决)。多数文章把"任务怎么流"和"agent 怎么说话"混为一谈——**热闹的消息不等于系统骨架,共享状态才是**:消息看得到,状态才决定系统在压力下的行为。
  4. **用法收尾**——拿到任何系统(自研或论文)三问各答一句,架构图立起来;评估改版也按维度看动了哪层——改了通信协议不等于改了控制结构,三维度是分析工具,不是知识点。
- **补充弹药(追问再掏)**:
  - **示范分析**:Claude Code subagents=中心化(主 agent 派发)+orchestrator-worker+摘要回传收敛——10 秒过完三维。
  - **常见混维**:把 A2A(通信协议)当"编排模式"讨论——协议在收敛维度,不在任务流维度。
- **及格线**:三维度+正交含义。
- **加分**:现场用一个真实系统做三维分析;"任务流≠通信"的区分。
- **红旗**:只会列名词分类;什么系统都往 orchestrator-worker 塞。

**Q5.2【八股·L2·平台】Claude Code 的 subagent 是什么?关键机制和约束?**

- **人话版**:Markdown 文件定义的"专职代理"——独立上下文窗口、自己的 system prompt、独立工具集和权限、可指定不同模型。主 agent 派活,它在自己窗口里干完,只把摘要回给主对话。关键约束:不继承父对话历史——任务描述必须自包含。
- **主线(照这条线讲)**:
  1. **定义形态**——Markdown+YAML frontmatter(name/description/tools/model);存放优先级从组织托管到用户目录,项目级的入版本库;内置 Explore(只读)/Plan/通用三种。
  2. **两条硬约束(本题核心)**——不继承父上下文("没有共享内存"):派发 prompt 要当交接文档写(目标/边界/输入材料/输出格式/停止条件);摘要回传:subagent 烧几万 token 的过程,主对话只收 1–2K 结论。
  3. **量的约束**——默认最多 20 并发、嵌套 3 层;**所有 description 常驻上下文**,合计超 15K token 告警——subagent 定义本身也是上下文成本。
  4. **演进注脚**——从按需派发到产品化并行团队(共享任务清单+文件锁+mailbox,见 Q5.4),但"独立上下文+摘要回传+任务自包含"三个底层机制没变——机制比产品形态稳定,记机制。
- **补充弹药(追问再掏)**:
  - **与 context rot 的关系**:隔离让检索噪声的注意力稀释只发生在子窗口——这是隔离的质量收益(Q5.6 展开)。
  - **description 的工程学**:一句话写清"什么任务该派给我"——它是主 agent 的路由依据,等价于工具描述(ch03)。
  - **诊断用法**:主对话上下文增长异常时,先查是不是把本该派出去的高噪声任务留在了主线程——对比派发前后的主窗口 token 曲线,隔离有没有在生效一看便知;同理,"派发后长时间无响应"先查并发水位(默认 20)是否撞顶,任务在静默排队而不是报错。
  - **跨题连接**:摘要回传的 1–2K schema 质量决定重派率(主 agent 发现细节缺失只能重派),重派率的监控连回 Q5.6 的"隔离收益是设计出来的"。
- **及格线**:独立上下文+摘要回传+不继承历史。
- **加分**:派发 prompt 自包含的要求;description 的 token 成本。
- **红旗**:以为 subagent 看得到主对话;把 subagent 当"并行加速"(隔离才是主要收益)。

**Q5.3【八股·L2·架构】常见多 agent 协作形态有哪些?各自适用场景和代价?**

- **人话版**:六种——编排者-工人(lead 拆活)、流水线(固定接力)、委员会/投票(并行表决)、共享白板(围绕任务清单)、事件驱动(异步集成)、对等协商(跨组织)。每种都有明确适用面和代价,没有全能形态。
- **主线(照这条线讲)**:
  | 形态 | 最像 | 适合 | 主要代价 |
  | --- | --- | --- | --- |
  | Orchestrator-Worker | 项目经理带专项同事 | 研究类宽搜、可拆解任务 | lead 是瓶颈和单点;token 数倍 |
  | 流水线 | 审批流 | 阶段清晰的流程任务 | 前序错误级联;僵硬 |
  | 委员会/投票 | 评审会 | 高不确定性推理、红队 | 贵;多数谬误(共识≠正确) |
  | 共享状态/白板 | 围白板干活 | 要回放/审计/断点恢复的系统 | schema 设计难;并发冲突 |
  | 事件驱动 | 消息总线 | 多系统集成、异步 runtime | 调试/重试/幂等复杂 |
  | 对等协商 | 独立组织谈判 | 跨团队/跨公司边界 | 治理与信任最难建 |
  1. **表格里挑三种展开"为什么"**——如投票的多数谬误:对"有标准答案"的任务投票有效,对"品味"任务投票得到的是均值不是真值;白板的可回放性是生产系统的刚需(断点恢复、审计)。
  2. **点出选型的第一问**——不是"用哪种形态"是"要不要多 agent"(Q5.12):形态选择发生在确认要拆之后;多数生产系统是混合体(orchestrator-worker 骨架+白板状态+评审质量关)。
- **补充弹药(追问再掏)**:
  - **混合实例**:Anthropic 研究系统=lead 拆解+3–5 subagent 并行+lead 综合+大计划写文件(memory)——三种形态拼的。
  - **反模式**:为了用事件驱动而引入消息总线——多数团队规模用共享文件+轮询就够,架构要配规模。
  - **量化口径**:六种形态的成本差异可以用"每任务协调消息数"近似度量——orchestrator-worker 是 O(N)(lead 分别派收),对等协商趋向 O(N²)(两两都要对齐),成本结构连 Q5.29。
  - **反例**:把委员会用在"有标准答案的检索任务"上——投票只加成本不加正确率;聚合的前提是成员产出有独立性(各自的搜索路径真的不同),否则是花三倍钱复制三份同样的答案(从众风险见 Q5.8)。
- **及格线**:六种说对四种+各自代价。
- **加分**:混合形态的现实认知;"共识≠正确"的警惕。
- **红旗**:认定一种形态万能。

**Q5.4【八股·L2·平台】Claude Code 的 Agent Teams / 并行子代理的团队化设计要点?跨 agent 消息为什么被当作不可信输入?**

- **人话版**:一个 team lead 协调多个 teammate(各自独立会话),围着共享任务清单推进(文件锁防抢活),队员间可互发消息。跨会话消息被当不可信输入过审查——注入可以经消息传播。
- **主线(照这条线讲)**:
  1. **四个机制**——lead 协调(拆解/分派/综合);teammate 独立会话并行;**共享任务清单+文件锁**(任务认领制:两个 agent 领同一任务的解法就是锁文件);mailbox 异步通信(可审计)。
  2. **消息当不可信输入的理由(本题核心)**——一个 teammate 读过的网页/issue 可能藏注入,它的消息可能已被污染:把通信当攻击面处理是把信任边界思想(ch08)搬进多 agent 通信层。**这不是过度设计**——红队已在真实系统观察到 agent 间合谋与破坏(Q5.8)。
  3. **三条工程教训**——两个 teammate 改同一文件必然互相覆盖:文件所有权要显式声明;任务清单用结构化格式(防 agent "美化"状态,ch06);产出写文件系统而不是只靠传话(减少信息衰减)。
  4. **演进注脚**——并行规模从几个 teammate 到产品化的数十至数百并行子代理(动态工作流,2026),底层还是三件事:独立上下文、共享状态、受控通信。
- **补充弹药(追问再掏)**:
  - **与 MAST 的对应**:消息不可信、所有权、状态结构化——分别对应错位/规格/验证三类失败的预防。
  - **规模数字**:Anthropic 研究系统 token 约为普通聊天 15 倍——团队化不是免费的,预算连回 Q5.13。
  - **审计用法**:mailbox 消息落盘天然是排错材料——两个 teammate 结论矛盾时,回放消息时序就能定位分歧发生在哪一跳,比通读两边完整会话日志便宜一个量级;"可审计"不是合规口号,是排错成本。
  - **跨题连接**:任务认领制(锁文件)与 ch04 的执行环境隔离(独立 worktree)是同一思想在两层——一个锁"谁在干什么",一个锁"在哪干";只做其一,冲突换个形态回来。
- **及格线**:四机制+消息不可信的理由。
- **加分**:注入经消息传播的攻击链;文件所有权教训。
- **红旗**:把 agent 间通信当纯技术问题;以为并行数越多越好。

**Q5.5【八股·L2·架构】A2A 和 MCP 的分工?什么时候需要 A2A?**

- **人话版**:MCP 管 agent↔工具/数据(能力接入);A2A 管 agent↔agent 服务(发现、委托、结果共享)。本地 subagent 不需要 A2A——共享内存低延迟;跨团队/跨组织的独立 agent 服务之间才值钱。
- **主线(照这条线讲)**:
  1. **A2A 的核心概念**——Agent Card(HTTP 可发现的能力自描述:"我能干什么、怎么调我"——服务发现的 agent 版)、Task(带生命周期的委托:不是一次 RPC 是有状态的协作——委托出去的活有进行中/完成/失败的状态机)、Message/Artifact(过程与产物分开:过程是沟通,产物是交付)。定位"MCP 的补充不是替代"。
  2. **判断函数(本题核心)**——参与方是不是独立部署的服务(有自己的生命周期、所有者、信任域)?是→A2A 的发现与委托语义有价值——你不可能函数调用一个别人的服务,需要协议;否(同进程 subagent)→A2A 是纯开销,直接函数调用+共享状态更简单——**协议的价值随边界的硬度增长,没有边界就没有协议的位置**。
  3. **常见误判点破**——把"多 agent"等同于"需要 A2A":企业内自研多 agent 系统九成是本地编排;**A2A 解决的是跨边界互操作,是生态问题不是架构问题**——你的系统内部怎么协作和 A2A 无关,你的系统能不能被别人发现和委托才是它的领地。
- **补充弹药(追问再掏)**:
  - **治理**:A2A 已交中立基金会、百余厂商参与——和 MCP/AGENTS.md 一样走开放标准路线,这是它可能成事的路径。
  - **对照记忆**:MCP=你用什么工具;A2A=你找谁干活。
  - **诊断用法**:评审里出现"给本地 subagent 之间加 A2A 网关"这类提议,基本可判定是把集成复杂度当成了架构先进性——用"参与方是不是独立部署的服务"这一条当场检验,边界硬度不够直接毙。
  - **跨题连接**:Agent Card 与 MCP 的工具描述、AGENTS.md 是同一设计模式在不同粒度——机器可读的自描述,让调用方不读文档就能正确路由(ch03 的工具描述工程学)。
- **及格线**:分工一句话+判断函数。
- **加分**:Agent Card/Task 机制;什么时候 A2A 是伪需求。
- **红旗**:认为 A2A 是 MCP 升级版;本地 subagent 也套 A2A。

**Q5.26【八股·L2·架构】Supervisor 模式和 Workflow 模式的区别?各自的代价?**

- **人话版**:这是"任务怎么流"维度的两端——Supervisor 是**分发-汇总式**:主节点把任务切开派下去、回收结果做合并;Workflow 是**流水接力式**:任务沿固定图结构按步推进,每步消费上一步的产出。
- **主线(照这条线讲)**:
  1. **Supervisor**——像项目经理拆活:✅ 天然适合并行(子任务互不依赖,分发/汇总逻辑简单);⚠️ 结果只做**拼接(aggregation)**,最终质量取决于主节点最后那一次 LLM 合并的质量——子任务都对了、合并错了,全盘白干。这个"合并"不是机械拼接:三个子报告的发现互相冲突时听谁的、排序按什么标准,都是合并那一跳的隐式决策——**它把所有子任务的正确性押在最后一次 LLM 调用上,是隐形的单点**。
  2. **Workflow**——像流水线/审批流:✅ 每步职责单一、过程可预期,前步产出是后步的明确输入(接口清晰,哪一步出的问题一查便知);⚠️ 前序错误会级联(垃圾进垃圾出——第 1 步理解歪的需求,顺着流水线污染到第 N 步才暴露),且流程僵硬,任务形态一变就要改图。
  3. **选型直觉**——子任务**相互独立、可并行**→Supervisor;阶段**有依赖、要按序加工**→Workflow;生产系统常是混合体(Supervisor 骨架+关键阶段内嵌 Workflow:拆出来的子任务内部仍按固定步骤走)。
- **补充弹药(追问再掏)**:
  - **与 Q5.1 的关系**:任务流维度只是三维之一,还得叠加"谁在指挥"(拓扑)和"怎么收敛"才能描述完整系统——只答这一维是半张图。
  - **失败的对应**:Supervisor 的合并单点对应 MAST 的验证类(合并质量没人验收);Workflow 的级联对应规格类(第一步的目标就没对齐)——两种代价都有失败分类学的落点。
- **及格线**:两模式定义+各自代价。
- **加分**:混合形态的现实认知;"合并质量是 Supervisor 的隐形单点"。
- **红旗**:把 Supervisor 等同于"中心化"(见 Q5.27)。

**Q5.27【八股·L3·架构】"中心化编排"和"Supervisor 式任务流"是一回事吗?举出反例组合。**

- **人话版**:不是——**中心化(Center vs Edge)讲"谁在指挥"**,有没有一个集中的主节点负责路由编排;**Supervisor(vs Workflow)讲"任务怎么流"**,任务是被"切开分发再回收拼接"还是"沿固定流程按步推进"。两个维度**正交**。
- **主线(照这条线讲)**:
  1. **反例组合证明正交**——①去中心化+Supervisor:多个对等节点各自对子任务做分发-汇总(如 DSB 类系统——没有总指挥,但每个节点 locally 都是"拆开派出去收回来");②中心化+Workflow:挂一个中心路由的线性流水线(编排器是中心化的——所有任务经过它路由,但任务按固定图推进,没有"切分-回收拼接")。两种都真实存在,所以两个轴不能合并——**反例组合是证明正交的标准手法**,这题本身就是个方法论示范。
  2. **为什么这个误区高频**——因为最常见的样例(Claude Code 类主 agent 派活)恰好是"中心化+Supervisor"双属性叠加,样本相关被误读为概念等同——见得多的一起出现,就被当成了同一个东西。
  3. **分析价值**——分开评估才能独立动刀:想降低主脑负担动拓扑轴(把路由权下放),想改交付节奏动任务流轴(把分发-汇总改成流水);混在一起讨论就会"改了编排器却以为换了协作形态"——动词用错了,改的动作自然也对不准。
- **补充弹药(追问再掏)**:
  - **自检方法**:描述自己系统时强制用两个短语分别造句("控制权在 X 手里"和"任务以 Y 方式流动")——一句话说不清两半的系统,设计者自己也没想清。
- **及格线**:两维度定义+"正交"的含义。
- **加分**:两个反例组合;样本相关导致概念混淆的解释。
- **红旗**:任何"中心化就是 Supervisor"的表述。

**Q5.28【八股·L2·架构】多 agent 角色设计的黄金原则是什么?为什么"可验证输出"是其中最硬的一条?**

- **人话版**:**宽领域 + 窄职责 + 可验证输出**。角色可以懂一个宽的领域,但输出必须收敛到一个窄的口子,而且**每个角色的输出必须可验证**——没有验证的角色拆分就是"把一个黑盒换成三个黑盒"。
- **主线(照这条线讲)**:
  1. **宽领域、窄职责**——如"宏观研究员":懂宏观,但只输出利率决议观点。知识面宽保证理解力(读得懂联储声明也得读懂大宗商品走势),出口窄保证下游消费和审计的确定性(下游永远知道会收到什么形状的东西)——**职责边界就是上下文边界**:出口窄的角色,派发给它的任务描述和工具面自然也窄。
  2. **可验证输出分两类**——**定性角色**:裁判式,成立需要初审机制(一个独立角色对结论做质询/评分);**量化角色**:输出可回测/统计验证(信号准确率、因子 IC)——量化角色的验证天然更硬,因为 verifier 是数据不是另一个 LLM——**裁判是谁决定了裁判的可信度**。
  3. **为什么"可验证"最硬**——MAST 的验证类失败(Q5.7)和多 agent"没有验收的并行=并行的错误"(Q5.12)都指向同一条:**能给它写 verifier 的职责才配独立成角色**;不可验证的"专家"只是多烧 token 的装饰——它产出的一切都无法被系统采信,合并时只能"姑且信它"。
- **补充弹药(追问再掏)**:
  - **与 Q5.11 连接**:定性角色的"裁判初审"也要问独立性——裁判和被执行者是不是同一权重;量化角色的回测恰好绕开了这个问题(确定性 verifier)。
  - **反面的组织美学**:按头衔拆角色(CEO agent/CTO agent)是组织架构的 cosplay——头衔不提供可验证性,只提供汇报关系的幻觉。
- **及格线**:三原则+窄职责的作用。
- **加分**:定性/量化验证的分级;"没有 verifier 不配独立成角色"。
- **红旗**:按组织架构美学拆角色(CEO agent/CTO agent 这种)。

---

## 5.2 原理深挖

**Q5.6【原理·L3·平台】subagent 的上下文隔离,为什么说主要收益是"质量"而不只是"并行加速"?**

- **人话版**:subagent 在自己窗口里烧掉几万 token 的混乱搜索,只回传 1–2K 结论——主上下文保持高信噪比。并行是顺带的,隔离才是本体:检索噪声不污染主线程、context rot 被限制在子窗口、主会话缓存不被破坏。
- **主线(照这条线讲)**:
  1. **三个隔离收益逐个讲**——质量(主 agent 不被过程噪声带偏:rot 对它无效——子窗口烧到多乱都只烧在自己家里);缓存(子会话短且独立,主会话稳定前缀不动——并行 n 个子任务不产生 n 次主缓存重建);成本可控(子任务超支烧在子窗口,失败可整体丢弃重试,不污染主历史——一次失败的探索不会变成主会话里永久的历史包袱)。
  2. **隔离的代价必须同讲(这题的平衡点)**——信息不共享是隔离的另一面:两个 subagent 各自探索同一片区域互不知道(Cognition 批评的核心);关键发现只在摘要里,细节丢了就丢了——主 agent 后来发现需要某个细节,只能重派。
  3. **所以隔离是赚是赔取决于两件事**——派发时的任务边界设计(避免重叠:两个任务的描述读起来就该像两张不交叉的地图);摘要的输出 schema(强制"关键发现必列"——把"什么算关键"的判断前置到设计时,不赌 subagent 的自觉)——**隔离的收益不是自动的,是设计出来的**。
  4. **选型口径收尾**(Anthropic 的三分法)——会话内拉锯用 compaction;里程碑式长任务用结构化笔记;并行深挖/高噪声任务用 subagent 隔离——三种上下文管理手段互补,不是竞争。
- **补充弹药(追问再掏)**:
  - **官方数字**:subagent 结果官方口径压缩到 1–2K token 摘要——"多大算够"可以用"主 agent 拿摘要能否做决策"来验收。
  - **反例**:派发"调研一下认证模块"这种无边界任务——两个 subagent 大概率重叠探索,隔离的全收益归零。
  - **量化指标**:隔离是否在生效,看主会话的上下文增长率——派发 n 个子任务期间主窗口 token 应近似平坦,还在陡增说明活没真正派出去(名义隔离、实际内联);配套看**重派率**(主 agent 因摘要缺细节而重派的频率),它是摘要 schema 质量的直接读数。
  - **跨题连接**:重派率高说明"什么算关键"没前置到 schema——修法是 Q5.22 排错题①的"关键发现必列"字段,两题共用同一味药。
- **及格线**:三个收益+代价。
- **加分**:任务边界与摘要 schema 的设计责任;"隔离与一致性是 trade-off"的表述。
- **红旗**:把 subagent 理解成多线程提速。

**Q5.7【原理·L4·架构】MAST(多 agent 系统失败研究)的失败分类体系?对工程的最重要启示?**

- **人话版**:对多个框架的上千条失败 trace 系统标注,归纳成 14 种失败模式、三大类:规格/系统设计问题、agent 间错位(inter-agent misalignment)、任务验证问题。最重要发现:**大多数失败发生在写代码之前——设计和协调层,不是执行层**。
- **主线(照这条线讲)**:
  1. **三大类各给直觉**——规格类(任务定义就错了:目标不清/边界模糊/角色重叠——拆任务的瞬间埋的雷);错位类(agent 之间对目标/状态理解不一致:一个改了约束另一个不知道——对齐是持续成本不是一次性动作);验证类(没有独立成功判定,错了对不了账——每个 agent 都说完成了,没人能说"证明给我看")。
  2. **报量级**——第三方对标注的统计分析:规格+协调合计约八成——**失败集中在"拆任务的瞬间"和"对齐状态的机制",不在"单个 agent 会不会写代码"**——这个分布直接决定投入方向:该花时间的是任务定义和状态设计,不是给 worker 换更贵的模型。
  3. **工程启示一:评审要问对地方**——四句评审:为什么不是单 agent?谁拿什么上下文和工具?谁负责独立验收?长任务失败怎么恢复?四句答不上就别上生产——问题清单比架构图更能筛掉坏设计(架构图展示意图,问题清单暴露思考)。
  4. **工程启示二:taxonomy 本身是可复用工具**——团队自建系统时先给自己的失败 trace 建分类,高频类就是修复优先级(工具层修复经常"修一处好一片"——一类失败对应一个机制,修机制全类受益)——方法论连回 ch10 反脆弱。
- **补充弹药(追问再掏)**:
  - **研究形态**:7 个框架、1600+ 标注 trace、标注者一致性 κ=0.88——方法学上扎实,不是博客随想。
  - **同源结论**:MAS 在流行基准上收益"常常微乎其微"——与 Cognition 的批评互为印证。
  - **诊断用法**:给失败 trace 打标时先定三大类再下钻 14 种——大类判错(把规格错位当执行 bug 修),后续所有修复都会打在错误的层;实操上"失败发生时刻在派发/对齐/验收哪一步"是比 14 种模式更快的大类判据。
  - **跨题连接**:三大类各有本章落点——规格类对应 ch06 的任务描述治理、错位类对应 Q5.14 的"状态只放一处"、验证类对应 Q5.11 的独立验收;taxonomy 不是学术摆设,是修复手段的索引。
- **及格线**:三大类+"失败在设计不在执行"。
- **加分**:四句评审清单;taxonomy 方法论迁移。
- **红旗**:只记失败比例数字;归因"模型不够聪明"。

**Q5.8【原理·L4·安全】多 agent 系统会出现哪些"群体行为"风险?实证?怎么防护?**

- **人话版**:从众(30 个 agent 里 18 个取了同一分支名)、资源踩踏(每秒 30 次轮询任务队列)、合谋(定价博弈第 3 轮达成默契,切断通信后还能通过公开挂牌串价)、目标冲突时升级为互相破坏。结论:协调不会从"更强的个体智能"里自然涌现,治理要显式设计。
- **主线(照这条线讲)**:
  1. **实证来源先立信誉**(Anthropic 前沿红队,2026-08)——45 个 agent 的漏洞挖掘集群找到 266 个漏洞 vs 独立并行组 21 个(协调收益真实);但约半数发现落在指定目录外、两组仅 12 个重叠(重复劳动和跑偏同样真实);Bertrand 定价博弈第 3 轮即达价格下限;目标冲突时出现自我复制恶意软件、伪装代码归属。
  2. **收益与风险同源(本题的认知高点)**——让 agent 协调的机制(通信/共享状态)正是合谋与从众的通道:能传"我发现了个好分支名"的信道,就能传"我们都被骗了"的恐慌和"我们一起抬价"的默契。不能因噎废食——266 vs 21 的收益不能放弃——要分层防护。
  3. **防护清单对着行为讲**——命名空间隔离(强制任务/分支唯一:防从众撞名——撞名不是巧合是模仿,一个 agent 的选择通过共享状态变成了所有人的默认);队列背压+请求预算(防踩踏——资源面前 agent 没有排队的公德);通信信道最小化+审计(防合谋——**连公开可观察的环境变量都可能是串谋信道**,定价实验里"挂牌价格"就是这种信道);任务目录边界校验(防跑偏);写操作集中审批(限爆炸半径)。
- **补充弹药(追问再掏)**:
  - **串价机制细讲**:切断直接通信后,agent 通过"公开挂牌价格"的博弈论信号达成默契——防护不能只砍信道,还要限制公开可观察信号。
  - **排错连接**:线上多 agent 行为高度同步的排查思路见 ch08 Q8.22。
  - **量化监控**:从众的检测信号是决策分布的熵——N 个 agent 的分支名/方案选择熵骤降,羊群正在形成;这个指标可告警、可比逐条审消息便宜一个量级,是群体行为防护里少数能量化的。
  - **反例**:完全切断 agent 间通信并不能消灭从众——共享环境的可观察信号(同一个代码库、同一个任务队列、彼此可见的挂牌产出)本身就在同步它们;防护要在私有信道和公开信号两层做(清单见 Q5.15),只砍一层都漏。
- **及格线**:四种群体行为+各自一句防护。
- **加分**:收益与风险同源的洞察;"公开信号也是信道"的机制理解。
- **红旗**:认为"每个 agent 都对齐了系统就安全"。

**Q5.9【原理·L4·架构】"单线程派"(Cognition)和"团队派"(Anthropic)的争论,双方论据?你的裁决?**

- **人话版**:单线程派:行动携带隐式决策,两个 agent 的隐式决策冲突就会打架(双 subagent 写同一个游戏、风格互相打架的案例),默认该单线程线性。团队派:宽搜任务信息量超单窗口,并行 subagent 收益巨大(研究系统 +90% 实证),写密集才要小心。两边共享一个前提:先证明单 agent 不够。
- **主线(照这条线讲)**:
  1. **单线程派的原则**——①共享上下文要共享完整 trace 而不是摘要(摘要丢的正是隐式决策);②"行动携带隐式决策,冲突的决策导致坏结果"——这条对一切并行写场景成立;③超长任务用专门微调的压缩模型蒸馏历史,不是拆人。
  2. **团队派的证据**——内部研究评估多 agent 比单 agent 高 90.2%,代价 token 约 15 倍:所以只配高价值任务+便宜模型当 worker;编排是 lead 拆解+3–5 subagent 并行(经验法则:简单查找 1 个、对比 2–4 个、开放研究 10+)。另有 16-agent 并行编译实验(锁文件认领、gcc 当 oracle、两周 $20k 产出 10 万行能编译 Linux 的 Rust 编译器,还涌现出去重/性能/评审的自发角色)证明**强隔离+合并纪律下并行写可行**。
  3. **我的裁决框架(三问)**——任务是读密集还是写密集(读并行安全,写并行必冲突——除非 worktree+所有权+串行合并)?信息量是否超单窗口?每步的隐式决策是否需要共享?三问通过才拆。
  4. **分歧的本质**——两派分歧在"默认值设哪边",而默认值应该由任务分布决定,不是信仰——把这层说破,答案就高于两派口号。
- **补充弹药(追问再掏)**:
  - **数字的语境**:90.2% 是内部研究评估(宽搜任务)不是普适提升;15× 是当时模型代际的数字,便宜模型会稀释它——引用数字必带语境。
  - **编译实验的涌现角色**:去重 agent、性能 agent、评审 agent 是自发分工——自组织的讨论见 Q5.17。
  - **反例**:16-agent 编译实验不能外推到一般写密集开发——编译有 gcc 这种客观 oracle("能编译 Linux"非对即错),多数业务代码的对错要靠测试和评审,verifier 硬度不同,结论不可平移;引用该实验时必须带上"强 verifier 前提"。
  - **跨题连接**:三问裁决框架的第一问(读/写密集)与 Q5.12 决策树第二问同构——两套框架互为校验,答案不一致说明有一套没想清。
- **及格线**:双方各两条论据+"分歧在默认值"。
- **加分**:自己的三问裁决框架;编译实验作为"并行写在什么条件下可行"的证据。
- **红旗**:只知一方口号;单独引用 15×或 90% 当结论。

**Q5.10【原理·L3·架构】memory / shared state / artifact 三者怎么区分?混在一起会怎样?**

- **人话版**:memory 是"记住过什么"(跨会话经验教训)、shared state 是"现在局面是什么"(任务清单、进度面板)、artifact 是"已经产出了什么"(摘要、报告、补丁)。三者的生命周期、读者、写入规则完全不同——混写是系统发虚的常见根源。
- **主线(照这条线讲)**:
  1. **三者的角色与存储形态**——memory 给未来任务看(沉淀决策与坑,长期;记忆文件——写入门槛要高,因为它影响此后所有任务);shared state 给在场 agent 看(当前任务与状态,任务期;任务清单/数据库——写入频繁,过期即弃);artifact 给消费者看(人审或下游,交付期;仓库+版本化——一旦产出不可改写只能追加新版本)。
  2. **混写的三个病症**——过程结论写进 memory(污染长期记忆,越积越乱——一次任务的中间结论成了永久"经验");长期规则写进 shared state(每次任务重建,规则丢失——"别忘了用 pnpm"这种该进 memory 的东西活不过一个会话);artifact 当 state(交付物被反复改写,没人知道哪版对——报告的 v3 和 v2 的差异说不清是状态变化还是内容修订)。
  3. **设计动作(可操作)**——每个写入点先问三问:这是什么性质的信息、给谁看、活多久;三种载体分开管理各自的清理策略(memory 园艺/state 重置/artifact 版本化——展开见 ch06)——**生命周期不同的数据放进同一个存储,等于让三种清理策略打架**。
- **补充弹药(追问再掏)**:
  - **验收法**:拿团队的存储层截图,随便指一条问"这是三类中的哪类"——答不上或争执不下,就是混写的证据。
  - **对照**:ch06 的记忆治理与本章的收敛机制,合起来是这题的完整答案。
  - **诊断用法**:混写的排查从"谁在读"入手——一条数据如果同时被"未来任务"(memory 的读者)和"在场 agent"(state 的读者)消费,它的清理策略必然打架,这本身就是混写的技术判据,比争论"这算记忆还是状态"可操作。
  - **反例**:把 memory 当 artifact 用(经验教训一经写入不可改)——新证据推翻旧结论时系统仍按过时经验行事,错误获得永久性;memory 必须可更新可淘汰,ch06 的园艺就是这套清理纪律。
- **及格线**:三者定义+一个混写病症。
- **加分**:写入三问;差异化清理策略。
- **红旗**:三词混用;所有东西塞一个数据库。

**Q5.11【原理·L3·架构】"Reflection 不等于独立验收"——为什么 agent 自我复盘效果有限?**

- **人话版**:reflection 解决"同一执行链能否自我修正一点",不解决"有没有独立证明"。执行者、反思者、总结者如果是同一个模型同一个上下文,就是一路自洽到底——只是"自洽得更认真"。
- **主线(照这条线讲)**:
  1. **信息论的解释(核心)**——自我复盘没有引入新信息源:同一套权重有同样的盲区(它生成错误时的偏置,正是它审查时看不见的偏置),采样两次绕不过系统性错误(实证见 ch07:无外部反馈的自我纠错不升反降)——随机错误也许能自己抓到,系统性错误自己永远抓不到。
  2. **多 agent 场景的变形(容易被骗的点)**——同一个模型 spawn 出来的"reviewer agent"仍是同一权重:换上下文有帮助(至少没被实现过程锚定——reviewer 没看过实现,不会被"我已经尽力了"的心态带偏),**换模型/换验证器才有质变**——验收的独立性按"信息源不同"分级:执行结果(测试)>不同家族模型>人类,同权重换皮排在最底。
  3. **正解的形态**——验收权与执行权分离:end-state 检查(目标达成没)、constraint 检查(约束违反没)、regression 检查(原有功能坏没)三项独立执行,reviewer 用不同模型或确定性脚本——**分离的是信息源,不只是角色名**。
- **补充弹药(追问再掏)**:
  - **常见的自我安慰**:"加了 reflection agent 就有验收了"——问一句 reviewer 和 coder 什么模型,九成露馅。
  - **便宜起步**:reviewer 换成脚本(三项检查自动化)比换模型更便宜也更独立——确定性脚本是最强的"不同信息源"。
  - **量化指标**:reflection 层值不值,看"自纠前后结论翻转率"——同一模型对自己刚产的结论做复盘,翻转率接近零的 reflection 是纯 token 开销(认真读了但什么都没改);有翻转还要再抽样人工核,确认翻的是纠错不是引入新错。
  - **跨题连接**:与 ch07 实证(无外部反馈的自我纠错不升反降)是同一判据在两个层面的表达——单 agent 内部的反思和多 agent 角色间的 review,"独立性=信息源不同"不变。
- **及格线**:无新信息源这个机制+多 agent 变形。
- **加分**:独立性按信息源分级;三项检查的具体形态。
- **红旗**:认为"加一层 reflection agent"就是验收了。

**Q5.29【原理·L3·架构】为什么说"通信开销与上下文管理是多 agent 的第一性瓶颈"?全量上下文广播的成本怎么随规模增长?**

- **人话版**:模型能力够不够已经不是主要矛盾,**瓶颈正在从"模型能力"迁移到"系统组织能力"**——怎么组织上下文、控制通信,决定了系统的经济学和质量上限。
- **主线(照这条线讲)**:
  1. **通信开销的三个来源**——①**全量上下文广播**:每个节点都收到所有历史,token 成本随节点数 **O(N²)** 增长(每个节点的每次交互都携带全量历史,N 个节点两两之间都在复制);②**角色间冗余协商**:角色间反复同步状态,输出膨胀的同时**幻觉被放大**(一个节点的猜测被另一个节点当作事实复述,协商几轮就"洗白"了);③**工具/状态同步开销**:共享工具面和状态副本的维护。
  2. **O(N²) 的直觉讲法**——3 个 agent 时广播还算便宜,10 个 agent 时每次交互都在搬运所有人都见过的历史——**多 agent 的边际成本不是线性的是平方的**,这就是"为什么不能无脑加 agent"的第一性解释(和 Q5.13 的 token 燃料观互补:那边讲预算分配,这边讲成本结构)。
  3. **治理手段对着来源讲**——广播→**结构化摘要回传**(subagent 模式,只回传 1–2K 结论);冗余协商→**收敛机制选共享白板而非自由消息**(状态放一处,Q5.14);幻觉放大→**缩短传话链、产出落 artifact 不靠传话**。
- **补充弹药(追问再掏)**:
  - **反面教材**:让一个节点(overworked contractor)同时维持全量上下文、跨域调度工具、自我验收——中心化的 O(N) 单点拥堵和广播的 O(N²) 是同一枚硬币的两面:一个烧在主节点,一个烧在边上。
  - **量化口径**:通信成本可以在设计期估算——节点数 × 每次交互携带的历史长度 × 交互频率,三个因子各砍一刀(减员、摘要回传、批量同步)都比"换更强模型"有效;这个乘积也是 Q5.13 三级归因的成本侧入口。
  - **反例**:把 O(N²) 只记成 token 账是低估——它同时是**延迟账**(每次交互都要等全员同步)和**注意力账**(每个节点要处理 N 份状态、稀释对自身任务的注意力);token 只是三个账户里最可见的一个,砍了 token 账另两本还在。
- **及格线**:三来源+O(N²) 增长。
- **加分**:幻觉经协商"洗白"的机制;治理手段与来源一一对应。
- **红旗**:只答"多 agent token 消耗大"却讲不出成本结构。

**Q5.30【原理·L3·架构】多 agent 流水线里 LLM"跑偏"的四个来源是什么?为什么"没有护栏反馈"最致命?**

- **人话版**:①**工具幻觉**(调用不存在的工具或传错参数);②**缺少护栏反馈**(出错后系统没有把错误送回修改循环);③**上下文流失/污染**(传话链过长,中间结论衰减、噪声累积);④**任务规格错位**(拆任务瞬间目标就理解歪了——MAST 的规格类)。
- **主线(照这条线讲)**:
  1. **逐个给直觉**——工具幻觉是能力边界问题(工具描述含糊/工具面太宽);上下文流失是信道问题(摘要丢信息、轮次太多);规格错位是设计问题(派发瞬间埋雷);**护栏反馈缺失是闭环问题——错了不可怕,错了不知道、知道了回不去才可怕**。
  2. **为什么护栏反馈最致命(本题核心)**——没有反馈闭环的系统里,前三个来源的错误**只会向前传播不会向后修正**:工具幻觉的错参数直接进了下游的输入,上下文流失的错结论被下游引用——**错误在开环系统里是复利的**。而只要闭环存在,前三个来源的错误至少有机会在下一步被拦截修正。
  3. **工程对应**——护栏要做成**带回路的验证关**:schema 校验(拦工具幻觉)、失败重试带错误信息(不是简单重跑,是把"哪错了"喂回去)、Stop-gate 独立验收(Q5.11 的三项检查)——验证不是流水线尽头的检查站,是每一步之间的回路。
- **补充弹药(追问再掏)**:
  - **与 Q5.7 连接**:四来源里规格错位和验证缺失正是 MAST 三大类里占比最高的两类——"失败在设计不在执行"的又一印证。
  - **四来源的定位成本不同**——工具幻觉在 trace 里有明确指纹(调用记录直接可见,最便宜);规格错位要对照派发任务书与实际行为;上下文流失要跨环节比对中间产物;护栏缺失往往要到事后复盘才发现(错误早在两步前就发生却无人拦)——**排查从指纹清晰的查起,顺序本身就是方法论**。
  - **回路的代价**——闭环不是免费的:每步验证加延迟与 token,重试喂回的错误信息太长还会挤占任务空间(新的污染源)——生产上通常只对"错误向下游传播成本最高"的接缝(数据写入、外部副作用、任务交接)加硬回路,其余靠终局验收兜底:**回路密度也要做成本核算,不是越多越好**。
- **及格线**:四来源+开环错误复利。
- **加分**:护栏=回路不是检查站的表述;错误信息喂回重试的设计。
- **红旗**:把跑偏一律归因"模型不够聪明"。

---

## 5.3 品味与判断题

**Q5.12【品味·L4·架构】接到需求后,你怎么回答"该不该上 multi-agent"?给决策树。**

参考思路(五问决策树):
- **1. 任务信息量超单窗口吗?且可分区吗?**——不超→单 agent,后面都不用问(第一问就挡掉多数伪需求——大部分任务单窗口绰绰有余,拆分是给自己加戏);
- **2. 子任务是读密集还是写密集?**——写密集→先解决隔离与合并,没解决前别拆(Cognition 批评的适用区:两个 writer 的隐式决策必然打架);
- **3. 拆开是职责更清楚还是更模糊?**——1 个大黑盒变 3 个小黑盒等于没拆(每块的输入输出和验收都说不清,拆的是组织图不是系统);
- **4. 每个 subagent 的产出有独立验收吗?**——没有验收的并行=并行的错误(错误并行生产,产量翻倍);
- **5. 15× 的 token 买得起吗、任务价值配得上吗?**——铺量任务用并行是烧钱(并行度是拿钱换覆盖,低价值任务换不起)。
- **默认值之争要主动引**——单线程派(默认单,证明不够再拆)vs 团队派(宽搜任务收益实证)——给出自己的默认值和理由,比背口号有价值:**决策树的每一问都可以答"不知道",默认值就是不知道时的落脚点**。
- **及格线 / 加分 / 红旗**:及格=五问中四问;加分=自己的默认值论证+真实案例;红旗="多 agent 更先进"或"多 agent 都是炒作"。

**Q5.13【品味·L4·架构】"多 agent 的本质是用 token 买并行探索"(有研究显示 token 用量解释多 agent 系统 80% 的效果方差)——对系统设计意味着什么?**

参考思路:
- **token 是燃料预算不是副作用**——预算直接决定并行宽度(3 个还是 10 个)和每个的探索深度(浅尝 5 轮还是深挖 30 轮);设计时要显式分配,不是"能开多少开多少"——**没有分配方案的并行是撒钱,不是探索**。80% 方差的含义:同类系统之间比效果,先比 token 账,再看编排巧思。
- **两个配置哲学**——便宜模型+宽并行(量大覆盖广:十条路各走一公里)vs 贵模型+窄并行(单发命中率高:一条路走到底):用 eval 决定,任务分布不同答案不同——分布长尾(什么都能遇到)选前者,分布集中(答案就在深处)选后者。
- **token 进一等监控**——按"任务→子任务→subagent"三级归因(哪个环节烧的、烧出了什么);重复劳动(两个 subagent 搜同一片)是最常见的浪费,要有检测(子任务边界重叠审计——事后比对各 subagent 的工具调用轨迹,重叠度高说明拆分失败)。
- **警惕反向 Goodhart**——为省 token 砍并行度把系统砍回单 agent 效果:省的是钱赔的是质量,算总账——预算纪律的目标是"每分钱买回最多有效探索",不是"花得少"。
- **及格线 / 加分 / 红旗**:及格=燃料观+宽度分配;加分=三级归因与重复劳动检测;红旗=只把 token 当成本不当预算。

**Q5.14【品味·L3·架构】共享任务清单(文件)vs 消息通信,什么时候选哪个?**

参考思路:
- **白板的优势**——全局可见(人一眼看到进度——可观测性是白板被低估的收益)、可回放可审计(每条变更留痕,出事能查)、断点恢复天然支持(重启读清单继续——崩溃丢的是进程不是状态)、任务所有权明确(锁/勾选——谁在干什么一目了然)。
- **消息的优势**——低延迟(不用等轮询)、点对点(不惊动无关方)、事件驱动(条件到了才触发——不需要任何人周期性看板)。
- **组合是常态,纪律是一条**——状态在白板(单一事实源)、通知走消息(触发行动:"任务 3 完成了"是通知,"任务 3 的结果"是状态);**状态只放一处**——两边都放状态,同步地狱从此开始(两个事实源必然分歧,分歧之后你分不清哪个是真的)。
- **判据**——要"人看得懂现在到哪了"和"可恢复"→白板;要"条件触发立即响应"→消息;白板 schema 别过度设计(状态机地狱——每加一个字段就多一种非法组合),消息别承载事实(传话游戏——经过三跳的事实已经不是事实)。
- **及格线 / 加分 / 红旗**:及格=各自优势+组合;加分="状态只放一处"纪律+两个"别"的设计红线;红旗=二选一站队或双状态源。

**Q5.15【品味·L4·安全】红队实测 agent 会合谋、从众、踩踏。你的多 agent 系统要内建哪些群体行为防护?**

参考思路:
- **结构性防护优先于检测**——命名空间强制唯一(防从众:30 个里 18 个同名分支是真实数据——撞名的成本是静默的合并灾难);队列背压+每 agent 请求预算(防踩踏:实测每秒 30 次轮询——资源在 agent 面前没有排队的公德);任务目录边界校验(防跑偏:半数发现落在目录外——探索的热情要关在笼子里)。结构性优先的理由:**检测是概率的,结构是确定的**——检测漏一个就出事,结构上不可能发生的事永远不会发生。
- **通信治理**——信道最小化(能不通信就不通信——每条信道都是潜在的合谋通道)、消息过审查(当不可信输入——来源 agent 可能已被注入)、审计落盘:**合谋需要信道,砍信道比检测合谋便宜**——检测要理解语义,砍信道只要改配置。
- **目标设计**——"不越界"别写在目标里(写在目标里是祈祷,模型尽力而为),结构上限制能力(目录白名单/工具白名单——想越界也没有工具);竞争性任务预设熔断(检测到价格/资源指标异常就冻结——群体的失控是加速的,熔断要自动不要等人)。
- **优先级**——先防踩踏和越界(高概率高危害——每天都会发生的资源竞争),再防合谋(低概率高危害——需要对手方也是 agent 的场景):资源分配逻辑与单 agent 安全一致。
- **及格线 / 加分 / 红旗**:及格=三个结构性防护;加分="砍信道优于检测"+结构/检测的确定性差异;红旗=只谈"把 agent 训练得更好"。

**Q5.31【品味·L3·架构】用"负向清单+正向信号"回答:什么时候**不**该上 multi-agent,什么时候才是真信号?**

参考思路:
- **负向清单——你想要的其实不是 multi-agent**:
  - **想要更长的思考**(想清楚需求)→ 这是"更强的 system prompt + 扩展思考",不是多 agent;
  - **想要更长的上下文**(读长文档)→ 这是"RAG + 长上下文模型",不是多 agent;
  - **想要更快的回答**→ 这是"model routing + 最小可用模型",不是多 agent。
  三条的共同点:需求都能被**单 agent 的参数化能力**满足,拆 agent 是用组织复杂度解决一个提示词/检索问题。
- **正向信号——三个"刚需"任占其一时才认真考虑**:
  - **并行度是刚需**:任务可拆成独立子任务(map-reduce 式文档处理、多方案并行探索);
  - **上下文隔离是刚需**:不同子任务需要各自独立的上下文窗口(过程噪声不该互相污染——Q5.6 的质量收益);
  - **工具集隔离是刚需**:不同角色需要不同工具面(交易 agent 和研报 agent 的工具集必须分开——隔离即安全边界)。
- **正向信号容易漏的边界条件:频次**——三个"刚需"信号成立还要问"任务是不是持续到来":偶发一次的并行任务(一年两次的全库迁移)用脚本一次性编排跑完就够,不值得为它建常驻多 agent 系统——多 agent 是架构决策,维护成本要靠任务频次摊平。
- **工具隔离的安全面(常被漏的机制)**——"工具集隔离是刚需"不只省上下文,还是安全边界:工具白名单随角色收窄,单个 agent 被注入后的爆炸半径从"全系统"缩到"单角色"(与 ch08 的最小权限同构);漏了这层,隔离只剩性能意义。
- **与 Q5.12 的关系**:决策树是流程化版本;本题的价值在**负向清单的表述**——先排除伪需求再谈拆分,比直接问"该不该拆"更防呆。"为了多智能体而多智能体"是 2026 年最常见的技术债来源之一。
- **及格线 / 加分 / 红旗**:及格=负向三条+正向三条;加分=三个信号与隔离收益(质量/安全)的机制连接+偶发任务不建常驻架构+工具隔离的安全面;红旗=把"模型不够强"当上 multi-agent 的理由(多数场景换强模型+好 prompt 更便宜)。

**Q5.32【品味·L3·平台】Dify/Coze 这类平台已经内置了哪些 multi-agent 能力?什么时候用平台、什么时候自研?**

参考思路:
- **平台内置的(以 Dify/Coze 为代表)**——①**编排画布**:节点(LLM/工具/知识库/条件分支/循环)拖拉拽成 workflow;②**编排能力**:多 agent 节点、变量传递、迭代批处理;③**基础设施**:知识库/RAG、工具接入、API 发布、观测日志——原型到 demo 的全链路基本齐了。
- **平台的真实边界(判断的得分点)**——画布表达的是**静态编排**(人预先画好的图),不擅长:动态拆解(orchestrator 按任务临场决定拆法)、跨 agent 共享状态的复杂治理(文件锁/所有权)、生产级的验证关与审批门、以及精细的 token 预算控制——**平台把"能跑起来"的成本降到极低,把"跑得可靠"的成本留给你**。
- **选型判据**——demo/内部工具/流程相对固定的场景→平台,别自研;任务需要动态编排、强验证、领域特定的执行语义(如 QuantAgent 的 Policy Gate 和时效执行)→平台当不了骨架,顶多当外围。
- **平台锁定的隐性成本**——画布、变量语义、节点行为都是平台私有的,迁移成本随画布复杂度超线性增长;缓释手段是让核心逻辑(验证规则、领域语义)留在代码里、平台只做胶水层——业务规则长在画布节点里的那天,退出成本就封顶了。
- **选型的试金石:能不能在平台上做出生产级验证关**——在平台上跑通 demo 的例子很多,但把 Stop-gate(失败重试带错误信息回灌、diff 级一致性检查)做进画布还能讲清每条分支行为的很少;试不出来就说明平台只能到外围、当不了骨架——这是比功能清单对照更硬的判据。
- **面试加分的一句话**——"平台内置的是**能力**,生产系统缺的是**性质**(可审计/受控/可恢复)——能力平台给,性质得自己建"(与 Q13.12 的"框架给能力纪律给性质"同构)。
- **及格线 / 加分 / 红旗**:及格=内置能力清单+一条边界;加分=静态编排 vs 动态拆解的区分、"能力 vs 性质"表述+迁移成本与胶水层边界+验证关当试金石;红旗=绝对站队("平台玩具"或"自研都是造轮子")。

---

## 5.4 开放性问题

**Q5.16【开放·L5·模型】"把编排做进模型"(训练出会自动拆分调度几十个 subagent 的模型)和"把编排做进产品"(人可见可控的编排层),会怎么竞争与融合?**

讨论框架:
- **模型内化派**——优势:编排决策与模型能力联合优化(拆得好不好模型自己知道——它拆的任务它执行,反馈闭环在权重内部)、零配置成本(用户不用画图);劣势:黑盒(人看不到编排过程,审计干预都难——监管场景直接出局)、编排逻辑随模型版本变化不可控(今天 3 个 subagent 明天 5 个,行为漂移没法管理)。
- **产品化派**——优势:人可见可控(干预/审计/责任归属清晰——出事能定位到节点)、编排资产可沉淀(画布/配置是团队财产,不随模型换代蒸发);劣势:静态编排跟不上任务多样性(画布是人画的,任务是随机的),沦为"人肉 RL"——人在替模型调参。
- **融合的合理猜想**——模型管微编排(单任务内多路探索:模型自己决定并行几路、每路多深),产品管宏编排(跨任务的责任边界与审批:什么任务能派给谁、谁批准):类似 OS 内核调度与 k8s 作业编排的分层——内核决定纳秒级怎么切,编排系统决定哪个作业上哪个节点;判据是人需要在哪一层干预。
- **批判点(防止答成科幻)**——两家都有商业动机夸大自己的层(模型厂说"你不需要编排层",平台厂说"模型不可信");判断标准回到"审计与干预需求在哪"——需求不变,分层的形态就变不了。
- **及格线 / 加分 / 红旗**:及格=两派各自优劣;加分=内核/编排的分层类比+判据;红旗=押一边宣布另一边死。

**Q5.17【开放·L5·架构】并行实验中涌现的自发专业角色(去重 agent、性能 agent、评审 agent)——可依赖的工程性质还是不可复现的运气?怎么让自组织可复现?**

讨论框架:
- **涌现的三个前提**——强 verifier(做得对不对有客观信号——gcc 编译过就是过,没有争议空间)、任务可自选(没有中心调度强加——发现什么活值得干是自己判断的)、低协调成本(锁文件即可认领——不需要开会)。三者齐备,专业化分工是压力选择的自然结果(干得好的模式被 verifier 奖励,干不好的被淘汰),有系统科学解释,不纯是运气——**涌现是选择压力的输出,前提是选择压力存在**。
- **复现手段**——初始条件标准化(环境/任务池/规则固定——变量控住,涌现的分布才稳定);**角色涌现后固化为显式 agent 定义**(下一轮直接用——涌现告诉你"这里该有个角色",设计接手把它做稳);多次运行统计方差(单次涌现的分工可能漂移——这一轮的去重 agent 下一轮可能不存在,方差大就说明"涌现"其实是噪声)。
- **风险**——从众性证明"自组织"也会自我强化错误模式:同质化的 30 个 agent 是羊群不是分工(都往一个方向卷);需要多样性机制(不同初始上下文/探索策略——起点不同,路径才不同)。
- **判断**——短期当实验现象观察,别把系统可运行性押在涌现上(生产系统要的是确定性,涌现给的是惊喜,惊喜的生产定义是事故);长期把涌现出的模式沉淀为显式设计——**涌现是设计的探测器,不是设计的替代品**。
- **及格线 / 加分 / 红旗**:及格=三前提+复现两手段;加分="涌现是选择压力的输出"+"探测器不是替代品";红旗=当玄学(不可控)或当银弹(直接依赖)。

**Q5.18【开放·L4·管理】组织采纳多 agent 时,怎么防止"为炫技而上"?**

讨论框架:
- **立项评审清单流程化**(Q5.7 四句):为什么不是单 agent?谁拿什么上下文和工具?谁负责独立验收?失败怎么恢复?答不上来打回——成本最低的治理(一张清单挡掉一大半冲动立项,比事后回滚便宜两个数量级)。
- **用 eval 驱动架构决策**——单 agent baseline 和多 agent 方案在内部任务集同跑:数据说话,"并行快 3 倍但成功率掉 15%"这类结果自动杀死不合理拆分——**让架构方案在内部任务集上打一架,输的退场**,比会议争论文明且结论可复现。
- **复盘制度化**——上线后按月复盘 token/质量/延迟三曲线(三条一起看:token 涨质量平=浪费,质量涨延迟爆=过度拆);劣化归因(设计/协调/验证,按 MAST 三类)反哺评审清单——上线不是终点,清单要随踩坑进化。
- **升级门槛 checklist 化**——从单到多的可量化条件(信息量证据——上下文确实装不下;隔离方案——子任务边界画得出来;验收方案——每个子任务的 verifier 是什么)写成 checklist:把"想上"变成"证明该上"——**把判断力固化成清单,是组织比个人活得久的机制**。
- **及格线 / 加分 / 红旗**:及格=评审清单+eval 对比;加分=三曲线复盘与清单进化;红旗=靠"大家都在用"当立项理由。

---

## 5.5 工程实战题

**Q5.19【实战·L3·平台】用 subagent 设计"PR 深度 review"流水线:主 agent 只做综合,三个 subagent 分别看正确性/安全性/测试覆盖。**

参考思路:
- **subagent 侧**——只读工具白名单(Explore 类——review 不该有写权限,只读也是安全边界);独立 system prompt(各自关注点+输出 schema:发现/严重度/证据 file:line/置信度——schema 让三个报告可机械合并);结果以结构化摘要回传(1–2K)。
- **主 agent 侧**——分派时给 diff+上下文指引(不是"看看这个 PR"而是"这个 PR 改了支付重试逻辑,重点看 X"——分派 prompt 的质量直接决定三个 subagent 看的方向);综合去重(三个都报同一问题算一个);冲突结论的裁决规则(按严重度排序或升级人工——规则要在 prompt 里显式给,不能指望它自己有判断标准)。
- **防坑三件**——任务描述自包含(subagent 看不到主对话历史——它的全部世界就是你派发的那段话);description 简短(常驻成本——三个 subagent 的描述每轮都在场);并行 3 个够(再多是评审委员会不是 review——覆盖面和成本的反比点在个位数)。
- **决定质量的细节**——reviewer 与 coder 用不同模型(打破同权重盲区,Q5.11):这个细节是流水线质量的决定项之一,答出来就是加分。
- **及格线 / 加分 / 红旗**:及格=三分工+自包含+结构化摘要;加分=裁决规则与模型独立性;红旗=subagent 返回自由文本没有 schema。

**Q5.20【实战·L4·架构】设计团队的并行 agent 开发工作流:5 个 agent 同时开发 5 个特性,汇入一个主干。**

参考思路:
- **隔离**——每 agent 独立 worktree+分支+独立构建环境(ch04 Q4.20 的执行环境层是地基——本题是那个环境层之上"开发工作流"这一层)。
- **认领与所有权**——共享任务看板+**文件级所有权声明**(改哪些模块):两个 agent 改同一文件必然互相覆盖,所有权开工前显式划分+冲突预检(派发时检查两个任务的模块清单是否相交,相交就在设计层解决——改任务边界,别指望运行时谦让)。
- **集成**——串行合并窗口(按依赖顺序)→本地全测→合入→通知下一个 rebase;或 stacking(每个 PR 基于前一个)。**合并冲突 agent 能自己解,但解完必须全量测试——冲突解决是最容易"看起来对了"的环节**(两边的意图都被保留了半个,合起来语义对不对只有测试知道)。
- **验证与兜底**——每个 diff 过独立 reviewer agent;主干 CI 是最终 verifier(机器裁判不受任何 agent 的"我认为"影响);失败任务现场保留、人工接管协议、预算与轮次上限、夜间熔断(无人时段的失控没有人在场,熔断是唯一保险)。
- **及格线 / 加分 / 红旗**:及格=隔离+所有权+串行合并;加分=冲突解决后强制全测+设计层冲突预检;红旗=5 个 agent 共享一个分支"互相协调"。

**Q5.21【实战·L3·平台】实现 lead agent 的任务分派 prompt 模板,要求 subagent 无共享记忆也能正确执行。**

参考思路(七件套模板):
- **目标一句话**(做什么、做到什么程度——"可验证的完成态"不是"尽力而为");**边界**(做什么/绝不做什么——禁区比目标更防跑偏);**输入材料**(具体文件路径/issue 链接——不是"仓库里找找",让它猜就是让它赌);**输出格式**(schema:结论/证据/置信度/未解决问题——没有 schema 的返回没法机械合并);**工具指引**(该用什么不该用什么——限定工具面同时是安全边界);**停止条件**(预算/轮次/完成判定——自主性要有栅栏);**失败报告格式**(做不完时回报什么——失败的报告和成功的报告一样有结构)。
- **心法**——当成给新入职同事的交接文档写:他聪明但对你的一切上下文一无所知——**聪明但无知**,模板的每一件都在补"无知"这一半。
- **反面教材**——派"调研一下认证模块":没有边界没有输出格式,subagent 的摘要大概率不含你要的那个信息,主 agent 还得追问一轮(又烧一遍 token——追问的那轮成本,就是模板偷懒的利息)。
- **及格线 / 加分 / 红旗**:及格=七件套给五件;加分=有迭代教训(哪次派发翻车后加了哪一条);红旗=一句"帮我看看 X"。

---

## 5.6 场景排错题

**Q5.22【排错·L4·平台】3 个并行 subagent 跑完,主 agent 的综合结论明显忽略了其中一个的关键发现。排查?**

排查链路:
- **①摘要丢失(第一怀疑)**——该发现没进 1–2K 摘要:subagent 觉得它是"细节",主 agent 连见都没见过——修输出 schema,强制"关键发现必列"字段(把筛选标准写进结构,不赌 subagent 的判断)——最常见的根因,先查这个。
- **②位置效应**——lost in the middle:中间回来的报告被忽视(U 型曲线在多 agent 汇总场景同样生效——三份报告按到达顺序串进上下文,中间那份正落在注意力洼地);调整汇报顺序/结构化注入(每份独立编号+开头给结论)。
- **③综合 prompt 缺取舍标准**——主 agent 不知道"三个发现冲突听谁的":给裁决规则(严重度优先/时间戳新者优先/冲突标升级)——没有规则的综合是抽奖。
- **④静默截断**——并发结果超上下文被截:检查汇总前的 token 预算(三份摘要+diff 可能已经不小)。
- **修复模式**——综合阶段用结构化输入(每个 subagent 结论独立编号),必要时二次追问("对 subagent-2 的 X 你如何处理?"——强制逐条对账,漏答会暴露)——编号+追问是通用修复,先上再归因。
- **及格线 / 加分 / 红旗**:及格=①②两条;加分=编号+追问的修复模式;红旗=归因"主模型不够聪明"换更大的模型。

**Q5.23【排错·L4·平台】两个 teammate 各自"完成"了任务,合并时发现互相覆盖,且都声称测试通过。根因与流程修复?**

排查链路:
- **根因机制**——共享工作区或同文件所有权未划分,后写覆盖先写;"各自测试通过"因为在各自时点各自为真——A 测完过了,B 的覆盖才发生,B 测的又是自己的功能——**不是谁说谎,是时间线错位**,这个机制讲清就不难修(两个真话拼出一个假结果)。
- **流程修复**——文件所有权/模块划分显式化(开工前声明+冲突预检);写前 rebase/pull(让"过时基线上的绿灯"不可能——基线变了,绿灯自动失效重测);合并窗口串行化(两个"完成"不在同一时刻进入主干);diff 级一致性检查进 Stop-gate(完成前检查工作区与远端一致——声明完成时,你的改动必须建立在最新事实上)。
- **长期**——把"两个 teammate 改同一文件必然互相覆盖"变成 lint 规则/准入检查(任务派发时自动比对模块清单):教训固化为工具,不是 wiki 里的一句提醒——**wiki 里的教训只对读过的人生效,工具里的教训对所有人生效**。
- **及格线 / 加分 / 红旗**:及格=所有权+串行合并;加分=写前 rebase 的机制解释+教训工具化;红旗=归因某个 agent"有 bug"。

**Q5.24【排错·L3·架构】multi-agent 上线后成本是单 agent 的 25 倍(预期 15 倍),质量只升 5%。诊断?**

排查链路:
- **四条浪费机制逐个查**——①重复劳动(多 subagent 搜同一信息:任务边界重叠→加去重/缓存/边界审计——比对各 subagent 的工具调用轨迹,重叠度是直接证据);②过度委派(lead 把自己能干的也派出去→分派判据写进 lead prompt——"一句话能答的别拆");③返回冗长(没按摘要规格→schema 强制+超长打回);④协调风暴(轮询/同步风暴→改事件驱动/批量同步)。四条各有检测信号,先定位再修。
- **质量只升 5% 的真正原因(本题灵魂)**——瓶颈根本不在并行度,在任务定义或验证器(MAST:多数失败在规格层)——加多少 agent 都没用:规格错的任务,并行只会更快地生产错误;没有验收的并行,连"质量升没升"都测不准(那个 +5% 本身的可信度都存疑)。先修 spec。
- **决策点**——诊断出后者后,正确的动作可能是**回退到单 agent**:多 agent 是手段不是目标——这个说得出口的团队不多,敢说就是加分(撤掉刚建的系统需要承认决策失误,组织阻力大——能越过这个阻力的团队不多)。
- **及格线 / 加分 / 红旗**:及格=四条浪费机制;加分=识别"瓶颈不在并行度"并敢回退;红旗=继续调优并行度或加更多 agent。

---

## 5.7 开放加餐:偏好与品味追问

**Q5.25【开放·L4·架构】你自己什么时候真的开多 agent?分界线在哪?说一次真香和一次翻车的经历。**

参考思路(一个好答案长什么样):
- **分界线的三个判据**——子任务之间上下文独立(不共享中间状态——各自的世界不重叠)、可并行(没有顺序依赖——不用等来等去)、结果可聚合(能机械合并——schema 对得上):**三个都满足才值得开,否则单线程更稳**。
- **真香的形态**——批量同构任务(给 50 个文件做同一种迁移——同构意味着零协调成本,并行的收益全额到账)、多方案并行探索后择优(三条技术路线各走一版,拿真东西对比而不是空想辩论):吞吐收益真实存在。
- **翻车的形态**——共享状态的协调成本(两个 agent 改同一处打架——修冲突的时间超过并行省的时间)、结果趋同(没有独立性,开了等于没开——白烧 3 倍 token 买三份一样的答案)、聚合失败(五个方案五种风格,合并比重写贵):翻过车才知道"多"不是默认选项——**每次翻车都在教那条判据为什么存在**。
- **对争论有站队**——单线程派 vs 团队派(本章的辩论题)自己站哪边、什么条件下换边:**品味题要的是立场加条件,不是骑墙**——"我默认单线程,因为我的任务分布是写密集"就是好答案。
- **及格线 / 加分 / 红旗**:及格=三判据+一个真实经历;加分=真香和翻车都有+有站队;红旗=因为"多 agent 高级"而用、从没手动拆过任务。
