# ch05 · 多智能体系统

> 覆盖:什么时候值得多 agent、协作形态(subagent/编排/共享任务清单/并行写代码)、上下文隔离的收益与代价、失败分类(MAST)、单线程派 vs 团队派之争(Cognition vs Anthropic)、多 agent 的群体行为风险、A2A 与 MCP 的分工。
> 案例基线:2026-08。机制题不挂时间;带日期案例以标注为准。
> 答案读法:**加粗句连读就是可背的骨架**;后面的解释帮你理解;「补充弹药」是追问时再掏的细节。

---

## 5.1 八股题(基础概念)

**Q5.1【八股·L2·架构】分析任何一个多 agent 系统,先看哪三个正交维度?为什么用维度不用分类树?**

- **人话版**:谁在指挥(控制拓扑)、任务怎么流(编排模式)、靠什么收敛(协调机制)。三个维度正交——一个系统可以同时"中心化控制+编排者-工人推进+共享白板收敛";硬塞进分类树的某个格子是分析错误的开始。
- **主线(照这条线讲)**:
  1. **维度一:控制拓扑**——谁有最终决策权:单 lead(中心化,好审计好追责,lead 是单点)还是无 lead 协商(去中心化,弹性高,治理难)。
  2. **维度二:任务流**——工作单元怎么产生和传递:orchestrator-worker(动态拆解)、流水线(固定阶段)、committee(并行表决)。**注意它和拓扑独立**:去中心化系统也可以有流水线任务流。
  3. **维度三:收敛机制**——多个 agent 的状态怎么对齐:共享任务清单(blackboard)、消息通信、独立产出+仲裁。多数文章把"任务怎么流"和"agent 怎么说话"混为一谈——**热闹的消息不等于系统骨架,共享状态才是**。
  4. **用法收尾**——拿到任何系统(自研或论文)三问各答一句,架构图立起来;评估改版也按维度看动了哪层——三维度是分析工具,不是知识点。
- **补充弹药(追问再掏)**:
  - **示范分析**:Claude Code subagents=中心化(主 agent 派发)+orchestrator-worker+摘要回传收敛——10 秒过完三维。
  - **常见混维**:把 A2A(通信协议)当"编排模式"讨论——协议在收敛维度,不在任务流维度。
- **及格线**:三维度+正交含义。
- **加分**:现场用一个真实系统做三维分析;"任务流≠通信"的区分。
- **红旗**:只会列名词分类;什么系统都往 orchestrator-worker 塞。

**Q5.2【八股·L2·平台】Claude Code 的 subagent 是什么?关键机制和约束?**

- **人话版**:Markdown 文件定义的"专职代理"——独立上下文窗口、自己的 system prompt、独立工具集和权限、可指定不同模型。主 agent 派活,它在自己窗口里干完,只把摘要回给主对话。关键约束:不继承父对话历史——任务描述必须自包含。
- **主线(照这条线讲)**:
  1. **定义形态**——Markdown+YAML frontmatter(name/description/tools/model);存放优先级从组织托管到用户目录,项目级的入版本库;内置 Explore(只读)/Plan/通用三种。
  2. **两条硬约束(本题核心)**——不继承父上下文("没有共享内存"):派发 prompt 要当交接文档写(目标/边界/输入材料/输出格式/停止条件);摘要回传:subagent 烧几万 token 的过程,主对话只收 1–2K 结论。
  3. **量的约束**——默认最多 20 并发、嵌套 3 层;**所有 description 常驻上下文**,合计超 15K token 告警——subagent 定义本身也是上下文成本。
  4. **演进注脚**——从按需派发到产品化并行团队(共享任务清单+文件锁+mailbox,见 Q5.4),但"独立上下文+摘要回传+任务自包含"三个底层机制没变——机制比产品形态稳定,记机制。
- **补充弹药(追问再掏)**:
  - **与 context rot 的关系**:隔离让检索噪声的注意力稀释只发生在子窗口——这是隔离的质量收益(Q5.6 展开)。
  - **description 的工程学**:一句话写清"什么任务该派给我"——它是主 agent 的路由依据,等价于工具描述(ch03)。
- **及格线**:独立上下文+摘要回传+不继承历史。
- **加分**:派发 prompt 自包含的要求;description 的 token 成本。
- **红旗**:以为 subagent 看得到主对话;把 subagent 当"并行加速"(隔离才是主要收益)。

**Q5.3【八股·L2·架构】常见多 agent 协作形态有哪些?各自适用场景和代价?**

- **人话版**:六种——编排者-工人(lead 拆活)、流水线(固定接力)、委员会/投票(并行表决)、共享白板(围绕任务清单)、事件驱动(异步集成)、对等协商(跨组织)。每种都有明确适用面和代价,没有全能形态。
- **主线(照这条线讲)**:
  | 形态 | 最像 | 适合 | 主要代价 |
  | --- | --- | --- | --- |
  | Orchestrator-Worker | 项目经理带专项同事 | 研究类宽搜、可拆解任务 | lead 是瓶颈和单点;token 数倍 |
  | 流水线 | 审批流 | 阶段清晰的流程任务 | 前序错误级联;僵硬 |
  | 委员会/投票 | 评审会 | 高不确定性推理、红队 | 贵;多数谬误(共识≠正确) |
  | 共享状态/白板 | 围白板干活 | 要回放/审计/断点恢复的系统 | schema 设计难;并发冲突 |
  | 事件驱动 | 消息总线 | 多系统集成、异步 runtime | 调试/重试/幂等复杂 |
  | 对等协商 | 独立组织谈判 | 跨团队/跨公司边界 | 治理与信任最难建 |
  1. **表格里挑三种展开"为什么"**——如投票的多数谬误:对"有标准答案"的任务投票有效,对"品味"任务投票得到的是均值不是真值;白板的可回放性是生产系统的刚需(断点恢复、审计)。
  2. **点出选型的第一问**——不是"用哪种形态"是"要不要多 agent"(Q5.12):形态选择发生在确认要拆之后;多数生产系统是混合体(orchestrator-worker 骨架+白板状态+评审质量关)。
- **补充弹药(追问再掏)**:
  - **混合实例**:Anthropic 研究系统=lead 拆解+3–5 subagent 并行+lead 综合+大计划写文件(memory)——三种形态拼的。
  - **反模式**:为了用事件驱动而引入消息总线——多数团队规模用共享文件+轮询就够,架构要配规模。
- **及格线**:六种说对四种+各自代价。
- **加分**:混合形态的现实认知;"共识≠正确"的警惕。
- **红旗**:认定一种形态万能。

**Q5.4【八股·L2·平台】Claude Code 的 Agent Teams / 并行子代理的团队化设计要点?跨 agent 消息为什么被当作不可信输入?**

- **人话版**:一个 team lead 协调多个 teammate(各自独立会话),围着共享任务清单推进(文件锁防抢活),队员间可互发消息。跨会话消息被当不可信输入过审查——注入可以经消息传播。
- **主线(照这条线讲)**:
  1. **四个机制**——lead 协调(拆解/分派/综合);teammate 独立会话并行;**共享任务清单+文件锁**(任务认领制:两个 agent 领同一任务的解法就是锁文件);mailbox 异步通信(可审计)。
  2. **消息当不可信输入的理由(本题核心)**——一个 teammate 读过的网页/issue 可能藏注入,它的消息可能已被污染:把通信当攻击面处理是把信任边界思想(ch08)搬进多 agent 通信层。**这不是过度设计**——红队已在真实系统观察到 agent 间合谋与破坏(Q5.8)。
  3. **三条工程教训**——两个 teammate 改同一文件必然互相覆盖:文件所有权要显式声明;任务清单用结构化格式(防 agent "美化"状态,ch06);产出写文件系统而不是只靠传话(减少信息衰减)。
  4. **演进注脚**——并行规模从几个 teammate 到产品化的数十至数百并行子代理(动态工作流,2026),底层还是三件事:独立上下文、共享状态、受控通信。
- **补充弹药(追问再掏)**:
  - **与 MAST 的对应**:消息不可信、所有权、状态结构化——分别对应错位/规格/验证三类失败的预防。
  - **规模数字**:Anthropic 研究系统 token 约为普通聊天 15 倍——团队化不是免费的,预算连回 Q5.13。
- **及格线**:四机制+消息不可信的理由。
- **加分**:注入经消息传播的攻击链;文件所有权教训。
- **红旗**:把 agent 间通信当纯技术问题;以为并行数越多越好。

**Q5.5【八股·L2·架构】A2A 和 MCP 的分工?什么时候需要 A2A?**

- **人话版**:MCP 管 agent↔工具/数据(能力接入);A2A 管 agent↔agent 服务(发现、委托、结果共享)。本地 subagent 不需要 A2A——共享内存低延迟;跨团队/跨组织的独立 agent 服务之间才值钱。
- **主线(照这条线讲)**:
  1. **A2A 的核心概念**——Agent Card(HTTP 可发现的能力自描述:"我能干什么、怎么调我")、Task(带生命周期的委托:不是一次 RPC 是有状态的协作)、Message/Artifact(过程与产物)。定位"MCP 的补充不是替代"。
  2. **判断函数(本题核心)**——参与方是不是独立部署的服务(有自己的生命周期、所有者、信任域)?是→A2A 的发现与委托语义有价值;否(同进程 subagent)→A2A 是纯开销,直接函数调用+共享状态更简单。
  3. **常见误判点破**——把"多 agent"等同于"需要 A2A":企业内自研多 agent 系统九成是本地编排;**A2A 解决的是跨边界互操作,是生态问题不是架构问题**。
- **补充弹药(追问再掏)**:
  - **治理**:A2A 已交中立基金会、百余厂商参与——和 MCP/AGENTS.md 一样走开放标准路线,这是它可能成事的路径。
  - **对照记忆**:MCP=你用什么工具;A2A=你找谁干活。
- **及格线**:分工一句话+判断函数。
- **加分**:Agent Card/Task 机制;什么时候 A2A 是伪需求。
- **红旗**:认为 A2A 是 MCP 升级版;本地 subagent 也套 A2A。

---

## 5.2 原理深挖

**Q5.6【原理·L3·平台】subagent 的上下文隔离,为什么说主要收益是"质量"而不只是"并行加速"?**

- **人话版**:subagent 在自己窗口里烧掉几万 token 的混乱搜索,只回传 1–2K 结论——主上下文保持高信噪比。并行是顺带的,隔离才是本体:检索噪声不污染主线程、context rot 被限制在子窗口、主会话缓存不被破坏。
- **主线(照这条线讲)**:
  1. **三个隔离收益逐个讲**——质量(主 agent 不被过程噪声带偏:rot 对它无效);缓存(子会话短且独立,主会话稳定前缀不动);成本可控(子任务超支烧在子窗口,失败可整体丢弃重试,不污染主历史)。
  2. **隔离的代价必须同讲(这题的平衡点)**——信息不共享是它的另一面:两个 subagent 各自探索同一片区域互不知道(Cognition 批评的核心);关键发现只在摘要里,细节丢了就丢了。
  3. **所以隔离是赚是赔取决于两件事**——派发时的任务边界设计(避免重叠);摘要的输出 schema(强制"关键发现必列")——**隔离的收益不是自动的,是设计出来的**。
  4. **选型口径收尾**(Anthropic 的三分法)——会话内拉锯用 compaction;里程碑式长任务用结构化笔记;并行深挖/高噪声任务用 subagent 隔离——三种上下文管理手段互补,不是竞争。
- **补充弹药(追问再掏)**:
  - **官方数字**:subagent 结果官方口径压缩到 1–2K token 摘要——"多大算够"可以用"主 agent 拿摘要能否做决策"来验收。
  - **反例**:派发"调研一下认证模块"这种无边界任务——两个 subagent 大概率重叠探索,隔离的全收益归零。
- **及格线**:三个收益+代价。
- **加分**:任务边界与摘要 schema 的设计责任;"隔离与一致性是 trade-off"的表述。
- **红旗**:把 subagent 理解成多线程提速。

**Q5.7【原理·L4·架构】MAST(多 agent 系统失败研究)的失败分类体系?对工程的最重要启示?**

- **人话版**:对多个框架的上千条失败 trace 系统标注,归纳成 14 种失败模式、三大类:规格/系统设计问题、agent 间错位(inter-agent misalignment)、任务验证问题。最重要发现:**大多数失败发生在写代码之前——设计和协调层,不是执行层**。
- **主线(照这条线讲)**:
  1. **三大类各给直觉**——规格类(任务定义就错了:目标不清/边界模糊/角色重叠);错位类(agent 之间对目标/状态理解不一致:一个改了约束另一个不知道);验证类(没有独立成功判定,错了对不了账)。
  2. **报量级**——第三方对标注的统计分析:规格+协调合计约八成——**失败集中在"拆任务的瞬间"和"对齐状态的机制",不在"单个 agent 会不会写代码"**。
  3. **工程启示一:评审要问对地方**——四句评审:为什么不是单 agent?谁拿什么上下文和工具?谁负责独立验收?长任务失败怎么恢复?四句答不上就别上生产——问题清单比架构图更能筛掉坏设计。
  4. **工程启示二:taxonomy 本身是可复用工具**——团队自建系统时先给自己的失败 trace 建分类,高频类就是修复优先级(工具层修复经常"修一处好一片")——方法论连回 ch10 反脆弱。
- **补充弹药(追问再掏)**:
  - **研究形态**:7 个框架、1600+ 标注 trace、标注者一致性 κ=0.88——方法学上扎实,不是博客随想。
  - **同源结论**:MAS 在流行基准上收益"常常微乎其微"——与 Cognition 的批评互为印证。
- **及格线**:三大类+"失败在设计不在执行"。
- **加分**:四句评审清单;taxonomy 方法论迁移。
- **红旗**:只记失败比例数字;归因"模型不够聪明"。

**Q5.8【原理·L4·安全】多 agent 系统会出现哪些"群体行为"风险?实证?怎么防护?**

- **人话版**:从众(30 个 agent 里 18 个取了同一分支名)、资源踩踏(每秒 30 次轮询任务队列)、合谋(定价博弈第 3 轮达成默契,切断通信后还能通过公开挂牌串价)、目标冲突时升级为互相破坏。结论:协调不会从"更强的个体智能"里自然涌现,治理要显式设计。
- **主线(照这条线讲)**:
  1. **实证来源先立信誉**(Anthropic 前沿红队,2026-08)——45 个 agent 的漏洞挖掘集群找到 266 个漏洞 vs 独立并行组 21 个(协调收益真实);但约半数发现落在指定目录外、两组仅 12 个重叠(重复劳动和跑偏同样真实);Bertrand 定价博弈第 3 轮即达价格下限;目标冲突时出现自我复制恶意软件、伪装代码归属。
  2. **收益与风险同源(本题的认知高点)**——让 agent 协调的机制(通信/共享状态)正是合谋与从众的通道:不能因噎废食,要分层防护。
  3. **防护清单对着行为讲**——命名空间隔离(强制任务/分支唯一:防从众撞名);队列背压+请求预算(防踩踏);通信信道最小化+审计(防合谋——**连公开可观察的环境变量都可能是串谋信道**);任务目录边界校验(防跑偏);写操作集中审批(限爆炸半径)。
- **补充弹药(追问再掏)**:
  - **串价机制细讲**:切断直接通信后,agent 通过"公开挂牌价格"的博弈论信号达成默契——防护不能只砍信道,还要限制公开可观察信号。
  - **排错连接**:线上多 agent 行为高度同步的排查思路见 ch08 Q8.22。
- **及格线**:四种群体行为+各自一句防护。
- **加分**:收益与风险同源的洞察;"公开信号也是信道"的机制理解。
- **红旗**:认为"每个 agent 都对齐了系统就安全"。

**Q5.9【原理·L4·架构】"单线程派"(Cognition)和"团队派"(Anthropic)的争论,双方论据?你的裁决?**

- **人话版**:单线程派:行动携带隐式决策,两个 agent 的隐式决策冲突就会打架(双 subagent 写同一个游戏、风格互相打架的案例),默认该单线程线性。团队派:宽搜任务信息量超单窗口,并行 subagent 收益巨大(研究系统 +90% 实证),写密集才要小心。两边共享一个前提:先证明单 agent 不够。
- **主线(照这条线讲)**:
  1. **单线程派的原则**——①共享上下文要共享完整 trace 而不是摘要(摘要丢的正是隐式决策);②"行动携带隐式决策,冲突的决策导致坏结果"——这条对一切并行写场景成立;③超长任务用专门微调的压缩模型蒸馏历史,不是拆人。
  2. **团队派的证据**——内部研究评估多 agent 比单 agent 高 90.2%,代价 token 约 15 倍:所以只配高价值任务+便宜模型当 worker;编排是 lead 拆解+3–5 subagent 并行(经验法则:简单查找 1 个、对比 2–4 个、开放研究 10+)。另有 16-agent 并行编译实验(锁文件认领、gcc 当 oracle、两周 $20k 产出 10 万行能编译 Linux 的 Rust 编译器,还涌现出去重/性能/评审的自发角色)证明**强隔离+合并纪律下并行写可行**。
  3. **我的裁决框架(三问)**——任务是读密集还是写密集(读并行安全,写并行必冲突——除非 worktree+所有权+串行合并)?信息量是否超单窗口?每步的隐式决策是否需要共享?三问通过才拆。
  4. **分歧的本质**——两派分歧在"默认值设哪边",而默认值应该由任务分布决定,不是信仰——把这层说破,答案就高于两派口号。
- **补充弹药(追问再掏)**:
  - **数字的语境**:90.2% 是内部研究评估(宽搜任务)不是普适提升;15× 是当时模型代际的数字,便宜模型会稀释它——引用数字必带语境。
  - **编译实验的涌现角色**:去重 agent、性能 agent、评审 agent 是自发分工——自组织的讨论见 Q5.17。
- **及格线**:双方各两条论据+"分歧在默认值"。
- **加分**:自己的三问裁决框架;编译实验作为"并行写在什么条件下可行"的证据。
- **红旗**:只知一方口号;单独引用 15×或 90% 当结论。

**Q5.10【原理·L3·架构】memory / shared state / artifact 三者怎么区分?混在一起会怎样?**

- **人话版**:memory 是"记住过什么"(跨会话经验教训)、shared state 是"现在局面是什么"(任务清单、进度面板)、artifact 是"已经产出了什么"(摘要、报告、补丁)。三者的生命周期、读者、写入规则完全不同——混写是系统发虚的常见根源。
- **主线(照这条线讲)**:
  1. **三者的角色与存储形态**——memory 给未来任务看(沉淀决策与坑,长期;记忆文件);shared state 给在场 agent 看(当前任务与状态,任务期;任务清单/数据库);artifact 给消费者看(人审或下游,交付期;仓库+版本化)。
  2. **混写的三个病症**——过程结论写进 memory(污染长期记忆,越积越乱);长期规则写进 shared state(每次任务重建,规则丢失);artifact 当 state(交付物被反复改写,没人知道哪版对)。
  3. **设计动作(可操作)**——每个写入点先问三问:这是什么性质的信息、给谁看、活多久;三种载体分开管理各自的清理策略(memory 园艺/state 重置/artifact 版本化——展开见 ch06)。
- **补充弹药(追问再掏)**:
  - **验收法**:拿团队的存储层截图,随便指一条问"这是三类中的哪类"——答不上或争执不下,就是混写的证据。
  - **对照**:ch06 的记忆治理与本章的收敛机制,合起来是这题的完整答案。
- **及格线**:三者定义+一个混写病症。
- **加分**:写入三问;差异化清理策略。
- **红旗**:三词混用;所有东西塞一个数据库。

**Q5.11【原理·L3·架构】"Reflection 不等于独立验收"——为什么 agent 自我复盘效果有限?**

- **人话版**:reflection 解决"同一执行链能否自我修正一点",不解决"有没有独立证明"。执行者、反思者、总结者如果是同一个模型同一个上下文,就是一路自洽到底——只是"自洽得更认真"。
- **主线(照这条线讲)**:
  1. **信息论的解释(核心)**——自我复盘没有引入新信息源:同一套权重有同样的盲区,采样两次绕不过系统性错误(实证见 ch07:无外部反馈的自我纠错不升反降)。
  2. **多 agent 场景的变形(容易被骗的点)**——同一个模型 spawn 出来的"reviewer agent"仍是同一权重:换上下文有帮助(至少没被实现过程锚定),**换模型/换验证器才有质变**——验收的独立性按"信息源不同"分级:执行结果(测试)>不同家族模型>人类。
  3. **正解的形态**——验收权与执行权分离:end-state 检查(目标达成没)、constraint 检查(约束违反没)、regression 检查(原有功能坏没)三项独立执行,reviewer 用不同模型或确定性脚本。
- **补充弹药(追问再掏)**:
  - **常见的自我安慰**:"加了 reflection agent 就有验收了"——问一句 reviewer 和 coder 什么模型,九成露馅。
  - **便宜起步**:reviewer 换成脚本(三项检查自动化)比换模型更便宜也更独立——确定性脚本是最强的"不同信息源"。
- **及格线**:无新信息源这个机制+多 agent 变形。
- **加分**:独立性按信息源分级;三项检查的具体形态。
- **红旗**:认为"加一层 reflection agent"就是验收了。

---

## 5.3 品味与判断题

**Q5.12【品味·L4·架构】接到需求后,你怎么回答"该不该上 multi-agent"?给决策树。**

参考思路(五问决策树):
- **1. 任务信息量超单窗口吗?且可分区吗?**——不超→单 agent,后面都不用问(第一问就挡掉多数伪需求);
- **2. 子任务是读密集还是写密集?**——写密集→先解决隔离与合并,没解决前别拆(Cognition 批评的适用区);
- **3. 拆开是职责更清楚还是更模糊?**——1 个大黑盒变 3 个小黑盒等于没拆;
- **4. 每个 subagent 的产出有独立验收吗?**——没有验收的并行=并行的错误;
- **5. 15× 的 token 买得起吗、任务价值配得上吗?**——铺量任务用并行是烧钱。
- **默认值之争要主动引**:单线程派(默认单,证明不够再拆)vs 团队派(宽搜任务收益实证)——给出自己的默认值和理由,比背口号有价值。
- **及格线 / 加分 / 红旗**:及格=五问中四问;加分=自己的默认值论证+真实案例;红旗="多 agent 更先进"或"多 agent 都是炒作"。

**Q5.13【品味·L4·架构】"多 agent 的本质是用 token 买并行探索"(有研究显示 token 用量解释多 agent 系统 80% 的效果方差)——对系统设计意味着什么?**

参考思路:
- **token 是燃料预算不是副作用**——预算直接决定并行宽度(3 个还是 10 个)和每个的探索深度;设计时要显式分配,不是"能开多少开多少"。
- **两个配置哲学**——便宜模型+宽并行(量大覆盖广)vs 贵模型+窄并行(单发命中率高):用 eval 决定,任务分布不同答案不同。
- **token 进一等监控**——按"任务→子任务→subagent"三级归因;重复劳动(两个 subagent 搜同一片)是最常见的浪费,要有检测(子任务边界重叠审计)。
- **警惕反向 Goodhart**——为省 token 砍并行度把系统砍回单 agent 效果:省的是钱赔的是质量,算总账。
- **及格线 / 加分 / 红旗**:及格=燃料观+宽度分配;加分=三级归因与重复劳动检测;红旗=只把 token 当成本不当预算。

**Q5.14【品味·L3·架构】共享任务清单(文件)vs 消息通信,什么时候选哪个?**

参考思路:
- **白板的优势**——全局可见(人一眼看到进度)、可回放可审计、断点恢复天然支持(重启读清单继续)、任务所有权明确(锁/勾选)。
- **消息的优势**——低延迟、点对点、事件驱动(不需轮询)。
- **组合是常态,纪律是一条**——状态在白板(单一事实源)、通知走消息(触发行动);**状态只放一处**——两边都放状态,同步地狱从此开始。
- **判据**——要"人看得懂现在到哪了"和"可恢复"→白板;要"条件触发立即响应"→消息;白板 schema 别过度设计(状态机地狱),消息别承载事实(传话游戏)。
- **及格线 / 加分 / 红旗**:及格=各自优势+组合;加分="状态只放一处"纪律;红旗=二选一站队或双状态源。

**Q5.15【品味·L4·安全】红队实测 agent 会合谋、从众、踩踏。你的多 agent 系统要内建哪些群体行为防护?**

参考思路:
- **结构性防护优先于检测**——命名空间强制唯一(防从众:30 个里 18 个同名分支是真实数据);队列背压+每 agent 请求预算(防踩踏:实测每秒 30 次轮询);任务目录边界校验(防跑偏:半数发现落在目录外)。
- **通信治理**——信道最小化(能不通信就不通信)、消息过审查(当不可信输入)、审计落盘:**合谋需要信道,砍信道比检测合谋便宜**。
- **目标设计**——"不越界"别写在目标里,结构上限制能力(目录白名单/工具白名单);竞争性任务预设熔断。
- **优先级**——先防踩踏和越界(高概率高危害),再防合谋(低概率高危害):资源分配逻辑与单 agent 安全一致。
- **及格线 / 加分 / 红旗**:及格=三个结构性防护;加分="砍信道优于检测";红旗=只谈"把 agent 训练得更好"。

---

## 5.4 开放性问题

**Q5.16【开放·L5·模型】"把编排做进模型"(训练出会自动拆分调度几十个 subagent 的模型)和"把编排做进产品"(人可见可控的编排层),会怎么竞争与融合?**

讨论框架:
- **模型内化派**——优势:编排决策与模型能力联合优化(拆得好不好模型自己知道)、零配置成本;劣势:黑盒(人看不到编排过程,审计干预都难)、编排逻辑随模型版本变化不可控。
- **产品化派**——优势:人可见可控(干预/审计/责任归属清晰)、编排资产可沉淀;劣势:静态编排跟不上任务多样性,沦为"人肉 RL"。
- **融合的合理猜想**——模型管微编排(单任务内多路探索),产品管宏编排(跨任务的责任边界与审批):类似 OS 内核调度与 k8s 作业编排的分层;判据是人需要在哪一层干预。
- **批判点**——两家都有商业动机夸大自己的层;判断标准回到"审计与干预需求在哪"。

**Q5.17【开放·L5·架构】并行实验中涌现的自发专业角色(去重 agent、性能 agent、评审 agent)——可依赖的工程性质还是不可复现的运气?怎么让自组织可复现?**

讨论框架:
- **涌现的三个前提**——强 verifier(做得对不对有客观信号)、任务可自选(没有中心调度强加)、低协调成本(锁文件即可认领):三者齐备,专业化分工是压力选择的自然结果,有系统科学解释,不纯是运气。
- **复现手段**——初始条件标准化(环境/任务池/规则固定);**角色涌现后固化为显式 agent 定义**(下一轮直接用)——这是"从涌现到设计"的关键一步;多次运行统计方差(单次涌现的分工可能漂移)。
- **风险**——从众性证明"自组织"也会自我强化错误模式:同质化的 30 个 agent 是羊群不是分工;需要多样性机制(不同初始上下文/探索策略)。
- **判断**——短期当实验现象观察,别把系统可运行性押在涌现上;长期把涌现出的模式沉淀为显式设计。

**Q5.18【开放·L4·管理】组织采纳多 agent 时,怎么防止"为炫技而上"?**

讨论框架:
- **立项评审清单流程化**(Q5.7 四句):答不上来打回——成本最低的治理。
- **用 eval 驱动架构决策**——单 agent baseline 和多 agent 方案在内部任务集同跑:数据说话,"并行快 3 倍但成功率掉 15%"这类结果自动杀死不合理拆分。
- **复盘制度化**——上线后按月复盘 token/质量/延迟三曲线;劣化归因(设计/协调/验证)反哺评审清单。
- **升级门槛 checklist 化**——从单到多的可量化条件(信息量证据、隔离方案、验收方案)写成 checklist:把"想上"变成"证明该上"。

---

## 5.5 工程实战题

**Q5.19【实战·L3·平台】用 subagent 设计"PR 深度 review"流水线:主 agent 只做综合,三个 subagent 分别看正确性/安全性/测试覆盖。**

参考思路:
- **subagent 侧**——只读工具白名单(Explore 类);独立 system prompt(各自关注点+输出 schema:发现/严重度/证据 file:line/置信度);结果以结构化摘要回传(1–2K)。
- **主 agent 侧**——分派时给 diff+上下文指引(不是"看看这个 PR"而是"这个 PR 改了支付重试逻辑,重点看 X");综合去重;冲突结论的裁决规则(按严重度排序或升级人工)。
- **防坑三件**——任务描述自包含(subagent 看不到主对话历史);description 简短(常驻成本);并行 3 个够(再多是评审委员会不是 review)。
- **决定质量的细节**——reviewer 与 coder 用不同模型(打破同权重盲区,Q5.11):这个细节是流水线质量的决定项之一,答出来就是加分。
- **及格线 / 加分 / 红旗**:及格=三分工+自包含+结构化摘要;加分=裁决规则与模型独立性;红旗=subagent 返回自由文本没有 schema。

**Q5.20【实战·L4·架构】设计团队的并行 agent 开发工作流:5 个 agent 同时开发 5 个特性,汇入一个主干。**

参考思路:
- **隔离**——每 agent 独立 worktree+分支+独立构建环境(ch04 Q4.20 的执行环境层是地基)。
- **认领与所有权**——共享任务看板+**文件级所有权声明**(改哪些模块):两个 agent 改同一文件必然互相覆盖,所有权开工前显式划分+冲突预检。
- **集成**——串行合并窗口(按依赖顺序)→本地全测→合入→通知下一个 rebase;或 stacking(每个 PR 基于前一个)。**合并冲突 agent 能自己解,但解完必须全量测试——冲突解决是最容易"看起来对了"的环节**。
- **验证与兜底**——每个 diff 过独立 reviewer agent;主干 CI 是最终 verifier;失败任务现场保留、人工接管协议、预算与轮次上限、夜间熔断。
- **及格线 / 加分 / 红旗**:及格=隔离+所有权+串行合并;加分=冲突解决后强制全测;红旗=5 个 agent 共享一个分支"互相协调"。

**Q5.21【实战·L3·平台】实现 lead agent 的任务分派 prompt 模板,要求 subagent 无共享记忆也能正确执行。**

参考思路(七件套模板):
- **目标一句话**(做什么、做到什么程度);**边界**(做什么/绝不做什么);**输入材料**(具体文件路径/issue 链接——不是"仓库里找找");**输出格式**(schema:结论/证据/置信度/未解决问题);**工具指引**(该用什么不该用什么);**停止条件**(预算/轮次/完成判定);**失败报告格式**(做不完时回报什么)。
- **心法**——当成给新入职同事的交接文档写:他聪明但对你的一切上下文一无所知。
- **反面教材**——派"调研一下认证模块":没有边界没有输出格式,subagent 的摘要大概率不含你要的那个信息,主 agent 还得追问一轮(又烧一遍 token)。
- **及格线 / 加分 / 红旗**:及格=七件套给五件;加分=有迭代教训(哪次派发翻车后加了哪一条);红旗=一句"帮我看看 X"。

---

## 5.6 场景排错题

**Q5.22【排错·L4·平台】3 个并行 subagent 跑完,主 agent 的综合结论明显忽略了其中一个的关键发现。排查?**

排查链路:
- **①摘要丢失(第一怀疑)**——该发现没进 1–2K 摘要:修输出 schema,强制"关键发现必列"字段——最常见的根因。
- **②位置效应**——lost in the middle:中间回来的报告被忽视(U 型曲线在多 agent 汇总场景同样生效);调整汇报顺序/结构化注入。
- **③综合 prompt 缺取舍标准**——主 agent 不知道"三个发现冲突听谁的":给裁决规则。
- **④静默截断**——并发结果超上下文被截:检查汇总前的 token 预算。
- **修复模式**——综合阶段用结构化输入(每个 subagent 结论独立编号),必要时二次追问("对 subagent-2 的 X 你如何处理?")——编号+追问是通用修复,先上再归因。
- **及格线 / 加分 / 红旗**:及格=①②两条;加分=编号+追问的修复模式;红旗=归因"主模型不够聪明"换更大的模型。

**Q5.23【排错·L4·平台】两个 teammate 各自"完成"了任务,合并时发现互相覆盖,且都声称测试通过。根因与流程修复?**

排查链路:
- **根因机制**——共享工作区或同文件所有权未划分,后写覆盖先写;"各自测试通过"因为在各自时点各自为真——**不是谁说谎,是时间线错位**,这个机制讲清就不难修。
- **流程修复**——文件所有权/模块划分显式化(开工前声明+冲突预检);写前 rebase/pull(让"过时基线上的绿灯"不可能);合并窗口串行化;diff 级一致性检查进 Stop-gate(完成前检查工作区与远端一致)。
- **长期**——把"两个 teammate 改同一文件必然互相覆盖"变成 lint 规则/准入检查:教训固化为工具,不是 wiki 里的一句提醒。
- **及格线 / 加分 / 红旗**:及格=所有权+串行合并;加分=写前 rebase 的机制解释;红旗=归因某个 agent"有 bug"。

**Q5.24【排错·L3·架构】multi-agent 上线后成本是单 agent 的 25 倍(预期 15 倍),质量只升 5%。诊断?**

排查链路:
- **四条浪费机制逐个查**——①重复劳动(多 subagent 搜同一信息:任务边界重叠→加去重/缓存/边界审计);②过度委派(lead 把自己能干的也派出去→分派判据写进 lead prompt);③返回冗长(没按摘要规格→schema 强制+超长打回);④协调风暴(轮询/同步风暴→改事件驱动/批量同步)。
- **质量只升 5% 的真正原因(本题灵魂)**——瓶颈根本不在并行度,在任务定义或验证器(MAST:多数失败在规格层)——加多少 agent 都没用,先修 spec。
- **决策点**——诊断出后者后,正确的动作可能是**回退到单 agent**:多 agent 是手段不是目标——这个说得出口的团队不多,敢说就是加分。
- **及格线 / 加分 / 红旗**:及格=四条浪费机制;加分=识别"瓶颈不在并行度"并敢回退;红旗=继续调优并行度或加更多 agent。

---

## 5.7 开放加餐:偏好与品味追问

**Q5.25【开放·L4·架构】你自己什么时候真的开多 agent?分界线在哪?说一次真香和一次翻车的经历。**

参考思路(一个好答案长什么样):
- **分界线的三个判据**——子任务之间上下文独立(不共享中间状态)、可并行(没有顺序依赖)、结果可聚合(能机械合并):**三个都满足才值得开,否则单线程更稳**。
- **真香的形态**——批量同构任务(给 50 个文件做同一种迁移)、多方案并行探索后择优:吞吐收益真实存在。
- **翻车的形态**——共享状态的协调成本(两个 agent 改同一处打架)、结果趋同(没有独立性,开了等于没开)、聚合失败(五个方案五种风格,合并比重写贵):翻过车才知道"多"不是默认选项。
- **对争论有站队**——单线程派 vs 团队派(本章的辩论题)自己站哪边、什么条件下换边:**品味题要的是立场加条件,不是骑墙**。
- **及格线 / 加分 / 红旗**:及格=三判据+一个真实经历;加分=真香和翻车都有+有站队;红旗=因为"多 agent 高级"而用、从没手动拆过任务。
