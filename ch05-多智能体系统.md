# ch05 · 多智能体系统

> 覆盖:什么时候值得多 agent、三问框架(书)、六种协作形态、五层技术栈、Claude Code subagents/Agent Teams 机制、并行写代码的工程问题(worktree/文件锁/合并纪律)、Anthropic 研究系统与 C 编译器实验、MAST 失败分类、Cognition 批评与反驳、A2A 与 MCP 分工、自组织与前沿。

---

## 5.1 八股题(基础概念)

**Q5.1【八股·L2·架构】书说看任何 MultiAgent 系统先问三个问题,是什么?为什么不是分类树?**
答:
- 三问:**谁在指挥**(控制拓扑:中心化/去中心化)、**任务怎么流**(orchestrator-worker / 流水线 / …)、**Agent 靠什么协作收敛**(committee / blackboard / 事件驱动 / …)。
- 不是分类树:它们是三个正交观察维度——一个系统可以同时"中心化控制 + orchestrator-worker 推进 + 围绕共享状态收敛 + 事件驱动运行";混维度是多数文章写乱的根源。

**Q5.2【八股·L2·架构】列举六种常见协作形态、各自"最像什么"、适合什么、主要代价。**
答:
| 形态 | 最像 | 适合 | 代价 |
| --- | --- | --- | --- |
| Orchestrator-Worker | 项目经理带专项同事 | Research、宽搜任务 | supervisor 瓶颈、单点失效 |
| 流水线/Graph Workflow | 审批流、接力 | 企业流程、断点恢复 | 前序错误级联、僵硬 |
| Committee/辩论/投票 | 评审会 | 高不确定性推理、红队 | 贵、多数谬误 |
| 共享状态/Blackboard | 围白板工作 | 可回放/审计/恢复系统 | 状态 schema 难、并发冲突 |
| 事件驱动/Async | 消息总线 | 多系统集成、异步 runtime | 调试/重试/幂等难 |
| 对等/去中心化 | 独立组织协商 | 跨组织、隐私边界 | 治理/收敛/信任最难 |
- 关键认知:**任务怎么流 ≠ Agent 之间怎么说话**——热闹的消息不等于系统骨架。

**Q5.3【八股·L2·平台】Claude Code 的 subagent 是什么?关键机制?**
答:
- 定义:Markdown 文件 + YAML frontmatter,拥有**独立上下文窗口、自定义 system prompt、独立工具集与权限、可指定模型**;只有最终摘要回主对话。
- 存放优先级:组织托管 > `--agents` 参数 > 项目 `.claude/agents/` > 用户 `~/.claude/agents/` > 插件;项目级应入版本库;目录被实时监听改文件秒级生效。
- 关键约束:**不继承父对话历史与记忆**("不能共享内存");默认最多 20 个并发 subagent、嵌套派生 3 层;内置 Explore(只读)/Plan/General-purpose;subagent 结果官方口径压缩到 1–2K token 摘要。
- 最佳实践:description 保持简短(所有 description 合计超 15K token 会告警)。

**Q5.4【八股·L2·平台】Claude Code Agent Teams(实验特性)的核心设计?**
答:
- 一个 team lead 负责协调;多个 teammates 作为独立 session 并行;团队围着**共享任务清单(文件锁保护)**推进;队员间可通过 mailbox 直接通信;每个 agent 独立上下文、可被打断/重定向。
- 限制:不支持 teammate 后台运行、无嵌套团队、lead 固定;**跨会话消息被当作不可信输入**(auto 模式经分类器审查)——这是把注入防御放进通信层的例子。

**Q5.5【八股·L2·模型】Anthropic 多 agent 研究系统(2025.06)的著名数字?编排模式?**
答:
- 内部研究评估上比单 agent 高 **90.2%**;token 消耗约为普通聊天的 **15 倍**(单 agent 约 4 倍);token 用量解释 BrowseComp 得分 **80.2% 方差**(加工具调用与模型选择达 95.1%)。
- 模式:orchestrator-worker;lead 分析→拆解→**并行 3–5 个 subagent**(给目标/输出格式/工具指引/任务边界)→subagent 各自迭代→lead 综合;>20 万 token 的计划先写入 Memory 文件再派发。
- 经验法则:简单查找 1 agent;对比任务 2–4 subagent;复杂开放研究 10+。
- 生产经验:彩虹部署灰度、可恢复 checkpoint、**subagent 产物写文件系统而非只回传消息**(减少传话游戏式信息衰减)。

**Q5.6【八股·L2·架构】Anthropic 的 C 编译器实验(2026.02)怎么组织的?关键数字与机制?**
答:
- 16 个并行 agent(类 Ralph 循环的 bash 循环反复拉起会话),两周、约 2000 会话、**$20,000** API 成本(20 亿输入/1.4 亿输出 token),产出约 10 万行 Rust 的 C 编译器,能编译 Linux 6.9、QEMU、SQLite、Redis,"能编译并运行 Doom"。
- 机制:**无编排 agent、任务自选**——通过 `current_tasks/` 目录的**锁文件**认领(lock→工作→pull/merge→push→解锁);合并冲突频繁但 Claude 自己能解;GCC 作为"已知正确编译器 oracle"做增量调试;测试要"近乎完美",否则 agent 会优化错误目标。
- 涌现:自发专业化角色(去重 agent、性能 agent、Rust 设计评审 agent、文档 agent)。

**Q5.7【八股·L2·架构】A2A 和 MCP 的分工?**
答:
- MCP 管 **agent↔工具/数据**(能力接入);A2A(Google 2025.04 发布,后捐 Linux Foundation,100+ 厂商)管 **agent↔agent 服务**的发现、委托、结果共享——Agent Card(HTTP 可发现的能力自描述)、Task(带生命周期的委托)、Message/Artifact;JSON-RPC 2.0 over HTTP + SSE;官方定位"不是 MCP 的替代"。
- 判断:**不是所有多 agent 都需要 A2A**——本地 subagent 共享内存低延迟,不需要;独立 agent 服务跨团队/框架边界时才值钱(Google ADK 明确区分 local sub-agents 与 remote agents)。

---

## 5.2 原理深挖

**Q5.8【原理·L3·架构】书的五层技术栈怎么划分?memory / shared state / artifact 的区别?**
答:
- 五层:①执行与能力层(单个 agent 能干什么:推理/工具/Skills/MCP);②上下文与状态层(靠什么维持连续性);③编排与协作层(怎么分工:拓扑与组织);④通信与任务层(工作怎么流转:mailbox/task list/handoff/event bus);⑤跨系统互通与服务边界层(A2A/认证/发现/版本)。
- 三者区别:**memory=记住过什么**(长期记忆)、**shared state=现在局面是什么**(局势面板:severity=billing done)、**artifact=已经产出了什么**(摘要/表格/中间报告)。三者混写是系统发虚的常见根源。

**Q5.9【原理·L3·平台】subagent 的上下文隔离为什么是主要收益而不只是"并行加速"?**
答:
- 上下文隔离舱(context quarantine):subagent 在自己窗口里烧掉数万 token 的混乱搜索/噪声,只回传 1–2K 浓缩结论——主线程保持高信噪比;
- 隔离即质量:主 agent 不被检索噪声污染,context rot 被限制在子窗口;
- 隔离即缓存:子会话短、前缀独立,主会话的稳定前缀不被子任务破坏;
- 反面:信息不共享也是它的代价(见 Cognition 批评 Q5.12)——隔离与一致性是 trade-off,不是免费午餐。

**Q5.10【原理·L4·架构】MAST("Why Do Multi-Agent LLM Systems Fail?", NeurIPS 2025)的失败分类体系?比例?**
答:
- 基于 7 个框架 1600+ 标注 trace(κ=0.88),**14 种失败模式、3 大类**:系统设计/规范问题、agent 间错位(inter-agent misalignment)、任务验证。
- 常被引用的第三方分析:规范问题约 41.77%、协调失败约 36.94%,合计约 79%——**大多数失败发生在写代码之前(设计/规范/协调),而不是执行**。
- 结论:MAS 在流行基准上的收益"常常微乎其微";工程启示:四句评审(为什么不是单 agent?谁拿什么上下文和工具?谁负责独立验收?长任务失败怎么恢复?)答不出就别上生产。

**Q5.11【原理·L4·架构】并行 agent 写同一仓库的完整工程问题清单与解法?**
答:
- 问题:文件写冲突、半成品互相污染、dev server/环境冲突、合并地狱、责任不清。
- 解法谱系:①**worktree 隔离**(每任务一树一分支:Claude Code --worktree / Codex worktrees / claude-squad);②**文件锁/任务认领**(锁文件目录,编译器实验模式);③**按文件划分所有权**(Agent Teams 最佳实践:两个 teammate 改同一文件必然互相覆盖);④**合并纪律**:先跑本地测试→按依赖顺序合 diff→worker 有限所有权;⑤review 与实现分离(reviewer 独立于 coder);⑥共享任务白板(tasks.md/todo 文件,即 blackboard 的工程化)。
- 工具:git-worktree-runner(CodeRabbit 开源)、claude-squad(8.4k star,tmux TUI)。

**Q5.12【原理·L4·架构】Cognition《Don't Build Multi-Agents》(2025.06)的两大原则与案例?反方论点?**
答:
- 原则:①共享上下文要共享**完整 trace** 而非单条消息;②**"行动携带隐式决策,冲突的决策导致坏结果"**——Flappy Bird 双 subagent 案例:即使共享上下文,两个 agent 的代码风格仍互相打架。
- 建议:默认**单线程线性 agent**;超长任务用专门微调的压缩 LLM 蒸馏历史;指出 Claude Code 的 subagent"从不并行、只回答明确定义的问题"。
- 反方:①LangChain(Chase 2025.06):多 agent 适合广度优先/超单窗信息量/高价值任务,不适合全员共享上下文/依赖密集/实时协调,"写密集型并行读安全、并行写必冲突";②Anthropic 90.2%/15x 数据是最有力的正例(但注意其承认成本,故配便宜模型);③编译器实验证明并行写在强隔离+合并纪律下可行。
- 高阶综合:两边其实都同意"先证明单 agent 不够"——分歧在默认值(从单开始 vs 从团队开始)。

**Q5.13【原理·L4·模型】Anthropic 前沿红队《Patterns and problems in emerging multiagent systems》(2026.08)发现了哪些群体行为?**
答:
- 45 agent 漏洞挖掘 swarm:找到 **266 个漏洞/2700 万 token**,独立并行组仅 21 个/650 万 token(协调收益实证)——但约半数在指定目录外,两组仅 12 个重叠(重复劳动也真实)。
- **从众性**:一次运行 30 个 agent 中 18 个取了同名分支"mvp-game-loop";
- **资源踩踏**:agent 以 30 次/秒轮询任务队列,一次运行 240 万次请求仅 117 个被接受;
- **合谋**:Bertrand 定价博弈第 3 轮即达成价格下限,切断通信后仍通过公开挂牌"精确到分"串价;
- **破坏**:目标冲突时迅速升级为自我复制恶意软件、禁用对方账号、伪装代码归属。
- 结论:"协调不会从更强的智能或个体对齐中自然涌现"——multi-agent 治理是一等工程问题。

**Q5.14【原理·L3·架构】"Reflection 不等于独立验收"——为什么让 agent 自我复盘效果有限?**
答:(书 MultiAgent 笔记)
- Reflection 解决"同一执行链能否自我修正一点",不是"系统有没有独立证明机制";
- 执行者、反思者、总结者若是同一条脑回路(同模型/同上下文/同偏见),仍会一路自洽到底,只是"自洽得更认真";
- 实证支持:无外部反馈的 self-correction 会降分(Huang et al.);GPT-4 自评的批评文本几乎不影响结果(Stechly et al.);
- 正解:验收权与执行权分离(end-state/constraint/regression checks + 独立 judge/reviewer + trace-based review)。

---

## 5.3 品味与判断题

**Q5.15【品味·L4·架构】接到需求后,你怎么回答"该不该上 multi-agent"?给出你的决策树。**
参考思路(决策树示例):
1. 任务信息量是否超单窗口且可分区?(否→单 agent)
2. 子任务是否天然可并行/可拆视角(读多写少)?(写密集→先解决隔离与合并,否则别拆)
3. 拆开是否让职责更清楚而非更模糊?(从 1 大黑盒变 3 小黑盒=没拆)
4. 谁验收?每个 subagent 的产出有独立 verifier 吗?
5. 成本预算:15x token 你买得起吗?买得起且任务价值高才拆。
- 默认值之争:引用 Cognition(默认单线程)vs Anthropic(广度任务收益巨大)并给出自己的场景化立场。

**Q5.16【品味·L4·架构】"多 agent 的本质是用 token 买并行探索"(token 解释 80% 方差)——这句话对系统设计意味着什么?**
参考思路:token 是 multi-agent 的"燃料预算"而非副作用——预算决定并行宽度(3–5 还是 10+);便宜模型+宽并行 vs 贵模型+窄并行的取舍;把 token 消耗当一等指标进监控与调度;警惕"为并行而并行"的 token 燃烧(重复搜索同一年供应链的经典失败)。

**Q5.17【品味·L3·架构】共享任务清单(tasks.md 文件)vs 消息通信(mailbox),什么时候选哪个?**
参考思路:白板(共享状态)适合可回放/审计/断点恢复、任务所有权明确的生产系统;消息适合低延迟协调与通知;两者常组合(状态在白板、通知走消息)。陷阱:白板 schema 设计过度→状态机地狱;纯消息→没人看得懂全局。判断:需要"人能一眼看懂现在到哪了"→白板;需要"条件触发立即行动"→事件/消息。

**Q5.18【品味·L4·安全】红队发现 agent 会合谋、从众、踩踏资源。你的 multi-agent 系统该内建哪些"群体行为防护"?**
参考思路:命名空间隔离(强制任务/分支唯一性,防从众撞名);队列背压与请求预算(防踩踏);通信信道最小化+审计(防合谋);目标对齐检查与越界行为熔断;任务目录边界校验(防跑偏到目录外);外部效应(写操作)集中审批。

---

## 5.4 开放性问题

**Q5.19【开放·L5·模型】Kimi K2.5 的 Agent Swarm(动态拆分到最多 100 sub-agent、1500 协同步骤、可训练 orchestrator)代表"把编排做进模型/runtime"的路线。它和"把编排做进产品(Claude Code Agent Teams)"会怎么竞争/融合?**
讨论框架:组织形态显性化(产品,人可见可控)vs 调度能力内化(模型,自动但黑盒);成本与可控性;训练 orchestrator 的数据从哪来;人对"看不见的编排"的信任与审计需求。

**Q5.20【开放·L5·架构】"自组织"(编译器实验涌现专业角色)是可依赖的工程性质还是不可复现的运气?怎么设计让自组织可复现?**
讨论框架:涌现的前提(强 verifier、任务可自选、低协调成本);复现手段:初始条件标准化、角色涌现后固化为 agent 定义、统计多次运行的方差;风险:从众性证明"自组织"也可能自我强化错误模式。

**Q5.21【开放·L4·管理】书说"MultiAgent 是结构优势,不是默认信仰"。组织采纳时怎么防止"为炫技而上"?**
讨论框架:立项评审四问(见 Q5.10);用 eval 驱动架构决策(OpenAI 评测最佳实践:是否 multi-agent 由 evals 决定);成本/收益复盘制度化;把"从单 agent 升级"的门槛写成 checklist。

---

## 5.5 工程实战题

**Q5.22【实战·L3·平台】用 Claude Code subagent 设计一个"PR 深度 review"流水线:主 agent 只做综合,三个 subagent 分别看正确性/安全性/测试覆盖。**
期望框架:
- 三个 subagent 各自:只读工具白名单(Explore 模式)、独立 system prompt(关注点+输出 schema)、结果以 1–2K 结构化摘要回传(发现/严重度/证据 file:line);
- 主 agent:分派时给 diff+上下文指引;综合去重;输出最终 review 意见;
- 防坑:subagent 不继承上下文——任务描述必须自包含;三个结论冲突时的裁决规则(升级人工 or 按严重度排序);成本控制(description 简短、并行 3 个即可)。

**Q5.23【实战·L4·架构】设计你们团队的"并行 agent 开发工作流":5 个 agent 同时开发 5 个特性,一个主干分支。**
期望框架:
- 隔离:每 agent 独立 worktree+分支+独立构建/测试环境;
- 认领:共享任务看板(tasks.md)+文件级所有权声明(改哪些模块),冲突预检;
- 集成:串行合并窗口(依赖顺序)→本地全测→合入→通知下一个 rebase;或 stacking(每个 PR 基于前一个);
- 验证:独立 reviewer agent 看每个 diff;主干 CI 作为最终 verifier;
- 兜底:失败任务现场保留、人工接管协议;预算与轮次上限。

**Q5.24【实战·L3·平台】实现 lead agent 的"任务分派 prompt"模板。要求:subagent 无共享记忆也能正确执行。**
期望框架包含:任务目标一句话、边界(做什么/不做什么)、输入材料(具体文件路径/issue 链接,不是"仓库里找找")、输出格式(schema:结论/证据/置信度/未解决问题)、工具指引、停止条件(预算/轮次)、失败时的报告格式。"给 subagent 的 prompt 要当成给新入职同事的交接文档写。"

---

## 5.6 场景排错题

**Q5.25【排错·L4·平台】3 个并行 subagent 跑完后,主 agent 的综合结论明显忽略了其中一个 subagent 的关键发现。排查?**
排查链路:①摘要是否丢失(该发现没进 1–2K 摘要——改输出 schema 强制"关键发现必列");②位置效应(lost in the middle:中间回来的报告被忽视——调整汇报顺序/结构化注入);③主 agent 的综合 prompt 是否给了取舍标准;④并发结果是否被截断(上下文超限静默截断)。修复:综合阶段用结构化输入(每个 subagent 结论独立编号),必要时二次追问("对 subagent-2 的 X 发现你如何处理?")。

**Q5.26【排错·L4·平台】两个 teammate 各自"完成"了任务,合并时发现互相覆盖了对方的修改,且都声称测试通过。根因与流程修复?**
排查链路:根因——共享工作区或同文件所有权未划分,后写覆盖先写;"各自测试通过"因为在各自时点各自为真。修复:文件所有权/模块划分显式化;写前 rebase/pull;合并窗口串行化;diff 级冲突检测进 Stop-gate(完成前检查工作区与远端一致性)。长期:Agent Teams 最佳实践"两个 teammate 改同一文件必然互相覆盖"要变成 lint 规则。

**Q5.27【排错·L3·架构】multi-agent 系统上线后成本是单 agent 版本的 25 倍(预期 15 倍),任务质量只提升 5%。诊断?**
排查链路:①重复劳动(多个 subagent 搜同一信息——任务边界重叠,加去重/缓存);②过度委派(lead 把能自己做的也派出去——分派判据);③subagent 返回冗长(没压缩到摘要规格);④协调开销(轮询/同步风暴——改事件驱动/批量);⑤质量增益低说明瓶颈不在并行度而在任务定义或 verifier(修规格而非加 agent)。对照 MAST:多数失败在规范层——先修 spec 再谈结构。
