# ch03 · 工具调用、MCP 与 Agent 检索

> 覆盖:function calling 机制、工具 schema 设计方法论、工具过载、MCP 协议(原语/传输/治理/安全)、CLI vs MCP vs code execution、结构化输出,以及 **coding agent 的检索实现**:为什么用 ripgrep 而不是 embedding、repo map、websearch 工具的上下文防污染、agent 怎么自主收窄搜索。
> 案例基线:2026-09。工具产品的具体形态随版本变化,机制与设计逻辑是考点。
> 答案读法:**加粗句连读就是可背的骨架**;正文按工程师语域写——通用工程词(端点、schema、鉴权、沙箱)直接用,**领域黑话(function calling、tool poisoning、constrained decoding 这类)随用随释**,只在括号里给一句解释;**宁可写长讲透,不压缩到失真**。
> 术语白话速查:**tool calling/function calling**=模型输出"我要调用某工具+参数"让程序执行;**JSON**=程序间传数据的标准文本格式;**JSON Schema**=描述 JSON 该长什么样的格式说明书;**MCP**=给 AI 用的标准化工具插座协议;**MCP server**=把某系统能力按 MCP 包装好的一端;**限流**=对方限制你的调用频率,超了报错;**幂等**=同一操作重复执行结果不变;**code execution**=让模型写代码并真的运行;**grep/ripgrep**=按文本内容搜文件的工具(ripgrep 是更快的新秀);**glob**=按文件名模式找文件(如 *.py);**工具描述**=工具的说明书,模型照它决定怎么用;**工具过载**=工具太多导致模型选错。

---

## 3.1 八股题(基础概念)

**Q3.1【八股·L1·平台】function calling 的标准循环是什么?"工具定义会被计费"对工具设计有什么影响?**

- **人话版**:五步——带 tools 定义发请求→模型返回"我要调某工具+参数"→你的代码执行→结果按 id 回传→模型继续或结束。工具定义每轮随上下文发送、按输入 token 计费——工具是每轮付租金的上下文占用,不是免费清单。
- **主线(照这条线讲)**:
  1. **循环细节讲全**——模型的调用请求带 call id;必须按 id 回传对应 tool_result(并行调用时多个 id 各自配对);一轮可发起多个调用;`tool_choice` 控制倾向(auto 自决/required 必调/指定某工具/none)。
  2. **计费机制展开**——一个工具定义几百到上千 token,挂 50 个就是几万 token 的常驻租金;Anthropic 文档里多 MCP 场景未优化先吃约 5.5 万 token 的实例就是账单版证明。
  3. **由此推出三条设计纪律**——工具数量克制(核心 ≤20);描述要高信息密度(不是省字数是省 token);低频工具延迟加载(见 Q3.6)。
  4. **补上错误回传这个设计点**——工具失败返回 is_error 标记+可读错误说明,模型据此调整;给裸 traceback 它只会瞎重试。"错误是给模型看的 UI"这个视角贯穿 ch03。
- **补充弹药(追问再掏)**:
  - **两家差异**:Anthropic 用 content block(tool_use/tool_result 按 id 配对),OpenAI 用 tool_calls 字段——机制同构,格式不同。
  - **演进**:2023 function calling→parallel calls→2024 Structured Outputs(constrained decoding),Q3.3 展开。
- **及格线**:五步循环+计费影响。
- **加分**:并行调用按 id 配对;错误回传设计。
- **红旗**:以为工具定义"用到才计费"。

**Q3.2【八股·L2·平台】MCP(Model Context Protocol)解决什么问题?核心概念(host/client/server)和原语是什么?**

- **人话版**:MCP 解决 N×M 集成——M 个应用接 N 个数据源原本写 M×N 个连接器,标准化后 M+N。host 里跑多个 client,每个 client 连一个 server;server 提供 tools/resources/prompts,也可以反向请求 sampling(借 host 的模型)或 elicitation(向用户追问)。
- **主线(照这条线讲)**:
  1. **先把 N×M 讲成真问题**——agent 要接 GitHub、Slack、数据库、监控,每个 host 各写一遍集成,重复且质量参差;协议标准化后 server 写一次所有 host 受益——"AI 应用的 USB-C"说的就是网络效应,不是技术先进性。
  2. **安全边界是设计精髓**——server 不能读整个对话、看不见其他 server,每连接 1:1 隔离。这既是权限模型的地基,也是分析 MCP 攻击面时的基准线(哪些能力越过了这个边界,哪些攻击利用了这个边界)。
  3. **五个原语各一句**——tools(模型可调的动作,主力)、resources(可读数据,用得少)、prompts(模板,用得少)、sampling(server 反向借用 host 模型——危险能力,要用户批准)、elicitation(server 向用户追问)。**评估一个 server 的攻击面,先把这五个过一遍**——sampling 被滥用就是数据外泄通道。
- **补充弹药(追问再掏)**:
  - **演进方向**:协议 2025 年起转向无状态化;传输层从 stdio+SSE 走向 Streamable HTTP;治理已交给中立基金会——协议本身在快速迭代,别把架构焊死在某个版本上。
  - **采纳面**:OpenAI/Google/微软先后支持,主流工具全接——已经是事实标准而非 Anthropic 私有物。
- **及格线**:N×M+三原语。
- **加分**:client/server 隔离的安全含义;sampling 的危险性。
- **红旗**:只会"USB-C"比喻说不出机制。

**Q3.3【八股·L2·平台】JSON mode 和 Structured Outputs(constrained decoding)的区别?为什么 agent 系统必须要后者级别的可靠性?**

- **人话版**:JSON mode 只保证"是合法 JSON",不保证符合 schema——复杂 schema 下 compliance 常只有八九成;Structured Outputs 用约束解码,采样阶段就只允许合法 token,100% 符合。
- **主线(照这条线讲)**:
  1. **机制差异是关键**——JSON mode 是"提示+软校验":模型大概率输出合法 JSON,但字段缺失、类型错误、枚举拼错都不拦;Structured Outputs 是约束解码:把 JSON Schema 编译成状态机,生成时动态屏蔽不合法 token——不是"生成完再校验",是**物理上生成不出非法输出**。代价是 schema 要满足条件(字段全 required、禁 additionalProperties——没法直接表达"这个字段可有可无",得用枚举含 null 绕)、首次请求有编译开销(结果可缓存)。
  2. **用累积概率算给面试官听**——agent 每一环(工具参数解析/路由/状态转移/评测打分)都依赖机器可读输出。单次 99% compliance,30 步循环的累积成功率是 0.99³⁰≈74%;单环只有 95% 的话,30 步后只剩 21%。一个坏 JSON 报废的不只是这一步:前面烧掉的 token、已执行的副作用、失败后的重试全算在它头上——这就是"必须要"的数学。**下游消费方越多、链路越长,越要把单点可靠性顶到物理上限**。
  3. **划清适用边界**——给人看的自由文本不需要锁(锁了反而降质量);机器消费的分类、抽取、工具参数必须锁。全锁死是浪费,全不锁是赌博。中间态:自由生成+事后解析+失败重试,适合低频调用;高频链路上的每个节点都该上约束解码。
- **补充弹药(追问再掏)**:
  - **实测参照**:复杂 schema 下旧模型 JSON mode 常低于 40%(官方口径),提示工程救不回来——这是该功能存在的直接动机。
  - **变体**:各家叫法不同(schema enforcement/strict mode),判别标准就一条——是"校验后拒绝"还是"生成时约束"。
  - **排错联动**:线上突然出现坏 JSON,先查是不是走了非 strict 的旧路径(灰度/回滚常把 strict 配置丢掉)——坏 JSON 的出现位置本身就是配置漂移的信号。
- **及格线**:约束解码原理+累积放大效应。
- **加分**:算得出累积成功率;知道 schema 限制条件。
- **红旗**:认为"prompt 里写清楚格式"够用。

**Q3.4【八股·L2·模型】temperature=0 就完全可复现了吗?采样参数在 agent 里的正确用法?**

- **人话版**:不。批处理调度、浮点顺序、路由、硬件差异都引入波动,seed 也只是 best-effort。新趋势是厂商直接弃用采样参数(只接受 1.0)——可控性从采样转移到约束解码和工具设计。
- **主线(照这条线讲)**:
  1. **为什么 0 也不确定**——采样只是随机性的一个来源:推理集群把多个请求拼 batch 提高利用率,批内组合每轮不同,浮点非结合运算的求和顺序一变,结果的微小差异就足以改变 argmax 的选择;请求路由到不同硬件(不同 GPU 型号/内核版本),数值行为不保证逐 bit 一致;服务端权重静默更新更是常事。**temperature=0 消掉的只是"采样随机性"这一层,消不掉"系统随机性"**。
  2. **agent 里的正确用法**——要"对"的任务(工具选择/参数抽取/代码编辑)低温度或不调;要"多"的任务(候选生成/头脑风暴/探索式规划)高温度,再由验证器筛。判据始终是:这个环节的错误由谁兜底——有验证器兜底的可以放开,没有的必须收紧。
  3. **趋势及其含义**——Anthropic 在新模型上弃用 temperature/top_p(只接受 1.0):训练和推理协议已把行为稳定性做进权重,采样旋钮的边际价值消失。**别把调温度当稳定性方案;稳定性靠验证器和重试策略**——这是从"祈祷式确定性"到"工程化确定性"的转移,和约束解码是同一潮流的两面。
- **补充弹药(追问再掏)**:
  - **可复现的正确姿势**:固定验证器做判定+多次采样的统计报告,而不是追求逐次相同输出——测"分布的稳定性"比测"单次的复现"更诚实。
  - **system_fingerprint 类字段**:观察模型侧静默更新的线索(排错实用,ch07)。
  - **迁移坑**:从支持 temperature 的模型迁移到弃用的模型,行为差异常被误诊为"新模型更差"——其实是以前靠低温压住的发散,现在要靠 prompt 和工具设计管。
- **及格线**:0≠确定的机制+一句正确用法。
- **加分**:弃用趋势及其含义。
- **红旗**:用 temperature=0 设计"确定性测试"。

**Q3.5【八股·L2·平台】"code execution as action"是什么主张?与"每个 API 包成工具"相比,优势和代价各是什么?**

- **人话版**:与其塞一百个工具定义让模型一个个调、中间结果反复流经模型,不如让 agent 直接写代码调这些 API——数据和控制留在执行环境,模型只看最终结果。代价:要提供带资源限制的沙箱。
- **主线(照这条线讲)**:
  1. **机制优势两条**——控制流:代码有循环/条件/错误处理,"取数→过滤→聚合→输出"一条程序搞定,不再一轮轮模型往返(每次往返=一次全上下文推理,又慢又贵);上下文:中间数据(一万行表格)留在执行环境,模型只看过滤后的 5 行——**这是上下文管理的降维打击**:ch02 那些压缩/清理手段,在"根本不进来"面前都是次要优化。
  2. **量化锚点**——Anthropic 例子:工具定义从 15 万 token 降到 2 千(100+ 个细粒度工具变成"一个代码执行环境+按需读 API 文档");programmatic tool calling 让复杂研究任务平均 token 省 37%(2025-11 官方数据)。两个数字分别打"定义占用"和"往返开销"两头。
  3. **代价三件**——沙箱成为硬依赖:执行比调用的爆炸半径大,资源限额/网络白名单/文件系统隔离一个不能少;排错变难:程序 bug 与工具 bug 混在一个栈里,trace 要同时记录模型生成的代码和执行环境日志;对模型编程能力要求高:弱模型写不对编排代码,错误从"选错工具"变成"写错程序"——问题没有消失,换了形态。
  4. **2026 年的定位**——不是取代 MCP:发现/鉴权/跨产品复用仍是协议层的事;形态上是"细粒度工具调用"换成"代码编排工具"——DeepSeek harness 的 Code 模式(模型写 TypeScript 编排)是产品化实例。
- **补充弹药(追问再掏)**:
  - **选型判据**:中间数据量大、控制流复杂→code execution;动作简单、要审计留痕→工具调用(一次调用一条审计日志,代码执行里十次 API 调用只有一条)。
  - **安全配套**:沙箱网络白名单+资源限额+超时熔断,否则等于给了 agent 一个无限制解释器。
  - **渐进路线**:不用二选一——先工具调用跑通语义,高频复合路径再固化成"生成代码执行",两种形态共存。
- **及格线**:中间数据不流经模型这个核心+沙箱代价。
- **加分**:量化数字;与 MCP 的分工定位。
- **红旗**:当 MCP 对立面讲二选一。

**Q3.6【八股·L2·平台】工具过载的量化表现是什么?tool_search(延迟加载)怎么工作、省多少?**

- **人话版**:工具超过 30–50 个,选择准确率明显退化,定义本身先吃几万 token。tool_search:上下文只放工具名+一句话摘要,提供 search_tools 元工具,命中才展开完整定义——省 85%+ 定义 token。
- **主线(照这条线讲)**:
  1. **两个伤害机制分开讲**——显性:定义占用(没干活先付钱,55K token 实例,每轮都付);隐性:选择退化(几十个相似工具里模型摇摆——注意力摊到几十个描述上,每个都没看清;**选错工具的连锁成本——错误调用+纠错往返+被污染的上下文——远超定义本身的几万 token**)。隐性伤害常被低估,因为账单上只看得见第一个。
  2. **还有一个二阶伤害**——工具数组轮间抖动破坏缓存:过载既是质量问题也是成本问题(连回 ch02 Q2.9——改一行工具定义是缓存上最贵的操作,过载的工具清单更难保持稳定)。
  3. **tool_search 机制与缓存细节**——常驻的只有注册表(名字+一句话);模型调 search_tools(query) 拿候选、动态注入完整定义。**注入位置必须在尾部消息或子代理上下文**——注入到 tools 定义会破坏稳定前缀,每搜一次缓存全失效(自己实现最容易翻车的地方)。
  4. **配套纪律**——核心工具 3–5 个保持常驻不延迟(高频工具反复展开反而贵:每次会话都要搜一遍);合并语义重叠工具比延迟加载更治本(过载的根因常常是清单没治理,不是加载方式)。
- **补充弹药(追问再掏)**:
  - **厂商现状**:Anthropic 的 Tool Search Tool 默认 regex+BM25 匹配、支持上万级延迟工具;Codex 源码里有同思路的 tool_search handler。
  - **评估**:记录命中率、工具选择准确率、单任务 token,和全量注入对比——用数据决定哪些工具进常驻区。
  - **治理顺序**:先删僵尸工具(半年没调用的)→合并重叠→再考虑延迟加载——顺序反了是给垃圾做优化。
- **及格线**:两个伤害+延迟加载机制。
- **加分**:注入位置的缓存考量;核心工具不延迟的例外。
- **红旗**:以为工具越多能力越强。

---

## 3.2 原理深挖

**Q3.7【原理·L3·平台】Anthropic《Writing tools for agents》的核心工具设计原则有哪些?各举一例说明为什么有效。**

- **人话版**:四条——按工作流合并工具、描述写给"初级工程师"看、返回高信号数据、命名空间化。统称 ACI:工具接口是给模型用的用户界面。
- **主线(照这条线讲)**:
  1. **按工作流合并**——模型要"约个会",别让它 list_users→查空闲→create_event 三连:每一跳都是出错机会(参数在跳之间传递,任何一跳的输出格式变化都会断链),三轮往返烧三倍延迟。合并成 schedule_event(participants, duration) 一步到位;查询类用 search_contacts(query) 直接表达意图,别给 list_contacts 逼模型暴力遍历。**合并的判据是"用户的任务动词"**:任务说"约",工具就该有"约"。
  2. **描述的写法**——像给初级工程师写 docstring:何时用/何时不用/参数含义/返回结构/常见坑。这不是文风问题:**描述是模型选工具的唯一依据**——模型没见过你的实现,它对工具的全部认知就是这段文字。实证:仅优化工具描述,同款模型在当时 SWE-bench Verified 拿到 SOTA。官方自述"优化工具花的时间比优化 prompt 还多"——工具是模型的手,手的定义质量直接决定执行面。
  3. **返回高信号**——UUID 换语义名(机制见 Q3.8);错误带"下一步建议"而非裸 traceback(模型修不了网络,但能照建议改道);concise|detailed 两档输出(约省 2/3)。判据还是那个:**模型下一步要做的决定,取决于返回里有什么**。
  4. **正交性**——一类能力一个主入口:两个语义重叠的工具让模型摇摆而不是更聪明——每次选择都是一次赌博,重叠让赌率翻倍。"重叠即 bug"。
- **补充弹药(追问再掏)**:
  - **ACI 一词的出处**:SWE-agent 论文(2024)提出 agent 是"新一类终端用户",需要专门设计的接口——HCI 手段(语义化/一致性/可发现性)几乎都能平移。
  - **自查方法**:把自己平台的工具清单给一个新同事看,不解释让他选"完成 X 用哪个"——选错的就是接口问题;新同事在这里就是模型的替身。
  - **迭代节奏**:工具描述要随失败案例迭代——每次"模型选错/用错"都记下来归因到某句描述,这个反馈环比任何设计指南都值钱。
- **及格线**:四原则+各一句为什么。
- **加分**:描述即选择依据的实证;ACI 当统一框架。
- **红旗**:把工具设计当 API 包装体力活。

**Q3.8【原理·L3·平台】为什么"把 UUID 换成语义名能降低幻觉"?背后机制是什么?**

- **人话版**:密码学 id 对模型是无意义噪声——它得在上下文里维护"id→实体"的映射,跨轮引用容易错配或编造。语义名把标识锚定在自然语言表征上,和训练分布一致,引用就稳了。
- **主线(照这条线讲)**:
  1. **机制拆开**——模型的"记忆"就是上下文里的 token;`usr_9f3ka02` 与其他 token 没有语义关联,注意力很难把它和"张三"稳定绑定——两个低关联 token 之间的连接本来就弱,轮数一多、上下文一长,这个脆弱映射就是最先断的之一(错配或干脆编一个)。`zhang-san(finance)` 自带语义,引用时注意力顺着语义就能找到——**标识的可靠性来自表征的稳定性**。
  2. **这不是玄学是分布问题**——训练语料里"用名字指代实体"无处不在,"用 hash 指代实体"几乎不存在;接口贴合训练分布,行为就稳。这和 Q3.12 里"输出格式影响 agent 行为"是同一原理的两个面:模型对输入格式的"熟练度"由训练分布决定,接口设计本质是在选择贴近哪个分布。
  3. **落地模式三条**——工具返回里 id 与语义名成对出现(不能消除外部 id 时,让模型每次见到 id 都有语义锚);错误信息里用名字引用实体;长任务里让模型把关键实体写进 todo(语义化的外部记忆,连回 ch06)。
  4. **一般化收尾**——这是"工具接口是给模型的 UI"的具体化;HCI 里降低认知负担的手段几乎都能平移,这个例子是最容易验证的一个。
- **补充弹药(追问再掏)**:
  - **快速验证**:同一任务分别用纯 id 接口和语义名接口跑十次,统计跨轮引用错误率——一天能做完的实验。
  - **边界**:外部系统的 id 无法消除时,至少保证"返回里 id 与名字总是成对"。
  - **同族问题**:时间戳格式(相对时间 vs ISO 绝对时间)、状态码(语义枚举 vs 数字)——所有"人对着能查表、模型对着只能猜"的表示都在此列。
- **及格线**:无语义噪声→映射出错这个链条。
- **加分**:从 token 表征/训练分布角度解释;平移到自己的工具设计。
- **红旗**:用"模型背不住 id"这种拟人化解释带过。

**Q3.9【原理·L4·架构】MCP 落地的三大工程坑(n+1 鉴权 / 工具发现与上下文爆炸 / 安全)分别怎么缓解?**

- **人话版**:鉴权上每个 server 一套 OAuth、用户授权 n 次——用资源服务器化和企业网关统一;上下文上几十个 server 定义直接撑爆——用延迟加载和 code execution;安全上第三方 server 的描述可投毒可后改——按不可信组件治理。
- **主线(照这条线讲)**:
  1. **n+1 鉴权**——协议层:OAuth Resource Server 模式(防恶意 server 骗 token——token 只对显式声明的 resource 有效,冒充者拿不到跨资源通用凭证);产品层:connectors/身份代理;企业侧:统一网关+凭证托管,用户无感授权。判断成熟度的信号:用户感知的授权次数是 1 还是 n——n 次授权说明治理没做完,1 次才是。
  2. **工具发现与上下文爆炸**——三层:延迟加载(只留名字,见 Q3.6)、按域拆 subagent(每个只挂相关工具,选择面变小)、code execution(定义变文件树按需读,或写代码直调)。三层的共同方向:**把"全部可用"变成"按需可见"**。
  3. **安全(最重的一块)**——第三方审计曾扫出数百个问题 server:tool poisoning(描述里藏指令——模型读描述的瞬间就被指挥了)、rug pull(上线后改描述,过审之后变异)、跨 server 影子工具、明文密钥。缓解:入库前审计配置、工具描述变更 diff 告警、最小权限(只读优先)、网络出口限制。**核心心法:工具描述是指挥模型的可执行代码,按代码的供应链标准管它**——你会审依赖的代码,就该审依赖的描述文本。
- **补充弹药(追问再掏)**:
  - **rug pull 的检测**:对入库版本的工具描述做 diff 监控——"更新了描述"这件事本身就是告警事件,不靠内容审查(内容审查无穷无尽,变更检测一行配置)。
  - **对照**:npm 时代攒的供应链纪律(lockfile/provenance/最小权限)整体平移——学过的人不用重新发明。
  - **纵深配置**:企业网关层做统一鉴权+审计+出口白名单,server 本身当不可信组件——信任建在网关,不建在 server。
- **及格线**:三坑+各一个缓解。
- **加分**:rug pull/poisoning 机制;"描述当代码管"的治理观。
- **红旗**:认为接上 MCP 就安全(协议不提供信任)。

**Q3.10【原理·L3·架构】CLI 工具 vs MCP server vs code execution,三者的选型框架?**

- **人话版**:三个问题定选型——消费频率(高频内置/低频延迟)、返回形态(结构化数据还是可解析文本)、中间数据量(大就交给代码)。CLI 是默认起点,MCP 为"发现与授权"付费,code execution 为"表达力"付沙箱成本;生产配置几乎都是三者组合。
- **主线(照这条线讲)**:
  1. **先问三句**——这个能力的消费频率(高频内置进核心工具区、低频延迟加载)?返回是结构化数据还是可解析文本(要 schema 校验→MCP;文本够用→CLI)?中间数据量大不大(大→code execution,数据留在执行环境)?三个答案基本圈定选型。
  2. **CLI**——通用能力和自动化的默认起点:生态成熟、可 pipe、agent 本来就会写命令——**写命令行是模型训练分布里最密集的技能之一**,这个"免费的能力存量"不用白不用;gh 的体验常好过包一个 GitHub MCP server。短板:输出格式不稳(版本一变解析就断)、鉴权手工、无结构化 schema。
  3. **MCP**——要持久连接、结构化返回、OAuth 权限、跨产品复用时;本质是为"能力发现与授权"付封装成本——一次封装 N 个 host 受益。不适合的场景:单一 host 独享、动作简单——封装收益小于维护成本。
  4. **Code execution**——多工具协调、大中间数据、复杂控制流;一个执行环境的表达力≈无限个细粒度工具。选它的前提是沙箱基础设施现成——从零建沙箱的成本常常超过它省的。
  5. **组合是常态**——核心动作 CLI/MCP、数据加工交代码、低频长尾延迟加载;别用"都用 MCP"回答——**这题考的不是三选一,是知道每个选项在为什么付费**。
- **补充弹药(追问再掏)**:
  - **迁移观察**:CLI 先行、高频路径再固化成 MCP/代码,是成本最低的演进顺序——先验证消费频率,再付封装成本。
  - **组织信号**:选型吵不起来的团队多是按"数据在哪、动作为主还是加工为主"分工;吵起来的多是在为技术偏好找理由。
- **及格线**:三者适用条件各一句+组合意识。
- **加分**:每个选项的"付费对象"(发现授权/表达力/生态存量);演进顺序。
- **红旗**:单一答案。

---
## 3.3 Coding agent 的检索工具

**Q3.11【八股·L2·用户】为什么主流 coding agent 的代码检索用 ripgrep/grep 文本匹配,而不是 embedding 语义检索?至少四条机制理由。**

- **人话版**:代码检索的查询天然是词法的——找一个符号名、一个报错串、一个字面量,精确匹配就是正确答案;agent 又是多轮循环,一次没找对可以收窄再找。加索引反而引入构建、同步、失步一堆成本。
- **主线(照这条线讲)**:
  1. **①查询天然精确**——报错里的 `PaymentFailedError`、要改的函数名,必须逐字匹配;embedding 反而可能"语义近似"到错的符号。Cline 官方博客(2025-05)总结为"grep 的精确匹配天然适合迭代收窄"。
  2. **②无索引成本**——embedding 索引要构建、要在代码变更后同步、失步了把 agent 带沟里;ripgrep 冷启动即用、天然尊重 gitignore。对频繁变更的代码库,这是决定性优势。
  3. **③循环吃掉了语义召回的价值**——glob→grep→read 每轮都能根据上轮结果调整;一次性 top-k 召回的边际价值被这个循环消化。Sourcegraph 大规模统计(2026-05,1281 次 agent 运行)印证:keyword search 约 8000 次 vs semantic search 约 2400 次——**agent 天然倾向最简单可用的词法工具**。
  4. **④静态注入有反作用**——索引召回是"一次性塞进来的静态上下文",无关部分改变 agent 注意力轨迹(Cline 称之为失焦风险),还不可逆消耗预算。
  5. **⑤行业投票**——Cursor 2024 年文档还是全仓文件 embedding,2025-11 转向自研 Instant Grep("比 ripgrep 快"),2026 年 embedding 索引页已下线——一线厂商用产品路线投了票。
- **补充弹药(追问再掏)**:
  - **Sourcegraph 金句**:"These are context problems, not intelligence problems"——瓶颈在检索基础设施不在模型智力,这句话可以直接引用。
  - **反向边界**:语义检索什么时候有用见 Q3.14——别把这题答成"语义检索无用论"。
- **及格线**:四条理由,一条能落到机制。
- **加分**:Sourcegraph 统计或 Cursor 转向;"循环结构带来的红利"这个本质。
- **红旗**:默认"语义检索更先进";不知道索引失步问题。

**Q3.12【八股·L3·平台】给 coding agent 设计 Grep/Glob 工具,Claude Code 的实现里有哪些值得抄的细节?每个细节防什么坑?**

- **人话版**:Grep 建在 ripgrep 上;输出带文件名+行号;offset 分页且越界返回"该偏移无结果";非法正则把 ripgrep 原始报错回传;grep/rg 退出码 1 解释为"无匹配"不是命令失败。每个细节都在防 agent 的某类误判。
- **主线(照这条线讲)**:
  1. **基于 ripgrep**——正则更强、速度快、默认尊重 .gitignore(搜被忽略文件得显式传路径——防把 node_modules 搜进来)。
  2. **输出=文件+行号+匹配行**——模型能直接定位、能引用 file:line、能决定下一步 read 还是收窄 glob。信息密度刚好,不倒整个文件。
  3. **offset 越界返回"No entries at this offset"**——官方明说设计意图:让模型"放宽或重置 offset,而不是得出'模式不存在'的结论"。**分页边界是"没找到"误判的高发点,措辞是精心设计的纠偏**。
  4. **非法正则回传 ripgrep 的诊断**——模型看到具体报错能自己改正则再搜:错误信息即纠错信号(与"返回裸 traceback"的反例成对照)。
  5. **退出码 1=无匹配而非失败**(v2.1.196 起)——防模型把"没搜到"当"工具坏了"去重试换工具。配套参数:glob/type 收窄、multiline 跨行。
- **补充弹药(追问再掏)**:
  - **Bash 白名单联动**:ls/cat/grep/find 等只读命令免审批;cat/head/grep/rg 单文件查看计入 read-before-edit——工具间的一致性设计。
  - **出题变体**:这题常反过来问——"某自研 agent 的 grep 总触发'没找到'误判,你猜它输出设计缺了什么"。
- **及格线**:五个细节说对三个。
- **加分**:每个细节答出"防什么误判"(这是设计意图题的核心);横向对比过其他 agent 的实现。
- **红旗**:以为工具设计就是"能调 grep";没想过输出格式影响 agent 行为。

**Q3.13【原理·L3·架构】aider 的 repo map 怎么把百万行仓库压进约 1k token?为什么用图排序而不是 embedding 排序?**

- **人话版**:tree-sitter 把每个文件解析出"定义了什么、引用了什么"(签名级),以文件为节点、引用为边构图,跑 PageRank 选最重要的定义,按 token 预算截断输出。用图不用 embedding:重要性来自结构(被引用多的重要),这个信号精确、可解释、免索引。
- **主线(照这条线讲)**:
  1. **三步流水线**——tree-sitter 提 definitions/references(按语言的 tags 规则,拿签名不拿实现体)→建图(文件为节点、引用为边、权重=引用次数)→**personalized PageRank** 排序(可给"已在对话中的文件"加权,让地图偏向当前任务相关区域),按 --map-tokens(默认 1k)截断。
  2. **为什么图排序**——目标是"重要性排序"不是"语义相似":引用图直接给出结构重要性;免索引免同步(按需生成,和 grep 一样冷启动即用);可解释(每个条目都是真实签名,模型引用零幻觉)。
  3. **用法**——作为"结构索引"常驻,agent 看地图决定打开哪个文件(先结构后内容的导航层)。
  4. **一个反直觉的边界**——aider 对弱模型默认**关掉** repo map(会被 map 干扰去改它):上下文工程没有免费午餐,再好的信号也要匹配模型能力——这个细节最能体现"答过题"。
- **补充弹药(追问再掏)**:
  - **动态性**:map 大小随会话状态调整(无文件入 chat 时显著扩大)——它不是静态快照。
  - **跨仓用法**:aider --show-repo-map 导出再 /read,轻量的跨仓知识传递。
- **及格线**:tree-sitter+图+PageRank 三步。
- **加分**:personalized 加权意图;弱模型关 map 的取舍;"排序≠语义相似"辨析。
- **红旗**:把 repo map 描述成"代码的 embedding 索引"(完全搞反)。

**Q3.14【原理·L4·架构】什么时候该给 agent 补语义/索引检索?"文本优先、规模触发混合"的具体判据?**

- **人话版**:小中仓库(<40 万行)纯 grep 就好,加索引反而降分;超大仓库、跨仓库、自然语言意图找代码时语义/结构化检索有真实增益。判据用数据,不用信仰。
- **主线(照这条线讲)**:
  1. **规模的量化证据**——Sourcegraph CodeScaleBench(2026-05,40+ 大仓/1281 次运行):<400K LOC 加索引工具平均 **-0.080**(负收益!),400K–2M LOC **+0.259**;多仓库任务 F1 +0.209 vs 单仓库 +0.085。语义/索引的价值随规模非线性增长。
  2. **grep 的真实短板具体化**——grep "allocate" 返回 47 个匹配、无结构排序,agent 要逐个读;find-references 返回"1 个定义+12 个调用点,按角色排序"。超大库逐文件追 import 会"分支爆炸"(每步合理、整体迷路)。
  3. **极端案例给量级感**——Kubernetes 级仓库(140 万行 Go):纯本地工具 6000 秒 0 分 vs 索引工具 89 秒 0.90——规模到了,索引是刚需不是优化。
  4. **混合的落地形态**——三层并存(keyword/semantic/deep search);新形态是把"搜索循环"整体下沉进工具(Code Finder:给它任务自己搜,只回传文件+行范围+说明,快 2 倍、省 40%)。
  5. **判断顺序收尾**——先问仓库规模和跨仓需求,再问查询分布(自然语言意图多不多),最后才轮到"要不要建索引"——多数团队卡在没问前两问就上了向量库。
- **补充弹药(追问再掏)**:
  - **给自己的仓库算账**:LOC、跨仓任务占比、grep 同名爆炸的频率——三个数十分钟算完,比任何架构争论有效。
  - **概念辨析**:这题不反 Q3.11——"默认词法、规模触发"和"为什么默认词法"是同一结论的两面。
- **及格线**:规模分界+两个短板案例。
- **加分**:引用量化数字;Code Finder 的"搜索循环工具化"新形态。
- **红旗**:中小仓库标配 embedding(简历驱动);或反向"语义检索无用论"。

**Q3.15【原理·L3·平台】coding agent 的 websearch 工具怎么设计才能不污染上下文?对比 Claude Code 的 WebSearch/WebFetch 机制。**

- **人话版**:搜索结果只给标题+URL 不给正文;抓网页先把 HTML 转 Markdown,再用小模型按提取 prompt 抽答案——主模型看到"答案"不是原始页面。官方自述"design 上就是有损",有损是特性:防的就是整页噪声灌进上下文。
- **主线(照这条线讲)**:
  1. **WebSearch 的克制**——返回 titles+URLs 不抓正文(读哪篇是模型的下一步决策,信息分级进入);每 session 上限 200 次(主对话+subagent 共享),到限返回提示让模型"用手头信息继续"而不是报错——**预算约束软化处理,防任务崩断**。
  2. **WebFetch 的三层防污染**——HTML→Markdown(内容协商直接要 Markdown);小快模型按提取 prompt 处理,主模型只收该模型的回答;跨域重定向不自动跟随(返回重定向信息让模型显式二次 fetch——每步网络动作都在模型显式决策里,这同时是安全设计)。
  3. **配套细节**——15 分钟响应缓存;域名白名单/ask 权限;User-Agent 标识。Gemini CLI 同思路:web_search 走 grounding,API 端先处理再回传"合成摘要+引用 URI"。
  4. **原则收尾**——上下文预算优先于召回完整性:宁可两次请求拿精确信息,不要一次塞十页噪声。这条和 grep 优于 embedding、工具返回给 concise 档是**同一个设计观**:能精确就不要概率,能小就不要大。
- **补充弹药(追问再掏)**:
  - **安全的另一半**:跨域重定向显式化防的是"跳转到恶意域"的注入链——和 ch08 的输入消毒连成一条线。
  - **自建版**:搜索 API(给标题/URL)+fetch(小模型抽取)两个工具就能复刻,一天工作量。
- **及格线**:小模型抽取层+搜索不带正文。
- **加分**:重定向显式化的安全含义;把三处设计统一到"上下文预算"原则。
- **红旗**:把搜索结果页原文直接塞上下文;不知道"有损是特性"。

**Q3.16【原理·L3·用户】agent 拿到 `TypeError: ...PaymentFailedError ...` 后,理想的检索路径是什么?"先结构后内容"和"错误驱动"怎么体现?**

- **人话版**:先用报错里的符号名 grep 定位定义和抛出点,读上下文,顺调用链找数据来源;卡住了再看目录结构/模块图换方向。报错信息是最好的检索 query——它自带精确符号。
- **主线(照这条线讲)**:
  1. **错误驱动优先**——报错串是高精度 query(符号/文件/行号都在里面),先 grep 符号(定义处+抛出处)是命中率最高的一步,比"让 agent 泛泛浏览模块"快一个量级。
  2. **先结构后内容做兜底**——错误信息不够时(逻辑错误没报错),先看结构(目录树/模块划分/repo map)锁定嫌疑模块再下钻;避免上来全文搜索淹没在无关匹配。官方工作流(explore→plan→code→commit)的 explore 段就是"先宽问题后窄领域"。
  3. **两个失败模式的识别**——"无限探索":读了几百个文件还没到正题(官方反模式警告:收窄范围或用 subagent 隔离);"分支爆炸":顺 import 逐文件追,每步合理整体迷路(Sourcegraph 观察)——这时换 find-references 类结构化导航,别继续线性追。
  4. **工程配套**——Glob 找文件/Grep 找内容/Read 读文件三件套是标配;探索型任务交给 Explore subagent(独立上下文里烧 token 只回传结论),主上下文保持干净。
- **补充弹药(追问再掏)**:
  - **量化对照**:Sourcegraph 案例——试错路径 96 次工具调用/84 分钟/0.32 vs 结构化导航 5 次/4.4 分钟/0.68;差一个数量级,这是本题最有力的论据。
  - **给 subagent 的派发**:错误串+嫌疑模块+期望输出格式(发现/位置/建议),自包含。
- **及格线**:错误驱动 grep 符号+结构兜底。
- **加分**:两个失败模式的识别与对策;量化对照。
- **红旗**:让 agent"把仓库读一遍";报错在手却泛泛语义搜索。

**Q3.17【品味·L4·架构】"模型越强,工具越简单越词法化"——Cursor 从 embedding(2024)走到 Instant Grep(2025-11),Anthropic 的 Tool Search 默认 regex+BM25。怎么解读?边界在哪?**

参考思路:
- **解读主线:复杂度的再分配**——模型弱的时代靠索引/语义层"预消化"信息弥补能力(先把语义算好,模型只做匹配);模型强了,自己会在循环里收窄,简单工具+强模型更灵活、零索引维护、不会失步。词法工具还有一条红利:可解释可审计——grep 的结果你能复现、能进 code review,embedding 召回说不清为什么,出问题没法归因。**这不是怀旧,是把复杂度从基础设施挪回模型循环**——挪回去的前提是循环里的那个脑子够用。
- **边界同样真实**——规模:超大/跨仓库场景索引工具有硬数据支撑(Q3.14 的 400K LOC 分界);意图:自然语言找代码("处理支付重试的那段逻辑")没有精确词可 grep,语义层是真需求。复杂度没有消失,是沉回了基础设施(形态从"索引"变成"带搜索循环的工具"如 Code Finder——工具自己跑搜索循环,只回传结果)。
- **给团队的建议**——先把 grep/glob/read 的工具质量做扎实(输出格式、错误语义、分页边界——多数团队这里都没做好,Q3.12 列的细节就是现成清单),再谈检索底座;两题答案合成一句:"默认词法、规模触发分层"。
- **及格线 / 加分 / 红旗**:及格=复杂度移回循环的解读+一个边界;加分=用双方证据论证并落到团队行动;红旗=单边站队(要么"索引已死"要么"语义永远是对的")。

---

## 3.4 品味与判断题

**Q3.18【品味·L3·平台】你的 agent 要接入内部 30 个系统,有人提议"全部包成 MCP server 一次挂上"。怎么决策?**

参考思路:
- **反对全挂的三个理由**——过载(30–50 退化阈值,50 个 server 的工具数轻松破百);定义 token 常驻成本(每轮几万 token 的租金,还没干活先付钱);攻击面扩大(每个 server 是一条供应链风险,30 条一起上等于把审计负担乘 30)。更根本的:按系统包装是**按组织结构切工具,不是按任务切**——模型要的是工作流("发布一个版本"),不是你们的部门墙(构建系统、部署系统、监控系统各一个 server)。
- **分层方案**——高频核心(文件/命令/代码)内置进 harness;按任务域拆 subagent,每个只挂 3–10 个相关工具(发布子代理挂发布相关,监控子代理挂监控相关);低频长尾走延迟加载或 code execution(需要时现学 API)。
- **先问一句**——这个系统是"动作型"(MCP/CLI 合适)还是"数据型"(检索/过滤/聚合——code execution 更优)?30 个系统里通常一半不值得包成工具:它们的数据消费模式决定了"写代码查"比"逐工具调"更自然。
- **决策流程收尾**——拿使用频率×任务归属两个维度给 30 个系统列表打分:高频+任务集中→子代理常驻;高频+跨任务→核心工具区;低频→延迟加载或砍掉。**用表格决策,不用会议室气势决策**。
- **及格线 / 加分 / 红旗**:及格=过载理由+分层;加分=动作型/数据型切分+两维打分表;红旗=全挂或全不接。

**Q3.19【品味·L4·架构】工具该粗粒度(一个工具一个工作流)还是细粒度(正交原子操作)?给一个判断函数。**

参考思路:
- **判断函数四个变量**——谁组合(模型组合→偏粗,给现成工作流——它不擅长自己编排长链;代码组合→可细——程序不怕链长)、错误语义(中间态要不要可见/回滚——合规要留痕就必须细,一步一条审计记录)、上下文预算(每次调用的往返成本——粗粒度一次往返,细粒度 N 次往返 N 次全上下文推理)、训练分布(模型对什么粒度更熟——"发邮件""建会议"这类工作流动词是训练语料里的常见任务表述,粗粒度工作流通常是更安全的默认)。
- **默认与例外**——对模型暴露粗粒度工作流工具,原子操作留给 code execution 组合(**粗给模型、细给代码**,两头各取所长);审计/合规要求中间态可见时,粗粒度是风险:一次"部署完成"背后盖了十条不可见的操作。
- **正交解法(加分的亮点)**——粒度和信息量是两个维度:response_format: concise|detailed 枚举不动粒度也能控信息量——**多数"粗细之争"其实是信息量之争**,拆开就化解:要审计给 detailed,日常给 concise,不用拆工具。
- **及格线 / 加分 / 红旗**:及格=判断函数至少三个变量;加分=粒度/信息量正交的辨析+"粗给模型、细给代码"的分工;红旗=只有立场没有函数。

**Q3.20【品味·L3·安全】第三方 MCP server 的可信模型怎么建?**

参考思路:
- **默认不可信**——描述可投毒(poisoning:发布时描述里就藏着指令)、可上线后偷改(rug pull:先干净后加料,过审之后变异);**工具描述是指挥模型的可执行代码,按代码的供应链标准管它**——这句当总纲。模型读到描述的瞬间就在被它影响,这和执行恶意代码没有本质区别,只是"执行环境"是模型的注意力。
- **治理四件**——入库前审计配置(把描述当 diff 对象逐行读一遍);工具描述变更 diff 告警(rug pull 的探测器——"描述变了"本身就是事件,不靠内容判断);凭证最小化(只读优先、per-server 隔离,一个 server 泄漏不连坐);网络出口限制(server 能连哪里白名单管死——数据外发要有明确的合法出口清单)。
- **分级信任**——社区 server 适合低敏感场景(查天气、读文档);核心系统(支付/生产数据/客户 PII)自建或深度审计;信任决策和 npm 依赖管理同构,直接抄供应链纪律:lockfile(锁版本)、provenance(发布者身份)、最小权限、定期清理。
- **一个组织建议**——别让"信任评估"变成一次性事件:server 是活的(会更新),信任也必须是持续监控——这和"依赖不能装完就不管"是同一条纪律。
- **及格线 / 加分 / 红旗**:及格=不可信默认+三件事;加分=与软件供应链的类比落地+持续监控;红旗=按 star 数和 README 判断可信。

**Q3.21【品味·L4·架构】如果明天 MCP 消失,你账下哪些资产归零、哪些保留?这个答案如何影响今天的架构?**

参考思路:
- **归零**——server 封装代码、协议适配层、registry 接入——所有"说协议"的部分。
- **保留**——工具的语义设计(名称/描述/返回格式——ACI 资产:这些知识讲的是"模型怎么用得顺手",与协议无关)、权限模型(谁能做什么)、审计日志(谁做过什么)、subagent 分组逻辑(哪个域用哪些工具)、延迟加载策略(哪些常驻哪些按需)。**协议是载体,接口语义是资产**——这句话就是本题的答案。
- **架构影响**——聪明才智花在语义与治理,不为协议特性深度绑定;工具层留薄适配(语义定义和协议绑定分离——换协议时重写的是薄薄一层适配,不是整个工具面);评审工具设计时不问"合不合 MCP 规范",问"描述清不清楚、返回高不高信号"。
- **历史依据(让判断有根)**——协议一直在换代(传输从 stdio+SSE 到 Streamable HTTP、授权模式也在演进),而"怎么给模型设计好用的工具"这个知识没贬值过——**押注半衰期长的层,是架构师的基本功**。
- **及格线 / 加分 / 红旗**:及格=分清载体与资产;加分=落到自己架构的隔离层设计;红旗=把职业身份绑在协议上。

---

## 3.5 开放性问题

**Q3.22【开放·L5·模型】如果让你重新发明工具调用协议,你会改什么?**

讨论框架:
- **结构化输出的原生性**——工具参数还是"JSON 文本层":模型在文本流里拼 JSON,协议再解析。能否把 schema 约束下沉到解码层之外,让工具调用成为模型的一等输出模态(和文本、图像并列,不是文本的特殊格式)——从根上消灭"参数拼错"这一整类失败。
- **渐进披露作为协议原语**——tool_search 的"先名字后定义"两级结构该是协议内建(工具自带 summary 字段,运行时按需展开),而不是应用层各显神通的技巧——现在每家自己实现一遍,注入位置的缓存坑每家重新踩一遍。
- **错误语义强类型化**——错误现在是字符串,模型靠读文本猜"该重试还是改道";协议级定义错误类别(可重试/需改参数/需授权/永久失败),模型据此走不同分支——错误处理从"理解自然语言"变成"读枚举"。
- **权限声明内嵌 schema**——工具自带"这个调用会花钱/会外发数据/会写生产"标注,权限系统按标注策略化审批——现在这些信息散在文档里,审计要人读。
- **批判点(防止答成愿望清单)**——每条都要问"这是协议该做的还是模型该学会的":过度协议化会把智能从模型挪回代码(开倒车——模型一代代在变强,协议层每硬编码一条规则就少一分灵活);好的答案每条都过这个筛子。
- **及格线 / 加分 / 红旗**:及格=两三条具体的协议改动+机制;加分=批判点(协议/模型边界的筛子);红旗=泛泛的"更安全更好用"。

**Q3.23【开放·L4·架构】Code execution 会不会吞掉 MCP?什么会幸存?**

讨论框架:
- **吞不掉的**——发现(server 怎么被找到:目录、注册、搜索——代码执行不解决"知道有什么可用")、鉴权(OAuth 流程要协议层标准化才免得每家重做)、跨 host 复用(一份封装全工具受益的网络效应)、权限声明:这些是协议层的价值,代码执行不提供——**它换掉的是调用方式,换不掉供给方式**。
- **会被吞的**——细粒度数据查询类工具:"取数据过滤后给我",写代码调 API 比逐工具调用高效得多(中间数据不过模型,Q3.5 的账)。这一类工具在 code execution 成熟后没有存在理由。
- **可能的终局**——MCP 做目录与授权层(你能用什么),执行层让代码完成(你怎么用)——2026 年主流厂商的形态在朝这个方向收敛;类比:npm 管包的发现与信任,怎么用包是开发者写代码的事。
- **追问(显深度)**——数十万并发沙箱的供给成本谁解决(执行环境本身是重基础设施:冷启动、资源配额、安全隔离——规模化之后这是云厂商的活,不是应用团队的自建项);沙箱逃逸的责任边界在哪(执行层的安全事故,协议层和应用层怎么分责)。
- **及格线 / 加分 / 红旗**:及格=吞不掉的几条+机制;加分=npm 类比+供给成本的追问;红旗=非黑即白的站队。

---

## 3.6 工程实战题

**Q3.24【实战·L3·平台】为内部"发布系统"设计 agent 工具集:清单(≤10)、返回设计、不暴露的端点及理由。**
> 场景白话:"发布系统"就是公司内部把代码上线的那套平台——工程师代码合并之后在它上面点"发布",它负责构建、把新版本部署到服务器、出问题再一键回滚到旧版本。"REST API 40 个端点"指它对外提供 40 个程序接口(端点=一个可调用的功能,如"创建发布""查版本状态");现在要让 agent 也能操作它——工具=包装好给模型调用的函数。

参考思路:
- **第一步想清楚:接口是给程序用的,工具是给模型用的**——程序能记住 40 个端点怎么调,模型记不住:工具超过十来个它就开始选错(本章的"工具过载")。所以不是把 40 个端点逐个翻译,而是**按工程师的日常动作合并**:平时人就干五件事——看看有哪些发布在进行(list_releases(status))、某版本健康不健康(get_release_health(id))、发个新版本(create_release)、出事回滚(rollback_release)、搜历史事故(search_incidents(query))——**五个工具覆盖九成场景**,其余 35 个端点是程序内部的实现细节,模型不需要知道。
- **返回设计:让模型"读得懂",不是把数据库吐给它**——内部代号(rel_8f3a2)换成有语义的名字("2026-09-06 支付服务 v2.3.1");健康状态直接给一句人话结论而不是原始 JSON(JSON=程序间传数据的标准格式,整坨字段,模型读着又贵又慢);报错带"下一步该干嘛"——"该版本已归档,先调 X 恢复再重试";列表默认分页,别一次吐 1000 条。
- **坚决不暴露的:搞砸了救不回来的**——删除类操作(尤其级联删除=删一条连带删一片)、凭证管理(改密码/密钥)——走人工或独立审批通道;**判断标准一句话:做错了能不能撤销?影响面可不可控?**
- **安全默认**——create_release 默认 dry_run(先演示"我将要做 X"不真做,人确认了才执行);rollback 需要人工批准。
- **工具描述怎么写**——当成给新来的初级工程师写文档:一句话用途、一句话"什么时候别用它"、每个参数什么意思、返回长什么样、附一个调用示例。
- **及格线 / 加分 / 红旗**:及格=按工作流合并成 5–8 个工具;加分=dry_run/approval 这类默认安全;红旗=40 个端点原样包成 40 个工具(模型根本选不过来,错误率暴涨)。

**Q3.25【实战·L3·平台】不依赖厂商特性,自己实现 tool_search 式延迟加载的最小可行版。**
> 场景白话:这个问题是工具太多塞不下:与其把上百个工具的定义全放进模型上下文,不如只给一个"搜索工具的工具"——模型说"我要查日志",系统现取最相关的几个工具定义给它用。本题要求不依赖厂商内置的这功能,自己实现最简版。

参考思路:
- **注册表**——常驻只有 name+一句话摘要(50 个工具几百 token);完整定义存本地,按 name 索引。摘要的写法有讲究:一句"什么时候用它"而不是"它是什么"——模型搜索时的 query 是任务意图("我要查日志"),描述要和这个意图对得上。
- **元工具**——search_tools(query) 返回 top-k 候选(名字+摘要+一句参数预览),选中后动态注入完整定义;检索用 BM25/正则就够(Anthropic 官方默认也是 regex+BM25,不是 embedding——别过度设计,几十上百个工具的规模,词法匹配的召回完全够用)。
- **缓存细节(最容易翻车)**——动态注入位置必须在尾部消息或子代理上下文;注入到 tools 定义=每搜一次缓存全失效(这条让无数自研实现"省了 token 贵了账单")。命中过的定义本会话缓存复用(同一工具二次使用直接注入,不重搜);session 级的"常用工具"可以逐步晋升进常驻区。
- **失败兜底**——search 没搜到候选时返回"可用类别清单+建议的 query 改写",而不是空结果(空结果让模型瞎猜工具名);top-k 给 3–5 个,太少漏召回,太多又回到过载。
- **评估闭环**——命中率、工具选择准确率、单任务 token,和全量注入对比——用数据证明省了多少、准确率降没降;准确率掉了先改摘要写法,再怀疑检索算法。
- **及格线 / 加分 / 红旗**:及格=注册表+元工具+注入位置正确;加分=缓存规避、空结果兜底与评估闭环;红旗=把完整定义动态塞进 tools 数组。

**Q3.26【实战·L4·平台】设计对不可靠外部 API 的工具封装:经常超时、返回半截 JSON、限流。**
> 场景白话:你要包一层的外部服务很不稳定——"超时"=等半天不回话;"半截 JSON"=返回的数据格式坏了缺胳膊少腿(JSON=程序间传数据的标准格式);"限流"=对方限制你的调用频率,超了就报错。要给这类接口套个"防护壳"再给 agent 用。

参考思路:
- **工具侧做稳定**——重试/指数退避/超时预算在封装层做掉:这是确定性代码擅长的事,别让模型操心(模型面对超时只会再调一次,不懂退避,反而加剧限流)。重试预算要显式:总共等多久、试几次,到顶返回明确的"试过了不行"。
- **返回语义化错误**——"服务暂不可用,已重试 2 次;可等 60s 再试,或改用 X 通道":错误信息里带三个要素——发生了什么、已经做了什么、下一步选项;模型能据此决策。绝不给原始 traceback(它修不了网络,只会瞎试)。"错误是给模型看的 UI"在不可靠 API 场景下最吃功夫也最值钱。
- **半截数据的两难处理**——要么截断+明确标注"数据不完整,缺 N 条"(模型知道边界,能自己决定"够用"还是"换路径");要么不返回(明确失败)。**最糟的是模糊的半截数据**:字段缺了但不声明,模型基于错误信息推理还自信满满——宁缺毋滥。
- **限流的专门处理**——429 响应读 Retry-After 头,工具层排队等而不是把"被限流"抛给模型(模型会立刻重试,火上浇油);并发配额在封装层限死,多 agent 共享一个凭证池时尤其要收口。
- **熔断与监控**——连续失败 N 次工具层直接拒绝并建议改道(熔断:防止 agent 在死胡同里反复撞);错误率/重试成功率反馈给 API owner——你的封装顺便成了对方可用性的监测器。
- **及格线 / 加分 / 红旗**:及格=语义化错误+重试内聚;加分=半截数据处理、限流排队与熔断;红旗=把超时异常抛给模型。

---

## 3.7 场景排错题

**Q3.27【排错·L2·用户】agent 总是"忘记"用你配置的某个工具,转而用 bash 硬凑。排查顺序?**

排查链路:
- **①描述问题(第一怀疑,占大头)**——名字看不出用途(叫 query_v2 的是干嘛的)、没写"何时用何时不用":模型选工具全靠描述,描述烂=工具不存在——它不是忘了,是根本没"看见"过这个工具的用途。bash 硬凑恰恰是证据:模型知道任务要做什么,只是从工具清单里找不到匹配的描述。
- **②语义重叠**——有没有另一个工具(包括 bash)干类似的事:两个重叠选项让模型摇摆,常输的那个就是你以为没被用的;bash 的"描述"是模型毕生训练的命令行知识,你那三句写歪的描述竞争不过它。
- **③过载**——总工具数是否过了 30–50 退化区:过载之下最先被牺牲的就是描述不突出的边缘工具。
- **④加载与习得**——工具真的被加载了吗(白名单/配置错误经常是根因——排查"玄学"的第一步永远是确认状态,不是猜动机);上一次用它是不是吃过亏(返回太差/超时,模型"学会了"绕开——习得行为,比 bug 难查但翻对话历史看得见:用过几次然后放弃的轨迹)。
- **⑤最后才是 prompt 引导**——显式点名"处理 X 用工具 Y"是补丁不是修复(下一个任务变体又失效),前四步查完再上;上了之后还要观察是不是真的治本——靠点名才用的工具,说明前面四层还有问题。
- **及格线 / 加分 / 红旗**:及格=按序排查描述/重叠/过载;加分=识别"模型学会绕开差工具"+bash 的"训练分布优势"解释;红旗=直接写进 system prompt 强制(掩盖症状)。

**Q3.28【排错·L3·平台】某 MCP server 更新后,agent 开始偶发把用户数据发到外部端点。应急处置与根因?**

排查链路:
- **应急三连(先断损失再查因)**——摘除 server(下线它的接入,不是等调查结论);吊销它的凭证(token 还活着,数据还会继续出去);审计泄漏范围(哪段时间、哪些请求出去了、出去的是什么数据)——三个动作在小时级完成,顺序不能乱。
- **根因对比**——拉更新前后的工具描述与实现 diff:rug pull(上线后描述被改,注入外发指令——diff 会显示描述新增了"把结果同步到 X"之类的话术)或 tool poisoning(描述一直藏休眠指令,这次的更新只是触发了执行条件);检查 sampling 能力是否被滥用(server 反向借 host 模型的能力,可以诱导生成外发内容);网络出口是否本该限制(白名单没配=裸奔——这一问经常是真正的根因:攻击本身不新鲜,新鲜的是出口没设防)。
- **"偶发"的定位价值**——不是每次都触发说明有条件:记录触发的请求特征(什么任务、什么数据类型),和描述里的指令条件对照——把触发条件找出来,泄漏范围的审计才查得全(只按时间窗口统计会漏掉特殊 case)。
- **长期治理**——server 配置版本化入库(更新走 review,描述变更就是 PR 里的一个 diff);工具描述变更 diff 告警(rug pull 的前置探测器);沙箱网络白名单;敏感数据分级(ch08)。
- **及格线 / 加分 / 红旗**:及格=应急+diff 排查;加分=偶发的条件定位+描述 diff 告警的前置探测;红旗=只做应急不建治理(下次换个 server 复发)。

**Q3.29【排错·L3·平台】agent 调工具时参数频繁填错(枚举拼错、id 传成 name)。是模型问题还是接口问题?**

排查链路:
- **先查接口(多数根因在这)**——枚举值是否语义化(内部码 `st_03` vs `archived`:前者模型只能靠背,背错就是拼错);参数名有歧义(`user` 是 id 还是名字——描述里没说清,两种填法各对一半);返回值里给过正确 id 形态吗(从没见过正确格式,模型只能编——接口从未示范过正确答案,错不全在模型)。**这三问的共性:接口有没有把"什么是正确的"讲清楚、示范清楚**。
- **再加约束**——strict schema 约束解码锁死枚举(物理上生成不出 `st_3` 这种拼错);参数描述给一个正确示例(示例比解释有效——模型模仿示例的可靠性高于遵守抽象描述)。
- **接口改造**——返回里 id+语义名成对(见 Q3.8,给引用锚点);错误信息带正确格式示例("期望格式如 users/12345,你传的是张三"——ACI 原则:错误是纠错信号)。
- **判断标准(归因的方法论)**——同类错误跨模型复现=接口问题(换强模型还错,说明接口没把信息给够);只在弱模型出现=能力问题(换模型或简化接口——把枚举变布尔、把自由字符串变选择)。这个对照实验半天能跑完,归因不靠感觉。
- **及格线 / 加分 / 红旗**:及格=先查接口三项;加分=strict 解码+成对返回+跨模型归因实验;红旗=归因模型然后换更贵模型硬扛。

---

## 3.8 开放加餐:偏好与品味追问

**Q3.30【开放·L3·平台】你见过设计得最好的一个 agent 工具是什么?好在哪里?你觉得"工具描述写得好"的标准是什么?**

参考思路(一个好答案长什么样):
- **例子要具体到细节**——比如 Grep 把"退出码 1"解释成"无匹配"、错误信息里带"下一步该试什么"、WebFetch 声明自己是"有损摘要不是原文":**能说出至少一个一手细节,证明观察过工具面而不只是用**——用过的会说"很好用",观察过的才说得出"它在哪个边界上替模型防了坑"。
- **描述的三条标准**——控制行为路径(模型照着描述决定怎么用,写错描述=教坏用户——描述是模型的操作手册,每句话都在塑造它的行为);边界声明(什么时候不该用这个工具——"别用它做 X"比"它能做 Y"更防错);失败语义(错了返回什么、该重试还是换路——模型遇到错误时的决策依据)。
- **有反例意识**——"返回整个文件的工具"(没有分页边界)、"描述写'功能强大'却不说参数语义"(没有行为路径)、"错误返回裸 traceback"(没有失败语义)这类反面教材,能对照说明好标准为什么成立——**说得出反例的标准才是真标准,说不出反例的是口号**。
- **及格线 / 加分 / 红旗**:及格=一个具体好例子+一条标准;加分=三条标准齐+反例;红旗=夸"功能多、速度快"(那是产品评测不是工具设计)。

**Q3.31【开放·L2·用户】你的 MCP 面板:现在装了哪些 server?砍过哪些?留下这些的判断标准是什么?**

参考思路(一个好答案长什么样):
- **留下的理由和砍掉的理由成对出现**——留下:高频使用/输出干净(返回即用,不用二次加工)/风险低(只读或来源可信);砍掉:描述诱导性强(模型老想去调它——一个"存在感太强"的工具是负资产)/返回太吵(一个结果占几千 token)/安全上信不过。**成对出现说明有真实的取舍经历**,只有留下清单的多半是装完没管过。
- **知道工具面的成本**——装得多不等于强:工具过载降低选择准确率、动态工具列表破坏缓存(ch02 的账)、每个 server 是一条供应链风险;**个位数的高质量面板优于几十个的杂货铺**——面板的整洁度本身就是工程判断力的展示面。
- **有审计习惯**——定期看"哪些工具调用成功率低/从未被调",删僵尸工具(调用日志是最诚实的投票);知道 rug pull 风险(server 上线后偷改描述),所以来源锁定版本——更新是显式决策不是被动接受。
- **及格线 / 加分 / 红旗**:及格=留/砍各有理由;加分=工具过载与缓存视角+审计习惯;红旗=只装不砍、"多多益善"逻辑。
