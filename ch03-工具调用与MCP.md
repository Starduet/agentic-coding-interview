# ch03 · 工具调用、MCP 与 Agent 检索

> 覆盖:function calling 机制、工具 schema 设计方法论、工具过载、MCP 协议(原语/传输/治理/安全)、CLI vs MCP vs code execution、结构化输出,以及 **coding agent 的检索实现**:为什么用 ripgrep 而不是 embedding、repo map、websearch 工具的上下文防污染、agent 怎么自主收窄搜索。
> 案例基线:2026-08。工具产品的具体形态随版本变化,机制与设计逻辑是考点。

---

## 3.1 八股题(基础概念)

**Q3.1【八股·L1·平台】function calling 的标准循环是什么?"工具定义会被计费"这件事对工具设计有什么影响?**

- **人话版**:五步——带 tools 定义发请求→模型返回"我要调某工具+参数"→你的代码执行→把结果按 id 回传→模型继续或结束。关键认知:工具定义每次请求都随上下文发送、按输入 token 计费——工具不是免费的列表,是每轮都要付租金的上下文占用。
- **展开**:
  - **循环细节**:模型的工具调用请求带 call id;你必须按 id 回传对应的 tool_result(并行调用时多个 id 各自配对);模型可以一轮发起多个调用。`tool_choice` 参数控制倾向:auto(模型自决)/required(必须调)/指定某工具/none。
  - **计费的推论**:一个工具的定义占几百到上千 token,挂 50 个工具就是几万 token 的"常驻租金"。所以工具数量要克制(经验:核心工具 ≤20)、描述要高信息密度、低频工具用延迟加载(见 Q3.6)。Anthropic 文档里多 MCP 场景未优化先吃 5 万 token 的实例,就是这个机制的账单版。
  - **错误回传也是设计点**:工具失败时返回 is_error 标记+可读的错误说明,模型能据此调整;返回裸 traceback 它只会瞎重试。
- **及格线**:五步循环+计费影响一句。
- **加分**:并行调用按 id 配对的细节;错误回传的设计意识。
- **红旗**:以为工具定义只在"用到时"计费;错误直接抛异常不给模型反馈。

**Q3.2【八股·L2·平台】MCP(Model Context Protocol)解决什么问题?它的核心概念(host/client/server)和原语(tools/resources/prompts/sampling/elicitation)分别是什么?**

- **人话版**:MCP 解决 N×M 集成问题——M 个应用接 N 个数据源,原本要写 M×N 个连接器,标准化后变成 M+N。host 里跑多个 client,每个 client 连一个 server;server 提供工具/资源/提示词三类东西,反过来也可以向 host 请求采样(sampling)或向用户追问(elicitation)。
- **展开**:
  - **为什么 N×M 是真问题**:你的 agent 要接 GitHub、Slack、数据库、监控……每个 host 各写一遍集成,重复且质量参差。协议标准化后,server 写一次所有 host 受益——"AI 应用的 USB-C"这个类比说的就是网络效应。
  - **安全边界的设计**:server 不能读整个对话、看不见其他 server——每个连接 1:1 隔离。这个设计是权限模型的地基,也是分析各种 MCP 攻击面时的基准线。
  - **原语的分工**:tools(模型可调用的动作)是主力;resources(可读数据)和 prompts(模板)用得少;sampling 让 server 反向借用 host 的模型能力(危险能力,要用户批准);elicitation 让 server 向用户追问。评估一个 MCP server 的攻击面时,先把这五个原语过一遍——sampling 能力被滥用就是数据外泄通道。
- **及格线**:N×M 问题+三原语。
- **加分**:client/server 隔离的安全含义;sampling 为什么危险。
- **红旗**:只会"USB-C"这个比喻说不出机制;不知道 server 之间互相隔离。

**Q3.3【八股·L2·平台】JSON mode 和 Structured Outputs(constrained decoding)的区别是什么?为什么 agent 系统必须要后者级别的可靠性?**

- **人话版**:JSON mode 只保证"输出是合法 JSON",不保证符合你的 schema——实测复杂 schema 下 compliance 经常只有八九成;Structured Outputs 用约束解码,在采样阶段就只允许符合 schema 的 token,做到 100% 符合。
- **展开**:
  - **机制差异**:约束解码把 JSON Schema 编译成状态机,生成时动态屏蔽不合法 token——不是"生成完再校验",是"物理上生成不出非法输出"。代价是 schema 要满足条件(字段全 required、禁 additionalProperties),且首次请求有编译开销。
  - **agent 为什么必须要**:agent 的每个环——工具参数解析、路由分类、状态转移、评测打分——都依赖机器可读输出。单次 99% 的 compliance,在 30 步循环里的累积成功率是 0.99³⁰≈74%;1% 的坏 JSON 在循环里会被放大成整条任务失败+烧掉前面所有 token。
  - **不是所有场景都要**:给人看的自由文本回答不需要;分类、抽取、工具参数这些"机器消费"的输出才必须锁死。
- **及格线**:约束解码的原理+循环放大效应。
- **加分**:算得出累积成功率;知道 schema 的限制条件。
- **红旗**:认为"prompt 里写清楚格式"就够了;混淆两个概念。

**Q3.4【八股·L2·模型】temperature=0 就完全可复现了吗?采样参数在 agent 里的正确用法?**

- **人话版**:不。批处理调度、浮点精度、路由、硬件差异都会引入波动,seed 参数也只是 best-effort。趋势是新一代模型干脆只接受 1.0——可控性从采样参数转移到约束解码、工具设计和评测上。
- **展开**:
  - **为什么 0 也不确定**:推理集群的批处理组合方式影响浮点求和顺序;请求路由到不同硬件;这些在数值上不保证逐 bit 一致。要可复现,靠的是输出层面的确定性(约束解码、后处理校验),不是采样层面的祈祷。
  - **agent 里的用法**:要"对"的任务(工具选择、参数抽取、代码编辑)低温度或不调;要"多"的任务(候选生成、探索)高温度。但注意厂商动向:Anthropic 在新模型上弃用 temperature/top_p(只接受 1.0)——因为训练和推理协议已经把行为稳定性做进去了,采样旋钮的边际价值在消失。
  - **工程启示**:别把"调温度"当稳定性方案;稳定性靠验证器和重试策略,采样参数只管多样性。
- **及格线**:0 不等于确定的机制+一两句正确用法。
- **加分**:知道弃用趋势及其含义(稳定性内化进训练)。
- **红旗**:坚信 temperature=0 可复现并据此设计"确定性测试"。

**Q3.5【八股·L2·平台】"code execution as action"是什么主张?它和"把每个 API 包成工具"相比,优势代价各是什么?**

- **人话版**:与其塞一百个工具定义让模型一个个调、中间结果反复流经模型,不如让 agent 直接写代码去调这些 API——数据和控制在执行环境里流转,模型只看最终结果。代价是你要提供一个带资源限制的沙箱。
- **展开**:
  - **机制优势**:代码有循环/条件/错误处理,一次生成的程序能完成"取数据→过滤→聚合→输出"整条链,不再是一轮轮模型往返;中间数据(一万行的表格)留在执行环境,模型只看过滤后的 5 行——这是上下文管理的降维打击。Anthropic 给过量化例子:工具定义从 15 万 token 降到 2 千。
  - **代价**:安全沙箱成为硬依赖(代码执行比工具调用的爆炸半径大);排错变难(程序 bug vs 工具 bug 混在一起);对模型的编程能力要求高。
  - **2026 年的演进**:这个思想已经产品化——Anthropic 的 programmatic tool calling 让工具结果在代码沙箱内处理(官方数据:复杂研究任务平均 token 省 37%),DeepSeek 的 harness 提供 Code 模式(模型写 TypeScript 编排工具调用)。它不是取代 MCP,是把"细粒度工具调用"换成"代码编排工具"。
- **及格线**:主张+中间数据不流经模型这个核心优势。
- **加分**:量级数字;沙箱代价;知道最新产品化形态。
- **红旗**:认为它是 MCP 的对立面要二选一(发现/鉴权/复用仍是协议层的事)。

**Q3.6【八股·L2·平台】工具过载的量化表现是什么?tool_search(延迟加载)怎么工作、省多少?**

- **人话版**:工具超过 30–50 个,选择准确率明显退化,而且定义本身先吃几万 token。解法是延迟加载:上下文里只放工具名+一句话摘要,提供一个 search_tools 元工具,命中才展开完整定义——能省 85% 以上的定义 token。
- **展开**:
  - **两个伤害机制**:定义占用(没干活先付钱)是显性的;选择退化是隐性的——模型在几十个相似工具里摇摆,选错工具的连锁成本(错误调用+纠错往返)远超定义本身。两者叠加,还可能破坏缓存(工具数组抖动)。
  - **tool_search 的机制**:常驻的只有注册表(name+一句话);模型调 search_tools(query) 拿到候选并动态注入完整定义。注意注入位置——动态注入到尾部消息或子代理上下文,别动稳定前缀。厂商已产品化:Anthropic 的 Tool Search Tool 默认用 regex+BM25 匹配(可自建 embedding 方案),支持上万级延迟工具;Codex 的 tool_search handler 同思路。
  - **配套纪律**:核心工具(3–5 个)保持常驻不延迟——高频工具的展开成本反而不划算;合并语义重叠的工具比延迟加载更治本。
- **及格线**:两个伤害+延迟加载机制。
- **加分**:注入位置的缓存考量;"常留核心工具不延迟"这个细节。
- **红旗**:以为工具越多能力越强;把 tool_search 当万能解不先做工具合并。

---

## 3.2 原理深挖

**Q3.7【原理·L3·平台】Anthropic《Writing tools for agents》的核心工具设计原则有哪些?各举一例说明为什么有效。**

- **人话版**:四条——按工作流合并工具(一个 schedule_event 好过查询/创建三件套)、描述写给"初级工程师"看、返回高信号数据(UUID 换成语义名、错误带修复示例)、命名空间化避免歧义。核心思想:工具接口是给模型的用户界面,设计得好不好直接决定 agent 表现。
- **展开**:
  - **合并工作流**:模型要"约个会",你得让它先 list_users 再查空闲再 create_event 三连——每一跳都是出错机会。合并成"schedule_event(participants, duration)"把三步变一步。反面:`list_contacts` 让模型暴力遍历,不如 `search_contacts(query)` 直接表达意图。
  - **描述的写法**:像给初级工程师写 docstring——什么时候用、什么时候不用、参数含义、返回结构、常见坑。这不是礼貌问题:描述是模型选择工具的唯一依据。实证:仅优化工具描述,同款模型在当时 SWE-bench Verified 上拿到 SOTA——工具即能力的放大器。
  - **返回高信号**:UUID/hash 对模型是无语义噪声(跨轮引用容易错配);返回里带语义名、带"正确格式的示例"、给 concise|detailed 两档输出(concise 约省 2/3 token)。错误信息带"下一步建议"而不是裸 traceback——模型修不了网络错误,但能照着建议改道。
  - **正交性**:一类能力一个主入口——两个语义重叠的工具会让模型摇摆而不是更聪明。
- **及格线**:四原则+各一句为什么。
- **加分**:有自己设计/改造工具集的实例;能把 ACI(工具接口=模型的用户界面)当统一框架讲。
- **红旗**:把工具设计当 API 包装的体力活;描述写成营销文案。

**Q3.8【原理·L3·平台】为什么"把 UUID 换成语义名能降低幻觉"?背后的机制是什么?**

- **人话版**:密码学 id 对模型是无意义噪声——它得在上下文里维护"id→实体"的映射,跨轮引用时容易错配或编造。语义名把标识锚定在自然语言上,和模型训练分布一致,引用就稳了。
- **展开**:
  - **机制**:模型的"记忆"就是上下文里的 token。`usr_9f3ka02` 这个串与其他 token 没有关联,注意力很难把它和"张三"绑定;`zhang-san(finance)` 自带语义,引用时模型的注意力能顺着语义找到它。这不是玄学,是 token 表征的分布问题。
  - **落地模式**:工具返回里 id 与语义名成对出现;错误信息里引用实体用名字;长任务里让模型把关键实体写进 todo/笔记(语义化的外部记忆)。
  - **一般化**:这个例子是"工具接口是给模型的 UI"这个思想的具体化——HCI 里降低用户认知负担的手段(语义化、一致性、可发现性),几乎都能平移到工具设计。
- **及格线**:无语义噪声→映射出错这个链条。
- **加分**:从 token 表征角度解释;平移到自己的工具设计。
- **红旗**:认为这是"细节优化不重要";用"模型背不住 id"这种拟人化解释带过机制。

**Q3.9【原理·L4·架构】MCP 落地的三大工程坑(n+1 鉴权 / 工具发现与上下文爆炸 / 安全)分别怎么缓解?**

- **人话版**:鉴权上,每个 server 一套 OAuth,用户要授权 n 次——用资源服务器化和企业网关统一凭证;上下文上,几十个 server 的定义直接撑爆——用 tool_search 延迟加载和 code execution;安全上,第三方 server 的描述可投毒可后改——按不可信组件治理:审计、diff 监控、最小权限。
- **展开**:
  - **n+1 鉴权**:协议层演进(OAuth Resource Server 模式,防恶意 server 骗 token);产品层用 connectors/身份代理;企业侧统一网关+凭证托管,用户无感授权。
  - **上下文爆炸**:三层缓解——延迟加载(工具只留名字)、按域拆 subagent(每个只挂相关工具)、code execution(定义变文件树按需读,或干脆写代码调)。
  - **安全**:第三方审计曾扫出数百个有问题 server——工具描述里藏指令(tool poisoning)、上线后改描述(rug pull)、跨 server 影子工具、明文密钥。缓解:入库前审计配置、工具描述变更 diff 告警、最小权限(只读优先)、网络出口限制。核心心法:**工具描述是指挥模型的可执行代码,要按代码的供应链标准管它**。
- **及格线**:三坑+各一个缓解。
- **加分**:rug pull/poisoning 的具体机制;把描述当供应链的治理观。
- **红旗**:认为接入了 MCP 就安全了(协议本身不提供信任)。

**Q3.10【原理·L3·架构】CLI 工具 vs MCP server vs code execution,三者的选型框架?**

参考框架:
- **默认问三句**:这个能力的消费频率(高频内置/低频延迟)?返回是结构化数据还是文本输出?中间数据量大不大?
- **CLI**:通用能力和自动化的默认起点——生态成熟、可组合、agent 会写命令;适合动作型、输出可文本解析的场景。缺点:输出格式不稳、鉴权手工、无结构化 schema。
- **MCP**:需要持久连接、结构化返回、OAuth 权限、跨产品复用时;本质是为"能力发现与授权"付封装成本。
- **Code execution**:多工具协调、大中间数据、复杂控制流——一个执行环境的表达力约等于无限个细粒度工具。
- **组合是常态**:核心动作 CLI/MCP、数据加工交代码、低频长尾延迟加载。
- **及格线 / 加分 / 红旗**:及格=三者的适用条件各一句;加分=给出组合使用的真实配置;红旗=单一答案"都用 MCP"。

---

## 3.3 Coding agent 的检索工具(新知识点)

**Q3.11【八股·L2·用户】为什么主流 coding agent 的代码检索用 ripgrep/grep 文本匹配,而不是 embedding 语义检索?给出至少四条机制层面的理由。**

- **人话版**:因为代码检索的查询天然是词法的——你在找一个符号名、一个报错字符串、一个字面量,精确匹配就是正确答案;而且 agent 是多轮循环,一次没找对可以收窄再找,不需要一次性语义召回兜底。加索引反而引入构建、同步、失步一堆成本。
- **展开**:
  - **①查询天然精确**:报错里的 `PaymentFailedError`、要改的函数名——这些必须逐字匹配,embedding 反而可能"语义近似"到错的符号上。Cline 的官方博客(2025-05)把这总结为"grep 的精确匹配天然适合迭代收窄"。
  - **②无索引成本**:embedding 索引要构建、要在代码变更后同步、失步了还会把 agent 带沟里;ripgrep 冷启动即用,gitignore 规则天然复用。对频繁变更的代码库,这是决定性优势。
  - **③agent 会迭代收窄**:glob→grep→read 的多轮循环,每轮都能根据上一轮结果调整——一次性 top-k 语义召回的边际价值被这个循环吃掉了。Sourcegraph 的大规模统计(2026-05,1281 次 agent 运行)印证:agent 的工具调用里 keyword search 约 8000 次 vs semantic search 约 2400 次——agent 天然倾向最简单可用的词法工具。
  - **④静态注入有反作用**:索引召回的内容是"一次性塞进上下文的静态上下文",和任务无关的部分改变 agent 的注意力轨迹(Cline 称之为失焦风险);而且不可逆地消耗上下文预算。
  - **⑤行业风向的活证据**:Cursor 2024 年的文档还是全仓文件 embedding 索引,2025-11 转向自研 Instant Grep("比 ripgrep 更快"),2026 年文档里 embedding 索引页已下线,只剩 grep+Explore subagent——一线厂商用产品路线给这个判断投了票。
- **及格线**:四条理由,至少一条能展开到机制。
- **加分**:引用 Sourcegraph 的调用分布统计或 Cursor 的转向;指出"这是循环结构带来的红利"这个本质。
- **红旗**:默认"语义检索更先进所以该用";不知道索引失步问题。

**Q3.12【八股·L3·平台】给 coding agent 设计 Grep/Glob 工具,Claude Code 的实现里有哪些值得抄的细节?每个细节防的是什么坑?**

- **人话版**:Grep 建在 ripgrep 上;输出带文件名+行号;offset 分页且越界返回"该偏移无结果";非法正则把 ripgrep 的原始报错回传;grep/rg 退出码 1 解释为"无匹配"不是命令失败。每个细节都在防 agent 的某类误判。
- **展开**(以下均来自 Claude Code 官方文档,2026-08):
  - **基于 ripgrep 而不是 POSIX grep**:ripgrep 的正则语法更强、速度快、默认尊重 .gitignore(要搜被忽略的文件得显式传路径——防止把 node_modules 搜进来)。
  - **输出=文件+行号+匹配行**:让模型能直接定位、能引用 `file:line`、能决定下一步是 read 还是收窄 glob。信息密度刚好,不倒整个文件。
  - **offset 越界返回"No entries at this offset"**:官方明说这是为了让模型"放宽或重置 offset,而不是得出'模式不存在'的结论"——分页边界是 agent 误判"没找到"的高发点,这个措辞是精心设计的纠偏。
  - **非法正则回传 ripgrep 的诊断信息**:模型看到具体报错就能自己改正则再搜——错误信息即纠错信号,和"返回裸 traceback"的反例形成对照。
  - **退出码 1=无匹配而非失败**(v2.1.196 起):防止模型把"没搜到"当成"工具坏了"去重试或换工具。
  - **配套参数**:`glob`(限定 `**/*.tsx`)、`type`(按语言)收窄范围;`multiline` 支持跨行匹配。
- **及格线**:说出 4 个细节里的 3 个。
- **加分**:每个细节都能说出"防什么误判"(这是设计意图题的核心);能对比自己用过的其他 agent 工具的差异。
- **红旗**:以为工具设计就是"能调 grep 就行";没想过输出格式对 agent 行为的影响。

**Q3.13【原理·L3·架构】aider 的 repo map 是怎么把百万行仓库压进约 1k token 的?为什么用图排序而不是 embedding 排序?**

- **人话版**:tree-sitter 把每个文件解析出"定义了什么、引用了什么"(函数/类签名级别),以文件为节点、引用关系为边构图,跑 PageRank 选出最重要的定义,在 token 预算内输出一棵"文件树+签名"的地图。用图不用 embedding,是因为"重要性"来自结构(被引用多的文件重要),这个信号是精确的、可解释的、不用维护索引。
- **展开**:
  - **三步流水线**:tree-sitter 提取 definitions/references(按语言的 tags 规则,拿的是签名不是实现体)→ 建图(文件为节点,引用为边,权重=引用次数)→ personalized PageRank 排序(可以给"已在对话里的文件"加权,让地图偏向当前任务的相关区域),按 `--map-tokens`(默认 1k)截断输出。
  - **为什么不用 embedding**:其一,目标是"排序重要性"不是"语义相似"——PageRank 在引用图上直接给出结构重要性;其二,免索引免同步,repo map 每次按需生成,和 grep 一样冷启动即用;其三,可解释——地图上每个条目都是真实签名,模型引用时零幻觉。
  - **地图的用法**:作为"结构索引"常驻上下文,agent 看地图决定打开哪个文件——先结构后内容的导航层。有趣的边界:aider 对弱模型默认关掉 repo map(会被 map 干扰去改它)——上下文工程没有免费午餐,再好的信号也要匹配模型能力。
- **及格线**:tree-sitter+图+PageRank 三步。
- **加分**:personalized 加权的设计意图;弱模型关 map 的取舍;"排序≠语义相似"的辨析。
- **红旗**:把 repo map 描述成"代码的 embedding 索引"(完全搞反)。

**Q3.14【原理·L4·架构】什么时候该给 agent 补语义/索引检索?"文本优先、规模触发混合"的具体判据是什么?**

- **人话版**:小中仓库(<40 万行)纯 grep 就好,加索引反而降分;超大仓库、跨仓库、自然语言意图找代码时,语义/结构化检索开始有真实增益。判据用数据说话,不用信仰。
- **展开**:
  - **规模的量化证据**(Sourcegraph CodeScaleBench,2026-05):40+ 大型仓库、1281 次 agent 运行——小于 400K LOC 时加索引/检索工具平均 -0.080 分(负收益!),400K–2M LOC 时 +0.259;多仓库任务索引工具 F1 +0.209,单仓库仅 +0.085。结论:语义/索引检索的价值随规模非线性增长。
  - **grep 的真实短板**(同研究的具体案例):搜 "allocate" 返回 47 个匹配、无结构排序——agent 要逐个读;而 find-references 返回"1 个定义+12 个调用点,按角色排序"。同义不同词、词同义异的情况 embedding 有优势;超大库逐文件追 import 会"分支爆炸"(每步都合理,整体走向迷路)。
  - **混合的落地形态**:三层并存(keyword/semantic/deep search)——日常 grep,语义层处理自然语言意图和超大库,把"搜索循环"整体下沉到工具里(如 Code Finder:给它任务自己搜,只回传文件+行范围+说明,比 agent 逐工具调用快 2 倍、省 40%)。Kubernetes 级仓库(140 万行 Go)的对照:纯本地工具 6000 秒 0 分 vs 索引工具 89 秒 0.90——规模到了,索引是刚需。
  - **判断顺序**:先问仓库规模和跨仓需求,再问查询分布(自然语言意图多不多),最后才轮到"要不要建索引"——多数团队卡在没问前两问就上了向量库。
- **及格线**:规模分界+两个短板案例。
- **加分**:引用量化数字;Code Finder 这种"搜索循环工具化"的新形态;能给自己团队的仓库算这笔账。
- **红旗**:中小仓库上也标配 embedding 检索(为简历驱动架构);或者反向一刀切"语义检索无用"。

**Q3.15【原理·L3·平台】coding agent 的 websearch 工具怎么设计才能不污染上下文?对比 Claude Code 的 WebSearch/WebFetch 机制。**

- **人话版**:搜索结果只给标题+URL 不给正文;抓网页时先把 HTML 转 Markdown,再用一个小模型按你的提取 prompt 抽答案——主模型看到的是"答案"不是原始页面。官方自己说这"design 上就是有损的",有损是特性:防的就是整页噪声灌进上下文。
- **展开**(Claude Code 官方文档,2026-08):
  - **WebSearch 的克制**:返回 result titles + URLs,不抓正文——读哪篇是模型的下一步决策,信息分级进入。每 session 上限 200 次(主对话+所有 subagent 共享),到限返回提示让模型"用手头信息继续"而不是报错——预算约束软化处理,防任务崩断。
  - **WebFetch 的三层防污染**:HTML→Markdown 转换(内容协商直接要 Markdown);小快模型按提取 prompt 处理内容,主模型只收该模型的回答(而不是原始页);跨域重定向不自动跟随(返回重定向信息让模型显式发起第二次 fetch——每一步网络动作都在模型的显式决策里,也是安全设计)。
  - **配套**:15 分钟响应缓存(同样的 fetch 不重复花钱);域名白名单/ask 权限规则;User-Agent 标识。Gemini CLI 的同思路:web_search 走 Google grounding,API 端先处理搜索结果再回传"合成摘要+引用 URI"。
  - **通用原则**:上下文预算优先于召回完整性——宁可两次请求拿精确信息,不要一次塞十页噪声。这条原则和 agent 检索用 grep(精确优于语义)、工具返回给 concise 档,是同一个设计观。
- **及格线**:小模型抽取层+搜索不带正文两层。
- **加分**:重定向显式化的安全含义;预算到限的软化处理;能把三处(搜索/抓取/工具返回)的设计统一到"上下文预算"原则下。
- **红旗**:把搜索引擎结果页原文直接塞上下文;不知道"有损是特性"。

**Q3.16【原理·L3·用户】agent 拿到一个 `TypeError: ...PaymentFailedError ...` 的报错后,理想的检索路径是什么?"先结构后内容"和"错误驱动"分别怎么体现?**

- **人话版**:先用报错里的符号名 grep 定位定义和抛出点,读上下文,顺着调用链(引用)找数据从哪来;卡住了再看目录结构/模块图决定换方向。错误信息是最好的检索 query——它带着精确符号。
- **展开**:
  - **错误驱动**:报错串本身就是高精度 query——符号名、文件名、行号都在里面。先 grep 符号(定义处+抛出处),这是命中率最高的一步;比"让 agent 泛泛浏览模块"快一个量级。
  - **先结构后内容**:当错误信息不够(比如逻辑错误没报错),先看结构——目录树/模块划分/repo map——锁定嫌疑模块再下钻内容;避免一上来全文搜索淹没在无关匹配里。Claude Code 官方工作流(explore→plan→code→commit)的 explore 段就是"先宽问题后窄领域"。
  - **防两个失败模式**:一是"无限探索"——agent 读了几百个文件填满上下文还没到正题(官方反模式警告:收窄调查范围或用 subagent 隔离);二是"分支爆炸"——顺着 import 逐文件追,每步都合理、整体迷路(Sourcegraph 的观察),这时该换 find-references 类的结构化导航而不是继续线性追。
  - **工程配套**:Glob 找文件、Grep 找内容、Read 读文件的三件套是标配;探索型任务交给 Explore subagent(独立上下文里烧 token,只回传结论)——主上下文保干净。
- **及格线**:错误驱动 grep 符号+结构兜底两步。
- **加分**:两个失败模式(无限探索/分支爆炸)的识别与对策;subagent 隔离探索的用法。
- **红旗**:让 agent"把仓库读一遍理解一下"(上下文灾难);报错在手却让 agent 泛泛语义搜索。

**Q3.17【品味·L4·架构】"模型越强,工具越简单越词法化"——Cursor 从 embedding 索引(2024)走到 Instant Grep(2025-11),Anthropic 的 Tool Search 默认 regex+BM25。这个趋势你怎么解读?它的边界在哪?**

参考思路:
- **解读**:复杂度从基础设施移回 agent 循环。模型弱的时代,要靠索引/语义层"预消化"信息弥补模型能力;模型强了,它自己会在多轮循环里收窄,简单工具+强模型的组合更灵活、零索引维护、不会失步。词法工具的另一个红利是可解释可审计——grep 的结果你能复现,embedding 召回你说不清为什么。
- **边界(反例同样真实)**:规模——超大仓库/跨仓库场景,索引工具的增益有硬数据支撑(见 Q3.14),复杂度又沉回基础设施(只是形态从"索引"变成"带搜索循环的工具"如 Code Finder);自然语言意图("处理支付重试的那段逻辑")没有精确词可 grep,语义层仍有位置。
- **判断**:这不是"索引已死",是复杂度的重新分配——默认路径简单化,规模触发再分层。给团队的建议:先把 grep/glob/read 的工具质量做扎实(多数团队这里都没做好),再谈要不要建检索底座。
- **及格线 / 加分 / 红旗**:及格=复杂度移回循环的解读+一个边界;加分=用双方证据论证;红旗=单边站队("索引全死"或"grep 万岁")。

---

## 3.4 品味与判断题

**Q3.18【品味·L3·平台】你的 agent 要接入内部 30 个系统,有人提议"全部包成 MCP server 一次挂上"。你怎么决策?**

参考思路:
- **反对全挂的三个理由**:工具过载(30–50 退化阈值)、定义 token 常驻成本、攻击面扩大 30 倍。另外按系统包装是"按组织结构切工具",不是按任务切——模型要的是工作流,不是你们公司的部门墙。
- **分层方案**:高频核心(文件/命令/代码)内置工具;按任务域拆 subagent,每个挂 3–10 个相关工具;低频长尾走 tool_search 延迟加载或 code execution(需要时现学 API)。
- **先问一句**:这个系统是"动作型"(MCP/CLI 合适)还是"数据型"(检索/过滤/聚合——code execution 更优)?30 个系统里通常一半都不值得包成工具。
- **及格线 / 加分 / 红旗**:及格=过载理由+分层方案;加分="动作型/数据型"的切分判断;红旗=全挂或全不接的二极管。

**Q3.19【品味·L4·架构】工具该粗粒度(一个工具一个工作流)还是细粒度(正交原子操作)?给一个判断函数。**

参考思路:
- **判断变量**:谁组合(模型组合→偏粗,给模型现成工作流;代码组合→可细,让 code execution 拼)、错误语义(中间态要不要可见/回滚——合规场景必须细粒度留痕)、上下文预算(每次调用的往返成本)、训练分布(模型对什么粒度的接口更熟——粗粒度工作流工具通常是更安全的默认)。
- **默认与例外**:对模型暴露粗粒度工作流工具,原子操作留给代码去组合;但审计/合规要求中间态可见时,粗粒度是风险。
- **正交解法**:粒度和信息量是两个维度——`response_format: concise|detailed` 这种枚举,不动粒度也能控信息量。
- **及格线 / 加分 / 红旗**:及格=判断函数有至少三个变量;加分=粒度/信息量正交的辨析;红旗=只有立场没有函数。

**Q3.20【品味·L3·安全】第三方 MCP server 的可信模型怎么建?**

参考思路:
- **默认不可信**:描述可投毒(poisoning)、可上线后改(rug pull)——把工具描述当可执行代码对待,它确实在指挥模型。
- **治理手段**:入库前审计配置;工具描述变更 diff 告警(rug pull 的探测器);凭证最小化(只读优先、per-server 隔离);网络出口限制;敏感系统自建不外采。
- **分工**:开源社区的 server 适合低敏感场景;核心系统(支付、生产数据)要么自建要么深度审计——信任决策和代码依赖管理同构,该抄 npm 时代攒下的供应链纪律。
- **及格线 / 加分 / 红旗**:及格=不可信默认+三件事;加分=与软件供应链治理的类比;红旗=按 star 数和 README 判断可信。

**Q3.21【品味·L4·架构】如果明天 MCP 消失,你账下哪些资产归零、哪些保留?这个答案如何影响今天的架构?**

参考思路:
- **归零**:server 封装代码、协议适配层、registry 接入。
- **保留**:工具的语义设计(名称/描述/返回格式——这些是 ACI 资产)、权限模型、审计日志、subagent 分组逻辑、延迟加载策略。协议是载体,接口语义是资产。
- **架构影响**:把聪明才智花在语义与治理上,不为协议特性做深度绑定;工具层留一个薄适配,协议换代时迁移成本最低。历史依据:协议一直在演(传输层、授权框架都换过代),但"怎么给模型设计好用的工具"这个知识没贬值过。
- **及格线 / 加分 / 红旗**:及格=分清载体与资产;加分=落到自己架构的隔离层设计;红旗=把职业身份绑定在某协议上。

---

## 3.5 开放性问题

**Q3.22【开放·L5·模型】如果让你重新发明工具调用协议,你会改什么?**

讨论框架:
- **结构化输出的原生性**:现在的工具参数还是"JSON 文本层",能否把 schema 约束下沉到解码层之外,让工具调用成为模型的一等输出模态(不是文本的特殊格式)。
- **渐进披露作为协议原语**:tool_search 这类"先名字后定义"的两级结构,应该是协议内建能力而不是应用层技巧。
- **错误语义强类型化**:现在错误是字符串;协议级定义错误类别(可重试/需改参数/需授权),模型能据此走不同分支。
- **权限声明内嵌 schema**:工具自带"这个调用会花钱/会外发数据"的标注,权限系统按标注策略化审批。
- **批判点**:每一条都要问"这是协议该做的还是模型该学会的"——过度协议化会把智能从模型挪回代码(开倒车)。

**Q3.23【开放·L4·架构】Code execution 会不会吞掉 MCP?什么会幸存?**

讨论框架:
- **吞不掉的**:发现(server 怎么被找到)、鉴权(OAuth 流程)、跨 host 复用、权限声明——这些是协议层的网络效应,代码执行不提供。
- **会被吞的**:细粒度数据查询类工具——"取数据过滤后给我"这类,写代码调 API 比逐工具调用高效得多。
- **可能的终局**:MCP 做目录与授权层(你能用什么),执行层让代码完成(你怎么用)——2026 年主流厂商的形态已经在朝这个方向收敛。
- **追问**:数十万并发沙箱的供给成本谁来解决(执行环境本身是重基础设施)。

---

## 3.6 工程实战题

**Q3.24【实战·L3·平台】为内部"发布系统"(REST API 40 个端点)设计 agent 工具集:工具清单(≤10)、返回设计、不暴露的端点及理由。**

参考思路:
- **工具清单(按工作流合并)**:`list_releases(status)`、`get_release_health(id→语义化摘要而非原始 JSON)`、`create_release(dry_run 参数默认 true)`、`rollback_release(需 approval 标记)`、`search_incidents(query)`——五个够了,40 个端点里多数是实现细节不是模型接口。
- **返回设计**:内部 id 换成语义名;concise|detailed 枚举;错误带"下一步建议"(如"该版本已归档,先调用 X 恢复");分页默认值。
- **不暴露的**:原始 CRUD(模型误操作面大)、级联删除、凭证管理——这些走人工或独立通道;判断标准:错误是否可逆+影响面是否可控。
- **工具描述示例**:按"给初级工程师的 docstring"写——用途一句、何时不用一句、参数含义、返回结构、一个调用示例。
- **及格线 / 加分 / 红旗**:及格=合并思路+5–8 个工具;加分=dry_run/approval 这类安全默认的设计;红旗=40 个端点 40 个工具的直接映射。

**Q3.25【实战·L3·平台】不依赖厂商特性,自己实现 tool_search 式延迟加载的最小可行版。**

参考思路:
- **注册表**:常驻上下文只有 name+一句话摘要(50 个工具约几百 token);完整定义存在本地。
- **元工具**:`search_tools(query)` 返回 top-k 候选(标题+摘要),模型选中后再调 `get_tool_definition(name)` 或直接在下轮请求动态注入。检索用 BM25/正则就够(Anthropic 官方默认也是 regex+BM25,不是 embedding)。
- **缓存细节(容易翻车处)**:动态注入的位置必须在尾部消息或子代理上下文——注入到 tools 定义会破坏稳定前缀,每搜一次缓存全失效。命中过的定义本会话缓存复用。
- **评估**:记录命中率、工具选择准确率、单任务 token,和全量注入对比——用数据证明省了多少、准确率降没降。
- **及格线 / 加分 / 红旗**:及格=注册表+元工具+注入位置正确;加分=缓存破坏的规避与评估闭环;红旗=把完整定义动态塞进 tools 数组(缓存杀手)。

**Q3.26【实战·L4·平台】设计对不可靠外部 API 的工具封装:API 经常超时、返回半截 JSON、限流。**

参考思路:
- **工具侧做稳定**:重试/退避/超时预算在封装层做掉——这些是确定性代码擅长的事,别让模型操心。
- **返回给模型的是语义化错误**:"服务暂不可用,已重试 2 次;可等 60s 再试,或改用 X 通道"——模型能据此决策;绝不让模型看原始 traceback(它修不了网络,只会瞎试)。
- **半截数据处理**:要么截断+明确标注"数据不完整,缺 N 条",要么不返回——模糊的半截数据会诱发模型基于错误信息推理。
- **熔断**:连续失败 N 次工具层直接拒绝并建议改道;附监控(错误率、重试成功率)反馈给 API owner。
- **及格线 / 加分 / 红旗**:及格=语义化错误+重试内聚;加分=半截数据的两难处理与熔断;红旗=把超时异常直接抛给模型。

---

## 3.7 场景排错题

**Q3.27【排错·L2·用户】agent 总是"忘记"用你配置的某个工具,转而用 bash 硬凑。排查顺序?**

排查链路:
- **①描述问题**:名字看不出用途、没写"何时用何时不用"、描述太短——模型选工具全靠描述,描述烂等于工具不存在。
- **②语义重叠**:有没有另一个工具干类似的事——两个重叠工具会让模型摇摆,常输的那个就是你以为没被用的。
- **③过载**:总工具数是否已过 30–50 的退化区。
- **④加载与权限**:工具真的被加载了吗(白名单/配置错误经常是根因);上一次用它是不是吃过亏(返回太差,模型"学会了"绕开)。
- **⑤最后才是 prompt 引导**:显式点名"处理 X 用工具 Y"——这是补丁不是修复,前四步查完再上。
- **及格线 / 加分 / 红旗**:及格=按顺序排查描述/重叠/过载;加分=指出"模型学会绕开差工具"这个习得行为;红旗=直接写进 system prompt 强制要求(症状掩盖)。

**Q3.28【排错·L3·平台】某 MCP server 更新后,agent 开始偶发把用户数据发到外部端点。应急处置与根因?**

排查链路:
- **应急三连**:摘除 server、吊销它的凭证、审计泄漏范围(哪段时间哪些请求出去了)。
- **根因对比**:拉更新前后的工具描述与实现 diff——rug pull(上线后描述被改,注入了外发指令)或 tool poisoning(描述里一直藏着休眠指令,这次触发了条件);检查 server 有没有 sampling 能力被滥用;网络出口是否本来就该限制(白名单没配等于裸奔)。
- **长期治理**:server 配置版本化入库(更新走 review);工具描述变更 diff 告警;沙箱网络白名单;敏感数据分级(见 ch08)。
- **及格线 / 加分 / 红旗**:及格=应急+diff 排查;加分=描述 diff 告警这类前置探测;红旗=只做应急不建治理,下次换个 server 再来一遍。

**Q3.29【排错·L3·平台】agent 调工具时参数频繁填错(枚举拼错、id 传成 name)。是模型问题还是接口问题?**

排查链路:
- **先查接口(多数根因在这)**:枚举值是不是语义化的(内部码 `st_03` vs 自然语言 `archived`);参数名有歧义(`user` 是 id 还是名字?);返回值里有没有给过正确的 id 形态(从没见过正确格式,模型只能编)。
- **再加约束**:schema 用 strict 约束解码锁死枚举;参数描述里给一个正确示例。
- **接口改造**:返回里 id+语义名成对;错误信息里带正确格式的示例(见 Q3.7/Q3.8 的 ACI 原则)。
- **判断标准**:同类错误反复出现在不同模型上=接口问题;只在弱模型上出现=能力问题(换模型或简化接口)。
- **及格线 / 加分 / 红旗**:及格=先查接口的三项;加分=strict 解码+成对返回的修复;红旗=归因模型然后换更贵的模型硬扛。
