# ch02 · 上下文工程、缓存与检索(重点章)

> 覆盖:context engineering 的定义与落地、上下文构成、context rot、prompt caching(前缀匹配/KV cache)、稳定前缀设计、compaction 与工具结果清理、agentic RAG(检索作为工具、迭代检索、query 改写、路由、Self-RAG/CRAG)、RAG vs 长上下文 vs agentic search 的选型。
> 案例基线:2026-09。机制类知识(缓存原理、压缩策略)不随时间变化;带日期的案例以标注为准。
> 答案读法:**加粗句连读就是可背的骨架**;正文按工程师语域写——通用工程词(前缀、缓存、命中、序列化)直接用,**领域黑话(prompt caching、compaction、context rot 这类)随用随释**,只在括号里给一句解释;**宁可写长讲透,不压缩到失真**。
> 术语白话速查:**token**=按字数计费的用量单位,约等于半个到一个汉字;**上下文窗口**=模型一次能"记住"的内容上限,超了就得删/压缩;**prompt caching**=厂商对"重复发送的旧内容"打折(约 1 折)省钱的机制;**KV cache**=推理时跳过重复计算的底层缓存;**前缀/稳定前缀**=请求里从头开始逐字不变的那段,缓存按它匹配,一动全失效;**system prompt**=请求里放"系统级指令"的位置;**tools 定义**=告诉模型有哪些工具可用的说明书,排在请求最前;**compaction(压缩)**=把聊了很久的对话总结成一页摘要、扔掉原文继续;**context rot**=上下文越长模型记事越不准的衰减现象;**RAG**=先去资料库里检索再回答;**embedding**=把文字变成向量做"语义相似"检索;**repo map**=仓库结构地图;**TTL**=缓存保留时长;**迭代检索**=搜一轮看结果再决定搜什么的多轮检索。

---

## 2.1 八股题(基础概念)

**Q2.1【八股·L1·平台】给 context engineering 下一个工作定义,并说说它和 prompt engineering 的关系。**

- **人话版**:prompt engineering 琢磨"这句话怎么措辞",context engineering 琢磨"模型干活时该看到哪些东西"——前者是后者的子集。我平时的时间分配大概二八开:两成调 prompt,八成管上下文。
- **主线(照这条线讲)**:
  1. **先给行业收敛的定义**——Anthropic(2025-09《Effective context engineering for AI agents》)的说法:策划什么进入有限上下文窗口的艺术与科学,目标是**找到能最大化任务达成概率的最小高信号 token 集**;LangChain 那边的版本是"动态系统,在正确的时机、以正确的格式提供正确的信息和工具"。两家口径殊途同归:管理对象是信息流,不是一段话。这个定义值得逐词拆:"有限窗口"意味着这是预算问题(硬约束),"高信号"意味着判据是信噪比而不是信息量,"最大化任务达成概率"意味着最终以结果验收——给模型塞了一堆"感觉会有用"的资料然后任务失败了,在 context engineering 的口径里是失败,不是"信息给全了、尽力了"。
  2. **再讲为什么它取代了 prompt engineering 的中心地位**——单轮对话时代你只控制一段话,措辞就是全部杠杆;agent 时代每一轮的上下文都是动态拼出来的:工具定义、工具结果、检索内容、记忆文件、子代理摘要、压缩后的历史。prompt 只占其中一小块。量级很说明问题:一个典型 agent 会话里,system prompt 加工具定义可能三万 token,历史加工具结果十几万,而你亲手写的那段指令两三千——**在 2K 上雕花、放着 130K 不管**,这就是继续把 prompt engineering 当主战场的实际处境。
  3. **核心是控制信噪比而不是多喂**——每多一段无关内容,代价不只是 token 费。质量上,注意力被稀释(context rot,见 Q2.6):模型对每段内容的利用率随总长度下降;行为上,无关内容会改变 agent 的轨迹——历史里躺着一份过期设计文档,agent 真的会去读它并按它行动,污染是行为性的,不只是经济性的。所以上下文工程的日常是做减法:删过期文档、截断工具输出、把大块内容外置成路径、几轮后清掉旧工具结果。
  4. **收尾给一个具体动作证明你真做过**——比如"我们把 system prompt 里的动态配置挪到尾部注入,缓存命中率从 60% 提到 90%,agent 行为反而更稳"——用一件事同时体现经济学和质量两个面。反向边界也要说:减法的判据是"与当前任务的信号相关度",不是字数——关键约束(验收标准、禁区)一个字都不能省,砍的是噪声不是规则。
- **补充弹药(追问再掏)**:
  - **时间线**:Harrison Chase 2025 年中提出 context engineering 叙事,Anthropic 2025-09 工程博客把它定型,2026 年已成 JD 常规词。
  - **反例**:把 context engineering 等同于 RAG——检索只是往上下文放内容的一种方式,清理与分层同样是它;面试官爱用这个陷阱筛"只会背名词"的人。
  - **相邻概念**:与 OpenAI 2026-06 提的 harness engineering 是互补切面——context 管"模型看到什么",harness 管"模型用什么姿势干活"(工具、验证、恢复);能主动把两家口径摆成一张图更出彩。
- **及格线**:能说出"上下文=推理时模型可见的全部 token,含工具定义和历史",并说清 prompt 只是子集。
- **加分**:引用"最小高信号 token 集"目标函数;有自己的做减法案例。
- **红旗**:等同于一回事或就是 RAG;认为"多喂点总没错"。

**Q2.2【八股·L1·平台】一个 coding agent 每轮请求的上下文由哪几部分构成?为什么"每轮重发全部历史"是 agent 成本模型的基石?**

- **人话版**:五块——system prompt、工具定义、记忆文件、对话历史、工具结果(文件内容/命令输出/搜索结果)。HTTP 和模型都是无状态的,所以每轮都把前面所有东西原样再发一遍。这一条推出后面所有经济账。
- **主线(照这条线讲)**:
  1. **五块构成报一遍,顺手给量级**——以 Claude Code 为例(社区对泄露 system prompt 的分析口径):system prompt 是数百条字符串动态拼装、万 token 级;每挂一个 MCP server,工具定义多吃几千到几万 token;工具输出是最大不定项,一条 npm install 日志就能吃上万 token。还有个视角转换值得主动点破:五块里只有"对话历史"是用户语义上的一部分,其余四块全是工程侧拼进去的——**上下文的构成是设计决定,不是自然形成**,这也是它能被优化的前提。
  2. **解释"重发"的机制根源**——API 无状态,服务端不保存会话;每轮请求必须自带全部历史。无状态不是偷懒,是服务水平扩展的前提(任何节点都能接任何请求,不需要会话粘性)。代价由调用方承担:一个 30 轮会话,第 k 轮要重发前 k-1 轮的全部内容,累计输入近似平方级增长——画出来是个阶梯形梯形,不是线性叠加。**这个梯形是 agent 不同于单次调用的根本成本结构**,后面所有经济账都从它出发。
  3. **推出两个直接后果**——成本上,重发部分如果能按约 0.1× 计费,prompt caching 就成了第一杠杆(Q2.15 的算术题整个建立在这上面);质量上,历史无界增长撞的不只是计费,还有 context rot(Q2.6)——所以压缩(compaction)和检索(要用再取)是质量必需,不只是省钱手段。质量与成本在"历史不该无限重发"这一点上同源。
  4. **收尾点出设计推论**——任何"每轮都变的东西"(时间戳/随机 id/环境快照)只要放在请求头部,就把它之后的所有重发内容从缓存价打回原价——这直接引出稳定前缀纪律(Q2.3),是下一题的钩子。
- **补充弹药(追问再掏)**:
  - **工具定义也计费**——"工具列表挂着就收费"这一点很多人不知道,它直接支撑 ch03 的工具过载题。
  - **数字自检法**:让平台 dump 每轮请求的 usage 字段,看 input token 的逐轮增长曲线,一眼验证"梯形"——面试里说"我看过自己项目的 usage 曲线"立刻和背书的人拉开差距。
  - **输出侧对照**:重发讲的是输入;输出 token 按原价且单价更贵,写代码类任务输出占比高,两头都要看——但输入侧有缓存杠杆,优化优先级更高。
- **及格线**:至少四类构成+无状态→全量重发的因果。
- **加分**:报得出自己项目里各块的实际占比;主动提"梯形累积"。
- **红旗**:以为历史只在第一轮发;不知道工具定义计费。

**Q2.3【八股·L1·用户】什么是稳定前缀(stable prefix)?为什么改一行工具定义是缓存上最贵的操作?**

- **人话版**:请求里从头开始、跨多轮逐字不变的那段——工具定义+system prompt+早期历史。所有厂商的缓存都按前缀匹配,从第一个不同的 token 起全部作废;工具定义排最前面,所以动它等于全清零。
- **主线(照这条线讲)**:
  1. **先讲前缀匹配的机械原理**——缓存系统按 token 块做哈希、逐块比对:前缀一致就复用 KV cache(KV cache=推理时把算过的中间结果留在显存里避免重算的底层机制)、按约 1/10 价计费;某块不同,该块及之后所有块全部失效、按原价重算。关键是"一变全废"是级联的,不是局部的——改第 100 个 token,第 100 个之后的一百万个 token 陪着重算,哪怕它们一个字没变。理解了级联,后面所有缓存纪律都是它的推论。
  2. **再讲 agent loop 为什么天然适合缓存**——每轮只往尾部追加新消息,前缀单调不变。理想状态下每轮只有新增部分按原价,历史全部 0.1×——这是长会话在商业上还能负担的根本原因。反过来说,**agent 本来是缓存友好的,前提是别把动态内容塞进头部**——把天然优势亲手拆掉,是自研 agent 最常见的初学者事故。
  3. **然后解释为什么 tools 定义最致命**——三家厂商的缓存层级都是 tools→system→messages,tools 在最上游。增删一个工具、改一个描述、甚至只改变工具数组顺序,tools 层哈希先变,三层缓存一起作废。一个字的工作量,全量重算的代价——"改一行工具定义"因此成为缓存上单价最高的一行。
  4. **对照收尾**——在最新一条消息里改内容几乎零成本(尾部追加不碰前缀);在 system prompt 里加个时间戳,效果和改工具定义同级——都是"把动态内容放进头部"的事故。这就是尾部注入模式存在的原因(Q2.10)。
- **补充弹药(追问再掏)**:
  - **真实事故形态**:MCP server 按启动顺序注册工具,轮间顺序抖动——工具没增删,数组顺序变了照样 miss;这类"什么都没改却 miss 了"的案子,九成出在序列化层。
  - **自查动作**:相邻两轮请求体做 diff,miss 的精确断点位置一眼可见——先定位再修,是缓存排错的总纲(Q2.36 展开)。
  - **协议层的官方解法(2026-09)**:GPT-6 Astra 支持 configuration_update 输入项,会话中途调 reasoning effort **不破坏已缓存前缀**(官方 latest-model 指南)——"变的部分"与"缓存的部分"在协议层分离;该家模型上的尾部注入绕法可升级为正式机制,见 ch09 Q9.29。
- **及格线**:前缀匹配+"从分歧点级联失效";知道 tools 在最前。
- **加分**:有自己踩坑的实例;主动把"时间戳进 system"归为同类事故。
- **红旗**:以为"内容相似就命中"——必须逐 token 相同。

**Q2.4【八股·L2·用户】Claude Code 的自动压缩(auto-compact)大致怎么工作?压缩时保什么、丢什么?**

- **人话版**:接近上下文上限时先做轻清理——把较老的工具输出换成占位符;不够再做重压缩——把整个会话摘要重写。决策和结论保留,原文和过程丢弃;最近改过的文件重读,大文件只留路径。
- **主线(照这条线讲)**:
  1. **两段式先分清**——第一段清旧 tool_result(社区叫 microcompact),价值衰减最快的内容先走:三轮前的命令输出,今天再看的价值趋近于零;第二段才是对整个会话的摘要(compaction)。触发点默认在约 92–95% 窗口占用,给收尾留一点余量。先轻后重的顺序本身就是设计:能用便宜手段撑住,就不动用有损大招。
  2. **保留策略讲逻辑而不是背清单**——Claude Code 保 system prompt、CLAUDE.md、当前 plan,重读最近编辑过的少数文件(超几 K 的只留路径);丢全部 verbatim 工具输出与中间思考。背后的判断标准是**重建成本**:文件可以随时重读所以只留路径——重读一次几秒;用户三小时前说的约束没法重读,原始信源已经下线,所以必须进摘要。用这一个判据,清单不用背也能自己推出来。
  3. **可定制与硬限制**——`/compact` 可带保留指令("保留所有 API 兼容性决策"),指令可常驻 CLAUDE.md 变成默认策略;反复触发压缩会撞 thrashing 检测直接报错。thrashing 不是 bug 是产品立场:压缩刚做完新内容又把窗口填满,说明这个会话的任务粒度已经超出单个上下文该承载的规模,产品在逼你开新会话、把状态外置——**把"窗口不够"翻译成"该换工作方式"**。
  4. **收尾上升到原则**——压缩是有损 GC(垃圾回收):能回收的回收,不能回收的别指望回收器保真。关键结论要外置成 todo/进度文件这类结构性载体:它们每轮完整重注入、不经过有损摘要——不赌摘要的保真度(这条原则接 Q2.13 和 ch06)。
- **补充弹药(追问再掏)**:
  - **观察方法**:压缩边界后看 agent 第一轮做什么——重读哪些文件、复述什么、问什么,能反推摘要实际保住了什么;比读文档更接近真相。
  - **对照**:Codex 的压缩把摘要插在最后一条真实用户消息之前,因为模型被训练把摘要当历史不当新指令——细节见 Q2.13。
  - **触发前的体感信号**:agent 开始重复读同一批文件、忘记早前决定——高窗口占用下 rot 先于压缩生效,别等系统动压缩才意识到该整理。
- **及格线**:两段式+触发时机+"文件只留路径"。
- **加分**:讲出"重建成本"这个保留判据;主动把结论外置当原则讲。
- **红旗**:以为压缩无损;以为窗口够大就不用管(context rot 远早于上限就生效)。

**Q2.5【八股·L2·用户】什么是工具结果清理(tool result clearing)?从写入到驻留,有几个可以动手的层次?**

- **人话版**:工具输出是上下文最大的污染源——一万行日志看第二眼的价值是零,但它一直躺在历史里,每轮重发、持续稀释注意力。治理有四个时机:进上下文之前过滤、写入时截断、驻留期换占位符、极端情况整段隔离。
- **主线(照这条线讲)**:
  1. **写入前过滤(最便宜)**——PreToolUse/PostToolUse hook 或中间件层把一万行日志裁成几百 token 的关键行(错误优先、头尾保留)。信息还没进上下文就被裁,零驻留成本,也不存在"事后找回"的需求缺口——**越靠前的清理越便宜**,这个位置排序是本题的骨架。例:构建日志只保留 error/warning 行加首尾各 20 行。
  2. **写入时截断**——默认上限(常见 25K token 级)、超长文件只给路径加首尾。简单粗暴,但要留"要看原文再说一次"的逃生通道:占位文本写清"已截断,可用 read(offset) 续读"——否则排错时正好截掉现场,agent 拿着半截报错瞎猜。
  3. **驻留期清理**——超过 N 轮的旧 tool_result 替换成占位文本("原始输出已清理,可重新调用")。Anthropic 的 context editing API 就是这层的产品化,默认保留最近 3 组。它与前两层的区别:前两层管"进来多少",这层管"留下多久"——多数团队只做了截断,让三个月前的日志陪跑到会话结束。
  4. **源头隔离(极端形态)**——让 subagent 在自己窗口里烧掉几万 token 的搜索噪声,只回传 1–2K 摘要——"清理"的最高形态是根本没让它进主上下文。判断该用哪层:输出大但主任务只需要结论的,直接上隔离;输出本身是后续步骤输入的,只能过滤+截断。
- **补充弹药(追问再掏)**:
  - **缓存联动**:清理会打断缓存(清理点之后全失效),所以要批量清——这是 context editing 设计 clear_at_least 参数的原因(详见 Q2.14)。
  - **排错保留**:占位符要可追溯可重取,否则 agent 排错时"想看原始报错"拿不到;清理策略要给排错场景开白名单(失败任务的最近输出不清理)。
  - **度量**:按工具统计"输出 token 占比"——通常两三种工具贡献 80% 以上的工具 token,治理优先级一眼排出。
- **及格线**:四个层次说出三个,各配一个例子。
- **加分**:主动讲"批量清理优于涓流清理"的缓存原因。
- **红旗**:只想到截断一层;不知道旧结果还在持续稀释注意力。

**Q2.6【八股·L1·模型】什么是 context rot / lost in the middle?对"堆上下文"的做法意味着什么?**

- **人话版**:上下文越长,模型对每一段的利用率越低。经典实验:关键信息放开头或结尾效果好,放中间明显变差(U 型曲线)。它是缓慢滑坡,不是到某长度断崖。
- **主线(照这条线讲)**:
  1. **先摆两个标志性实证**——Lost in the Middle(Liu et al., TACL 2023):多文档 QA 里金标段放中间时准确率显著低于放开头结尾,甚至低于不给上下文——多给的"帮助"是负数;Context Rot(Salesforce,2024):按工作记忆/检索/推理三维度分解,极简任务也随长度系统性退化,多维复合时恶化超过单项之和。两个实验一个给空间分布(中间最惨),一个给长度效应(越长越惨),互相咬合。
  2. **再讲三条机制解释**(Anthropic 口径)——n 个 token 产生 n² 个注意力对,每 token 分到的注意力预算被稀释;训练数据偏短,模型没学过超长范围的精确间接寻址("第 40 页表格里的那个数字");位置编码插值在远距离失真。三个因素叠加,所以是梯度下滑而不是悬崖——**没有一条是"修个 bug"能解决的,它们是架构级的**。
  3. **然后是工程换算**——"多比少好"在超过几十 K 后反转:堆上下文的质量成本(注意力稀释)开始超过它省下的检索往返。有效工作内存按十几 K 到几十 K 估计,比按标称窗口估计安全(实证支持见 Q2.12 NoLiMa)。给个直觉:标称 1M 的窗口,当 50K 用是工程,当 500K 用是赌博。
  4. **收尾落到设计立场**——上下文是稀缺资源,该按"质量预算"管理而不只按"长度预算":rot 之下,长度预算花满的那一刻质量已经开始亏。压缩与检索因此不是省钱手段,是质量手段——这句话把本章所有工具的定位一口气立住。
- **补充弹药(追问再掏)**:
  - **一句话记忆**:"上下文变长,模型能关注的反而变少"——Chroma 的原话,可以直接引用。
  - **自己复现的方法**:把关键约束分别放在会话开头/中段,对比 agent 后续遵守率——低成本探针,半天出结果。
  - **与缓存的分工**:rot 是质量问题、缓存是经济问题——两个独立理由指向同一动作(少而精),面试里分开讲再合流,结构感立刻出来。
- **及格线**:U 型曲线+"滑坡不是悬崖"。
- **加分**:三条机制解释;质量预算的管理立场。
- **红旗**:拿"窗口 1M"当"能用好 1M"设计系统。

---

## 2.2 原理深挖(缓存与压缩机制)

**Q2.7【原理·L2·平台】Anthropic / OpenAI / Gemini 三家的 prompt caching 机制有什么差异?设计上的选型影响是什么?**

- **人话版**:三种风格——Anthropic 显式打点(你自己标缓存断点),OpenAI 全自动(前缀一样自动命中),Gemini 双轨(默认隐式+可选显式缓存但付存储费)。
- **主线(照这条线讲)**:
  1. **Anthropic 显式断点**——`cache_control` 标记、每请求最多 4 个断点、层级固定 tools→system→messages;前缀要求 100% 逐 token 相同;TTL 5 分钟(默认)或 1 小时(可选);命中读 0.1×,写入付 1.25×/2× 溢价;最短可缓存约 1K–4K token,低于下限**静默不缓存**。显式的好处是行为可预测——断点在哪、命中没命中,日志里看得见;代价是断点设计成为你的责任,打错位置等于白标。
  2. **OpenAI 全自动**——无需参数,1024 token 起、按 128-token 块匹配;折扣约 90%;保留 5–10 分钟(闲时可延长),另有长效选项;靠前缀哈希路由,prompt_cache_key 做会话粘性(同一 key 尽量路由到同一节点,命中率更稳)。省心,但命中率受服务端负载影响,不可控因素多一点——你的前缀纪律做得再好,也只能"不拖后腿",不能"保证命中"。
  3. **Gemini 双轨**——隐式缓存默认开(有最少 token 门槛,折扣 75–90%);显式 cachedContent 适合反复复用的大前缀,但按 token·小时付存储费——**把"缓存占显存"这件事从隐式的服务端成本,变成了显式的账单项**。
  4. **选型影响收尾**——轻会话三家差不多,不值得当选型依据;重前缀(大工具集+长 system)场景,显式断点的可控性 vs 全自动的省心各有拥趸,此时要看团队有没有能力维护断点纪律;离线批量反复用同一大前缀时(每晚全量知识库问答),Gemini 显式反而最便宜——存储费远低于反复 prefill;**TTL 要和业务节奏匹配**——人审间隔超过 5 分钟的场景,要么 1h TTL 要么接受 miss,这一条经常比折扣率更影响实际成本。
- **补充弹药(追问再掏)**:
  - **二阶细节**:Anthropic 命中不计速率限制;低于最短缓存长度不报错只是静默 miss——这两个是排查"为什么没命中"时的高频盲区。
  - **迁移注意**:换厂商时缓存策略不可移植——显式断点代码在自动缓存厂商上是死代码,自动缓存的前缀纪律在显式厂商上要补断点逻辑;架构上把"缓存策略"做成 provider 适配层,别散在业务代码里。
  - **时效注脚**:本领域价格与参数变化快,以上为 2026-09 口径,引用前复核。
- **及格线**:"显式打点 vs 自动 vs 双轨"三家各一句+0.1× 量级。
- **加分**:TTL 匹配业务节奏;静默不缓存这类二阶坑。
- **红旗**:把 prompt caching 和 KV cache 说成无关的两回事;以为 OpenAI 也要手动打点。

**Q2.8【原理·L3·模型】prompt caching 和 KV cache 是什么关系?厂商为什么愿意给缓存命中的部分打一折?**

- **人话版**:KV cache 是 Transformer 解码的计算优化——算过的 K/V 存下来,新 token 不用对全历史重算;厂商的 prompt caching 本质是把这份 KV cache 跨请求复用。命中时服务商几乎不付计算成本,折扣是省下计算的分成。
- **主线(照这条线讲)**:
  1. **先讲清 KV cache 本身**——自回归解码每生成一个新 token,都要对全部历史做 attention;KV cache 把每层已算出的 Key/Value 张量留在显存里,避免每步重算。代价是显存占用随上下文线性涨——这也是长上下文推理贵的根源之一:贵不只贵在算,先贵在"装得下"。
  2. **再讲跨请求复用怎么做**——推理引擎(vLLM/SGLang 一类)对请求的 token 块做哈希;发现新请求前缀与某份已缓存 KV 相同,跳过 prefill(预填充=对整段输入做的首次全量前向计算)直接进解码。**prefill 恰是长上下文最贵的部分**:它是算力密集的全量矩阵运算,百万 token 的 prefill 要几十秒——TTFT(首字延迟)主要花在这;这就是 Q2.36 里"40–60 秒才吐字"是缓存全 miss 指纹的原因。前缀命中把这段大头直接抹掉,所以打一折还有得赚。
  3. **补上折扣的博弈面**——对你省 90% 输入费;对厂商,prefill 计算趋近于零、只付显存驻留成本,还换来更粘性的长会话负载(用户不会被成本逼走)。双赢所以价格能长期成立——**理解成"厂商发善心"就错了,它是成本结构的忠实反映**。
  4. **用显存成本解释两个设计**——TTL 存在是因为 KV cache 占显存、不可能无限期保留,5 分钟是"会话轮次间隔"附近的一个商业折中;最短缓存长度存在是因为太短的前缀省不了多少、不值得占一个显存位。机制理解到位,产品的每个参数都显得理所当然——这正是原理题要展示的推导能力。
- **补充弹药(追问再掏)**:
  - **延伸**:PagedAttention 这类显存管理技术(把 KV cache 切成页管理,类似操作系统虚拟内存)决定了缓存复用的粒度和稳定性——想深挖可以往这问。
  - **对照记忆**:KV cache=单请求内避免重算;prompt caching=跨请求复用 KV cache。一句口诀答完关系。
  - **自建场景**:自托管开源模型时这套机制照样存在(vLLM 的 prefix caching 开关),断点纪律从"省钱技巧"变成"吞吐技巧"——命中率高=GPU 利用率高。
- **及格线**:独立讲清两层关系+"prefill 是大头"。
- **加分**:从显存成本推出 TTL/最短长度的存在;提 PagedAttention。
- **红旗**:以为命中是"语义相似匹配"。

**Q2.9【原理·L3·平台】列举至少 5 种会破坏缓存命中的操作,按杀伤力排序,并说说各自怎么修。**

- **人话版**:按杀伤力:改工具定义(全失效)> system 里放动态内容(时间戳/随机 id)> 历史中段被改 > 断点位置漂移 > TTL 过期。共同规律:越靠前的改动,作废得越彻底。
- **主线(照这条线讲)**:
  1. **①改 tools 定义**——三层全失效。修法:工具集进会话就冻结,要改开新会话;MCP 工具按固定顺序注册(排序后再注入),别让启动顺序决定请求内容。这一条的本质是:**把"工具清单"当部署产物管理,而不是运行时状态**。
  2. **②system prompt 动态化**——时间戳、"今天是周几"、环境快照、随机用户名,每一项都在每轮改变头部。修法:动态内容一律尾部注入(最新用户消息或 tool_result 里,即 system-reminder 模式,见 Q2.10)。判别自己的 system 干不干净:两次请求的 system 字段做 diff,理想结果是零差异。
  3. **③消息序列中段被改**——回溯编辑历史、中间插图、消息重排。修法:历史 append-only,任何补充说明追加在尾部。这类破坏多来自"好心"的功能设计(允许用户编辑上一条消息重新生成),设计时就要意识到它在拆缓存。
  4. **④断点/块边界漂移**(显式断点厂商)——消息数量增长导致断点回看不到之前的写入。修法:长会话在前缀中段加第二个断点兜底(4 个断点预算的典型分配:tools 后一个、system 后一个、长历史中段一两个)。
  5. **⑤TTL 过期**——5 分钟 TTL 下人审 8 分钟,缓存已蒸发。修法:换 1h TTL(写贵些仍划算)或调整工作节奏(批量审查而不是逐条卡点)。这一条杀伤力最小但最冤:前缀纪律全对,败给了日历。
  6. **统一原理收尾**——五条排序的依据就一条:改动位置越靠前,级联失效覆盖的范围越大。记住这个排序逻辑,遇到没见过的破坏形态也能自己归位。
- **补充弹药(追问再掏)**:
  - **真实世界注脚**:2026-07 OpenAI 把 Codex 上下文上限从 372K 降到 272K 引发社区强烈反弹——缓存相关容量决策直接影响用户成本体感。
  - **排查方法论**:dump 每轮 usage 的三个字段(cache_read/cache_creation/input),对 miss 请求做前缀 diff,定位精确断点——先定位再修,不猜。
  - **序列化陷阱**:"内容没变只是重新序列化"照样 miss——字段顺序、浮点格式化、时区表示都是前缀的一部分;跨语言拼请求体时这一条高发。
- **及格线**:前两条+级联失效的机制。
- **加分**:diff 排查方法论;序列化陷阱这类二阶细节。
- **红旗**:不理解"级联"以为只失效改动那一块。

**Q2.10【原理·L3·平台】Claude Code 用 `<system-reminder>` 把动态信息塞进消息里而不是改 system prompt。这个设计解决了什么矛盾?自己实现等价机制要注意什么?**

- **人话版**:system prompt 必须逐字稳定才能吃缓存,但运行时总有动态信息要给模型(todo 状态、权限提醒)。解法:动态内容包上标签、嵌进**最新的用户消息或工具结果**——尾部追加不碰前缀,缓存无损。
- **主线(照这条线讲)**:
  1. **先点破矛盾**——稳定前缀(经济学)vs 运行时状态(必须更新),这是每个自研 agent 都会撞上的:权限提醒、todo 进度、环境变化,全都是"此刻才有的信息"。尾部注入让两者共存:前缀逐字不变,动态内容随最新消息进来,用完即沉入历史——下一次它只是"过去说过的话",不再占用头部。
  2. **证据在泄露的 system prompt 里**——官方契约明确写了"工具结果和用户消息可能包含 system-reminder 标签……它们与所在工具结果无直接关系"。这句自描述很重要:防止模型把提醒误解为工具输出的一部分(把权限警告当成命令结果)或用户指令(把系统提醒当成用户要求)——**注入的内容是系统想说的,但说话的位置天然属于别人,标签就是在补这个归属声明**。
  3. **自建的三个注意**——注入点必须在稳定前缀之后(最新消息内,一行都不能碰到 system 层);标签要自描述(来源是系统、非指令、非工具返回,模型该以什么权重对待它要说清);内容要幂等或可丢弃(同类提醒在历史里堆积时,旧份不再起作用——防"每次都注入当前 todo"把历史灌成 todo 墓地)。
  4. **收尾指出它同时是安全设计**——提醒措辞固定、来源明确,比每轮现拼一段"系统提示"更可控、更可审计:安全策略的文本是常量,能进 code review;注入内容沉入历史后要不要清理,连回 Q2.5 的驻留治理。
- **补充弹药(追问再掏)**:
  - **对照错误做法**:把动态信息追加到 system prompt 尾部——system 层整体哈希变了照样全失效;必须落在 messages 层的尾部。"我只在 system 末尾加了一行"是最常见也最冤的自首。
  - **变体**:各家命名不同(环境快照/进度注入),机制同族——判别标准就一条:落在稳定前缀之后吗。
  - **测试方法**:构造"连续 N 轮注入提醒"的会话,观察 agent 行为是否稳定——不幂等的实现在第三轮就开始走样。
- **及格线**:尾部注入不破坏前缀这个核心。
- **加分**:标签自描述的安全含义;幂等性考量。
- **红旗**:方案落在 system 层任何位置。

**Q2.11【原理·L3·平台】CLAUDE.md / AGENTS.md 这类项目记忆文件,在缓存设计里扮演什么角色?两家的加载层级有什么差异?**

- **人话版**:项目记忆放在请求前部,每轮都重发,但因为有缓存按 0.1× 计——这是"常驻上下文"在经济上可行的原因。Claude Code 四层拼接、子目录懒加载;Codex 的 AGENTS.md 从根到 cwd 逐级拼接、就近生效。
- **主线(照这条线讲)**:
  1. **缓存角色先讲透**——记忆文件内容进稳定前缀,只要文件不变,每轮都按缓存价"重读"。这带来一个不直觉的推论:记忆文件的成本模型不是"写了就花钱",而是**"改一次才花一次钱"**——变更触发后缀缓存重建。所以记忆文件追求的不是"少写"而是稳定+高信号:变更频率本身就是它的设计约束,值得当配置管理(变更走 review)而不是随手改。
  2. **Claude Code 四层**——企业托管(MDM 下发、不可排除)→用户全局 `~/.claude/CLAUDE.md`→项目根(入 git)→CLAUDE.local(个人);自根向下**拼接而非覆盖**,四层的并集才是 agent 看到的全部;子目录 CLAUDE.md 懒加载(agent 碰到该目录文件才注入);`@path` 导入递归上限 4 跳。拼接意味着冲突不会被上层消解——两条矛盾的规范会同时在场,靠模型自己权衡,所以跨层内容要主动去重。
  3. **Codex 的 AGENTS.md**——用户级→仓库根→各级目录直到 cwd,逐级拼接、越近优先,总量有上限(约 32KiB)。关键差异:AGENTS.md 被 Codex/Gemini CLI/Cursor/Copilot 等几十家支持,是跨厂商事实标准——写一份全工具受益。
  4. **用经济学收尾**——"一份记忆维护 N 个工具 < N 份各自维护"是 AGENTS.md 收敛的根本原因;而从缓存角度,它是前缀资产:内容常驻但按 0.1× 计费,是全上下文里**单位信息性价比最高的一块**——这也是为什么值得把高频使用的项目事实从对话迁进记忆文件:放历史里每轮原价重发还占注意力,放前缀里打折常驻。
- **补充弹药(追问再掏)**:
  - **懒加载的排错含义**:"规则没生效"的第一怀疑就是懒加载没触发——让 agent 复述规则验证注入。
  - **路径作用域**:`.claude/rules/*.md` 支持 paths glob 作用域——规则只在匹配路径的任务里进上下文,是懒加载的规则版。
  - **上限的意义**:Codex 的 32KiB 上限是产品在逼你做信噪比决策——没有上限的记忆文件会自然膨胀到没人审计。
- **及格线**:拼接不覆盖+懒加载两个机制点。
- **加分**:从缓存角度论"记忆文件要稳定";AGENTS.md 的收敛逻辑。
- **红旗**:以为子目录覆盖根文件;不知道记忆文件计费。

**Q2.12【原理·L4·模型】NoLiMa 这类基准测出了什么?为什么说"标称窗口长度 ≠ 有效上下文长度"?**

- **人话版**:NoLiMa 专测"问题里没有答案的字面出现"的场景——逼模型联想而非匹配。结果:一堆标称 128K+ 的模型,32K 时就掉到短文本基线一半以下。营销数字是窗口,工程要按有效长度设计。
- **主线(照这条线讲)**:
  1. **先讲方法为什么狠**——NoLiMa(Modarressi et al., ICML 2025)构造的问题与"针"**无字面重叠**:问 A,答案段只提它的同义描述("获奖小说"vs 书名本身),模型必须联想推理才能定位。这比大海捞针(NIAH)难一个量级——NIAH 只考字面匹配,而字面匹配恰是注意力最擅长的事;**用 NIAH 衡量长上下文,等于用视力表考听力**,会系统性高估。
  2. **报结果量级**——13 个标称 128K+ 的模型,输入 <1K 时都还行;拉到 32K 时 13 个里 11 个跌破短文本基线的 50%;当时最好的模型有效长度约 16K 量级,最差的从 73.6 掉到 0.9——不是"变差",是"归零"。标称与有效之间差着数量级,这就是本题要钉死的事实。
  3. **推出工程换算**——依赖"把 50 万 token 塞进去都能用好"的设计在赌一个不成立的假设;有效工作内存按十几 K~几十 K 估计更安全,超出部分靠检索(要用哪段取哪段)和压缩(留结论丢过程)支撑。注意这不是否定长窗口——长窗口的价值在"装得下待筛选的原料",筛选动作还是要靠检索和分层完成。
  4. **收尾连回 context rot**——NoLiMa 的长度退化与 Q2.6 的注意力稀释互为印证:一个测"标称与有效的差距",一个解释"为什么长会稀释"——两条独立证据指向同一设计结论,证据链闭合,这在面试里是很扎实的答法。
- **补充弹药(追问再掏)**:
  - **对厂商图表的免疫力**:报告里"长上下文通过率"曲线如果用 NIAH 类任务,参考价值打折——先问它考的是字面还是联想。
  - **自测建议**:用自家任务构造"关键约束在中段"的探针集,比公开榜更贴近你的分布——公开榜证明模型不行,自建探针证明"在你的任务上行不行"。
  - **时效**:模型迭代快,具体名次月月变,但"有效<标称"的结构性结论稳定——记结论别记名次。
- **及格线**:联想检索 vs 字面匹配的区分+有效长度远小于标称。
- **加分**:对 NIAH 局限的批判;两条证据链的互相印证。
- **红旗**:用"窗口 1M"论证"不需要检索和压缩"。

**Q2.13【原理·L3·平台】compaction 的信息损失怎么管理?对比 Claude Code 和 Codex CLI 的保留策略,通用原则是什么?**

- **人话版**:压缩是有损 GC。原则:保"决策与结论"、丢"过程与原文"——文件可重读、命令可重跑,而用户两小时前说的约束没法重新获得。关键状态别赌摘要,外置成文件。
- **主线(照这条线讲)**:
  1. **Claude Code 的取舍**——保 system prompt、CLAUDE.md、plan、最近改过的少数文件(大文件只留路径);丢全部 verbatim 工具输出与中间思考;反复压缩触发 thrashing 报错逼你开新会话。它赌的是:文件系统的现状比对话的过程更接近"任务真状态"——窗口里的历史可以丢,因为磁盘还在。
  2. **Codex 的取舍**(Rust 实现,源码可查)——摘要后按预算从新到旧装回最近用户消息,加 CompactionSummary 和初始环境基线。一个值得学的细节:mid-turn 触发压缩时,摘要插在**最后一条真实用户消息之前**——模型被训练成把摘要当"历史的一部分"而非"新指令",插错位置,agent 可能把摘要里的旧信息当作用户刚下的新命令执行;**同样的内容,放在序列的不同位置,行为含义不同**——这是上下文位置敏感性的具体应用。
  3. **提炼通用原则**——压缩解决"塞不下",不解决"记不住":它是容量工具,不是记忆工具。要跨窗口存活的状态(目标、验收、已否决方案及原因)应写进 todo/进度文件/记忆文件这类结构性载体:它们每轮完整重注入、不受有损摘要影响——**把"必须记得的"从概率性通道(摘要)迁到确定性通道(文件)**。
  4. **用实证收尾**——公开的长任务实验(数天级)都收敛到"JSON 进度文件+固定开机序列",连游戏化长程任务也靠进度笔记维持数十小时一致性——结构性外置是被反复验证的形态,不是理论偏好(ch06 展开)。
- **补充弹药(追问再掏)**:
  - **成本面**:压缩点之后缓存失效,一次压缩=一次前缀重建——压缩时机也是经济决策(批量、选里程碑点)。
  - **验收方法**:固定任务集跑"压缩前终止+注入摘要续跑"对照,量化压缩损失(排错版见 Q2.39)。
  - **分级策略**:不是所有会话都值得同一压缩力度——短会话不压缩直接结束,长任务按里程碑 checkpoint,超长任务干脆"分段会话+结构化交接",三档对应三种损失模型。
- **及格线**:保结论丢过程+关键状态外置。
- **加分**:Codex 摘要插入位置的细节;压缩与缓存的联动成本。
- **红旗**:把 compaction 当唯一长任务方案。

**Q2.14【原理·L4·平台】Anthropic 的 context editing / memory tool / token counting 三件套分别解决什么?为什么清理工具结果反而要配 `clear_at_least` 这种"至少清多少"的参数?**

- **人话版**:editing 自动把老工具结果换占位符;memory tool 给模型一个文件式记忆,重要结论先写进去再清理;token counting 让你在撞墙前主动触发。clear_at_least 存在是因为清理会打断缓存——每次只清一点等于反复作废缓存,不如攒一波批量清。
- **主线(照这条线讲)**:
  1. **三件套的分工**——editing 管"上下文里的垃圾"(默认约 100K input tokens 触发、从最老的 tool_result 开始替换、保留最近 3 组、客户端保留完整原始历史可随时恢复);memory tool 管"别丢的重要信息"(清理前提醒模型先写进记忆目录——给"要扔东西"配了一个"先抢救"动作);token counting 管"什么时候动手"(免费接口返回编辑前后 token 数,把压缩从被动撞限变成主动调度)。三层合起来是一个完整的存储层级设计:活上下文→记忆文件→原始历史归档。
  2. **重点讲 clear_at_least 的经济学**——清理是替换历史内容,清理点之后的缓存全部失效。每轮清一点=每轮付一次"从清理点到末尾的全价重算";攒一批再清,一次失效换最大回收。给个数字感:每轮清 5K、连清 10 轮,每次失效覆盖的却是"清理点之后的全部后缀"——失效范围由位置决定,不由清理量决定,**这就是涓流清理的隐藏杠杆**:你为 5K 的回收付了几十 K 的重算。这是"有损操作要批量化"的一般工程原则,数据库 vacuum、GC 都同构。
  3. **组合的设计精华**——memory+editing 的组合点在"先抢救再清理":清理前提醒模型把重要结果写入记忆,信息保住了再丢原文。官方量级:组合在 agentic search 任务上比基线 +39%(仅 editing +29%)——**多出来的 10 个点就是"抢救"动作的价值**,它可以精确归因。
  4. **报数字收尾**——100 轮 web search 任务 token 降 84%:说明清理的收益主要来自"工具输出占上下文大头"的真实负载结构——三件套打的是同一个靶(工具输出),只是打的时机不同。
- **补充弹药(追问再掏)**:
  - **可恢复性**:客户端保留完整原始历史——占位符不是删除是"折叠",需要时能还原,这是产品化的安全垫。
  - **自建等价物**:不依赖厂商 API 也能做——hook 层批量替换旧 tool_result+进度文件,机制相同。
  - **触发阈值的设计空间**:100K 默认值是通用负载的折中;自己的任务分布不同(工具输出占比 90% 的 agent),最优阈值可能明显更低——值得用 eval 扫一遍。
- **及格线**:三件套各一句+清理打断缓存。
- **加分**:不提示的情况下推出 clear_at_least 的存在理由;"先抢救再清理"的组合设计。
- **红旗**:以为清理零代价、越频繁越好。

**Q2.15【原理·L3·平台】算一笔账:30 轮长会话,平均历史 300K token,输入价 $3/MTok、缓存读 $0.30。缓存 90% 命中 vs 全 miss,输入成本差多少?**

- **人话版**:累计输入约 9M token。全 miss 是 9M×$3=$27;90% 命中是 8.1M×$0.30+0.9M×$3≈$5.1。差 5 倍多——会话越长命中率越高,差距越接近 10 倍。
- **主线(照这条线讲)**:
  1. **先算主干**——每轮输入≈当前历史长度,取均值 300K 近似,30 轮累计 9M token(严格的梯形求和不必现场算,均值近似在面试里完全够用,但要说得出"我用均值近似了梯形"——近似得坦白,反而是加分)。90% 命中:8.1M×$0.30=$2.43,0.9M×$3=$2.70,合计约 $5.1;全 miss:9M×$3=$27。差约 5.3 倍。算术不难,难的是**立刻知道该算什么**:听到"长会话成本"三个字,条件反射出"累计输入=轮数×平均长度"这个被积函数。
  2. **补二阶项显专业**——首轮有约 1.25× 写溢价(量级几毛,随缓存规模涨);未命中的 10% 仍按原价——所以差距是 5 倍多而不是正好 10 倍。能主动说这两点说明真算过账,不是背结论。
  3. **从数字推三个行动**——命中率是第一成本杠杆:60%→90% 的输入费降幅比换便宜模型大,且不牺牲质量;"空闲也在花钱"(后台 agent 每轮按缓存价重读全部长上下文,长驻会话有固定成本——0.1× 乘以巨大基数仍是真金白银);超 TTL 的间隔每发生一次就是一轮全价——"节奏税"要用 TTL 选型或流程消除(批量人审,而不是逐条卡 8 分钟)。
  4. **汇报口径收尾**——单看总账容易吵(任务结构不同没法比),按"每任务输入成本"+"缓存命中率"两个指标拆开,优化才可归因:省的钱来自命中率提升还是任务变短,对应完全不同的下一步。
- **补充弹药(追问再掏)**:
  - **官方量级参照**:Claude Code 公开示例中单会话 cache read 940K+write 50K 合计约 $0.55,同样输入全 miss 约 $2.97——和我们的计算同数量级,互相印证。
  - **换模型敏感度**:输出占比高的任务,输出价对总账的影响会超过缓存——先看自己的 token 结构再决定优化哪头。
  - **量纲检查**:MTok 单位换算错一位是这类心算的高频事故,报答案前先过一遍量纲。
- **及格线**:算对量级(约 5 倍)+说清"重发被缓存摊薄"。
- **加分**:写溢价/TTL/空闲成本这些二阶项;拆指标的汇报口径。
- **红旗**:算出 10 倍就停(忽略未命中部分仍原价)。

**Q2.16【原理·L3·模型】为什么上下文翻倍,检索和推理质量下降得比线性快?用注意力预算解释。**

- **人话版**:n 个 token 产生 n² 个注意力对——长度翻倍、注意力关系翻四倍,每 token 分到的预算被稀释;而且"找出来、记住、组合"三层能力各自衰减还乘法叠加,所以恶化超线性。
- **主线(照这条线讲)**:
  1. **先把 n² 讲实**——attention 是全局的,每对 token 都有权重:10 万 token 有一百亿对,20 万 token 有四百亿对——长度翻倍,关系数翻四倍,每对分到的 softmax 权重被摊薄。这不是比喻,是结构性质;所谓"注意力预算",就是 softmax 归一化下每对关系能分到的权重上限——分母变大了,每一份必然变小。
  2. **再叠加两个放大器**——训练数据偏短:模型见过的"第 300K token 精确引用第 5K token"的样本极少,没学会超长范围的精确指代;位置编码插值在远距离失真:为了塞进训练长度做的外推/内插,距离越远误差越大。三个因素相乘,退化被放大——所以实测曲线比"平分注意力"预测的还要差。
  3. **讲复合效应这个关键概念**——Context Rot 的核心结论:工作记忆/检索/推理**各自**随长度衰减,复合任务(先找到→再记住→再组合)的恶化超过单项之和。真实工程任务几乎都是复合任务("从那份三小时前的日志里找到报错,结合配置改动定位原因"=检索×记忆×推理三连),所以用户体感的退化比单维实验更狠——**实验是下界,生产是现实**。
  4. **落到设计推论**——给上下文设"质量预算"不只设"长度预算":超过经验拐点(几十 K 量级)后,继续堆历史的质量成本开始超过它省下的检索往返。这时该做的是把过程外置、只留结论,而不是换更大的窗口——换大窗口只是把同一个滑坡往后挪了一段。
- **补充弹药(追问再掏)**:
  - **一句话版本**:"上下文是稀缺资源"——把这条当设计公理,后面的压缩/检索/分层全顺理成章。
  - **反直觉点**:质量拐点先于长度上限到来——"还没塞满就已经变笨"是最容易被忽视的失效模式,监控里只有 token 数没有质量,所以它静默发生。
  - **验证动作**:同一任务分别在 20K/100K/400K 上下文下跑固定 eval,画出自己的拐点——比引用论文更有说服力。
- **及格线**:n² 稀释+复合叠加。
- **加分**:"拐点先于上限"的推论;质量预算的管理动作。
- **红旗**:只用"贵"解释压缩——质量和成本是两个独立理由。

---
## 2.3 Agentic RAG:检索作为工具

**Q2.17【八股·L2·架构】Agentic RAG 和传统 RAG 的本质区别是什么?"agentic"体现在哪三个决策权上?**

- **人话版**:传统 RAG 是固定管道——每查必检索、必 top-k、必一次性塞入。Agentic RAG 把检索变成 agent 的工具:要不要检索、查哪个源、查几轮,模型运行时自己定。
- **主线(照这条线讲)**:
  1. **先报三个决策权**(微软 Azure 架构指南 2026-06 的口径,行业已收敛)——是否检索(模型可能本来就知道,"法国首都"不需要查库)、检索什么源(向量库/关键词/网页/API 各是不同工具,按问题性质选)、检索几步(首轮不好就改写再查,迭代到够)。传统管道里这三个决策全部写死在代码里;agentic 化就是把它们从配置文件移交回模型——**判断一个系统 agentic 与否的试金石,就是看这三处是硬编码还是模型说了算**。
  2. **给一个标准实现形态**——LangGraph 官方教程的五节点图:`generate_query_or_respond`(模型先二选一:直接回答还是调 retrieve)→`retrieve`(执行检索)→`grade_documents`(逐篇判相关)→`generate_answer`(只用相关文档)→`rewrite_question`(全不相关就改写回检索)。第一和最后一个节点就是决策权的落点;中间三步传统 RAG 也有——**agentic RAG 不是替换管道,是给管道装了两个阀门**。
  3. **报代价防止把它讲成银弹**——标准 RAG 2–3 秒,agentic RAG 3–5 次工具调用通常 8–15 秒,成本和延迟都上一个台阶;模型判断还引入了新的失败面(该检索的没检索、检索轮次失控)。所以检索前路由(判断哪些查询不值得走全链路)成了配套必件(Q2.19)——**多出来的智能每一分都要付钱,配套的省钱机制不是可选项**。
  4. **收尾一句话定位**——"检索从管道降格为工具"是 2026 年微软/OpenAI/Google 产品文档的实际形态,不是争论中的概念;答这题时把"降格"这个词用准,立场立刻显专业——检索没有消失,它不再是必经之路。
- **补充弹药(追问再掏)**:
  - **工程约束**:迭代上限 5–10 次(微软指南口径);工具总数 <20、每次调用注入 3–5 条结果起步。
  - **判别式**:看系统里"检索"是不是可被模型跳过的——必经检索的都还是传统管道。
  - **失败面清单**:不必要检索(浪费)、该检索没检索(幻觉)、迭代失控(延迟爆炸)——三个失败面分别由路由、prompt、上限兜底,能对着说就是完整方案。
- **及格线**:"检索从管道变成工具"+三个决策权中两个。
- **加分**:五节点图;延迟/成本数字。
- **红旗**:把 agentic RAG 理解成"RAG 加了个更好的 rerank"。

**Q2.18【八股·L2·模型】Self-RAG 和 CRAG 各自的核心机制是什么?2025–2026 年它们的落地形态是什么?**

- **人话版**:Self-RAG 把"反思"训练进模型——生成时吐出控制信号(要不要检索、结果相关吗、答案有据吗);CRAG 外挂一个轻量评估器——先给检索结果打分,按置信度决定直接用、去网上补、还是拆开重组。一个改训练,一个改管道。
- **主线(照这条线讲)**:
  1. **Self-RAG 的机制**(arXiv 2310.11511)——训练模型生成四类反思 token:是否需要检索/检索段落是否相关/是否支持输出(groundedness)/生成是否有用。推理时这些 token 就是控制信号,模型自决"这题要不要查"。7B/13B 版在开放域 QA 上超 ChatGPT,靠的就是按需检索+自我批判。
  2. **CRAG 的机制**(arXiv 2401.15884)——不动模型,加轻量检索评估器输出置信度;低置信触发两个纠错动作:web 检索补充、decompose-then-recompose(拆开检索结果、剥无关再重组)。即插即用,任何 RAG 框架可加。
  3. **一句话分野**——Self-RAG 的智能在权重里(要训练),CRAG 的智能在管道里(外挂组件)。面试里先给这条分野,再讲细节,条理立刻清楚。
  4. **落地形态(重点,别只会背论文)**——思想已化进产品:LangGraph 的 grade_documents+rewrite_question 就是 CRAG 的评估-纠错环;Azure AI Search agentic retrieval(2026-04 GA)把"查询规划→并行子查询→语义排序→结构化引用"做成托管服务;OpenAI file_search 内置自动改写+专用 reranker。用 2026 年的任何一家托管检索,里面都有这两篇的影子。
- **补充弹药(追问再掏)**:
  - **演化观察**:反思 token 的思想在推理模型时代变成了"思维链里的检索决策"——模型在思考过程中决定搜什么,形态变了机制没变。
  - **LlamaIndex 定位**:CRAG/Self-RAG/HyDE 这些 2023–2024 术语被其称为"行业基本功(table stakes)"——新意已尽,工程化已深。
- **及格线**:训练 vs 外挂的分野+各一个机制细节。
- **加分**:落地产品的具体名字;"基本功化"的行业视角。
- **红旗**:只会论文名和年份,说不出机制落到了哪。

**Q2.19【原理·L3·架构】检索前路由(route before retrieve)怎么做?什么信号用来判断"这个查询不值得走 agent 检索链路"?**

- **人话版**:在 agent 链路前加一道便宜的门——规则或小模型先判断:是不是闲聊、是不是模型已知事实、要不要检索、要的话查哪个源。简单查询直接答或单次检索,省下 8–15 秒和几倍 token。
- **主线(照这条线讲)**:
  1. **三种路由形态由轻到重**——规则层(关键词/意图分类,最便宜,一天能上线);小模型层(几千 token 成本判断"要不要检索+查哪个源",量大后替换规则);产品参数层(Azure AI Search 的 reasoning_effort 三档,Minimal 档跳过 LLM 查询规划直接单查询——把"用质量换开销"做成显式开关,默认 Low)。三层的共同点:决策成本要比省下的成本低一个量级,否则是负优化。
  2. **判断信号给一套**——查询类型(事实问句 vs 开放分析——前者多半单次检索就够)、时效性(要不要最新数据——不要的话连 web 源都不用挂)、领域(查哪个索引/哪个库)、会话上下文(追问可复用上轮检索结果——"那它的第二个参数呢"不用重新检索)、历史统计(这类查询检索历史上帮了多大忙——从来没用上的类别直接降级)。OpenAI web_search 的 tool_choice=auto 本质是把路由交给模型自判——也是一种形态,省了路由器,但把成本判断也交给了不心疼 token 的一方。
  3. **讲清代价不对称这个设计原则**——该检索的没检索→幻觉,用户可感知的伤害大且不可逆(信任一次受损);不该检索的检索了→浪费延迟和钱,可容忍且可观测。**所以路由阈值要偏向"多检索"一侧**,宁可多花点查一下,再用缓存和结果复用把多余检索的成本压掉——方向错了后面的优化全白做。这个不对称也是所有"用便宜判断省昂贵操作"系统的共同设计依据(审核、风控同构)。
  4. **落地路径收尾**——规则起步(一天能上线,覆盖头部 80% 流量)、按误路由样本迭代(每周看一遍"路由错了"的 case)、量大后上小模型分类器;路由器本身的延迟(分类一次 <500ms)和成本要小于它省下的,这条红线每季度复核一次——流量结构变了,红线也会变。
- **补充弹药(追问再掏)**:
  - **LlamaIndex 的演进线**:naive top-k→检索模式→auto_routed(轻量 agent 路由)→多索引组合路由——可以当路线图引用。
  - **缓存联动**:同类查询的检索结果短缓存,把"宁多检索"的浪费压回来。
  - **路由的可观测**:路由决策要落日志(走了哪档、依据什么信号)——否则误路由排查只能靠猜。
- **及格线**:便宜前置判断+代价不对称。
- **加分**:信号集的具体程度;路由器自身成本的清醒。
- **红旗**:所有查询走全链路"保证质量";或弱规则硬门无兜底。

**Q2.20【原理·L3·架构】迭代检索/多轮检索怎么防止两种失败:无限循环检索,和越改写越偏?**

- **人话版**:设硬上限(5–10 次迭代、或同一问题最多再搜 3 次);每次改写必须携带"上一轮为什么没找到"的信息——让改写有方向,不是换个说法再赌一次。
- **主线(照这条线讲)**:
  1. **防循环三件套**——硬上限是底线(5–10 次迭代封顶,超了走兜底);结果重叠检测(本轮结果与上轮高度重叠就提前终止——再查也是这批文档,信息已到手,继续查只是烧钱);指令级约束(微软指南原文级别的写法:"Don't search more than three times for the same subsequent question"直接进 prompt——行为约束不需要技巧,把丑话说在前面往往最有效)。
  2. **防漂移的核心是改写带方向**——把 grade_documents 的判定("前 5 篇都是讲部署的不是讲计费的")反馈给改写器,改写器针对缺口补词("计费 pricing 费率"加权),而不是盲目同义替换。无方向的改写每轮都在随机游走:换个说法再赌一次,赌注还是同样的 token。**判别一个迭代检索系统成不成熟,看改写器的输入里有没有上轮的失败信息**。
  3. **升级手段:查询分解**——复合问题("对比 A 和 B 在高并发下的表现")拆成子查询并行检索,命中率比一轮大而全高得多(Azure agentic retrieval 默认路径就是"分解成 3 个子查询并行");分解同时天然缓解漂移——每个子查询目标小而明确,随机游走的方差小。
  4. **到顶后的兜底要设计**——迭代到上限还不行,降级路径是"基于现有信息回答+明说缺什么",而不是拿半截信息硬编;面向用户的系统再考虑升级人工或更贵的检索源(web 兜底)。**兜底是产品决策不是技术细节**:宁可回答"我没查到,已有信息显示…"也不要静默编造——这是 agentic 检索系统的信任底线。
- **补充弹药(追问再掏)**:
  - **度量**:每查询的平均迭代轮数、改写后的召回提升率——没有这两个数,迭代策略的调优是盲调。
  - **失败样本库**:把"迭代到顶仍失败"的查询单独归档,它们是路由器和分解器的最佳训练素材。
  - **漂移的可观测**:记录每轮改写后的 query 和对应召回质量,画出"迭代轮次-相关度"曲线——健康曲线单调升,漂移曲线随机震荡,一眼可辨。
- **及格线**:上限+有方向改写。
- **加分**:重叠检测/查询分解/兜底三件再亮一件。
- **红旗**:只设上限没有兜底,到 10 次硬停拿半截信息硬答。

**Q2.21【原理·L3·平台】怎么评估一套 agentic 检索系统?哪些指标不需要 ground truth 就能先跑起来?**

- **人话版**:三层看——检索层(召回了没)、生成层(答案有据吗)、agent 层(工具用得对不对)。冷启动先跑不要标注的:LLM 裁判评 faithfulness(答案是否被检索内容支撑)和检索相关性;有标注再补 recall 和完整度。
- **主线(照这条线讲)**:
  1. **先把指标体系立住**——检索侧 context precision(召回里的相关占比)/context recall(该召回的召回几成,需 ground truth);生成侧 faithfulness(答案是否有上下文支撑、无捏造——精度向)/completeness(对标准答案的覆盖——召回向)。**faithfulness 和 completeness 是两个方向,别混**:一个系统可能很忠实但什么都答不全。
  2. **无标注先跑的(A 部分)**——LLM-as-judge 评 faithfulness 与检索相关性(RAGAS、Azure Foundry 的 Retrieval 评估器同思路);工具调用轨迹的启发式审计(该检索的轮次检索了吗/重复查询率/每次请求工具调用数)。这些适合进 CI 当回归信号。
  3. **agent 特有的四个维度**(微软指南 2026-06 明确列出)——工具选择准确率、检索效率(每次请求的工具调用数)、端到端延迟、单请求成本。传统 RAG 只看前两层;agentic 系统必须把第三层当一等指标——**一次"成功但调了 20 次工具"的回答在成本上是失败的**。
  4. **金标集冷启动的方法**——"标注派对"(团队半天集中标几百条)+LLM 生成 query-chunk 对先铺量再人工清洗(Generative Benchmarking);起步 50 条高质量好过 500 条随手标。
- **补充弹药(追问再掏)**:
  - **调参方法论**:参数扫描(top_k/chunk 大小/注入条数)配 NDCG/XDCG 这类排序指标,比拍脑袋调参可信。
  - **裁判治理**:rubric 拆成可勾选项、交换位置、换家族裁判——LLM 裁判的偏差治理见 ch07。
- **及格线**:faithfulness/completeness 的方向区分+一个 agent 特有指标。
- **加分**:无标注指标的 CI 用法;金标集冷启动方法。
- **红旗**:只有一个终判指标("答对没"),检索烂和生成烂混在一起没法归因。

**Q2.22【原理·L4·架构】检索结果怎么和上下文预算联动?注入多少条、以什么形态进上下文,你的决策框架是什么?**

- **人话版**:注入条数按"最小够用"定(3–5 条起步,按评估调);形态优先给"标识符+摘要"而不是全文,超出预算的部分给路径让 agent 需要时再取。原则:检索为上下文服务,不是反过来。
- **主线(照这条线讲)**:
  1. **条数的量化起点**——微软指南:每次工具调用 3–5 条起步;OpenAI file_search 的 max_num_results 默认 10 但官方示例常用 2(省 token 和延迟,"可能牺牲回答质量")——**注入量是显式 trade-off 旋钮,该用 eval 调,不是拍脑袋**。
  2. **形态三级给框架**——全文注入(短且关键)→摘要+引用(长文档)→只给标识符(路径/链接/query,agent 需要时再拉——即 Anthropic 说的 just-in-time retrieval)。Claude Code 是混合范式的活例:CLAUDE.md 预载(常驻)+grep/glob 按需(即时)。
  3. **驻留期治理常被漏**——注入的检索结果也是历史的一部分,几轮后同样该被清理/摘要化;**检索系统只管"取",上下文管理管"留多久"**——两个生命周期显式分开,否则检索越勤快上下文越脏。
  4. **决策框架收尾**——三个问题的顺序:这条信息任务相关性多高(决定全文/摘要/标识符)、多久后会失效(决定驻留策略)、重取成本多高(决定要不要缓存/预载)。答完三问,注入策略自动出来。
- **补充弹药(追问再掏)**:
  - **反模式**:top-k=10 无脑全注入,从没算过每条的边际价值;检索结果常驻不清理,三轮后上下文一半是过期检索。
  - **产品参照**:Azure agentic retrieval 按检索 token 计费(2000 次约 $4.32)、Gemini 3 grounding 按实际查询数计费——检索成本已进入计量单位,注入量治理等于成本治理。
- **及格线**:3–5 条量级+标识符按需拉取。
- **加分**:驻留期治理;三问决策框架。
- **红旗**:top-k 定了就不动,与评估脱钩。

**Q2.23【品味·L4·架构】2025–2026 年吵了很久"RAG is dead"。你的立场是什么?什么规模该放弃检索、什么规模必须回来?**

参考思路:
- **先正本清源**:死的是"naive 单向量+固定 top-k+必经检索"的管道,不是检索本身——LlamaIndex 2025-05 的宣言标题就是"RAG is dead, long live agentic retrieval"。把争论的靶子先钉准,后面的讨论才不空转。
- **"可以不走检索"一方的硬证据**——Karpathy 2026-04 的个人知识库:raw/ 目录丢原始资料,LLM 当"编译器"维护约百篇/40 万字互联 markdown wiki,明确说个人规模不需要向量库;2026-03 的研究显示 8B+向量库记忆(30%)胜过 235B 无记忆(14%)——组织好的上下文比更大的模型值钱。
- **"必须回来"一方的硬数字**——Atomic 的反驳(2026-04):40 万字≈54 万 token,每次全注入约 $0.01 尚可;1 万页≈4000 万 token≈$100 且塞不进窗口;无索引的 wiki 导航耗时 2–3 个月,叠加 embeddings 底座约 40 分钟,某实验 embeddings 让索引读取量降 200 倍。结论:策展层活在检索底座之上,不是二选一。
- **我的裁决变量(给出可操作的)**——语料 token 量×查询频次(总量大必须检索)、变更频率(频繁变更索引维护贵,偏 grep/wiki)、查询分布(集中问少数文档→预载;长尾→检索)。2026 共识:检索降格为 agent 的按需工具,与记忆/策展分层共存——微软/OpenAI/Google 的产品文档已经这么组织了。
- **及格线 / 加分 / 红旗**:及格=分清"管道死了"和"检索死了"+一个量化判据;加分=双方数字都记得并能给出自家业务的演算;红旗=跟着口号站队。

---

## 2.4 品味与判断题

**Q2.24【品味·L4·架构】把"上下文是稀缺资源、缓存是经济学、压缩是垃圾回收"落成你团队的上下文工程军规,至少 5 条。**

参考思路(军规示例,重要的是每条背后有机制):
- **静态在前动态在后**——请求结构按变化频率排序:tools/system/早期历史在前,动态状态(时间戳、todo 进度、环境快照)一律尾部注入,断点打在"最后一个跨请求相同的块"上。机制:前缀匹配的级联失效——头部一动全盘作废。例子:动态配置挪到尾部注入,命中率 60%→90%;反例:system 里放"当前时间",每轮全量重算。
- **工具定义冻结**——进会话就不改,要改开新会话;MCP 工具排序后再注入,不随启动顺序抖动。机制:tools 在缓存层级最上游,一个字的描述改动作废三层。落地形态:工具清单当部署产物管理,变更走发布流程而不是运行时热改。
- **工具输出先过滤再入上下文**——超阈值截断或外置,错误信息优先保留;给 agent 留"取原文"的通道(read(offset) 续读/重新调用)。机制:输出是最大污染源且价值随时间衰减——一万行日志第二轮看的价值趋近零,却每轮重发。
- **压缩有损,结论外置**——关键决策写文件/todo,不赌摘要;长任务用结构化进度文件。机制:摘要是概率性通道(保什么靠模型判断),文件是确定性通道(每轮完整重注入);rot 之下,留在窗口里的结论本身也在贬值。
- **主动算账**——token counting 决定何时 compact;缓存命中率进监控,跌破阈值告警。机制:压缩与缓存都是经济决策不是技术装饰——命中率从 90% 掉到 40% 每天多烧的钱,和一台服务器的事故损失同量级,但前者静默。
- **军规要可执行可审计(元规则)**——写进 review checklist 和 CI(比如 lint 规则禁止 system prompt 模板里出现日期函数),每条能说出"违反时的可观察症状"(命中率暴跌/TTFT 变长/agent 重复读文件)。机制:不能被执行的军规是文化装饰,三个月后只剩 wiki 里的一页 PPT。
- **及格线 / 加分 / 红旗**:及格=5 条且每条附一句机制理由;加分=某条有团队真实翻车案例背书,或有 CI 化的执行手段;红旗=写成"写好 prompt 多测试"这类无机制含量的空话。

**Q2.25【品味·L3·平台】什么时候该果断开新会话,什么时候值得压缩续命?**

参考思路:
- **开新会话的四个信号**——任务已完成且状态已外置(续命无意义,新任务不该背着旧任务的尸体走);上下文堆满与下一步无关的过程(压缩救不了信噪比——摘要还是摘要,垃圾的比例没变);同一问题纠正两次仍失败(错误上下文本身在误导模型,官方 best practice 口径:连续修不好就 /clear 换思路——带着失败的尝试继续修,模型会被自己走过的死路带偏);反复 compaction 撞 thrashing(产品已经在明示该换了)。
- **续命的正当理由只有一个**——任务中间态复杂且未外置、重建成本高于压缩损失。但要立刻自问:这个"未外置"是不是外置习惯缺失的症状?多数续命需求是习惯问题的代偿——如果每次都靠续命过关,该修的是"重要状态随手落文件"这个习惯,而不是压缩策略。
- **高级形态**——里程碑式长任务用"结构化笔记+新会话"组合(数天级任务的标准姿势,见 ch06),比在线压缩稳:每个会话职责单一、上下文干净、失败可从笔记重启;选模型时把"对压缩的容忍度"当能力维度(已有厂商以"原生训练为跨压缩上下文运行"为卖点——压缩行为与模型训练开始耦合,同一个 harness 换模型,压缩表现可能大变)。
- **一句话判据**:要"历史"(接着聊,语气和上下文连贯)就 resume;要"状态"(接着干,任务进行到哪)就新会话+读文件。**历史是给对话用的,状态是给任务用的**——大多数工作场景要的其实是状态,只是误以为要历史。
- **及格线 / 加分 / 红旗**:及格=三个以上开新会话判据;加分=把续命需求归因到外置缺失;红旗=相信"聊得越久越连贯"——context rot 恰恰相反。

**Q2.26【品味·L4·架构】"100% 缓存命中但上下文持续膨胀" vs "频繁压缩但缓存反复失效",你怎么调度这个权衡?**

参考思路:
- **先把两边的成本拆成同单位可比**——膨胀的账:质量成本(rot 超拐点后任务成功率下降,这是主导项)+金钱成本(0.1× 乘以巨大基数仍是钱,百万 token 级历史每轮 0.1× 也是真金白银)。压缩的账:金钱成本(压缩点之后全价重算一次+摘要本身的生成费)+质量成本(有损,丢的可能是关键约束)。两边都不是免费选项,先承认这是"两害相权"再谈调度——把任何一边说成白赚的答案直接出局。
- **调度策略三条**——批量优先于涓流(把"该忘的"集中一次清理,一次失效换最大回收,clear_at_least 思想);里程碑处压缩+外置,不在随机点压缩(信息损失最小化——里程碑处"该保留的"最清晰;失效时机也可控——不打断任务中段);用 token counting 把触发从"撞限"改成"达到质量拐点"(预算管理而非容量管理,在 rot 伤到你之前动手)。
- **顶层方案:砍问题规模**——subagent 隔离让噪声根本不进主上下文:搜索、试错、读大文件都在子窗口里烧掉,主线程只收 1–2K 摘要。这不是在两个坏选项里挑较好的,是把问题的规模砍掉——**最好的答案是改变问题而不是优化权衡**,这一层说出来,和背参数调优的候选人就分层了。
- **监控配套**——这个调度不能拍脑袋:命中率、上下文长度分布、任务成功率三条曲线同屏看;膨胀方案的成功率拐点出现在哪个长度,就是压缩触发的经验阈值——用数据定,每季度复核。
- **及格线 / 加分 / 红旗**:及格=两边成本构成说清;加分=批量清理+subagent 隔离两个结构性手段+监控配套;红旗=只看 token 成本或只看质量成本的半边视角。

**Q2.27【品味·L3·用户】CLAUDE.md/AGENTS.md 的每一行,用什么标准决定去留?**

参考思路:
- **唯一判据**——"删掉这一行,agent 会不会开始犯错?"写**不可从代码推断的事实**:构建命令(代码里看不出来用 pnpm 还是 npm)、部署坑(只有踩过的人知道)、团队约定(分支命名、commit 格式)、禁区(不许动的目录)。可推断的内容都是浪费——模型自己会读代码,"入口文件在 src/ 下"这种行是纯噪音。这一条能砍掉大多数膨胀的记忆文件。
- **量与质的配套**——入口约 200 行内;可验证的具体指令("测试用 pnpm test:unit")优于抽象原则("保持整洁")——前者可执行可检查,后者只是愿望;按目录/任务拆子文件懒加载,不堆根文件:根文件是全任务常驻的,每一行都在为所有任务付注意力税。
- **硬约束分流**——禁止目录、危险命令这类"绝不能违反"的约束交给 hooks/权限确定性执行——**文档管软约束(尽力遵守,模型可能忘),代码管硬约束(违反不了,根本到不了模型面前)**;把硬约束写在记忆文件里赌模型自觉,是常见但致命的错位——事故复盘时"它明明写了不许"不构成辩护理由。
- **生命周期**——记忆文件是活文档:定期回改(引用它的行为消失/被反复误解,就是过期信号);删比注释好(注释掉的内容照样被读到);变更走 review——它进稳定前缀、影响每个会话,权重和代码同级。
- **及格线 / 加分 / 红旗**:及格="不可推断才写"+硬约束分流;加分=有具体重构经历(600 行砍到 150 行行为反而变好);红旗=记忆文件当产品文档写,给人看的不给 agent 看的占大半。

**Q2.28【品味·L4·管理】agent 平台月账单涨了 3 倍,CTO 要求降本。你第一件事查什么?**

参考思路:
- **先分账再动手**——按五个维度切:缓存命中率(最大杠杆,先看有没有从 90% 掉到 40% 这种结构性的坑)、上下文长度分布与会话轮数(谁在膨胀——任务真的变长还是清理缺失)、模型分布(该用小模型的活是不是全在大模型上跑)、哪个工具在倾倒 token(通常两三种工具占 80%)、无人值守任务的空转重读(后台 agent 每轮 0.1× 重读长历史,积少成多)。产出一张"钱去哪了"的归因表,而不是直接给方案——**没有归因的降本方案是在赌**。
- **常见根因排序(按修复的性价比)**——缓存破坏(tools 抖动/动态 system)>工具输出未过滤>模型不分级>无效长会话。排序依据是可逆性:前两个修复对行为零影响(纯止血,无回归风险),先做;模型路由影响轻微,其次;砍功能最后——可逆性从高到低,正好是行动顺序。
- **反面提醒**——先去砍单价(换便宜模型)再查结构问题是典型错误顺序:"贵"经常不是单价问题,是钱烧在低质量吞吐上(方向错的探索、无验证器的试错、污染后的重做——这些 token 花了,任务没成);先止血浪费再谈换模型,顺序反了会一边省单价一边继续漏大头。
- **汇报口径**——"每任务成本×质量"双曲线,降本不降质要能证明(eval 不变或变好);只报省了多少不报质量变没变,三个月后会被反噬——那时代价是信任,不是钱。
- **及格线 / 加分 / 红旗**:及格=先查命中率与长度分布;加分=归因表+双曲线汇报+按可逆性排修复顺序;红旗=第一反应是换模型或砍订阅。

---

## 2.5 开放性问题

**Q2.29【开放·L5·模型】如果让你参与设计一个"为压缩而训练"的 agent 模型,训练目标和数据怎么构造?**

讨论框架:
- **数据从哪来**——真实长会话回放(带"压缩前后任务是否仍成功"标签)信号最强——它就是压缩的最终验收;合成任务(拼接任务+注入压缩点)可铺量但有分布偏差(拼接的会话缺少真实会话的冗余和噪声结构,训出来的摘要器在真实负载上可能过拟合"干净"的模式)。难点:什么是"好摘要"没有天然标签,只能靠下游任务成功率当奖励——这个信号又稀疏(一次任务成败归因到一段摘要,中间隔着几十步),**绕过奖励稀疏是本方向的核心研究问题**,能把这个矛盾本身讲清楚就是好答案。
- **奖励怎么定**——主奖励:压缩后续任务成功率(终局指标,稀疏但真);次级:token 节省(必须设,否则模型什么都往摘要里写——摘要越全越安全是局部最优;但不能设高,否则什么都不写——把约束也丢了)。两个奖励的配平本身是个扫描实验。约束项:惩罚摘要里出现原文没有的断言(保真——压缩引入的幻觉比丢信息更糟:丢了大不了重读,编了会误导后续所有步骤)。
- **本质张力**——摘要的任务相关性(对下一个任务有用才留)vs 一般性(压缩时无法预知下一个任务是什么):这是预测问题——模型在做"下游任务会需要什么"的预判。可聊的方案方向:训练时模拟多任务续接(同一段历史接不同后续任务,奖励按最坏情况或平均情况结算),或按任务类型做条件化摘要(压缩时给定任务描述,摘要器有条件可依)。
- **加分方向**——专门的"记忆写入"动作头(把外置记忆变成模型的一等能力而不是 harness 的 hook——模型自己决定什么值得写,比"清理前统一提醒"更精细);评测怎么构造(固定任务集×不同压缩策略的对照矩阵——压缩率扫一排,成功率曲线画出来,拐点就是该模型的最优工作点)。
- **及格线 / 加分 / 红旗**:及格=奖励三件(成功率/节省/保真)说全;加分=奖励稀疏的本质矛盾+条件化摘要方案;红旗=只说"用高质量数据训练摘要能力",没有目标和数据的构造。

**Q2.30【开放·L5·平台】缓存、压缩、检索三者边界会不会被"无限上下文+免费缓存"的模型进步抹掉?哪些工程投入是抗未来的?**

讨论框架:
- **机制上抹不掉的**——context rot 是注意力机制的内在约束,不是计费策略:n² 注意力对、训练数据长度分布、位置编码失真,三个来源没有一个随"窗口变大"消失。窗口免费了,"塞太多质量下降"还在——**压缩与检索的质量价值因此保留**:它们解决的是"模型处理不好太多信息",不是"信息太贵"。把这条机制讲清,题目的前半就答完了:边界会移动,不会消失。
- **会贬值的**——稳定前缀纪律是特定缓存实现(显式断点/TTL/层级哈希)的产物;全自动缓存普及后,打点技巧贬值。但要精确到条目:尾部注入不贬值——"动态内容别混进静态指令区"是信息架构问题,哪怕缓存免费,把时间戳和系统指令搅在一起仍然让行为不可预测;贬值的只是它缓存收益的那一半。
- **抗未来的投入**——信息分层治理(什么常驻/按需/丢弃——这是"该看什么"的问题,模型再强也需要答案);外置状态(结构性载体不受任何窗口政策影响);验证器(客观信号与上下文无关)。判别一个投入抗不抗未来,就看它依赖什么:**依赖厂商计费规则的是套利,依赖模型能力边界的是工程**——检索存在因为 rot,不是因为 OpenAI 的价目表。
- **反方向的诚实(高级)**——也不能全押"永远需要":有效上下文长度确实在涨(NoLiMa 一代测的 16K 到如今年代的几十 K 量级),小语料场景"全塞进去+强模型"已经打赢检索栈。所以答案是分层的:语料规模小的,已经在被抹掉;规模大的,十年内看不到——**按自己的语料规模下注,而不是按行业口号**。
- **及格线 / 加分 / 红旗**:及格=rot 不可抹+举出抗未来的投入;加分=尾部注入的精确贬值分析+"套利 vs 工程"判据;红旗=二选一站队(全抹掉或永不变)。

**Q2.31【开放·L4·架构】为"上下文质量"设计一套可度量的指标体系,让你能在团队之间横向对比。**

讨论框架:
- **三层指标**——输入侧:信噪比(任务相关 token 占比,LLM 裁判抽样估计——抽 50 轮,标注每段内容"对当前任务是信号/噪声/有害",有害指过期文档这类主动误导项)、重复内容率、动态内容占比(尾部注入纪律的遵守率——头部出现动态内容即违规)。过程侧:缓存命中率、compact 频次与触发原因分布、上下文利用率峰值、每任务工具调用 token 分布。结果侧:任务成功率与上下文长度的相关性——**理想是弱相关**;强负相关说明 rot 在伤你(长度成了质量的税);关键信息放中段的探针任务通过率(主动测 U 型曲线的代价)。
- **横向对比的前提**——固定任务集;否则各团队的"质量"不可比(任务难度不同,指标差异可能只是任务的差异)。进一步:任务集要按类型分层(排错/特性开发/重构各一档),不然团队构成差异污染对比。
- **防博弈**——命中率可被"少干活"刷高(不检索不调用,命中率漂亮任务没完成):必须与任务成功率绑着看;信噪比可被"只放最少东西"刷高(保守上下文,缺信息任务失败)。**所有单一指标都能被 Goodhart,组合拳是必然**——给每个指标配一句"它的作弊方式是什么",这个思考本身就是面试加分项。
- **落地顺序**——先上过程侧(平台日志现成,零标注成本,当天出数),输入/结果侧后补(需要裁判与探针);先纵向(自己跟自己比,看趋势)再横向(团队间比,看分布)——纵向建立信任后,横向对比才不会变成甩锅工具。
- **及格线 / 加分 / 红旗**:及格=三层各至少一个具体指标;加分=防博弈设计与落地顺序;红旗=拿"总 token 消耗"当质量指标(那是成本,不是质量)。

---

## 2.6 工程实战题

**Q2.32【实战·L3·平台】自研 agent(Anthropic API)长会话缓存命中率只有 40%。给系统性的排查与优化方案。**

参考思路:
- **排查先行,方法论一条**——按请求维度 dump usage 三字段(cache_read/cache_creation/input),对 miss 请求做**前缀 diff**:system 变了?tools 变了?消息中段被改?断点漂移?还是 TTL 过期(看相邻请求时间差)?命中率问题九成能靠 diff 定位到具体断点——先定位再修,不猜。40% 这种数字背后通常是两三个稳定复现的破坏源,不是一个玄学问题。
- **按根因给修复**——tools 抖动→冻结定义与顺序(排序后注入);动态 system→尾部注入(system-reminder 模式);长会话→中段第二断点兜底;长人间隔→1h TTL。四类修复覆盖绝大多数 miss,每类修完看命中率曲线的台阶——修一个对一个,归因清楚。
- **结构性优化(修完单点再做)**——工具输出过滤前置(最大污染源从源头变小)、context editing 批量清理(驻留期治理)、高噪声任务拆 subagent(噪声根本不进主上下文)——这三个同时改善质量和成本,是"治本"层;单点修复是把命中率拉回及格线,结构性优化让它留在那里。
- **验证闭环**——命中率与单任务成本前后对比(量化收益);**必须回归任务成功率**——压缩加深可能丢信息,省钱把任务做砸了不算优化;固定任务集跑一遍,成功率持平或更好才算过关。
- **及格线 / 加分 / 红旗**:及格=diff 方法论+两大修复;加分=质量回归环节+修复与验证一一对应的归因习惯;红旗=不看 usage 字段凭感觉调。

**Q2.33【实战·L3·平台】设计一个工具结果压缩管道:20 种工具里 5 种(日志查询/大文件读取/HTTP 抓取)会返回巨量输出。**
> 场景白话:agent 调工具拿回的内容有时大得吓人——查一次日志几万行、读一个大文件全量、抓一个网页带全部 HTML;"巨量输出"会撑爆模型上下文(模型一次能"记住"的内容有上限),所以要做一个"压缩管道":结果进模型之前先把它变小。

参考思路:
- **四层管道按位置设计**——工具侧(强制分页/limit 参数,成为 API 契约的一部分;concise|detailed 输出枚举,Anthropic 口径 concise 省约 2/3——让"要多少"成为调用方的显式决定)→进入前(中间件按规则截断:头尾保留、错误优先——grep 思路把 error/warning 行捞出来;超长再上小模型摘要)→驻留期(超 N 轮的旧结果换占位符,可追溯可重取)→极端(超大输出外置文件,上下文只留路径+一句话摘要)。**位置越靠前越便宜**:进上下文之前裁掉的是零驻留成本,驻留期才裁的要付缓存断裂的代价。
- **先分类再动手**——5 种巨量工具不等于同一种治理:日志查询适合过滤(error 行优先,上下文里真正有人读的就是报错);大文件读取适合分页+offset(read(file, offset, limit) 契约);HTTP 抓取适合正文抽取(剥 HTML 标签和导航,正文才是信号)。**按工具类型配策略,而不是一套参数打天下**——"20 种工具里挑 5 种"这个题设真正想听的是这句。
- **防坑三件**——压缩不能破坏错误现场(排错需要原文,占位要可重取——"想看原始报错"必须拿得到);diff 类输出慎压缩(精确匹配被破坏,构建日志 diff 里那一个失败的行就是全部信号);小模型摘要要抽检质量(摘要错比没有更糟——错得自信,agent 顺着错的走)。
- **度量闭环**——按工具分别统计压缩率、每任务 token、TTFT、任务成功率;对比数据决定哪层再收紧——"哪个工具在倾倒 token"的答案常和数据直觉不符,度量先行再优化。
- **及格线 / 加分 / 红旗**:及格=四层给三层;加分=分工具配策略+逃生通道设计;红旗=一刀切截断不留重取通道。

**Q2.34【实战·L4·架构】大仓库 50 万行,设计"按需下钻的上下文路由":agent 默认只看到架构概览,通过工具按需取细节。定义工具集、缓存策略、防迷路机制。**
> 场景白话:50 万行代码的大仓库不可能整个塞给模型(装不下,塞多了反而记不住重点);"按需下钻"=默认只给一张"地图"(有哪些模块、谁调用谁),要深入哪块再用工具把那块的细节取过来——像人看地图先看全貌再放大局部。

参考思路:
- **工具集少而粗**——`get_architecture(node)` 当前层级结构、`zoom_in(node)` 下钻一层、`get_related(node)` 关联内容(调用方/被调用方)、`diff_architecture(commit)` 变更对比;写操作走人工确认。核心思想:把"塞满上下文"变成"教 agent 顺着结构请求上下文"——四个工具覆盖"看地图/放大/查关联/看变更"四个导航动作,再多就碎,再少就缺动作。
- **缓存策略**——架构图版本化(仓库变更才失效,平时是稳定前缀的一部分);下钻结果按 node 缓存(同一 node 二次访问吃缓存);主上下文只保留已访问路径的"面包屑"(node id 列表,几行 token)——**地图常驻、细节按需、足迹轻量**,三层正好对应三种缓存生命周期。
- **防迷路三件**——访问预算(最大下钻深度/次数,超了强制总结返回);面包屑回链(agent 能说出自己在结构里的位置,防止下钻五层后忘了任务);已探索区域记进结构化笔记防重复下钻(同一个 node 第三次 zoom_in 就是导航失败的信号,该查为什么)。
- **主动对比两个替代方案(本题真正的考点)**——与 aider repo map 同族(tree-sitter 把全仓符号压进约 1K token 常驻):何时图谱值得维护(架构复杂+长期项目,导航深度真的需要),何时 repo map 或纯 grep 就够(中小仓库——50 万行的题设下图谱开始划算,5 万行就不一定);与全仓 embedding 对比:结构化导航可解释可审计(能说出"为什么看了这个 node"),但维护成本高(架构变更要同步图谱)——**诚实评估维护成本是这个方案的主要风险**,答不出这层的方案是纸面方案。
- **及格线 / 加分 / 红旗**:及格=工具集+预算+面包屑;加分=与 repo map/embedding 的主动对比+维护成本诚实评估;红旗=几十个细粒度 getter(另一种上下文爆炸)。

**Q2.35【实战·L2·用户】你的 CLAUDE.md 膨胀到 600 行,agent 行为反而变差。怎么重构?**

参考思路:
- **诊断先行的机制解释**——600 行里混着三类:真约定(留)、某类任务才需要的(拆走)、过期的(删)。行为变差的机制有二:关键约定被长文稀释(600 行里的 30 行金子,注意力按比例分不到);过期内容在主动误导(去年就废弃的构建命令,agent 还在用)。**两个都是"多"的病**——行数本身是毒性,不是中性的。
- **重构动作**——按任务域拆成 skills/子目录文件(路径触发懒加载)/rules(paths 作用域——只在匹配路径的任务里进上下文);每行过"删掉会不会犯错"测试(不会就删);硬约束迁去 hooks/权限(确定性执行,不赌模型自觉);过期内容直接删——注释掉也会被读到,删比注释好。**重构不是搬运是再决策**:每一行都要重新挣自己的位置。
- **目标形态与验证**——根文件 <200 行,只留项目一句话、常用命令、硬约定、分层资产索引(指向拆出去的文件);重构前后跑固定任务集对比成功率——用数据证明"少即是多",不然说服不了同事("你把我们的文档删了"是要面对的政治问题,数据是唯一的辩护律师)。
- **防复发机制(加分项)**——给记忆文件定 review 节奏(月度清理,过期信号:引用行为消失/被反复误解);新增行过一遍"能不能从代码推断"——把门槛立在入口,别等下一次 600 行。
- **及格线 / 加分 / 红旗**:及格=拆分+删减+行数目标;加分=任务集验证+防复发机制;红旗=平均拆成 4 个 150 行文件全部常驻(总量没变,换个方式膨胀)。

---

## 2.7 场景排错题

**Q2.36【排错·L3·平台】现象:agent 每轮要 40–60 秒才开始吐字,账单 input 计费暴涨,cache_read 几乎为 0。最可能的三个根因?**

排查链路:
- **先从 TTFT 数量级反推**——40–60 秒的首字延迟是百万级 token 全量重算 prefill 的典型特征(正常秒级):这个数字本身就是"缓存没命中+前缀巨大"的指纹,**从症状直接缩小范围**是排错的第一步。配合账单(input 计费暴涨=原价计费的量巨大;cache_read≈0=基本没命中),三个证据指向同一结论,先定性质再找断点。
- **①前缀被动态化(第一怀疑)**——system/首条消息里的时间戳、环境快照、随机 id,每轮全量重算。验证:相邻两轮请求体 diff,看头部第一个分歧点——diff 是排错的主战工具,不是最后手段;定位到"system 第一行的时间戳"这种精确断点,修复就只是挪位置的事。
- **②tools 定义抖动**——MCP server 启动顺序/可用工具集轮间变化,工具数组不稳定,三层全失效。验证:diff 工具列表的顺序与内容(名称没变顺序变了也算变)。这类问题间歇出现(取决于启动顺序),比稳定 miss 更狡猾——日志里 miss 的分布像玄学,排序注入一招根治。
- **③TTL 失配**——每轮之间人工审查超过 TTL(5 分钟)。验证:相邻请求时间差,超过 5 分钟的间隔与 miss 对齐即可定罪;它的 TTFT 通常没前两个夸张(前缀没坏只是缓存过期),按间隔排除。
- **修复对应与闭环**——尾部注入/冻结+排序工具集/换 1h TTL;修后 TTFT 应回秒级、cache_read 占比回 90% 量级——**TTFT 本身就是免费的验证仪表**,不需要额外搭监控。
- **及格线 / 加分 / 红旗**:及格=前两个根因+diff 验证;加分=从 TTFT 数量级反推+用修复后的 TTFT 做验证闭环;红旗=归因"模型变慢了"去提工单。

**Q2.37【排错·L2·用户】现象:长会话中途 agent 突然"失忆"——忘了前面的决定,重新读文件,重复问已回答的问题。发生了什么?**

排查链路:
- **第一怀疑:auto-compact 刚触发**——对照时间线看压缩边界(先问:失忆是突然发生的还是渐进的?压缩丢东西是阶跃式的)。机制是摘要丢了早期决策,尤其两类高危信息:用户约束("只用 pnpm""别动迁移文件"这类一句话规矩,在摘要里长得像闲聊)和已否决的方案及原因(最容易被"保结论丢过程"的摘要当成过程丢掉——代价是 agent 重走一遍已证明失败的路线)。**"重新读文件"未必是失忆,可能是压缩后正常重接地;"重复问已答的问题"才是摘要丢信息的硬症状**——两个现象分开诊断。
- **次选两个**——子代理返回后主线程只拿到摘要(设计如此,看是不是预期内:subagent 场景下"忘了细节但记得结论"是正常形态);记忆文件被覆盖/截断(CLAUDE.md 被别的工具或人改了,检查文件修改时间和 git 历史)。三个根因的鉴别方法:看失忆的范围——全丢=压缩;细节丢结论在=子代理;约定丢=记忆文件。
- **修复习惯(比修 bug 更重要)**——重要决定让 agent 当场写进 todo/进度文件:结构性载体每轮重新注入、不受压缩影响——这是把"记忆"从概率性的摘要迁到确定性的文件。压缩发生后主动做一次"重新锚定"(复述任务清单和约束再继续),成本一轮,买回大部分损失。
- **及格线 / 加分 / 红旗**:及格=定位 compaction;加分=三类失忆的鉴别方法+外置习惯;红旗=以为模型"记忆体"坏了,反复重启会话(重启丢得更多)。

**Q2.38【排错·L3·平台】现象:接入 8 个 MCP server 后,agent 选工具经常选错、响应变慢、成本翻倍,部分请求超上下文。诊断与处置?**

排查链路:
- **定性:工具过载的三个伤害同时出现**——定义占用(多 server 未干活先吃数万 token,Anthropic 实例约 55K:8 个 server 轻松吃掉几万,直接推高每轮成本、部分请求超限)→成本翻倍与超限;选择退化(工具一多模型选错率上升,30–50 个工具以上准确率明显下降)→选错工具;定义膨胀还可能破坏缓存(工具列表不稳)→变慢的另一半原因。**三个症状对应同一个根因,先定性再动手**——"选错工具"单看会被误诊为模型不行,连着成本和延迟一起看,过载就是唯一解释。
- **处置分层(按性价比)**——启用 tool_search/延迟加载(只留名称,命中再展开,省 85%+ 定义 token——先做,对行为影响最小);按任务域拆 subagent(每个只挂相关工具,选择面变小准确率回升);合并语义重叠工具(8 个 server 里常有三个都能查数据库——合并成一个);低频 server 直接砍(用使用日志说话,半年没用过的工具不配占前缀)。
- **验证三指标**——工具选择准确率、单任务 token、TTFT 前后对比;不达标继续砍,达标了固化配置进基线(把"挂哪些 server"当受控配置管理,不是随手的开发习惯)。
- **及格线 / 加分 / 红旗**:及格=过载机制+延迟加载;加分=指出缓存受害的二阶效应+砍工具的日志依据;红旗=归因模型不行,换更贵的模型硬扛(定义 token 照吃,还更贵)。

**Q2.39【排错·L4·平台】现象:compaction 之后任务成功率从 85% 掉到 60%。怎么定位压缩丢了什么?**

排查链路:
- **第一步:构造对照隔离变量**——同批任务分别跑"压缩前终止+新会话注入摘要续跑"与"不压缩短任务";两个条件只差压缩这一个变量,差异归因才干净——直接在生产流量上对比,任务难度分布不同,掉的那 25 个点可能混着任务本身的方差。**排错题的第一步永远是"把变量隔离出来"**,不是急着改配置。
- **第二步:对失败 case 做差异归类**——逐个比对摘要 vs 原文,把丢失的信息分类:早期用户约束?失败的尝试及原因(丢了导致重蹈覆辙)?文件路径(丢了导致重新探索)?任务边界(丢了导致范围蔓延)?**高频丢失类别就是修复靶点**——归类比逐案修重要:20 个失败 case 若 14 个都丢了"已否决方案",要修的是摘要器对这一类的盲区,不是 20 个各自的问题。
- **第三步:按类别外置修复**——高频丢失的类别迁到结构性载体:用户约束进 CLAUDE.md/任务文件;失败尝试写进度笔记(动手前先记"已试过 X 不行,因为 Y");调整 /compact 的 focus 指令显式点名保留类别;必要时换"checkpoint+新会话+结构化交接"替代在线压缩——**修复的本质是把该类别信息从摘要通道迁走,而不是求摘要器变全能**。
- **第四步:固化进回归**——把这次的失败 case 转成 eval 集,压缩策略任何改动(阈值/指令/模型升级)先跑这组——同类问题只允许认真修一次。
- **及格线 / 加分 / 红旗**:及格=对照实验+差异归类;加分=外置方案落到具体载体+回归固化;红旗=调大上下文窗口绕过(根因是信息丢了不是空间不够,钱解决不了 rot)。

---

## 2.8 开放加餐:偏好与品味追问

**Q2.40【开放·L3·用户】你写 CLAUDE.md/AGENTS.md 的个人风格是什么?哪几条铁律是踩坑踩出来的?**

参考思路(一个好答案长什么样):
- **有固定的文件骨架**——验证命令放哪、禁区放哪、项目事实放哪,张口能说出比例:"我的文件八成是验证命令和项目怪癖,规范类不超过两成"——**结构反映对"什么值得写"的判断**:验证命令和怪癖是"不可从代码推断的事实",规范类多半可推断、模型自己会发现。比例本身就是一套压缩过的价值观。
- **铁律带来历**——"有一次它把生成的迁移脚本直接执行了,从此我加了'迁移一律先出 dry-run diff'";"有一次它把 warning 当 error 修,顺手改了锁文件,从此我写明'warn 不修,只修 error'"。**每条铁律背后有一个具体事故**——没有事故来历的规则多半是抄来的模板,面试官追问"这条为什么存在"就露馅。反向也成立:真踩过坑的人规则写得极具体(带命令、带文件名、带例外条件),因为痛感具体。
- **知道文件是活的**——多久回改一次(真实答案:跟仓库节奏,大重构后必过一遍)、怎么判断一条过期了(引用它的行为消失/被反复误解/对应的目录都没了):把记忆文件当代码对待(有 review、有清理),不是写完就供着。
- **边界感**——知道自己不写什么:不写模型自己能查到的(目录结构)、不写硬约束(进 hooks)、不写一次性任务的临时要求(进当次 prompt)——**"不写什么"的清单比"写什么"更能区分老手**。
- **及格线 / 加分 / 红旗**:及格=有骨架+一条带来历的铁律;加分=清理习惯、"过期判断"和明确的"不写"清单;红旗=背通用模板条款、说不出任何一条自己的规则。

**Q2.41【开放·L3·平台】上下文快满时,你倾向让 harness 自动压缩,还是自己手动触发?你的处置习惯是什么?**

参考思路(一个好答案长什么样):
- **先表态再讲 trade-off**——自动省心但不可控(压缩时机和保留策略是黑盒,可能在任务最要紧的中段丢关键约束);手动可控但要有自觉(忘了就爆窗,爆窗比有损压缩更糟——直接截断,连摘要都没有)。多数老手的答案是"平时自动+关键节点手动":把自动当兜底保险丝,不当日常依赖。
- **预防性压缩的习惯**——在阶段性收尾(一个子任务完成、测试转绿)时主动压,而不是等满了被动压:**被动压缩发生在任务中途,最容易丢"进行到哪、为什么这么做"**——收尾点的信息结构最清晰(该保留的都已成结论),中段的信息最纠缠(一半是进行时的中间态)。挑时机压,损失的期望最小。
- **压缩后的动作**——重新锚定关键约束(任务清单/禁区/未决问题),新会话先让它复述理解再继续(与 ch06 的"丢失后重新接地"同一纪律);能说出压缩后最容易丢的三样东西的,是真踩过的:早期用户约束、已否决方案、任务边界。
- **及格线 / 加分 / 红旗**:及格=两个 trade-off 说清;加分=预防性压缩的时机逻辑+压缩后重新锚定;红旗=从没想过、永远被动等自动。
