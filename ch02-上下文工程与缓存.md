# ch02 · 上下文工程、缓存与检索(重点章)

> 覆盖:context engineering 的定义与落地、上下文构成、context rot、prompt caching(前缀匹配/KV cache)、稳定前缀设计、compaction 与工具结果清理、agentic RAG(检索作为工具、迭代检索、query 改写、路由、Self-RAG/CRAG)、RAG vs 长上下文 vs agentic search 的选型。
> 案例基线:2026-08。机制类知识(缓存原理、压缩策略)不随时间变化;带日期的案例以标注为准。

---

## 2.1 八股题(基础概念)

**Q2.1【八股·L1·平台】给 context engineering 下一个工作定义,并说说它和 prompt engineering 的关系。**

- **人话版**:prompt engineering 是琢磨"这句话怎么措辞",context engineering 是琢磨"模型干活时该看到哪些东西"——system prompt、工具定义、历史、文件内容、检索结果,全是你的管理对象。前者是后者的子集。
- **展开**:
  - **行业的收敛定义**:Anthropic(2025-09《Effective context engineering for AI agents》)的说法是,上下文工程是"策划什么进入有限上下文窗口的艺术与科学",目标是**找到能最大化任务达成概率的最小高信号 token 集**。LangChain 的 Harrison Chase 差不多同期给的版本是"构建动态系统,在正确的时机、以正确的格式,提供正确的信息和工具"。
  - **为什么 2025 年之后这个词取代了 prompt engineering**:单轮对话时代你只控制一段话;agent 时代每一轮的上下文都是动态拼出来的(工具结果、检索内容、记忆文件、子代理摘要),prompt 只占其中一小块。管理对象从"一段话"变成了"一条持续变化的信息流"。
  - **核心不是多喂,是控制信噪比**:上下文里每多一段无关内容,不只是花钱,还会稀释模型对真正相关内容的注意力(见 context rot,Q2.6)。所以上下文工程经常是做减法——删过期文档、截断工具输出、把大块内容外置成路径。
- **及格线**:能说出"上下文=推理时模型可见的全部 token,包括工具定义和历史",并说清 prompt 只是其中一部分。
- **加分**:能引用"最小高信号 token 集"这个目标函数,并举一个自己做减法改进效果的实际例子。
- **红旗**:把 context engineering 等同于"把 prompt 写长写细";或者认为它就是 RAG——检索只是往上下文里放内容的一种方式。

**Q2.2【八股·L1·平台】一个 coding agent 每轮请求的上下文由哪几部分构成?为什么说"每轮都重发全部历史"是 agent 成本模型的基石?**

- **人话版**:五块——system prompt、工具定义、记忆文件(CLAUDE.md/AGENTS.md)、对话历史、工具结果(文件内容、命令输出、搜索结果)。HTTP 是无状态的,模型也是无状态的,所以每一轮都要把前面所有东西原样再发一遍。
- **展开**:
  - **量级感**(以 Claude Code 为例,社区对泄露 system prompt 的分析口径):system prompt 是数百条字符串动态拼装,万 token 级;每多挂一个 MCP server,工具定义就多吃几千到几万 token;工具输出是最大的不定项,一个 `npm install` 日志就能吃掉上万 token。
  - **重发历史的直接后果**:一个 30 轮会话的累计输入 token 不是"平均长度×轮数"这么简单可以忽略的——它是逐轮递增的,第 30 轮要把前 29 轮全部重发。这就是为什么 prompt caching(0.1× 计费)和 compaction(压缩历史)是 agent 工程里投入产出比最高的两件事(见 Q2.9、Q2.15)。
  - **设计推论**:任何"每轮都变的东西"(时间戳、随机 id、动态配置)如果放在请求头部,会让重发的部分从缓存价变回原价——这直接引出"稳定前缀"纪律(Q2.3)。
- **及格线**:能列出至少四类构成,并说清"无状态 API→全量重发"这个因果。
- **加分**:能报出自己项目里各块的实际 token 占比("我们 system+tools 大概 12K,历史是主要膨胀项"),或有工具输出截断的具体实践。
- **红旗**:不知道工具定义也计费;以为历史"只在第一轮发一次"。

**Q2.3【八股·L1·用户】什么是稳定前缀(stable prefix)?为什么改一行工具定义是缓存上最贵的操作?**

- **人话版**:请求里从第一个 token 开始、跨多轮逐字不变的那一段(system prompt + 工具定义 + 早期对话)。所有厂商的 prompt caching 都按前缀匹配——从第一个不一样的 token 开始,后面的缓存全部作废。工具定义排在最前面,所以改它等于全清零。
- **展开**:
  - **前缀匹配的机械原理**:缓存系统按 token 块做哈希,逐块比对。前缀一致就复用 KV cache,按约 1/10 价格计费;一旦某块不同,该块及之后所有块的缓存全部失效,按原价重算。这个"一变全废"是级联的,不是局部的。
  - **agent loop 天然适合缓存**:每轮只是往尾部追加新消息,前缀单调增长,所以理想情况下每轮只有新增部分按原价、其余 0.1×。
  - **为什么工具定义最致命**:三家厂商的缓存层级都是 tools→system→messages,tools 在最上游。改一个工具描述、增删一个工具、甚至改变工具数组顺序,都会让 tools 层哈希变化,三层缓存一起作废。相比之下,在最新一条消息里改内容几乎零成本——这就是动态信息要走"尾部注入"的原因(见 Q2.12)。
- **及格线**:能说清前缀匹配 + "从分歧点开始全失效";知道 tools 定义在最前。
- **加分**:能说出自己踩过的坑(比如 MCP server 按启动顺序注册工具导致轮间顺序抖动),或主动提"注入一个时间戳和改工具定义是同一级别的事故"。
- **红旗**:认为缓存命中是"内容相似就命中"——必须逐 token 相同。

**Q2.4【八股·L2·用户】Claude Code 的自动压缩(auto-compact)大致怎么工作?压缩时保什么、丢什么?**

- **人话版**:快到上下文上限时先做轻清理——把比较老的工具输出替换成占位符;不够再做大压缩——把整个会话摘要重写。摘要会保留任务目标、关键决策和最近改过的文件路径,丢掉原始工具输出和中间思考过程。
- **展开**:
  - **两段式**:第一段是清理旧 tool_result(社区叫 microcompact),价值衰减最快的先走;第二段才是对整个会话做摘要(compaction)。触发点默认在接近模型窗口上限时(约 92–95% 占用)。
  - **保留策略的取舍逻辑**(以 Claude Code 的公开行为为准):system prompt 和 CLAUDE.md 重新注入;最近编辑过的少数几个文件重读(超过几 K token 的大文件只留路径);对话中的决策和结论进摘要;verbatim 的工具输出、中间思考全部丢弃。判断标准是"重建成本低的外置,决策性的保留"——文件可以随时重读,所以只留路径;用户在三小时前说的约束没法重读,所以必须进摘要。
  - **可以定制**:`/compact` 支持带指令("保留所有 API 兼容性决策"),这个指令可以常驻在 CLAUDE.md 里。反复触发压缩会撞上 thrashing 检测直接报错——产品层面在逼你开新会话,这是有意设计。
- **及格线**:知道"接近上限触发、先清工具输出再摘要、文件只留路径"三件事。
- **加分**:能讲出"压缩是有损 GC,关键结论应该外置成文件而不是赌摘要"这条原则,并联系自己长任务里写进度笔记的习惯。
- **红旗**:以为 compaction 是无损的、或者以为上下文窗口够大就不用管压缩(context rot 在远早于上限的地方就开始生效)。

**Q2.5【八股·L2·用户】什么是工具结果清理(tool result clearing)?从写入到驻留,有几个可以动手的层次?**

- **人话版**:工具输出是上下文最大的污染源——一万行日志看第二眼的价值是零,但它一直躺在历史里。治理可以从四个时机下手:进上下文之前过滤、写入时截断、驻留期替换成占位符、极端情况整段摘要。
- **展开**:
  - **写入前过滤**:PreToolUse/PostToolUse hook 或中间件层面,把 1 万行日志过滤成几百 token 的关键行(错误优先、头尾保留)。这是最便宜的层次,信息还没进上下文就被裁了。
  - **截断**:默认上限(常见 25K token 级)、超长文件只返回路径加首尾。简单粗暴,但排错时可能正好截掉现场——所以要有"要看原文再说一次"的逃生通道。
  - **驻留期清理**:把超过 N 轮的旧 tool_result 替换成占位文本("原始输出已清理,可重新调用")。Anthropic 的 context editing API 就是产品化的这一层,默认保留最近 3 组。
  - **隔离层**:让 subagent 在自己的窗口里烧掉几万 token 的搜索噪声,只回传 1–2K 摘要——这是"清理"的极端形式,从源头就没让它进主上下文。
- **及格线**:能说出至少三个层次并各举一个例子。
- **加分**:指出"清理会使清理点之后的缓存失效,所以要批量清而不是涓流清"(这是 context editing 设计 clear_at_least 参数的原因)。
- **红旗**:只想到"截断"一层;没意识到旧工具结果还占着上下文持续产生注意力稀释。

**Q2.6【八股·L1·模型】什么是 context rot / lost in the middle?对"堆上下文"的做法意味着什么?**

- **人话版**:上下文越长、塞的东西越多,模型对每一段的利用率越低。经典实验是:关键信息放开头或结尾时效果好,放中间明显变差(U 型曲线)。它是缓慢滑坡,不是到某个长度才断崖。
- **展开**:
  - **两个标志性实证**:Lost in the Middle(Liu et al., TACL 2023)在多文档 QA 里发现金标段放中间时准确率显著低于放开头结尾,甚至低于不给上下文;Context Rot(Salesforce,2024)把任务拆成工作记忆/检索/推理三个维度,发现即便极简任务也随长度系统性退化,且多维复合时恶化超过单项之和。
  - **机制解释**(Anthropic 的口径):n 个 token 产生 n² 个注意力对,每个 token 分到的"注意力预算"被稀释;训练数据大多偏短,模型没学过在超长范围里维持精确的间接寻址;位置编码插值在远距离失真。三个因素叠加,所以是梯度下滑。
  - **工程含义**:上下文是稀缺资源,"多总比少好"在超过几十 K 之后反转。这直接论证了按需检索(just-in-time retrieval)和压缩的必要性——不是为了省钱,是为了质量。
- **及格线**:能说出 U 型曲线和"滑坡不是悬崖"。
- **加分**:能联系 NoLiMa(见 Q2.14)说明标称窗口≠有效窗口,或给出自己观察到的"上下文一长 agent 开始忘事"的实际经历。
- **红旗**:拿"窗口 1M"当"能用好 1M"来设计系统。

---

## 2.2 原理深挖(缓存与压缩机制)

**Q2.7【原理·L2·平台】Anthropic / OpenAI / Gemini 三家的 prompt caching 机制有什么差异?设计上的选型影响是什么?**

- **人话版**:Anthropic 是显式打点——你自己在请求里标 cache_control 断点;OpenAI 是全自动——前缀一样自动命中,不用管;Gemini 两轨——默认隐式缓存,另有显式 cachedContent 但要付存储费。
- **展开**:
  - **Anthropic(显式断点)**:`cache_control` 标记,每请求最多 4 个断点,层级固定 tools→system→messages;要求前缀 100% 逐 token 相同;TTL 5 分钟(默认)或 1 小时(可选);命中部分读 0.1×,写入付 1.25×(5min)/2×(1h)溢价;有最短可缓存长度(约 1K–4K token,低于下限静默不缓存);命中不计入速率限制。显式的好处是行为可预测,坏处是你得自己设计断点位置。
  - **OpenAI(全自动)**:无需任何参数,1024 token 起按 128-token 块匹配;折扣约 90%;保留 5–10 分钟(闲时可延长),另有按小时计的长效选项;用 prompt 前缀哈希做路由,提供 prompt_cache_key 参数做会话粘性。省心,但命中率更依赖服务端负载状况。
  - **Gemini(双轨)**:隐式缓存默认开启(有最少 token 门槛,折扣 75–90%);显式 cachedContent 适合确定会反复用到的大前缀,但要付存储费(按 token·小时)。选型影响:轻会话场景三家都差不多;重前缀(大型工具集+长 system)场景,Anthropic 的断点可控性和 OpenAI 的自动化各有拥趸;离线批量复用同一大前缀时 Gemini 显式缓存反而最便宜。
- **及格线**:能说出"显式打点 vs 自动 vs 双轨"三家各一句,以及 0.1× 这个量级。
- **加分**:知道 TTL 与自己业务节奏的匹配问题(人审间隔超过 5 分钟要考虑 1h TTL);知道低于最短缓存长度不报错只是静默 miss。
- **红旗**:把 prompt caching 和 KV cache 说成两个不相关的东西(见 Q2.8);以为 OpenAI 也需要手动打点。

**Q2.8【原理·L3·模型】prompt caching 和 KV cache 是什么关系?厂商为什么愿意给缓存命中的部分打一折?**

- **人话版**:KV cache 是 Transformer 解码时的计算优化——已经算过的 K/V 向量存下来,新 token 不用对全历史重算 attention。厂商的 prompt caching 本质就是把这份 KV cache 跨请求复用起来。命中时服务商几乎不用付计算成本,折扣就是省下的计算分你一半。
- **展开**:
  - **KV cache 是什么**:自回归解码时,每生成一个新 token 都要对全部历史做 attention;KV cache 把每层已算出的 Key/Value 张量留在显存里,避免每步重算。它的代价是显存占用随上下文线性涨,这也是长上下文推理贵的根源之一。
  - **跨请求复用怎么做**:推理引擎(vLLM/SGLang 这类)对请求的 token 块做哈希,发现新请求的前缀和某份已缓存的 KV 相同,就跳过 prefill(预填充)阶段直接复用。prefill 恰恰是长上下文最贵的部分——所以前缀命中省的是大头。
  - **折扣的经济学**:对你省 90% 输入费;对厂商,prefill 计算接近零、只付显存驻留成本,还换来了更粘性的长会话负载。双赢所以这个价格能长期成立。同理也解释了 TTL 的存在——KV cache 占显存,不可能无限期为你保留。
- **及格线**:能独立讲清"KV cache=单请求内避免重算,prompt caching=跨请求复用 KV cache"这层关系。
- **加分**:从显存成本角度解释 TTL 和最短缓存长度为什么存在;或提到 PagedAttention 这类显存管理技术对缓存能力的影响。
- **红旗**:认为缓存命中是"语义相似匹配"——是逐 token 的精确匹配。

**Q2.9【原理·L3·平台】列举至少 5 种会破坏缓存命中的操作,按杀伤力排序,并说说各自怎么修。**

- **人话版**:按杀伤力:改工具定义(全失效)> system prompt 里放动态内容(时间戳/随机 id)> 在历史中间插改内容 > 断点位置漂移 > TTL 过期。共同规律是:越靠前的改动,作废得越彻底。
- **展开**:
  - **①改 tools 定义**:三层全失效。修法:工具集进入会话后冻结;要改就开新会话;MCP server 的工具注册要按固定顺序排序。
  - **②system prompt 动态化**:时间戳、"今天是周几"、环境快照、用户名随机拼接。修法:动态内容一律移到最新用户消息或 tool_result 里注入(tail injection / system-reminder 模式,见 Q2.12)。
  - **③消息序列中段被改**:回溯编辑历史、中间插图片、消息重排。修法:历史 append-only,任何"补充说明"都追加在尾部。
  - **④断点/块边界漂移**(显式断点厂商):消息数量增长导致断点回看不到之前的写入块。修法:长会话在前缀中段加第二个断点兜底。
  - **⑤TTL 过期**:5 分钟 TTL 下,人审 8 分钟再发下一轮,缓存已经蒸发。修法:改 1h TTL(写溢价 2× 但仍划算),或调整工作节奏。2026-07 OpenAI 把 Codex 上下文上限从 372K 降到 272K 引发社区强烈反弹,也说明缓存相关的容量决策直接影响用户成本体感。
- **及格线**:至少说出前两条并解释"级联失效"。
- **加分**:能给出排查方法(相邻两轮请求体做前缀 diff,看 miss 的精确位置);知道"命中部分不计速率限制"这类二阶细节。
- **红旗**:认为"内容没变只是重新序列化了一下"没关系——序列化顺序变化同样 miss。

**Q2.10【原理·L3·平台】Claude Code 用 `<system-reminder>` 把动态信息塞进消息里,而不是改 system prompt。这个设计解决了什么矛盾?自己实现等价机制要注意什么?**

- **人话版**:system prompt 必须逐字稳定才能吃缓存,但 agent 运行时总有动态信息要告诉模型(todo 状态、权限提醒、当前目录)。解法是把动态内容包在标签里、嵌进**最新的用户消息或工具结果**——尾部追加不碰前缀,缓存无损。
- **展开**:
  - **解决的矛盾**:稳定前缀(缓存)vs 运行时状态(必须每轮更新)。尾部注入让两者共存:前缀保持逐字不变,动态内容随最新消息进来,用完即沉入历史。
  - **泄露的 system prompt 里能看到契约**:官方明确写了"工具结果和用户消息可能包含 `<system-reminder>` 标签……它们与所在工具结果无直接关系"——这句自描述很重要,防止模型把提醒误解为工具输出的一部分。
  - **自建时的三个注意**:注入点必须在稳定前缀之后(最新消息内);标签要自描述(让模型知道这是系统级提醒、不是用户指令、也不是工具返回);内容要幂等或可丢弃,防止同类提醒在历史里无限堆积。
- **及格线**:说清"尾部注入不破坏前缀"这个核心。
- **加分**:指出这同时是安全设计——提醒的措辞固定、来源明确,比每轮现拼一段"系统提示"更可控;能聊"注入的内容沉入历史后要不要后续清理"。
- **红旗**:把动态信息改成每次注入到 system prompt 尾部——system 层整体哈希变了照样全失效。

**Q2.11【原理·L3·平台】CLAUDE.md / AGENTS.md 这类项目记忆文件,在缓存设计里扮演什么角色?两家的加载层级有什么差异?**

- **人话版**:项目记忆放在请求前部,每轮都重发,但因为有缓存,重发成本是 0.1×——这就是"常驻上下文"在经济上可行的原因。Claude Code 是四层拼接,Codex 的 AGENTS.md 是从根目录到当前目录逐级拼接、就近生效。
- **展开**:
  - **缓存角色**:记忆文件内容进 system/首条消息,属于稳定前缀的一部分——只要文件不变,每一轮都按缓存价"重读"。所以记忆文件追求的不是"少写"而是"稳定+高信号",改一次记忆文件等于付一次缓存重建成本。
  - **Claude Code 的四层**:企业托管(MDM 下发、不可排除)→ 用户全局 `~/.claude/CLAUDE.md` → 项目根 `./CLAUDE.md`(入 git)→ `CLAUDE.local.md`(个人不入 git);自根向下**拼接而非覆盖**;子目录的 CLAUDE.md 按需懒加载(agent 碰到该目录的文件才注入);`@path` 语法可导入其他文件,递归上限 4 跳。
  - **Codex 的 AGENTS.md**:`~/.codex/AGENTS.md` → 仓库根 → 各级目录直到 cwd,逐级拼接,越靠近 cwd 优先级越高,默认总量有上限(约 32KiB)。同一份 AGENTS.md 被 Codex、Gemini CLI、Cursor 等几十家工具支持,已成跨厂商事实标准——维护一份记忆服务 N 个工具,比每个工具维护一份划算,这是它能收敛的根本原因。
- **及格线**:说出"拼接不覆盖"和"子目录按需加载"两个机制点。
- **加分**:从缓存角度解释"记忆文件为什么不应该频繁改动";知道 AGENTS.md 的跨厂商收敛逻辑。
- **红旗**:以为子目录的 CLAUDE.md 会覆盖根文件;以为记忆文件不计费。

**Q2.12【原理·L4·模型】NoLiMa 这类基准测出了什么?为什么说"标称窗口长度 ≠ 有效上下文长度"?**

- **人话版**:NoLiMa 专测"问题里没有你要找的那个词的字面出现"的场景——逼模型做联想而不是字面匹配。结果:一堆号称 128K+ 的模型,32K 时就掉到短文本基线的一半以下。所以营销数字是窗口,工程要按有效长度设计。
- **展开**:
  - **方法**:NoLiMa(Modarressi et al., ICML 2025)构造的问题与"针"没有字面重叠(比如问 A,答案段只提它的同义描述),模型必须做联想推理才能定位。这比大海捞针(NIAH)难得多——NIAH 只考验字面匹配,会系统性高估长上下文能力。
  - **结果量级**:13 个标称 128K+ 的模型,输入不到 1K 时都表现不错;拉到 32K 时 13 个里 11 个跌破短文本基线的 50%;当时最好的模型有效长度也只有约 16K 量级,最差的从 73.6 掉到 0.9。
  - **工程换算**:如果你的设计依赖"把 50 万 token 塞进去模型都能用好",大概率在赌一个不成立的假设。有效工作内存按十几 K 到几十 K 估计更安全,超出部分靠检索(要用哪段取哪段)和压缩(留下结论丢掉过程)来支撑——这与 Q2.6 的 context rot 结论互相印证。
- **及格线**:能说清"联想检索 vs 字面匹配"这个区分,以及"有效长度远小于标称长度"的结论。
- **加分**:指出 NIAH 类基准的局限并因此对厂商的"长上下文通过率"图表保持怀疑;能给出自己项目里的窗口预算经验值。
- **红旗**:用"窗口 1M"论证"不需要检索和压缩"。

**Q2.13【原理·L3·平台】compaction 的信息损失怎么管理?对比 Claude Code 和 Codex CLI 的保留策略,通用原则是什么?**

- **人话版**:压缩是有损 GC。原则是保"决策与结论"、丢"过程与原文"——因为文件可以重读、命令可以重跑,而用户两小时前说的约束没法重新获得。关键状态别赌摘要,外置成文件。
- **展开**:
  - **Claude Code 的取舍**:保 system prompt、CLAUDE.md、当前 plan、最近改过的少数文件(大文件只留路径);丢全部 verbatim 工具输出和中间思考。反复压缩触发 thrashing 报错,逼你开新会话。
  - **Codex CLI 的取舍**(Rust 实现的开源代码可查):摘要后按预算从新到旧装回最近用户消息,外加一份 CompactionSummary 和初始环境基线;有个值得学的细节——mid-turn(执行中途)触发压缩时,摘要插在**最后一条真实用户消息之前**,因为模型被训练成把摘要当"历史的一部分"而不是"新指令"。
  - **通用原则**:压缩解决的是"塞不下",不是"记不住"。真正要跨窗口存活的状态(任务目标、验收标准、已否决的方案及原因)应该写进 todo/进度文件/记忆文件这些结构性载体,它们每轮重新注入且不受有损摘要影响。Anthropic 长任务实验里用 JSON 进度文件+固定开机序列跑通数天级任务,就是这个思路(见 ch06)。
- **及格线**:说出"保结论丢过程"和"关键状态外置"两条。
- **加分**:能对比两家任何一个具体差异并评价;知道压缩点之后缓存失效带来的成本考量。
- **红旗**:认为"压缩完模型还记得所有细节";把 compaction 当成唯一的长任务方案。

**Q2.14【原理·L4·平台】Anthropic 的 context editing / memory tool / token counting 三件套分别解决什么?为什么清理工具结果反而要配 `clear_at_least` 这种"至少清多少"的参数?**

- **人话版**:context editing 自动把老工具结果换成占位符;memory tool 给模型一个文件式记忆目录,重要结论先写进去再清理;token counting 让你在撞墙前主动触发压缩。clear_at_least 存在是因为清理会打断缓存——每次只清一点等于反复把缓存作废,不如攒一波批量清。
- **展开**:
  - **三件套的分工**:editing 管"上下文里的垃圾"(默认约 100K input tokens 触发,从最老的 tool_result 开始替换,保留最近 3 组,客户端保留完整原始历史);memory tool 管"别丢的重要信息"(清理前会提醒模型先把重要结果写进记忆);token counting 管"什么时候动手"(免费接口,返回编辑前后 token 数,把压缩从被动撞限变成主动调度)。
  - **clear_at_least 的经济学**:清理是替换历史中的内容,清理点之后的缓存前缀全部失效。如果每轮清一点,每轮都付一次"从清理点到末尾的全价重算";攒够一批再清,每次失效的代价"值回票价"。这是"有损操作要批量化"的一般工程原则。
  - **官方量级**(2025-09 发布口径):memory+editing 组合在 agentic search 任务上比基线 +39%(仅 editing +29%);100 轮 web search 任务 token 消耗降 84%。这两个数字说明清理的收益主要来自"工具输出占上下文大头"的真实负载。
- **及格线**:三件套各一句 + 说清清理导致缓存失效。
- **加分**:能推出 clear_at_least 的存在理由而不需要提示;指出"客户端保留完整历史"意味着可以随时恢复(设计上的安全垫)。
- **红旗**:以为清理是无代价的、越多越频繁越好。

**Q2.15【原理·L3·平台】算一笔账:30 轮长会话,平均历史 300K token,输入价 $3/MTok、缓存读 $0.30。缓存 90% 命中 vs 全 miss,输入成本差多少?**

- **人话版**:30 轮累计输入约 9M token。全 miss 是 9M×$3 = $27;90% 命中是 8.1M×$0.30 + 0.9M×$3 ≈ $5.1。差 5 倍多——而且会话越长、命中率越高,差距越接近 10 倍。
- **展开**:
  - **计算过程**:每轮输入≈当前历史长度(逐轮递增,这里取均值近似);累计 9M;命中部分按 0.1× 计费,未命中按原价,首轮还要付一次约 1.25× 的写溢价(量级几毛钱,可提但别让它淹没主干)。
  - **从这个数字推出的三个行动**:第一,缓存命中率是 agent 平台最大的成本杠杆,命中率从 60% 提到 90% 输入费近乎腰斩再打折;第二,"空闲也在花钱"——后台 agent/定时任务每轮都按缓存价重读全部长上下文,长驻会话的固定成本不可忽略;第三,超过 TTL 的间隔(比如人审 8 分钟)每发生一次就是一轮全价,这类"节奏税"要通过 TTL 选型或流程设计消除。
  - **汇报口径建议**:单看总账容易吵,按"每任务输入成本"和"缓存命中率"两个指标拆开,优化效果才可归因。
- **及格线**:算对量级(约 5 倍差距),说得出"重发历史被缓存摊薄"。
- **加分**:主动提写溢价、TTL、空闲成本这些二阶项;能把结论推广成"缓存命中率是第一成本杠杆"。
- **红旗**:把缓存省钱误解为"厂商送的,不用管";算出 10 倍就停,忽略未命中 10% 仍按原价这部分。

**Q2.16【原理·L3·模型】为什么上下文翻倍,检索和推理质量下降得比线性快?用注意力预算解释。**

- **人话版**:n 个 token 产生 n² 个注意力对,长度翻倍注意力关系翻四倍,每个 token 分到的注意力被稀释。更糟的是"找出来、记住、组合"三层能力都随长度衰减,还乘法叠加——所以恶化是超线性的。
- **展开**:
  - **n² 的含义**:注意力是全局的,每对 token 之间都有一个权重。长度翻倍,计算量翻四倍,而"预算"摊到每对上的密度下降;再叠加训练数据偏短(模型没见过这么长的精确指代任务)和位置编码在远距离的插值失真,退化被放大。
  - **复合效应**:Context Rot 的核心结论是工作记忆、检索、推理三个维度各自衰减,复合任务(要先找到、再记住、再组合)的恶化超过单项之和。真实工程任务几乎都是复合任务。
  - **设计推论**:给上下文设"质量预算"而不只设"长度预算"——超过某个经验拐点(几十 K 量级)后,继续堆历史的质量成本开始超过它省下的检索往返成本。这时该做的不是更大的窗口,而是把过程外置、只留结论。
- **及格线**:n² 稀释 + "复合叠加"两点。
- **加分**:能推出"质量拐点先于长度上限到来"并给出自己观察;把结论落到"上下文是稀缺资源要预算管理"。
- **红旗**:只用"贵"解释为什么要压缩——质量退化和成本是两个独立的理由。

---

## 2.3 Agentic RAG:检索作为工具(新知识点)

**Q2.17【八股·L2·架构】Agentic RAG 和传统 RAG 的本质区别是什么?"agentic"体现在哪三个决策权上?**

- **人话版**:传统 RAG 是固定管道——每个查询必然检索、必然 top-k、必然一次性塞进 prompt。Agentic RAG 把检索变成 agent 的一个工具:要不要检索、查哪个源、查几轮,都是模型在运行时自己决定的。
- **展开**:
  - **三个决策权**(微软 Azure 架构指南 2026-06 的口径,行业已收敛):是否检索(也许模型本来就知道)、检索什么源(向量库/关键词/网页/API 各是不同工具)、检索几步(首轮结果不好就改写再查,迭代到够用为止)。
  - **形态上的具体体现**:LangGraph 官方教程的五节点图是标准实现——`generate_query_or_respond`(模型先决定"直接回答还是调用 retrieve")→ `retrieve` → `grade_documents`(逐篇判相关性)→ `generate_answer`(只用相关文档)→ `rewrite_question`(全不相关就改写问题回到检索)。第一和最后一个节点就是"决策权"的落点。
  - **代价**:标准 RAG 2–3 秒,agentic RAG 3–5 次工具调用通常 8–15 秒,成本和延迟都上一个台阶。所以路由(判断哪些查询不值得走 agent 链路)成了配套必件,见 Q2.19。
- **及格线**:说出"检索从管道变成工具"和三个决策权中至少两个。
- **加分**:能画出五节点图或说出自家系统的延迟/成本数字;主动提"迭代上限"这个工程约束(微软指南建议 5–10 次)。
- **红旗**:把 agentic RAG 理解为"RAG 加了个更好的 rerank";说不出决策权在哪。

**Q2.18【八股·L2·模型】Self-RAG 和 CRAG 各自的核心机制是什么?2025–2026 年它们的落地形态是什么?**

- **人话版**:Self-RAG 是把"反思"训练进模型——让它学会在生成时吐出控制信号(要不要检索、检索结果相不相关、答案有没有依据);CRAG 是外挂一个轻量评估器——先给检索结果打分,按置信度决定直接用、去网上补、还是拆开重组。前者改模型,后者改管道。
- **展开**:
  - **Self-RAG**(arXiv 2310.11511):训练模型生成四类反思 token——是否需要检索、检索段落是否相关、是否支持输出(groundedness)、生成是否有用。推理时这些 token 就是控制信号,模型自己决定"这次要不要检索"。7B/13B 的 Self-RAG 在开放域 QA 上超过 ChatGPT,靠的就是按需检索+自我批判。
  - **CRAG**(arXiv 2401.15884):不动模型,加一个轻量检索评估器输出置信度,高置信直接用、低置信触发两个纠错动作——web 检索补充、decompose-then-recompose(把检索结果拆开、剥掉无关部分再重组)。即插即用,任何 RAG 框架都能加。
  - **现在的落地形态**:思想已经化进产品,没人再单独提论文名。LangGraph 的 `grade_documents`+`rewrite_question` 就是 CRAG 的评估-纠错环;Azure AI Search 的 agentic retrieval(2026-04 GA)把"查询规划→并行子查询→语义排序→结构化引用"做成托管服务;OpenAI file_search 内置了自动改写和专用 reranker。你用 2026 年的任何一家托管检索,里面都有这两个论文的影子。
- **及格线**:说出"一个改训练、一个外挂评估器"的分野和各自一个机制细节。
- **加分**:能指出"反思 token 的思想在推理模型时代变成了思维链里的检索决策";给出落地产品的具体名字。
- **红旗**:只会背论文名和年份,说不出机制落地到哪了。

**Q2.19【原理·L3·架构】检索前路由(route before retrieve)怎么做?什么信号用来判断"这个查询不值得走 agent 检索链路"?**

- **人话版**:在 agent 链路前面加一道便宜的门——规则或小模型先判断:这个查询是不是闲聊、是不是模型已知事实、要不要检索、要的话查哪个源。简单查询直接走单次检索或直接回答,省下 8–15 秒和几倍 token。
- **展开**:
  - **三种路由形态**:规则层(关键词/意图分类器,最便宜);小模型层(几千 token 成本判断"要不要检索+查哪个源");产品参数层(Azure AI Search 的 reasoning_effort 三档,Minimal 档直接跳过 LLM 查询规划走单查询——把"用质量换开销"做成了显式开关)。LlamaIndex 把这条演进路线总结为 naive top-k → 检索模式 → auto_routed → 多索引组合路由。
  - **判断信号**:查询类型(事实问句 vs 开放分析)、时效性(要不要最新数据)、领域(查哪个索引)、会话上下文(追问往往可以复用上轮的检索结果)、历史统计(这类查询历史上检索帮助多大)。OpenAI web_search 的 tool_choice=auto 本质上是把路由决策交给模型本身——模型自判要不要搜。
  - **路由错误的代价不对称**:该检索的没检索 → 幻觉(用户可感知的伤害大);不该检索的检索了 → 浪费延迟和钱(可容忍)。所以路由器阈值要偏向"多检索"一侧,再用缓存和结果复用把多余检索的成本压下来。
- **及格线**:说出"便宜的前置判断+不对称代价"的框架。
- **加分**:给出具体信号集和路由器实现选型(规则起步、小模型进阶);知道托管服务已经把路由做成参数。
- **红旗**:建议所有查询都走完整 agent 链路"保证质量";或者反过来用一个很弱的关键词规则做硬门,没有兜底。

**Q2.20【原理·L3·架构】迭代检索/多轮检索怎么防止两种失败:无限循环检索,和越改写越偏?**

- **人话版**:设硬上限(常见 5–10 次迭代,或同一问题最多再搜 3 次);每次改写必须携带"上一轮为什么没找到"的信息,让改写是有方向的而不是换个说法再赌一次。
- **展开**:
  - **防循环**:迭代上限是底线;进阶做法是检测"检索结果集合与上一轮高度重叠"就提前终止(再查也是同样结果);微软指南里甚至有指令级写法——"Don't search more than three times for the same subsequent question",直接写进 prompt 也是有效的,因为这是行为约束不是知识。
  - **防漂移**:改写要显式携带失败信息。标准做法是把 grade_documents 的判定("前 5 篇都是讲部署的不是讲计费的")反馈给改写器,改写器针对缺口补查询词,而不是盲目同义替换。进一步可以做查询分解——把复合问题拆成子查询并行检索,比一轮大而全的查询命中率高得多(Azure agentic retrieval 的默认路径就是"分解成 3 个子查询并行")。
  - **终止后的兜底**:迭代到上限还不行,要设计降级路径——回答"基于现有信息我不确定,缺的是 X"比硬编强;面向用户的系统还要考虑这时是否升级到人工或更贵的检索源(web 搜索兜底)。
- **及格线**:上限+有方向改写两点。
- **加分**:结果重叠检测、查询分解并行、降级兜底三件里再说一件。
- **红旗**:只设上限没有兜底——到 10 次就硬停,模型拿半截信息硬答。

**Q2.21【原理·L3·平台】怎么评估一套 agentic 检索系统?哪些指标不需要 ground truth 就能先跑起来?**

- **人话版**:三层看——检索层(召回了没)、生成层(答案有没有依据)、agent 层(工具用得对不对)。冷启动时先跑不需要标注的:LLM 裁判评"答案是否被检索内容支撑"(faithfulness)、"工具选择是否合理"。有了标注再补 context recall、答案完整度。
- **展开**:
  - **指标体系**:检索侧 context precision(召回的里面多少相关)/context recall(该召回的召回了几成,需要 ground truth);生成侧 faithfulness(答案是否有上下文支撑、无捏造——精度向)/completeness(对标准答案的覆盖——召回向)。这两个方向别混:一个系统可能很"忠实"但什么都答不全。
  - **无需标注先跑的**:LLM-as-judge 评 faithfulness 和检索相关性(RAGAS、Azure Foundry 的 Retrieval 评估器都是这个思路);工具调用轨迹的启发式审计(该检索的轮次检索了吗、重复查询率、每次请求工具调用数)。这些适合做 CI 里的回归信号。
  - **agent 特有的维度**(微软指南 2026-06 明确列出):工具选择准确率、检索效率(每次请求的工具调用数)、端到端延迟、单请求成本。传统 RAG 只看前两层,agentic 系统必须把第三层当一等指标——一次"成功但调了 20 次工具"的回答在成本上是失败的。
  - **金标集怎么起步**:社区成熟做法是"标注派对"(团队半天集中标几百条)+ LLM 生成 query-chunk 对(Generative Benchmarking)先铺量,再人工清洗。
- **及格线**:能分清 faithfulness 和 completeness 的精度/召回方向;说出一个 agent 特有指标。
- **加分**:知道哪些指标不需要 ground truth、适合先上 CI;提参数扫描(top_k/chunk 大小)这种调参方法论。
- **红旗**:只用"答案对不对"一个终判指标——检索烂和生成烂混在一起没法归因优化。

**Q2.22【原理·L4·架构】检索结果怎么和上下文预算联动?注入多少条、以什么形态进上下文,你的决策框架是什么?**

- **人话版**:注入条数按"最小够用"定(常见 3–5 条起步,按评估调),形态上优先给"标识符+摘要"而不是全文——超出预算的部分给路径让 agent 需要时再取。原则是检索为上下文服务,不是反过来。
- **展开**:
  - **条数的量化起点**:微软指南建议每次工具调用 3–5 条结果起步;OpenAI file_search 的 max_num_results 默认 10 但官方示例常用 2——省 token 和延迟,"可能牺牲回答质量"。这说明注入量是个显式 trade-off 旋钮,该按评估调而不是拍脑袋。
  - **形态分级**:全文注入(短且关键的内容)→ 摘要+引用(长文档)→ 只给标识符(路径/链接/query,agent 需要时再拉,即 Anthropic 说的 just-in-time retrieval)。Claude Code 是混合范式的活例子:CLAUDE.md 预载(常驻)+ grep/glob/head 按需(即时),避免陈旧索引。
  - **和压缩的联动**:注入的检索结果也是历史的一部分,几轮之后同样该被清理/摘要化——检索系统只管"取",上下文管理管"留多久"。设计时把两个生命周期显式分开,否则检索越勤快上下文越脏。
- **及格线**:说出 3–5 条的量级和"标识符+按需拉取"的形态。
- **加分**:指出检索结果的驻留期治理;把注入量和评估指标挂钩("条数是用 eval 调出来的")。
- **红旗**:top-k=10 无脑全注入,从没想过每条的边际价值。

**Q2.23【品味·L4·架构】2025–2026 年吵了很久"RAG is dead"。你的立场是什么?什么规模该放弃检索、什么规模必须回来?**

- **人话版**:死的是"naive 单向量+固定 top-k+必经检索"的管道,不是检索本身。百来篇文档的个人知识库确实可以不建索引——直接全进上下文或维护成 wiki;但万页级企业语料,窗口再大也塞不下、塞下也用不好,检索+rerank 一定回来。我的判断标准是量化的 token 经济学,不是潮流。
- **展开(两个方向+判据)**:
  - **"可以不走检索"的一方有真实证据**:Karpathy 2026-04 的个人知识库方案——原始资料丢进 raw/ 目录,让 LLM 当"编译器"维护约百篇/40 万字的互联 markdown wiki,个人规模明确说不向量库;2026-03 有论文显示 8B 模型+向量库记忆(30%)胜过 235B 无记忆(14%),侧面说明"组织好的上下文"比"更大的模型"值钱。
  - **"必须回来"的一方也有硬数字**:Atomic 的反驳文(2026-04)算过账——40 万字≈54 万 token,每次全注入约 $0.01 尚可接受;但 1 万页≈4000 万 token≈$100 且根本塞不进窗口;无索引的 wiki 导航要 2–3 个月,叠加 embeddings 底座约 40 分钟,某实验里 embeddings 让索引读取量降了 200 倍。结论是"策展层活在检索底座之上,不是二选一"。
  - **我的裁决变量**:语料规模(token 化后 vs 窗口预算×查询频次的乘积)、变更频率(频繁变更的语料索引维护成本高,偏 grep/wiki)、查询分布(集中问少数文档→预载;长尾→检索)。2026 年的主流共识:检索降格为 agent 的按需工具,和记忆/策展分层共存——这已经是微软/OpenAI/Google 产品文档的实际形态,不是争论了。
- **及格线**:能区分"naive 管道死了"和"检索死了"两句话;给出至少一个量化判据。
- **加分**:引用双方的具体数字并指出各自的适用边界;落到自家业务给出演算。
- **红旗**:跟着喊口号站队;或者认为"长上下文模型会终结这个讨论"而不给经济学论证。

---

## 2.4 品味与判断题

**Q2.24【品味·L4·架构】把"上下文是稀缺资源、缓存是经济学、压缩是垃圾回收"落成你团队的上下文工程军规,至少 5 条。**

参考思路(军规示例,重要的是每条背后有机制):
- **静态在前动态在后**:请求结构按"变化频率"排序——tools/system/早期历史在前,动态状态一律尾部注入(system-reminder 模式);断点打在"最后一个跨请求相同的块"上。
- **工具定义冻结**:进入会话就不改;要改开新会话。配套:工具按稳定顺序注册。
- **工具输出先过滤再入上下文**:超过阈值截断或外置,错误信息优先保留;给 agent 留"取原文"的通道。
- **压缩有损,结论外置**:关键决策写文件/笔记/todo,不赌摘要;长任务用结构化进度文件而非无限续会话。
- **主动算账**:用 token counting 决定何时 compact,而不是撞限被动触发;定期看缓存命中率,它是第一成本杠杆。
- **及格线 / 加分 / 红旗**:及格=5 条且每条能说出一句话的机制理由;加分=有团队真实的翻车案例支撑其中某条;红旗=写成"写好 prompt、多测试"这类没有机制含量的空话。

**Q2.25【品味·L3·平台】什么时候该果断开新会话,什么时候值得压缩续命?**

参考思路:
- **开新会话的信号**:任务已完成且状态已外置(续命没有意义);上下文里堆了大量与下一步无关的过程(压缩也救不了信噪比);同一问题纠正两次还失败(官方 best practice 的口径:连续修不好就 /clear 换思路,错误上下文本身在误导模型);反复 compaction 撞 thrashing。
- **续命的理由**:任务中间态复杂且未外置,重建成本高于压缩损失——但这个理由经常是"没做好外置"的结果,该修的是外置习惯而不是一直续命。
- **高级点**:里程碑式长任务用"结构化笔记+新会话"组合(Anthropic 长任务实验的做法),比在线压缩稳;选模型时把"对 compaction 的容忍度"当能力看——已有模型厂商明确以"原生训练为跨压缩上下文运行"为卖点,压缩行为和模型训练开始耦合。
- **及格线 / 加分 / 红旗**:及格=给出至少三个开新会话的判据;加分=把"续命需求"重新归因为外置缺失;红旗=凭感觉"聊得越久越连贯"——实际上 context rot 让久会话更不可靠。

**Q2.26【品味·L4·架构】"100% 缓存命中但上下文持续膨胀" vs "频繁压缩但缓存反复失效",你怎么调度这个权衡?**

参考思路:
- **先识别两边的真实成本**:膨胀的质量成本(context rot,超拐点后注意力稀释、成功率下降)+ 金钱成本(缓存价也是钱,0.1××巨大基数);压缩的金钱成本(压缩点后全价重算+摘要生成的输出费)+ 质量成本(有损)。
- **调度策略**:批量优先于涓流——把"该忘的"集中在一次清理/压缩里,一次失效换最大回收(clear_at_least 思想);里程碑处压缩+外置,而不是随机点压缩;用 token counting 把触发从"撞限"改成"达到质量预算拐点"。
- **顶层方案**:从源头减少需要管理的历史——subagent 隔离让噪声根本不进主上下文,主线程只收 1–2K 摘要。这不是在两个坏选项里挑,是把问题的规模砍掉。
- **及格线 / 加分 / 红旗**:及格=说出两边各自的成本构成;加分=提出"批量清理"和"subagent 隔离"两个结构性手段;红旗=只看 token 成本不看质量成本,或反过来。

**Q2.27【品味·L3·用户】CLAUDE.md/AGENTS.md 的每一行,用什么标准决定去留?**

参考思路:
- **唯一判据**:"删掉这一行,agent 会不会开始犯错?"——写**不可从代码推断的事实**(构建命令、部署坑、团队约定、"别动 legacy 目录"),可推断的内容都是浪费(模型自己会读代码)。
- **量与质**:入口文件控制在约 200 行内;可验证的具体指令("测试用 pnpm test:unit")优于抽象原则("保持代码整洁");按目录/任务拆分到子文件用懒加载,而不是全堆在根文件。
- **硬约束别放这里**:禁止目录、危险命令这类"绝对不能违反"的约束,交给 hooks/权限系统确定性执行——文档管软约束(模型尽力遵守),代码管硬约束(违反不了)。
- **及格线 / 加分 / 红旗**:及格="不可推断才写"+硬约束分流;加分=有具体重构经历(600 行砍到 150 行行为反而变好);红旗=把 CLAUDE.md 当产品文档写,给人看的不给 agent 看的占大半。

**Q2.28【品味·L4·管理】agent 平台月账单涨了 3 倍,CTO 要求降本。你第一件事查什么?**

参考思路:
- **先分账再动手**:缓存命中率(最大杠杆)、上下文长度分布与会话轮数(谁在膨胀)、模型分布(该用小模型的活是不是全在大模型上)、哪个工具在倾倒 token、无人值守任务的空转重读。按"无行为改变的修复"优先排序。
- **常见根因排序**:缓存破坏(tools 抖动/动态 system)> 工具输出未过滤 > 模型不分级 > 无效长会话。前两个修复对行为零影响,先做;模型路由影响轻微,其次;砍功能最后。
- **别犯的错**:先去砍单价(换便宜模型)再查结构问题——"贵"经常不是单价问题,是调度问题:钱都烧在低质量的重复吞吐上(方向错的探索、无验证器的试错、污染后的重做)。
- **及格线 / 加分 / 红旗**:及格=先查缓存命中率和长度分布;加分=给出"每任务成本×质量"双曲线的汇报口径;红旗=上来就建议换模型/砍订阅。

---

## 2.5 开放性问题

**Q2.29【开放·L5·模型】如果让你参与设计一个"为压缩而训练"的 agent 模型,训练目标和数据怎么构造?**

讨论框架:
- **数据从哪来**:真实长会话的回放(带"压缩前后任务是否仍成功"的标签)是最有信号的;合成长任务(拼接任务+注入压缩点)可以铺量但有分布偏差。关键难点是"好的摘要"没有天然标签——只能用下游任务成功率做奖励,这个信号又稀疏。
- **奖励怎么定**:压缩后续任务成功率为主奖励,token 节省做次级奖励(否则模型学会"什么都写进摘要"或"什么都不写");要不要惩罚"摘要里出现原文没有的断言"(保真约束)。
- **张力**:摘要的任务相关性(对下一个任务有用的才留)vs 一般性(无法预知下一个任务)——这本质是预测问题,可以聊"训练时模拟多任务续接"的方案。
- **加分方向**:要不要专门的"记忆写入"动作头(把外置记忆变成模型的一等能力而不是 harness 的 hook);评测怎么构造(固定任务集+不同压缩策略的对照)。

**Q2.30【开放·L5·平台】缓存、压缩、检索三者边界会不会被"无限上下文+免费缓存"的模型进步抹掉?哪些工程投入是抗未来的?**

讨论框架:
- **机制上不会被抹掉的部分**:context rot 是注意力机制的内在约束,不是计费策略——窗口免费了,"塞太多质量下降"还在,压缩与检索的质量价值保留。
- **会贬值的部分**:稳定前缀纪律是特定缓存实现(显式断点/TTL)的产物,厂商转向全自动缓存后,打点技巧的价值在缩水(但"动态内容尾部注入"仍是好习惯,因为它是信息架构问题不只是缓存问题)。
- **抗未来的投入**:信息分层治理(什么常驻/什么按需/什么丢弃)、外置状态(文件/记忆工具)、验证器——它们不依赖任何一家的缓存实现,模型再进步也需要"该看什么"的答案。
- **判据**:一项投入如果只对某家厂商的计费规则敏感,它是套利;对模型能力边界敏感的,才是工程。

**Q2.31【开放·L4·架构】为"上下文质量"设计一套可度量的指标体系,让你能在团队之间横向对比。**

讨论框架:
- **输入侧**:信噪比(任务相关 token 占比,可用 LLM 裁判抽样估计)、重复内容率、动态内容占比(尾部注入是否遵守纪律)。
- **过程侧**:缓存命中率、compact 频次与触发原因分布、上下文利用率峰值、每任务工具调用的 token 分布。
- **结果侧**:任务成功率与上下文长度的相关性(理想是弱相关——长不意味着差,但强负相关说明 rot 在伤你)、探针任务(关键信息放中部)通过率。
- **难点与加分**:归因(成功率降是因为上下文还是模型);横向对比要固定任务集;警惕指标博弈(为命中率牺牲必要性内容)。

---

## 2.6 工程实战题

**Q2.32【实战·L3·平台】自研 agent(Anthropic API)长会话缓存命中率只有 40%。给系统性的排查与优化方案。**

参考思路:
- **排查**:按请求维度 dump usage 字段(cache_read/cache_creation/input 三个数),对 miss 请求做**前缀 diff**——system 变了?tools 变了?消息中段被改?断点漂移?还是 TTL 过期(看相邻请求时间戳间隔)?命中率问题 90% 能靠 diff 定位到具体断点。
- **常见修复**:冻结 tools 定义与注册顺序;system 里的时间戳/动态配置改尾部注入;长会话在中段加第二断点;人审间隔长的场景换 1h TTL。
- **结构性优化**:工具输出过滤前置、context editing 批量清理、高噪声任务拆 subagent——这三个同时改善质量和成本。
- **验证**:命中率与单任务成本前后对比,并回归任务成功率(压缩加深可能丢信息,省钱把任务做砸了不算优化)。
- **及格线 / 加分 / 红旗**:及格=前缀 diff 方法论+两大修复(tools 冻结、尾部注入);加分=验证环节带质量回归;红旗=不看 usage 字段凭感觉调。

**Q2.33【实战·L3·平台】设计一个工具结果压缩管道:20 种工具里 5 种(日志查询/大文件读取/HTTP 抓取)会返回巨量输出。**

参考思路:
- **分层管道**:工具侧(强制分页/limit 参数,提供 concise|detailed 输出格式枚举——参考 Anthropic 工具设计指南,concise 约省 2/3)→ 进入前(中间件按规则截断:头尾保留、错误优先、小模型摘要)→ 驻留期(超过 N 轮的旧结果换占位符)→ 极端(超大输出外置文件,上下文只留路径+一句话摘要)。
- **防坑**:压缩不能破坏错误现场(排错时需要原文——占位符要可追溯可重取);diff 类输出慎压缩(精确匹配会被破坏);摘要用小模型时要抽检质量。
- **度量**:管道上线前后对比每任务 token、TTFT、任务成功率;按工具分别统计压缩率,找漏网的污染源。
- **及格线 / 加分 / 红旗**:及格=四层里给出三层;加分=错误现场可追溯的设计+分工具度量;红旗=一刀切截断不留逃生通道。

**Q2.34【实战·L4·架构】大仓库 50 万行,设计"按需下钻的上下文路由":agent 默认只看到架构概览,通过工具按需取细节。定义工具集、缓存策略、防迷路机制。**

参考思路:
- **工具集(少而粗)**:`get_architecture(node)` 返回当前层级结构;`zoom_in(node)` 下钻;`get_related(node)` 关联内容;`diff_architecture(commit)` 变更对比。写操作走人工确认。核心思想:把"塞满上下文"变成"教 agent 顺着结构请求上下文"。
- **缓存策略**:架构图版本化(仓库变更才失效);下钻结果按 node 缓存;主上下文只保留已访问路径的"面包屑"(node id 列表),需要时再下钻。
- **防迷路**:访问预算(最大下钻深度/次数);面包屑回链;超预算强制总结返回;已探索区域记进结构化笔记避免重复下钻。
- **对比方案要主动聊**:这和 aider 的 repo map(tree-sitter 把全仓符号压进约 1K token)是同族思路——结构索引优于全文塞入;和 embedding 检索相比,结构化导航可解释、可审计,但维护成本高。什么时候图谱值得维护(架构复杂+长期项目),什么时候 grep 就够了(中小仓库),是这个题真正的考点。
- **及格线 / 加分 / 红旗**:及格=工具集+预算+面包屑;加分=与 repo map/embedding 的主动对比和适用条件;红旗=工具设计成几十个细粒度 getter(变成另一种上下文爆炸)。

**Q2.35【实战·L2·用户】你的 CLAUDE.md 膨胀到 600 行,agent 行为反而变差。怎么重构?**

参考思路:
- **诊断先行**:600 行里大概率混着三类——真约定(留)、某类任务才需要的(拆走)、过期的(删)。行为变差的机制:关键约定被长文稀释+过期内容在误导。
- **重构动作**:按任务域拆成 skills/子目录文件(路径触发懒加载)/rules(paths 作用域);每行过"删掉会不会犯错"测试;硬约束迁去 hooks/权限;过期内容直接删(不注释——注释了也会被读到)。
- **目标形态**:根文件<200 行,只留项目一句话、常用命令、硬约定、指向分层资产的索引。验证:重构前后各跑一遍固定任务集对比成功率。
- **及格线 / 加分 / 红旗**:及格=拆分+删减+行数目标;加分=用任务集验证而不是拍脑袋;红旗=把 600 行平均拆成 4 个 150 行文件全部常驻——总量没变,只是换了个方式膨胀。

---

## 2.7 场景排错题

**Q2.36【排错·L3·平台】现象:agent 每轮要 40–60 秒才开始吐字,账单 input 计费暴涨,cache_read 几乎为 0。最可能的三个根因?**

排查链路:
- **①前缀被动态化**:system/首条消息里有时间戳、环境快照、随机 id——每轮全量重算 prefill。40–60s 的 TTFT 正是百万级 token 重 prefill 的典型特征(正常秒级)。这是第一怀疑对象。
- **②tools 定义抖动**:MCP server 启动顺序/可用工具集轮间变化导致工具数组不稳定——三层缓存全失效,症状同上。
- **③TTL 失配**:每轮之间人工审查超过 TTL(5 分钟),缓存自然蒸发——但这个 TTFT 通常没有前两个夸张,可按间隔时间排除。
- **验证方法**:相邻两轮请求体做前缀 diff,定位 miss 的精确位置;修复后 TTFT 应回到秒级。三个根因的修复分别是尾部注入/冻结工具集/换长 TTL。
- **及格线 / 加分 / 红旗**:及格=前两个根因+diff 验证法;加分=从 TTFT 数量级反推"重 prefill"这个诊断;红旗=归因于"模型变慢了"去联系厂商。

**Q2.37【排错·L2·用户】现象:长会话中途 agent 突然"失忆"——忘了前面的决定,重新读文件,重复问已回答的问题。发生了什么?**

排查链路:
- **第一怀疑:auto-compact 刚触发**。检查压缩边界时间点;机制是摘要丢了早期决策——尤其是用户约束和已否决的方案,这两类信息最容易在"保结论丢过程"的摘要里被当成过程丢掉。
- **次选**:子代理返回后主线程只拿到摘要;或记忆文件被覆盖/截断。
- **修复习惯(比修 bug 更重要)**:重要决定让 agent 当场写进 todo/进度文件(结构性载体,每轮重新注入、不受压缩影响),不依赖对话记忆;超长任务用里程碑笔记而不是无限续会话。
- **及格线 / 加分 / 红旗**:及格=定位 compaction;加分=指出"哪类信息最容易丢"并给出外置习惯;红旗=以为模型"记忆体"坏了反复重启会话。

**Q2.38【排错·L3·平台】现象:接入 8 个 MCP server 后,agent 选工具经常选错、响应变慢、成本翻倍,部分请求超上下文。诊断与处置?**

排查链路:
- **根因:工具过载**。多 server 场景工具定义没干活先吃数万 token(Anthropic 文档的实例约 55K);工具选择准确率在 30–50 个工具以上明显退化;定义膨胀还可能破坏缓存(成本翻块的另一半原因)。
- **处置分层**:启用 tool_search/延迟加载(工具只留名称,命中再展开,可省 85%+ 定义 token);按任务域拆 subagent(每个只挂相关 server 的工具);合并语义重叠工具;低频 server 直接砍。
- **验证**:工具选择准确率、单任务 token、TTFT 三指标前后对比。
- **及格线 / 加分 / 红旗**:及格=说出过载机制+延迟加载;加分=指出缓存受害这个二阶效应,给出拆分粒度;红旗=归因于模型不行,换更贵的模型硬扛。

**Q2.39【排错·L4·平台】现象:compaction 之后任务成功率从 85% 掉到 60%。怎么定位压缩丢了什么?**

排查链路:
- **构造对照**:同批任务分别跑"压缩前终止+新会话注入摘要"与"不压缩短任务",隔离压缩这一个变量。
- **差异分析**:对失败 case 逐个比对摘要 vs 原文,归类丢的是哪类信息——早期用户约束?失败的尝试及原因(导致重蹈覆辙)?文件路径?任务边界?归类后高频丢失类别就是修复靶点。
- **修复**:把高频丢失类别外置(用户约束进 CLAUDE.md/任务文件;失败尝试写进度笔记);调整 /compact 的 focus 指令显式点名要保留的类别;必要时换"checkpoint+新会话+结构化交接"替代在线压缩。
- **及格线 / 加分 / 红旗**:及格=对照实验+差异归类;加分=外置方案落到具体载体;红旗=直接调大上下文窗口绕过(钱解决不了 context rot,而且根因是信息丢了不是空间不够)。
