# ch02 · 上下文工程与缓存(重点章)

> 覆盖:context engineering 定义与演进、上下文构成与量级、context rot / lost in the middle 实证、RAG vs 长上下文 vs agentic search、prompt caching 三家机制、KV cache 原理、**稳定前缀设计**、system-reminder 模式、**compaction 与工具结果压缩**、Anthropic context management API、上下文经济账、书的 Context 四维度与权重治理。
> 这是 coding agent 工程里投入产出比最高的一章,题量最大。

---

## 2.1 八股题(基础概念)

**Q2.1【八股·L1·平台】给"context engineering"下一个工作定义,并说明它和 prompt engineering 的关系。**
答:
- LangChain(Harrison Chase, 2025.06):"构建**动态系统**,以正确的格式、在正确的时机提供正确的信息和工具,使 LLM 能合理地完成任务"——prompt engineering 是其子集。
- Anthropic(2025.09):核心问句从"怎么措辞 prompt"变成"**什么样的上下文配置最可能产生期望行为**";上下文 = 推理时出现的 token 集合;目标是"**找到能最大化目标达成概率的最小高信号 token 集**"。
- 书的表述:"模型在什么时刻、以什么形式、拿到哪一组材料来理解当前任务,以及哪一组噪声应该被屏蔽"——重点不是多喂,而是控制信息分发。

**Q2.2【八股·L1·平台】一个 coding agent 的上下文由哪几部分构成?大致量级?**
答(以 Claude Code 为例):
- system prompt(泄露分析:数百条字符串动态拼装,新版粗估 2–4 万 token 级,早期版本约 1–2 万)
- 工具定义(内置 20+ 工具 + MCP 工具;多 MCP 场景未优化可达数万 token)
- CLAUDE.md / auto memory(官方建议每文件 <200 行;auto memory 只注入 MEMORY.md 前 200 行/25KB)
- 对话历史 + 文件内容 + 命令/工具输出(工具输出默认截断于 25K token)
- skill/subagent 描述等动态注入。
- 关键认知:**每轮请求都会重发全部历史**——这是 agent 成本模型的基石。

**Q2.3【八股·L1·用户】什么是"稳定前缀"(stable prefix)?为什么它是 coding agent 的生命线?**
答:
- 定义:请求中跨多轮保持逐 token 不变的头部(system prompt + tools 定义 + 早期对话历史)。
- 原理:所有厂商的 prompt caching 都基于**前缀匹配**——前缀相同则复用 KV cache,命中部分按约 1/10 价格计费。
- agent loop 天然是"追加式"的:每轮只追加新消息,前缀单调增长,所以天然适合缓存;一旦前缀被改动(哪怕是注入一个时间戳),从改动点开始的全部缓存作废。
- 反面:改 tools 定义会使 tools/system/messages 三层缓存**全部失效**——是最致命的操作。

**Q2.4【八股·L2·用户】Claude Code 的自动压缩(auto-compact)机制大致怎么工作?**
答:
- 两段式:先**清掉较旧的工具输出**(社区称 microcompact),不够再**摘要整个会话**(compaction)。
- 触发:接近模型上下文上限时(1M 窗口模型默认约 967K 触发,可 `/autocompact` 调)。
- 保留规则:system prompt 不变;项目 CLAUDE.md 与 plan 重新注入;**最近修改的最多 5 个文件**重读,>5000 token 的文件只留路径;skill 内容每个 cap 5000、总计 25000 token;verbatim 工具输出与中间思考全部换成摘要。
- 手动:`/compact <focus 指令>` 可定制保留什么(指令可常驻 CLAUDE.md)。

**Q2.5【八股·L2·用户】什么是"工具结果压缩/清理"(tool result clearing)?有哪些实现层次?**
答:
- 动机:工具输出(bash 日志、文件全文、搜索结果)是上下文的最大污染源,且价值随时间衰减。
- 层次:①写入前过滤(PreToolUse hook 把 1 万行日志过滤到几百 token);②截断(默认 25K 上限、超长文件只留路径);③事后清理(把旧 tool_result 替换为占位文本——Anthropic context editing 的 `clear_tool_uses` 策略,默认保留最近 3 组);④摘要化(compaction 把全部工具输出换成摘要);⑤ subagent 隔离(子代理烧掉数万 token 的搜索上下文,只回传 1–2K 摘要)。

**Q2.6【八股·L1·模型】什么是 lost in the middle / context rot?**
答:
- Lost in the Middle(Liu et al., TACL):多文档 QA 中,关键信息在**开头或结尾**时性能最高,在中部显著下降(U 型曲线);金标段放中部时甚至低于无上下文基线。
- Context Rot(Chen et al. 2024, Salesforce):沿"长度 × 位置 × 任务维度(工作记忆/检索/推理)"分解,即便极简任务也随长度系统性退化,且维度**复合叠加**时恶化超过单项之和。
- Anthropic 的架构解释:注意力预算(n 个 token 产生 n² 个注意力对)、训练数据偏短、位置编码插值。**是梯度下滑不是悬崖。**

**Q2.7【八股·L2·平台】RAG、长上下文、agentic search 三种给上下文的方式怎么选?**
答(Anthropic 框架):
- 传统 RAG:**推理前预计算检索**(embedding 召回、一次性塞入)——快,但塞入低相关信息造成 context pollution。
- 长上下文:一次塞满——贵(部分厂商超长档输入价翻倍)且受 context rot 拖累。
- Agentic search:**运行时按需检索**(just-in-time)——agent 只在上下文里留轻量标识符(文件路径、query、链接),用时再取。
- Claude Code 是混合范式:CLAUDE.md 预载(常驻)+ glob/grep/head/tail 即时检索(按需),避免陈旧索引。
- 判据:动态性强的代码库偏 agentic search;法律/金融等静态域可偏 RAG;总原则 "do the simplest thing that works"。

**Q2.8【八股·L2·用户】为什么说"过期的 Context 比没有 Context 更危险"?**
答:(书 Part 1.3)
- 过期文档带着"官方口吻"误导模型;和源码冲突时模型不一定分得清谁更新;
- 归档 spec、迁移中的旧实现、测试夹具会因**出现频率高**被模型误当成当前标准(权重错配);
- 解法不只是"补文档",更多时候是**仓库治理**:给归档 spec 标 archived/superseded by、旧目录写明"只读参考"、删掉过期样例——context engineering 很大程度上是信息架构治理。

---

## 2.2 原理深挖(prompt caching 与压缩机制)

**Q2.9【原理·L2·平台】Anthropic / OpenAI / Gemini 三家的 prompt caching 机制与价格差异?**
答(2026-08 基线):
- **Anthropic(显式)**:`cache_control` 断点标记,每请求最多 4 个;层级固定 tools→system→messages,要求前缀 100% 逐 token 相同;TTL 5min(默认)/1h(可选),命中免费续期。价格:写 1.25×(5min)/2×(1h),**读 0.1×**;最短可缓存 1024–4096 token(低于下限静默不缓存);命中不计速率限制;支持 `max_tokens:0` 预热。
- **OpenAI(全自动)**:无需参数,1024 token 起,按 128-token 块增量匹配;折扣 90%(0.1×,GPT-5 起);保留 5–10 分钟(闲时可到 1h),另有 24h 选项;按"prompt 前缀哈希"路由到同一实例,`prompt_cache_key` 做会话粘性。
- **Gemini(双轨)**:implicit caching 默认开启(最少 2048/4096 token,命中 75–90% 折扣);explicit `cachedContent` 需付**存储费**(如 2.5 Pro $4.50/MTok/小时)。

**Q2.10【原理·L3·模型】prompt caching 和 KV cache 是什么关系?为什么厂商愿意打 1 折?**
答:
- Transformer 自回归解码时每个新 token 要对全部历史做 attention;KV cache 把每层已算出的 Key/Value 张量存下来避免每步重算。
- 服务商的 prompt caching 本质是**跨请求复用 KV cache**:推理引擎(vLLM/SGLang 等)对 token 块做哈希,前缀相同则跳过 prefill 重算。
- 打折的经济学:命中时服务商几乎不付计算成本(prefill 是长上下文的主要开销),折扣是"省下的计算"的分成;对厂商还换来更粘性的长会话负载。

**Q2.11【原理·L3·平台】列举至少 5 种会破坏缓存命中的操作,按杀伤力排序。**
答:
1. **改动 tools 定义**(新增/删除/改描述/改顺序)——三层全失效,最致命;
2. system prompt 里放时间戳/日期/随机 id/动态用户名;
3. 在消息序列**中间**插入或修改内容(包括图片);
4. thinking 参数/工具定义序列化顺序变化;
5. 断点位置移动(Anthropic 每个断点回看 20 个块,消息从 10 块涨到 35 块时第 35 块的断点回看不到第 15 块的写入——需要第二个断点兜底);
6. TTL 过期(5min TTL 下,思考/阅读超过 5 分钟再发下一轮)。
- 真实案例:Claude Code 曾修复 `--resume` 时工具定义重新渲染导致的 miss,以及长会话约每小时一次的 cache miss。

**Q2.12【原理·L3·平台】Claude Code 用 `<system-reminder>` 把动态信息塞进消息里,这和直接改 system prompt 有什么本质区别?设计一个你自己的等价机制要注意什么?**
答:
- 区别:system-reminder 嵌在**最新用户消息或 tool result 内部**(尾部追加),不触碰稳定前缀,缓存不失效;system prompt 必须逐 token 稳定。
- 泄露的 system prompt 里明文规定了该契约:"Tool results and user messages may include `<system-reminder>` tags… They bear no direct relation to the specific tool results in which they appear"。
- 自建注意:①注入点必须在**前缀之后**(最新消息内);②标签要自描述,提醒模型其非指令性来源;③内容要幂等/可丢弃,防止越积越多。

**Q2.13【原理·L3·平台】CLAUDE.md / AGENTS.md 在缓存设计上的角色是什么?各家的加载层级?**
答:
- 角色:项目记忆放在**前缀**,每轮命中缓存价(0.1×)重读,这是"常驻上下文"在经济上可行的原因。
- Claude Code 四层:企业托管(MDM 下发、不可排除)→ 用户 `~/.claude/CLAUDE.md` → 项目 `./CLAUDE.md`(入 git)→ `CLAUDE.local.md`;自根向下**拼接而非覆盖**;子目录的 CLAUDE.md **按需 lazy load**(读到该目录文件时才加载);`@path` import 递归上限 4 跳。
- Codex AGENTS.md:`~/.codex/AGENTS.md` → 项目根 → cwd 逐级拼接,默认总量上限 32 KiB(`project_doc_max_bytes`),越靠近 cwd 优先级越高。

**Q2.14【原理·L4·模型】NoLiMa 基准测出了什么?它对"长上下文模型"的营销数字意味着什么?**
答:
- NoLiMa(Modarressi et al., ICML 2025):问题与"针"**无字面重叠**,逼模型做联想检索。
- 结果:13 个声称 128K+ 的模型,<1K 时都强;**32K 时 13 个中 11 个跌破短文本基线的 50%**;最好的 GPT-4.1 有效长度仅约 16K;最差的(Gemma 3 4B)从 73.6 掉到 0.9。
- 含义:标称窗口长度 ≠ 有效上下文长度;NIAH(大海捞针)类基准会高估真实长上下文能力,因为真实任务需要的是联想而非字面匹配。
- 工程含义:不要因为"窗口 1M"就把仓库塞满——有效工作记忆可能只有 16–32K,这直接论证了 agentic search 与压缩的必要性。

**Q2.15【原理·L3·平台】compaction 的信息损失怎么管理?Claude Code 和 Codex CLI 的保留策略对比。**
答:
- Claude Code:保 system prompt、CLAUDE.md、plan、最近 5 个文件(>5K token 只留路径)、skill 摘要(5K/25K 上限);丢全部 verbatim 工具输出与中间思考。反复压缩会触发 thrashing 检测报错——设计上逼你开新会话。
- Codex CLI(rust 源码):摘要后保留 ①最近用户消息(20K token 预算,newest-first 装入);②CompactionSummary;③初始上下文/WorldState 基线——mid-turn 时插在最后一条真实用户消息**之前**,因为模型被训练为"把摘要视为历史最后一项"。
- 通用原则:压缩是有损 GC,保留"决策与结论"、丢弃"过程与原文";关键状态应该外置(写文件/笔记),而不是赌摘要保得住。

**Q2.16【原理·L4·平台】Anthropic Context Management API(2025.09)三件套分别是什么?清理工具结果为什么反而要配 `clear_at_least`?**
答:
- ①**Context editing**(tool result clearing):默认 100K input tokens 触发,从最老的 tool_result 开始替换为占位文本,默认保留最近 3 组;客户端保留完整原始历史,编辑在服务端做。
- ②**Memory tool**:客户端文件式记忆(`/memories` 目录,view/create/str_replace…),启用后自动注入"先看记忆目录"的指令;配合 editing 时清理前会提醒模型先把重要结果写入记忆。
- ③**Token counting**:免费接口,可同时返回编辑前后 token 数,用于**主动触发**压缩而非被动撞限。
- `clear_at_least` 的原因:**清理会使清理点之后的缓存前缀失效**——如果每次只清一点,缓存反复作废,得不偿失;批量清理让每次失效"值回票价"。
- 官方数字:memory+editing 在 agentic search 任务 +39%(仅 editing +29%);100 轮 web search 任务 token 消耗降 84%。

**Q2.17【原理·L3·平台】算一笔账:一个 30 轮的长会话,平均历史 300K token,Sonnet 4.5($3/$15,缓存读 $0.30,写 $3.75),缓存 90% 命中 vs 全部 miss,输入成本差多少?**
答:
- 每轮输入 ≈300K,30 轮累计输入 9M token。
- 命中 90%:8.1M×$0.30 + 0.9M×$3(未命中部分按原价,忽略写溢价)≈ $2.43 + $2.7 ≈ $5.1;若计入首轮 1.25× 写溢价,再加大约 $0.2。
- 全 miss:9M×$3 = $27。
- 差约 5 倍以上;会话越长、命中占比越高,节省比例越接近 90%。(Claude Code 官方实例:单会话 cache read 940K + write 50K + 少量 input/output 合计 $0.55,同样输入全 miss 约 $2.97。)
- 追问点:为什么"空闲不动也在花钱"——定时任务/后台 agent 每轮按缓存价重读全部长上下文。

**Q2.18【原理·L3·模型】解释 attention budget 与 n² 注意力对:为什么上下文翻倍,检索质量下降得比线性快?**
答:
- n 个 token 产生 n² 个注意力关系,每个 token 的"注意力预算"被稀释;训练数据大多偏短,模型没学过在超长范围内维持精确的间接寻址;位置编码插值在远距离上失真。
- 复合效应:检索(找到)× 工作记忆(记住)× 推理(组合)三层能力都随长度衰减,且乘法叠加(Context Rot 的核心结论:复合恶化 > 单项之和)。
- 实践推论:上下文是**稀缺资源**,"多总比少好"在超过某个点(经验上几十 K)之后反转。

---

## 2.3 书的四维度框架(原理进阶)

**Q2.19【原理·L3·架构】书提出 Context 设计的四个维度是什么?各举一例。**
答:
- **生命周期**:项目级(AGENTS.md/架构规则)、任务级(specs/issue/设计稿/验收标准)、运行级(当前 diff/终端输出/测试结果/trace)。
- **形态**:规则(约束/禁令)、样例(golden path/参考实现)、流程(Skills/playbooks/checklist)、信号(日志/失败测试/截图对比)。
- **加载方式**:常驻加载(项目规则)、按需检索(specs/文档)、隔离加载(subagent 独立窗口)、反馈回注(测试结果/diff 回送模型)。
- **质量**:权威性(源码>旧 wiki)、新鲜度(过期要么更新要么别喂)、一致性(冲突规则必有一乱)、成本(每段 context 都要解释自己为什么值得占 token)。

**Q2.20【原理·L4·架构】"不是没有 Context,而是权重错了"——书的这个判断包含哪些具体机制?怎么治理?**
答:
- 权重错配机制:①出现频率(旧写法在仓库里更多→被当标准);②权威错觉(被复制引用多次的过期文档显得权威);③迁移期新旧并存(没说哪边是 source of truth 就会选"更熟悉的");④多处表述不一致(specs/issue/注释/归档各一版,模型自己挑)。
- 治理:明确 source of truth 并标注;排除顺序写进任务(优先参考 A 不要参考 B);归档物打 archived/deprecated/superseded 标;能删的过期样例直接删——**很多 Context 问题的解法是仓库卫生,不是写文档**。

**Q2.21【原理·L3·用户】书里列了三种"伪 Context Engineering",分别是什么?你见过第四种吗?**
答:
- ①把整个仓库喂进去(噪声、过期样例、主线代码同权重);②把 AGENTS.md 写成几千字"项目宪法"(过期文档带官方口吻误导、关键信息被淹没);③什么都继续写回 prompt(约束重复几十次还在人肉复制)。
- 第四种常见候选:无限追加的 append-only 记忆(见 ch06 semantic drift);把检索结果原样塞入不裁剪;用截图轰炸代替结构化信号。

---

## 2.4 品味与判断题

**Q2.22【品味·L4·架构】"上下文是稀缺资源,缓存是经济学,压缩是垃圾回收,稳定前缀是把三者串起来的工程纪律"——请把这句话展开成你团队的上下文工程军规(至少 5 条)。**
参考思路(军规示例):
1. 静态在前动态在后,断点打在"最后一个跨请求相同的块"上;
2. tools 定义一旦进入会话就冻结(要改就开新会话);
3. 动态状态一律走尾部注入(system-reminder 模式);
4. 工具输出先过滤再入上下文,超过阈值截断或外置;
5. 压缩有损,关键结论必须外置成文件/笔记,不赌摘要;
6. 长会话主动算账:token counting 决定何时 compact,而不是撞限被动触发。

**Q2.23【品味·L3·平台】什么时候该果断开新会话,什么时候值得继续压缩续命?**
参考思路:
- 开新会话:任务已完成交接(状态已外置);上下文里堆了大量与下一步无关的过程;同一问题纠正两次失败(书/官方 best practice:连续 2 次修不好就 /clear 换思路);反复 compaction 触发 thrashing。
- 续命:任务中间态复杂且未外置,重建成本高于压缩损失;里程碑式的长任务应该用"结构化笔记"而不是纯 compaction 保命(Anthropic:Claude Plays Pokémon 用进度笔记维持数小时一致性)。
- 高级点:GPT-5.1-Codex-Max 是"首个原生训练为跨压缩上下文运行"的模型——压缩策略与模型训练开始耦合,选模型时要把 compaction 行为当成能力的一部分。

**Q2.24【品味·L4·架构】你要在"100% 缓存命中率但上下文持续膨胀"和"频繁压缩但缓存反复失效"之间做调度。设计这个权衡的决策框架。**
参考思路:
- 变量:上下文增长率、context rot 边际损失、缓存折扣(10×)、压缩成本(一次全量重读+摘要)、压缩后缓存重建成本、TTL。
- 框架:定义"有效注意力预算"拐点(经验几十 K–百余 K),超过后继续膨胀的**质量成本**开始超过缓存节省的**金钱收益**;
- 调度策略:周期性批量清理(clear_at_least 思想)而不是涓流清理;把"该忘的"集中在一次失效里;里程碑处压缩+外置,而不是随机点;
- 顶层方案:subagent 分区把噪声隔离在主线程之外,主上下文只接收 1–2K 摘要——从源头减少需要管理的历史。

**Q2.25【品味·L3·用户】CLAUDE.md 每一行该用什么标准决定去留?**
参考思路:
- 官方标准:"删掉这一行,Claude 会不会犯错?"——写**不可从代码推断的事实**(构建命令、坑、约定),不写可推导内容;
- <200 行;可验证的具体指令优先于抽象原则;
- 硬约束(禁止目录、危险命令)不该写在 CLAUDE.md 里赌模型遵守,应该交给 hooks/权限系统确定性执行——**文档管软约束,代码管硬约束**。

**Q2.26【品味·L4·管理】你的 agent 平台月账单涨了 3 倍,CTO 要求"降本"。你第一件事查什么?**
参考思路:
- 先分账:缓存命中率(最大的杠杆,命中从 60%→90% 等于输入费近腰斩再打折)、上下文平均长度与会话轮数分布、模型分布(该用 Haiku 的活是不是全在 Opus)、工具输出失控点(哪个工具在倾倒 token)、无人值守的后台任务/定时任务空转重读。
- 常见根因排序:缓存破坏(tools 抖动/动态 system)> 工具输出未过滤 > 模型不分级 > 无效长会话。
- 降本顺序:先修缓存与压缩(不改行为),再模型路由(轻度影响),最后砍功能。**"贵"经常不是单价问题,是调度问题(书 Part 5.1:真正吃预算的是低质量吞吐)。**

---

## 2.5 开放性问题

**Q2.27【开放·L5·模型】如果让你重新设计一个"为压缩而训练"的 agent 模型(GPT-5.1-Codex-Max 方向),训练目标和数据该怎么构造?**
讨论框架:在压缩摘要之上继续推理的训练数据从哪来(真实长会话的回放?合成?)、奖励怎么定义(压缩后续任务成功率 vs token 节省)、摘要保真度与任务相关性的张力、是否需要专门的"记忆写入"动作头。

**Q2.28【开放·L5·平台】缓存、压缩、检索三者的边界会不会被"无限上下文 + 免费缓存"的模型进步抹掉?哪些工程投入是抗未来的,哪些会贬值?**
讨论框架:即使窗口免费,attention budget/context rot 仍是模型内在约束,压缩与检索的价值会保留;稳定前缀纪律可能因厂商缓存机制演进而部分贬值(如 OpenAI 全自动缓存已无需打点);抗未来的投入:信息分层治理、外置状态、验证器——它们不依赖任何一家的缓存实现。

**Q2.29【开放·L4·架构】为"上下文质量"设计一套可度量的指标体系,让你能在团队间横向对比。**
讨论框架:输入侧(信噪比:任务相关 token 占比、重复内容率)、过程侧(缓存命中率、compact 频次、上下文利用率峰值)、结果侧(任务成功率与上下文长度的相关性、lost-in-middle 类探针任务的通过率)。

---

## 2.6 工程实战题

**Q2.30【实战·L3·平台】你们的自研 agent(Anthropic API)长会话缓存命中率只有 40%。给出系统性的排查与优化方案。**
期望框架:
- 排查:按请求维度 dump usage(cache_read_input_tokens/cache_creation_input_tokens/input_tokens),对 miss 的请求做前缀 diff(system 变?tools 变?消息中段被改?断点位置?TTL 过期?);
- 常见修复:冻结 tools 定义与顺序;去掉 system 里的时间戳/动态内容,改尾部注入;断点策略(长会话在前缀中段加第二个断点);>5min 间隔的场景用 1h TTL;
- 结构性优化:工具输出过滤前置、context editing 批量清理、subagent 隔离高噪声任务;
- 验证:命中率与单任务成本的前后对比,回归确认任务成功率不降(压缩加深可能丢信息)。

**Q2.31【实战·L3·平台】设计一个"工具结果压缩管道":agent 会调用 20 种工具,其中 5 种(日志查询、大文件读取、HTTP 抓取)会返回巨量输出。**
期望框架:
- 分层:①工具侧:强制分页/limit 参数、提供 concise/detailed 输出格式枚举(参考 Anthropic 工具设计:concise 约省 2/3);②进入前:PostToolUse/中间件按规则截断(头尾保留、错误优先、结果摘要化——可用小模型做摘要);③驻留期:超过 N 轮的旧结果替换为占位(context editing);④极端:超大输出直接外置文件,上下文只留路径+摘要。
- 防坑:压缩不能破坏错误现场(排错时需要原文);占位文本要可追溯("原始结果存于 X");对 diff 类输出慎压缩(精确匹配会被破坏)。

**Q2.32【实战·L4·架构】设计 GraphSpec 式的"动态上下文路由器":大仓库 50 万行,agent 默认只看到顶层架构图,通过工具按需下钻。定义工具集、缓存策略、防迷路机制。**
期望框架(书的主线项目思路):
- 工具:`get_architecture(node_id)` 返回当前层级;`zoom_in(node)` 下钻;`get_related(node)` 节点关联内容;`diff_architecture(commit)` 变更对比;写操作走人工确认。
- 缓存:架构图版本化(变更才失效);下钻结果按 node 缓存;主上下文只保留已访问路径的"面包屑"。
- 防迷路:访问预算(最大下钻深度/次数);面包屑回链;超出预算强制总结返回;结构化笔记记录已探索区域。
- 本质:把"塞满上下文"变成"教 agent 像人一样顺着图谱请求上下文"。

**Q2.33【实战·L2·用户】你的 CLAUDE.md 已经膨胀到 600 行,agent 行为反而变差。怎么重构?**
期望框架:
- 分层:600 行里多数是"某类任务才需要的"→拆成 skills/子目录 CLAUDE.md(按路径 lazy load)/rules(paths 作用域);
- 保留判据:删掉会犯错的留,其余删;硬约束迁去 hooks/权限;过期内容直接删而不是注释;
- 目标:根 CLAUDE.md <200 行,只留:项目一句话、常用命令、硬约定、指向分层资产的索引。

---

## 2.7 场景排错题

**Q2.34【排错·L3·平台】现象:agent 每轮响应都要 40–60 秒才开始吐字,账单里 input token 计费暴涨,cache_read 几乎为 0。最可能的三个根因?**
排查链路:
- ①前缀被动态化:system prompt/首条消息里有时间戳、环境变量快照、随机 id→每轮全量重算 prefill(40–60s 的 TTFT 正是百万级 token 重 prefill 的特征);
- ②tools 定义抖动:MCP server 启动顺序/可用工具集在轮间变化;
- ③TTL 失配:每轮之间人审间隔超过 TTL(5min)。
- 验证:相邻两轮请求体做前缀 diff;看 miss 的精确断点位置;修复后 TTFT 应回到秒级。

**Q2.35【排错·L2·用户】现象:长会话进行到一半,agent 突然"失忆"——忘了前面的决定,重新读文件,甚至重复问已回答的问题。发生了什么?**
排查链路:
- 优先怀疑 auto-compact 刚触发:检查是否有 compaction 边界;摘要丢了早期决策(决策没有外置到文件/todo)。
- 次选:子代理返回后主线程只拿到摘要;或 memory 文件被覆盖。
- 修复习惯:重要决定让 agent 写进 todo/计划文件(结构性笔记),而不是依赖对话记忆;对超长任务用"里程碑笔记"而非无限续会话。

**Q2.36【排错·L3·平台】现象:接入了 8 个 MCP server 后,agent 选工具经常选错、响应变慢、成本翻倍,部分请求还超上下文。诊断与处置?**
排查链路:
- 根因:工具过载——多 server 场景工具定义未干活先吃数万 token(Anthropic 文档实例约 55K);工具选择准确率在 **30–50 个工具以上退化**;定义膨胀还可能破坏缓存。
- 处置:启用 tool_search/延迟加载(工具只留名称,命中再展开;可省 85%+ 定义 token);按任务域拆 subagent(每个 subagent 只挂相关 server 的工具);合并语义重叠工具;砍掉低频 server。
- 验证:工具选择准确率、单任务 token、TTFT 三指标前后对比。

**Q2.37【排错·L4·平台】现象:compaction 之后任务成功率从 85% 掉到 60%。怎么定位是压缩丢了什么?**
排查链路:
- 构造对照:同一批任务分别在"压缩前终止+新会话注入摘要"与"不压缩(短任务)"下跑,隔离压缩变量;
- 对失败 case 做摘要 vs 原文的差异分析:丢的是哪类信息(早期用户约束?失败的尝试及原因?文件路径?任务边界?);
- 修复:把高频丢失类别外置(用户约束进 CLAUDE.md/任务文件;失败尝试写进笔记);调整 /compact focus 指令;必要时换"checkpoint+新会话+结构化交接"替代在线 compaction。
