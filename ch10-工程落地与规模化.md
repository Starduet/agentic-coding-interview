# ch10 · 工程落地与规模化

> 覆盖:书 Part 2 定盘(需求收敛/范围裁剪/技术栈/仓库/系统设计/规范资产化)、Part 3 长主干(execution graph/官方路径/事实-证据-投影)、Part 4 接世界(运行时可见性/外部边界/交付闭环)、Part 5 自运行(成本吞吐治理/反脆弱 Harness/human-on-the-loop);调研补充:Spec Kit/Kiro、CI/CD 集成、组织采用数据。这是"书的工程主线"最重的一章。

---

## 10.1 八股题(基础概念)

**Q10.1【八股·L2·管理】书说拿到 PRD 后第一步不是开工,而是收四类决策。是哪四类?**
答:
- 这个版本到底只回答**哪个问题**(版本目标);
- 这个版本明确**不回答什么**(非目标);
- 当前阶段**切到哪一层为止**(阶段边界);
- **什么结果才算做成**(验收口径)。
- 背景:粗糙 PRD 直接丢给 AI,AI 会"把模糊补成具体实现、把未来能力提前带入、把待讨论边界固化成仓库事实"——替项目**偷做决策**。

**Q10.2【八股·L2·管理】"范围裁剪最难的不是少做功能,而是把阶段切开"——书给的按风险拆分法?**
答:一个阶段只优先消掉**一类**风险,四类风险:
- 价值风险(用户在不在乎)、交互风险(体验怎么走)、契约风险(数据/接口/命令面怎么定)、治理风险(护栏/审计/回滚/验证到什么程度);
- 一个阶段同时正面解决四类=切太大;真正的硬刀是"当前阶段只验证哪一类问题"——比"做/不做某功能"更重要,因为问题常不是功能不该存在,而是它不该现在进场。

**Q10.3【八股·L2·用户】书的"tasks.md 不是施工顺序表,而是孵化 issue"——"可收敛的不确定性"定义?**
答:一个任务值得独立成 issue,当且仅当:
- 单独拿出来讨论,有机会在一轮或几轮对话里把边界收住;
- 不先收住,后面很多实现会一起漂;
- 收住之后,能为后续一批实现提供共同前提。
- 反面:任务写成"先做后端→再做前端→再补测试"的施工清单,默认项目已完成收敛——起盘阶段最缺的恰恰是问题边界。

**Q10.4【八股·L2·用户】spec-first 流程里 discussion→issue→proposal/design/specs/tasks 各回答什么?**
答:
- discussion:大家在担心什么/想推进什么(方向、争议);
- issue:这次先收哪个可协作问题(why now、范围、非目标、未决点、验收);
- proposal.md:为什么要开这个 change(背景、边界、已定/待定);
- design.md:这个 change 准备怎么做(主资产、路径、模式、失败链路);
- specs/<capability>/spec.md:相关能力在**行为上必须成立什么**(requirement + scenario——最容易被漏的一层);
- tasks.md:怎样按依赖关系落下去(串行阻塞项、可并行项、审核点、验证动作)。

**Q10.5【八股·L2·架构】"控制面资产化"的三对照(书 Part 2.5)?**
答:
- 背八股/看源码/靠资深工程师脑补经验 → 把经验 **skill 化**;
- 靠口头提醒"这次记得别乱来" → 把长期边界 **rule 化**;
- 靠飞书文档/聊天记录/零散提纲保存变化 → 把 change **spec 化**。
- 核心:AI Native 的升华不是文档变多,而是**隐性判断被外置成可稳定复用的控制面资产**。

**Q10.6【八股·L1·用户】execution graph(书 Part 3.1)比线性施工清单多了什么要素?**
答:tasks.md 升级为执行图要包含:**串行阻塞项**(依赖链)、**并行切片**(可同时开的)、**verifier**(每步怎么验证)、**checkpoint**(断点)、**handoff artifact**(交接产物)——把"任务拆解"升级成"执行图设计"。

**Q10.7【八股·L1·用户】"先收官方路径,不要先铺满功能"(书 Part 3.2)是什么意思?**
答:AI 最喜欢的"顺手把副线一起铺开"恰是失控起点;当前 phase 的官方路径=主资产沿**最小闭环**先长出来(能从入口走到验证的那条路),副线(SDK、报表、边角配置)后置;判断框架:这一步是不是主资产闭环上不可绕过的一环。

**Q10.8【八股·L2·用户】交付闭环的 release gate 卡什么?approval pause 为什么值钱?(书 Part 4.4)**
答:
- release gate:确定性检查全绿(测试/lint/安全)、契约/迁移核验、风险分级后的按级审批;
- approval pause:在不可逆动作(发布/写生产/删数据)前的结构性暂停——它是把"人还在控制面"物化的机制,而不是流程仪式;
- 配套:incident replay(事故可回放)、rollback(预先演练过的回退路径)、人工升级通道(什么时候必须叫人)。

**Q10.9【八股·L2·管理】成本治理里"真正吃预算的不是模型单价,而是低质量吞吐"怎么理解?(书 Part 5.1)**
答:
- 低质量吞吐=花了 token 却产出要返工的结果:方向错误的探索、无验证器的反复试错、上下文污染后的重做、无人值守的空转;
- 治理四件套:模型分层(旗舰做决策/主力做实现/便宜做铺量)、任务分层(价值密度决定投入)、上下文预算(见 ch02)、并行度与 verifier 成本(验证也要算账);
- 呼应调研:Claude Code 平均 $13/开发者/活跃日、$150–250/月,90% 用户日花费 <$30;agent teams 耗 token 约 7×——并行度是预算杠杆也是风险。

**Q10.10【八股·L2·架构】反脆弱 Harness(书 Part 5.4)的核心机制?**
答:
- **failure taxonomy**:把失败分类(规格层/组织层/协调层/验证层/运行时层…);
- 哪些失败必须被升级成 rule、verifier、skill 或 cleanup job——同一错误出现三次还靠人提醒,就不是反脆弱;
- **background cleanup / doc gardening / garbage collection 不是边角活**:过期规则、归档 spec、死代码是 semantic drift 的温床;
- 目标:系统因为失败变稳,而不是更乱。

**Q10.11【八股·L1·管理】human-in-the-loop 和 human-on-the-loop 的区别?(书引 Thoughtworks)**
答:
- in the loop:agent 每做错,人下场补代码/补 prompt/补说明——人是肉身 QA;
- on the loop:agent 反复做错时,人去改**产生结果的那条流程本身**(改 rule/verifier/skill/hook);
- 人类角色上移:目标定义、节奏控制、边界设计、风险升级、责任兜底。

---

## 10.2 原理深挖

**Q10.12【原理·L3·管理】"粗糙 PRD 的正确用法是让 AI 暴露问题,不是暴露代码"(书 Part 2.1)。设计这个"问题地图"流程。**
答:
- 让 AI 产出两层:①开工前必须讨论清楚的问题清单(现在卡在哪);②从起盘到交付还会在哪分歧(这条线会长成什么样);
- 问题地图的价值:把"没定的"显形——显形后可以追问/反驳/收紧,没显形就是赌;
- 案例锚点:GraphSpec 从模糊想法→问题地图(痛点切口/非目标/基础契约三组问题进 GitHub Discussions)→项目章程;
- 输出物不是代码或功能样稿,是**可反驳的第一轮锚点**(主切口/不做什么/最小契约锁哪层/哪些现在拍板哪些后谈)。

**Q10.13【原理·L3·管理】为什么书反对把 proposal 写成"详细施工图",主张写成"项目章程"?**
答:
- 施工图式 proposal 细得太早——替未来每个局部问题做决定,诱导 AI 直接滑向实现;
- 章程式 proposal 的五职责:给产品下定义(是/不是什么)、给版本下定义(只回答哪个问题)、给边界下定义(明确不做什么)、给讨论下定义(哪些已定/待定)、给推进下定义(issue 按什么规则长出来);
- 判别:如果文档写得像实现说明书,它会诱导写代码;写得像章程,会诱导继续切问题、细化、验证。

**Q10.14【原理·L3·架构】技术栈定盘的"最小底板"原则(书 Part 2.2):开局该定什么、明确不定什么?**
答:
- 该定(低耦合、定错便宜):主语言与类型系统、最小交互层与开发底座、工程约束(包管理/lint/monorepo 与否)、**"哪些现在还没资格被决定"清单**;
- 不该定(高耦合、依赖真实问题):数据库与 ORM、消息队列/agent 框架、宿主方案(Tauri/Expo/…)、重型 harness(全量 E2E/压测);
- 流程:问题写具体→AI 给候选不拍板→判断是否需要"查新"(模型知识有时差)→最小 demo 验证最危险假设→结论写回文档(为什么选/为什么不选/何时重估)。

**Q10.15【原理·L4·架构】外部边界进 loop(书 Part 4.3):数据库、第三方 API、文件系统、IPC 各自的接入要点?**
答:
- 数据库:状态可见(migration 状态、种子数据)、agent 能查询不能乱写(只读副本/审批);
- 第三方 API:失败语义结构化(4xx/5xx/限流各意味什么、重试策略)——agent 最怕的是把"第三方挂了"当成"我的代码错了";
- 文件系统:路径约定、工作区边界(防越界读写);
- IPC/权限模型:宿主能力的显式契约(能调什么、返回什么);
- 总原则:把"落地前最后一层"做成 agent 可观察、可验证、可安全操作的面。

**Q10.16【原理·L3·平台】AI 代码进 CI/CD 的关键工程决策?**
答:
- 触发与权限:@claude/@codex 评论触发或 automation 模式;GitHub Actions 权限最小化(contents/PR/issues write + id-token:write;OIDC 联邦免长密钥);
- 成本与失控控制:--max-turns、超时、并发限制、精简 CLAUDE.md;
- 产出策略:默认 Draft PR,人审合并;高风险路径(安全/迁移/CI 自身)禁止 agent 直接改;
- 与 AI review 组合:CodeRabbit/Graphite 先扫,人做最终判断——分级门禁而非全人工。

---

## 10.3 品味与判断题

**Q10.17【品味·L4·管理】你团队要立项一个新项目。两个方案:A) 先写完整 spec 套件再开工;B) 直接 vibe 出 MVP 再补文档。你的裁决条件?**
参考思路:
- 裁决变量:返工成本(生产级 vs 探索)、需求确定度、团队对"砍什么"的共识程度;
- 中间态(书的立场):章程级轻 spec(目标/非目标/验收)+TestDriven 最小闭环——不需要完整 spec 套件,但不能没有"这一版只回答什么";
- 警惕:spec 套件也可能成为仪式(没人维护的文档比没有更糟);vibe MVP 的隐性成本是把"该砍什么"的学费推迟到生产。

**Q10.18【品味·L3·架构】"验收标准前置"在 agent 时代为什么从"好习惯"变成"刚需"?**
参考思路:AI 会用"看起来差不多"糊过模糊验收;验收可执行化(测试/断言/截图对比)是 agent 自我纠错的前提——没有验收=每个结果都要人肉判定,harness 退化成聊天;顺带回答:验收写不出来说明需求还没收敛,这本身就是最有价值的信号。

**Q10.19【品味·L4·管理】"反脆弱"落到 KPI 上怎么度量?给出 3 个指标。**
参考思路:同类错误复发率(同 taxonomy 类别的重复故障);失败→规则/verifier 的转化率与时长;人工干预密度(每千次操作的人肉介入次数)趋势;补充:过期资产清理率(doc gardening 的 backlog 消化)。反对只测"agent 成功率"——它不反映系统在学习。

**Q10.20【品味·L3·用户】spec 工具(Spec Kit/Kiro/OpenSpec)对你们的 brownfield 项目是减负还是负担?**
参考思路:判断——已有文档存量多少、变更频率、团队规模;Spec Kit(GitHub-native、/specify→/plan→/tasks、30+ agent 支持)适合流程化团队;OpenSpec(轻量、以 change 为中心、brownfield 定位)适合存量演进;Kiro(EARS 格式、自动查需求矛盾、property-based testing)适合新立项;共同风险:spec 与代码漂移(需要 /converge 类对账机制)。

---

## 10.4 开放性问题

**Q10.21【开放·L5·管理】如果"agent 写代码"的比例到 80%,软件工程的组织形态(角色/评审/晋升/所有权)会怎么重组?**
讨论框架:代码所有权→意图与验证所有权;评审从 diff 审查转向 spec 审查与抽样审计;晋升标准从产出量转向判断力与系统设计;新角色:控制面工程师/验证工程师;风险:初级工程师成长路径断裂(没有补全时代的练手场)。

**Q10.22【开放·L4·管理】METR RCT(资深开发者慢 19%)与自评数据(1.4–2x 提升)并存。你如何在自家组织建立可信的效能度量?**
讨论框架:度量分层——客观交付指标(PR 周期/变更失败率)优先于自评;任务分类(新功能/维护/迁移的增益不同);RCT 不可行时的准实验(对照团队/AB 工具开关);警惕 Goodhart(为指标制造小 PR)。

---

## 10.5 工程实战题

**Q10.23【实战·L3·管理】为一个"内部运维 agent 化"项目做需求收敛:目标是让 agent 处理 60% 的常规工单。给出你的章程一页纸。**
期望框架:产品定义(是什么/不是什么——不是全自动运维,是带审批的执行建议);版本目标(这一版只回答:告警诊断建议+只读查询自动执行);非目标(不做自动扩缩容/不做写操作);阶段风险(先消价值风险:诊断建议有没有人用);验收口径(诊断采纳率、MTTR 变化、误操作为 0);issue 生成规则。

**Q10.24【实战·L4·架构】设计你们团队的 execution graph:一个"支付模块重构"的 change,涉及 12 个任务、3 人 + 多个 agent。**
期望框架:
- 任务分层:契约先行(schema/错误语义冻结)→串行阻塞项(数据库迁移)→并行切片(按文件所有权)→verifier 挂载点(每片的机械验证)→checkpoint(契约变更点)→handoff(每片完成产出交接单);
- 人机分工:人定契约与验收、agent 执行片、reviewer agent+人双审;
- 失败路径:回滚点、阻塞升级。

**Q10.25【实战·L3·平台】把 agent 接进你们的发布流程:目标是"PR 合并→agent 生成 changelog→预发验证→人工批准→发布"。**
期望框架:触发(GitHub Actions @claude);工具面(只读仓库+写 changelog 文件+调用部署 API 的只读版);verifier(changelog 与 commit 的覆盖率检查、预发 smoke);approval pause(发布前硬闸,结构化不可跳过);失败路径(预发失败自动回退 PR 状态);审计(全程 trace 落盘)。

**Q10.26【实战·L4·管理】你们 agent 平台月成本 $40k,要求 3 个月降到 $20k 且任务质量不降。给方案。**
期望框架:
- 先分账(按团队/任务类型/模型/缓存命中率);
- 无损优化(预期降 30–50%):缓存修复(tools 冻结/前缀稳定)、工具输出过滤、compaction 阈值调优、低价值后台任务清理;
- 结构优化(10–30%):模型路由(铺量任务降档)、subagent 摘要规格化、Batch API(异步任务 5 折);
- 质量护栏:每步用内部 eval 回归,质量降即回滚;
- 汇报口径:成本/任务 与 质量 双曲线(降本不降质的可信证明)。

---

## 10.6 场景排错题

**Q10.27【排错·L3·管理】现象:团队买了最贵的工具和订阅,6 个月后交付速度没有变化,大家开始说"AI 不行"。诊断?**
排查链路:①度量本身(有没有 baseline 与客观指标,还是纯体感);②瓶颈定位——书的三卡点:底层闭环不足(改来改去总返工)/交互停留在问答/模式没固化(每次重讲规则);③控制面资产检查(AGENTS.md/spec/verifier 存在吗,还是每个任务裸奔);④任务分布(是不是把最不适合的任务硬塞给 agent——见 METR 分布差)。通常结论:工具到位,纪律缺位。

**Q10.28【排错·L4·架构】现象:agent 生成的 PR 通过了全部 CI,但合并后一个月内引发 3 次生产事故,都是"边界条件没考虑"。系统性修复?**
排查链路:先做事故归因分类——测试盲区(验证器上限:CI 测不到的边界)/spec 缺失(验收没写边界条件)/agent 行为(为过测试走捷径);
- 修复分层:每个事故→一个回归测试+一条规则/checklist;验收模板强制"边界条件清单"节;对 agent 的产出加"边界自证"要求(列出做了哪些边界处理+证据);
- 升级机制:同类第三次出现必须升级为 verifier(反脆弱判据)。
