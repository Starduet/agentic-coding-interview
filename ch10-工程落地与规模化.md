# ch10 · 工程落地与规模化

> 覆盖:需求收敛与验收前置、spec 工具与流程、AI 代码进 CI/CD、成本治理、失败驱动的系统改进(failure taxonomy)、组织采纳与效能度量、human-in-the-loop 的角色上移。
> 案例基线:2026-08。方法论题不挂时间;带日期案例以标注为准。

---

## 10.1 八股题(基础概念)

**Q10.1【八股·L2·管理】拿到 PRD 后、让 agent 开工前,要先收哪四类决策?为什么"粗糙 PRD 直接丢给 AI"是危险的?**

- **人话版**:这个版本只回答哪个问题(目标)、明确不回答什么(非目标)、做到哪层为止(阶段边界)、什么结果算做成(验收口径)。危险在于:AI 会把模糊补成具体实现、把未来能力提前带入、把待讨论的边界固化成仓库里的事实——它在替你做决策,而且做完不告诉你。
- **展开**:
  - **四类决策各封一个失控方向**:目标不收→AI 脑补需求;非目标不收→AI"顺手"扩写(它没有成本压力,扩写是它的美德);阶段边界不收→一次做四层(价值/交互/契约/治理全铺开,每层都浅);验收不收→AI 用"看起来差不多"交差,每个结果都要人重新判定。
  - **机制**:模型的默认行为是"最大化覆盖需求的理解"——模糊处它一定填空,而且填得自信。人脑接 PRD 时会自动挂起模糊点("这个回头问一下"),模型不会,它当所有信息都齐了。
  - **轻量做法**:四类决策各写一行就是一个合格的"任务前置块";重一点用 plan mode 让 agent 先复述它的理解,人校对完再放行——模糊点在计划里会暴露出来。
- **及格线**:四类决策+AI 替你做危险决策的机制。
- **加分**:四类各对应一种失控形态;plan mode 当校对口的用法。
- **红旗**:认为"AI 会自己理解需求";把四类决策当成走流程的表格。

**Q10.2【八股·L2·管理】"范围裁剪最难的不是少做功能,而是按风险切阶段"——一个阶段应该只优先消掉哪类风险?四类风险是什么?**

- **人话版**:价值风险(用户在不在乎)、交互风险(体验怎么走)、契约风险(数据/接口怎么定)、治理风险(护栏/审计/回滚到什么程度)。一个阶段只正面解决一类——同时解四类等于切太大,每类都验证不充分。
- **展开**:
  - **为什么按风险不按功能切**:功能维度看"做不做 X",风险维度看"现在最怕什么没答案"。常见病不是"X 不该存在"而是"X 不该现在进场"——价值没验证就把契约焊死,后面全返工。先问"这个阶段结束时,我最想确认哪个问题有答案",功能自然归位。
  - **顺序的参考**:多数产品先消价值风险(做的东西有人要吗)再消交互,契约要早于大规模实现(接口返工最贵),治理随风险等级渐进——但不是铁律,金融/医疗类产品治理风险前置。
  - **对 agent 工作流的意义**:阶段切小+每阶段一个验证目标,agent 的任务包就小而清晰——大阶段交给 agent 等于把"切阶段"的决策也外包给了它(Q10.1 的失控)。
- **及格线**:四类风险+一次只解一类。
- **加分**:"不是不该存在是不该现在进场"的辨析;对 agent 任务包大小的影响。
- **红旗**:按功能清单切阶段(做用户系统→做订单→做报表,没有验证目标)。

**Q10.3【八股·L2·用户】spec-first 的工具链(Spec Kit/Kiro/OpenSpec 一类)里,discussion→issue→proposal/design→spec→tasks 各自回答什么?最容易被漏的是哪层?**

- **人话版**:discussion 收方向和争议,issue 收"这次先解决哪个可讨论的问题",proposal/design 定"为什么做和怎么做",spec 定"行为上必须成立什么"(requirement+scenario),tasks 定落地顺序。最容易漏的是 spec——大家爱写设计(怎么做)不爱写行为要求(什么算对)。
- **展开**:
  - **spec 层的价值**:它是对"正确"的可执行描述——scenario 是验收用例的雏形,直接喂给测试和 agent 的验收标准;没有这层,设计写完直接跳 tasks,验收标准散落在人的脑子里,agent 每个产出都要人肉判定。
  - **工具参考**:GitHub Spec Kit(/specify→/plan→/tasks 的流水线,几十种 agent 支持)适合 GitHub 流程成熟的团队;OpenSpec 轻量、以变更为中心、面向存量项目演进;Kiro 用 EARS 格式写需求、自动查矛盾。共同风险:spec 与代码漂移——必须有对账机制(定期 converge/审计),否则 spec 变成第二份过期文档。
  - **判断**:这些工具对"已有文档存量、变更频繁"的项目是减负还是负担,取决于团队是否真的会在 PR 里改 spec——不会的话,轻量到一页纸的"目标/非目标/验收"比完整套件更诚实。
- **及格线**:五层各一句+spec 层最容易被漏。
- **加分**:spec→验收用例的转化;工具选型的判断;漂移风险。
- **红旗**:把 spec 工具当"更高级的文档工具";流程齐全但没人维护 spec。

**Q10.4【八股·L2·架构】AI 代码进 CI/CD 的关键工程决策有哪些?**

- **人话版**:四个——触发与权限(评论触发/最小化 token/无长密钥)、失控控制(轮次/超时/并发上限)、产出策略(默认 Draft PR 人审合并,高危路径禁 agent 直接改)、与 AI review 工具的分级组合。
- **展开**:
  - **权限最小化**:GitHub Actions 只给所需权限(contents/PR write),用 OIDC 联邦免长密钥;agent 的凭证 per-workflow 短期化——CI 里的凭证泄漏是供应链攻击的入口(ch08)。
  - **失控控制**:--max-turns、超时、并发限制、token 预算熔断;CI 里的 agent 没有人盯着,预算是唯一的缰绳(ch04 Q4.23)。
  - **产出策略**:默认 Draft PR——人审是合入的必要条件;安全敏感路径(CI 配置自身、鉴权、依赖)禁止 agent 直接改,变更必须走人审通道(2026-08 的 Copilot Autofix 事件是反例教材,见 ch08 Q8.10)。
  - **分级门禁**:确定性检查(测试/lint/安全扫描)全自动放前面,LLM review 辅助在后,人做最终判断——顺序不能反(确定性便宜的先筛,贵的判断放最后)。
- **及格线**:四个决策。
- **加分**:OIDC/短期凭证;分级门禁的顺序逻辑;引用 Autofix 类事故说明人审通道的必要。
- **红旗**:agent 直接有 push 权限;把 AI review 放在测试前面。

**Q10.5【八股·L2·管理】成本治理里"真正吃预算的不是模型单价,而是低质量吞吐"——怎么理解?治理抓手有哪些?**

- **人话版**:低质量吞吐=花了 token 产出要返工的结果——方向错的探索、没有验证器的反复试错、上下文污染后的重做、无人值守的空转。单价的优化是乘法系数,吞吐质量的优化是数量级。抓手指:模型分层、任务分层、上下文预算、验证器成本。
- **展开**:
  - **低质量吞吐的四种形态**:方向错误(任务没收敛就开工,做完才发现);无验证试错(没有测试反馈,模型瞎试到预算耗尽);污染重做(上下文里过期信息导致改错再改回);空转(后台任务失败重试无限循环、重复读长上下文)。
  - **治理四件套**:模型分层(旗舰决策/主力实现/便宜铺量);任务分层(价值密度决定投入,低价值任务不许用旗舰);上下文预算(缓存命中率+工具输出治理,ch02);验证器算账(验证也要 token——但省的是返工,ROI 通常为正,要度量不能信仰)。
  - **量级参照**:官方口径 Claude Code 平均每开发者每活跃日约 $13、月 $150–250,90% 用户日花费低于 $30;多 agent 并行 token 约 7 倍起——并行度是预算的最大杠杆也是最大风险。
- **及格线**:四种形态+四件套。
- **加分**:验证器成本的 ROI 度量;并行度的杠杆意识。
- **红旗**:成本治理=砍单价换便宜模型;只盯订阅账单不看 token 去向。

**Q10.6【八股·L2·架构】failure taxonomy(失败分类)是什么?"反脆弱"的判据是什么?**

- **人话版**:失败分类就是给 agent 的每次失败贴标签归因——规格层(任务没说清)、工具层(工具返回不稳/描述差)、验证层(没有判定标准)、运行时(超时中断)。反脆弱的判据:同类错误第三次出现时,系统里多了一条规则/验证器/自动清理,还是还在靠人肉提醒——前者系统因失败变稳,后者只是失败很多。
- **展开**:
  - **taxonomy 的杠杆**:没有分类,每次失败都是新故事,治理无从下手;有了分类,高频类一目了然,修复优先级自动浮现(工具层的修复经常"修一处好一片")。
  - **升级机制(反脆弱的核心)**:失败→归因→对应的固化动作:规格层失败→改任务模板/记忆文件;工具层失败→修工具描述与返回;验证层失败→补 verifier;运行时失败→加熔断/重试策略。每次升级都是把"人的教训"变成"系统的免疫力"。
  - **容易被低估的一半:清理**:过期规则、归档 spec、死代码的定期清理(doc gardening)不是边角活——它们是 semantic drift 的温床(ch06);只加不删的反脆弱是另一种腐烂。
- **及格线**:分类四层+"第三次"判据。
- **加分**:每层对应的固化动作;清理作为反脆弱的另一半。
- **红旗**:有失败复盘会但没有固化动作;把反脆弱当名词不当机制。

**Q10.7【八股·L1·管理】human-in-the-loop 和 human-on-the-loop 的区别?为什么说后者才是规模化?**

- **人话版**:in the loop 是 agent 每做错,人下场补——补代码、补 prompt、补说明,人是肉身 QA;on the loop 是 agent 反复做错同一类事时,人去改**产生结果的那条流程**(改规则/验证器/记忆文件),让这类错误从此不可能。前者的人力随任务量线性涨,后者摊薄。
- **展开**:
  - **角色上移的谱系**:执行(写代码)→审查(看 diff)→验收设计(定标准)→系统改进(修流程)。AI 时代人的价值沿这条轴向上移:目标定义、节奏控制、边界设计、风险升级、责任兜底。
  - **判断团队阶段的问题**:你的团队在哪个位置?如果工程师每天的主要 AI 相关工作是"修 agent 的产出",还在 in the loop;如果是"维护规则和验证器",已经 on the loop。前者不可持续(人力天花板),后者才有复利。
  - **落地抓手**:每次人工干预记一笔"这类事能不能自动化"——干预日志是升级 backlog 的原料;没有这个日志,on the loop 只是口号。
- **及格线**:两者区别+人力线性vs摊薄。
- **加分**:干预日志这个抓手;角色上移的谱系。
- **红旗**:把 on the loop 理解为"不让人看"(它恰恰要求更高质量的人机界面)。

---

## 10.2 原理深挖

**Q10.8【原理·L3·管理】"粗糙 PRD 的正确用法是让 AI 暴露问题,不是暴露代码"——设计这个"问题暴露"流程。**

参考思路:
- **流程**:把 PRD 丢给 agent,但任务不是"实现它",而是两个产出:①开工前必须讨论清楚的问题清单(现在卡在哪、哪些模糊、哪些互相矛盾);②从现在到交付会依次遇到的分歧点(这条线会长成什么样)。人拿着这两份东西去收敛,收敛完再开工。
- **为什么有效**:模糊点在人脑里是挂起的,在模型那里是被自信填空的——让它输出"问题"而不是"实现",它的填空冲动变成了暴露问题的探针;问题显形后可以追问/反驳/收紧,不显形就是赌。
- **输出物的形态**:可反驳的第一轮锚点——主切口(解决谁的什么问题)、不做什么、最小契约锁哪层、哪些现在拍板哪些后谈。每项都要能被人否掉重写,不是论文式的背景综述。
- **及格线 / 加分 / 红旗**:及格=两个产出+人收敛后再开工;加分="填空冲动变探针"的机制解释;红旗=让 AI 直接出方案然后人挑毛病(顺序反了,方案的框架已经锚定讨论)。

**Q10.9【原理·L3·架构】技术栈选型在 agent 时代怎么做法?"现在该定的"和"明确先不定的"怎么划分?**

参考思路:
- **现在该定的(定错便宜、影响全局)**:主语言与类型系统、最小开发底座(包管理/lint/monorepo 与否)、工程约束、以及一份**"哪些还没资格被决定"的清单**——把"现在不定"也写成显式决策,防止 AI 顺手替你定。
- **明确先不定的(依赖真实问题、返工贵)**:数据库与 ORM、消息队列、宿主方案、重型测试设施——这些应该等问题显形,用最小 demo 验证最危险的假设再定。
- **流程**:问题写具体→让 AI 给候选方案与trade-off(它知识新但不了解你的约束——别让它拍板)→查新(模型知识有时滞,新版本的工具问它最新情况)→demo 验证→结论写回文档(为什么选/为什么不选/何时重估)。
- **agent 时代的特殊性**:AI 把"改主意的成本"降低了(重构便宜了),但架构级的返工(数据模型/接口契约)依然贵——该审慎的还是审慎;真正变便宜的是框架层的试错,可以激进些。
- **及格线 / 加分 / 红旗**:及格=两类划分+不定清单;加分=AI 给候选不拍板的分工;红旗=全听 AI 的推荐(它没有你的约束上下文)或者反过来完全不咨询。

**Q10.10【原理·L3·平台】外部依赖(数据库/第三方 API/文件系统)进 agent 的 loop,各自要做什么改造?**

参考思路:
- **数据库**:状态可见(migration 状态、种子数据可查);agent 能查不能乱写——只读副本+写操作走审批通道;schema 变更是高危动作要人审。
- **第三方 API**:失败语义结构化(4xx/5xx/限流各意味着什么、该重试还是该改道)——agent 最怕把"对方挂了"当"我的代码错了"修半天;超时与退避封装在工具层(ch03 Q3.26)。
- **文件系统**:路径约定与工作区边界(防越界读写);哪些目录是 agent 可写区(沙箱策略的输入,ch08)。
- **IPC/宿主能力**:显式契约(能调什么、返回什么、权限边界)。
- **总原则**:把"世界"对 agent 做成一个可观察、可验证、可安全操作的面——每个外部依赖都过一遍"它失败了 agent 会怎么误解"的检查。
- **及格线 / 加分 / 红旗**:及格=四个各一条;加分="失败语义"这个共同主线;红旗=直接把 root 凭证塞给 agent 让它"自己探索"。

**Q10.11【原理·L4·管理】微软 2026-07 公开了内部 Claude Code 与 Copilot CLI 早期落地的实证研究(arXiv)。这类企业内部研究对落地团队的价值是什么?你怎么用好"别家的实证"?**

参考思路:
- **价值**:真实企业的采用数据(谁在用、用在什么任务、效率变化、失败模式)比厂商白皮书可信——它有失败样本;落地团队可以直接对标它的"成功条件"(什么类型的团队/任务收益最大)来设计自己的试点。
- **用好别家实证的三问**:样本像我们吗(规模/领域/工程成熟度)?指标是什么(自评还是客观)?时间点用的哪代模型(半年的代差足以反转结论)?
- **对冲**:任何外部研究只提供假设,自己的 baseline 才提供答案——正确用法是"用它的结论设计试点,用自己的数据做决策",而不是直接引用当依据。
- **及格线 / 加分 / 红旗**:及格=三问;加分="假设来源 vs 决策依据"的分工;红旗=拿别家研究当自己汇报的论据。

---

## 10.3 品味与判断题

**Q10.12【品味·L4·管理】新项目立项:A)先写完整 spec 套件再开工;B)vibe 出 MVP 再补文档。你的裁决条件?**

参考思路:
- **裁决变量**:返工成本(生产级 vs 探索性)、需求确定度(用户验证过吗)、团队对"砍什么"的共识。三个都低→B 合理;任一高→至少要"一页纸级"的收敛。
- **中间态(多数项目的正确位置)**:轻量收敛(目标/非目标/验收各一行)+测试驱动的最小闭环——不需要完整套件,但不能没有"这一版只回答什么";MVP 可以 vibe,转正(接用户)前必须补验收。
- **两个警惕**:spec 套件沦为仪式(没人维护的文档比没有更糟——它带着权威口吻误导);vibe MVP 的隐性成本是把"该砍什么"的学费推迟到生产期付(利息很高)。
- **及格线 / 加分 / 红旗**:及格=裁决变量+中间态;加分=两个警惕的具体案例;红旗=绝对站队 A 或 B。

**Q10.13【品味·L3·架构】"验收标准前置"为什么在 agent 时代从好习惯变成刚需?**

参考思路:
- **机制**:AI 会用"看起来差不多"糊过模糊验收(它没有羞耻心,只有目标函数);验收可执行化(测试/断言/截图对比)是 agent 自我纠错的前提——没有验收信号,每个产出都要人肉判定,agent 的并行能力全废在等人。
- **一个反直觉的副产品**:验收写不出来说明需求还没收敛——写验收的过程本身就是最后的需求收敛,Q10.1 的第四类决策在这里落地;所以"验收难写"不是麻烦是信号。
- **分层**:可执行验收(给 agent 闭环)+人审点(给风险控制)——验收自动化不是取消人审,是把人审从"每个产出"变成"每个风险等级"。
- **及格线 / 加分 / 红旗**:及格=自我纠错前提这个机制;加分="写不出=没收敛"的信号观;红旗=验收前置当口号,实际还是"做完人看着办"。

**Q10.14【品味·L4·管理】"反脆弱"落到 KPI 上怎么度量?给三个指标。**

参考思路:
- **同类错误复发率**:按 taxonomy 分类的重复故障趋势——降了说明失败在变成免疫力;
- **失败→规则的转化率与时长**:失败发生后多久变成规则/verifier(当天=好,季度末=没有反脆弱);转化率本身(多少失败被固化,多少只进了周报);
- **人工干预密度**:每千次 agent 操作的人肉介入次数趋势——这是 on the loop 的量化(ch10.7);
- **反对的指标**:单纯"agent 成功率"——它不反映系统在学习(成功率可以靠砍任务难度刷高)。
- **及格线 / 加分 / 红旗**:及格=三指标;加分=对"成功率"的反驳;红旗=用成功率/采用率当反脆弱的证据。

**Q10.15【品味·L3·管理】2026-06 GitHub Copilot 从订阅制全面转向用量计费引发用户出走潮("What a joke"),而另一边按席位订阅的工具活得不错。计费模式怎么影响 agent 工具的采用与行为?你给团队选型时怎么权衡?**

参考思路:
- **计费塑造行为**:用量计费把"每次调用都是钱"显性化——用户行为趋向保守(攒任务、砍探索),重度用户账单不可预测(焦虑),轻度用户觉得被宰;订阅制给确定性但厂方承担重尾风险,于是有额度窗口(5 小时/周上限)这种混合形态。没有完美的,只有谁承担方差的问题。
- **对采用的影响**:可预测性对企业管理是刚需——预算失控比"单价贵"更致命;所以企业偏好订阅+上限,个人/小团队偏好 BYOK 按量(可控且便宜)。
- **选型权衡**:预测你的用量分布(重尾部:少数重度用户吃掉大部分用量——为他们买高档订阅,其余人按量);合同里盯"计费口径变更"条款(2026-06 的出走潮就是单方面改口径的信任成本);BYOK 路线作为价格风险的对冲。
- **及格线 / 加分 / 红旗**:及格=计费塑造行为+两类用户偏好;加分=重尾分布的采购策略;红旗=只比月费单价。

---

## 10.4 开放性问题

**Q10.16【开放·L5·管理】如果 agent 写的代码到 80%,软件工程的组织形态(角色/评审/晋升/所有权)会怎么重组?**

讨论框架:
- **所有权**:从代码所有权转向意图与验证所有权——拥有"需求定义+验收标准"的人实际控制产出质量;代码 review 转向 spec 审查+产出抽检。
- **晋升标准**:从产出量(写了多少)转向判断力与系统设计(定义对了多少、验证器建了多少)——度量体系要跟着换,否则 Goodhart。
- **新角色**:控制面工程师(维护规则/记忆/验证器资产)、验证工程师(verifier 库的建设与审计)。
- **风险**:初级工程师的成长路径断裂——没有"写代码练手"的阶段,怎么积累判断力?可能的替代:让新人从验证器和失败分析入手(反而更接近责任核心)。

**Q10.17【开放·L4·管理】随机对照实验(AI 让资深开发慢 19%)与自评数据(快 1.4–2 倍)并存,你在自家组织怎么建立可信的效能度量?**

讨论框架:
- **度量分层**:客观交付指标(PR 周期/变更失败率/返工率)优先于自评;自评只用于发现"体感与实际的差距"本身(那个 RCT 里感知与现实差 40 个点,是最有管理价值的数字)。
- **任务分类**:新功能/维护/迁移的增益不同,混着量必然互相稀释——按类分层再看。
- **准实验设计**:RCT 不可行时用对照团队/工具开关 AB;任何"提升 X%"必须能回答"相对什么 baseline、什么任务、谁测的"。
- **防 Goodhart**:指标与考核弱挂钩,与改进强挂钩——一旦挂考核,小 PR 刷量之类的行为立刻出现。

---

## 10.5 工程实战题

**Q10.18【实战·L3·管理】为"内部运维 agent 化"项目做需求收敛:目标是让 agent 处理 60% 常规工单。给一页纸的立项章程。**

参考思路(一页纸的骨架):
- **产品定义**:是什么(带审批的执行建议系统)/不是什么(不是全自动运维);
- **版本目标**:这一版只回答"告警诊断建议+只读查询自动执行有没有人用";
- **非目标**:不做自动扩缩容、不做任何写操作;
- **阶段风险**:先消价值风险(诊断建议的采纳率),契约和治理后置;
- **验收口径**:诊断采纳率>X%、MTTR 变化、误操作=0(写操作结构性不存在,天然为 0);
- **issue 生成规则**:什么问题值得单独讨论(可收敛的不确定性)。
- **及格线 / 加分 / 红旗**:及格=六节齐;加分="误操作=0 靠结构性禁止而非管理"这类设计巧思;红旗=章程写成功能清单。

**Q10.19【实战·L4·架构】为"支付模块重构"设计执行图:12 个任务、3 人+多个 agent。**

参考思路:
- **分层**:契约先行(schema/错误语义冻结,人拍板)→串行阻塞项(数据库迁移——顺序依赖)→并行切片(按文件所有权分,可交给 agent 并行)→verifier 挂载点(每片的机械验证)→checkpoint(契约变更点必须停下人审)→handoff(每片完成产出交接单)。
- **人机分工**:人定契约与验收;agent 执行片;reviewer agent+人双审;合并按依赖顺序串行。
- **失败路径**:回滚点(每片独立可退)、阻塞升级(卡住超时叫人)。
- **这题考的是**:能否把"任务清单"升级成"带依赖、验证、检查点的执行图"——多数人只会列 todo。
- **及格线 / 加分 / 红旗**:及格=分层+人机分工;加分=契约先行与检查点的理由;红旗=12 个任务平铺给 3 个人+agent"一起做"。

**Q10.20【实战·L4·管理】agent 平台月成本 $40k,要求 3 个月降到 $20k 且质量不降。给方案。**

参考思路:
- **先分账**:按团队/任务类型/模型/缓存命中率切——找到钱真正去了哪(常见:缓存破坏 30%+、无效长会话 20%+、模型不分级 20%+)。
- **无损优化(预期 30–50%)**:缓存修复(tools 冻结/前缀稳定,ch02)、工具输出过滤、压缩阈值调优、低价值后台任务清理——行为不变只省钱,先做;
- **结构优化(10–30%)**:模型路由(铺量降档)、subagent 摘要规格化、异步任务走批处理折扣;
- **质量护栏**:每步用内部 eval 回归,质量降即回滚——降本不降质要能证明,不是口号;
- **汇报口径**:成本/任务 × 质量 双曲线趋势图。
- **及格线 / 加分 / 红旗**:及格=分账+两层优化;加分=eval 回归护栏与双曲线汇报;红旗=直接换便宜模型当方案。

---

## 10.6 场景排错题

**Q10.21【排错·L3·管理】团队买了最贵的工具订阅,6 个月后交付速度没变化,开始说"AI 不行"。诊断?**

排查链路:
- **①度量本身**:有没有 baseline 和客观指标,还是纯体感——"没变化"是怎么测出来的?多数团队答不上来,先补度量;
- **②瓶颈定位(三类卡点)**:底层闭环不足(改来改去总返工——缺验证器)/交互停留在问答(没用上 agent 的执行闭环)/规则没固化(每个任务重新教一遍——没有记忆资产);
- **③资产盘点**:AGENTS.md/spec/verifier 存在吗,还是每个任务裸奔——工具到位纪律缺位是标准结局;
- **④任务分布**:是不是把最不适合的任务(强上下文依赖的遗留维护)硬塞给了 agent。
- **结论的呈现**:通常不是"AI 不行"是"地基没打"——但要拿度量说话,不然变成立场之争。
- **及格线 / 加分 / 红旗**:及格=三类卡点;加分=先补度量的诊断顺序;红旗=顺着情绪换工具或砍预算。

**Q10.22【排错·L4·架构】agent 生成的 PR 全部过了 CI,合并后一个月内引发 3 次生产事故,都是"边界条件没考虑"。系统性修复?**

排查链路:
- **先归因分类**:测试盲区(CI 测不到的边界——验证器上限问题)/spec 缺失(验收没写边界条件)/agent 行为(为过测试走捷径——reward hacking 的生产版);
- **修复分层**:每个事故→一个回归测试+一条规则或 checklist 项(失败固化的标准动作);验收模板强制加"边界条件清单"节(空态/并发/超时/重入/降级——agent 最容易漏的就这几类);对 agent 产出加"边界自证"要求(列出处理了哪些边界+证据);
- **升级机制**:同类第三次出现必须升级为 verifier(自动化边界测试)——人肉 checklist 不能是终态;
- **诚实面对**:CI 绿≠正确(ch07 验证器上限)——这个事故模式不会归零,目标是从"每次都惊吓"到"每类只惊吓一次"。
- **及格线 / 加分 / 红旗**:及格=归因+失败固化;加分=边界自证与升级为 verifier;红旗=加更多人审每个 PR(不规模化,且人看边界条件的能力恰恰最弱)。
