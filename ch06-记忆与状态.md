# ch06 · 记忆与状态管理

> 覆盖:记忆文件体系(CLAUDE.md/AGENTS.md/rules/auto memory)、memory tool、什么该写回仓库什么该忘、长任务的接力三件套(进度日志/交接文档/仓库记忆)、结构化笔记 vs 压缩、会话持久化、记忆的防腐治理、记忆与检索的关系。
> 案例基线:2026-08。机制题不挂时间;带日期案例以标注为准。

---

## 6.1 八股题(基础概念)

**Q6.1【八股·L1·用户】CLAUDE.md 的层级加载与懒加载机制是什么?这个设计在优化什么?**

- **人话版**:四层从上到下拼接——企业托管(下发、不可排除)、用户全局、项目根(入 git)、CLAUDE.local(个人);子目录的 CLAUDE.md 平时不加载,agent 碰到那个目录的文件时才注入。优化的是"记忆的总量与相关性":全局的薄、局部的按需。
- **展开**:
  - **拼接而非覆盖**:四层内容都生效,冲突时靠优先级和内容本身的清晰度——这也是为什么根文件要克制(它每轮常驻,每行都是成本)。
  - **懒加载的机制**:子目录 CLAUDE.md 在 agent 读到该目录文件时注入——前端目录的规范只在做前端任务时进上下文。配合 `.claude/rules/*.md` 的 paths 作用域(规则文件声明"只在这些路径生效"),实现记忆的路径作用域化。
  - **配套细节**:`@path` 语法导入其他文件(递归上限 4 跳,防循环);`/init` 生成初始记忆并可吸收其他工具的规则文件;入口文件官方建议 <200 行。
  - **设计的一般原则**:记忆入口要小、按需展开——和检索的"标识符+按需拉取"是同一个思想:常驻的是索引,内容用的时候再取。
- **及格线**:四层+懒加载两个机制。
- **加分**:从"总量与相关性"角度解释设计意图;与其他工具 rules 作用域的对照。
- **红旗**:以为子目录文件会覆盖根文件;不知道懒加载导致"规则没生效"的排查方向。

**Q6.2【八股·L1·用户】AGENTS.md 是什么?它为什么能成为跨厂商的收敛标准?**

- **人话版**:仓库根目录一份纯 Markdown 的"给 agent 的 README"——项目概述、构建测试命令、代码风格、禁区。没有强制 schema,几十家工具(Codex、Gemini CLI、Cursor、Copilot、Devin 等)都支持。收敛的原因很朴素:一份记忆维护 N 个工具,比 N 份各自维护便宜。
- **展开**:
  - **形态**:纯 Markdown、无 schema、monorepo 里就近生效(根一份、各包一份,越近优先);常见节:项目一句话/构建与测试命令/代码风格/测试说明/PR 规范。
  - **收敛逻辑**:同一个开发者用多个 agent 工具是常态,每个工具一套规则文件的维护成本是 N 倍且必然漂移;一份标准文件+各工具的导入机制(Claude Code 官方建议 @AGENTS.md 导入或软链),把 N 份变 1 份。由中立的基金会维护(不属任何厂商)是它被广泛接受的政治前提。
  - **与 CLAUDE.md 的关系**:不冲突——把 AGENTS.md 当单一事实源,工具各自的增强(作用域规则、skills)通过引用挂上去;两套并存还各自漂移是反模式(ch11 有收敛方案)。
- **及格线**:是什么+维护成本的收敛逻辑。
- **加分**:中立治理的意义;单事实源+引用的落地方式。
- **红旗**:把它当某家厂商的私有格式;不知道 monorepo 就近生效。

**Q6.3【八股·L2·平台】Anthropic 的 memory tool 机制是什么?它和 CLAUDE.md 这类人写记忆是什么关系?**

- **人话版**:memory tool 给模型一个文件式的记忆目录(命令式的 view/create/改/删),启用后系统自动注入"开始前先看记忆目录"的指令——模型自己维护跨会话的工作记忆。和 CLAUDE.md 的分工:一个治理"项目是什么"(人写、项目级、常驻),一个治理"我做到哪了"(模型写、跨会话、按需读)。
- **展开**:
  - **机制细节**:客户端本地存储(/memories 目录);view 有长度截断;与 context editing 配合时,清理旧工具结果前会提醒模型"先把重要的写进记忆"——先抢救再清理,两个功能的组合点是设计精华(官方数据:组合比基线 +39%)。
  - **两种记忆的属性差异**:人写记忆的权威性来自意图(它代表团队的决定,模型不该覆盖);模型写记忆的价值来自过程连续性(它记得上次的尝试和教训)。分层共存——宪法(人写)+ 日志(模型写)。
  - **治理问题(必须主动提)**:模型写的记忆谁审?错误结论被写进记忆会污染后续所有会话(semantic drift 的入口)。要有记忆的清理机制和抽检——这是 memory tool 落地时最容易缺的一环。
- **及格线**:机制+两者分工。
- **加分**:与 context editing 的组合设计;模型记忆的治理问题。
- **红旗**:把 memory tool 等同于 CLAUDE.md;没想过模型记忆也会腐烂。

**Q6.4【八股·L2·用户】auto memory(MEMORY.md)这类自动记忆的注入限制是什么?为什么要限制?**

- **人话版**:自动记忆持久化各类笔记,但每会话只注入 MEMORY.md 的前 200 行(或 25KB)——入口被硬性限死,其余内容靠 agent 主动去读。限制的原因:常驻上下文是每轮付费的,无限注入的记忆会挤占任务空间。
- **展开**:
  - **机制**:入口文件相当于记忆的"目录页"——只放索引和最高频的条目;细节内容放子文件,需要时 view。
  - **为什么必须硬限制**:记忆是 append 倾向最强的上下文(每次会话都想留点什么),没有硬顶它会无限膨胀;200 行的约束反过来逼你做信息架构——什么配进目录、什么归档。
  - **配套习惯**:定期园艺(见 Q6.12)——把过期的移出入口、合并重复的、把"不再适用"的标记掉;入口的每一行同样过"删掉会不会犯错"的测试(ch02 Q2.27)。
- **及格线**:注入上限+为什么要限。
- **加分**:目录页/子文件的信息架构;园艺习惯。
- **红旗**:以为记忆多多益善;不知道注入有上限导致"为什么 agent 不知道这事"的困惑。

**Q6.5【八股·L2·架构】"什么必须写回仓库、什么必须及时忘掉"——怎么判断?append-only 的记忆会得什么病?**

- **人话版**:该写回的是决策及理由、契约(schema/API/命令)、验证标准、会被后续任务依赖的结论;该忘掉的是过程噪声——检索废料、失败尝试的细节、过期快照。append-only 的病叫 semantic drift:旧结论因为出现次数多被当成现行标准。
- **展开**:
  - **判断标准**:这个信息会被未来的任务依赖吗(会→写回,不会→忘);它有时效吗(有→带过期机制,没有→可以长期)。决策的理由比决策本身重要——"我们为什么不用 X"能防止半年后有人(或 agent)好心把 X 加回来。
  - **semantic drift 的机制**:只增不减的记忆里,旧写法出现 100 次、新规范出现 2 次——模型的频率直觉会把旧的当主流;被复制引用多次的过期文档还会获得"权威错觉"。这与 ch02 的"过期上下文比没有更危险"同源,但发生在跨会话尺度。
  - **防护**:归档物打 archived/superseded 标;记忆条目带"适用条件/复审时间";定期清理过期规则;规则与代码冲突时以代码为准的默认约定。
- **及格线**:写回/忘掉的分界+drift 的机制。
- **加分**:决策理由的价值;防护三件套。
- **红旗**:所有会话内容都存下来"以后可能有用";不知道记忆也会成为污染源。

**Q6.6【八股·L1·用户】长任务"不断线"的三件套——progress log / handoff artifact / repo memory——各是什么、给谁看?**

- **人话版**:progress log 是任务推进的时序记录(做了什么/结论/下一步);handoff artifact 是交接文档,新会话只读它就能接手;repo memory 是仓库内长期沉淀的决策与契约。分别给人看、给下一个 agent 看、给所有未来任务看。
- **展开**:
  - **progress log**:追加式、时序化,价值在过程可回放(出事时能定位"从哪一步开始歪的");放仓库不放对话——对话会被压缩,文件不会。
  - **handoff artifact**:与 log 的区别是"蒸馏"——只含状态与决策(当前状态/关键决策及理由/失败尝试与原因/未决问题/环境入口),不含过程噪声。检验标准:新会话只喂这份文档能否 10 分钟接手。
  - **repo memory**:跨越单个任务的沉淀(架构决策记录、契约、checklist)——log 和 handoff 随任务结束归档,repo memory 留下来变成组织资产。
  - **三者与压缩的关系**:它们是 compaction 的结构性替代——压缩是有损滚动摘要,这三样是显式外部状态,不受 context rot 影响、每轮可重新注入(见 Q6.8)。
- **及格线**:三者定义+读者。
- **加分**:handoff 与 log 的"蒸馏"区别;"文件不怕压缩"的属性论证。
- **红旗**:三件混做一个大文档;handoff 里贴大段代码(该给路径)。

**Q6.7【八股·L1·平台】会话持久化(--continue/--resume)和 checkpoint/rewind 的产品化形态?它们各自能恢复什么、不能恢复什么?**

- **人话版**:--continue/--resume 恢复对话历史;checkpoint/rewind 在每次编辑前快照,能回滚代码/对话/环境三态。都不能恢复的是:bash 造成的外部副作用(装的包、发的请求)和已被 compaction 摘要掉的细节。
- **展开**:
  - **resume 的边界**:恢复的是"对话文本",不是"工作状态"——一个历经多次压缩的会话,resume 回来的早期内容可能只是摘要;对话很长时,新会话+读进度文件常比 resume 更干净(不带历史包袱)。
  - **checkpoint 的边界**:覆盖编辑类工具的改动,bash 的外部影响不在快照内;本地保留约 30 天;官方明确"不是 git 替代"。
  - **工程结论**:长任务不要赌"会话可恢复",要主动在里程碑写外置状态(git commit+进度文件)——会话是易失的,仓库是持久的。这是记忆设计的总纲。
- **及格线**:各自能/不能恢复什么。
- **加分**:"从状态重建优于恢复会话"的立场及理由。
- **红旗**:把 resume 当长任务的可靠性方案。

---

## 6.2 原理深挖

**Q6.8【原理·L3·模型】为什么"结构化笔记"能维持数小时级长任务的一致性,而 compaction 做不到?**

- **人话版**:笔记是显式的外部状态——当前目标/已试过什么/下一步写在固定文件里,每次新会话或压缩后重读,以极小 token 恢复全部决策上下文;compaction 是有损滚动摘要,信息每过一次就衰减一次,还不受 context rot 影响(笔记是重新注入的,不是躺在历史里被稀释)。
- **展开**:
  - **机制差异**:compaction 的摘要在上下文里"随波逐流"——继续变长、继续被稀释、再次压缩再衰减;笔记在上下文外,每轮以完整保真度重新进入,不存在衰减链。信息论的说法:一个是有损信道里的累积误差,一个是无损存储+按需读取。
  - **选型口径**(Anthropic 的三分法):会话内拉锯用 compaction;里程碑式长任务用结构化笔记;并行深挖用 subagent——工具与任务形态匹配。
  - **实证参照**:公开的长任务实验(数天级)都收敛到同一模式——JSON 进度文件+固定开机序列(读 git log→读进度→领任务→跑基线测试);连游戏化的长程任务(Pokémon 这种)也是靠进度笔记维持数十小时一致性的。
  - **写什么**:不是流水账,是决策状态——目标与验收、已完成与结论、已否决及原因、下一步、阻塞点。这正好是 handoff 的 schema(Q6.10)。
- **及格线**:显式外部状态 vs 有损滚动摘要的机制对比。
- **加分**:三分法选型;笔记的"写什么"清单。
- **红旗**:认为窗口够大就不需要笔记;把笔记写成流水账。

**Q6.9【原理·L3·架构】为什么给模型维护的状态文件用 JSON 而不是 Markdown?"给模型读的用 Markdown、给模型改的用结构化格式"这个原则的依据是什么?**

- **人话版**:Markdown 对模型太"好改"——agent 会顺手重写、美化、重排,状态的机器可读性和 diff 可审计性就没了。JSON 有 schema 约束,增删字段可校验、可 diff、可被工具消费。所以读的文档用 Markdown(表达自由),改的状态用结构化格式(变更可控)。
- **展开**:
  - **Markdown 状态的翻车模式**:agent 在"整理"任务清单时把未完成项也"整理"没了;重排顺序导致 diff 里全是格式噪声,人没法审;自由文本里塞歧义表述("这个差不多完成了")。
  - **结构化的收益**:字段级校验(缺 required 字段直接报错,状态不可能"悄悄"不完整);diff 干净(每个变更对应明确的字段操作);可被工具链消费(CI 检查"是否有未完成任务就发布了")。
  - **这个原则的推广**:todo 列表在 Claude Code 里是工具操作(TodoWrite)而不是让模型自由写文本,同一家思想——状态变更走确定性接口,不走自由生成。任务清单、进度文件、功能清单,凡是"agent 要改的状态"都适用。
- **及格线**:翻车模式+结构化收益。
- **加分**:TodoWrite 的同构设计;原则向 CI 消费的推广。
- **红旗**:所有 agent 可写文件都用 Markdown;或者反过来把只读文档也搞成 JSON(降低可读性没收益)。

**Q6.10【原理·L4·架构】设计 handoff artifact 的信息架构:该含什么、绝不含什么?**

- **人话版**:含六样——任务目标与验收标准(原文引用)、当前状态(完成/进行/阻塞)、关键决策及理由、环境与入口(分支/启动命令/依赖坑)、失败尝试与原因、未决问题;绝不含四样——原始检索废料、大段代码(给路径不给粘贴)、会过期的快照(给获取方式)、无关项目背景。检验:新会话只喂它,10 分钟内能否接手。
- **展开**:
  - **为什么"失败尝试与原因"最重要**:没有它,下一个 agent 大概率重蹈覆辙——重复失败尝试是长任务最大的隐藏成本;有了它,失败尝试变成了资产。
  - **为什么给路径不给粘贴**:粘贴的代码会过期(文件又改了),路径永远指向当前事实——这和记忆文件"写不可推断的事实"同一逻辑:不复制可重新获取的东西。
  - **为什么验收标准要原文引用**:转述会丢约束的语气和细节("尽快"vs"必须在本周内,否则回滚")——验收歧义是接力任务的经典死因。
  - **更新时机**:每会话结束前、每个里程碑后、发现阻塞立即——handoff 是保鲜品,过期交接文档比没有更危险(带着权威口吻的旧状态)。
- **及格线**:六含四不含给个七七八八+检验标准。
- **加分**:失败尝试的价值论证;"给路径不给粘贴"的原理。
- **红旗**:把对话日志复制一段当 handoff。

**Q6.11【原理·L3·平台】Devin 的 playbook/learnings 这类"经验沉淀"机制解决什么组织问题?**

- **人话版**:playbook 把"某人成功过一次的流程"外置成可复用可分享的模板(固定段落:流程/规格/建议/禁止动作/需要用户提供什么);learnings 从历史会话自动沉淀经验。解决的是:多 agent 多会话没有共享记忆,知识不进文件就随会话蒸发。
- **展开**:
  - **组织本质**:同一个团队里,每个人和 agent 摸索出的有效做法,默认是孤岛;沉淀机制把个人成功变成团队可复制——和传统团队的 runbook/sop 完全同构,只是"执行者"变成了 agent,模板的可执行性要求更高(段落要能被 agent 直接遵守,不是给人看的散文)。
  - **"禁止动作"段的设计**:把负面约束显式化——agent 没有常识默认值,"别动 X"必须说出来;这也呼应 ch02 的原则:高频约束要外置成结构化载体,不活在人的口头提醒里。
  - **通用化**:任何团队都能用轻量版实现——把高频任务的正确姿势写进 skill/模板文件,版本化管理,失败案例反向更新它;关键动作是"固化",形式不限。
- **及格线**:外置共享知识这个本质。
- **加分**:禁止动作段的设计逻辑;轻量版自实现。
- **红旗**:认为这是某产品的 proprietary 功能,和自建无关。

**Q6.12【原理·L4·架构】"记住什么、忘掉什么"的治理机制怎么落地?给一个记忆生命周期策略。**

参考框架(策略示例):
- **写入分级**:决策/契约/坑→repo(评审后进);任务中间态→会话内/todo;个人偏好→用户层。每类有明确的 owner 和评审方式——写入库的记忆该走 PR。
- **过期机制**:每条规则带"适用条件/复审时间"元数据;归档打标(archived/superseded);定期 doc gardening(清理 job 进 backlog,像清技术债一样清记忆债)。
- **冲突仲裁**:source of truth 声明写进根记忆(代码>AGENTS.md>wiki);记忆与代码冲突默认以代码为准——这条默认值能消灭一大类混乱。
- **度量**:记忆命中率(被引用的规则占比——长期 0 命中的规则是删除候选)、过期规则引发的故障数、入口文件的行数趋势。
- **及格线 / 加分 / 红旗**:及格=写入分级+过期机制;加分=度量指标;红旗=只有写入没有清理(所有记忆系统的默认结局是腐烂)。

---

## 6.3 品味与判断题

**Q6.13【品味·L3·架构】记忆放文件(仓库内)还是向量库/数据库?给判断框架。**

参考思路:
- **文件(默认)**:人可读可审、git 版本化、agent 天然会 grep/read、零基础设施——决策与规则这类"必须确定性命中"的内容,文件是唯一正确形态(规则进向量库=概率检索赌规则命中)。
- **向量库(按需)**:海量非结构化知识(千份文档)、语义检索为主的场景——检索是概率性的,适合"找不到也没事,找到更好"的内容。
- **数据库**:多用户并发写、跨服务共享、需要权限模型——组织级记忆平台。
- **2026 年的新证据**:有实验显示 8B 模型+向量库记忆(30% 准确率)胜过 235B 无记忆(14%)——组织好的记忆比更大的模型值钱;但同批研究也显示持久 agent 的查询里约一半与历史语义相似/精确重复,混合检索(BM25+向量)比纯语义高 7 个点——记忆检索不是纯语义问题。
- **及格线 / 加分 / 红旗**:及格=三类载体各配适用内容;加分=用新证据支撑;红旗=规则进向量库,或千份文档全塞 prompt。

**Q6.14【品味·L4·管理】团队的记忆资产(specs/rules/skills)谁维护?怎么防腐烂?**

参考思路:
- **所有权跟代码走**:记忆文件变更走 PR 评审——"谁改代码谁改配套规则";没有 owner 的记忆必然腐烂。
- **防腐四件**:规则带触发条件与复审期;doc gardening 进 backlog(每季度清一次);规则失效引发的故障回写为规则更新(失败驱动维护);记忆健康度度量(引用率/过期率)。
- **反对的做法**:AI 自动生成无人审核的规则堆积——semantic drift 的温床;一次性的"文档冲刺"(写完没人管,一年后全过期)。
- **组织视角**:记忆资产是复利资产——维护成本前置,收益是每个后续任务都省;多数团队的问题是只看到维护成本没算复利。
- **及格线 / 加分 / 红旗**:及格=PR 化+定期清理;加分=失败驱动维护或复利视角;红旗=交给"有空的人"或者 AI 自动堆。

**Q6.15【品味·L3·用户】什么时候 --resume 续会话,什么时候新会话+读 handoff?**

参考思路:
- **resume 适合**:短中断(几分钟到几小时)、上下文仍然高信噪比、任务连续没换。
- **新会话+handoff 适合**:跨天/跨任务;上次会话已经污染(大量失败尝试、跑偏的探索——带着这些历史反而是负资产);任务到了里程碑(自然的重启点)。
- **主动优于被动**:长任务应该在里程碑**主动**写 handoff 并计划性地开新会话,而不是等上下文满了被动压缩——前者你控制保留什么,后者赌摘要的质量。
- **判据一句话**:要的是"历史"还是"状态"?要历史(接着聊)resume;要状态(接着干)新会话+读文件。
- **及格线 / 加分 / 红旗**:及格=两类场景;加分="主动交接优于被动压缩";红旗=一律 resume 到天荒地老。

**Q6.16【品味·L4·架构】2026 年有两条新闻线:一边是个人知识库"去检索化"(Karpathy 2026-04:LLM 当编译器维护约百篇互联 markdown wiki,明确说个人规模不需要向量库);一边是记忆研究的反向结论(带向量库记忆的 8B 模型胜过无记忆的 235B)。你怎么调和?**

参考思路:
- **两者不矛盾,是规模的两个区间**:百篇量级,互联 wiki+全读/顺链跳转就够——维护索引是纯开销;亿级 token 量级,检索是刚需——问题是"记忆的组织形态"不是"要不要记忆"。
- **共同信号**:记忆的价值被系统性低估了——"组织好的上下文>更大的模型"这个不等式在两边都成立;预算该从堆模型分一部分给记忆建设。
- **调和后的行动**:先问规模再选形态——百篇:wiki+链接;千篇:BM25/关键词够用(实验显示混合检索优于纯语义);万篇以上:分层(策展入口+检索底座,Atomic 的"策展层活在检索底座之上")。
- **及格线 / 加分 / 红旗**:及格=规模分界;加分="组织好的上下文>更大的模型"的不等式;红旗=用一条新闻否定另一条(两边的极端粉丝都存在)。

---

## 6.4 开放性问题

**Q6.17【开放·L5·模型】模型原生支持 memory tool 之后,人写的记忆文件会膨胀还是萎缩?**

讨论框架:
- **萎缩派论据**:模型自己记的过程细节(试过什么、踩过什么坑)不需要人写了——人写记忆里相当部分是这类"本可以自动沉淀"的内容。
- **膨胀派论据**:正因为模型能消费更多结构化记忆,人会写更多"意图类"内容(边界、偏好、否决理由)——这类永远来自人;而且信任模型自写记忆的组织,会用"宪法式"人写记忆来锚定不可漂移的部分。
- **关键变量**:模型写记忆的可审计性(错误结论进记忆的污染链)——审计成本高则人写记忆承担更多;治理工具成熟则模型记忆扩张。
- **落点**:总量都涨,但结构分化——人写"意图与边界",模型写"过程与教训",两者的比例是组织信任度的函数。

**Q6.18【开放·L4·架构】为开源项目设计"贡献者的记忆入口":新 contributor 的 agent 第一次进仓库,该看到什么?**

讨论框架:
- **最小充分集**:项目一句话(它是什么/不是什么)、构建测试命令(可直接复制执行)、代码风格的关键约定、禁区(别动的目录/别提的 PR 类型)、指向深层文档的索引。
- **分层加载**:根文件薄、子目录就近、路径作用域——一次性全塞等于全不塞。
- **冷启动检验**:让没见过的 agent 完成一个 hello task(加个小功能),一次通过率就是记忆入口的质量指标——可测量就可迭代。
- **反模式**:把 CONTRIBUTING.md 原样当 agent 记忆(写给人的流程性内容,agent 要的是可执行事实);入口文件里塞十页架构哲学。

---

## 6.5 工程实战题

**Q6.19【实战·L3·平台】为 monorepo(前端/后端/共享包)设计记忆文件布局。**

参考思路:
- **布局**:根 AGENTS.md(全局命令/约定/索引,薄)→ 各包自己的 AGENTS.md 或 .claude/rules(paths 作用域)→ 高频深目录的子目录 CLAUDE.md(懒加载)。
- **配套**:CI 校验记忆里的命令仍然有效(命令失效是记忆腐烂的头号形态——构建命令改了没人更新记忆文件);新包用 /init 引导;入口总量控制(根 <200 行)。
- **内容分配原则**:全局的只放"跨包通用"(发布流程/monorepo 约定);包级的放"本包特有"(测试命令/坑)——放错层级的代价是要么冗余要么漏。
- **及格线 / 加分 / 红旗**:及格=三层布局+命令校验;加分=内容分配原则;红旗=一个巨型根文件装所有包的知识。

**Q6.20【实战·L4·架构】设计"跨夜长任务"的记忆方案:任务 8 小时,会话每约 1 小时被上下文上限打断。**

参考思路:
- **初始化会话**(人监督):建进度文件(JSON)+计划+验收标准;确认端到端测试可跑——基线是接力的锚点。
- **每个会话的开机序列**(固定、写进配置):读 git log→读进度文件→选最高优先级未完成项→跑端到端测试确认基线→开工。
- **里程碑纪律**:每个子任务完成即 commit(checkpoint);会话结束前更新 handoff(阻塞点/下一步/给下一班的忠告)。
- **人工检查点**:早晨的第一件事是审夜间产出——不是不信任,是验收标准的执行;异常率超阈值应该有自动熔断(夜间任务全停)。
- **参照物**:Anthropic 的轮班工程师方案(ch04 Q4.8)就是这个模式——已被数天级任务验证。
- **及格线 / 加分 / 红旗**:及格=初始化+开机序列+commit;加分=早晨人审与夜间熔断;红旗=指望一个会话扛 8 小时(compaction 会把早期约束磨没)。

**Q6.21【实战·L3·平台】实现"记忆园艺"自动化:每周运行的 job,清理过期规则、标记冲突、汇报健康度。**

参考思路:
- **元数据前提**:规则要带创建时间/触发条件/最后命中时间——没有元数据的园艺只能靠语义判断(贵且不稳)。
- **job 逻辑**:超期未命中→删除候选(输出 PR 供人审,不自动删——记忆变更要留人审环节);与代码冲突检测(规则提到的路径/命令还存在吗);新增建议(从近期会话的高频纠正中提取候选,人工确认入库)。
- **报告指标**:总条数/命中率/过期率/入口行数趋势——趋势比绝对值有用(入口行数单调涨说明有人在当人形 append)。
- **及格线 / 加分 / 红旗**:及格=元数据+删除候选 PR;加分=从会话提取新增建议;红旗=自动删除不过人审。

---

## 6.6 场景排错题

**Q6.22【排错·L2·用户】agent 每次新会话都重新犯上周已纠正过的错误,你明明把纠正写进了 CLAUDE.md。排查?**

排查链路:
- **①加载了吗**:层级对不对(放错目录)、入口超行数被截断、懒加载没触发(规则写在子目录但任务没碰那个目录)——先用"让 agent 复述规则"验证注入;
- **②措辞可执行吗**:"保持代码整洁"是愿望,"禁止向 src/legacy 写新代码"是规则——前者无效;
- **③被仓库现实压倒**:旧代码里大量反例,频率直觉让模型跟反例走——这是仓库治理问题,改规则措辞没用,要么清反例要么把规则升级为 lint/hook;
- **④被稀释**:规则周围堆了太多低价值内容——做减法。
- **及格线 / 加分 / 红旗**:及格=①②③链路;加分=区分"规则问题"和"仓库治理问题"这两类根因;红旗=把同一条规则再写一遍加粗。

**Q6.23【排错·L3·平台】跨会话 resume 后 agent 行为异常:重复已完成步骤、丢失早先约束。根因?**

排查链路:
- **根因一**:resume 的会话已历经多次 compaction,早期约束只存在于有损摘要里——看起来"记得",细节已丢;
- **根因二**:关键结论没写进持久层(记忆/任务文件),只活在对话里。
- **修复方向**:长任务不依赖 resume,改"每会话从仓库状态重建上下文"(开机序列);约束落在任务文件/记忆文件(持久层)而不是对话(易失层);检查 auto memory 是否启用、有没有把约束写进去。
- **预防**:把"会话是易失的"当设计公理——所有必须跨会话存活的信息,写文件。
- **及格线 / 加分 / 红旗**:及格=compaction 累积损失;加分=从状态重建的修复;红旗=反复 resume 重试指望"这次记得"。

**Q6.24【排错·L4·架构】团队 wiki、AGENTS.md、代码注释三处对同一约定说法不一,agent 随机采用其一。治理?**

排查与治理:
- **定义 source of truth 顺序并写进根记忆**:如"代码>AGENTS.md>wiki"——先给 agent 一个裁决默认值;
- **一次性清理**:冲突清单以代码为准回写;wiki 该页加"以 repo 为准"指针;三处中只保留 SoT,其余引用;
- **防复发**:新约定只允许写进一处(约定写在 CONTRIBUTING 里的流程里);gardening job 定期做一致性 diff;规则文件的 PR 模板里加"是否与其他文档冲突"检查项。
- **及格线 / 加分 / 红旗**:及格=SoT 顺序+清理;加分=防复发机制;红旗=三处都改成一样但没定 SoT(下次漂移继续)。
