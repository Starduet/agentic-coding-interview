# ch06 · 记忆与状态管理

> 覆盖:记忆文件体系(CLAUDE.md/AGENTS.md/rules/auto memory)、memory tool、什么该写回仓库什么该忘、长任务接力三件套(进度日志/交接文档/仓库记忆)、结构化笔记 vs 压缩、会话持久化、记忆防腐治理,以及**会话接力的三条纪律**(上下文丢失后重新接地/交接先验证再续/决策密集检查点)。
> 案例基线:2026-08。机制题不挂时间;带日期案例以标注为准。
> 答案读法:**加粗句连读就是可背的骨架**;后面的解释帮你理解;「补充弹药」是追问时再掏的细节。

---

## 6.1 八股题(基础概念)

**Q6.1【八股·L1·用户】CLAUDE.md 的层级加载与懒加载机制?这个设计在优化什么?**

- **人话版**:四层从上到下拼接——企业托管(下发、不可排除)、用户全局、项目根(入 git)、CLAUDE.local(个人);子目录的 CLAUDE.md 平时不加载,agent 碰到那个目录的文件时才注入。优化的是记忆的"总量与相关性":全局的薄、局部的按需。
- **主线(照这条线讲)**:
  1. **拼接而非覆盖**——四层内容都生效,冲突靠优先级和内容清晰度解决;这也是根文件必须克制的原因:它每轮常驻,每行都是成本。
  2. **懒加载的机制与价值**——子目录 CLAUDE.md 在 agent 读到该目录文件时注入:前端目录的规范只在做前端任务时进上下文。配合 `.claude/rules/*.md` 的 paths 作用域,实现记忆的路径作用域化——**入口小、按需展开**。
  3. **配套细节**——`@path` 导入其他文件(递归上限 4 跳防循环);`/init` 生成初始记忆并可吸收其他工具的规则文件;入口文件官方建议 <200 行。
  4. **设计的一般原则收尾**——记忆入口要小、按需展开,和检索的"标识符+按需拉取"是同一个思想:**常驻的是索引,内容用的时候再取**——这一句把本章和 ch02/ch03 串起来了。
- **补充弹药(追问再掏)**:
  - **排错含义**:"规则没生效"的第一怀疑就是懒加载没触发——让 agent 复述规则验证注入,一分钟定位。
  - **对照**:Codex 的 AGENTS.md 是根到 cwd 逐级就近生效——两家都做作用域化,粒度不同。
- **及格线**:四层+懒加载两个机制。
- **加分**:从"总量与相关性"解释设计意图;与其他章"按需取用"原则的贯通。
- **红旗**:以为子目录覆盖根文件;不知道注入有上限。

**Q6.2【八股·L1·用户】AGENTS.md 是什么?它为什么能成为跨厂商的收敛标准?**

- **人话版**:仓库根目录一份纯 Markdown 的"给 agent 的 README"——项目概述、构建测试命令、代码风格、禁区。没有强制 schema,几十家工具都支持。收敛原因很朴素:一份记忆维护 N 个工具,比 N 份各自维护便宜。
- **主线(照这条线讲)**:
  1. **形态**——纯 Markdown、无 schema、monorepo 就近生效(根一份、各包一份,越近优先);常见节:项目一句话/构建与测试命令/代码风格/测试说明/PR 规范。
  2. **收敛逻辑(本题核心)**——开发者用多个 agent 工具是常态,每工具一套规则文件的维护成本是 N 倍且必然漂移;一份标准文件+各工具导入机制(Claude Code 官方建议 @AGENTS.md 导入或软链)把 N 份变 1 份。**由中立基金会维护(不属任何厂商)是被广泛接受的政治前提**——这句常被漏但很关键。
  3. **与 CLAUDE.md 的关系**——不冲突:AGENTS.md 当单一事实源,工具各自的增强(作用域规则/skills)通过引用挂上去;两套并存各自漂移是反模式(ch11 Q11.14 给收敛方案)。
- **补充弹药(追问再掏)**:
  - **规模感**:开源项目采用数以万计、OpenAI 主仓里各包就近几十份——已是事实标准不是提案。
  - **内容原则**:写"不可从代码推断的事实"(同 ch02 Q2.27)——AGENTS.md 是 CLAUDE.md 判据的跨厂商版。
- **及格线**:是什么+维护成本的收敛逻辑。
- **加分**:中立治理的意义;单事实源+引用的落地。
- **红旗**:当某厂商私有格式;不知道 monorepo 就近生效。

**Q6.3【八股·L2·平台】Anthropic 的 memory tool 机制?它和 CLAUDE.md 这类人写记忆的关系?**

- **人话版**:memory tool 给模型一个文件式记忆目录(view/建/改/删),启用后自动注入"开始前先看记忆目录"——模型自己维护跨会话的工作记忆。分工:CLAUDE.md 治理"项目是什么"(人写/项目级/常驻),memory tool 治理"我做到哪了"(模型写/跨会话/按需读)。
- **主线(照这条线讲)**:
  1. **机制细节**——客户端本地存储(/memories 目录);view 有长度截断;**与 context editing 的组合点是设计精华**:清理旧工具结果前提醒模型"先把重要的写进记忆"——先抢救再清理(官方数据:组合比基线 +39%)。
  2. **两种记忆的属性差异(本题核心)**——人写记忆的权威性来自**意图**(代表团队决定,模型不该覆盖);模型写记忆的价值来自**过程连续性**(记得上次的尝试和教训)。分层共存:宪法(人写)+日志(模型写)。
  3. **治理问题必须主动提**——模型写的记忆谁审?错误结论进记忆会污染后续所有会话(semantic drift 的入口):要有清理机制和抽检——**这是 memory tool 落地时最容易缺的一环**,提出来就是加分。
- **补充弹药(追问再掏)**:
  - **渐进形态**:auto memory 持久化多类笔记但每会话只注入 MEMORY.md 前 200 行/25KB——入口硬顶的设计(下一题展开)。
  - **组织落地**:模型记忆的审计走 PR 或定期抽检报告——和人写记忆同一条治理线。
- **及格线**:机制+两者分工。
- **加分**:与 context editing 的组合设计;模型记忆的治理问题。
- **红旗**:把 memory tool 等同于 CLAUDE.md;没想过模型记忆会腐烂。

**Q6.4【八股·L2·用户】auto memory(MEMORY.md)这类自动记忆的注入限制是什么?为什么要限制?**

- **人话版**:自动记忆持久化各类笔记,但每会话只注入 MEMORY.md 的前 200 行(或 25KB)——入口被硬性限死,其余靠 agent 主动读。限制的原因:常驻上下文每轮付费,无限注入的记忆会挤占任务空间。
- **主线(照这条线讲)**:
  1. **机制**——入口文件相当于记忆的"目录页":只放索引和最高频条目;细节放子文件,需要时 view。
  2. **为什么必须硬限制**——记忆是 append 倾向最强的上下文(每次会话都想留点什么),没有硬顶它会无限膨胀;**200 行的约束反过来逼你做信息架构**——什么配进目录、什么归档,这个被动设计其实是主动训练。
  3. **配套习惯**——定期园艺(Q6.12):过期条目移出入口、合并重复、标"不再适用";入口每一行同样过"删掉会不会犯错"测试。
- **补充弹药(追问再掏)**:
  - **诊断用法**:入口文件行数的增长趋势是"记忆健康度"的最便宜指标——单调涨说明有人在当人形 append。
  - **对照**:与 CLAUDE.md 的 200 行建议同源——所有常驻上下文都有硬顶,这是普遍规律不是产品怪癖。
- **及格线**:注入上限+为什么要限。
- **加分**:目录页/子文件的信息架构;园艺习惯。
- **红旗**:以为记忆多多益善;不知道上限导致"agent 不知道这事"的困惑。

**Q6.5【八股·L2·架构】"什么必须写回仓库、什么必须及时忘掉"——怎么判断?append-only 的记忆会得什么病?**

- **人话版**:该写回的是决策及理由、契约(schema/API/命令)、验证标准、会被后续任务依赖的结论;该忘掉的是过程噪声——检索废料、失败尝试的细节、过期快照。append-only 的病叫 semantic drift:旧结论因为出现次数多被当成现行标准。
- **主线(照这条线讲)**:
  1. **判断标准两问**——这个信息会被未来的任务依赖吗(会→写回,不会→忘);它有时效吗(有→带过期机制,没有→可长期)。**决策的理由比决策本身重要**:"我们为什么不用 X"能防止半年后有人(或 agent)好心把 X 加回来。
  2. **semantic drift 的机制**——只增不减的记忆里,旧写法出现 100 次、新规范 2 次:模型的频率直觉把旧的当主流;被反复引用的过期文档还获得"权威错觉"。与 ch02 的"过期上下文比没有更危险"同源,但发生在**跨会话尺度**。
  3. **防护三件**——归档物打 archived/superseded 标;记忆条目带"适用条件/复审时间";记忆与代码冲突时以代码为准的默认约定。
- **补充弹药(追问再掏)**:
  - **来源权威的裁定**:多来源冲突(摘要 vs 政策 vs 代码)怎么定权威——ch10 Q10.10 有专门的裁定框架(来源权威分级),两题互为表里。
  - **量化自检**:统计"被引用的规则占比"——长期 0 命中的规则是删除候选。
- **及格线**:写回/忘掉的分界+drift 机制。
- **加分**:决策理由的价值;防护三件套。
- **红旗**:所有会话内容都存"以后可能有用"。

**Q6.6【八股·L1·用户】长任务"不断线"的三件套——progress log / handoff artifact / repo memory——各是什么、给谁看?**

- **人话版**:progress log 是任务推进的时序记录(做了什么/结论/下一步);handoff artifact 是交接文档,新会话只读它就能接手;repo memory 是仓库内长期沉淀的决策与契约。分别给人看、给下一个 agent 看、给所有未来任务看。
- **主线(照这条线讲)**:
  1. **progress log**——追加式、时序化,价值在过程可回放(出事时定位"从哪一步开始歪的");**放仓库不放对话**——对话会被压缩,文件不会。
  2. **handoff artifact**——与 log 的区别是"蒸馏":只含状态与决策(当前状态/关键决策及理由/失败尝试与原因/未决问题/环境入口),不含过程噪声。**检验标准:新会话只喂这份文档能否 10 分钟接手**。
  3. **repo memory**——跨越单个任务的沉淀(架构决策记录、契约、checklist):log 和 handoff 随任务结束归档,repo memory 留下来变成组织资产。
  4. **与压缩的关系收尾**——它们是 compaction 的结构性替代:压缩是有损滚动摘要,这三样是显式外部状态,不受 rot 影响、每轮可重新注入(机制见 Q6.8)。
- **补充弹药(追问再掏)**:
  - **命名对照**:行业各种叫法(交接文档/进度文件/checkpoint)都落在这三件的组合上——按"给谁看"分类就不会被名词绕晕。
  - **模板化**:handoff 的字段清单见 Q6.10,可直接当模板用。
- **及格线**:三者定义+读者。
- **加分**:"蒸馏"区别;"文件不怕压缩"的属性论证。
- **红旗**:三件混做一个大文档;handoff 里贴大段代码(该给路径)。

**Q6.7【八股·L1·平台】会话持久化(--continue/--resume)和 checkpoint/rewind 各自能恢复什么、不能恢复什么?**

- **人话版**:--resume 恢复对话历史;checkpoint 在每次编辑前快照,能回滚代码/对话/环境三态。都不能恢复的:bash 的外部副作用(装的包、发的请求)和已被压缩掉的细节。
- **主线(照这条线讲)**:
  1. **resume 的边界**——恢复的是"对话文本"不是"工作状态":历经多次压缩的会话 resume 回来的早期内容可能只是摘要;对话很长时,**新会话+读进度文件常比 resume 更干净**(不带历史包袱)。
  2. **checkpoint 的边界**——覆盖编辑类工具的改动,bash 的外部影响不在快照内;本地保留约 30 天;官方明确"不是 git 替代"。
  3. **工程结论(本题落点)**——长任务不要赌"会话可恢复",要主动在里程碑写外置状态(git commit+进度文件):**会话是易失的,仓库是持久的**——这是记忆设计的总纲,后面所有方案都是这句的展开。
- **补充弹药(追问再掏)**:
  - **实践参照**:公开的多天级任务全部用"新会话+开机序列"而非长会话 resume(ch04 Q4.8)。
  - **产品对照**:各家 checkpoint 的覆盖范围不同(有的含环境状态),采购时按"能不能回滚 bash 副作用"问。
- **及格线**:各自能/不能恢复什么。
- **加分**:"从状态重建优于恢复会话"的立场及理由。
- **红旗**:把 resume 当长任务的可靠性方案。

---

## 6.2 原理深挖

**Q6.8【原理·L3·模型】为什么"结构化笔记"能维持数小时级长任务的一致性,而 compaction 做不到?**

- **人话版**:笔记是显式外部状态——目标/已试过什么/下一步写在固定文件里,每次新会话或压缩后重读,以极小 token 恢复全部决策上下文;compaction 是有损滚动摘要,信息每过一次就衰减一次。一个是无损存储+按需读取,一个是有损信道里的累积误差。
- **主线(照这条线讲)**:
  1. **机制差异是核心**——compaction 的摘要在上下文里"随波逐流":继续变长、继续被稀释、再次压缩再衰减;笔记在上下文外,**每轮以完整保真度重新进入**,不存在衰减链。
  2. **选型口径(Anthropic 三分法)**——会话内拉锯用 compaction;里程碑式长任务用结构化笔记;并行深挖用 subagent 隔离:工具与任务形态匹配,没有全能选项。
  3. **实证参照**——公开的多天级实验收敛到同一模式:JSON 进度文件+固定开机序列(读 git log→读进度→领任务→跑基线测试);连游戏化长程任务(Pokémon)也靠进度笔记维持数十小时一致性——**跨场景验证过的模式才叫稳**。
  4. **写什么(不是流水账)**——决策状态:目标与验收、已完成与结论、**已否决及原因**、下一步、阻塞点——正好是 handoff 的 schema(Q6.10)。
- **补充弹药(追问再掏)**:
  - **量化直觉**:笔记重注入约几百 token,等价的信息如果靠"保留完整历史"要几万 token 且持续衰减——性价比差两个数量级。
  - **反例**:把笔记写成时序流水账(今天干了 A 又干了 B)——信息密度低,重读成本高,还不如摘要。
- **及格线**:显式外部状态 vs 有损滚动摘要的机制对比。
- **加分**:三分法选型;笔记的"写什么"清单。
- **红旗**:认为窗口够大就不需要笔记;笔记写成流水账。

**Q6.9【原理·L3·架构】为什么给模型维护的状态文件用 JSON 而不是 Markdown?"给模型读的用 Markdown、给模型改的用结构化格式"的依据?**

- **人话版**:Markdown 对模型太"好改"——agent 会顺手重写、美化、重排,状态的机器可读性和 diff 可审计性就没了。JSON 有 schema 约束,增删字段可校验、diff 干净、可被工具消费。读的文档用 Markdown(表达自由),改的状态用结构化格式(变更可控)。
- **主线(照这条线讲)**:
  1. **Markdown 状态的翻车模式(先讲现象)**——agent"整理"任务清单时把未完成项也整理没了;重排顺序导致 diff 全是格式噪声,人没法审;自由文本塞歧义表述("这个差不多完成了")。
  2. **结构化的三层收益**——字段级校验(缺 required 直接报错,状态不可能"悄悄"不完整);diff 干净(每个变更对应明确字段操作);可被工具链消费(CI 检查"是否有未完成任务就发布")。
  3. **原则的推广(本题落点)**——todo 列表在 Claude Code 里是工具操作(TodoWrite)而不是让模型自由写文本,同一家思想:**状态变更走确定性接口,不走自由生成**。任务清单、进度文件、功能清单,凡是"agent 要改的状态"都适用。
- **补充弹药(追问再掏)**:
  - **边界**:只读文档(架构说明/背景)仍用 Markdown——强制 JSON 降低可读性没有收益;判据就是"谁拥有写权"。
  - **中间态**:YAML/TOML 在"人也要经常手改"的场景是折中——但 schema 校验不能省。
- **及格线**:翻车模式+结构化收益。
- **加分**:TodoWrite 的同构设计;原则向 CI 消费的推广。
- **红旗**:所有 agent 可写文件都用 Markdown;或只读文档也 JSON 化。

**Q6.10【原理·L4·架构】设计 handoff artifact 的信息架构:该含什么、绝不含什么?**

- **人话版**:含六样——任务目标与验收标准(原文引用)、当前状态、关键决策及理由、环境与入口(分支/启动命令/依赖坑)、失败尝试与原因、未决问题;绝不含四样——原始检索废料、大段代码(给路径)、会过期的快照(给获取方式)、无关项目背景。检验:新会话只喂它,10 分钟内能否接手。
- **主线(照这条线讲)**:
  1. **六含里讲两个"为什么"**——"失败尝试与原因"最重要:没有它,下一个 agent 大概率重蹈覆辙——重复失败尝试是长任务最大的隐藏成本,有了它失败变资产;"验收标准要原文引用"因为转述丢约束的语气和细节("尽快"vs"必须本周内否则回滚")——**验收歧义是接力任务的经典死因**。
  2. **四不含的共同逻辑**——不给"可重新获取的东西":粘贴的代码会过期(文件又改了),路径永远指向当前事实;快照同理,给获取方式;这与记忆文件"写不可推断的事实"同一逻辑。
  3. **更新时机(保鲜)**——每会话结束前、每个里程碑后、发现阻塞立即:handoff 是保鲜品,**过期交接文档比没有更危险**(带着权威口吻的旧状态,ch10 的来源权威问题)。
- **补充弹药(追问再掏)**:
  - **10 分钟检验的操作**:让一个没参与过的同事(或新会话)只读 handoff 接手——接不住的字段就是缺的字段,比评审清单有效。
  - **对照工具**:lorelum 的 practice 把这个思路命名为 decision-dense checkpoint(见 Q6.11),字段几乎一致——独立来源收敛到同一设计,说明它是结构必然。
- **及格线**:六含四不含给个七七八八+检验标准。
- **加分**:失败尝试的价值论证;"给路径不给粘贴"的原理。
- **红旗**:把对话日志复制一段当 handoff。

---

## 6.3 会话接力的三条纪律(上下文丢失后怎么办)

**Q6.11【原理·L3·平台】"决策密集检查点"(decision-dense checkpoint):跨窗口的检查点应该写什么、不写什么?**

- **人话版**:检查点只写"约束未来决策的信息"——当前目标、权威来源、验收边界、已定决策、未完成范围、仍然有效的证据、重大风险;被否决的选项和失败实验只留结论,长输出直接丢弃。反模式:记了碰过的每个文件和测试日志,却漏了"鉴权拒绝还没验证"和"这个领域模型只是个未确认的假设"。
- **主线(照这条线讲)**:
  1. **判据一句话**——一条信息要不要进检查点,看它**会不会改变下一个决策**:"authz rejection 未验证"会(决定先补验证还是继续),"刚才跑了哪些命令"不会(历史可回放)。
  2. **两类最容易被漏的信息(也是反模式的靶心)**——未验证的关键假设(必须标"未确认"而不是写成事实——**给不确定性贴标签,不把不确定转成确定**);仍然有效的证据清单(哪些验证过了、覆盖什么范围——下一个会话不用重跑)。
  3. **"密集"的含义**——不是写得多,是决策密度高:把长过程压缩成约束("试过方案 A,因 X 否决"一句话),检查点的信息/ token 比是它的质量指标。
- **补充弹药(追问再掏)**:
  - **与其他载体的关系**:它是 progress log(时序全记录)与 handoff(交接蒸馏)之间每轮更新的轻量版——三层载体按频率分工:检查点每轮、handoff 每会话、log 随时。
  - **出处**:这条 practice 来自 lorelum 的 agentic-coding 包(工具中立工程规范),与各家官方最佳实践互相印证。
- **及格线**:"约束未来决策"判据+一个反模式。
- **加分**:两类易漏信息;"给不确定性贴标签"的原则。
- **红旗**:检查点=过程流水账;把未验证假设写成事实。

**Q6.12【原理·L3·平台】"上下文丢失后重新接地"(reground after context loss):压缩或新会话后,为什么不能直接信摘要继续干活?正确的开机动作是什么?**

- **人话版**:摘要只能用来"定位",不能用来"信任"。正确动作:用摘要找到权威来源(任务原文/已批准的计划/当前 diff/已记录的证据),重读它们并与摘要对照,丢掉被取代的假设,然后明确说出当前目标、未完成边界、有效证据、下一步,再继续动手。
- **主线(照这条线讲)**:
  1. **为什么不能信摘要(机制)**——摘要有损且可能过期:它生成时的状态可能已被后续变更取代;更阴险的是**摘要会把"未验证的假设"固化成"事实"口吻**——下一个上下文继承的不是状态,是误差。
  2. **反模式(题目自带的例子很好用)**——摘要说"feature complete",后续 agent 便继续做清理;而当前验收标准和 diff 显示鉴权流程根本没实现也没验证——**"完成"是摘要的推断不是证据**,信了它整条任务的方向就错了。
  3. **正确的开机序列(可操作)**——用摘要定位四样:权威任务源、已批准计划/决策、当前工件/diff、已记录证据→逐项重读并对照摘要→显式声明"当前目标/未完成/有效证据/下一步"→**声明完了才许写代码**——"先声明后动手"是纪律的落点。
  4. **与轮班模式的关系**——它就是 ch04 开机序列的"防摘要"加强版:开机序列假设进度文件可信,重新接地还要求对照权威源验证进度文件本身。
- **补充弹药(追问再掏)**:
  - **成本论证**:重读权威源几百 token,纠一个方向错误省几万 token——重新接地是便宜的保险。
  - **产品化形态**:Anthropic 长任务方案里"每会话读 git log+进度+跑基线测试"就是这个纪律的工程化。
- **及格线**:摘要用于定位不用于信任+开机动作。
- **加分**:"假设固化成事实"的机制;"先声明后动手"的纪律。
- **红旗**:压缩完直接接着干;把摘要当状态源。

**Q6.13【原理·L3·平台】"交接先验证再续"(validate handoff before continuation):接手另一个 agent/会话的交接时,第一步做什么?**

- **人话版**:把 handoff 里"下一步动作所依赖的声明"逐条拎出来,对照权威需求、当前工件状态、引用的证据,分类为接受/收窄/拒绝/升级——处置完了才动手。反模式:交接说"所有检查都过了"就继续,而它引的只是组件测试,分支此后又改了集成路径。
- **主线(照这条线讲)**:
  1. **核心动作:识别"依赖声明"**——不是逐条审全部内容,只审"我的下一步动作依赖哪些声明":要改支付模块,依赖的声明是"支付测试已过""schema 已冻结"——这两条必须验证,其余可以信。
  2. **四种处置(给操作框架)**——接受(有证据支撑且覆盖当前状态);收窄(证据只覆盖部分,按部分采用);拒绝(与当前状态矛盾);升级(无法验证又影响重大,问人)。**并发变更要专门查**:交接生成后代码又动过,它引的证据可能已过期(连到 ch07 的"证据绑定工件状态")。
  3. **为什么这条纪律值得单独记**——多 agent/多会话系统的信息衰减发生在交接点:执行中的误差有验证环兜着,**交接误差直接进入下一个会话的"初始事实"**且无人质疑——所以接手方的验证责任比交出方的诚实更重要。
- **补充弹药(追问再掏)**:
  - **轻量落地**:在 handoff 模板里给每个关键声明加"证据指针"字段(测试名/commit/链接)——接手方按指针验,不用全信人格。
  - **与 Q6.12 的分工**:重新接地管"摘要不可信",交接验证管"声明不可信"——两个不可信都防,接力才稳。
- **及格线**:依赖声明+四种处置。
- **加分**:"误差进入初始事实"的机制;证据指针的落地。
- **红旗**:接手就干;把交接文档当权威源。

**Q6.14【原理·L3·平台】Devin 的 playbook/learnings 这类"经验沉淀"机制解决什么组织问题?**

- **人话版**:playbook 把"某人成功过一次的流程"外置成可复用可分享的模板(固定段落:流程/规格/建议/禁止动作/需要用户提供什么);learnings 从历史会话自动沉淀经验。解决:多 agent 多会话没有共享记忆,知识不进文件就随会话蒸发。
- **主线(照这条线讲)**:
  1. **组织本质**——同一个团队里每个人和 agent 摸索出的有效做法默认是孤岛;沉淀机制把个人成功变成团队可复制——和传统团队的 runbook/SOP 同构,只是执行者变成 agent,**模板的可执行性要求更高**(段落要能被 agent 直接遵守,不是给人看的散文)。
  2. **"禁止动作"段的设计逻辑**——agent 没有常识默认值,"别动 X"必须说出来;负面约束显式化——呼应"高频约束要外置成结构化载体,不活在人的口头提醒里"。
  3. **通用化**——任何团队都能轻量实现:高频任务的正确姿势写进 skill/模板文件、版本化管理、失败案例反向更新;关键动作是"固化",形式不限——别把它当某产品的 proprietary 功能。
- **补充弹药(追问再掏)**:
  - **与 skill 的区别**:playbook 是"任务流程模板"(怎么干这类活),skill 是"能力包"(给 agent 的新工具/知识)——一个管流程一个管能力,常配合用。
  - **治理**:learnings 自动沉淀的内容同样要人审(错误的"经验"会批量污染,同 Q6.3 的模型记忆治理)。
- **及格线**:外置共享知识这个本质。
- **加分**:禁止动作段的设计逻辑;轻量版自实现。
- **红旗**:当产品功能看,与自建无关。

**Q6.15【原理·L4·架构】"记住什么、忘掉什么"的治理机制怎么落地?给一个记忆生命周期策略。**

参考框架(策略示例):
- **写入分级**——决策/契约/坑→repo(评审后进,走 PR);任务中间态→会话内/todo;个人偏好→用户层。每类有明确 owner 和评审方式——写入库的记忆该走 PR,这句是多数团队缺的。
- **过期机制**——每条规则带"适用条件/复审时间"元数据;归档打标(archived/superseded);定期 doc gardening 进 backlog——**像清技术债一样清记忆债**,排期而不是等爆发。
- **冲突仲裁**——source of truth 声明写进根记忆(代码>AGENTS.md>wiki);记忆与代码冲突默认以代码为准——这一条默认值能消灭一大类混乱。
- **度量**——记忆命中率(被引用规则占比:长期 0 命中是删除候选)、过期规则引发的故障数、入口行数趋势。
- **及格线 / 加分 / 红旗**:及格=写入分级+过期机制;加分=度量指标;红旗=只有写入没有清理(所有记忆系统的默认结局是腐烂)。

---

## 6.4 品味与判断题

**Q6.16【品味·L3·架构】记忆放文件(仓库内)还是向量库/数据库?给判断框架。**

参考思路:
- **文件(默认)**——人可读可审、git 版本化、agent 天然会 grep/read、零基础设施;**决策与规则这类"必须确定性命中"的内容,文件是唯一正确形态**(规则进向量库=概率检索赌规则命中——这个反面判断是本题的核心)。
- **向量库(按需)**——海量非结构化知识(千份文档)、语义检索为主:适合"找不到也没事,找到更好"的内容。
- **数据库**——多用户并发写、跨服务共享、需要权限模型:组织级记忆平台。
- **2026 年的证据**——实验:8B+向量库记忆(30%)胜过 235B 无记忆(14%)——组织好的记忆比更大的模型值钱;同一批研究显示持久 agent 的查询约一半与历史语义相似/精确重复,混合检索(BM25+向量)比纯语义高 7 个点——记忆检索不是纯语义问题。
- **及格线 / 加分 / 红旗**:及格=三类载体各配适用内容;加分=新证据支撑;红旗=规则进向量库,或千份文档全塞 prompt。

**Q6.17【品味·L4·管理】团队的记忆资产(specs/rules/skills)谁维护?怎么防腐烂?**

参考思路:
- **所有权跟代码走**——记忆文件变更走 PR 评审:"谁改代码谁改配套规则";没有 owner 的记忆必然腐烂。
- **防腐四件**——规则带触发条件与复审期;doc gardening 进 backlog(每季度);规则失效引发的故障回写为规则更新(失败驱动维护);健康度度量(引用率/过期率)。
- **反对的做法**——AI 自动生成无人审核的规则堆积(semantic drift 的温床);一次性"文档冲刺"(写完没人管,一年后全过期)。
- **复利视角**——记忆资产是复利资产:维护成本前置,收益是每个后续任务都省;多数团队只看到维护成本没算复利。
- **及格线 / 加分 / 红旗**:及格=PR 化+定期清理;加分=失败驱动维护或复利视角;红旗=交给"有空的人"或 AI 自动堆。

**Q6.18【品味·L3·用户】什么时候 --resume 续会话,什么时候新会话+读 handoff?**

参考思路:
- **resume 适合**——短中断(几分钟到几小时)、上下文仍高信噪比、任务连续没换。
- **新会话+handoff 适合**——跨天/跨任务;上次会话已污染(大量失败尝试、跑偏的探索——带着这些历史是负资产);任务到了里程碑(自然重启点)。
- **主动优于被动**——长任务应该在里程碑**主动**写 handoff 并计划性开新会话,而不是等上下文满了被动压缩:**前者你控制保留什么,后者赌摘要的质量**。
- **一句话判据**——要"历史"(接着聊)resume;要"状态"(接着干)新会话+读文件。
- **及格线 / 加分 / 红旗**:及格=两类场景;加分="主动交接优于被动压缩";红旗=一律 resume 到天荒地老。

**Q6.19【品味·L4·架构】2026 年两条新闻线:一边是个人知识库"去检索化"(Karpathy 2026-04:LLM 当编译器维护约百篇互联 markdown wiki,明确说个人规模不需要向量库);一边是记忆研究的反向结论(带向量库记忆的 8B 胜过无记忆的 235B)。怎么调和?**

参考思路:
- **两者不矛盾,是规模的两个区间**——百篇量级:互联 wiki+顺链跳转就够,维护索引是纯开销;亿级 token 量级:检索是刚需。问题是"记忆的组织形态",不是"要不要记忆"。
- **共同信号(比调和更重要)**——"组织好的上下文 > 更大的模型"这个不等式在两边都成立:预算该从堆模型分一部分给记忆建设——这是可落地的决策变化。
- **调和后的行动**——先问规模再选形态:百篇 wiki+链接;千篇 BM25/关键词够用(混合检索优于纯语义的实验证据);万篇以上分层(策展入口+检索底座)。
- **及格线 / 加分 / 红旗**:及格=规模分界;加分=不等式及其预算含义;红旗=用一条新闻否定另一条。

---

## 6.5 开放性问题

**Q6.20【开放·L5·模型】模型原生支持 memory tool 之后,人写的记忆文件会膨胀还是萎缩?**

讨论框架:
- **萎缩派**——模型自己记过程细节(试过什么、踩过什么坑),人写记忆里这类"本可自动沉淀"的内容不再需要人写。
- **膨胀派**——模型能消费更多结构化记忆,人会写更多"意图类"内容(边界、偏好、否决理由)——这类永远来自人;不信任模型自写记忆的组织会用"宪法式"人写记忆锚定不可漂移的部分。
- **关键变量**——模型写记忆的可审计性:错误结论进记忆的污染链。审计成本高则人写记忆承担更多;治理工具成熟则模型记忆扩张。
- **落点**——总量都涨但结构分化:人写"意图与边界",模型写"过程与教训",比例是组织信任度的函数。

**Q6.21【开放·L4·架构】为开源项目设计"贡献者的记忆入口":新 contributor 的 agent 第一次进仓库,该看到什么?**

讨论框架:
- **最小充分集**——项目一句话(是什么/不是什么)、构建测试命令(可直接复制执行)、关键风格约定、禁区(别动的目录/别提的 PR 类型)、指向深层文档的索引。
- **分层加载**——根文件薄、子目录就近、路径作用域:一次性全塞等于全不塞。
- **冷启动检验**——让没见过的 agent 完成 hello task(加个小功能),一次通过率是记忆入口的质量指标——可测量就可迭代。
- **反模式**——把 CONTRIBUTING.md 原样当 agent 记忆(写给人的流程性内容,agent 要的是可执行事实);入口文件塞十页架构哲学。

---

## 6.6 工程实战题

**Q6.22【实战·L3·平台】为 monorepo(前端/后端/共享包)设计记忆文件布局。**

参考思路:
- **布局**——根 AGENTS.md(全局命令/约定/索引,薄)→各包 AGENTS.md 或 .claude/rules(paths 作用域)→高频深目录的子目录 CLAUDE.md(懒加载)。
- **配套**——CI 校验记忆里的命令仍有效(命令失效是记忆腐烂的头号形态:构建命令改了没人更新记忆文件);新包用 /init 引导;入口总量控制(根 <200 行)。
- **内容分配原则**——全局只放跨包通用(发布流程/monorepo 约定);包级放本包特有(测试命令/坑)——放错层级的代价是冗余或漏。
- **及格线 / 加分 / 红旗**:及格=三层布局+命令校验;加分=内容分配原则;红旗=一个巨型根文件装所有包的知识。

**Q6.23【实战·L4·架构】设计"跨夜长任务"的记忆方案:任务 8 小时,会话每约 1 小时被上下文上限打断。**

参考思路:
- **初始化会话(人监督)**——建进度文件(JSON)+计划+验收标准;确认端到端测试可跑:基线是接力的锚点。
- **每个会话的开机序列(固定、写进配置)**——读 git log→读进度文件→**按 Q6.12 重新接地:对照验收标准验证进度声明**→选最高优先级未完成项→跑端到端测试确认基线→开工。
- **里程碑纪律**——每个子任务完成即 commit(checkpoint);会话结束前更新 handoff(阻塞点/下一步/给下一班的忠告,按 Q6.10 模板)。
- **人工检查点与熔断**——早晨第一件事审夜间产出(验收标准的执行);异常率超阈值自动全停。
- **参照物**——Anthropic 轮班工程师方案(ch04 Q4.8)+ 本章三条接力纪律,这个组合已被多天级任务验证。
- **及格线 / 加分 / 红旗**:及格=初始化+开机序列+commit;加分=重新接地环节与早晨熔断;红旗=指望一个会话扛 8 小时。

**Q6.24【实战·L3·平台】实现"记忆园艺"自动化:每周运行的 job,清理过期规则、标记冲突、汇报健康度。**

参考思路:
- **元数据前提**——规则要带创建时间/触发条件/最后命中时间:没有元数据的园艺只能靠语义判断(贵且不稳)。
- **job 逻辑**——超期未命中→删除候选(输出 PR 供人审,**不自动删**——记忆变更留人审环节);与代码冲突检测(规则提到的路径/命令还存在吗);新增建议(从近期会话高频纠正中提取候选,人工确认入库)。
- **报告指标**——总条数/命中率/过期率/入口行数趋势:趋势比绝对值有用。
- **及格线 / 加分 / 红旗**:及格=元数据+删除候选 PR;加分=从会话提取新增建议;红旗=自动删除不过人审。

---

## 6.7 场景排错题

**Q6.25【排错·L2·用户】agent 每次新会话都重新犯上周已纠正过的错误,你明明把纠正写进了 CLAUDE.md。排查?**

排查链路:
- **①加载了吗(第一怀疑)**——层级对不对(放错目录)、入口超行数被截断、懒加载没触发(规则在子目录但任务没碰那个目录):先让 agent 复述规则验证注入,一分钟定位加载层问题。
- **②措辞可执行吗**——"保持代码整洁"是愿望,"禁止向 src/legacy 写新代码"是规则:前者无效。
- **③被仓库现实压倒**——旧代码里大量反例,频率直觉让模型跟反例走:这是**仓库治理问题不是规则问题**,改措辞没用——要么清反例要么把规则升级为 lint/hook(确定性执行)。
- **④被稀释**——规则周围堆满低价值内容:做减法。
- **及格线 / 加分 / 红旗**:及格=①②③链路;加分=区分"规则问题"和"仓库治理问题"两类根因;红旗=把同一条规则再写一遍加粗。

**Q6.26【排错·L3·平台】跨会话 resume 后 agent 行为异常:重复已完成步骤、丢失早先约束。根因?**

排查链路:
- **根因一**——resume 的会话已历经多次 compaction,早期约束只存在于有损摘要里:看起来"记得",细节已丢。
- **根因二**——关键结论没写进持久层(记忆/任务文件),只活在对话里。
- **修复方向**——长任务不依赖 resume,改"每会话从仓库状态重建上下文"(开机序列+Q6.12 重新接地);约束落在任务文件/记忆文件(持久层)而不是对话(易失层);检查 auto memory 是否启用、有没有把约束写进去。
- **预防**——把"会话是易失的"当设计公理:所有必须跨会话存活的信息,写文件。
- **及格线 / 加分 / 红旗**:及格=compaction 累积损失;加分=从状态重建的修复;红旗=反复 resume 重试指望"这次记得"。

**Q6.27【排错·L4·架构】团队 wiki、AGENTS.md、代码注释三处对同一约定说法不一,agent 随机采用其一。治理?**

排查与治理:
- **定义 source of truth 顺序并写进根记忆**——如"代码>AGENTS.md>wiki":先给 agent 一个裁决默认值。
- **一次性清理**——冲突清单以代码为准回写;wiki 该页加"以 repo 为准"指针;三处中只保留 SoT,其余引用。
- **防复发**——新约定只允许写进一处(写进 CONTRIBUTING 流程);gardening job 定期一致性 diff;规则文件的 PR 模板加"是否与其他文档冲突"检查项。
- **及格线 / 加分 / 红旗**:及格=SoT 顺序+清理;加分=防复发机制;红旗=三处都改成一样但没定 SoT(下次漂移继续)。

---

## 6.8 开放加餐:偏好与品味追问

**Q6.28【开放·L3·用户】你的记忆文件/规则库里,你自己最值钱的一条是什么?为什么是它?**

参考思路(一个好答案长什么样):
- **值钱的定义先立住**——防止反复踩坑(不写就会再踩)、跨项目仍成立(不是某仓库的细节)、别人抄不走(项目/团队特有的非公开事实):**三条占两条就算值钱**。
- **好例子的形态**——某个环境怪癖("这台机器上测试必须先起 X 否则必挂")、某个带理由的技术禁令("别用 X 库做 Y,因为我们在 Z 上踩过——这个理由防止半年后有人好心换回去")。
- **知道什么不值钱**——放之四海皆准的通用规范(和没写一样)、可从代码推断的事实(浪费上下文)、过期的决策(只剩误导):能主动说"我的库里哪些该删了"的,是真在经营记忆。
- **及格线 / 加分 / 红旗**:及格=一条具体且带理由的条目;加分=值钱三判据+主动清理意识;红旗=背出的是通用规范、或说"每条都很重要"。
