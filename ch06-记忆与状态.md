# ch06 · 记忆与状态管理

> 覆盖:记忆文件体系(CLAUDE.md/AGENTS.md/rules/auto memory/MEMORY.md)、Anthropic memory tool、memory/state/artifact 之辨、system of record(书 Part 5.2)、append-only 与 semantic drift、长任务三件套(progress log / handoff artifact / repo memory)、会话持久化与 checkpoint、Devin playbook、结构化笔记模式。

---

## 6.1 八股题(基础概念)

**Q6.1【八股·L1·用户】CLAUDE.md 的层级加载与 lazy load 机制?**
答:
- 四层:企业托管(MDM 下发,不可排除)→ 用户 ~/.claude/CLAUDE.md → 项目 ./CLAUDE.md(入 git)→ CLAUDE.local.md(个人);自根向下**拼接而非覆盖**;
- 子目录 CLAUDE.md **按需加载**:agent 读到该目录的文件时才注入(路径作用域);`.claude/rules/*.md` 支持 paths 前缀的 glob 作用域;
- `@path` import 递归上限 4 跳;`/init` 生成并可吸收 Cursor/Copilot 规则与 AGENTS.md;最佳实践 <200 行。

**Q6.2【八股·L1·用户】AGENTS.md 是什么?为什么它成了收敛标准?**
答:
- 仓库根目录纯 Markdown 的"给 agent 的 README",无强制 schema;monorepo 中就近生效(OpenAI 主仓约 88 个);
- 由 Linux 基金会 Agentic AI Foundation 维护,超 6 万开源项目采用;被 Codex、Gemini CLI、Jules、Cursor、Copilot coding agent、Devin、opencode 等数十家支持;
- 常见节:项目概述/构建测试命令/代码风格/测试说明/安全与 PR 规范;
- 收敛逻辑:一份项目记忆维护 N 个工具的成本 < N 份各自维护;Claude Code 官方建议 @AGENTS.md 导入或软链。

**Q6.3【八股·L2·平台】Anthropic 的 memory tool 机制?和 CLAUDE.md 是什么关系?**
答:
- 客户端文件式记忆:目录 /memories,命令 view/create/str_replace/insert/delete/rename;启用后 API 自动注入"开始前先看记忆目录"的指令;view 超 16,000 字符截断;
- 与 context editing 配合时,清理前会提醒模型先把重要结果写入记忆(先抢救再清理);
- 关系:CLAUDE.md 是**人写的、项目级、常驻前缀**的项目记忆;memory tool 是**模型写的、跨会话、按需读**的工作记忆——一个治理"项目是什么",一个治理"我做到哪了"。

**Q6.4【八股·L2·用户】Claude Code 的 auto memory 与 MEMORY.md 的注入限制?**
答:auto memory 默认开启,持久化 user/feedback/project/reference 四类笔记;**每会话只注入 MEMORY.md 的前 200 行/25KB**——这就是"记忆入口要小、按需展开"的强制设计;其余内容靠 agent 主动 view。

**Q6.5【八股·L2·架构】书 Part 5.2 对 system of record 的核心问题是什么?**
答:**什么必须写回 repo 成为长期可信状态,什么必须及时忘掉。**
- 必须写回:决策及其理由、契约(schema/API/命令面)、验证标准、会被后续任务依赖的结论;
- 必须忘掉:过程性噪声(检索废料、失败的尝试细节、过期环境快照);
- 警惕:**append-only context 特别容易把系统拖进 semantic drift**——只增不减的记忆让"旧结论因出现次数多而被当成现行标准"(与 ch02 权重错配同源)。

**Q6.6【八股·L1·用户】长任务"不断线"的三件套(书 Part 3.5)?**
答:
- **Progress log**:任务推进的时序记录(做了什么/结论/下一步),放 repo;
- **Handoff artifact**:交接产物——新会话/新 agent 只读它就能续命(不含过程噪声,只含状态与决策);
- **Repo memory**:仓库内的长期记忆(决策记录/契约/checklist),区别于会话内记忆;
- 各归其位:progress log 给人看、handoff 给下一个 agent 看、repo memory 给所有未来任务看。

**Q6.7【八股·L1·平台】会话持久化与 checkpoint 的产品化形态?**
答:
- `claude --continue`(最近会话)/`--resume <id>`;/rename 命名;`--no-session-persistence` 关闭落盘;
- checkpoint/rewind:每次编辑前自动快照,双击 Esc 恢复代码/对话/环境三态;仅覆盖编辑类工具(bash 副作用不在内),本地存 30 天,官方明确"不是 git 替代"。

---

## 6.2 原理深挖

**Q6.8【原理·L3·架构】为什么 Anthropic 长程 harness 选择"JSON 功能清单"而不是 Markdown 进度文件?**
答:
- Markdown 对模型太"好改"——agent 顺手重写、美化、重排,破坏了状态的机器可读性与 diff 可审计性;JSON schema 约束结构,增删字段可校验、可 diff、可被工具消费;
- 一般原则:**给模型读的用 Markdown,给模型改状态用的用结构化格式**;
- 加分:这也解释了为什么任务清单(TodoWrite)在 Claude Code 里是工具操作而不是让模型自由发挥的文本。

**Q6.9【原理·L3·模型】"结构化笔记维持数小时一致性"(Claude Plays Pokémon)的机制为什么有效?**
答:
- 里程碑式长任务里,compaction 是"有损滚动摘要",笔记是"显式外部状态":
- 笔记把"当前目标/已试过什么/下一步"锚定在固定文本上,不随对话轮次衰减,不受 context rot 影响;
- 每次新会话/压缩后重读笔记=以极小 token 恢复全部决策上下文——这是"外部记忆比上下文记忆更稳"的实证;
- 选型(Anthropic 框架):会话式拉锯用 compaction;里程碑式长任务用结构化笔记;并行深挖用 subagent 分区。

**Q6.10【原理·L4·架构】设计 handoff artifact 的信息架构:该含什么、绝不含什么?**
答:
- 该含:任务目标与验收标准(原文引用)、当前状态(完成/进行/阻塞)、关键决策及理由、环境与入口(分支/启动命令/依赖坑)、失败尝试与原因(防止下一个 agent 重蹈)、未决问题与人肉上下文;
- 绝不含:原始检索废料、大段代码(引用路径而非粘贴)、会随时间过期的快照(给获取方式不给快照)、与任务无关的项目背景;
- 检验:**新会话只喂这份文档,能否 10 分钟内接手**——不能就继续收敛。

**Q6.11【原理·L3·平台】Devin 的 playbook 与 learnings 机制解决什么组织问题?**
答:
- playbook:可复用可分享的提示模板,固定段落 Procedure/Specifications/Advice/**Forbidden Actions**/Required from User——把"某人成功过一次的流程"外置成文件;
- learnings:从历史会话自动沉淀经验,把个人成功变成团队可复制;
- 组织本质:对抗多 agent/多会话"无共享记忆"的缺陷——知识不进文件就会随会话蒸发;与书的"控制面资产化"(skill 化/rule 化/spec 化)同一思想。

**Q6.12【原理·L4·架构】"记住什么、忘掉什么"的治理机制怎么落地?给出你们的记忆生命周期策略。**
答(策略示例):
- 写入分级:决策/契约/坑 → repo(评审后);任务中间态 → 会话内/todo;个人偏好 → 用户层;
- 过期机制:每条规则带"适用条件/复审时间";归档物打 archived/superseded;定期 doc gardening(书 Part 5.4 的清理 job);
- 冲突仲裁:source of truth 声明(源码>specs>wiki);记忆与代码冲突时以代码为准的默认规则;
- 度量:记忆命中率(被引用的规则占比)、过期规则引发的故障数。

---

## 6.3 品味与判断题

**Q6.13【品味·L3·架构】记忆应该放在文件(repo 内)还是向量库/数据库?给出你的判断框架。**
参考思路:
- repo 文件:人可读可审、版本化、agent 天然会 grep、无基础设施成本——默认选择;
- 向量库:海量非结构化知识(千份文档)、语义检索为主——按需引入;
- 数据库:多用户并发写、跨服务共享、需要权限模型;
- 反模式:把"决策与规则"放进向量库(检索是概率性的,规则要确定性命中);把海量的代码切片常驻 prompt(见 ch02)。
- 加分:书的原则——记忆的第一属性是**可信状态**,第二才是可检索性。

**Q6.14【品味·L4·管理】团队的记忆资产(specs/rules/skills)该由谁维护?怎么防止它腐烂?**
参考思路:所有权跟代码走(PR 评审变更);腐烂防护——规则带触发条件与复审期;doc gardening 进 backlog;把"规则失效引发的故障"回写为规则更新;度量记忆健康度(引用率/过期率)。反对"AI 自动生成无人审核的规则堆积"——那是 semantic drift 的温床。

**Q6.15【品味·L3·用户】什么时候用 --resume 续会话,什么时候新会话+handoff?**
参考思路:短中断(几分钟,上下文仍有高信噪比)→resume;跨天/跨任务/上次会话已污染(大量失败尝试)→新会话+读 handoff/进度文件;长任务应该**主动**在里程碑写 handoff,而不是依赖会话可恢复——会话是易失的,repo 是持久的。

---

## 6.4 开放性问题

**Q6.16【开放·L5·模型】"记忆即上下文管理的对偶":如果模型原生支持 memory tool 成为主流,CLAUDE.md 这类人写记忆的角色会膨胀还是萎缩?**
讨论框架:人写记忆的价值在权威性与意图(不可被模型覆盖的宪法);模型自写记忆的价值在过程连续性;两者分层共存 vs 互相侵蚀;治理问题(谁审核模型写的记忆)。

**Q6.17【开放·L4·架构】为开源项目设计"贡献者的记忆入口":新 contributor 的 agent 第一次进仓库该看到什么?**
讨论框架:AGENTS.md 的最小充分集;分层加载(根→目录);quickstart 命令;禁区;指向深层文档的索引;冷启动评估(新 agent 完成首个 hello task 的成功率)。

---

## 6.5 工程实战题

**Q6.18【实战·L3·平台】为你们的 monorepo(前端/后端/共享包)设计记忆文件布局。**
期望框架:根 AGENTS.md(全局命令/约定/索引)→ 各包 AGENTS.md 或 .claude/rules(paths 作用域);高频深目录(sub-dir)lazy CLAUDE.md;CI 侧校验(记忆中命令的有效性测试);/init 引导新包;入口总量控制(根 <200 行)。

**Q6.19【实战·L4·架构】设计一个"跨夜长任务"的记忆方案:任务 8 小时,会话每 ~1 小时会被上下文上限打断。**
期望框架:initializer 会话建立:进度文件(JSON)+计划+验收标准;每会话开机序列(读 git log→进度→选任务→跑端到端测试);里程碑 git commit 即 checkpoint;handoff 在每次会话结束前更新(阻塞点/下一步);早晨的人工检查点;预算与熔断。参照 Anthropic 轮班工程师方案。

**Q6.20【实战·L3·平台】实现"记忆gardening"自动化:一个每周运行的 job,清理过期规则、标记冲突、汇报记忆健康度。**
期望框架:规则元数据(创建时间/触发条件/最后命中时间);job 逻辑——超期未命中→候选删除(输出 PR 供人审);与代码冲突检测(规则提到的路径/命令是否存在);新增建议(从近期会话高频纠正中提取,人工确认后入库);报告指标(总数/命中率/过期率)。

---

## 6.6 场景排错题

**Q6.21【排错·L2·用户】agent 每次新会话都重新犯上周已经纠正过的错误,尽管你把纠正写进了 CLAUDE.md。排查?**
排查链路:①规则是否真的被加载(层级错误/超过行数被截断/lazy load 没触发);②措辞是否可执行("保持整洁" vs "禁止向 src/legacy 写新代码");③与仓库现状冲突(旧代码大量反例,权重压过规则——需要仓库治理);④规则被更长文档稀释;⑤测试:让 agent 复述规则验证注入。

**Q6.22【排错·L3·平台】跨会话 resume 后 agent 行为异常:重复已完成的步骤、丢失早先约束。根因?**
排查链路:resume 的会话可能已历经多次 compaction,早期约束只存在于摘要;auto memory 未启用或没写关键结论;修复:长任务不依赖 resume,改"每会话从 repo 状态重建上下文"(开机序列);把约束落在任务文件/CLAUDE.md(持久层)而非对话(易失层)。

**Q6.23【排错·L4·架构】团队 wiki、AGENTS.md、代码注释三处对同一约定说法不一,agent 随机采用其一。治理?**
排查与治理:定义 source of truth 顺序并写进根记忆(如:代码>AGENTS.md>wiki);冲突清单一次性清理(以代码为准回写);wiki 中该约定页加"以 repo 为准"指针;gardening job 定期做一致性 diff;新约定只允许写进一处(SoT),其他地方引用。
